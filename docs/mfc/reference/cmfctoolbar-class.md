---
title: CMFC툴바 클래스
ms.date: 11/04/2016
f1_keywords:
- CMFCToolBar
- AFXTOOLBAR/CMFCToolBar
- AFXTOOLBAR/CMFCToolBar::AddBasicCommand
- AFXTOOLBAR/CMFCToolBar::AddCommandUsage
- AFXTOOLBAR/CMFCToolBar::AddToolBarForImageCollection
- AFXTOOLBAR/CMFCToolBar::AdjustLayout
- AFXTOOLBAR/CMFCToolBar::AdjustSize
- AFXTOOLBAR/CMFCToolBar::AllowChangeTextLabels
- AFXTOOLBAR/CMFCToolBar::AreTextLabels
- AFXTOOLBAR/CMFCToolBar::AutoGrayInactiveImages
- AFXTOOLBAR/CMFCToolBar::ButtonToIndex
- AFXTOOLBAR/CMFCToolBar::CalcFixedLayout
- AFXTOOLBAR/CMFCToolBar::CalcSize
- AFXTOOLBAR/CMFCToolBar::CanHandleSiblings
- AFXTOOLBAR/CMFCToolBar::CleanUpImages
- AFXTOOLBAR/CMFCToolBar::CleanUpLockedImages
- AFXTOOLBAR/CMFCToolBar::CanBeClosed
- AFXTOOLBAR/CMFCToolBar::CanBeRestored
- AFXTOOLBAR/CMFCToolBar::CanFocus
- AFXTOOLBAR/CMFCToolBar::CommandToIndex
- AFXTOOLBAR/CMFCToolBar::Create
- AFXTOOLBAR/CMFCToolBar::CreateEx
- AFXTOOLBAR/CMFCToolBar::Deactivate
- AFXTOOLBAR/CMFCToolBar::EnableCustomizeButton
- AFXTOOLBAR/CMFCToolBar::EnableDocking
- AFXTOOLBAR/CMFCToolBar::EnableLargeIcons
- AFXTOOLBAR/CMFCToolBar::EnableQuickCustomization
- AFXTOOLBAR/CMFCToolBar::EnableReflections
- AFXTOOLBAR/CMFCToolBar::EnableTextLabels
- AFXTOOLBAR/CMFCToolBar::FromHandlePermanent
- AFXTOOLBAR/CMFCToolBar::GetAllButtons
- AFXTOOLBAR/CMFCToolBar::GetAllToolbars
- AFXTOOLBAR/CMFCToolBar::GetBasicCommands
- AFXTOOLBAR/CMFCToolBar::GetButton
- AFXTOOLBAR/CMFCToolBar::GetButtonInfo
- AFXTOOLBAR/CMFCToolBar::GetButtonSize
- AFXTOOLBAR/CMFCToolBar::GetButtonStyle
- AFXTOOLBAR/CMFCToolBar::GetButtonText
- AFXTOOLBAR/CMFCToolBar::GetColdImages
- AFXTOOLBAR/CMFCToolBar::GetColumnWidth
- AFXTOOLBAR/CMFCToolBar::GetCommandButtons
- AFXTOOLBAR/CMFCToolBar::GetCount
- AFXTOOLBAR/CMFCToolBar::GetCustomizeButton
- AFXTOOLBAR/CMFCToolBar::GetDefaultImage
- AFXTOOLBAR/CMFCToolBar::GetDisabledImages
- AFXTOOLBAR/CMFCToolBar::GetDisabledMenuImages
- AFXTOOLBAR/CMFCToolBar::GetDroppedDownMenu
- AFXTOOLBAR/CMFCToolBar::GetGrayDisabledButtons
- AFXTOOLBAR/CMFCToolBar::GetHighlightedButton
- AFXTOOLBAR/CMFCToolBar::GetHotBorder
- AFXTOOLBAR/CMFCToolBar::GetHotTextColor
- AFXTOOLBAR/CMFCToolBar::GetHwndLastFocus
- AFXTOOLBAR/CMFCToolBar::GetOVERWRITESetText
- AFXTOOLBAR/CMFCToolBar::GetImageSize
- AFXTOOLBAR/CMFCToolBar::GetImages
- AFXTOOLBAR/CMFCToolBar::GetImagesOffset
- AFXTOOLBAR/CMFCToolBar::GetInvalidateItemRect
- AFXTOOLBAR/CMFCToolBar::GetItemID
- AFXTOOLBAR/CMFCToolBar::GetItemRect
- AFXTOOLBAR/CMFCToolBar::GetLargeColdImages
- AFXTOOLBAR/CMFCToolBar::GetLargeDisabledImages
- AFXTOOLBAR/CMFCToolBar::GetLargeImages
- AFXTOOLBAR/CMFCToolBar::GetLockedColdImages
- AFXTOOLBAR/CMFCToolBar::GetLockedDisabledImages
- AFXTOOLBAR/CMFCToolBar::GetLockedImages
- AFXTOOLBAR/CMFCToolBar::GetLockedImageSize
- AFXTOOLBAR/CMFCToolBar::GetLockedMenuImages
- AFXTOOLBAR/CMFCToolBar::GetMenuButtonSize
- AFXTOOLBAR/CMFCToolBar::GetMenuImageSize
- AFXTOOLBAR/CMFCToolBar::GetMenuImages
- AFXTOOLBAR/CMFCToolBar::GetOrigButtons
- AFXTOOLBAR/CMFCToolBar::GetOrigResetButtons
- AFXTOOLBAR/CMFCToolBar::GetResourceID
- AFXTOOLBAR/CMFCToolBar::GetRouteCommandsViaFrame
- AFXTOOLBAR/CMFCToolBar::GetRowHeight
- AFXTOOLBAR/CMFCToolBar::GetShowTooltips
- AFXTOOLBAR/CMFCToolBar::GetSiblingToolBar
- AFXTOOLBAR/CMFCToolBar::GetUserImages
- AFXTOOLBAR/CMFCToolBar::HitTest
- AFXTOOLBAR/CMFCToolBar::InsertButton
- AFXTOOLBAR/CMFCToolBar::InsertSeparator
- AFXTOOLBAR/CMFCToolBar::InvalidateButton
- AFXTOOLBAR/CMFCToolBar::IsAddRemoveQuickCustomize
- AFXTOOLBAR/CMFCToolBar::IsAltCustomizeMode
- AFXTOOLBAR/CMFCToolBar::IsAutoGrayInactiveImages
- AFXTOOLBAR/CMFCToolBar::IsBasicCommand
- AFXTOOLBAR/CMFCToolBar::IsButtonExtraSizeAvailable
- AFXTOOLBAR/CMFCToolBar::IsButtonHighlighted
- AFXTOOLBAR/CMFCToolBar::IsCommandPermitted
- AFXTOOLBAR/CMFCToolBar::IsCommandRarelyUsed
- AFXTOOLBAR/CMFCToolBar::IsCustomizeMode
- AFXTOOLBAR/CMFCToolBar::IsDragButton
- AFXTOOLBAR/CMFCToolBar::IsExistCustomizeButton
- AFXTOOLBAR/CMFCToolBar::IsFloating
- AFXTOOLBAR/CMFCToolBar::IsLargeIcons
- AFXTOOLBAR/CMFCToolBar::IsLastCommandFromButton
- AFXTOOLBAR/CMFCToolBar::IsLocked
- AFXTOOLBAR/CMFCToolBar::IsOneRowWithSibling
- AFXTOOLBAR/CMFCToolBar::IsUserDefined
- AFXTOOLBAR/CMFCToolBar::LoadBitmap
- AFXTOOLBAR/CMFCToolBar::LoadBitmapEx
- AFXTOOLBAR/CMFCToolBar::LoadParameters
- AFXTOOLBAR/CMFCToolBar::LoadState
- AFXTOOLBAR/CMFCToolBar::LoadToolBar
- AFXTOOLBAR/CMFCToolBar::LoadToolBarEx
- AFXTOOLBAR/CMFCToolBar::OnChangeHot
- AFXTOOLBAR/CMFCToolBar::OnFillBackground
- AFXTOOLBAR/CMFCToolBar::OnReset
- AFXTOOLBAR/CMFCToolBar::OnSetAccData
- AFXTOOLBAR/CMFCToolBar::OnSetDefaultButtonText
- AFXTOOLBAR/CMFCToolBar::RemoveAllButtons
- AFXTOOLBAR/CMFCToolBar::RemoveButton
- AFXTOOLBAR/CMFCToolBar::RemoveStateFromRegistry
- AFXTOOLBAR/CMFCToolBar::ReplaceButton
- AFXTOOLBAR/CMFCToolBar::ResetAll
- AFXTOOLBAR/CMFCToolBar::ResetAllImages
- AFXTOOLBAR/CMFCToolBar::RestoreOriginalState
- AFXTOOLBAR/CMFCToolBar::SaveState
- AFXTOOLBAR/CMFCToolBar::SetBasicCommands
- AFXTOOLBAR/CMFCToolBar::SetButtonInfo
- AFXTOOLBAR/CMFCToolBar::SetButtonStyle
- AFXTOOLBAR/CMFCToolBar::SetButtonText
- AFXTOOLBAR/CMFCToolBar::SetButtons
- AFXTOOLBAR/CMFCToolBar::SetCommandUsageOptions
- AFXTOOLBAR/CMFCToolBar::SetCustomizeMode
- AFXTOOLBAR/CMFCToolBar::SetGrayDisabledButtons
- AFXTOOLBAR/CMFCToolBar::SetHeight
- AFXTOOLBAR/CMFCToolBar::SetHotBorder
- AFXTOOLBAR/CMFCToolBar::SetHotTextColor
- AFXTOOLBAR/CMFCToolBar::SetLargeIcons
- AFXTOOLBAR/CMFCToolBar::SetLockedSizes
- AFXTOOLBAR/CMFCToolBar::SetMenuSizes
- AFXTOOLBAR/CMFCToolBar::SetNonPermittedCommands
- AFXTOOLBAR/CMFCToolBar::SetOneRowWithSibling
- AFXTOOLBAR/CMFCToolBar::SetPermament
- AFXTOOLBAR/CMFCToolBar::SetRouteCommandsViaFrame
- AFXTOOLBAR/CMFCToolBar::SetShowTooltips
- AFXTOOLBAR/CMFCToolBar::SetSiblingToolBar
- AFXTOOLBAR/CMFCToolBar::SetSizes
- AFXTOOLBAR/CMFCToolBar::SetToolBarBtnText
- AFXTOOLBAR/CMFCToolBar::SetTwoRowsWithSibling
- AFXTOOLBAR/CMFCToolBar::SetUserImages
- AFXTOOLBAR/CMFCToolBar::StretchPane
- AFXTOOLBAR/CMFCToolBar::TranslateChar
- AFXTOOLBAR/CMFCToolBar::UpdateButton
- AFXTOOLBAR/CMFCToolBar::WrapToolBar
- AFXTOOLBAR/CMFCToolBar::AllowShowOnList
- AFXTOOLBAR/CMFCToolBar::CalcMaxButtonHeight
- AFXTOOLBAR/CMFCToolBar::DoPaint
- AFXTOOLBAR/CMFCToolBar::DrawButton
- AFXTOOLBAR/CMFCToolBar::DrawSeparator
- AFXTOOLBAR/CMFCToolBar::OnUserToolTip
- AFXTOOLBAR/CMFCToolBar::m_bDontScaleImages
- AFXTOOLBAR/CMFCToolBar::m_dblLargeImageRatio
helpviewer_keywords:
- CMFCToolBar [MFC], AddBasicCommand
- CMFCToolBar [MFC], AddCommandUsage
- CMFCToolBar [MFC], AddToolBarForImageCollection
- CMFCToolBar [MFC], AdjustLayout
- CMFCToolBar [MFC], AdjustSize
- CMFCToolBar [MFC], AllowChangeTextLabels
- CMFCToolBar [MFC], AreTextLabels
- CMFCToolBar [MFC], AutoGrayInactiveImages
- CMFCToolBar [MFC], ButtonToIndex
- CMFCToolBar [MFC], CalcFixedLayout
- CMFCToolBar [MFC], CalcSize
- CMFCToolBar [MFC], CanHandleSiblings
- CMFCToolBar [MFC], CleanUpImages
- CMFCToolBar [MFC], CleanUpLockedImages
- CMFCToolBar [MFC], CanBeClosed
- CMFCToolBar [MFC], CanBeRestored
- CMFCToolBar [MFC], CanFocus
- CMFCToolBar [MFC], CanHandleSiblings
- CMFCToolBar [MFC], CommandToIndex
- CMFCToolBar [MFC], Create
- CMFCToolBar [MFC], CreateEx
- CMFCToolBar [MFC], Deactivate
- CMFCToolBar [MFC], EnableCustomizeButton
- CMFCToolBar [MFC], EnableDocking
- CMFCToolBar [MFC], EnableLargeIcons
- CMFCToolBar [MFC], EnableQuickCustomization
- CMFCToolBar [MFC], EnableReflections
- CMFCToolBar [MFC], EnableTextLabels
- CMFCToolBar [MFC], FromHandlePermanent
- CMFCToolBar [MFC], GetAllButtons
- CMFCToolBar [MFC], GetAllToolbars
- CMFCToolBar [MFC], GetBasicCommands
- CMFCToolBar [MFC], GetButton
- CMFCToolBar [MFC], GetButtonInfo
- CMFCToolBar [MFC], GetButtonSize
- CMFCToolBar [MFC], GetButtonStyle
- CMFCToolBar [MFC], GetButtonText
- CMFCToolBar [MFC], GetColdImages
- CMFCToolBar [MFC], GetColumnWidth
- CMFCToolBar [MFC], GetCommandButtons
- CMFCToolBar [MFC], GetCount
- CMFCToolBar [MFC], GetCustomizeButton
- CMFCToolBar [MFC], GetDefaultImage
- CMFCToolBar [MFC], GetDisabledImages
- CMFCToolBar [MFC], GetDisabledMenuImages
- CMFCToolBar [MFC], GetDroppedDownMenu
- CMFCToolBar [MFC], GetGrayDisabledButtons
- CMFCToolBar [MFC], GetHighlightedButton
- CMFCToolBar [MFC], GetHotBorder
- CMFCToolBar [MFC], GetHotTextColor
- CMFCToolBar [MFC], GetHwndLastFocus
- CMFCToolBar [MFC], GetIgnoreSetText
- CMFCToolBar [MFC], GetImageSize
- CMFCToolBar [MFC], GetImages
- CMFCToolBar [MFC], GetImagesOffset
- CMFCToolBar [MFC], GetInvalidateItemRect
- CMFCToolBar [MFC], GetItemID
- CMFCToolBar [MFC], GetItemRect
- CMFCToolBar [MFC], GetLargeColdImages
- CMFCToolBar [MFC], GetLargeDisabledImages
- CMFCToolBar [MFC], GetLargeImages
- CMFCToolBar [MFC], GetLockedColdImages
- CMFCToolBar [MFC], GetLockedDisabledImages
- CMFCToolBar [MFC], GetLockedImages
- CMFCToolBar [MFC], GetLockedImageSize
- CMFCToolBar [MFC], GetLockedMenuImages
- CMFCToolBar [MFC], GetMenuButtonSize
- CMFCToolBar [MFC], GetMenuImageSize
- CMFCToolBar [MFC], GetMenuImages
- CMFCToolBar [MFC], GetOrigButtons
- CMFCToolBar [MFC], GetOrigResetButtons
- CMFCToolBar [MFC], GetResourceID
- CMFCToolBar [MFC], GetRouteCommandsViaFrame
- CMFCToolBar [MFC], GetRowHeight
- CMFCToolBar [MFC], GetShowTooltips
- CMFCToolBar [MFC], GetSiblingToolBar
- CMFCToolBar [MFC], GetUserImages
- CMFCToolBar [MFC], HitTest
- CMFCToolBar [MFC], InsertButton
- CMFCToolBar [MFC], InsertSeparator
- CMFCToolBar [MFC], InvalidateButton
- CMFCToolBar [MFC], IsAddRemoveQuickCustomize
- CMFCToolBar [MFC], IsAltCustomizeMode
- CMFCToolBar [MFC], IsAutoGrayInactiveImages
- CMFCToolBar [MFC], IsBasicCommand
- CMFCToolBar [MFC], IsButtonExtraSizeAvailable
- CMFCToolBar [MFC], IsButtonHighlighted
- CMFCToolBar [MFC], IsCommandPermitted
- CMFCToolBar [MFC], IsCommandRarelyUsed
- CMFCToolBar [MFC], IsCustomizeMode
- CMFCToolBar [MFC], IsDragButton
- CMFCToolBar [MFC], IsExistCustomizeButton
- CMFCToolBar [MFC], IsFloating
- CMFCToolBar [MFC], IsLargeIcons
- CMFCToolBar [MFC], IsLastCommandFromButton
- CMFCToolBar [MFC], IsLocked
- CMFCToolBar [MFC], IsOneRowWithSibling
- CMFCToolBar [MFC], IsUserDefined
- CMFCToolBar [MFC], LoadBitmap
- CMFCToolBar [MFC], LoadBitmapEx
- CMFCToolBar [MFC], LoadParameters
- CMFCToolBar [MFC], LoadState
- CMFCToolBar [MFC], LoadToolBar
- CMFCToolBar [MFC], LoadToolBarEx
- CMFCToolBar [MFC], OnChangeHot
- CMFCToolBar [MFC], OnFillBackground
- CMFCToolBar [MFC], OnReset
- CMFCToolBar [MFC], OnSetAccData
- CMFCToolBar [MFC], OnSetDefaultButtonText
- CMFCToolBar [MFC], RemoveAllButtons
- CMFCToolBar [MFC], RemoveButton
- CMFCToolBar [MFC], RemoveStateFromRegistry
- CMFCToolBar [MFC], ReplaceButton
- CMFCToolBar [MFC], ResetAll
- CMFCToolBar [MFC], ResetAllImages
- CMFCToolBar [MFC], RestoreOriginalState
- CMFCToolBar [MFC], SaveState
- CMFCToolBar [MFC], SetBasicCommands
- CMFCToolBar [MFC], SetButtonInfo
- CMFCToolBar [MFC], SetButtonStyle
- CMFCToolBar [MFC], SetButtonText
- CMFCToolBar [MFC], SetButtons
- CMFCToolBar [MFC], SetCommandUsageOptions
- CMFCToolBar [MFC], SetCustomizeMode
- CMFCToolBar [MFC], SetGrayDisabledButtons
- CMFCToolBar [MFC], SetHeight
- CMFCToolBar [MFC], SetHotBorder
- CMFCToolBar [MFC], SetHotTextColor
- CMFCToolBar [MFC], SetLargeIcons
- CMFCToolBar [MFC], SetLockedSizes
- CMFCToolBar [MFC], SetMenuSizes
- CMFCToolBar [MFC], SetNonPermittedCommands
- CMFCToolBar [MFC], SetOneRowWithSibling
- CMFCToolBar [MFC], SetPermament
- CMFCToolBar [MFC], SetRouteCommandsViaFrame
- CMFCToolBar [MFC], SetShowTooltips
- CMFCToolBar [MFC], SetSiblingToolBar
- CMFCToolBar [MFC], SetSizes
- CMFCToolBar [MFC], SetToolBarBtnText
- CMFCToolBar [MFC], SetTwoRowsWithSibling
- CMFCToolBar [MFC], SetUserImages
- CMFCToolBar [MFC], StretchPane
- CMFCToolBar [MFC], TranslateChar
- CMFCToolBar [MFC], UpdateButton
- CMFCToolBar [MFC], WrapToolBar
- CMFCToolBar [MFC], AllowShowOnList
- CMFCToolBar [MFC], CalcMaxButtonHeight
- CMFCToolBar [MFC], DoPaint
- CMFCToolBar [MFC], DrawButton
- CMFCToolBar [MFC], DrawSeparator
- CMFCToolBar [MFC], OnUserToolTip
- CMFCToolBar [MFC], m_bDontScaleImages
- CMFCToolBar [MFC], m_dblLargeImageRatio
ms.assetid: e7679c01-fb94-44c0-98c6-3af955292fb5
ms.openlocfilehash: 9a90327bde693b87a53838da35adde21ed48dfbb
ms.sourcegitcommit: 7a6116e48c3c11b97371b8ae4ecc23adce1f092d
ms.translationtype: MT
ms.contentlocale: ko-KR
ms.lasthandoff: 04/22/2020
ms.locfileid: "81754037"
---
# <a name="cmfctoolbar-class"></a>CMFC툴바 클래스

클래스는 `CMFCToolBar` [CToolBar 클래스와](../../mfc/reference/ctoolbar-class.md)유사하지만 사용자 인터페이스 기능에 대한 추가 지원을 제공합니다. 여기에는 평면 도구 모음, 핫 이미지 포함 도구 모음, 큰 아이콘, 페이저 단추, 잠긴 도구 모음, rebar 컨트롤, 이미지 아래 텍스트, 배경 이미지, 탭 도구 모음이 포함됩니다. `CMFCToolBar` 클래스에는 기본 제공 사용자 지정 도구 모음과 메뉴, 메뉴 사이 끌어 놓기, 콤보 상자 단추, 편집 상자 단추, 색 선택 및 롤업 단추 도구 모음이 포함됩니다.

자세한 내용은 Visual Studio 설치의 **\\VC\\atlmfc\\src mfc** 폴더에 있는 소스 코드를 참조하십시오.

## <a name="syntax"></a>구문

```
class CMFCToolBar : public CMFCBaseToolBar
```

## <a name="members"></a>멤버

### <a name="public-constructors"></a>Public 생성자

|속성|Description|
|----------|-----------------|
|`CMFCToolBar::CMFCToolBar`|기본 생성자입니다.|
|`CMFCToolBar::~CMFCToolBar`|소멸자|

### <a name="public-methods"></a>Public 메서드

|속성|Description|
|----------|-----------------|
|[CMFC툴바::추가 베이직 명령](#addbasiccommand)|사용자가 메뉴를 열 때 항상 표시되는 명령 목록에 메뉴 명령을 추가합니다.|
|[CMFC툴바::추가 명령사용법](#addcommandusage)|지정된 명령과 연결된 카운터를 하나씩 증분합니다.|
|[CMFC툴바::추가도구바포이미지컬렉션](#addtoolbarforimagecollection)|사용자 인터페이스 리소스의 이미지를 응용 프로그램의 이미지 컬렉션에 추가합니다.|
|[CMFC툴바::레이아웃 조정](#adjustlayout)|도구 모음의 크기와 위치를 다시 계산합니다. [(CBasePane 재정의::adjustLayout).](../../mfc/reference/cbasepane-class.md#adjustlayout)|
|[CMFC툴바::조정크기](#adjustsize)|도구 모음의 크기를 다시 계산합니다.|
|[CMFCToolBar::허용변경텍스트레이블](#allowchangetextlabels)|도구 모음 단추의 이미지 아래에 텍스트 레이블을 표시할 수 있는지 여부를 지정합니다.|
|[CMFC툴바::아어텍스트라벨](#aretextlabels)|이미지 아래의 텍스트 레이블이 현재 도구 모음 단추에 표시되는지 여부를 지정합니다.|
|[CMFC툴바:::오토그레이인액티브이미지](#autograyinactiveimages)|비활성 버튼 이미지의 자동 생성을 사용하거나 사용하지 않도록 설정합니다.|
|[CMFC툴바::버튼토인덱스](#buttontoindex)|이 도구 모음에서 지정된 [CMFCToolBarButton 클래스](../../mfc/reference/cmfctoolbarbutton-class.md) 개체의 인덱스를 반환합니다.|
|[CMFCToolBar:::석회화레이아웃](#calcfixedlayout)|도구 모음의 수평 크기를 계산합니다. [(재정의 CBasePane::석회화 레이아웃.)](../../mfc/reference/cbasepane-class.md#calcfixedlayout)|
|[CMFC툴바::석회화](#calcsize)|레이아웃 계산 프로세스의 일부로 프레임워크에서 호출됩니다. [(재정의 CPane::CalcSize.)](../../mfc/reference/cpane-class.md#calcsize)|
|[CMFCToolBar:::할 수 있는 자매](#canhandlesiblings)|도구 모음 및 해당 형제를 동일한 창에 배치할지를 결정합니다.|
|[CMFC툴바::정리 이미지](#cleanupimages)|도구 모음 이미지에 할당된 시스템 리소스를 해제합니다.|
|[CMFC툴바::클린업잠긴 이미지](#cleanuplockedimages)|잠긴 도구 모음 이미지에 할당된 시스템 리소스를 해제합니다.|
|[CMFC툴바:::수있습니다.](#canbeclosed)|사용자가 도구 모음을 닫을 수 있는지 여부를 지정합니다. [(재정의 CBasePane::수 닫힌.)](../../mfc/reference/cbasepane-class.md#canbeclosed)|
|[CMFC툴바:::캔복원](#canberestored)|사용자 지정 후 시스템에서 도구 모음을 원래 상태로 복원할 수 있는지 여부를 결정합니다.|
|[CMFC툴바::캔포커스](#canfocus)|창이 포커스를 수신할 수 있는지 여부를 지정합니다. [(재정비::캔포커스.)](../../mfc/reference/cbasepane-class.md#canfocus)|
|[CMFCToolBar:::할 수 있는 자매](#canhandlesiblings)|도구 모음 및 해당 형제를 동일한 창에 배치할지를 결정합니다.|
|[CMFC툴바::커맨드토인덱스](#commandtoindex)|지정된 명령 ID를 사용하여 도구 모음에서 단추의 인덱스를 반환합니다.|
|[CMFC툴바::만들기](#create)|`CMFCToolBar` 개체를 만듭니다.|
|[CMFC툴바::만들기](#createex)|큰 `CMFCToolBar` 아이콘과 같은 추가 스타일 옵션을 사용하는 개체를 만듭니다.|
|[CMFC툴바::D](#deactivate)|도구 모음을 비활성화합니다.|
|[CMFCToolBar::사용자 지정 버튼 사용](#enablecustomizebutton)|도구 모음 끝에 나타나는 단추 추가 또는 제거 단추를 **활성화하거나** 사용하지 않도록 설정합니다.|
|[CMFC툴바::인에이블도킹](#enabledocking)|창을 주 프레임에 도킹할 수 있습니다. [(CBasePane 재정의::사용 도킹.)](../../mfc/reference/cbasepane-class.md#enabledocking)|
|[CMFCToolBar:::인에이블대형아이콘](#enablelargeicons)|도구 모음 단추에서 큰 아이콘을 사용하거나 사용하지 않도록 설정합니다.|
|[CMFCToolBar:::인에이블퀵커렌드](#enablequickcustomization)|사용자가 **Alt** 키를 누르고 단추를 새 위치로 끌 수 있도록 도구 모음의 빠른 사용자 지정을 활성화하거나 사용하지 않도록 설정합니다.|
|[CMFCToolBar::사용 반사](#enablereflections)|명령 반사를 활성화하거나 사용하지 않도록 설정합니다.|
|[CMFCToolBar::텍스트 레이블 사용](#enabletextlabels)|도구 모음 단추 이미지 에서 텍스트 레이블을 사용 하거나 사용하지 않도록 설정 합니다.|
|[CMFC툴바::부터영구](#fromhandlepermanent)|지정된 창 핸들을 `CMFCToolBar` 포함하는 개체에 대한 포인터를 검색합니다.|
|[CMFC툴바::GetAllButtons](#getallbuttons)|도구 모음에서 읽기 전용 단추 목록을 반환합니다.|
|[CMFC툴바::겟올툴바](#getalltoolbars)|응용 프로그램의 모든 도구 모음의 읽기 전용 목록을 반환합니다.|
|[CMFC툴바::GetBasicCommands](#getbasiccommands)|응용 프로그램에 정의된 기본 명령의 읽기 전용 목록을 반환합니다.|
|[CMFC툴바::GetButton](#getbutton)|지정된 도구 모음 `CMFCToolBarButton` 단추 인덱스가 있는 개체에 대한 포인터를 반환합니다.|
|[CMFC툴바::겟버튼정보](#getbuttoninfo)|지정된 인덱스에서 단추의 명령 ID, 스타일 및 이미지 인덱스를 반환합니다.|
|[CMFCToolBar::GetButtonSize](#getbuttonsize)|도구 모음에서 각 단추의 크기를 반환합니다.|
|[CMFC툴바::겟버튼 스타일](#getbuttonstyle)|지정된 인덱스에 있는 도구 모음 단추의 현재 스타일을 반환합니다.|
|[CMFC툴바::GetButton텍스트](#getbuttontext)|지정된 인덱스가 있는 단추의 텍스트 레이블을 반환합니다.|
|[CMFC툴바::겟콜드이미지](#getcoldimages)|응용 프로그램에서 콜드 도구 모음 단추 이미지 컬렉션에 대한 포인터를 반환합니다.|
|[CMFC툴바::게열 폭](#getcolumnwidth)|도구 모음 단추의 너비를 반환합니다.|
|[CMFC툴바::명령 단추 가져옵니다.](#getcommandbuttons)|응용 프로그램의 모든 도구 모음에서 지정된 명령 ID가 있는 단추 목록을 반환합니다.|
|[CMFC툴바::겟카운트](#getcount)|도구 모음에서 단추 및 구분 기호 수를 반환합니다.|
|[CMFCToolBar::Get사용자 지정 버튼](#getcustomizebutton)|도구 모음과 연결된 `CMFCCustomizeButton` 개체에 대한 포인터를 검색합니다.|
|[CMFC툴바::GetDefaultImage](#getdefaultimage)|지정된 명령 ID가 있는 도구 모음 단추에 대한 기본 이미지 인덱스를 반환합니다.|
|[CMFC툴바::Get장애인이미지](#getdisabledimages)|응용 프로그램에서 비활성화된 도구 모음 단추에 사용되는 이미지 컬렉션에 대한 포인터를 반환합니다.|
|[CMFCToolBar:::GetDisabled메뉴이미지](#getdisabledmenuimages)|응용 프로그램의 비활성화된 메뉴 단추에 사용되는 이미지 컬렉션에 대한 포인터를 반환합니다.|
|[CMFC툴바:::GetddownDownMenu](#getdroppeddownmenu)|현재 하위 메뉴를 표시하고 있는 메뉴 단추 개체에 대한 포인터를 검색합니다.|
|[CMFCToolBar::GetGray 비활성화 단추](#getgraydisabledbuttons)|비활성화된 단추의 이미지가 일반 단추 이미지의 흐리게 표시된 버전인지 또는 비활성화된 단추 이미지 컬렉션에서 가져온 것인지 지정합니다.|
|[CMFCToolBar::[=[발아하기]](#gethighlightedbutton)|현재 강조 표시된 도구 모음 단추에 대한 포인터를 반환합니다.|
|[CMFC툴바::겟핫보더](#gethotborder)|도구 모음 단추를 핫 트래킹하는지 여부를 결정합니다.|
|[CMFC툴바::겟핫텍스트컬러](#gethottextcolor)|강조 표시된 도구 모음 단추의 텍스트 색상을 반환합니다.|
|[CMFC툴바:::GetHwndLastFocus](#gethwndlastfocus)|도구 모음이 시작되기 직전에 입력 포커스가 있는 창에 핸들을 반환합니다.|
|[CMFC툴바::GetIgnoreSetText](#getignoresettext)|단추 레이블 설정 호출이 무시되는지 여부를 지정합니다.|
|[CMFC툴바::겟이미지사이즈](#getimagesize)|도구 모음 단추 이미지의 현재 크기를 반환합니다.|
|[CMFC툴바::겟이미지](#getimages)|응용 프로그램에서 기본 단추 이미지 컬렉션에 대한 포인터를 반환합니다.|
|[CMFC툴바::이미지 오프셋](#getimagesoffset)|도구 모음 단추 이미지의 전역 목록에서 이 도구 모음의 도구 모음 단추 이미지를 찾는 데 사용되는 인덱스 오프셋을 반환합니다.|
|[CMFCToolBar:::무효화항목 Rect](#getinvalidateitemrect)|지정된 인덱스의 단추에 대해 다시 그려야 하는 클라이언트 영역을 검색합니다.|
|[CMFC툴바:::겟아이템ID](#getitemid)|지정된 인덱스에서 도구 모음 단추의 명령 ID를 반환합니다.|
|[CMFC툴바::겟아이템렉트](#getitemrect)|지정된 인덱스에서 단추의 경계 사각형을 반환합니다.|
|[CMFC툴바::겟라지콜드이미지](#getlargecoldimages)|응용 프로그램에서 큰 콜드 도구 모음 단추 이미지 컬렉션에 대한 포인터를 반환합니다.|
|[CMFCToolBar::GetLarge장애인 이미지](#getlargedisabledimages)|응용 프로그램에서 큰 비활성화 된 도구 모음 단추 이미지의 컬렉션에 대 한 포인터를 반환 합니다.|
|[CMFC툴바::겟라지이미지](#getlargeimages)|응용 프로그램에서 큰 도구 모음 단추 이미지 컬렉션에 대한 포인터를 반환합니다.|
|[CMFC툴바::겟록콜드이미지](#getlockedcoldimages)|도구 모음에서 잠긴 콜드 이미지 컬렉션에 대한 포인터를 반환합니다.|
|[CMFCToolBar::GetLocked장애인 이미지](#getlockeddisabledimages)|도구 모음에서 잠긴 비활성화된 이미지 컬렉션에 대한 포인터를 반환합니다.|
|[CMFC툴바::겟록이미지](#getlockedimages)|도구 모음에서 잠긴 단추 이미지 컬렉션에 대한 포인터를 반환합니다.|
|[CMFC툴바::겟잠금이미지크기](#getlockedimagesize)|잠긴 도구 모음 이미지의 기본 크기를 반환합니다.|
|[CMFC툴바:::겟잠금메뉴이미지](#getlockedmenuimages)|도구 모음에서 잠긴 도구 모음 메뉴 이미지 컬렉션에 대한 포인터를 반환합니다.|
|[CMFCToolBar:::겟메뉴버튼크기](#getmenubuttonsize)|응용 프로그램에서 메뉴 단추의 크기를 반환합니다.|
|[CMFC툴바::겟메뉴이미지크기](#getmenuimagesize)|응용 프로그램에서 메뉴 단추 이미지의 크기를 반환합니다.|
|[CMFC툴바::겟메뉴이미지](#getmenuimages)|응용 프로그램의 메뉴 단추 이미지 컬렉션에 대한 포인터를 반환합니다.|
|[CMFC툴바::게도리버튼](#getorigbuttons)|도구 모음의 사용자 지정되지 않은 단추 컬렉션을 검색합니다.|
|[CMFCToolBar::게토리리리셋버튼](#getorigresetbuttons)|도구 모음의 사용자 지정되지 않은 재설정 단추 컬렉션을 검색합니다.|
|[CMFC툴바:::GetResourceID](#getresourceid)|도구 모음의 리소스 ID를 검색합니다.|
|[CMFC툴바::겟루트커맨드비아프레임](#getroutecommandsviaframe)|부모 프레임 또는 소유자인 개체가 도구 모음에 명령을 보내는지 결정합니다.|
|[CMFC툴바::겟로우높이](#getrowheight)|도구 모음 단추의 높이를 반환합니다.|
|[CMFC툴바::겟쇼툴팁](#getshowtooltips)|도구 모음 단추에 대해 도구 설명이 표시되는지 여부를 지정합니다.|
|[CMFC툴바:::더시매툴바](#getsiblingtoolbar)|도구 모음의 형제를 검색합니다.|
|[CMFC툴바::GetUserimages](#getuserimages)|응용 프로그램에서 사용자 정의 도구 모음 단추 이미지 컬렉션에 대한 포인터를 반환합니다.|
|[CMFC툴바::히트테스트](#hittest)|지정된 위치에 있는 도구 모음 단추의 인덱스를 반환합니다.|
|[CMFC툴바::삽입버튼](#insertbutton)|도구 모음에 단추를 삽입합니다.|
|[CMFCToolBar::삽입분리기](#insertseparator)|도구 모음에 구분 기호를 삽입합니다.|
|[CMFCToolBar::무효화 단추](#invalidatebutton)|제공된 인덱스에 있는 도구 모음 단추의 클라이언트 영역을 무효화합니다.|
|[CMFCToolBar::IsAddRemove빠른 사용자 정의](#isaddremovequickcustomize)|**사용자 지정** 메뉴 옵션을 사용하여 사용자가 도구 모음 단추를 추가하거나 제거할 수 있는지 여부를 결정합니다.|
|[CMFC툴바::이Alt사용자 정의 모드](#isaltcustomizemode)|빠른 사용자 *지정이* 단추를 드래그하는 데 사용되는지 여부를 지정합니다.|
|[CMFC툴바::이오토그레이인액티브이미지](#isautograyinactiveimages)|비활성(강조 표시되지 않은) 단추 이미지의 자동 생성이 활성화되어 있는지 여부를 지정합니다.|
|[CMFC툴바::기본 명령](#isbasiccommand)|명령이 기본 명령 목록에 있는지 여부를 결정합니다.|
|[CMFC툴바::이버튼엑스트라사이즈사용 가능](#isbuttonextrasizeavailable)|도구 모음에 테두리가 확장된 단추를 표시할 수 있는지 여부를 결정합니다.|
|[CMFC툴바::버튼 강조 표시](#isbuttonhighlighted)|도구 모음의 단추를 강조 표시할지 여부를 결정합니다.|
|[CMFC툴바::명령 허용](#iscommandpermitted)|명령이 허용되는지 여부를 결정합니다.|
|[CMFCToolBar:::명령이 거의 사용되지 않습니다.](#iscommandrarelyused)|명령이 거의 사용되지 않는지 여부를 [결정합니다(CMFCToolBar:SetCommandUsageOptions](#setcommandusageoptions)참조).|
|[CMFC툴바::사용자 지정 모드](#iscustomizemode)|도구 모음 프레임워크가 사용자 지정 모드에 있는지 여부를 지정합니다.|
|[CMFC툴바::이드래그 버튼](#isdragbutton)|도구 모음 단추를 드래그할지 여부를 결정합니다.|
|[CMFCToolBar::존재가 없는 사용자 지정 단추](#isexistcustomizebutton)|도구 모음에 **사용자 지정** 단추가 포함되어 있는지 여부를 확인합니다.|
|[CMFC툴바::떠다니기](#isfloating)|도구 모음이 부동 인지 여부를 확인 합니다.|
|[CMFC툴바::이라지아이콘](#islargeicons)|응용 프로그램의 도구 모음이 현재 큰 아이콘을 표시할지 여부를 지정합니다.|
|[CMFC툴바::IsLastCommandFrom버튼](#islastcommandfrombutton)|지정된 도구 모음 단추에서 가장 최근에 실행된 명령을 보냈는지 여부를 결정합니다.|
|[CMFC툴바:::잠김](#islocked)|도구 모음이 잠겨 있는지 여부를 결정합니다.|
|[CMFC툴바::이스원로우위드형제](#isonerowwithsibling)|도구 모음 및 해당 형제 도구 모음이 동일한 행에 배치되는지 여부를 결정합니다.|
|[CMFC툴바::이유저 정의](#isuserdefined)|도구 모음이 사용자 정의인지 여부를 지정합니다.|
|[CMFC툴바::로드비트맵](#loadbitmap)|애플리케이션 리소스에서 도구 모음 이미지를 로드합니다.|
|[CMFC툴바::로드비트맵](#loadbitmapex)|애플리케이션 리소스에서 도구 모음 이미지를 로드합니다. 큰 이미지가 포함되어 있습니다.|
|[CMFC툴바::로드매개변수](#loadparameters)|Windows 레지스트리에서 전역 도구 모음 옵션을 로드합니다.|
|[CMFC툴바::로드스테이트](#loadstate)|Windows 레지스트리에서 도구 모음 상태 정보를 로드합니다. [(재정의 Cpane::로드 상태.)](../../mfc/reference/cpane-class.md#loadstate)|
|[CMFC툴바::로드툴바](#loadtoolbar)|응용 프로그램 리소스에서 도구 모음을 로드합니다.|
|[CMFC툴바::로드툴바렉스](#loadtoolbarex)|`CMFCToolBarInfo` 도우미 클래스를 사용하여 응용 프로그램에서 큰 이미지를 사용할 수 있도록 하여 응용 프로그램 리소스에서 도구 모음을 로드합니다.|
|[CMFC툴바::온체인지핫](#onchangehot)|사용자가 도구 모음에서 단추를 선택할 때 프레임워크에서 호출됩니다.|
|[CMFC툴바::온필백](#onfillbackground)|도구 모음 배경을 채우기 위해 [CBasePane::DoPaint의](../../mfc/reference/cbasepane-class.md#dopaint) 프레임워크에서 호출합니다.|
|[CMFC툴바::온리셋](#onreset)|도구 모음을 원래 상태로 복원합니다.|
|[CMFC툴바::온셋데이터](#onsetaccdata)|[(CBasePane 재정의::OnSetAccData.)](../../mfc/reference/cbasepane-class.md#onsetaccdata)|
|[CMFC툴바::기본 설정 단추 텍스트](#onsetdefaultbuttontext)|도구 모음 단추의 텍스트를 기본 상태로 복원합니다.|
|`CMFCToolBar::OnUpdateCmdUI`|내부적으로 사용됩니다.|
|[CMFC툴바::모든 버튼 제거](#removeallbuttons)|도구 모음에서 모든 단추를 제거합니다.|
|[CMFC툴바::제거버튼](#removebutton)|도구 모음에서 지정된 인덱스가 있는 단추를 제거합니다.|
|[CMFC툴바::제거상태로부터레지스트리](#removestatefromregistry)|Windows 레지스트리에서 도구 모음의 상태 정보를 삭제합니다.|
|[CMFC툴바::대체 버튼](#replacebutton)|도구 모음 단추를 다른 도구 모음 단추로 바꿉습니다.|
|[CMFC툴바::모두 재설정](#resetall)|모든 도구 모음을 원래 상태로 복원합니다.|
|[CMFC툴바::리셋올이미지](#resetallimages)|응용 프로그램의 모든 도구 모음 이미지 컬렉션을 지웁습니다.|
|[CMFCToolBar::RestoreOriginalState](#restoreoriginalstate)|도구 모음의 원래 상태를 복원합니다.|
|[CMFC툴바::저장상태](#savestate)|Windows 레지스트리에서 도구 모음에 대한 상태 정보를 저장합니다. [(재정의 CPane::SaveState](../../mfc/reference/cpane-class.md#savestate).)|
|`CMFCToolBar::Serialize`|( `CBasePane::Serialize`을 재정의합니다.)|
|[CMFC툴바::기본 명령](#setbasiccommands)|사용자가 메뉴를 열 때 항상 표시되는 명령 목록을 설정합니다.|
|[CMFC툴바::버튼정보](#setbuttoninfo)|도구 모음 단추의 명령 ID, 스타일 및 이미지 ID를 설정합니다.|
|[CMFC툴바::버튼 스타일](#setbuttonstyle)|지정된 인덱스에서 도구 모음 단추의 스타일을 설정합니다.|
|[CMFC툴바::설정 버튼 텍스트](#setbuttontext)|도구 모음 단추의 텍스트 레이블을 설정합니다.|
|[CMFC툴바::버튼](#setbuttons)|도구 모음의 단추를 설정합니다.|
|[CMFCToolBar::설정 명령 사용 옵션](#setcommandusageoptions)|거의 사용되지 않는 명령이 응용 프로그램의 메뉴에 나타나지 않는 경우를 지정합니다.|
|[CMFC툴바::셋커마이징모드](#setcustomizemode)|응용 프로그램의 모든 도구 모음에 대해 사용자 지정 모드를 활성화하거나 사용하지 않도록 설정합니다.|
|[CMFCToolBar::설정회색 비활성화 단추](#setgraydisabledbuttons)|도구 모음의 비활성화된 단추를 흐리게 하는지 또는 비활성화된 이미지가 비활성화된 단추에 사용되는지 여부를 지정합니다.|
|[CMFC툴바::셋높이](#setheight)|도구 모음의 높이를 설정합니다.|
|[CMFC툴바::셋핫보더](#sethotborder)|도구 모음 단추를 핫 트래킹할지 여부를 지정합니다.|
|[CMFC툴바::세트핫텍스트컬러](#sethottextcolor)|핫 도구 모음 단추의 텍스트 색상을 설정합니다.|
|[CMFCToolBar::세트대형아이콘](#setlargeicons)|도구 모음 단추에 큰 아이콘이 표시되는지 여부를 지정합니다.|
|[CMFC툴바::세트로크사이즈](#setlockedsizes)|도구 모음에서 잠긴 단추 및 잠긴 이미지의 크기를 설정합니다.|
|[CMFCToolBar:::세트메뉴크기](#setmenusizes)|도구 모음 메뉴 단추와 해당 이미지의 크기를 설정합니다.|
|[CMFCToolBar::설정비 허용 명령](#setnonpermittedcommands)|사용자가 실행할 수 없는 명령 목록을 설정합니다.|
|[CMFC툴바::셋원로우위드형제](#setonerowwithsibling)|도구 모음과 해당 형제를 동일한 행에 배치합니다.|
|[CMFC툴바::세퍼마멘트](#setpermament)|사용자가 도구 모음을 닫을 수 있는지 여부를 지정합니다.|
|[CMFC툴바::셋루트커맨드비아프레임](#setroutecommandsviaframe)|상위 프레임 또는 소유자가 도구 모음에 명령을 보낼지 여부를 지정합니다.|
|[CMFC툴바::세트쇼툴팁](#setshowtooltips)|프레임워크에 도구 설명이 표시되는지 여부를 지정합니다.|
|[CMFC툴바::세형제도구바](#setsiblingtoolbar)|도구 모음의 형제를 지정합니다.|
|[CMFC툴바::세트사이즈](#setsizes)|모든 도구 모음에서 단추 및 이미지의 크기를 지정합니다.|
|[CMFC툴바::셋툴바브엔텍스트](#settoolbarbtntext)|도구 모음에 있는 단추의 속성을 지정합니다.|
|[CMFC툴바::셋투로우와 형제](#settworowswithsibling)|도구 모음과 해당 형제를 별도의 행에 배치합니다.|
|[CMFC툴바::셋유저 이미지](#setuserimages)|응용 프로그램에서 사용자 정의 이미지의 컬렉션을 설정합니다.|
|[CMFC툴바::스트레치파인](#stretchpane)|도구 모음을 세로 또는 수평으로 늘입니다. [(재정의 CBasePane::스트레치 파인](../../mfc/reference/cbasepane-class.md#stretchpane).)|
|[CMFC툴바:::번역차](#translatechar)|지정된 키 코드가 유효한 키보드 단축키에 해당하는 경우 단추 명령을 실행합니다.|
|[CMFC툴바::업데이트 버튼](#updatebutton)|지정된 단추의 상태를 업데이트합니다.|
|[CMFC툴바::랩툴바](#wraptoolbar)|지정된 치수 내에서 도구 모음 단추의 위치를 지정합니다.|

### <a name="protected-methods"></a>Protected 메서드

|속성|Description|
|----------|-----------------|
|[CMFC툴바::허용쇼온리스트](#allowshowonlist)|**사용자 지정** 대화 상자의 도구 모음 창에 있는 **도구 모음** 창에 도구 모음이 표시되는지 여부를 결정합니다.|
|[CMFC툴바:::캘맥스버튼높이](#calcmaxbuttonheight)|도구 모음에서 단추의 최대 높이를 계산합니다.|
|[CMFC툴바::D페인트](#dopaint)|도구 모음을 다시 그립니다.|
|[CMFC툴바::D로 버튼](#drawbutton)|도구 모음 단추를 다시 그립니다.|
|[CMFC툴바::D로세파레이터](#drawseparator)|도구 모음에서 구분 기호를 다시 그립니다.|
|[CMFC툴바::온유저툴팁](#onusertooltip)|단추의 도구 설명이 표시될 때 프레임워크에서 호출합니다.|

### <a name="data-members"></a>데이터 멤버

|속성|Description|
|----------|-----------------|
|[CMFC툴바::m_bDontScaleImages](#m_bdontscaleimages)|높은 DPI 모드에서 도구 모음 이미지의 배율을 조정할지 여부를 지정합니다.|
|[CMFC툴바::m_dblLargeImageRatio](#m_dbllargeimageratio)|큰 이미지의 치수(높이 또는 너비)와 일반 이미지의 크기 사이의 비율을 지정합니다.|

## <a name="remarks"></a>설명

개체를 `CMFCToolBar` 응용 프로그램에 통합하려면 다음 단계를 따르십시오.

1. 기본 `CMFCToolBar` 프레임 창에 개체를 추가합니다.

1. 주 프레임 창에 대한 WM_CREATE 메시지를 처리할 때 [CMFCToolBar::만들기](#create) 또는 [CMFCToolBar::CreateEx를](#createex) 호출하여 도구 모음을 만들고 스타일을 지정합니다.

1. [CBasePane::EnableDocking을](../../mfc/reference/cbasepane-class.md#enabledocking) 호출하여 도킹 스타일을 지정합니다.

콤보 상자 또는 드롭다운 도구 모음과 같은 특수 단추를 삽입하려면 상위 리소스에 더미 단추를 예약하고 [CMFCToolBar::ReplaceButton을](#replacebutton)사용하여 런타임에 더미 단추를 교체합니다. 자세한 내용은 [연습: 도구 모음에 컨트롤 넣기](../walkthrough-putting-controls-on-toolbars.md)를 참조하십시오.

`CMFCToolBar`는 MFC 라이브러리 클래스 [CMFCMenuBar 클래스,](../../mfc/reference/cmfcmenubar-class.md) [CMFCPopupMenuBar 클래스](../../mfc/reference/cmfcpopupmenubar-class.md)및 [CMFCDropDownToolBar 클래스의 기본 클래스입니다.](../../mfc/reference/cmfcdropdowntoolbar-class.md)

## <a name="example"></a>예제

다음 예제에서는 `CMFCToolBar` 클래스에서 다양한 메서드를 사용하는 방법을 보여 줍니다. 이 예제에서는 도구 모음의 창 레이블의 텍스트를 설정하고, 테두리를 설정하고, 창 의 스타일을 설정하고, 도구 모음 끝에 나타나는 **단추 추가 또는 제거** 단추를 사용하도록 설정하는 방법을 보여 주며, 이 방법을 보여 주실 수 있습니다. 이 코드 조각은 IE [데모 샘플의](../../overview/visual-cpp-samples.md)일부입니다.

[!code-cpp[NVC_MFC_IEDemo#6](../../mfc/reference/codesnippet/cpp/cmfctoolbar-class_1.h)]
[!code-cpp[NVC_MFC_IEDemo#8](../../mfc/reference/codesnippet/cpp/cmfctoolbar-class_2.cpp)]

## <a name="requirements"></a>요구 사항

**헤더:** afxtoolbar.h

## <a name="inheritance-hierarchy"></a>상속 계층 구조

[CObject](../../mfc/reference/cobject-class.md)

[CCmdTarget](../../mfc/reference/ccmdtarget-class.md)

[CWnd](../../mfc/reference/cwnd-class.md)

[CBasePane](../../mfc/reference/cbasepane-class.md)

[CPane](../../mfc/reference/cpane-class.md)

[CMFC베이스툴바](../../mfc/reference/cmfcbasetoolbar-class.md)

`CMFCToolBar`

## <a name="cmfctoolbaraddbasiccommand"></a><a name="addbasiccommand"></a>CMFC툴바::추가 베이직 명령

사용자가 메뉴를 열 때 항상 표시되는 명령 목록에 메뉴 명령을 추가합니다.

```
static void __stdcall AddBasicCommand(UINT uiCmd);
```

### <a name="parameters"></a>매개 변수

*uiCmd*<br/>
【인】 추가할 명령을 지정합니다.

### <a name="remarks"></a>설명

메뉴를 열 때 기본 명령이 항상 표시됩니다. 이 메서드는 사용자가 최근에 사용한 명령을 보도록 선택할 때 의미가 있습니다.

[CMFCToolBar::SetBasicCommands 메서드를](#setbasiccommands) 사용하여 사용자가 메뉴를 열 때 항상 표시되는 명령 목록을 설정합니다. [CMFCToolBar::GetBasicCommands 메서드를](#getbasiccommands) 사용하여 응용 프로그램에서 사용하는 기본 명령 목록을 검색합니다.

## <a name="cmfctoolbaraddcommandusage"></a><a name="addcommandusage"></a>CMFC툴바::추가 명령사용법

지정된 명령과 연결된 카운터를 하나씩 증분합니다.

```
static void __stdcall AddCommandUsage(UINT uiCommand);
```

### <a name="parameters"></a>매개 변수

*uiCommand*<br/>
【인】 명령 카운터가 증분하도록 지정합니다.

### <a name="remarks"></a>설명

사용자가 메뉴 항목을 선택할 때 프레임워크는 이 메서드를 호출합니다.

프레임워크는 명령 카운터를 사용하여 최근에 사용한 메뉴 항목을 표시합니다.

이 메서드는 [CMFCCmdUsageCount::AddCmd](../../mfc/reference/cmfccmdusagecount-class.md#addcmd) 메서드를 사용 하 여 명령 카운터를 증가 합니다.

## <a name="cmfctoolbaraddtoolbarforimagecollection"></a><a name="addtoolbarforimagecollection"></a>CMFC툴바::추가도구바포이미지컬렉션

사용자 인터페이스 리소스의 이미지를 응용 프로그램의 이미지 컬렉션에 추가합니다.

```
static BOOL __stdcall AddToolBarForImageCollection(
    UINT uiResID,
    UINT uiBmpResID=0,
    UINT uiColdResID=0,
    UINT uiMenuResID=0,
    UINT uiDisabledResID=0,
    UINT uiMenuDisabledResID=0);
```

### <a name="parameters"></a>매개 변수

*uiResID*<br/>
【인】 로드할 이미지가 있는 도구 모음의 리소스 ID입니다.

*uiBmpResID*<br/>
【인】 도구 모음 이미지가 있는 비트맵의 리소스 ID입니다.

*uiColdResID*<br/>
【인】 "콜드" 도구 모음 이미지가 있는 비트맵의 리소스 ID입니다.

*uiMenuResID*<br/>
【인】 메뉴 이미지가 있는 비트맵의 리소스 ID입니다.

*ui장애인ResID*<br/>
【인】 비활성화된 도구 모음 이미지가 있는 비트맵의 리소스 ID입니다.

*uiMenu장애인ResID*<br/>
【인】 비활성화 된 메뉴 이미지와 비트 맵의 리소스 ID입니다.

### <a name="return-value"></a>Return Value

메서드가 성공하면 TRUE입니다. *uiResID* 또는 *uiBmpResID* 가 유효한 리소스를 지정하지 않거나 다른 오류가 발생하는 경우 FALSE입니다.

### <a name="remarks"></a>설명

이 메서드를 호출하여 도구 모음 이미지가 있는 비트맵을 로드하고 도구 모음 이미지 컬렉션에 추가합니다. 이 메서드는 임시 도구 모음 개체를 만들고 [CMFCToolBar::LoadToolBar](#loadtoolbar)를 호출합니다.

## <a name="cmfctoolbaradjustlayout"></a><a name="adjustlayout"></a>CMFC툴바::레이아웃 조정

도구 모음의 크기와 위치를 다시 계산합니다.

```
virtual void AdjustLayout();
```

### <a name="remarks"></a>설명

도구 모음이 만들어진 경우 이 메서드를 호출하여 크기와 위치를 다시 계산합니다.

프레임워크는 도구 모음의 레이아웃을 변경해야 할 때마다 이 메서드를 호출합니다. 예를 들어 사용자가 다른 컨트롤 모음을 이동하거나 응용 프로그램 창크기를 조정하거나 도구 모음을 사용자 지정할 때 레이아웃이 변경되어야 합니다.

이 메서드를 재정의하여 에서 파생된 클래스에서 `CMFCToolbar`고유한 동적 레이아웃을 제공합니다.

## <a name="cmfctoolbaradjustsize"></a><a name="adjustsize"></a>CMFC툴바::조정크기

도구 모음의 크기를 다시 계산합니다.

```cpp
void AdjustSize();
```

### <a name="remarks"></a>설명

이 방법을 사용하면 도구 모음이 상위 프레임의 경계에 맞는지 확인합니다. 이 메서드는 도구 모음에 상위 프레임이 없는 경우 아무 작업을 수행하지 않습니다.

[CMFCToolBar::adjustLayout](#adjustlayout) 메서드는 도구 모음의 부모가 개체가 아닌 경우 크기를 `CMFCReBar` 다시 계산하기 위해 이 메서드를 호출합니다.

## <a name="cmfctoolbarallowchangetextlabels"></a><a name="allowchangetextlabels"></a>CMFCToolBar::허용변경텍스트레이블

도구 모음 단추의 이미지 아래에 텍스트 레이블을 표시할 수 있는지 여부를 지정합니다.

```
virtual BOOL AllowChangeTextLabels() const;
```

### <a name="return-value"></a>Return Value

TRUE 이미지 아래에 텍스트 레이블을 표시할 수 있는 경우 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

이 메서드는 사용자 지정 대화 상자에서 호출하여 선택한 도구 모음에 대한 **도구 모음** 페이지에서 **텍스트 레이블 표시** 확인란을 사용하도록 설정할지 여부를 결정합니다.

기본 구현은 TRUE를 반환합니다.

사용자가 이미지 아래의 도구 모음 `CMFCToolBar` 단추에 텍스트 레이블이 표시되는지 여부를 결정하지 않으려면 파생된 개체에서 이 메서드를 재정의하고 FALSE를 반환합니다.

## <a name="cmfctoolbarallowshowonlist"></a><a name="allowshowonlist"></a>CMFC툴바::허용쇼온리스트

**사용자 지정** 대화 상자의 **도구 모음** 창에 있는 도구 모음 목록에 도구 모음이 표시되는지 여부를 결정합니다.

```
virtual BOOL AllowShowOnList() const;
```

### <a name="return-value"></a>Return Value

TRUE 도구 모음 개체를 도구 모음 사용자 지정 페이지의 목록 상자에 표시할 수 있는 경우 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

이 메서드는 도구 모음 사용자 지정 페이지의 목록에 파생 된 특정 개체를 포함 `CMFCToolBar`해야 하는지 여부를 확인 하기 위해 프레임 워크에서 호출 됩니다.

기본 구현은 항상 TRUE를 반환합니다. 사용자 지정 대화 상자의 도구 모음 목록에 도구 모음이 나타나지 않으려면 이 메서드를 재정의합니다.

## <a name="cmfctoolbararetextlabels"></a><a name="aretextlabels"></a>CMFC툴바::아어텍스트라벨

이미지 아래의 텍스트 레이블이 현재 도구 모음 단추에 표시되는지 여부를 지정합니다.

```
BOOL AreTextLabels() const;
```

### <a name="return-value"></a>Return Value

TRUE 도구 모음 단추에 이미지 아래에 텍스트 레이블이 표시되는 경우 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

[CMFCToolBar:::EnableText레이블를](#enabletextlabels) 사용하여 텍스트가 표시되는지 여부를 지정합니다. 기본값은 FALSE입니다. [CMFCToolBar::AllowChanges를](#allowchangetextlabels) 호출하여 사용자가 사용자 지정 대화 상자에서 이 설정을 변경할 수 있는지 여부를 지정합니다.

## <a name="cmfctoolbarautograyinactiveimages"></a><a name="autograyinactiveimages"></a>CMFC툴바:::오토그레이인액티브이미지

비활성 버튼 이미지의 자동 생성을 사용하거나 사용하지 않도록 설정합니다.

```
static void AutoGrayInactiveImages(
    BOOL bEnable=TRUE,
    int nGrayImagePercentage=0,
    BOOL bRedrawAllToolbars=TRUE);
```

### <a name="parameters"></a>매개 변수

*bEnable*<br/>
【인】 비활성 이미지를 흐리게 할지 여부를 지정하는 부울 값입니다. 이 매개변수가 TRUE이면 비활성 이미지가 흐리게 표시됩니다. 그렇지 않으면 비활성 이미지가 흐리게 표시되지 않습니다.

*n회색이미지백분율*<br/>
【인】 비활성 이미지의 휘도 백분율을 지정합니다. *bEnable가* FALSE이면 이 값은 무시됩니다.

*b리드로우올툴바*<br/>
【인】 응용 프로그램의 모든 도구 모음을 다시 그릴지 여부를 지정하는 부울 값입니다. 이 매개변수가 TRUE이면 이 메서드는 모든 도구 모음을 다시 그립니다.

### <a name="remarks"></a>설명

*bEnable가* TRUE인 경우 프레임워크는 *nGrayImagePercent를* 사용하여 일반 이미지에서 비활성 이미지를 생성합니다. 그렇지 않으면 [CMFCToolBar::GetColdImages](#getcoldimages) 메서드를 사용하여 비활성 이미지 집합을 제공해야 합니다. 이 옵션은 기본적으로 사용되지 않습니다.

*nGrayImagePercent* 매개 변수에 대한 자세한 내용은 [CMFCToolBarImages::회색 이미지](../../mfc/reference/cmfctoolbarimages-class.md#grayimages)를 참조하십시오.

## <a name="cmfctoolbarbuttontoindex"></a><a name="buttontoindex"></a>CMFC툴바::버튼토인덱스

이 도구 모음에서 지정된 [CMFCToolBarButton 클래스](../../mfc/reference/cmfctoolbarbutton-class.md) 개체의 인덱스를 반환합니다.

```
int ButtonToIndex(const CMFCToolBarButton* pButton) const;
```

### <a name="parameters"></a>매개 변수

*p 버튼*<br/>
【인】 도구 모음 단추 개체에 대한 포인터입니다.

### <a name="return-value"></a>Return Value

도구 모음 단추의 내부 목록에서 *pButton의* 인덱스; 또는 -1 지정된 버튼이 이 도구 모음에 없는 경우

## <a name="cmfctoolbarcalcfixedlayout"></a><a name="calcfixedlayout"></a>CMFCToolBar:::석회화레이아웃

도구 모음의 수평 크기를 계산합니다.

```
virtual CSize CalcFixedLayout(
    BOOL bStretch,
    BOOL bHorz);
```

### <a name="parameters"></a>매개 변수

*b스트레치*<br/>
【인】 TRUE는 도구 모음을 상위 프레임 크기로 늘입니다.

*b호르츠 (주)*<br/>
【인】 도구 모음을 가로로 방향을 지정하는 경우 FALSE를 사용하여 도구 모음의 방향을 수직으로 지정합니다.

### <a name="return-value"></a>Return Value

도구 `CSize` 모음의 크기를 지정하는 개체입니다.

### <a name="remarks"></a>설명

이 메서드는 메서드를 사용 하 여 `CMFCToolBar::CalcLayout` 도구 모음의 크기를 계산 합니다. *bStretch가* TRUE인 경우 *dwMode* 매개변수에 대한 LM_STRETCH 플래그를 전달합니다. *bHorz가* TRUE인 경우 LM_HORZ 플래그를 전달합니다.

이 메서드를 사용하는 예제는 VisualStudioDemo 샘플을 참조하십시오.

## <a name="cmfctoolbarcalcmaxbuttonheight"></a><a name="calcmaxbuttonheight"></a>CMFC툴바:::캘맥스버튼높이

도구 모음에서 단추의 최대 높이를 계산합니다.

```
virtual int CalcMaxButtonHeight();
```

### <a name="return-value"></a>Return Value

단추의 최대 높이입니다.

### <a name="remarks"></a>설명

이 방법은 도구 모음의 모든 도구 모음 단추 중최대 높이를 계산합니다. 높이는 현재 도구 모음 도킹 상태와 같은 요인에 따라 달라질 수 있습니다.

사용자 고유의 높이 계산을 `CMFCToolBar` 제공하기 위해 파생된 클래스에서 이 메서드를 재정의합니다.

## <a name="cmfctoolbarcalcsize"></a><a name="calcsize"></a>CMFC툴바::석회화

레이아웃 계산 프로세스의 일부로 프레임워크에서 호출됩니다.

```
virtual CSize CalcSize(BOOL bVertDock);
```

### <a name="parameters"></a>매개 변수

*b버독*<br/>
【인】 TRUE 도구 모음이 수직으로 도킹되도록 지정합니다. FALSE는 도구 모음이 수평으로 도킹되도록 지정합니다.

### <a name="return-value"></a>Return Value

도구 `CSize` 모음의 단추의 전체 크기를 지정하는 개체입니다.

### <a name="remarks"></a>설명

이 메서드는 텍스트 레이블의 영역 및 테두리 크기와 같은 각 단추의 크기에 영향을 주는 특성을 고려합니다.

도구 모음에 단추가 없는 경우 이 메서드는 [CMFCToolBar::GetButtonSize](#getbuttonsize) 메서드를 사용하여 단일 단추의 예약된 크기를 반환합니다.

## <a name="cmfctoolbarcanbeclosed"></a><a name="canbeclosed"></a>CMFC툴바:::수있습니다.

사용자가 도구 모음을 닫을 수 있는지 여부를 지정합니다.

```
virtual BOOL CanBeClosed() const;
```

### <a name="return-value"></a>Return Value

TRUE 도구 모음을 사용자가 닫을 수 있는 경우; 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

프레임워크는 이 메서드를 호출하여 사용자가 도구 모음을 닫을 수 있는지 여부를 결정합니다. 메서드가 TRUE를 반환하는 경우 프레임워크는 도구 모음의 시스템 메뉴에서 SC_CLOSE 명령을 활성화하고 사용자는 사용자 지정 대화 상자의 도구 모음 목록에 있는 확인란을 사용하여 도구 모음을 닫을 수 있습니다.

기본 구현은 TRUE를 반환합니다. 사용자가 닫을 수 없는 도구 `CMFCToolBar` 모음 개체를 만들기 위해 파생된 클래스에서 이 메서드를 재정의합니다.

## <a name="cmfctoolbarcanberestored"></a><a name="canberestored"></a>CMFC툴바:::캔복원

사용자 지정 후 시스템에서 도구 모음을 원래 상태로 복원할 수 있는지 여부를 결정합니다.

```
virtual BOOL CanBeRestored() const;
```

### <a name="return-value"></a>Return Value

TRUE 도구 모음을 응용 프로그램 리소스에서 복원할 수 있는 경우 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

프레임워크는 이 메서드를 호출하여 사용자 지정 후 도구 모음을 원래 상태로 반환할 수 있는지 여부를 결정합니다. 원래 상태는 응용 프로그램 리소스에서 로드됩니다.

TRUE를 반환하는 경우 `CanBeRestored` 사용자 지정 대화 상자의 도구 **모음** 페이지에서 선택한 도구 모음에 대한 **재설정** 단추를 사용할 수 있습니다.

기본 구현은 로드될 때 도구 모음의 원래 리소스 ID가 0이 아닌 경우 TRUE를 반환합니다. 일반적으로 사용자 정의 도구 모음만 복원할 수 없습니다.

메서드를 `CanBeRestored` 재정의하여 파생 클래스에서 이 동작을 사용자 지정할 수 있습니다.

## <a name="cmfctoolbarcanfocus"></a><a name="canfocus"></a>CMFC툴바::캔포커스

창이 포커스를 수신할 수 있는지 여부를 지정합니다.

```
virtual BOOL CanFocus() const;
```

### <a name="return-value"></a>Return Value

이 메서드는 FALSE를 반환합니다.

### <a name="remarks"></a>설명

이 메서드는 도구 모음 개체가 포커스를 받을 수 없으므로 기본 클래스 [구현인 CBasePane::CanFocus를](../../mfc/reference/cbasepane-class.md#canfocus)재정의합니다.

## <a name="cmfctoolbarcanhandlesiblings"></a><a name="canhandlesiblings"></a>CMFCToolBar:::할 수 있는 자매

도구 모음 및 해당 형제를 동일한 창에 배치할지를 결정합니다.

```
BOOL CanHandleSiblings();
```

### <a name="return-value"></a>Return Value

TRUE 도구 모음에 형제가 있고 도구 모음과 해당 형제가 동일한 창에 배치된 경우 true입니다. 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

내부 CMFC사용자 지정 단추::CreatePopupMenu 메서드는 **사용자 지정** 팝업 메뉴를 표시 하는 방법을 결정 하려면이 메서드를 호출 합니다. 이 메서드가 TRUE를 반환하는 경우 프레임워크는 **한 행에 단추 표시** 또는 두 행 **단추에 단추 표시를** 표시합니다.

일반적으로 이 메서드를 사용할 필요가 없습니다. 도구 모음에 나타나는 **사용자 지정** 단추를 사용하려면 [CMFCToolBar::EnableCustomIzeButton](#enablecustomizebutton) 메서드를 호출합니다. **한 행에서 단추 표시를** 사용하거나 **두 행 단추에 단추 표시를** 사용하려면 [CMFCToolBar::SetSiblingToolBar를](#setsiblingtoolbar)호출합니다.

## <a name="cmfctoolbarcleanupimages"></a><a name="cleanupimages"></a>CMFC툴바::정리 이미지

도구 모음 이미지에 할당된 시스템 리소스를 해제합니다.

```
static void CMFCToolBar::CleanUpImages();
```

### <a name="remarks"></a>설명

프레임워크는 응용 프로그램이 종료될 때 이 메서드를 호출합니다.

## <a name="cmfctoolbarcleanuplockedimages"></a><a name="cleanuplockedimages"></a>CMFC툴바::클린업잠긴 이미지

잠긴 도구 모음 이미지에 할당된 시스템 리소스를 해제합니다.

```cpp
void CleanUpLockedImages();
```

### <a name="remarks"></a>설명

응용 프로그램의 시각적 스타일이 변경될 때 이 메서드를 호출합니다. 이 메서드를 사용하는 예제는 VisualStudioDemo 샘플을 참조하십시오.

## <a name="cmfctoolbarcommandtoindex"></a><a name="commandtoindex"></a>CMFC툴바::커맨드토인덱스

지정된 명령 ID를 사용하여 도구 모음에서 단추의 인덱스를 반환합니다.

```
int CommandToIndex(
    UINT nIDFind,
    int iIndexFirst=0) const;
```

### <a name="parameters"></a>매개 변수

*니드찾기*<br/>
【인】 명령 ID를 지정합니다.

*아이인덱스퍼스트*<br/>
【인】 시작할 초기 인덱스를 지정합니다.

### <a name="return-value"></a>Return Value

메서드가 성공한 경우 도구 모음 단추의 0기반 인덱스입니다. -1 지정된 ID가 있는 버튼이 없는 경우.

### <a name="remarks"></a>설명

`CMFCToolBar` 개체는 도구 모음의 단추의 내부 목록을 유지 관리합니다. 이 함수를 호출하여 단추의 명령 ID가 지정된 목록에서 단추의 인덱스를 검색합니다.

*iIndex가* 0보다 큰 경우 이 메서드는 iIndex 보다 인덱스가 있는 도구 모음의 모든 *단추를 무시합니다.*

## <a name="cmfctoolbarcreate"></a><a name="create"></a>CMFC툴바::만들기

`CMFCToolBar` 개체를 만듭니다.

```
virtual BOOL Create(
    CWnd* pParentWnd,
    DWORD dwStyle=AFX_DEFAULT_TOOLBAR_STYLE,
    UINT nID=AFX_IDW_TOOLBAR);
```

### <a name="parameters"></a>매개 변수

*pParentWnd*<br/>
【인】 도구 모음의 상위 창에 대한 포인터입니다.

*dwStyle*<br/>
【인】 도구 모음 스타일입니다. 스타일 목록은 Windows SDK의 [도구 모음 컨트롤 및 단추 스타일을](/windows/win32/Controls/toolbar-control-and-button-styles) 참조하십시오.

*nID*<br/>
【인】 도구 모음의 자식 창의 ID입니다.

### <a name="return-value"></a>Return Value

이 메서드가 성공하면 TRUE입니다. 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

이 메서드는 컨트롤 막대를 만들고 도구 모음에 연결 합니다. TBSTYLE_FLAT 스타일로 컨트롤 막대를 만듭니다. [CMFCToolBar::CreateEx를](#createex) 다른 컨트롤 바 스타일을 원하는 경우 호출합니다.

## <a name="cmfctoolbarcreateex"></a><a name="createex"></a>CMFC툴바::만들기

큰 `CMFCToolBar` 아이콘과 같은 추가 스타일 옵션을 사용하는 개체를 만듭니다.

```
virtual BOOL CreateEx(
    CWnd* pParentWnd,
    DWORD dwCtrlStyle=TBSTYLE_FLAT,
    DWORD dwStyle=AFX_DEFAULT_TOOLBAR_STYLE,
    CRect rcBorders=CRect(1,
    1,
    1,
    1),
    UINT nID=AFX_IDW_TOOLBAR);
```

### <a name="parameters"></a>매개 변수

*pParentWnd*<br/>
【인】 도구 모음의 상위 창에 대한 포인터입니다.

*dwCtrl스타일*<br/>
【인】 포함된 컨트롤 막대 개체를 만들기 위한 추가 스타일입니다.

*dwStyle*<br/>
【인】 도구 모음 스타일입니다. 적절한 스타일 목록은 [도구 모음 컨트롤 및 단추 스타일을](/windows/win32/Controls/toolbar-control-and-button-styles) 참조하십시오.

*rcBorders*<br/>
【인】 도구 `CRect` 모음 창 테두리의 너비를 지정하는 개체입니다.

*nID*<br/>
【인】 도구 모음의 자식 창의 ID입니다.

### <a name="return-value"></a>Return Value

이 메서드가 성공하면 0이 아닙니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

이 메서드는 컨트롤 막대를 만들고 도구 모음에 연결 합니다.

[CMFCToolBar](#create) 대신 이 메서드를 호출합니다. 예를 들어 *dwCtrlStyle을* TBSTYLE_FLAT |으로 설정합니다. TBSTYLE_TRANSPARENT Internet Explorer 4에서 사용하는 도구 모음과 유사한 도구 모음을 만들 수 있습니다.

### <a name="example"></a>예제

다음 예제에서는 `CreateEx` `CMFCToolBar` 클래스의 메서드를 사용 하는 방법을 보여 줍니다. 이 코드 조각은 IE [데모 샘플의](../../overview/visual-cpp-samples.md)일부입니다.

[!code-cpp[NVC_MFC_IEDemo#6](../../mfc/reference/codesnippet/cpp/cmfctoolbar-class_1.h)]
[!code-cpp[NVC_MFC_IEDemo#7](../../mfc/reference/codesnippet/cpp/cmfctoolbar-class_3.cpp)]

## <a name="cmfctoolbardeactivate"></a><a name="deactivate"></a>CMFC툴바::D

도구 모음을 비활성화합니다.

```
virtual void Deactivate();
```

### <a name="remarks"></a>설명

이 메서드는 강조 표시된 도구 모음 단추에서 포커스를 제거하여 도구 모음을 비활성화합니다. 도구 모음에 포커스가 없거나 소멸될 때 프레임워크는 이 메서드를 호출합니다.

## <a name="cmfctoolbardopaint"></a><a name="dopaint"></a>CMFC툴바::D페인트

도구 모음을 다시 그립니다.

```
virtual void DoPaint(CDC* pDC);
```

### <a name="parameters"></a>매개 변수

*pDC*<br/>
【인】 장치 컨텍스트에 대한 포인터입니다.

### <a name="remarks"></a>설명

도구 모음의 일부를 다시 그려야 하는 경우 이 메서드는 프레임워크에서 호출됩니다.

이 메서드를 재정의하여 `CMFCToolBar`에서 파생된 개체의 모양을 사용자 지정합니다.

## <a name="cmfctoolbardrawbutton"></a><a name="drawbutton"></a>CMFC툴바::D로 버튼

도구 모음 단추를 다시 그립니다.

```
virtual BOOL DrawButton(
    CDC* pDC,
    CMFCToolBarButton* pButton,
    CMFCToolBarImages* pImages,
    BOOL bHighlighted,
    BOOL bDrawDisabledImages);
```

### <a name="parameters"></a>매개 변수

*pDC*<br/>
【인】 장치 컨텍스트에 대한 포인터입니다.

*p 버튼*<br/>
【인】 그릴 단추에 대한 포인터입니다.

*p 이미지*<br/>
【인】 도구 모음 이미지에 대한 포인터입니다.

*b 강조 표시*<br/>
【인】 TRUE 단추를 강조 표시 하는 경우; 그렇지 않으면 거짓.

*bDraw장애인 이미지*<br/>
【인】 비활성화된 버튼이 흐리게 표시된 경우 TRUE입니다. 그렇지 않으면 거짓.

### <a name="return-value"></a>Return Value

TRUE 단추를 다시 칠한 경우; 버튼이 숨겨져 있으면 FALSE입니다.

### <a name="remarks"></a>설명

[CMFCToolBar::DrawButton](#drawbutton) 메서드는 도구 모음 단추를 다시 그려야 하는 경우 이 메서드를 호출합니다.

도구 모음에서 단추모양을 사용자 지정하려면 이 메서드를 재정의합니다.

## <a name="cmfctoolbardrawseparator"></a><a name="drawseparator"></a>CMFC툴바::D로세파레이터

도구 모음에서 구분 기호를 다시 그립니다.

```
virtual void DrawSeparator(
    CDC* pDC,
    const CRect& rect,
    BOOL bHorz);
```

### <a name="parameters"></a>매개 변수

*pDC*<br/>
【인】 장치 컨텍스트에 대한 포인터입니다.

*rect*<br/>
【인】 구분 기호가 그려지는 위치의 경계 사각형(픽셀)입니다.

*b호르츠 (주)*<br/>
【인】 구분 기호가 수평인 경우 TRUE, 구분 기호가 수직인 경우 FALSE입니다.

### <a name="remarks"></a>설명

[CMFCToolBar::DoPaint는](#dopaint) 각 [CMFCToolBar::DTBBS_SEPARATOR](#drawseparator) 스타일이 있는 개체에 대해 [CMFCToolBar::DrawButton을](#drawbutton) 호출하는 대신 이 메서드를 호출합니다.

[CMFCToolBar에서](../../mfc/reference/cmfctoolbar-class.md) 파생된 클래스에서 이 메서드를 재정의하여 도구 모음에서 구분 기호의 모양을 사용자 지정합니다. 기본 구현에서는 [CMFCVisualManager::OnDrawSeparator를](../../mfc/reference/cmfcvisualmanager-class.md#ondrawseparator) 호출하여 현재 시각적 관리자에 의해 모양이 결정되는 구분 기호를 그립니다.

## <a name="cmfctoolbarenablecustomizebutton"></a><a name="enablecustomizebutton"></a>CMFCToolBar::사용자 지정 버튼 사용

도구 모음에 나타나는 사용자 지정 단추를 활성화하거나 사용하지 않도록 설정합니다.

```cpp
void EnableCustomizeButton(
    BOOL bEnable,
    int iCustomizeCmd,
    const CString& strCustomizeText,
    BOOL bQuickCustomize=TRUE);

void EnableCustomizeButton(
    BOOL bEnable,
    int iCustomizeCmd,
    UINT uiCustomizeTextResId,
    BOOL bQuickCustomize=TRUE);
```

### <a name="parameters"></a>매개 변수

*bEnable*<br/>
【인】 사용자 지정 단추를 활성화하거나 사용하지 않도록 설정합니다.

*i사용자 정의Cmd*<br/>
【인】 사용자 지정 단추의 명령 ID입니다.

*str사용자 정의 텍스트*<br/>
【인】 사용자 지정 단추의 텍스트 레이블입니다.

*uiizeTextResId*<br/>
【인】 사용자 지정 단추 레이블의 리소스 문자열 ID입니다.

*b빠른 사용자 정의*<br/>
【인】 단추에서 아래로 떨어지는 메뉴에서 단추 추가 또는 제거 옵션을 **활성화하거나** 사용하지 않도록 설정합니다.

### <a name="remarks"></a>설명

*iCustomizeCmd가* -1이면 여러 도구 모음 단추가 도구 모음 영역에 맞지 않을 때 프레임워크에 사용자 지정 버튼이 표시됩니다. 단추에는 더 많은 단추가 있음을 나타내는 이중 왼쪽 을 가리키는 화살표 또는 갈매기가 표시됩니다.

*iCustomizeCmd가* 유효한 명령 ID를 지정하고 *bEnable가* TRUE인 경우 사용자 지정 버튼이 항상 표시됩니다. 단추에는 작은 아래쪽 화살표가 있으며 명령이 포함된 메뉴가 열립니다. 이 명령은 *strCustomizeText*에 의해 지정된 텍스트 레이블을 사용합니다. *bQuickCustomize도* TRUE이면 메뉴에 **버튼 추가 또는 제거** 옵션이 표시됩니다.

프레임워크는 *iCustomizeCmd에*의해 지정된 항목 앞에 도구 모음 영역에 맞지 않는 단추를 메뉴에 동적으로 추가합니다. 갈매기가 아래쪽 화살표 옆에 표시됩니다.

## <a name="cmfctoolbarenabledocking"></a><a name="enabledocking"></a>CMFC툴바::인에이블도킹

창을 주 프레임에 도킹할 수 있습니다.

```
virtual void EnableDocking(DWORD dwAlignment);
```

### <a name="parameters"></a>매개 변수

*dw정렬*<br/>
【인】 활성화할 도킹 정렬을 지정합니다.

### <a name="remarks"></a>설명

이 메서드는 데이터 멤버를 AFX_CBRS_FLOAT 설정 하여 기본 클래스 [구현인 CBasePane::EnableDocking을](../../mfc/reference/cbasepane-class.md#enabledocking) `CBasePane::m_dwControlBarStyle` 확장합니다. 그런 다음 이 메서드는 *dwAlignment을* 기본 클래스 구현에 전달합니다.

## <a name="cmfctoolbarenablelargeicons"></a><a name="enablelargeicons"></a>CMFCToolBar:::인에이블대형아이콘

도구 모음 단추에서 큰 아이콘을 사용하거나 사용하지 않도록 설정합니다.

```cpp
void EnableLargeIcons(BOOL bEnable);
```

### <a name="parameters"></a>매개 변수

*bEnable*<br/>
【인】 TRUE는 큰 아이콘을 활성화하려면 FALSE가 큰 아이콘을 사용하지 않도록 설정합니다.

### <a name="remarks"></a>설명

기본적으로 큰 아이콘이 활성화됩니다.

## <a name="cmfctoolbarenablequickcustomization"></a><a name="enablequickcustomization"></a>CMFCToolBar:::인에이블퀵커렌드

사용자가 **Alt** 키를 누르고 단추를 새 위치로 끌 수 있도록 도구 모음의 빠른 사용자 지정을 활성화하거나 사용하지 않도록 설정합니다.

```
static void EnableQuickCustomization(BOOL bEnable=TRUE);
```

### <a name="parameters"></a>매개 변수

*bEnable*<br/>
【인】 TRUE빠른 사용자 정의를 가능하게, FALSE는 빠른 사용자 정의를 사용하지 않도록 설정합니다.

## <a name="cmfctoolbarenablereflections"></a><a name="enablereflections"></a>CMFCToolBar::사용 반사

명령 반사를 활성화하거나 사용하지 않도록 설정합니다.

```cpp
void EnableReflections(BOOL bEnable = TRUE);
```

### <a name="parameters"></a>매개 변수

*bEnable*<br/>
【인】 TRUE는 명령 반사를 사용하도록 설정합니다. FALSE는 명령 반사를 사용하지 않도록 설정합니다.

### <a name="remarks"></a>설명

콤보 상자와 같은 포함된 컨트롤이 포함된 도구 모음 단추에 대해 명령 반사를 사용하도록 설정하려면 이 메서드를 호출합니다.

명령 반사에 대한 자세한 내용은 [TN062: Windows 컨트롤에 대한 메시지 반사를](../../mfc/tn062-message-reflection-for-windows-controls.md)참조하십시오.

## <a name="cmfctoolbarenabletextlabels"></a><a name="enabletextlabels"></a>CMFCToolBar::텍스트 레이블 사용

도구 모음 단추 이미지 에서 텍스트 레이블을 사용 하거나 사용하지 않도록 설정 합니다.

```cpp
void EnableTextLabels(BOOL bEnable=TRUE);
```

### <a name="parameters"></a>매개 변수

*bEnable*<br/>
TRUE 텍스트 레이블이 도구 모음 단추 이미지 아래에 나타나는 경우 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

텍스트 레이블을 사용하도록 설정하면 도구 모음의 모든 단추가 확대되어 레이블이 이미지 아래에 표시될 수 있는 공간을 제공합니다. 사용자 지정 대화 **상자에도구 모음** 페이지에 **텍스트 레이블 표시** 확인란이 있습니다. 사용자가 도구 모음을 선택하고 이 옵션을 선택하면 프레임워크에서 선택한 도구 모음을 호출합니다. `EnableTextLabels` [CMFCToolBar::AllowChangeText 레이블에서](#allowchangetextlabels) FALSE를 반환하여 [CMFCToolBar에서](../../mfc/reference/cmfctoolbar-class.md) 파생된 개체에 대한 확인란을 비활성화할 수 있습니다.

## <a name="cmfctoolbarfromhandlepermanent"></a><a name="fromhandlepermanent"></a>CMFC툴바::부터영구

지정된 창 핸들을 `CMFCToolBar` 포함하는 개체에 대한 포인터를 검색합니다.

```
static CMFCToolBar* __stdcall FromHandlePermanent(HWND hwnd);
```

### <a name="parameters"></a>매개 변수

*Hwnd*<br/>
【인】 찾을 창 핸들입니다.

### <a name="return-value"></a>Return Value

지정된 창 `CMFCToolBar` 핸들을 포함하는 개체에 대한 포인터 또는 `CMFCToolBar` 해당 개체가 없는 경우 NULL입니다.

### <a name="remarks"></a>설명

이 공유 메서드는 지정된 창 핸들을 `CMFCToolBar` 포함 하는 개체에 대 한 응용 프로그램의 각 도구 모음을 검사 합니다.

## <a name="cmfctoolbargetallbuttons"></a><a name="getallbuttons"></a>CMFC툴바::GetAllButtons

도구 모음에서 읽기 전용 단추 목록을 반환합니다.

```
const CObList& GetAllButtons() const;
```

### <a name="return-value"></a>Return Value

[CMFCToolBarButton](../../mfc/reference/cmfctoolbarbutton-class.md) 클래스 개체의 컬렉션을 포함 하는 [CObList 클래스](../../mfc/reference/coblist-class.md) 개체에 대 한 상수 참조입니다.

## <a name="cmfctoolbargetalltoolbars"></a><a name="getalltoolbars"></a>CMFC툴바::겟올툴바

응용 프로그램의 모든 도구 모음의 읽기 전용 목록을 반환합니다.

```
static const CObList& GetAllToolbars();
```

### <a name="return-value"></a>Return Value

`CMFCToolBar` 개체 컬렉션을 포함하는 [CObList 클래스](../../mfc/reference/coblist-class.md) 개체에 대한 const 참조입니다.

## <a name="cmfctoolbargetbasiccommands"></a><a name="getbasiccommands"></a>CMFC툴바::GetBasicCommands

응용 프로그램에 정의된 기본 명령의 읽기 전용 목록을 반환합니다.

```
static const CList<UINT,UINT>& GetBasicCommands();
```

### <a name="return-value"></a>Return Value

기본 명령 컬렉션을 포함하는 [CList Class](../../mfc/reference/clist-class.md) 개체에 대한 const 참조입니다.

### <a name="remarks"></a>설명

[CMFCToolBar::추가 기본 명령](#addbasiccommand) 또는 [CMFCToolBar::SetBasicCommand를](#setbasiccommands)호출하여 기본 명령을 추가합니다.

## <a name="cmfctoolbargetbutton"></a><a name="getbutton"></a>CMFC툴바::GetButton

지정된 인덱스에서 [CMFCToolBarButton 클래스](../../mfc/reference/cmfctoolbarbutton-class.md) 개체에 대한 포인터를 반환합니다.

```
CMFCToolBarButton* GetButton(int iIndex) const;
```

### <a name="parameters"></a>매개 변수

*iIndex*<br/>
【인】 반환할 단추의 인덱스를 지정합니다.

### <a name="return-value"></a>Return Value

도구 모음 단추에 대한 포인터입니다. 또는 NULL에서 이러한 버튼이 없는 경우

## <a name="cmfctoolbargetbuttoninfo"></a><a name="getbuttoninfo"></a>CMFC툴바::겟버튼정보

지정된 인덱스에서 단추의 명령 ID, 스타일 및 이미지 인덱스를 반환합니다.

```cpp
void GetButtonInfo(
    int nIndex,
    UINT& nID,
    UINT& nStyle,
    int& iImage) const;
```

### <a name="parameters"></a>매개 변수

*nIndex*<br/>
【인】 도구 모음의 단추 목록에서 단추 인덱스를 지정합니다.

*nID*<br/>
【아웃】 단추의 명령 ID입니다.

*nStyle*<br/>
【아웃】 단추의 스타일입니다.

*아이 이미지*<br/>
【아웃】 단추에 대 한 이미지의 인덱스입니다.

### <a name="remarks"></a>설명

메서드는 `GetButtonInfo` 지정된 인덱스에서 도구 모음 단추를 검색 하 고 단추의 명령 ID, 스타일 및 이미지 인덱스를 검색합니다.

지정된 인덱스의 단추가 없는 경우 프레임워크는 *nID* 및 *nStyle을* 0으로 설정하고 메서드가 반환될 때 *iImage를* -1로 설정합니다.

## <a name="cmfctoolbargetbuttonsize"></a><a name="getbuttonsize"></a>CMFC툴바::겟버튼사이즈

도구 모음에서 각 단추의 크기를 반환합니다.

```
CSize GetButtonSize() const;
```

### <a name="return-value"></a>Return Value

도구 모음의 각 단추의 크기를 지정하는 [CSize 클래스](../../atl-mfc-shared/reference/csize-class.md) 개체입니다.

### <a name="remarks"></a>설명

[CMFCToolBar::SetSizes](#setsizes) 또는 [CMFCToolBar::SetLockedSizes를](#setlockedsizes) 호출하여 도구 모음의 각 단추의 크기를 설정합니다.

## <a name="cmfctoolbargetbuttonstyle"></a><a name="getbuttonstyle"></a>CMFC툴바::겟버튼 스타일

지정된 인덱스에 있는 도구 모음 단추의 현재 스타일을 반환합니다.

```
UINT GetButtonStyle(int nIndex) const;
```

### <a name="parameters"></a>매개 변수

*nIndex*<br/>
【인】 도구 모음 단추의 인덱스를 지정합니다.

### <a name="return-value"></a>Return Value

도구 모음 단추의 스타일을 지정하는 값입니다. . 가능한 스타일 목록은 [도구 모음 컨트롤 스타일을](../../mfc/reference/toolbar-control-styles.md) 참조하십시오.

### <a name="remarks"></a>설명

[CMFCToolBar::SetButtonStyle호출하여](#setbuttonstyle) 도구 모음 단추의 스타일을 설정합니다.

## <a name="cmfctoolbargetbuttontext"></a><a name="getbuttontext"></a>CMFC툴바::GetButton텍스트

지정된 인덱스가 있는 단추의 텍스트 레이블을 반환합니다.

```
CString GetButtonText(int nIndex) const;

void GetButtonText(
    int nIndex,
    CString& rString) const;
```

### <a name="parameters"></a>매개 변수

*nIndex*<br/>
【인】 도구 모음 단추의 인덱스입니다.

*rString*<br/>
【아웃】 도구 모음 단추의 레이블 텍스트입니다.

### <a name="return-value"></a>Return Value

도구 모음 단추의 레이블 텍스트입니다.

### <a name="remarks"></a>설명

[CMFCToolBar::SetButtonText](#setbuttontext) 또는 [CMFCToolBar::SetToolBarBtnText를](#settoolbarbtntext) 호출하여 텍스트 레이블을 설정합니다.

## <a name="cmfctoolbargetcoldimages"></a><a name="getcoldimages"></a>CMFC툴바::겟콜드이미지

응용 프로그램에서 콜드 도구 모음 단추 이미지 컬렉션에 대한 포인터를 반환합니다.

```
static CMFCToolBarImages* GetColdImages();
```

### <a name="return-value"></a>Return Value

콜드 도구 모음 단추 이미지 컬렉션에 대한 포인터입니다.

### <a name="remarks"></a>설명

콜드 이미지는 사용자가 도구 모음 단추와 상호 작용하지 않을 때 사용되는 이미지입니다. [CMFCToolBar::로드비트맵 또는](#loadbitmapex) [CMFCToolBar::로드비트맵을](#loadbitmap) 호출하여 콜드 이미지를 로드합니다.

## <a name="cmfctoolbargetcolumnwidth"></a><a name="getcolumnwidth"></a>CMFC툴바::게열 폭

도구 모음 단추의 너비를 반환합니다.

```
virtual int GetColumnWidth() const;
```

### <a name="return-value"></a>Return Value

도구 모음 단추의 너비를 지정하는 값입니다.

### <a name="remarks"></a>설명

프레임워크는 도구 모음 레이아웃을 계산하기 위해 이 메서드를 호출합니다. 파생 클래스에서 이 메서드를 재정의하여 도구 모음에 대해 다른 열 너비를 지정합니다.

## <a name="cmfctoolbargetcommandbuttons"></a><a name="getcommandbuttons"></a>CMFC툴바::명령 단추 가져옵니다.

응용 프로그램의 모든 도구 모음에서 지정된 명령 ID가 있는 단추 목록을 반환합니다.

```
static int GetCommandButtons(
    UINT uiCmd,
    CObList& listButtons);
```

### <a name="parameters"></a>매개 변수

*uiCmd*<br/>
【인】 단추의 명령 ID입니다.

*목록단추*<br/>
【아웃】 도구 모음 단추 목록을 받는 [CObList 클래스](../../mfc/reference/coblist-class.md) 개체에 대한 참조입니다.

### <a name="return-value"></a>Return Value

지정된 명령 ID가 있는 단추 의 수입니다.

## <a name="cmfctoolbargetcount"></a><a name="getcount"></a>CMFC툴바::겟카운트

도구 모음에서 단추 및 구분 기호 수를 반환합니다.

```
int GetCount() const;
```

### <a name="return-value"></a>Return Value

도구 모음의 단추 및 구분 기호 수입니다.

## <a name="cmfctoolbargetcustomizebutton"></a><a name="getcustomizebutton"></a>CMFCToolBar::Get사용자 지정 버튼

도구 모음과 연결된 `CMFCCustomizeButton` 개체에 대한 포인터를 검색합니다.

```
CMFCCustomizeButton* GetCustomizeButton();
```

### <a name="return-value"></a>Return Value

도구 모음과 `CMFCCustomizeButton` 연결된 개체에 대한 포인터입니다.

### <a name="remarks"></a>설명

이 메서드는 도구 모음의 끝에 나타나는 **사용자 지정** 단추를 검색합니다. [CMFCToolBar::사용자 지정 버튼](#enablecustomizebutton) 메서드를 사용하여 도구 모음에 **사용자 지정** 단추를 추가합니다.

[CMFCToolBar::IsExistCustomizeButton](#isexistcustomizebutton) 메서드를 호출하여 도구 모음에 유효한 `CMFCCustomizeButton` 개체가 포함되어 있는지 확인할 수 있습니다.

## <a name="cmfctoolbargetdefaultimage"></a><a name="getdefaultimage"></a>CMFC툴바::GetDefaultImage

지정된 명령 ID가 있는 도구 모음 단추에 대한 기본 이미지 인덱스를 반환합니다.

```
static int GetDefaultImage(UINT uiID);
```

### <a name="parameters"></a>매개 변수

*uiID*<br/>
【인】 단추의 명령 ID를 지정합니다.

### <a name="return-value"></a>Return Value

공유 이미지 목록의 도구 모음 이미지 인덱스입니다.

### <a name="remarks"></a>설명

이 공유 방법을 사용하여 지정된 명령 ID가 있는 도구 모음 단추에 대한 기본 이미지 인덱스를 검색합니다. 반환 값은 응용 프로그램의 모든 도구 모음에 대한 도구 모음 단추 이미지의 공유 컬렉션에 대한 인덱스입니다. [CMFCToolBar::GetImages](#getimages) 메서드를 호출하여 이 컬렉션에 대한 포인터를 가져옵니다.

## <a name="cmfctoolbargetdisabledimages"></a><a name="getdisabledimages"></a>CMFC툴바::Get장애인이미지

응용 프로그램에서 비활성화된 도구 모음 단추에 사용되는 이미지 컬렉션에 대한 포인터를 반환합니다.

```
static CMFCToolBarImages* __stdcall GetDisabledImages();
```

### <a name="return-value"></a>Return Value

비활성화된 도구 모음 단추 이미지 컬렉션에 대한 포인터입니다.

### <a name="remarks"></a>설명

[CMFCToolBar편집박스버튼 클래스](../../mfc/reference/cmfctoolbareditboxbutton-class.md) 및 [CMFCToolBar::LoadBitmap](#loadbitmap) 메서드를 사용하여 비활성화된 도구 모음 단추 이미지를 로드합니다.

## <a name="cmfctoolbargetdisabledmenuimages"></a><a name="getdisabledmenuimages"></a>CMFCToolBar:::GetDisabled메뉴이미지

응용 프로그램의 비활성화된 메뉴 단추에 사용되는 이미지 컬렉션에 대한 포인터를 반환합니다.

```
static CMFCToolBarImages* __stdcall GetDisabledMenuImages();
```

### <a name="return-value"></a>Return Value

비활성화된 메뉴 이미지 컬렉션에 대한 포인터입니다.

### <a name="remarks"></a>설명

[CMFCToolBar편집박스버튼 클래스](../../mfc/reference/cmfctoolbareditboxbutton-class.md) 메서드를 사용하여 비활성화된 이미지를 로드합니다.

## <a name="cmfctoolbargetdroppeddownmenu"></a><a name="getdroppeddownmenu"></a>CMFC툴바:::GetddownDownMenu

현재 하위 메뉴를 표시하고 있는 메뉴 단추 개체에 대한 포인터를 검색합니다.

```
CMFCToolBarMenuButton* GetDroppedDownMenu(int* pIndex = NULL) const;
```

### <a name="parameters"></a>매개 변수

*p인덱스*<br/>
【아웃】 도구 모음 단추 컬렉션에서 단추 인덱스를 받습니다.

### <a name="return-value"></a>Return Value

하위 메뉴를 표시하는 메뉴가 없는 경우 하위 메뉴 또는 NULL을 표시하는 메뉴 단추 개체에 대한 포인터입니다.

### <a name="remarks"></a>설명

이 메서드가 NULL이 아닌 값을 반환하고 *pIndex가* NULL이 아닌 경우 *pIndex가* 가리키는 값은 도구 모음 단추 컬렉션의 메뉴 단추 인덱스로 설정됩니다.

## <a name="cmfctoolbargetgraydisabledbuttons"></a><a name="getgraydisabledbuttons"></a>CMFCToolBar::GetGray 비활성화 단추

비활성화된 단추의 이미지가 일반 단추 이미지의 흐리게 표시된 버전인지 또는 비활성화된 단추 이미지 컬렉션에서 가져온 것인지 지정합니다.

```
BOOL GetGrayDisabledButtons() const;
```

### <a name="return-value"></a>Return Value

비활성화 된 버튼의 이미지를 어둡게 하는 TRUE; FALSE는 비활성화 된 이미지의 컬렉션에서 이미지를 얻을 수 있습니다.

### <a name="remarks"></a>설명

[CMFCToolBar::SetGray비활성화 단추를](#setgraydisabledbuttons) 사용하여 흐리게 표시된 이미지와 비활성화된 이미지 컬렉션의 이미지 간에 전환합니다.

## <a name="cmfctoolbargethighlightedbutton"></a><a name="gethighlightedbutton"></a>CMFCToolBar::[=[발아하기]

현재 강조 표시된 도구 모음 단추에 대한 포인터를 반환합니다.

```
CMFCToolBarButton* GetHighlightedButton() const;
```

### <a name="return-value"></a>Return Value

도구 모음 단추 개체에 대한 포인터; 또는 NULL을 누르면 버튼이 강조 표시됩니다.

### <a name="remarks"></a>설명

키보드 포커스가 있는 경우 도구 모음 버튼이 강조 표시됩니다. 도구 모음 버튼이 이 응용 프로그램에서 핫 트래킹되는 경우 도구 모음 단추도 강조 표시됩니다(자세한 내용은 [CMFCToolBar::GetHotBorder](#gethotborder) 및 [CMFCToolBar::SetHotBorder](#sethotborder)참조)와 도구 모음 단추 또는 메뉴 항목에 키보드 포커스가 없을 때 마우스가 이를 가리킵니다.

## <a name="cmfctoolbargethotborder"></a><a name="gethotborder"></a>CMFC툴바::겟핫보더

도구 모음 단추를 핫 *트래킹하는지*여부를 결정합니다. 단추를 핫트래킹하면 마우스가 단추를 가로질러 이동할 때 강조 표시됩니다.

```
BOOL GetHotBorder() const;
```

### <a name="return-value"></a>Return Value

TRUE 도구 모음 버튼이 핫 트래킹되는 경우; 그렇지 않으면 false입니다.

### <a name="remarks"></a>설명

기본적으로 도구 모음 단추는 핫 트래킹됩니다.

## <a name="cmfctoolbargethottextcolor"></a><a name="gethottextcolor"></a>CMFC툴바::겟핫텍스트컬러

강조 표시된 도구 모음 단추의 텍스트 색상을 반환합니다.

```
static COLORREF GetHotTextColor();
```

### <a name="return-value"></a>Return Value

현재 강조 표시된 텍스트 색상을 나타내는 [COLORREF](/windows/win32/gdi/colorref) 값입니다.

### <a name="remarks"></a>설명

[CMFCToolBar::SetHotTextColor를](#sethottextcolor) 호출하여 강조 표시된 도구 모음 단추에 대한 새 텍스트 색상을 설정합니다.

## <a name="cmfctoolbargethwndlastfocus"></a><a name="gethwndlastfocus"></a>CMFC툴바:::GetHwndLastFocus

도구 모음이 시작되기 직전에 입력 포커스가 있는 창에 핸들을 반환합니다.

```
HWND GetHwndLastFocus() const;
```

### <a name="return-value"></a>Return Value

이전에 입력 포커스가 있었던 [CMFCBaseToolBar 클래스에서](../../mfc/reference/cmfcbasetoolbar-class.md)파생되지 않은 창에 대한 핸들입니다. 또는 NULL이 없는 경우

### <a name="remarks"></a>설명

컨트롤이 `CMFCToolBar` 입력 포커스를 수신하면 나중에 복원할 수 있도록 포커스를 잃은 핸들을 창에 저장합니다.

## <a name="cmfctoolbargetignoresettext"></a><a name="getignoresettext"></a>CMFC툴바::GetIgnoreSetText

단추 레이블 설정 호출이 무시되는지 여부를 지정합니다.

```
BOOL GetIgnoreSetText() const;
```

### <a name="return-value"></a>Return Value

TRUE 단추 레이블 설정 호출이 무시되는 경우 그렇지 않으면 false입니다.

### <a name="remarks"></a>설명

## <a name="cmfctoolbargetimages"></a><a name="getimages"></a>CMFC툴바::겟이미지

응용 프로그램에서 기본 단추 이미지 컬렉션에 대한 포인터를 반환합니다.

```
static CMFCToolBarImages* GetImages();
```

### <a name="return-value"></a>Return Value

응용 프로그램의 모든 도구 모음에 대한 기본 이미지 컬렉션을 포함하는 [CMFCToolBarImages 클래스](../../mfc/reference/cmfctoolbarimages-class.md) 개체에 대한 포인터입니다.

### <a name="remarks"></a>설명

이 공유 메서드는 응용 프로그램에 대 한 모든 기본 도구 모음 이미지의 컬렉션에 대 한 액세스를 제공 합니다. [CMFCToolBar::LoadBitmap](#loadbitmap) 메서드를 호출하여 컬렉션에 이미지를 추가합니다.

## <a name="cmfctoolbargetimagesize"></a><a name="getimagesize"></a>CMFC툴바::겟이미지사이즈

도구 모음 단추 이미지의 현재 크기를 반환합니다.

```
CSize GetImageSize() const;
```

### <a name="return-value"></a>Return Value

도구 모음 단추 이미지의 현재 크기를 나타내는 [CSize 클래스](../../atl-mfc-shared/reference/csize-class.md) 개체입니다.

## <a name="cmfctoolbargetimagesoffset"></a><a name="getimagesoffset"></a>CMFC툴바::이미지 오프셋

도구 모음 단추 이미지의 전역 목록에서 이 도구 모음의 도구 모음 단추 이미지를 찾는 데 사용되는 인덱스 오프셋을 반환합니다.

```
int GetImagesOffset() const;
```

### <a name="return-value"></a>Return Value

도구 모음 이미지의 인덱스 오프셋입니다.

### <a name="remarks"></a>설명

모든 도구 모음 기본 이미지는 전역 [CMFCToolBarImages 클래스](../../mfc/reference/cmfctoolbarimages-class.md) 목록에 저장됩니다. 도구 모음의 각 단추에 대한 이미지는 해당 목록에 연속적으로 저장됩니다. 이미지 의 인덱스를 계산하려면 도구 모음의 단추 인덱스를 해당 도구 모음 단추의 이미지 목록 시작 부분의 오프셋에 추가합니다.

[CMFCToolBar::ButtonToIndex를](#buttontoindex) 호출하여 단추에 대한 포인터가 지정된 도구 모음 단추의 인덱스를 가져옵니다.

[CMFCToolBar::GetImages를](#getimages) 호출하여 도구 모음 이미지 컬렉션에 대한 포인터를 가져옵니다.

## <a name="cmfctoolbargetinvalidateitemrect"></a><a name="getinvalidateitemrect"></a>CMFCToolBar:::무효화항목 Rect

지정된 인덱스의 단추에 대해 다시 그려야 하는 클라이언트 영역을 검색합니다.

```
virtual void GetInvalidateItemRect(
    int nIndex,
    LPRECT lpRect) const;
```

### <a name="parameters"></a>매개 변수

*nIndex*<br/>
【인】 클라이언트 영역을 검색할 단추의 인덱스입니다.

*Lprect*<br/>
【아웃】 클라이언트 영역을 받는 RECT 개체에 대한 포인터입니다.

### <a name="remarks"></a>설명

*lpRect* 매개 변수는 NULL이 아니어야 합니다. 제공된 인덱스에 단추가 없는 경우 *lpRect는* 0으로 초기화된 RECT 개체를 받습니다.

## <a name="cmfctoolbargetitemid"></a><a name="getitemid"></a>CMFC툴바:::겟아이템ID

지정된 인덱스에서 도구 모음 단추의 명령 ID를 반환합니다.

```
UINT GetItemID(int nIndex) const;
```

### <a name="parameters"></a>매개 변수

*nIndex*<br/>
【인】 도구 모음 단추의 인덱스를 지정합니다.

### <a name="return-value"></a>Return Value

도구 모음 단추의 명령 ID; 또는 지정된 인덱스가 있는 단추가 없는 경우 0입니다.

## <a name="cmfctoolbargetitemrect"></a><a name="getitemrect"></a>CMFC툴바::겟아이템렉트

지정된 인덱스에서 단추의 경계 사각형을 반환합니다.

```
virtual void GetItemRect(
    int nIndex,
    LPRECT lpRect) const;
```

### <a name="parameters"></a>매개 변수

*nIndex*<br/>
【인】 도구 모음 단추의 인덱스를 지정합니다.

*Lprect*<br/>
【아웃】 이미지 경계 `CRect` 사각형의 좌표를 받는 개체에 대한 포인터입니다.

### <a name="remarks"></a>설명

지정된 `CRect` 인덱스의 단추가 없는 경우 *lpRect* 점이 0으로 설정된 개체입니다.

### <a name="example"></a>예제

다음 예제에서는 `GetItemRect` `CMFCToolBar` 클래스의 메서드를 사용 하는 방법을 보여 줍니다. 이 코드 조각은 IE [데모 샘플의](../../overview/visual-cpp-samples.md)일부입니다.

[!code-cpp[NVC_MFC_IEDemo#6](../../mfc/reference/codesnippet/cpp/cmfctoolbar-class_1.h)]
[!code-cpp[NVC_MFC_IEDemo#9](../../mfc/reference/codesnippet/cpp/cmfctoolbar-class_4.cpp)]

## <a name="cmfctoolbargetlargecoldimages"></a><a name="getlargecoldimages"></a>CMFC툴바::겟라지콜드이미지

응용 프로그램에서 큰 콜드 도구 모음 단추 이미지 컬렉션에 대한 포인터를 반환합니다.

```
static CMFCToolBarImages* GetLargeColdImages();
```

### <a name="return-value"></a>Return Value

큰 차가운 이미지의 컬렉션에 대한 포인터입니다.

### <a name="remarks"></a>설명

콜드 이미지는 사용자가 도구 모음 단추와 상호 작용하지 않을 때 사용되는 이미지입니다. [CMFCToolBar::LoadBitmapEx를](#loadbitmapex) 호출하여 큰 콜드 이미지를 로드합니다.

## <a name="cmfctoolbargetlargedisabledimages"></a><a name="getlargedisabledimages"></a>CMFCToolBar::GetLarge장애인 이미지

응용 프로그램에서 큰 비활성화 된 도구 모음 단추 이미지의 컬렉션에 대 한 포인터를 반환 합니다.

```
static CMFCToolBarImages* GetLargeDisabledImages();
```

### <a name="return-value"></a>Return Value

큰 비활성화 된 도구 모음 단추 이미지의 컬렉션에 대 한 포인터입니다.

### <a name="remarks"></a>설명

큰 이미지는 일반 도구 모음 단추 이미지의 큰 버전입니다. [CMFCToolBar::로드비트맵 또는](#loadbitmapex) [CMFCToolBar::LoadBitmap을](#loadbitmap) 호출하여 큰 이미지를 로드합니다.

## <a name="cmfctoolbargetlargeimages"></a><a name="getlargeimages"></a>CMFC툴바::겟라지이미지

응용 프로그램에서 큰 도구 모음 단추 이미지 컬렉션에 대한 포인터를 반환합니다.

```
static CMFCToolBarImages* GetLargeImages();
```

### <a name="return-value"></a>Return Value

큰 도구 모음 단추 이미지 컬렉션에 대한 포인터입니다.

### <a name="remarks"></a>설명

큰 이미지는 일반 도구 모음 단추 이미지의 큰 버전입니다. [CMFCToolBar::LoadBitmapEx에](#loadbitmapex) 전화하여 큰 이미지를 로드합니다.

## <a name="cmfctoolbargetlockedcoldimages"></a><a name="getlockedcoldimages"></a>CMFC툴바::겟록콜드이미지

도구 모음에서 잠긴 콜드 이미지 컬렉션에 대한 포인터를 반환합니다.

```
CMFCToolBarImages* GetLockedColdImages();
```

### <a name="return-value"></a>Return Value

도구 모음이 잠겨 있지 않은 경우 잠긴 콜드 이미지 또는 NULL 컬렉션에 대한 포인터입니다.

### <a name="remarks"></a>설명

잠긴 이미지는 사용자가 도구 모음을 사용자 지정할 수 없을 때 프레임워크에서 사용하는 일반 도구 모음 단추 이미지의 버전입니다. 콜드 이미지는 사용자가 도구 모음 단추와 상호 작용하지 않을 때 사용되는 이미지입니다.

이 메서드는 도구 모음이 잠겨 있지 않은 경우 NULL을 반환합니다. 이 메서드는 도구 모음이 잠겨 있지 않은 경우 디버그 빌드에서 어설션 실패를 생성합니다. 잠긴 도구 모음에 대한 자세한 내용은 [CMFCToolBar::IsLocked](#islocked)을 참조하십시오.

[CMFCToolBar::LoadBitmapEx](#loadbitmapex) 메서드를 호출하여 잠긴 콜드 이미지를 로드합니다.

## <a name="cmfctoolbargetlockeddisabledimages"></a><a name="getlockeddisabledimages"></a>CMFCToolBar::GetLocked장애인 이미지

도구 모음에서 잠긴 비활성화된 이미지 컬렉션에 대한 포인터를 반환합니다.

```
CMFCToolBarImages* GetLockedDisabledImages();
```

### <a name="return-value"></a>Return Value

도구 모음이 잠겨 있지 않은 경우 잠긴 비활성화된 이미지 또는 NULL 컬렉션에 대한 포인터입니다.

### <a name="remarks"></a>설명

잠긴 이미지는 사용자가 도구 모음을 사용자 지정할 수 없을 때 프레임워크에서 사용하는 일반 도구 모음 단추 이미지의 버전입니다. 비활성화된 이미지는 단추에 TBBS_DISABLED 스타일이 있을 때 프레임워크에서 사용하는 이미지입니다.

이 메서드는 도구 모음이 잠겨 있지 않은 경우 NULL을 반환합니다. 이 메서드는 도구 모음이 잠겨 있지 않은 경우 디버그 빌드에서 어설션 실패를 생성합니다. 잠긴 도구 모음에 대한 자세한 내용은 [CMFCToolBar::IsLocked](#islocked)을 참조하십시오.

[CMFCToolBar::LoadBitmapEx](#loadbitmapex) 메서드를 호출하여 잠긴 비활성화된 이미지를 로드합니다.

## <a name="cmfctoolbargetlockedimages"></a><a name="getlockedimages"></a>CMFC툴바::겟록이미지

도구 모음에서 잠긴 단추 이미지 컬렉션에 대한 포인터를 반환합니다.

```
CMFCToolBarImages* GetLockedImages();
```

### <a name="return-value"></a>Return Value

도구 모음이 잠겨 있지 않은 경우 잠긴 도구 모음 단추 이미지 또는 NULL 컬렉션에 대한 포인터입니다.

### <a name="remarks"></a>설명

잠긴 이미지는 사용자가 도구 모음을 사용자 지정할 수 없을 때 프레임워크에서 사용하는 일반 도구 모음 단추 이미지의 버전입니다.

이 메서드는 도구 모음이 잠겨 있지 않은 경우 NULL을 반환합니다. 이 메서드는 도구 모음이 잠겨 있지 않은 경우 디버그 빌드에서 어설션 실패를 생성합니다. 잠긴 도구 모음에 대한 자세한 내용은 [CMFCToolBar::IsLocked](#islocked)을 참조하십시오.

## <a name="cmfctoolbargetlockedimagesize"></a><a name="getlockedimagesize"></a>CMFC툴바::겟잠금이미지크기

잠긴 도구 모음 이미지의 기본 크기를 반환합니다.

```
CSize GetLockedImageSize() const;
```

### <a name="return-value"></a>Return Value

`CSize` 도구 모음이 잠겨 있지 않은 경우 잠긴 도구 `CSize` 모음 이미지 또는 빈 구조의 크기를 지정하는 구조입니다.

### <a name="remarks"></a>설명

잠긴 이미지는 사용자가 도구 모음을 사용자 지정할 수 없을 때 프레임워크에서 사용하는 일반 도구 모음 단추 이미지의 버전입니다.

이 메서드는 `CSize` 도구 모음이 잠겨 있지 않은 경우 너비가 0이고 높이가 0인 구조를 반환합니다. 이 메서드는 도구 모음이 잠겨 있지 않은 경우 디버그 빌드에서 어설션 실패를 생성합니다. 잠긴 도구 모음에 대한 자세한 내용은 [CMFCToolBar::IsLocked](#islocked)을 참조하십시오.

[CMFCToolBar::SetLockedSizes 메서드를](#setlockedsizes) 호출하여 잠긴 이미지 크기를 지정합니다.

## <a name="cmfctoolbargetlockedmenuimages"></a><a name="getlockedmenuimages"></a>CMFC툴바:::겟잠금메뉴이미지

도구 모음에서 잠긴 도구 모음 메뉴 이미지 컬렉션에 대한 포인터를 반환합니다.

```
CMFCToolBarImages* GetLockedMenuImages();
```

### <a name="return-value"></a>Return Value

도구 모음이 잠겨 있지 않은 경우 잠긴 도구 모음 메뉴 이미지 또는 NULL 컬렉션에 대한 포인터입니다.

### <a name="remarks"></a>설명

잠긴 이미지는 사용자가 도구 모음을 사용자 지정할 수 없을 때 프레임워크에서 사용하는 일반 도구 모음 메뉴 이미지의 버전입니다.

이 메서드는 도구 모음이 잠겨 있지 않은 경우 NULL을 반환합니다. 이 메서드는 도구 모음이 잠겨 있지 않은 경우 디버그 빌드에서 어설션 실패를 생성합니다. 잠긴 도구 모음에 대한 자세한 내용은 [CMFCToolBar::IsLocked](#islocked)을 참조하십시오.

잠긴 메뉴 이미지를 로드하려면 [CMFCToolBar::LoadBitmapEx](#loadbitmapex) 메서드를 호출합니다.

## <a name="cmfctoolbargetmenubuttonsize"></a><a name="getmenubuttonsize"></a>CMFCToolBar:::겟메뉴버튼크기

응용 프로그램에서 메뉴 단추의 크기를 반환합니다.

```
static CSize GetMenuButtonSize();
```

### <a name="return-value"></a>Return Value

메뉴 `CSize` 단추의 크기를 픽셀 단위로 나타내는 개체입니다.

### <a name="remarks"></a>설명

도구 모음의 메뉴 단추 크기는 전역 변수로 유지되며 이 정적 방법으로 검색할 수 있습니다.

이 전역 변수를 설정하려면 [CMFCToolBar::SetMenus를 호출합니다.](#setmenusizes)

## <a name="cmfctoolbargetmenuimages"></a><a name="getmenuimages"></a>CMFC툴바::겟메뉴이미지

응용 프로그램의 메뉴 단추 이미지 컬렉션에 대한 포인터를 반환합니다.

```
static CMFCToolBarImages* GetMenuImages();
```

### <a name="return-value"></a>Return Value

메뉴 이미지 컬렉션에 대한 포인터입니다.

### <a name="remarks"></a>설명

[CMFCToolBar::LoadBitmapEx](#loadbitmapex) 메서드를 호출하여 메뉴 이미지를 로드합니다.

[CMFCToolBar::SetMenuSizes 메서드를](#setmenusizes) 호출하여 단추 크기와 이미지의 크기를 설정합니다.

## <a name="cmfctoolbargetmenuimagesize"></a><a name="getmenuimagesize"></a>CMFC툴바::겟메뉴이미지크기

응용 프로그램에서 메뉴 단추 이미지의 크기를 반환합니다.

```
static CSize GetMenuImageSize();
```

### <a name="return-value"></a>Return Value

메뉴 `CSize` 이미지의 크기를 나타내는 개체입니다.

### <a name="remarks"></a>설명

이 메서드는 전역 변수로 유지 되는 도구 모음 메뉴 단추에 이미지의 크기를 반환 합니다. 이 전역 변수를 설정하려면 [CMFCToolBar::SetMenus를 호출합니다.](#setmenusizes)

## <a name="cmfctoolbargetorigbuttons"></a><a name="getorigbuttons"></a>CMFC툴바::게도리버튼

도구 모음의 사용자 지정되지 않은 단추 컬렉션을 검색합니다.

```
const CObList& GetOrigButtons() const;
```

### <a name="return-value"></a>Return Value

도구 모음의 사용자 지정되지 않은 단추 목록에 대한 참조입니다.

### <a name="remarks"></a>설명

프레임워크는 사용자가 사용자 지정하기 전에 도구 모음 단추의 복사본을 만듭니다. [CMFCToolBar::SetButtons](#setbuttons) 메서드는 제공된 배열의 각 단추복사본을 원래 단추 목록에 추가합니다. [CMFCToolBar::RestoreOriginalState](#restoreoriginalstate) 메서드는 리소스 파일에서 로드하여 도구 모음의 원래 상태를 복원합니다.

도구 모음의 원래 단추 목록을 설정하려면 [CMFCToolBar::SetOrigButtons 메서드를 호출합니다.](#setorigbuttons)

## <a name="cmfctoolbargetorigresetbuttons"></a><a name="getorigresetbuttons"></a>CMFCToolBar::게토리리리셋버튼

도구 모음의 사용자 지정되지 않은 재설정 단추 컬렉션을 검색합니다.

```
const CObList& GetOrigResetButtons() const;
```

### <a name="return-value"></a>Return Value

도구 모음의 사용자 지정되지 않은 재설정 단추 목록에 대한 참조입니다.

### <a name="remarks"></a>설명

사용자가 사용자 지정 모드 중에 **재설정** 단추를 클릭하면 프레임워크는 이 메서드를 사용하여 도구 모음에서 제거된 단추를 복원합니다.

[CMFCToolBar::SetButtons](#setbuttons) 메서드는 [CMFCToolBar::OnReset](#onreset) 메서드를 호출한 후 원래 재설정 단추 목록에 각 도구 모음 단추의 복사본을 추가합니다. 사용자가 **재설정** 단추를 누를 때 단추모양을 사용자 지정하려면 [CMFCToolBar::OnReset](#onreset) 메서드를 재정의할 수 있습니다.

## <a name="cmfctoolbargetresourceid"></a><a name="getresourceid"></a>CMFC툴바:::GetResourceID

도구 모음의 리소스 ID를 검색합니다.

```
UINT GetResourceID() const;
```

### <a name="return-value"></a>Return Value

도구 모음의 리소스 ID입니다.

### <a name="remarks"></a>설명

[CMFCToolBar::LoadToolBarEx](#loadtoolbarex) 메서드를 호출하여 도구 모음의 리소스 ID를 설정합니다.

## <a name="cmfctoolbargetroutecommandsviaframe"></a><a name="getroutecommandsviaframe"></a>CMFC툴바::겟루트커맨드비아프레임

부모 프레임 또는 소유자인 개체가 도구 모음에 명령을 보내는지 결정합니다.

```
BOOL GetRouteCommandsViaFrame();
```

### <a name="return-value"></a>Return Value

부모 프레임이 도구 모음에 명령을 보내는 경우 0이 아닙니다. 소유자가 도구 모음에 명령을 보내는 경우 0입니다.

### <a name="remarks"></a>설명

기본적으로 상위 프레임은 도구 모음에 명령을 보냅니다. 이 동작을 변경하려면 [CMFCToolBar::SetRouteCommandsViaFrame을](#setroutecommandsviaframe) 호출합니다.

이 메서드가 0이 아닌 값을 반환하는 경우 메서드를 사용하여 `CMFCToolBar::GetCommandTarget` 상위 프레임 개체에 대한 포인터를 검색할 수 있습니다. 이 메서드를 사용하는 예제는 VisualStudioDemo 샘플을 참조하십시오.

## <a name="cmfctoolbargetrowheight"></a><a name="getrowheight"></a>CMFC툴바::겟로우높이

도구 모음 단추의 높이를 반환합니다.

```
virtual int GetRowHeight() const;
```

### <a name="return-value"></a>Return Value

도구 모음 단추의 높이(픽셀)입니다.

### <a name="remarks"></a>설명

프레임워크는 도구 모음 레이아웃을 계산하기 위해 이 메서드를 호출합니다. 파생 클래스에서 이 메서드를 재정의하여 도구 모음에 대해 다른 높이를 지정합니다.

## <a name="cmfctoolbargetshowtooltips"></a><a name="getshowtooltips"></a>CMFC툴바::겟쇼툴팁

도구 모음 단추에 대해 도구 설명이 표시되는지 여부를 지정합니다.

```
static BOOL GetShowTooltips();
```

### <a name="return-value"></a>Return Value

TRUE 도구 모음 단추에 대해 도구 설명이 표시되는 경우 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

기본적으로 도구 설명이 표시됩니다. [CMFCToolBar::SetShowTooltips를](#setshowtooltips)호출하여 이 정적 플래그를 변경할 수 있습니다.

## <a name="cmfctoolbargetsiblingtoolbar"></a><a name="getsiblingtoolbar"></a>CMFC툴바:::더시매툴바

도구 모음의 형제를 검색합니다.

```
CMFCToolBar* GetSiblingToolBar();
```

### <a name="return-value"></a>Return Value

형제 도구 모음에 대한 포인터입니다.

### <a name="remarks"></a>설명

**한 행의 단추 표시** 및 두 행 **단추의 단추 표시를** 활성화하는 방법에 대한 자세한 내용은 [CMFCToolBar::SetSiblingToolBar](#setsiblingtoolbar)를 참조하십시오.

## <a name="cmfctoolbargetuserimages"></a><a name="getuserimages"></a>CMFC툴바::GetUserimages

응용 프로그램에서 사용자 정의 도구 모음 단추 이미지 컬렉션에 대한 포인터를 반환합니다.

```
static CMFCToolBarImages* GetUserImages();
```

### <a name="return-value"></a>Return Value

응용 프로그램의 모든 도구 모음에 대한 사용자 정의 도구 모음 단추 이미지 컬렉션에 대한 포인터입니다.

### <a name="remarks"></a>설명

[CMFCToolBar::SetUserImages](#setuserimages) 메서드를 호출하여 응용 프로그램에서 사용자 정의 이미지의 컬렉션을 설정합니다.

## <a name="cmfctoolbarhittest"></a><a name="hittest"></a>CMFC툴바::히트테스트

지정된 위치에 있는 도구 모음 단추의 인덱스를 반환합니다.

```
virtual int HitTest(CPoint point);
```

### <a name="parameters"></a>매개 변수

*지점*<br/>
【인】 클라이언트 좌표에서 테스트할 점입니다.

### <a name="return-value"></a>Return Value

지정된 위치에 있는 단추의 인덱스 또는 -1 이러한 버튼이 없거나 단추가 구분 기호인 경우 -1입니다.

## <a name="cmfctoolbarinsertbutton"></a><a name="insertbutton"></a>CMFC툴바::삽입버튼

도구 모음에 단추를 삽입합니다.

```
virtual int InsertButton(
    const CMFCToolBarButton& button,
    INT_PTR iInsertAt=-1);

virtual int InsertButton(
    CMFCToolBarButton* pButton,
    int iInsertAt=-1);
```

### <a name="parameters"></a>매개 변수

*단추*<br/>
【인】 삽입할 단추를 지정합니다.

*아이 인서트*<br/>
【인】 버튼을 삽입할 0기반 위치를 지정합니다.

### <a name="return-value"></a>Return Value

오류가 발생할 경우 단추를 삽입한 위치 또는 -1입니다.

### <a name="remarks"></a>설명

*iInsertAt가* -1이면 이 메서드는 도구 모음 단추 목록의 끝에 단추를 추가합니다.

[CMFCToolBar::삽입분리기](#insertseparator) 메서드를 호출하여 도구 모음에 구분 기호를 삽입합니다.

## <a name="cmfctoolbarinsertseparator"></a><a name="insertseparator"></a>CMFCToolBar::삽입분리기

도구 모음에 구분 기호를 삽입합니다.

```
virtual int InsertSeparator(INT_PTR iInsertAt=-1);
```

### <a name="parameters"></a>매개 변수

*아이 인서트*<br/>
【인】 구분 기호를 삽입할 0기반 위치를 지정합니다. 이 매개변수는 0보다 커야 합니다.

### <a name="return-value"></a>Return Value

오류가 발생할 경우 구분 기호가 삽입된 위치 또는 -1입니다.

### <a name="remarks"></a>설명

이 메서드를 호출하여 두 개의 기존 단추 사이에 구분 기호를 삽입합니다. *iInsertAt가* -1이면 이 메서드는 도구 모음 단추 목록의 끝에 구분 기호를 추가합니다.

빈 도구 모음에 구분 기호를 추가하려면 이 메서드를 사용할 수 없습니다.

[CMFCToolBar::InsertButton](#insertbutton) 메서드를 호출하여 도구 모음에 단추를 삽입합니다.

## <a name="cmfctoolbarinvalidatebutton"></a><a name="invalidatebutton"></a>CMFCToolBar::무효화 단추

제공된 인덱스에 있는 도구 모음 단추의 클라이언트 영역을 무효화합니다.

```
CMFCToolBarButton* InvalidateButton(int nIndex);
```

### <a name="parameters"></a>매개 변수

*nIndex*<br/>
【인】 도구 모음에서 단추의 0기반 인덱스입니다.

### <a name="return-value"></a>Return Value

이러한 개체가 `CMFCToolBarButton` 없는 경우 제공된 인덱스 또는 NULL에 있는 개체에 대한 포인터입니다.

### <a name="remarks"></a>설명

프레임워크는 도구 모음 단추와 연결된 클라이언트 영역을 업데이트할 때 이 메서드를 호출합니다. 제공된 인덱스에 있는 `CMFCToolBarButton` 개체의 클라이언트 사각형을 사용 하 고 [CWnd::InvalidateRect](../../mfc/reference/cwnd-class.md#invalidaterect) 메서드를 호출 합니다.

## <a name="cmfctoolbarisaddremovequickcustomize"></a><a name="isaddremovequickcustomize"></a>CMFCToolBar::IsAddRemove빠른 사용자 정의

**사용자 지정** 메뉴 옵션을 사용하여 사용자가 도구 모음 단추를 추가하거나 제거할 수 있는지 여부를 결정합니다.

```
BOOL IsAddRemoveQuickCustomize();
```

### <a name="return-value"></a>Return Value

TRUE 사용자가 **사용자 지정** 메뉴 옵션을 사용하여 도구 모음을 수정할 수 있는 경우 그렇지 않으면 false입니다.

### <a name="remarks"></a>설명

## <a name="cmfctoolbarisaltcustomizemode"></a><a name="isaltcustomizemode"></a>CMFC툴바::이Alt사용자 정의 모드

빠른 사용자 *지정이* 단추를 드래그하는 데 사용되는지 여부를 지정합니다. 빠른 사용자 지정이 활성화되면 사용자는 Alt 키를 길게 누르고 단추를 새 위치로 끌 수 있습니다.

```
static BOOL __stdcall IsAltCustomizeMode();
```

### <a name="return-value"></a>Return Value

TRUE 빠른 사용자 지정이 단추를 드래그하는 데 사용되는 경우; 그렇지 않으면 false입니다.

### <a name="remarks"></a>설명

## <a name="cmfctoolbarisautograyinactiveimages"></a><a name="isautograyinactiveimages"></a>CMFC툴바::이오토그레이인액티브이미지

비활성(강조 표시되지 않은) 단추 이미지의 자동 생성이 활성화되어 있는지 여부를 지정합니다.

```
static BOOL IsAutoGrayInactiveImages();
```

### <a name="return-value"></a>Return Value

TRUE 비활성 이미지를 자동으로 어둡게 하는 옵션이 활성화되어 있는 경우 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

[CMFCToolBar::AutoGrayInactiveImages](#autograyinactiveimages)를 호출하여 비활성 이미지의 자동 디밍을 활성화하거나 비활성화할 수 있습니다.

## <a name="cmfctoolbarisbasiccommand"></a><a name="isbasiccommand"></a>CMFC툴바::기본 명령

명령이 기본 명령 목록에 있는지 여부를 결정합니다.

```
static BOOL IsBasicCommand(UINT uiCmd);
```

### <a name="parameters"></a>매개 변수

*uiCmd*<br/>
【인】 검사할 명령을 지정합니다.

### <a name="return-value"></a>Return Value

TRUE 지정된 명령이 기본 명령 목록에 속하는 경우 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

이 정적 메서드는 *uiCmd가* 지정한 명령이 기본 명령의 전역 목록에 속하는지 여부를 결정합니다. [CMFCToolBar::AddBasicCommand](#addbasiccommand) 또는 [CMFCToolBar::SetBasicCommand를](#setbasiccommands)호출하여 기본 명령 목록을 변경할 수 있습니다.

## <a name="cmfctoolbarisbuttonextrasizeavailable"></a><a name="isbuttonextrasizeavailable"></a>CMFC툴바::이버튼엑스트라사이즈사용 가능

도구 모음에 테두리가 확장된 단추를 표시할 수 있는지 여부를 결정합니다.

```
virtual BOOL IsButtonExtraSizeAvailable() const;
```

### <a name="return-value"></a>Return Value

막대가 테두리 크기가 더 많은 단추를 표시할 수 있는 경우 TRUE입니다. 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

테두리가 확장된 단추를 표시할 수 있는 경우 도구 모음 개체는 TRUE를 반환합니다. 도구 모음 단추는 [CMFCToolBarButton::OnChangeParentWnd](../../mfc/reference/cmfctoolbarbutton-class.md#onchangeparentwnd) 알림을 처리할 때 이 메서드를 호출하고 그에 따라 내부 추가 테두리 크기 플래그를 설정합니다. 이 내부 플래그는 [나중에 CMFCToolBarButton::IsExtraSize](../../mfc/reference/cmfctoolbarbutton-class.md#isextrasize)를 호출하여 검색할 수 있습니다.

막대에서 파생된 클래스에서 `CMFCToolBar` 이 메서드를 재정의하고 막대에 추가 테두리 크기의 도구 모음 단추를 표시하고 FALSE를 반환할 수 있는 경우 TRUE를 반환합니다. 기본 구현은 TRUE를 반환합니다.

## <a name="cmfctoolbarisbuttonhighlighted"></a><a name="isbuttonhighlighted"></a>CMFC툴바::버튼 강조 표시

지정된 단추를 강조 표시할지 여부를 결정합니다.

```
BOOL IsButtonHighlighted(int iButton) const;
```

### <a name="parameters"></a>매개 변수

*iButton*<br/>
【인】 도구 모음 단추의 인덱스를 지정합니다.

### <a name="return-value"></a>Return Value

TRUE 지정된 단추를 강조 표시하면 그렇지 않으면 false입니다.

### <a name="remarks"></a>설명

## <a name="cmfctoolbariscommandpermitted"></a><a name="iscommandpermitted"></a>CMFC툴바::명령 허용

명령이 허용되는지 여부를 결정합니다.

```
static BOOL IsCommandPermitted(UINT uiCmd);
```

### <a name="parameters"></a>매개 변수

*uiCmd*<br/>
【인】 검사할 명령을 지정합니다.

### <a name="return-value"></a>Return Value

TRUE 지정된 명령이 허용되는 경우; 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

이 정적 메서드는 *uiCmd가* 지정한 명령이 허용되지 않는 명령의 전역 목록에 속하는지 여부를 결정합니다.

[CMFCToolBar::SetNon허용 명령](#setnonpermittedcommands): 호출하여 허용되지 않는 명령 목록을 변경할 수 있습니다.

## <a name="cmfctoolbariscommandrarelyused"></a><a name="iscommandrarelyused"></a>CMFCToolBar:::명령이 거의 사용되지 않습니다.

명령이 거의 사용되지 않는지 여부를 결정합니다.

```
static BOOL IsCommandRarelyUsed(UINT uiCmd);
```

### <a name="parameters"></a>매개 변수

*uiCmd*<br/>
【인】 검사할 명령을 지정합니다.

### <a name="return-value"></a>Return Value

TRUE 지정된 명령이 거의 사용되지 않는 경우; 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

메서드는 `IsCommandRarelyUsed` 다음 조건 중 하나 이상이 발생할 때 FALSE를 반환합니다.

- 지정된 명령은 기본 명령 목록에 속합니다.

- 지정된 명령은 표준 명령 중 하나입니다.

- 프레임워크가 사용자 지정 모드에 있습니다.

- 기본 명령 목록이 비어 있습니다.

- 명령 호출의 20% 이상이 지정된 명령에 대한 호출입니다.

## <a name="cmfctoolbariscustomizemode"></a><a name="iscustomizemode"></a>CMFC툴바::사용자 지정 모드

도구 모음 프레임워크가 사용자 지정 모드에 있는지 여부를 지정합니다.

```
static BOOL IsCustomizeMode();
```

### <a name="return-value"></a>Return Value

TRUE 프레임워크가 사용자 지정 모드인 경우 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

[CMFCToolBar::SetCustomizeMode를](#setcustomizemode)호출하여 사용자 지정 모드를 전환할 수 있습니다.

사용자가 사용자 지정 대화 [상자(CMFCToolBar사용자 지정Dialog 클래스)를](../../mfc/reference/cmfctoolbarscustomizedialog-class.md)호출할 때 프레임워크가 모드를 변경합니다.

## <a name="cmfctoolbarisdragbutton"></a><a name="isdragbutton"></a>CMFC툴바::이드래그 버튼

도구 모음 단추를 드래그할지 여부를 결정합니다.

```
BOOL IsDragButton(const CMFCToolBarButton* pButton) const;
```

### <a name="parameters"></a>매개 변수

*p 버튼*<br/>
【인】 도구 모음 단추에 대한 포인터입니다.

### <a name="return-value"></a>Return Value

TRUE 지정된 단추를 드래그하는 경우; 그렇지 않으면 false입니다.

### <a name="remarks"></a>설명

## <a name="cmfctoolbarisexistcustomizebutton"></a><a name="isexistcustomizebutton"></a>CMFCToolBar::존재가 없는 사용자 지정 단추

도구 모음에 **사용자 지정** 단추가 포함되어 있는지 여부를 확인합니다.

```
BOOL IsExistCustomizeButton();
```

### <a name="return-value"></a>Return Value

TRUE 도구 모음에 **사용자 지정** 버튼이 포함되어 있는 경우 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

이 메서드가 TRUE를 반환하는 경우 [CMFCToolBar::GetCustomizeButton](#getcustomizebutton) 메서드는 도구 모음 끝에 나타나는 **사용자 지정** 단추에 대한 포인터를 반환합니다.

[CMFCToolBar::사용자 지정 버튼](#enablecustomizebutton) 메서드를 사용하여 도구 모음에 **사용자 지정** 단추를 추가합니다.

## <a name="cmfctoolbarisfloating"></a><a name="isfloating"></a>CMFC툴바::떠다니기

도구 모음이 부동 인지 여부를 확인 합니다.

```
virtual BOOL IsFloating() const;
```

### <a name="return-value"></a>Return Value

도구 모음이 부동 하는 경우 TRUE; 그렇지 않으면 false입니다.

## <a name="cmfctoolbarislargeicons"></a><a name="islargeicons"></a>CMFC툴바::이라지아이콘

응용 프로그램의 도구 모음이 현재 큰 아이콘을 표시할지 여부를 지정합니다.

```
static BOOL IsLargeIcons();
```

### <a name="return-value"></a>Return Value

응용 프로그램이 큰 아이콘을 사용하는 경우 TRUE; 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

[CMFCToolBar::SetLargeIcons를](#setlargeicons) 호출하여 큰 아이콘과 일반 아이콘 사이를 전환합니다.

사용자가 **사용자 지정** 대화 상자의 **옵션** 페이지에서 큰 아이콘 **확인란을** 전환하면 프레임워크가 자동으로 모드를 변경합니다.

## <a name="cmfctoolbarislastcommandfrombutton"></a><a name="islastcommandfrombutton"></a>CMFC툴바::IsLastCommandFrom버튼

지정된 도구 모음 단추에서 가장 최근에 실행된 명령을 보냈는지 여부를 결정합니다.

```
static BOOL IsLastCommandFromButton(CMFCToolBarButton* pButton);
```

### <a name="parameters"></a>매개 변수

*p 버튼*<br/>
【인】 버튼에 대한 포인터.

### <a name="return-value"></a>Return Value

*true pButton* 지정 하는 단추에서 마지막 명령을 보낸 경우; 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

이 메서드는 를 호출하여 `CWnd::GetCurrentMessage` [MSG 구조에](/windows/win32/api/winuser/ns-winuser-msg) 대한 포인터를 가져옵니다. 그런 다음 단추의 HWND를 `MSG::lParam` 멤버와 `MSG::hwnd` 비교하여 단추가 명령의 원본인지 확인합니다.

## <a name="cmfctoolbarislocked"></a><a name="islocked"></a>CMFC툴바:::잠김

도구 모음이 잠겨 있는지 여부를 결정합니다.

```
BOOL IsLocked() const;
```

### <a name="return-value"></a>Return Value

도구 모음이 잠겨 있는 경우 TRUE입니다. 그렇지 않으면 false입니다.

### <a name="remarks"></a>설명

이 메서드는 사용자가 도구 모음 단추를 재배치 하는 등 사용자 지정 작업을 수행할 수 없는 경우 TRUE를 반환 합니다.

잠긴 도구 모음은 별도의 이미지 목록을 사용합니다. 이러한 이미지 목록에 대한 자세한 내용은 [CMFCToolBar::LoadBitmapEx](#loadbitmapex)을 참조하십시오.

## <a name="cmfctoolbarisonerowwithsibling"></a><a name="isonerowwithsibling"></a>CMFC툴바::이스원로우위드형제

도구 모음 및 해당 형제 도구 모음이 동일한 행에 배치되는지 여부를 결정합니다.

```
BOOL IsOneRowWithSibling();
```

### <a name="return-value"></a>Return Value

TRUE 도구 모음과 해당 형제가 동일한 행에 위치하는 경우 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

[CMFC사용자 지정 단추::CreatePopupMenu](internal-classes.md) 메서드는 이 메서드를 호출하여 **사용자 지정** 팝업 메뉴를 표시하는 방법을 결정합니다. 이 메서드가 TRUE를 반환하는 경우 프레임워크는 **한 행에 단추 표시 단추를 표시합니다.** 그렇지 않으면 프레임워크에 **두 행에 단추 표시 버튼이** 표시됩니다.

일반적으로 이 메서드를 사용할 필요가 없습니다. **한 행에서 단추 표시를** 사용하거나 **두 행 단추에 단추 표시를** 사용하려면 [CMFCToolBar::SetSiblingToolBar를](#setsiblingtoolbar)호출합니다.

## <a name="cmfctoolbarisresourcechanged"></a><a name="isresourcechanged"></a>CMFC툴바::리소스 변경

```
virtual BOOL IsResourceChanged() const;
```

### <a name="return-value"></a>Return Value

### <a name="remarks"></a>설명

## <a name="cmfctoolbarissibling"></a><a name="issibling"></a>CMFC툴바::이형제

```
BOOL IsSibling();
```

### <a name="return-value"></a>Return Value

### <a name="remarks"></a>설명

## <a name="cmfctoolbarisuserdefined"></a><a name="isuserdefined"></a>CMFC툴바::이유저 정의

도구 모음이 사용자 정의인지 여부를 지정합니다.

```
BOOL IsUserDefined() const;
```

### <a name="return-value"></a>Return Value

TRUE 도구 모음이 사용자가 만든 경우 그렇지 않으면 거짓.

## <a name="cmfctoolbarloadbitmap"></a><a name="loadbitmap"></a>CMFC툴바::로드비트맵

애플리케이션 리소스에서 도구 모음 이미지를 로드합니다.

```
virtual BOOL LoadBitmap(
    UINT uiResID,
    UINT uiColdResID=0,
    UINT uiMenuResID=0,
    BOOL bLocked=FALSE,
    UINT uiDisabledResID=0,
    UINT uiMenuDisabledResID=0);
```

### <a name="parameters"></a>매개 변수

*uiResID*<br/>
【인】 핫 도구 모음 이미지를 참조하는 비트맵의 리소스 ID입니다.

*uiColdResID*<br/>
【인】 콜드 도구 모음 이미지를 참조하는 비트맵의 리소스 ID입니다.

*uiMenuResID*<br/>
【인】 일반 메뉴 이미지를 참조하는 비트맵의 리소스 ID입니다.

*차단*<br/>
【인】 TRUE는 도구 모음을 잠급전지 않습니다. 그렇지 않으면 거짓.

*ui장애인ResID*<br/>
【인】 비활성화된 도구 모음 이미지를 참조하는 비트맵의 리소스 ID입니다.

*uiMenu장애인ResID*<br/>
【인】 비활성화된 메뉴 이미지를 참조하는 비트맵의 리소스 ID입니다.

### <a name="return-value"></a>Return Value

메서드가 성공하면 0이 아니고, 실패하면 0입니다.

### <a name="remarks"></a>설명

[CMFCToolBar::LoadToolBarEx](#loadtoolbarex) 메서드는 이 메서드를 호출하여 도구 모음과 관련된 이미지를 로드합니다. 이미지 리소스의 사용자 지정 로드를 수행하려면 이 메서드를 재정의합니다.

`LoadBitmapEx` 메서드를 호출하여 도구 모음을 만든 후 추가 이미지를 로드합니다.

## <a name="cmfctoolbarloadbitmapex"></a><a name="loadbitmapex"></a>CMFC툴바::로드비트맵

```
virtual BOOL LoadBitmapEx(
    CMFCToolBarInfo& params,
    BOOL bLocked = FALSE);
```

### <a name="parameters"></a>매개 변수

【인】 *매개 변수*<br/>
【인】 *블로블*<br/>

### <a name="return-value"></a>Return Value

### <a name="remarks"></a>설명

## <a name="cmfctoolbarloadlargeiconsstate"></a><a name="loadlargeiconsstate"></a>CMFCToolBar::로드라지아이콘스테이트

```
static BOOL __stdcall LoadLargeIconsState(LPCTSTR lpszProfileName = NULL);
```

### <a name="parameters"></a>매개 변수

【인】 *lpsz프로필이름*<br/>

### <a name="return-value"></a>Return Value

### <a name="remarks"></a>설명

## <a name="cmfctoolbarloadparameters"></a><a name="loadparameters"></a>CMFC툴바::로드매개변수

Windows 레지스트리에서 전역 도구 모음 옵션을 로드합니다.

```
static BOOL LoadParameters(LPCTSTR lpszProfileName=NULL);
```

### <a name="parameters"></a>매개 변수

*lpszProfileName*<br/>
【인】 Windows 레지스트리 키의 상대 경로를 지정합니다.

### <a name="return-value"></a>Return Value

메서드가 성공하면 0이 아니고, 실패하면 0입니다.

### <a name="remarks"></a>설명

이 메서드는 메뉴 애니메이션 유형, 메뉴 그림자 스타일 및 Windows 레지스트리에서 큰 아이콘을 표시할지 여부와 같은 전역 매개 변수를 로드합니다.

[CWinAppEx::LoadState](../../mfc/reference/cwinappex-class.md#loadstate) 메서드는 응용 프로그램의 초기화 프로세스의 일부로이 메서드를 호출합니다.

## <a name="cmfctoolbarloadstate"></a><a name="loadstate"></a>CMFC툴바::로드스테이트

Windows 레지스트리에서 도구 모음 상태 정보를 로드합니다.

```
virtual BOOL LoadState(
    LPCTSTR lpszProfileName=NULL,
    int nIndex=-1,
    UINT uiID=(UINT)-1);
```

### <a name="parameters"></a>매개 변수

*lpszProfileName*<br/>
【인】 Windows 레지스트리 키의 상대 경로를 지정합니다.

*nIndex*<br/>
【인】 도구 모음의 컨트롤 ID를 지정합니다.

*uiID*<br/>
【인】 도구 모음의 리소스 ID를 지정합니다.

### <a name="return-value"></a>Return Value

메서드가 성공하면 0이 아니고, 실패하면 0입니다.

### <a name="remarks"></a>설명

프레임워크는 이 메서드를 응용 프로그램의 초기화 프로세스의 일부로 호출합니다. 자세한 내용은 [CWinAppEx::LoadState](../../mfc/reference/cwinappex-class.md#loadstate)를 참조하십시오.

## <a name="cmfctoolbarloadtoolbar"></a><a name="loadtoolbar"></a>CMFC툴바::로드툴바

응용 프로그램 리소스에서 도구 모음을 로드합니다.

```
virtual BOOL LoadToolBar(
    UINT uiResID,
    UINT uiColdResID=0,
    UINT uiMenuResID=0,
    BOOL bLocked=FALSE,
    UINT uiDisabledResID=0,
    UINT uiMenuDisabledResID=0,
    UINT uiHotResID=0);
```

### <a name="parameters"></a>매개 변수

*uiResID*<br/>
【인】 도구 모음의 리소스 ID입니다.

*uiColdResID*<br/>
【인】 콜드 도구 모음 이미지를 참조하는 비트맵의 리소스 ID입니다.

*uiMenuResID*<br/>
【인】 일반 메뉴 이미지를 참조하는 비트맵의 리소스 ID입니다.

*차단*<br/>
【인】 도구 모음이 잠겨 있는지 여부를 지정하는 부울 값입니다. 이 매개변수가 *TRUE이면*도구 모음이 잠겨 있습니다. 그렇지 않으면 도구 모음이 잠겨 있지 않습니다.

*ui장애인ResID*<br/>
【인】 비활성화된 도구 모음 이미지를 참조하는 비트맵의 리소스 ID입니다.

*uiMenu장애인ResID*<br/>
【인】 비활성화된 메뉴 이미지를 참조하는 비트맵의 리소스 ID입니다.

*uiHotResID*<br/>
【인】 핫 도구 모음 이미지를 참조하는 비트맵의 리소스 ID입니다.

### <a name="return-value"></a>Return Value

메서드가 성공하면 0이 아니고, 실패하면 0입니다.

### <a name="remarks"></a>설명

프레임워크는 초기화 중에 이 메서드를 호출하여 도구 모음과 연결된 이미지를 로드합니다.

### <a name="example"></a>예제

다음 예제에서는 `LoadToolBar` `CMFCToolBar` 클래스에서 메서드를 사용 하는 방법을 보여 줍니다. 이 코드 조각은 IE [데모 샘플의](../../overview/visual-cpp-samples.md)일부입니다.

[!code-cpp[NVC_MFC_IEDemo#6](../../mfc/reference/codesnippet/cpp/cmfctoolbar-class_1.h)]
[!code-cpp[NVC_MFC_IEDemo#7](../../mfc/reference/codesnippet/cpp/cmfctoolbar-class_3.cpp)]

## <a name="cmfctoolbarloadtoolbarex"></a><a name="loadtoolbarex"></a>CMFC툴바::로드툴바렉스

`CMFCToolBarInfo` 도우미 클래스를 사용하여 응용 프로그램에서 큰 이미지를 사용할 수 있도록 하여 응용 프로그램 리소스에서 도구 모음을 로드합니다.

```
virtual BOOL LoadToolBarEx(
    UINT uiToolbarResID,
    CMFCToolBarInfo& params,
    BOOL bLocked=FALSE);
```

### <a name="parameters"></a>매개 변수

*uiToolbarResID*<br/>
【인】 도구 모음의 리소스 ID입니다.

*params*<br/>
【인】 도구 모음 `CMFCToolBarInfo` 이미지에 대한 리소스 번호가 포함된 개체에 대한 참조입니다.

*차단*<br/>
【인】 도구 모음이 잠겨 있는지 여부를 지정하는 부울 값입니다. 이 매개변수가 TRUE이면 도구 모음이 잠깁니다. 그렇지 않으면 도구 모음이 잠겨 있지 않습니다.

### <a name="return-value"></a>Return Value

메서드가 성공하면 0이 아니고, 실패하면 0입니다.

### <a name="remarks"></a>설명

응용 프로그램 리소스에서 도구 모음 이미지를 로드하려면 이 메서드를 호출합니다.

## <a name="cmfctoolbarm_dbllargeimageratio"></a><a name="m_dbllargeimageratio"></a>CMFC툴바::m_dblLargeImageRatio

큰 이미지의 치수(높이 또는 너비)와 일반 이미지의 크기 사이의 비율을 지정합니다.

```
AFX_IMPORT_DATA static double m_dblLargeImageRatio;
```

### <a name="remarks"></a>설명

기본 비율은 2입니다. 이 값을 변경하여 큰 도구 모음 이미지를 더 크거나 작게 만들 수 있습니다.

큰 이미지 집합을 지정하지 않으면 프레임워크에서 이 데이터 멤버를 사용합니다. 예를 들어 크기가 16x16인 작은 이미지 집합만 제공하고 큰 이미지의 크기가 24x24로 설정하려면 이 데이터 멤버를 1.5로 설정합니다.

## <a name="cmfctoolbarnextmenu"></a><a name="nextmenu"></a>CMFC툴바::다음메뉴

```
virtual BOOL NextMenu();
```

### <a name="return-value"></a>Return Value

### <a name="remarks"></a>설명

## <a name="cmfctoolbaronbeforeremovebutton"></a><a name="onbeforeremovebutton"></a>CMFC툴바::에 이전 제거 버튼

```
virtual BOOL OnBeforeRemoveButton(
    CMFCToolBarButton* pButton,
    DROPEFFECT dropEffect);
```

### <a name="parameters"></a>매개 변수

*p 버튼*<br/>
【인】 하지 않는.

*드롭 이펙트*<br/>
【인】 하지 않는.

### <a name="return-value"></a>Return Value

### <a name="remarks"></a>설명

## <a name="cmfctoolbaronchangehot"></a><a name="onchangehot"></a>CMFC툴바::온체인지핫

사용자가 도구 모음에서 단추를 선택할 때 프레임워크에서 호출됩니다.

```
virtual void OnChangeHot(int iHot);
```

### <a name="parameters"></a>매개 변수

*아이 핫*<br/>
【인】 선택한 도구 모음 단추의 인덱스를 지정합니다. 또는 -1을 선택하면 도구 모음 단추가 선택되어 있지 않습니다.

### <a name="remarks"></a>설명

사용자가 도구 모음에서 단추를 선택한 알림을 처리하려면 이 메서드를 재정의합니다.

## <a name="cmfctoolbaronchangevisualmanager"></a><a name="onchangevisualmanager"></a>CMFC툴바::온체인지비주얼매니저

```
virtual void OnChangeVisualManager();
```

### <a name="remarks"></a>설명

## <a name="cmfctoolbaronfillbackground"></a><a name="onfillbackground"></a>CMFC툴바::온필백

도구 모음 배경을 채우기 위해 [CBasePane::DoPaint의](../../mfc/reference/cbasepane-class.md#dopaint) 프레임워크에서 호출합니다.

```
virtual void OnFillBackground(CDC* pDC);
```

### <a name="parameters"></a>매개 변수

*pDC*<br/>
【인】 장치 컨텍스트에 대한 포인터입니다.

### <a name="remarks"></a>설명

[CMFCToolBar::DoPaint는](#dopaint) 도구 모음의 배경이 채워졌을 때 이 메서드를 호출합니다. 기본 구현은 아무 작업도 수행하지 않습니다.

파생 클래스에서 사용자 지정 배경을 그리려면 이 메서드를 재정의합니다.

## <a name="cmfctoolbaronglobalfontschanged"></a><a name="onglobalfontschanged"></a>CMFC툴바:::온글로벌폰트변경

```
virtual void OnGlobalFontsChanged();
```

### <a name="remarks"></a>설명

## <a name="cmfctoolbaronreset"></a><a name="onreset"></a>CMFC툴바::온리셋

도구 모음을 원래 상태로 복원합니다.

```
virtual void OnReset();
```

### <a name="remarks"></a>설명

도구 모음 재설정에 대한 알림을 처리하려면 이 메서드를 재정의합니다.

기본 구현은 아무 작업도 수행하지 않습니다. 도구 `OnReset` 모음에 도구 모음이 원래 상태로 돌아올 때 교체해야 하는 더미 단추가 있는 `CMFCToolBar` 경우 파생된 클래스에서 재정의합니다.

## <a name="cmfctoolbaronsetaccdata"></a><a name="onsetaccdata"></a>CMFC툴바::온셋데이터

```
virtual BOOL OnSetAccData(long lVal);
```

### <a name="parameters"></a>매개 변수

【인】 *lVal*<br/>

### <a name="return-value"></a>Return Value

### <a name="remarks"></a>설명

## <a name="cmfctoolbaronsetdefaultbuttontext"></a><a name="onsetdefaultbuttontext"></a>CMFC툴바::기본 설정 단추 텍스트

도구 모음 단추의 텍스트를 기본 상태로 복원합니다.

```
virtual BOOL OnSetDefaultButtonText(CMFCToolBarButton* pButton);
```

### <a name="parameters"></a>매개 변수

*p 버튼*<br/>
【인】 텍스트가 설정된 단추를 가리킵니다.

### <a name="return-value"></a>Return Value

TRUE 텍스트가 성공적으로 복원된 경우; 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

도구 모음 단추의 텍스트가 기본값으로 변경되고 있다는 알림을 처리하려면 이 메서드를 재정의합니다.

기본 구현은 응용 프로그램 리소스에서 단추의 텍스트를 로드합니다.

## <a name="cmfctoolbaronusertooltip"></a><a name="onusertooltip"></a>CMFC툴바::온유저툴팁

단추의 도구 설명이 표시될 때 프레임워크에서 호출합니다.

```
virtual BOOL OnUserToolTip(
    CMFCToolBarButton* pButton,
    CString& strTTText) const;
```

### <a name="parameters"></a>매개 변수

*p 버튼*<br/>
【인】 도구 설명이 표시될 도구 모음 단추를 가리킵니다.

*스트릿 텍스트*<br/>
【아웃】 도구 설명의 텍스트를 받는 `CString` 개체에 대한 참조입니다.

### <a name="return-value"></a>Return Value

true *strTTText가* 도구 설명 텍스트로 채워진 경우; 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

도구 모음 단추의 도구 설명이 표시될 때 프레임워크는 이 메서드를 호출합니다. TRUE를 반환하는 경우 `OnUserToolTip` 프레임워크는 *strTTText*에서 `OnUserToolTip` 반환된 텍스트를 포함하는 도구 설명이 표시됩니다. 그렇지 않으면 도구 설명에 단추 텍스트가 포함되어 있습니다.

도구 `OnUserToolTip` 모음 단추의 도구 팁을 사용자 지정하려면 재정의합니다. 기본 구현은 도구 설명 텍스트를 얻기 위해 [CMFCToolBar::OnUserToolTip을](#onusertooltip) 호출합니다.

## <a name="cmfctoolbarprevmenu"></a><a name="prevmenu"></a>CMFC툴바::P레브메뉴

```
virtual BOOL PrevMenu();
```

### <a name="return-value"></a>Return Value

### <a name="remarks"></a>설명

## <a name="cmfctoolbarprocesscommand"></a><a name="processcommand"></a>CMFC툴바::P로키스커맨드

도구 모음을 소유한 창에 WM_COMMAND 메시지를 게시합니다.

```
BOOL ProcessCommand(CMFCToolBarButton* pButton);
```

### <a name="parameters"></a>매개 변수

*p 버튼*<br/>
【인】 도구 모음의 단추에 대한 포인터입니다.

### <a name="return-value"></a>Return Value

이 메서드는 항상 TRUE를 반환 해야 합니다. MFC는 내부적으로 FALSE 값을 사용합니다.

### <a name="remarks"></a>설명

이 메서드는 [CWnd::PostMessage를](../../mfc/reference/cwnd-class.md#postmessage) 호출 하 고 *wParam* 매개 변수로 지정 된 단추의 명령 ID를 전달 하 여 도구 모음을 소유 하는 창에 WM_COMMAND 메시지를 게시 합니다.

[ON_COMMAND](message-map-macros-mfc.md#on_command) 매크로를 사용하여 WM_COMMAND 메시지를 멤버 함수에 매핑합니다.

## <a name="cmfctoolbarremoveallbuttons"></a><a name="removeallbuttons"></a>CMFC툴바::모든 버튼 제거

도구 모음에서 모든 단추와 구분 기호를 제거합니다.

```
virtual void RemoveAllButtons();
```

### <a name="remarks"></a>설명

프레임워크는 도구 모음을 다시 만들거나 삭제할 때 이 메서드를 호출합니다.

## <a name="cmfctoolbarremovebutton"></a><a name="removebutton"></a>CMFC툴바::제거버튼

도구 모음에서 지정된 인덱스가 있는 단추를 제거합니다.

```
virtual BOOL RemoveButton(int iIndex);
```

### <a name="parameters"></a>매개 변수

*iIndex*<br/>
【인】 제거할 단추의 0기반 인덱스를 지정합니다.

### <a name="return-value"></a>Return Value

TRUE 메서드가 성공하는 경우 또는 지정된 인덱스가 유효하지 않거나 인덱스가 **사용자 지정** 단추를 참조하는 경우 FALSE입니다.

### <a name="remarks"></a>설명

이 메서드는 단추 제거의 영향을 받는 추가 도구 모음 특성을 업데이트합니다. 예를 들어 이 메서드는 도구 모음에서 불필요한 구분 기호를 제거하고 바로 가기 키 테이블을 다시 작성합니다.

**사용자 지정** 단추에 대한 자세한 내용은 [CMFCToolBar::사용자 지정 버튼](#enablecustomizebutton)을 참조하십시오.

## <a name="cmfctoolbarremovestatefromregistry"></a><a name="removestatefromregistry"></a>CMFC툴바::제거상태로부터레지스트리

Windows 레지스트리에서 도구 모음의 상태 정보를 삭제합니다.

```
virtual BOOL RemoveStateFromRegistry(
    LPCTSTR lpszProfileName=NULL,
    int nIndex=-1,
    UINT uiID=(UINT)-1);
```

### <a name="parameters"></a>매개 변수

*lpszProfileName*<br/>
【인】 상태 정보가 있는 레지스트리 키를 지정합니다.

*nIndex*<br/>
【인】 도구 모음의 컨트롤 ID입니다.

*uiID*<br/>
【인】 도구 모음의 리소스 ID입니다. 이 매개 변수가 -1인 경우 이 메서드는 [CWnd::GetDLgCtrlID](../../mfc/reference/cwnd-class.md#getdlgctrlid) 메서드를 사용하여 리소스 ID를 검색합니다.

### <a name="return-value"></a>Return Value

메서드가 성공하면 0이 아니고, 실패하면 0입니다.

### <a name="remarks"></a>설명

프레임워크는 사용자 정의 도구 모음을 삭제할 때 이 메서드를 호출합니다.

Windows 레지스트리에 추가 상태 정보를 저장하는 경우 이 메서드를 재정의합니다.

## <a name="cmfctoolbarreplacebutton"></a><a name="replacebutton"></a>CMFC툴바::대체 버튼

도구 모음 단추를 다른 도구 모음 단추로 바꿉습니다.

```
int ReplaceButton(
    UINT uiCmd,
    const CMFCToolBarButton& button,
    BOOL bAll=FALSE);
```

### <a name="parameters"></a>매개 변수

*uiCmd*<br/>
【인】 바꿀 단추의 명령 ID입니다.

*단추*<br/>
【인】 삽입할 에 `CMFCToolBarButton` 대한 참조입니다.

*볼*<br/>
【인】 *uiCmd에*의해 지정된 명령 ID가 있는 모든 단추를 바꿀지 여부를 지정하는 부울 값입니다. 이 매개 변수가 TRUE이면 지정된 명령 ID가 있는 모든 단추가 대체됩니다. 그렇지 않으면 첫 번째 단추가 바뀝다.

### <a name="return-value"></a>Return Value

대체되는 단추 수입니다. 이 메서드는 도구 모음에 지정된 명령 ID가 있는 단추가 없는 경우 0을 반환합니다.

### <a name="remarks"></a>설명

리소스에서 로드할 수 없는 도구 모음 단추를 추가하려는 경우 이 메서드를 호출합니다. 디자인 타임에 자리 표시자 단추를 만들고 도구 모음을 초기화할 때 해당 단추를 사용자 지정 단추로 바꿀 수 있습니다. 이 메서드를 사용하는 예제는 VisualStudioDemo 샘플을 참조하십시오.

### <a name="example"></a>예제

다음 예제에서는 `ReplaceButton` `CMFCToolBar` 클래스에서 메서드를 사용 하는 방법을 보여 줍니다. 이 코드 조각은 IE [데모 샘플의](../../overview/visual-cpp-samples.md)일부입니다.

[!code-cpp[NVC_MFC_IEDemo#6](../../mfc/reference/codesnippet/cpp/cmfctoolbar-class_1.h)]
[!code-cpp[NVC_MFC_IEDemo#10](../../mfc/reference/codesnippet/cpp/cmfctoolbar-class_5.cpp)]

## <a name="cmfctoolbarresetall"></a><a name="resetall"></a>CMFC툴바::모두 재설정

모든 도구 모음을 원래 상태로 복원합니다.

```
static void __stdcall ResetAll();
```

### <a name="remarks"></a>설명

이 메서드는 복원할 수 있는 응용 프로그램의 각 도구 모음에서 [CMFCToolBar::RestoreOriginalState](#restoreoriginalstate) 메서드를 호출합니다. [CMFCToolBar::CanBeRestored](#canberestored) 메서드를 사용하여 도구 모음을 복원할 수 있는지 여부를 결정합니다.

## <a name="cmfctoolbarresetallimages"></a><a name="resetallimages"></a>CMFC툴바::리셋올이미지

응용 프로그램의 모든 도구 모음 이미지 컬렉션을 지웁습니다.

```
static void __stdcall ResetAllImages();
```

### <a name="remarks"></a>설명

이 메서드는 [CMFCToolBar::LoadToolBar](#loadtoolbar) 및 [CMFCToolBar::LoadBitmap](#loadbitmap) 메서드에 의해 초기화 되는 이미지 컬렉션을 지웁니까.

## <a name="cmfctoolbarresetimages"></a><a name="resetimages"></a>CMFC툴바::이미지 재설정

```
virtual void ResetImages();
```

### <a name="remarks"></a>설명

## <a name="cmfctoolbarrestorefocus"></a><a name="restorefocus"></a>CMFC툴바::복원 포커스

```
virtual void RestoreFocus();
```

### <a name="remarks"></a>설명

## <a name="cmfctoolbarrestoreoriginalstate"></a><a name="restoreoriginalstate"></a>CMFC툴바::복원오리지널스테이트

도구 모음의 원래 상태를 복원합니다.

```
virtual BOOL RestoreOriginalState();
```

### <a name="return-value"></a>Return Value

TRUE 메서드가 성공하는 경우 또는 메서드가 실패하거나 도구 모음이 사용자 정의된 경우 FALSE입니다.

### <a name="remarks"></a>설명

이 메서드는 [CMFCToolBar::LoadToolBar](#loadtoolbar) 메서드를 사용 하 여 리소스 파일에서 도구 모음을 로드 합니다.

사용자가 사용자 지정 대화 상자의 **도구 모음** 페이지에서 모든 단추 **재설정** 단추를 선택할 때 프레임워크는 이 메서드를 호출합니다.

## <a name="cmfctoolbarsaveparameters"></a><a name="saveparameters"></a>CMFC툴바::저장매개변수

```
static BOOL __stdcall SaveParameters(LPCTSTR lpszProfileName = NULL);
```

### <a name="parameters"></a>매개 변수

【인】 *lpsz프로필이름*<br/>

### <a name="return-value"></a>Return Value

### <a name="remarks"></a>설명

## <a name="cmfctoolbarsavestate"></a><a name="savestate"></a>CMFC툴바::저장상태

Windows 레지스트리에서 도구 모음에 대한 상태 정보를 저장합니다.

```
virtual BOOL SaveState(
    LPCTSTR lpszProfileName=NULL,
    int nIndex=-1,
    UINT uiID=(UINT)-1);
```

### <a name="parameters"></a>매개 변수

*lpszProfileName*<br/>
【인】 Windows 레지스트리 키의 상대 경로를 지정합니다.

*nIndex*<br/>
【인】 도구 모음의 컨트롤 ID입니다.

*uiID*<br/>
【인】 도구 모음의 리소스 ID입니다.

### <a name="return-value"></a>Return Value

메서드가 성공하면 0이 아니고, 실패하면 0입니다.

### <a name="remarks"></a>설명

프레임워크는 응용 프로그램 상태를 레지스트리에 저장할 때 이 메서드를 호출합니다. 자세한 내용은 [CWinAppEx::SaveState](../../mfc/reference/cwinappex-class.md#savestate)를 참조하십시오.

## <a name="cmfctoolbarsetbasiccommands"></a><a name="setbasiccommands"></a>CMFC툴바::기본 명령

사용자가 메뉴를 열 때 항상 표시되는 명령 목록을 설정합니다.

```
static void __stdcall SetBasicCommands(CList<UINT,UINT>& lstCommands);
```

### <a name="parameters"></a>매개 변수

*lstCommands*<br/>
【인】 명령 컬렉션을 `CList` 포함하는 개체에 대한 참조입니다.

### <a name="remarks"></a>설명

메뉴를 열 때 기본 명령이 항상 표시됩니다. 이 메서드는 사용자가 최근에 사용한 명령을 보도록 선택할 때 의미가 있습니다.

[CMFCToolBar::AddBasicCommand](#addbasiccommand) 메서드를 사용하여 기본 명령 목록에 명령을 추가합니다. [CMFCToolBar::GetBasicCommands 메서드를](#getbasiccommands) 사용하여 응용 프로그램에서 사용하는 기본 명령 목록을 검색합니다.

이 메서드를 사용하는 예제는 Explorer 샘플을 참조하십시오.

## <a name="cmfctoolbarsetbuttoninfo"></a><a name="setbuttoninfo"></a>CMFC툴바::버튼정보

도구 모음 단추의 명령 ID, 스타일 및 이미지 ID를 설정합니다.

```cpp
void SetButtonInfo(
    int nIndex,
    UINT nID,
    UINT nStyle,
    int iImage);
```

### <a name="parameters"></a>매개 변수

*nIndex*<br/>
【인】 속성이 설정된 단추의 0기반 인덱스입니다.

*nID*<br/>
【인】 단추의 명령 ID입니다.

*nStyle*<br/>
【인】 단추의 스타일입니다. 사용 가능한 도구 모음 단추 스타일 목록은 [도구 모음 컨트롤 스타일을](../../mfc/reference/toolbar-control-styles.md) 참조하십시오.

*아이 이미지*<br/>
【인】 단추의 0 기반 이미지 인덱스(즉, 도구 모음 이미지 컬렉션의 인덱스)입니다.

### <a name="remarks"></a>설명

도구 모음 단추의 속성을 설정 하려면이 메서드를 호출 합니다.

디버그 빌드에서 이 메서드는 *nIndex에* 의해 지정된 인덱스가 유효하지 않은 경우 어설션 오류를 생성합니다.

[CMFCToolBar::SetButtonStyle](#setbuttonstyle) 메서드를 호출하여 단추의 스타일만 설정합니다.

## <a name="cmfctoolbarsetbuttons"></a><a name="setbuttons"></a>CMFC툴바::버튼

도구 모음의 단추를 설정합니다.

```
virtual BOOL SetButtons(
    const UINT* lpIDArray,
    int nIDCount,
    BOOL bRemapImages=TRUE);
```

### <a name="parameters"></a>매개 변수

*lpID어레이*<br/>
【인】 삽입할 단추의 명령 아이디 배열에 대한 포인터입니다.

*니드 카운트*<br/>
【인】 *lpIDArray의*항목 수입니다.

*bRemap이미지*<br/>
【인】 기존 단추 이미지를 삽입된 단추와 연결할지 여부를 지정하는 부울 값입니다. 이 매개 변수가 TRUE이면 이미지가 다시 매핑됩니다.

### <a name="return-value"></a>Return Value

메서드가 성공하면 0이 아니고, 실패하면 0입니다.

### <a name="remarks"></a>설명

도구 모음에서 기존 단추를 제거하고 새 단추 컬렉션을 삽입하려면 이 메서드를 호출합니다.

이 메서드는 도구 모음에 **사용자 지정** 단추를 추가 하 고 도구 모음의 부모 창에 AFX_WM_RESETTOOLBAR 메시지를 보냅니다. **사용자 지정** 단추에 대한 자세한 내용은 [CMFCToolBar::사용자 지정 버튼](#enablecustomizebutton)을 참조하십시오.

## <a name="cmfctoolbarsetbuttonstyle"></a><a name="setbuttonstyle"></a>CMFC툴바::버튼 스타일

지정된 인덱스에서 도구 모음 단추의 스타일을 설정합니다.

```
virtual void SetButtonStyle(
    int nIndex,
    UINT nStyle);
```

### <a name="parameters"></a>매개 변수

*nIndex*<br/>
【인】 스타일을 설정할 도구 모음 단추의 0기반 인덱스입니다.

*nStyle*<br/>
【인】 단추의 스타일입니다. 사용 가능한 도구 모음 단추 스타일 목록은 [도구 모음 컨트롤 스타일을](../../mfc/reference/toolbar-control-styles.md) 참조하십시오.

### <a name="remarks"></a>설명

이 메서드는 사용자가 비활성화된 단추를 클릭할 수 없기 때문에 *nStyle이* TBBS_DISABLED 경우 TBBS_PRESSED 스타일을 제거합니다.

## <a name="cmfctoolbarsetbuttontext"></a><a name="setbuttontext"></a>CMFC툴바::설정 버튼 텍스트

도구 모음 단추의 텍스트 레이블을 설정합니다.

```
BOOL SetButtonText(
    int nIndex,
    LPCTSTR lpszText);
```

### <a name="parameters"></a>매개 변수

*nIndex*<br/>
【인】 도구 모음 단추의 인덱스입니다.

*lpszText*<br/>
【인】 도구 모음 단추의 텍스트 레이블입니다. NULL이 아닌 이어야 합니다.

### <a name="return-value"></a>Return Value

메서드가 성공하면 TRUE입니다. 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

제공된 인덱스가 유효한 도구 모음 단추를 참조하지 않는 경우 이 메서드는 FALSE를 반환합니다.

## <a name="cmfctoolbarsetcommandusageoptions"></a><a name="setcommandusageoptions"></a>CMFCToolBar::설정 명령 사용 옵션

거의 사용되지 않는 명령이 응용 프로그램의 메뉴에 나타나지 않는 경우를 지정합니다.

```
static BOOL SetCommandUsageOptions(
    UINT nStartCount,
    UINT nMinUsagePercentage=5);
```

### <a name="parameters"></a>매개 변수

*n스타트카운트*<br/>
【인】 프레임워크에 기본 및 최근에 사용한 명령만 표시하기 전에 명령을 실행해야 하는 횟수를 지정합니다.

*nMinUsage백분율*<br/>
【인】 최근에 사용한 명령으로 간주하려면 명령을 실행해야 하는 횟수의 백분율입니다.

### <a name="return-value"></a>Return Value

false if *nMinUsagePercentage* 같거나 100보다 큰 경우; 그렇지 않으면 TRUE.

### <a name="remarks"></a>설명

이 메서드를 호출하여 프레임워크에서 사용하는 알고리즘을 사용자 지정하여 기본 메뉴 항목과 최근에 사용한 메뉴 항목이 표시되는 방식을 결정합니다. 기본 명령에 대한 자세한 내용은 [CMFCToolBar::AddBasicCommand](#addbasiccommand)을 참조하십시오.

이 클래스는 `CMFCCmdUsageCount` 클래스를 사용하여 명령의 사용 수를 추적합니다. 이 클래스에 대한 자세한 내용은 [CMFCCmdUsageCountCount 클래스를](../../mfc/reference/cmfccmdusagecount-class.md)참조하십시오.

## <a name="cmfctoolbarsetcustomizemode"></a><a name="setcustomizemode"></a>CMFC툴바::셋커마이징모드

응용 프로그램의 모든 도구 모음에 대해 사용자 지정 모드를 활성화하거나 사용하지 않도록 설정합니다.

```
static BOOL __stdcall SetCustomizeMode(BOOL bSet=TRUE);
```

### <a name="parameters"></a>매개 변수

*bSet*<br/>
【인】 사용자 지정 모드를 활성화하거나 사용하지 않도록 설정할지 여부를 지정하는 부울 값입니다. 이 매개 변수를 TRUE로 설정하여 사용자 지정 모드 또는 FALSE를 활성화하여 비활성화합니다.

### <a name="return-value"></a>Return Value

TRUE 이 메서드를 호출하면 사용자 지정 모드가 변경됩니다. 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

이 메서드는 응용 프로그램의 각 도구 모음의 레이아웃을 조정 하고 다시 그립니다. [CMFCToolBar::IsCustomizeMode](#iscustomizemode) 메서드를 호출하여 응용 프로그램이 사용자 지정 모드에 있는지 확인합니다.

## <a name="cmfctoolbarsetgraydisabledbuttons"></a><a name="setgraydisabledbuttons"></a>CMFCToolBar::설정회색 비활성화 단추

도구 모음에서 사용할 수 없는 단추를 흐리게 하는지 또는 단추를 사용할 수 없는 이미지를 사용할지 여부를 지정합니다.

```cpp
void SetGrayDisabledButtons(BOOL bGrayDisabledButtons);
```

### <a name="parameters"></a>매개 변수

*b회색비활성화단추*<br/>
【인】 사용할 수 없는 단추를 표시 하는 방법을 지정 하는 부울 값입니다. 이 매개 변수가 TRUE이면 프레임워크가 단추를 어둡게 합니다. 그렇지 않으면 프레임워크에서 단추를 사용할 수 없는 이미지의 컬렉션을 사용합니다.

### <a name="remarks"></a>설명

기본적으로 사용할 수 없는 단추는 흐리게 표시됩니다.

## <a name="cmfctoolbarsetheight"></a><a name="setheight"></a>CMFC툴바::셋높이

도구 모음의 높이를 설정합니다.

```cpp
void SetHeight(int cyHeight);
```

### <a name="parameters"></a>매개 변수

*사이 높이*<br/>
【인】 도구 모음의 높이(픽셀)입니다.

### <a name="remarks"></a>설명

이 메서드는 높이를 설정 한 후 도구 모음을 다시 그립니다.

## <a name="cmfctoolbarsethelpmode"></a><a name="sethelpmode"></a>CMFC툴바::세트헬모드

```
static void __stdcall SetHelpMode(BOOL bOn = TRUE);
```

### <a name="parameters"></a>매개 변수

【인】 *bOn*<br/>

### <a name="remarks"></a>설명

## <a name="cmfctoolbarsethot"></a><a name="sethot"></a>CMFC툴바::세트핫

```
BOOL SetHot(CMFCToolBarButton* pMenuButton);
```

### <a name="parameters"></a>매개 변수

【인】 *p메뉴 버튼*<br/>

### <a name="return-value"></a>Return Value

### <a name="remarks"></a>설명

## <a name="cmfctoolbarsethotborder"></a><a name="sethotborder"></a>CMFC툴바::셋핫보더

도구 모음 단추를 핫 트래킹할지 여부를 지정합니다.

```cpp
void SetHotBorder(BOOL bShowHotBorder);
```

### <a name="parameters"></a>매개 변수

*b쇼핫보더*<br/>
【인】 도구 모음 단추를 핫 트랙할지 여부를 지정하는 부울 값입니다. 이 매개 변수가 TRUE이면 도구 모음에서 해당 단추를 핫트랙합니다. 그렇지 않으면 도구 모음에서 단추를 핫트랙하지 않습니다.

### <a name="remarks"></a>설명

단추를 핫 트래킹하는 경우 마우스가 단추를 가로질러 이동할 때 프레임워크가 단추를 강조 표시됩니다. 기본적으로 각 도구 모음은 해당 단추를 핫트랙합니다.

[CMFCToolBar::GetHotBorder](#gethotborder) 메서드를 호출하여 도구 모음이 해당 단추를 핫트랙하는지 여부를 확인합니다.

## <a name="cmfctoolbarsethottextcolor"></a><a name="sethottextcolor"></a>CMFC툴바::세트핫텍스트컬러

핫 도구 모음 단추의 텍스트 색상을 설정합니다.

```
static void SetHotTextColor(COLORREF clrText);
```

### <a name="parameters"></a>매개 변수

*clrText*<br/>
【인】 핫 트래킹된 도구 모음 단추의 텍스트 색상을 지정합니다.

### <a name="remarks"></a>설명

핫 트래킹 도구 모음 단추에 대한 자세한 내용은 [CMFCToolBar::GetHotBorder](#gethotborder) 및 [CMFCToolBar::SetHotBorder](#sethotborder)를 참조하십시오.

## <a name="cmfctoolbarsetignoresettext"></a><a name="setignoresettext"></a>CMFC툴바::세트무시세트텍스트

```cpp
void SetIgnoreSetText(BOOL bValue);
```

### <a name="parameters"></a>매개 변수

【인】 *b값*<br/>

### <a name="remarks"></a>설명

## <a name="cmfctoolbarsetlargeicons"></a><a name="setlargeicons"></a>CMFCToolBar::세트대형아이콘

도구 모음 단추에 큰 아이콘이 표시되는지 여부를 지정합니다.

```
static void SetLargeIcons(BOOL bLargeIcons=TRUE);
```

### <a name="parameters"></a>매개 변수

*bLargeicons*<br/>
【인】 사용할 아이콘을 지정하는 부울 값입니다. 이 매개 변수가 TRUE이면 프레임워크에 큰 아이콘이 표시됩니다. 그렇지 않으면 프레임워크에 일반 아이콘이 표시됩니다.

### <a name="remarks"></a>설명

사용자가 **사용자 지정** 대화 상자의 **옵션** 탭에서 **큰 아이콘** 확인란의 상태를 변경할 때 프레임워크는 이 메서드를 호출합니다. 이 메서드는 응용 프로그램의 모든 도구 모음 크기를 조정합니다.

기본적으로 프레임워크는 일반 아이콘을 표시합니다.

**사용자 지정** 대화 상자에 대한 자세한 내용은 [CMFCToolBar사용자 지정 Dialog 클래스](../../mfc/reference/cmfctoolbarscustomizedialog-class.md)를 참조하십시오.

## <a name="cmfctoolbarsetlockedsizes"></a><a name="setlockedsizes"></a>CMFC툴바::세트로크사이즈

도구 모음에서 잠긴 단추 및 잠긴 이미지의 크기를 설정합니다.

```cpp
void SetLockedSizes(
    SIZE sizeButton,
    SIZE sizeImage,
    BOOL bDontScale = FALSE);
```

### <a name="parameters"></a>매개 변수

*크기 버튼*<br/>
【인】 잠긴 도구 모음 단추의 크기를 지정합니다.

*크기이미지*<br/>
【인】 잠긴 도구 모음 이미지의 크기를 지정합니다.

*bDontScale*<br/>
높은 DPI 모드에서 잠긴 도구 모음 이미지의 배율을 조정할지 여부를 지정합니다.

### <a name="remarks"></a>설명

잠긴 단추의 기본 크기는 23x22 픽셀입니다. 잠긴 이미지의 기본 크기는 16x15 픽셀입니다.

[CMFCToolBar::GetLockedImageSize](#getlockedimagesize) 메서드를 호출하여 잠긴 이미지의 크기를 검색합니다. [CMFCToolBar::GetButtonSize](#getbuttonsize) 메서드를 호출하여 잠긴 도구 모음 단추의 크기를 검색합니다.

## <a name="cmfctoolbarsetmaskmode"></a><a name="setmaskmode"></a>CMFC툴바::셋마스크모드

```cpp
void SetMaskMode(BOOL bMasked);
```

### <a name="parameters"></a>매개 변수

【인】 *bMasked*<br/>

### <a name="remarks"></a>설명

## <a name="cmfctoolbarsetmenusizes"></a><a name="setmenusizes"></a>CMFCToolBar:::세트메뉴크기

도구 모음 메뉴 단추와 해당 이미지의 크기를 설정합니다.

```
static void __stdcall SetMenuSizes(
    SIZE sizeButton,
    SIZE sizeImage);
```

### <a name="parameters"></a>매개 변수

*크기 버튼*<br/>
【인】 도구 모음 단추의 크기를 픽셀 단위로 지정합니다.

*크기이미지*<br/>
【인】 도구 모음 이미지의 크기를 픽셀 단위로 지정합니다.

### <a name="remarks"></a>설명

기본적으로 메뉴 단추와 해당 이미지의 크기는 정의되지 않았습니다.

[CMFCToolBar::GetMenuButtonSize](#getmenubuttonsize) 메서드를 호출하여 메뉴 단추의 크기와 [CMFCToolBar::GetMenuImageSize](#getmenuimagesize) 메서드를 사용하여 메뉴 단추 이미지의 크기를 결정합니다.

이 메서드를 사용하는 예제는 IEDemo 및 MSMoneyDemo 샘플을 참조하십시오.

## <a name="cmfctoolbarsetnonpermittedcommands"></a><a name="setnonpermittedcommands"></a>CMFCToolBar::설정비 허용 명령

사용자가 실행할 수 없는 명령 목록을 설정합니다.

```
static void SetNonPermittedCommands(CList<UINT,UINT>& lstCommands);
```

### <a name="parameters"></a>매개 변수

*lstCommands*<br/>
【인】 사용자가 실행할 `CList` 수 없는 명령을 포함하는 개체에 대한 참조입니다.

### <a name="remarks"></a>설명

사용자가 특정 명령을 선택하지 못하도록 하려면 이 메서드를 호출합니다. 예를 들어 사용자가 보안상의 이유로 특정 명령을 선택하지 못하도록 할 수 있습니다. 이 메서드를 사용하는 예제는 MDITabsDemo 및 MenuSubSet 샘플을 참조하십시오.

이 메서드는 허용되지 않는 명령의 이전 목록을 지웁히 지웁습니다. 기본적으로 허용되지 않는 명령 목록은 비어 있습니다.

## <a name="cmfctoolbarsetonerowwithsibling"></a><a name="setonerowwithsibling"></a>CMFC툴바::셋원로우위드형제

도구 모음과 해당 형제를 동일한 행에 배치합니다.

```cpp
void SetOneRowWithSibling();
```

### <a name="remarks"></a>설명

사용자가 한 행의 단추 표시 단추를 클릭할 때 프레임워크는 이 **메서드를 호출합니다.**

[CMFCToolBar::SetSiblingToolBar](#setsiblingtoolbar) 메서드를 호출하여 **한 행의 단추 표시** 또는 두 행 **단추의 단추 표시를** 활성화합니다. 이 도구 모음에 대해 [CMFCToolBar::SetSiblingToolBar를](#setsiblingtoolbar) 호출하면 형제 도구 모음이 이 도구 모음의 행으로 이동됩니다. 그렇지 않으면 이 도구 모음이 형제의 행으로 이동됩니다.

프레임워크는 사용자가 두 행의 단추 표시 단추를 클릭할 때 [CMFCToolBar::SetTwoRowsWithSibling](#settworowswithsibling) **메서드를 호출합니다.**

## <a name="cmfctoolbarsetorigbuttons"></a><a name="setorigbuttons"></a>CMFC툴바::세터리버튼

```cpp
void SetOrigButtons(const CObList& lstOrigButtons);
```

### <a name="parameters"></a>매개 변수

【인】 *lstOrigButtons*<br/>

### <a name="remarks"></a>설명

## <a name="cmfctoolbarsetpermament"></a><a name="setpermament"></a>CMFC툴바::세퍼마멘트

사용자가 도구 모음을 닫을 수 있는지 여부를 지정합니다.

```cpp
void SetPermament(BOOL bPermament=TRUE);
```

### <a name="parameters"></a>매개 변수

*b퍼마멘트*<br/>
【인】 사용자가 도구 모음을 닫을 수 있는지 여부를 지정하는 부울 값입니다. 이 매개 변수가 TRUE이면 사용자가 도구 모음을 닫을 수 없습니다. 그렇지 않으면 사용자가 도구 모음을 닫을 수 있습니다.

### <a name="remarks"></a>설명

기본적으로 사용자는 각 도구 모음을 닫을 수 있습니다.

[CMFCToolBar::CanBeClosed](#canbeclosed) 메서드를 호출하여 사용자가 도구 모음을 닫을 수 있는지 여부를 확인합니다.

## <a name="cmfctoolbarsetroutecommandsviaframe"></a><a name="setroutecommandsviaframe"></a>CMFC툴바::셋루트커맨드비아프레임

상위 프레임 또는 소유자가 도구 모음에 명령을 보낼지 여부를 지정합니다.

```cpp
void SetRouteCommandsViaFrame(BOOL bValue);
```

### <a name="parameters"></a>매개 변수

*b값*<br/>
【인】 이 매개변수가 TRUE이면 부모 프레임이 도구 모음에 명령을 보냅니다. 그렇지 않으면 소유자가 도구 모음에 명령을 보냅니다.

### <a name="remarks"></a>설명

기본적으로 상위 프레임은 도구 모음에 명령을 보냅니다. [CMFCToolBar::GetRouteCommandsViaFrame](#getroutecommandsviaframe) 메서드를 호출하여 상위 프레임 또는 소유자가 도구 모음에 명령을 보내는지 여부를 확인합니다.

## <a name="cmfctoolbarsetshowtooltips"></a><a name="setshowtooltips"></a>CMFC툴바::세트쇼툴팁

프레임워크에 도구 설명이 표시되는지 여부를 지정합니다.

```
static void SetShowTooltips(BOOL bValue);
```

### <a name="parameters"></a>매개 변수

*b값*<br/>
【인】 이 매개 변수가 TRUE이면 프레임워크에 도구 설명이 표시됩니다. 그렇지 않으면 프레임워크에서 도구 팁을 숨깁니다.

### <a name="remarks"></a>설명

기본적으로 프레임워크에는 도구 설명이 표시됩니다.

[CMFCToolBar::GetShowTooltips](#getshowtooltips) 메서드를 호출하여 프레임워크에 도구 설명이 표시되는지 여부를 확인합니다.

## <a name="cmfctoolbarsetsiblingtoolbar"></a><a name="setsiblingtoolbar"></a>CMFC툴바::세형제도구바

도구 모음의 형제를 지정합니다.

```cpp
void SetSiblingToolBar(CMFCToolBar* pBrotherToolbar);
```

### <a name="parameters"></a>매개 변수

*p브라더툴바*<br/>
【인】 형제 도구 모음에 대한 포인터입니다.

### <a name="remarks"></a>설명

이 방법을 사용하면 사용자가 **사용자 지정** 팝업 메뉴를 표시할 때 표시되는 두 행 단추의 **한 행에 단추 표시** 또는 단추 **표시를** 사용할 수 있습니다. 사용자가 관련 도구 모음이 동일한 행에 나타나는지 또는 다른 행에 나타나는지 여부를 지정할 수 있도록 하려면 이 메서드를 호출합니다.

도구 모음에 나타나는 **사용자 지정** 단추를 사용하도록 설정한 후 이 메서드를 호출합니다. **사용자 지정** 단추를 사용하려면 [CMFCToolBar::CustomizeButton](#enablecustomizebutton) 메서드를 호출합니다.

도구 모음의 형제를 검색하려면 [CMFCToolBar::GetSiblingToolBar](#getsiblingtoolbar)를 호출합니다.

## <a name="cmfctoolbarsetsizes"></a><a name="setsizes"></a>CMFC툴바::세트사이즈

모든 도구 모음에서 단추 및 이미지의 크기를 지정합니다.

```
static void __stdcall SetSizes(
    SIZE sizeButton,
    SIZE sizeImage);
```

### <a name="parameters"></a>매개 변수

*크기 버튼*<br/>
【인】 도구 모음 단추의 크기(픽셀)입니다.

*크기이미지*<br/>
【인】 도구 모음 단추 이미지의 크기(픽셀)입니다.

### <a name="remarks"></a>설명

도구 모음 단추의 기본 크기는 23x22픽셀입니다. 도구 모음 단추 이미지의 기본 크기는 16x15 픽셀입니다.

[CMFCToolBar::GetImageSize](#getimagesize) 메서드를 호출하여 도구 모음 단추 이미지의 크기를 검색합니다. [CMFCToolBar::GetButtonSize](#getbuttonsize) 메서드를 호출하여 도구 모음 단추의 크기를 검색합니다.

## <a name="cmfctoolbarsettoolbarbtntext"></a><a name="settoolbarbtntext"></a>CMFC툴바::셋툴바브엔텍스트

도구 모음에 있는 단추의 속성을 지정합니다.

```cpp
void SetToolBarBtnText(
    UINT nBtnIndex,
    LPCTSTR szText=NULL,
    BOOL bShowText=TRUE,
    BOOL bShowImage=TRUE);
```

### <a name="parameters"></a>매개 변수

*nBtn인덱스*<br/>
【인】 도구 모음 단추 목록의 도구 모음 단추의 0기반 인덱스입니다.

*szText*<br/>
【인】 도구 모음 단추의 텍스트 레이블을 지정합니다.

*b쇼텍스트*<br/>
【인】 이 매개변수가 TRUE이면 프레임워크에 텍스트 레이블이 표시됩니다. 그렇지 않으면 프레임워크에서 텍스트 레이블을 숨깁니다.

*b쇼이미지*<br/>
【인】 이 매개 변수가 TRUE이면 프레임워크에 도구 모음 단추 이미지가 표시됩니다. 그렇지 않으면 프레임워크는 도구 모음 단추 이미지를 숨깁니다.

### <a name="remarks"></a>설명

기본적으로 프레임워크는 도구 모음 단추의 이미지를 보여 주지만 도구 모음 단추의 텍스트 레이블은 표시되지 않습니다.

디버그 빌드에서 *nBtnIndex가* 유효한 도구 모음 단추를 참조하지 않거나 도구 모음 단추가 구분기호인 경우 이 메서드는 어설션 오류를 생성합니다.

## <a name="cmfctoolbarsettworowswithsibling"></a><a name="settworowswithsibling"></a>CMFC툴바::셋투로우와 형제

도구 모음과 해당 형제를 별도의 행에 배치합니다.

```cpp
void SetTwoRowsWithSibling();
```

### <a name="remarks"></a>설명

프레임워크는 사용자가 두 행의 단추 표시 단추를 클릭할 때 이 **메서드를 호출합니다.**

[CMFCToolBar::SetSiblingToolBar](#setsiblingtoolbar) 메서드를 호출하여 **한 행의 단추 표시** 또는 두 행 **단추의 단추 표시를** 활성화합니다. 이 도구 모음에 대해 [CMFCToolBar::SetSiblingToolBar를](#setsiblingtoolbar) 호출하면 형제 도구 모음이 별도의 행으로 이동됩니다. 그렇지 않으면 이 도구 모음이 별도의 행으로 이동됩니다.

프레임워크는 사용자가 한 행의 단추 표시 단추를 클릭할 때 [CMFCToolBar::SetOneRowWithSibling](#setonerowwithsibling) **메서드를 호출합니다.**

## <a name="cmfctoolbarsetuserimages"></a><a name="setuserimages"></a>CMFC툴바::셋유저 이미지

응용 프로그램에서 사용자 정의 이미지의 컬렉션을 설정합니다.

```
static BOOL SetUserImages(CMFCToolBarImages* pUserImages);
```

### <a name="parameters"></a>매개 변수

*pUser 이미지*<br/>
【인】 사용자 정의 이미지 컬렉션에 대한 포인터입니다.

### <a name="return-value"></a>Return Value

메서드가 성공하면 0이 아닙니다. 그렇지 않으면 지정된 `CMFCToolBarImages` 개체가 유효하지 않거나 도구 모음의 기본 이미지 크기와 다른 이미지 크기가 있는 경우 0입니다.

### <a name="remarks"></a>설명

프레임워크는 사용자 정의 이미지를 사용하여 사용자가 사용자 지정한 도구 모음 단추를 그립니다. *pUserImages에서* 지정한 이미지 목록은 응용 프로그램의 모든 도구 모음 간에 공유됩니다.

이 메서드는 지정된 `CMFCToolBarImages` 개체가 유효하지 않거나 도구 모음의 기본 이미지 크기와 다른 이미지 크기가 있는 경우 디버그 빌드에서 어설션 오류를 생성합니다.

OutlookDemo, ToolTipDemo 및 VisualStudioDemo 샘플은 이 메서드를 사용하여 사용자 정의 이미지의 전역 컬렉션을 설정합니다. 응용 프로그램의 작업 디렉토리에 있는 UserImages.bmp라는 파일을 로드합니다.

[CMFCToolBar::GetUserImages](#getuserimages) 메서드를 호출하여 응용 프로그램에서 사용자 정의 이미지 컬렉션을 검색합니다.

## <a name="cmfctoolbarstretchpane"></a><a name="stretchpane"></a>CMFC툴바::스트레치파인

도구 모음을 세로 또는 수평으로 늘이고 필요한 경우 단추의 위치를 조정합니다.

```
virtual CSize StretchPane(
    int nLength,
    BOOL bVert);
```

### <a name="parameters"></a>매개 변수

*nLength*<br/>
【인】 창을 늘릴 픽셀 단위로 양입니다.

*bVert*<br/>
【인】 TRUE이면 창을 세로로 늘입니다. FALSE인 경우 창을 가로로 늘입니다.

### <a name="return-value"></a>Return Value

도구 `CSize` 모음 클라이언트 영역의 크기를 지정하는 개체입니다.

### <a name="remarks"></a>설명

이 메서드는 [CMFCToolBar::WrapToolBar를](#wraptoolbar) 호출하여 확장된 도구 모음 내의 단추를 재배치합니다.

반환 값은 [CMFCToolBar::CalcSize를](#calcsize)호출하여 결정됩니다.

## <a name="cmfctoolbartranslatechar"></a><a name="translatechar"></a>CMFC툴바:::번역차

지정된 키 코드가 유효한 키보드 단축키에 해당하는 경우 단추 명령을 실행합니다.

```
virtual BOOL TranslateChar(UINT nChar);
```

### <a name="parameters"></a>매개 변수

*Nchar*<br/>
【인】 가상 키 코드를 지정합니다. 표준 가상 키 코드 목록은 Winuser.h를 참조하십시오.

### <a name="return-value"></a>Return Value

지정된 키 코드를 인쇄할 수 없거나 유효한 키보드 단축키와 일치하지 않는 경우 FALSE입니다. TRUE 지정된 키 코드가 드롭다운 메뉴 옵션에 해당하는 경우 그렇지 않으면 [CMFCToolBar::ProcessCommand에서](#processcommand)반환 값입니다.

### <a name="remarks"></a>설명

프레임워크는 Alt 키와 함께 키를 누를 때 이 메서드를 호출합니다.

## <a name="cmfctoolbarupdatebutton"></a><a name="updatebutton"></a>CMFC툴바::업데이트 버튼

지정된 단추의 상태를 업데이트합니다.

```cpp
void UpdateButton(int nIndex);
```

### <a name="parameters"></a>매개 변수

*nIndex*<br/>
【인】 업데이트할 단추의 0기반 인덱스를 지정합니다.

### <a name="remarks"></a>설명

## <a name="cmfctoolbarwraptoolbar"></a><a name="wraptoolbar"></a>CMFC툴바::랩툴바

지정된 치수 내에서 도구 모음 단추의 위치를 지정합니다.

```
int WrapToolBar(
    int nWidth,
    int nHeight = 32767,
    CDC* pDC = NULL,
    int nColumnWidth = -1,
    int nRowHeight = -1);
```

### <a name="parameters"></a>매개 변수

*nWidth*<br/>
【인】 도구 모음의 최대 너비입니다.

*nHeight*<br/>
【인】 도구 모음의 최대 높이입니다. 도구 모음이 부동 인 경우 사용되지 않습니다.

*pDC*<br/>
【인】 장치 컨텍스트에 대한 포인터입니다. NULL이면 도구 모음에 대한 장치 컨텍스트가 사용됩니다.

*n열 너비*<br/>
【인】 단추 너비입니다. -1이면 현재 너비가 사용됩니다.

[in] m *nRowHeight* 버튼 높이입니다. -1이면 현재 높이가 사용됩니다.

### <a name="return-value"></a>Return Value

도구 모음의 단추 행 수입니다.

### <a name="remarks"></a>설명

이 메서드는 도구 모음 내에서 단추를 재배치하고 필요한 경우 단추를 추가 행으로 래핑합니다.

## <a name="cmfctoolbarm_bdontscaleimages"></a><a name="m_bdontscaleimages"></a>CMFC툴바::m_bDontScaleImages

높은 DPI 모드에서 도구 모음 이미지의 배율을 조정할지 여부를 지정합니다.

```
AFX_IMPORT_DATA static BOOL m_bDontScaleImages;
```

### <a name="remarks"></a>설명

## <a name="see-also"></a>참조

[계층 구조 차트](../../mfc/hierarchy-chart.md)<br/>
[클래스](../../mfc/reference/mfc-classes.md)<br/>
[CMFC메뉴바 클래스](../../mfc/reference/cmfcmenubar-class.md)<br/>
[CMFC팝메뉴바 클래스](../../mfc/reference/cmfcpopupmenubar-class.md)<br/>
[CMFC드롭다운툴바 클래스](../../mfc/reference/cmfcdropdowntoolbar-class.md)<br/>
[연습: 도구 모음에 컨트롤 배치](../../mfc/walkthrough-putting-controls-on-toolbars.md)
