---
title: CWnd 클래스
ms.date: 11/19/2018
f1_keywords:
- CWnd
- AFXWIN/CWnd
- AFXWIN/CWnd::CWnd
- AFXWIN/CWnd::accDoDefaultAction
- AFXWIN/CWnd::accHitTest
- AFXWIN/CWnd::accLocation
- AFXWIN/CWnd::accNavigate
- AFXWIN/CWnd::accSelect
- AFXWIN/CWnd::AnimateWindow
- AFXWIN/CWnd::ArrangeIconicWindows
- AFXWIN/CWnd::Attach
- AFXWIN/CWnd::BeginModalState
- AFXWIN/CWnd::BeginPaint
- AFXWIN/CWnd::BindDefaultProperty
- AFXWIN/CWnd::BindProperty
- AFXWIN/CWnd::BringWindowToTop
- AFXWIN/CWnd::CalcWindowRect
- AFXWIN/CWnd::CancelToolTips
- AFXWIN/CWnd::CenterWindow
- AFXWIN/CWnd::ChangeClipboardChain
- AFXWIN/CWnd::CheckDlgButton
- AFXWIN/CWnd::CheckRadioButton
- AFXWIN/CWnd::ChildWindowFromPoint
- AFXWIN/CWnd::ClientToScreen
- AFXWIN/CWnd::CloseWindow
- AFXWIN/CWnd::ContinueModal
- AFXWIN/CWnd::Create
- AFXWIN/CWnd::CreateAccessibleProxy
- AFXWIN/CWnd::CreateCaret
- AFXWIN/CWnd::CreateControl
- AFXWIN/CWnd::CreateEx
- AFXWIN/CWnd::CreateGrayCaret
- AFXWIN/CWnd::CreateSolidCaret
- AFXWIN/CWnd::DeleteTempMap
- AFXWIN/CWnd::DestroyWindow
- AFXWIN/CWnd::Detach
- AFXWIN/CWnd::DlgDirList
- AFXWIN/CWnd::DlgDirListComboBox
- AFXWIN/CWnd::DlgDirSelect
- AFXWIN/CWnd::DlgDirSelectComboBox
- AFXWIN/CWnd::DragAcceptFiles
- AFXWIN/CWnd::DragDetect
- AFXWIN/CWnd::DrawAnimatedRects
- AFXWIN/CWnd::DrawCaption
- AFXWIN/CWnd::DrawMenuBar
- AFXWIN/CWnd::EnableActiveAccessibility
- AFXWIN/CWnd::EnableDynamicLayout
- AFXWIN/CWnd::EnableD2DSupport
- AFXWIN/CWnd::EnableScrollBar
- AFXWIN/CWnd::EnableScrollBarCtrl
- AFXWIN/CWnd::EnableToolTips
- AFXWIN/CWnd::EnableTrackingToolTips
- AFXWIN/CWnd::EnableWindow
- AFXWIN/CWnd::EndModalLoop
- AFXWIN/CWnd::EndModalState
- AFXWIN/CWnd::EndPaint
- AFXWIN/CWnd::ExecuteDlgInit
- AFXWIN/CWnd::FilterToolTipMessage
- AFXWIN/CWnd::FindWindow
- AFXWIN/CWnd::FindWindowEx
- AFXWIN/CWnd::FlashWindow
- AFXWIN/CWnd::FlashWindowEx
- AFXWIN/CWnd::FromHandle
- AFXWIN/CWnd::FromHandlePermanent
- AFXWIN/CWnd::get_accChild
- AFXWIN/CWnd::get_accChildCount
- AFXWIN/CWnd::get_accDefaultAction
- AFXWIN/CWnd::get_accDescription
- AFXWIN/CWnd::get_accFocus
- AFXWIN/CWnd::get_accHelp
- AFXWIN/CWnd::get_accHelpTopic
- AFXWIN/CWnd::get_accKeyboardShortcut
- AFXWIN/CWnd::get_accName
- AFXWIN/CWnd::get_accParent
- AFXWIN/CWnd::get_accRole
- AFXWIN/CWnd::get_accSelection
- AFXWIN/CWnd::get_accState
- AFXWIN/CWnd::get_accValue
- AFXWIN/CWnd::GetActiveWindow
- AFXWIN/CWnd::GetAncestor
- AFXWIN/CWnd::GetCapture
- AFXWIN/CWnd::GetCaretPos
- AFXWIN/CWnd::GetCheckedRadioButton
- AFXWIN/CWnd::GetClientRect
- AFXWIN/CWnd::GetClipboardOwner
- AFXWIN/CWnd::GetClipboardViewer
- AFXWIN/CWnd::GetControlUnknown
- AFXWIN/CWnd::GetDC
- AFXWIN/CWnd::GetDCEx
- AFXWIN/CWnd::GetDCRenderTarget
- AFXWIN/CWnd::GetDescendantWindow
- AFXWIN/CWnd::GetDesktopWindow
- AFXWIN/CWnd::GetDlgCtrlID
- AFXWIN/CWnd::GetDlgItem
- AFXWIN/CWnd::GetDlgItemInt
- AFXWIN/CWnd::GetDlgItemText
- AFXWIN/CWnd::GetDSCCursor
- AFXWIN/CWnd::GetDynamicLayout
- AFXWIN/CWnd::GetExStyle
- AFXWIN/CWnd::GetFocus
- AFXWIN/CWnd::GetFont
- AFXWIN/CWnd::GetForegroundWindow
- AFXWIN/CWnd::GetIcon
- AFXWIN/CWnd::GetLastActivePopup
- AFXWIN/CWnd::GetLayeredWindowAttributes
- AFXWIN/CWnd::GetMenu
- AFXWIN/CWnd::GetNextDlgGroupItem
- AFXWIN/CWnd::GetNextDlgTabItem
- AFXWIN/CWnd::GetNextWindow
- AFXWIN/CWnd::GetOleControlSite
- AFXWIN/CWnd::GetOpenClipboardWindow
- AFXWIN/CWnd::GetOwner
- AFXWIN/CWnd::GetParent
- AFXWIN/CWnd::GetParentFrame
- AFXWIN/CWnd::GetParentOwner
- AFXWIN/CWnd::GetProperty
- AFXWIN/CWnd::GetRenderTarget
- AFXWIN/CWnd::GetSafeHwnd
- AFXWIN/CWnd::GetSafeOwner
- AFXWIN/CWnd::GetScrollBarCtrl
- AFXWIN/CWnd::GetScrollBarInfo
- AFXWIN/CWnd::GetScrollInfo
- AFXWIN/CWnd::GetScrollLimit
- AFXWIN/CWnd::GetScrollPos
- AFXWIN/CWnd::GetScrollRange
- AFXWIN/CWnd::GetStyle
- AFXWIN/CWnd::GetSystemMenu
- AFXWIN/CWnd::GetTitleBarInfo
- AFXWIN/CWnd::GetTopLevelFrame
- AFXWIN/CWnd::GetTopLevelOwner
- AFXWIN/CWnd::GetTopLevelParent
- AFXWIN/CWnd::GetTopWindow
- AFXWIN/CWnd::GetUpdateRect
- AFXWIN/CWnd::GetUpdateRgn
- AFXWIN/CWnd::GetWindow
- AFXWIN/CWnd::GetWindowContextHelpId
- AFXWIN/CWnd::GetWindowDC
- AFXWIN/CWnd::GetWindowedChildCount
- AFXWIN/CWnd::GetWindowInfo
- AFXWIN/CWnd::GetWindowlessChildCount
- AFXWIN/CWnd::GetWindowPlacement
- AFXWIN/CWnd::GetWindowRect
- AFXWIN/CWnd::GetWindowRgn
- AFXWIN/CWnd::GetWindowText
- AFXWIN/CWnd::GetWindowTextLength
- AFXWIN/CWnd::HideCaret
- AFXWIN/CWnd::HiliteMenuItem
- AFXWIN/CWnd::HtmlHelp
- AFXWIN/CWnd::Invalidate
- AFXWIN/CWnd::InvalidateRect
- AFXWIN/CWnd::InvalidateRgn
- AFXWIN/CWnd::InvokeHelper
- AFXWIN/CWnd::IsChild
- AFXWIN/CWnd::IsD2DSupportEnabled
- AFXWIN/CWnd::IsDialogMessage
- AFXWIN/CWnd::IsDlgButtonChecked
- AFXWIN/CWnd::IsDynamicLayoutEnabled
- AFXWIN/CWnd::IsIconic
- AFXWIN/CWnd::IsTouchWindow
- AFXWIN/CWnd::IsWindowEnabled
- AFXWIN/CWnd::IsWindowVisible
- AFXWIN/CWnd::IsZoomed
- AFXWIN/CWnd::KillTimer
- AFXWIN/CWnd::LockWindowUpdate
- AFXWIN/CWnd::MapWindowPoints
- AFXWIN/CWnd::MessageBox
- AFXWIN/CWnd::ModifyStyle
- AFXWIN/CWnd::ModifyStyleEx
- AFXWIN/CWnd::MoveWindow
- AFXWIN/CWnd::NotifyWinEvent
- AFXWIN/CWnd::OnAmbientProperty
- AFXWIN/CWnd::OnDrawIconicThumbnailOrLivePreview
- AFXWIN/CWnd::OnHelp
- AFXWIN/CWnd::OnHelpFinder
- AFXWIN/CWnd::OnHelpIndex
- AFXWIN/CWnd::OnHelpUsing
- AFXWIN/CWnd::OnToolHitTest
- AFXWIN/CWnd::OpenClipboard
- AFXWIN/CWnd::PaintWindowlessControls
- AFXWIN/CWnd::PostMessage
- AFXWIN/CWnd::PreCreateWindow
- AFXWIN/CWnd::PreSubclassWindow
- AFXWIN/CWnd::PreTranslateMessage
- AFXWIN/CWnd::Print
- AFXWIN/CWnd::PrintClient
- AFXWIN/CWnd::PrintWindow
- AFXWIN/CWnd::RedrawWindow
- AFXWIN/CWnd::RegisterTouchWindow
- AFXWIN/CWnd::ReleaseDC
- AFXWIN/CWnd::RepositionBars
- AFXWIN/CWnd::RunModalLoop
- AFXWIN/CWnd::ScreenToClient
- AFXWIN/CWnd::ScrollWindow
- AFXWIN/CWnd::ScrollWindowEx
- AFXWIN/CWnd::SendChildNotifyLastMsg
- AFXWIN/CWnd::SendDlgItemMessage
- AFXWIN/CWnd::SendMessage
- AFXWIN/CWnd::SendMessageToDescendants
- AFXWIN/CWnd::SendNotifyMessage
- AFXWIN/CWnd::SetActiveWindow
- AFXWIN/CWnd::SetCapture
- AFXWIN/CWnd::SetCaretPos
- AFXWIN/CWnd::SetClipboardViewer
- AFXWIN/CWnd::SetDlgCtrlID
- AFXWIN/CWnd::SetDlgItemInt
- AFXWIN/CWnd::SetDlgItemText
- AFXWIN/CWnd::SetFocus
- AFXWIN/CWnd::SetFont
- AFXWIN/CWnd::SetForegroundWindow
- AFXWIN/CWnd::SetIcon
- AFXWIN/CWnd::SetLayeredWindowAttributes
- AFXWIN/CWnd::SetMenu
- AFXWIN/CWnd::SetOwner
- AFXWIN/CWnd::SetParent
- AFXWIN/CWnd::SetProperty
- AFXWIN/CWnd::SetRedraw
- AFXWIN/CWnd::SetScrollInfo
- AFXWIN/CWnd::SetScrollPos
- AFXWIN/CWnd::SetScrollRange
- AFXWIN/CWnd::SetTimer
- AFXWIN/CWnd::SetWindowContextHelpId
- AFXWIN/CWnd::SetWindowPlacement
- AFXWIN/CWnd::SetWindowPos
- AFXWIN/CWnd::SetWindowRgn
- AFXWIN/CWnd::SetWindowText
- AFXWIN/CWnd::ShowCaret
- AFXWIN/CWnd::ShowOwnedPopups
- AFXWIN/CWnd::ShowScrollBar
- AFXWIN/CWnd::ShowWindow
- AFXWIN/CWnd::SubclassDlgItem
- AFXWIN/CWnd::SubclassWindow
- AFXWIN/CWnd::UnlockWindowUpdate
- AFXWIN/CWnd::UnsubclassWindow
- AFXWIN/CWnd::UpdateData
- AFXWIN/CWnd::UpdateDialogControls
- AFXWIN/CWnd::UpdateLayeredWindow
- AFXWIN/CWnd::UpdateWindow
- AFXWIN/CWnd::ValidateRect
- AFXWIN/CWnd::ValidateRgn
- AFXWIN/CWnd::WindowFromPoint
- AFXWIN/CWnd::WinHelp
- AFXWIN/CWnd::Default
- AFXWIN/CWnd::DefWindowProc
- AFXWIN/CWnd::DoDataExchange
- AFXWIN/CWnd::GetCurrentMessage
- AFXWIN/CWnd::InitDynamicLayout
- AFXWIN/CWnd::LoadDynamicLayoutResource
- AFXWIN/CWnd::OnActivate
- AFXWIN/CWnd::OnActivateApp
- AFXWIN/CWnd::OnAppCommand
- AFXWIN/CWnd::OnAskCbFormatName
- AFXWIN/CWnd::OnCancelMode
- AFXWIN/CWnd::OnCaptureChanged
- AFXWIN/CWnd::OnChangeCbChain
- AFXWIN/CWnd::OnChangeUIState
- AFXWIN/CWnd::OnChar
- AFXWIN/CWnd::OnCharToItem
- AFXWIN/CWnd::OnChildActivate
- AFXWIN/CWnd::OnChildNotify
- AFXWIN/CWnd::OnClipboardUpdate
- AFXWIN/CWnd::OnClose
- AFXWIN/CWnd::OnColorizationColorChanged
- AFXWIN/CWnd::OnCommand
- AFXWIN/CWnd::OnCompacting
- AFXWIN/CWnd::OnCompareItem
- AFXWIN/CWnd::OnCompositionChanged
- AFXWIN/CWnd::OnContextMenu
- AFXWIN/CWnd::OnCopyData
- AFXWIN/CWnd::OnCreate
- AFXWIN/CWnd::OnCtlColor
- AFXWIN/CWnd::OnDeadChar
- AFXWIN/CWnd::OnDeleteItem
- AFXWIN/CWnd::OnDestroy
- AFXWIN/CWnd::OnDestroyClipboard
- AFXWIN/CWnd::OnDeviceChange
- AFXWIN/CWnd::OnDevModeChange
- AFXWIN/CWnd::OnDrawClipboard
- AFXWIN/CWnd::OnDrawItem
- AFXWIN/CWnd::OnDropFiles
- AFXWIN/CWnd::OnEnable
- AFXWIN/CWnd::OnEndSession
- AFXWIN/CWnd::OnEnterIdle
- AFXWIN/CWnd::OnEnterMenuLoop
- AFXWIN/CWnd::OnEnterSizeMove
- AFXWIN/CWnd::OnEraseBkgnd
- AFXWIN/CWnd::OnExitMenuLoop
- AFXWIN/CWnd::OnExitSizeMove
- AFXWIN/CWnd::OnFontChange
- AFXWIN/CWnd::OnGetDlgCode
- AFXWIN/CWnd::OnGetMinMaxInfo
- AFXWIN/CWnd::OnHelpInfo
- AFXWIN/CWnd::OnHotKey
- AFXWIN/CWnd::OnHScroll
- AFXWIN/CWnd::OnHScrollClipboard
- AFXWIN/CWnd::OnIconEraseBkgnd
- AFXWIN/CWnd::OnInitMenu
- AFXWIN/CWnd::OnInitMenuPopup
- AFXWIN/CWnd::OnInputDeviceChange
- AFXWIN/CWnd::OnInputLangChange
- AFXWIN/CWnd::OnInputLangChangeRequest
- AFXWIN/CWnd::OnKeyDown
- AFXWIN/CWnd::OnKeyUp
- AFXWIN/CWnd::OnKillFocus
- AFXWIN/CWnd::OnLButtonDblClk
- AFXWIN/CWnd::OnLButtonDown
- AFXWIN/CWnd::OnLButtonUp
- AFXWIN/CWnd::OnMButtonDblClk
- AFXWIN/CWnd::OnMButtonDown
- AFXWIN/CWnd::OnMButtonUp
- AFXWIN/CWnd::OnMDIActivate
- AFXWIN/CWnd::OnMeasureItem
- AFXWIN/CWnd::OnMenuChar
- AFXWIN/CWnd::OnMenuDrag
- AFXWIN/CWnd::OnMenuGetObject
- AFXWIN/CWnd::OnMenuRButtonUp
- AFXWIN/CWnd::OnMenuSelect
- AFXWIN/CWnd::OnMouseActivate
- AFXWIN/CWnd::OnMouseHover
- AFXWIN/CWnd::OnMouseHWheel
- AFXWIN/CWnd::OnMouseLeave
- AFXWIN/CWnd::OnMouseMove
- AFXWIN/CWnd::OnMouseWheel
- AFXWIN/CWnd::OnMove
- AFXWIN/CWnd::OnMoving
- AFXWIN/CWnd::OnNcActivate
- AFXWIN/CWnd::OnNcCalcSize
- AFXWIN/CWnd::OnNcCreate
- AFXWIN/CWnd::OnNcDestroy
- AFXWIN/CWnd::OnNcHitTest
- AFXWIN/CWnd::OnNcLButtonDblClk
- AFXWIN/CWnd::OnNcLButtonDown
- AFXWIN/CWnd::OnNcLButtonUp
- AFXWIN/CWnd::OnNcMButtonDblClk
- AFXWIN/CWnd::OnNcMButtonDown
- AFXWIN/CWnd::OnNcMButtonUp
- AFXWIN/CWnd::OnNcMouseHover
- AFXWIN/CWnd::OnNcMouseLeave
- AFXWIN/CWnd::OnNcMouseMove
- AFXWIN/CWnd::OnNcPaint
- AFXWIN/CWnd::OnNcRButtonDblClk
- AFXWIN/CWnd::OnNcRButtonDown
- AFXWIN/CWnd::OnNcRButtonUp
- AFXWIN/CWnd::OnNcRenderingChanged
- AFXWIN/CWnd::OnNcXButtonDblClk
- AFXWIN/CWnd::OnNcXButtonDown
- AFXWIN/CWnd::OnNcXButtonUp
- AFXWIN/CWnd::OnNextMenu
- AFXWIN/CWnd::OnNotify
- AFXWIN/CWnd::OnNotifyFormat
- AFXWIN/CWnd::OnPaint
- AFXWIN/CWnd::OnPaintClipboard
- AFXWIN/CWnd::OnPaletteChanged
- AFXWIN/CWnd::OnPaletteIsChanging
- AFXWIN/CWnd::OnParentNotify
- AFXWIN/CWnd::OnPowerBroadcast
- AFXWIN/CWnd::OnQueryDragIcon
- AFXWIN/CWnd::OnQueryEndSession
- AFXWIN/CWnd::OnQueryNewPalette
- AFXWIN/CWnd::OnQueryOpen
- AFXWIN/CWnd::OnQueryUIState
- AFXWIN/CWnd::OnRawInput
- AFXWIN/CWnd::OnRButtonDblClk
- AFXWIN/CWnd::OnRButtonDown
- AFXWIN/CWnd::OnRButtonUp
- AFXWIN/CWnd::OnRenderAllFormats
- AFXWIN/CWnd::OnRenderFormat
- AFXWIN/CWnd::OnSessionChange
- AFXWIN/CWnd::OnSetCursor
- AFXWIN/CWnd::OnSetFocus
- AFXWIN/CWnd::OnSettingChange
- AFXWIN/CWnd::OnShowWindow
- AFXWIN/CWnd::OnSize
- AFXWIN/CWnd::OnSizeClipboard
- AFXWIN/CWnd::OnSizing
- AFXWIN/CWnd::OnSpoolerStatus
- AFXWIN/CWnd::OnStyleChanged
- AFXWIN/CWnd::OnStyleChanging
- AFXWIN/CWnd::OnSysChar
- AFXWIN/CWnd::OnSysColorChange
- AFXWIN/CWnd::OnSysCommand
- AFXWIN/CWnd::OnSysDeadChar
- AFXWIN/CWnd::OnSysKeyDown
- AFXWIN/CWnd::OnSysKeyUp
- AFXWIN/CWnd::OnTCard
- AFXWIN/CWnd::OnTimeChange
- AFXWIN/CWnd::OnTimer
- AFXWIN/CWnd::OnTouchInput
- AFXWIN/CWnd::OnTouchInputs
- AFXWIN/CWnd::OnUniChar
- AFXWIN/CWnd::OnUnInitMenuPopup
- AFXWIN/CWnd::OnUpdateUIState
- AFXWIN/CWnd::OnUserChanged
- AFXWIN/CWnd::OnVKeyToItem
- AFXWIN/CWnd::OnVScroll
- AFXWIN/CWnd::OnVScrollClipboard
- AFXWIN/CWnd::OnWindowPosChanged
- AFXWIN/CWnd::OnWindowPosChanging
- AFXWIN/CWnd::OnWinIniChange
- AFXWIN/CWnd::OnWndMsg
- AFXWIN/CWnd::OnXButtonDblClk
- AFXWIN/CWnd::OnXButtonDown
- AFXWIN/CWnd::OnXButtonUp
- AFXWIN/CWnd::PostNcDestroy
- AFXWIN/CWnd::ReflectChildNotify
- AFXWIN/CWnd::ReflectLastMsg
- AFXWIN/CWnd::ResizeDynamicLayout
- AFXWIN/CWnd::WindowProc
- AFXWIN/CWnd::m_hWnd
helpviewer_keywords:
- CWnd [MFC], CWnd
- CWnd [MFC], accDoDefaultAction
- CWnd [MFC], accHitTest
- CWnd [MFC], accLocation
- CWnd [MFC], accNavigate
- CWnd [MFC], accSelect
- CWnd [MFC], AnimateWindow
- CWnd [MFC], ArrangeIconicWindows
- CWnd [MFC], Attach
- CWnd [MFC], BeginModalState
- CWnd [MFC], BeginPaint
- CWnd [MFC], BindDefaultProperty
- CWnd [MFC], BindProperty
- CWnd [MFC], BringWindowToTop
- CWnd [MFC], CalcWindowRect
- CWnd [MFC], CancelToolTips
- CWnd [MFC], CenterWindow
- CWnd [MFC], ChangeClipboardChain
- CWnd [MFC], CheckDlgButton
- CWnd [MFC], CheckRadioButton
- CWnd [MFC], ChildWindowFromPoint
- CWnd [MFC], ClientToScreen
- CWnd [MFC], CloseWindow
- CWnd [MFC], ContinueModal
- CWnd [MFC], Create
- CWnd [MFC], CreateAccessibleProxy
- CWnd [MFC], CreateCaret
- CWnd [MFC], CreateControl
- CWnd [MFC], CreateEx
- CWnd [MFC], CreateGrayCaret
- CWnd [MFC], CreateSolidCaret
- CWnd [MFC], DeleteTempMap
- CWnd [MFC], DestroyWindow
- CWnd [MFC], Detach
- CWnd [MFC], DlgDirList
- CWnd [MFC], DlgDirListComboBox
- CWnd [MFC], DlgDirSelect
- CWnd [MFC], DlgDirSelectComboBox
- CWnd [MFC], DragAcceptFiles
- CWnd [MFC], DragDetect
- CWnd [MFC], DrawAnimatedRects
- CWnd [MFC], DrawCaption
- CWnd [MFC], DrawMenuBar
- CWnd [MFC], EnableActiveAccessibility
- CWnd [MFC], EnableDynamicLayout
- CWnd [MFC], EnableD2DSupport
- CWnd [MFC], EnableScrollBar
- CWnd [MFC], EnableScrollBarCtrl
- CWnd [MFC], EnableToolTips
- CWnd [MFC], EnableTrackingToolTips
- CWnd [MFC], EnableWindow
- CWnd [MFC], EndModalLoop
- CWnd [MFC], EndModalState
- CWnd [MFC], EndPaint
- CWnd [MFC], ExecuteDlgInit
- CWnd [MFC], FilterToolTipMessage
- CWnd [MFC], FindWindow
- CWnd [MFC], FindWindowEx
- CWnd [MFC], FlashWindow
- CWnd [MFC], FlashWindowEx
- CWnd [MFC], FromHandle
- CWnd [MFC], FromHandlePermanent
- CWnd [MFC], get_accChild
- CWnd [MFC], get_accChildCount
- CWnd [MFC], get_accDefaultAction
- CWnd [MFC], get_accDescription
- CWnd [MFC], get_accFocus
- CWnd [MFC], get_accHelp
- CWnd [MFC], get_accHelpTopic
- CWnd [MFC], get_accKeyboardShortcut
- CWnd [MFC], get_accName
- CWnd [MFC], get_accParent
- CWnd [MFC], get_accRole
- CWnd [MFC], get_accSelection
- CWnd [MFC], get_accState
- CWnd [MFC], get_accValue
- CWnd [MFC], GetActiveWindow
- CWnd [MFC], GetAncestor
- CWnd [MFC], GetCapture
- CWnd [MFC], GetCaretPos
- CWnd [MFC], GetCheckedRadioButton
- CWnd [MFC], GetClientRect
- CWnd [MFC], GetClipboardOwner
- CWnd [MFC], GetClipboardViewer
- CWnd [MFC], GetControlUnknown
- CWnd [MFC], GetDC
- CWnd [MFC], GetDCEx
- CWnd [MFC], GetDCRenderTarget
- CWnd [MFC], GetDescendantWindow
- CWnd [MFC], GetDesktopWindow
- CWnd [MFC], GetDlgCtrlID
- CWnd [MFC], GetDlgItem
- CWnd [MFC], GetDlgItemInt
- CWnd [MFC], GetDlgItemText
- CWnd [MFC], GetDSCCursor
- CWnd [MFC], GetDynamicLayout
- CWnd [MFC], GetExStyle
- CWnd [MFC], GetFocus
- CWnd [MFC], GetFont
- CWnd [MFC], GetForegroundWindow
- CWnd [MFC], GetIcon
- CWnd [MFC], GetLastActivePopup
- CWnd [MFC], GetLayeredWindowAttributes
- CWnd [MFC], GetMenu
- CWnd [MFC], GetNextDlgGroupItem
- CWnd [MFC], GetNextDlgTabItem
- CWnd [MFC], GetNextWindow
- CWnd [MFC], GetOleControlSite
- CWnd [MFC], GetOpenClipboardWindow
- CWnd [MFC], GetOwner
- CWnd [MFC], GetParent
- CWnd [MFC], GetParentFrame
- CWnd [MFC], GetParentOwner
- CWnd [MFC], GetProperty
- CWnd [MFC], GetRenderTarget
- CWnd [MFC], GetSafeHwnd
- CWnd [MFC], GetSafeOwner
- CWnd [MFC], GetScrollBarCtrl
- CWnd [MFC], GetScrollBarInfo
- CWnd [MFC], GetScrollInfo
- CWnd [MFC], GetScrollLimit
- CWnd [MFC], GetScrollPos
- CWnd [MFC], GetScrollRange
- CWnd [MFC], GetStyle
- CWnd [MFC], GetSystemMenu
- CWnd [MFC], GetTitleBarInfo
- CWnd [MFC], GetTopLevelFrame
- CWnd [MFC], GetTopLevelOwner
- CWnd [MFC], GetTopLevelParent
- CWnd [MFC], GetTopWindow
- CWnd [MFC], GetUpdateRect
- CWnd [MFC], GetUpdateRgn
- CWnd [MFC], GetWindow
- CWnd [MFC], GetWindowContextHelpId
- CWnd [MFC], GetWindowDC
- CWnd [MFC], GetWindowedChildCount
- CWnd [MFC], GetWindowInfo
- CWnd [MFC], GetWindowlessChildCount
- CWnd [MFC], GetWindowPlacement
- CWnd [MFC], GetWindowRect
- CWnd [MFC], GetWindowRgn
- CWnd [MFC], GetWindowText
- CWnd [MFC], GetWindowTextLength
- CWnd [MFC], HideCaret
- CWnd [MFC], HiliteMenuItem
- CWnd [MFC], HtmlHelp
- CWnd [MFC], Invalidate
- CWnd [MFC], InvalidateRect
- CWnd [MFC], InvalidateRgn
- CWnd [MFC], InvokeHelper
- CWnd [MFC], IsChild
- CWnd [MFC], IsD2DSupportEnabled
- CWnd [MFC], IsDialogMessage
- CWnd [MFC], IsDlgButtonChecked
- CWnd [MFC], IsDynamicLayoutEnabled
- CWnd [MFC], IsIconic
- CWnd [MFC], IsTouchWindow
- CWnd [MFC], IsWindowEnabled
- CWnd [MFC], IsWindowVisible
- CWnd [MFC], IsZoomed
- CWnd [MFC], KillTimer
- CWnd [MFC], LockWindowUpdate
- CWnd [MFC], MapWindowPoints
- CWnd [MFC], MessageBox
- CWnd [MFC], ModifyStyle
- CWnd [MFC], ModifyStyleEx
- CWnd [MFC], MoveWindow
- CWnd [MFC], NotifyWinEvent
- CWnd [MFC], OnAmbientProperty
- CWnd [MFC], OnDrawIconicThumbnailOrLivePreview
- CWnd [MFC], OnHelp
- CWnd [MFC], OnHelpFinder
- CWnd [MFC], OnHelpIndex
- CWnd [MFC], OnHelpUsing
- CWnd [MFC], OnToolHitTest
- CWnd [MFC], OpenClipboard
- CWnd [MFC], PaintWindowlessControls
- CWnd [MFC], PostMessage
- CWnd [MFC], PreCreateWindow
- CWnd [MFC], PreSubclassWindow
- CWnd [MFC], PreTranslateMessage
- CWnd [MFC], Print
- CWnd [MFC], PrintClient
- CWnd [MFC], PrintWindow
- CWnd [MFC], RedrawWindow
- CWnd [MFC], RegisterTouchWindow
- CWnd [MFC], ReleaseDC
- CWnd [MFC], RepositionBars
- CWnd [MFC], RunModalLoop
- CWnd [MFC], ScreenToClient
- CWnd [MFC], ScrollWindow
- CWnd [MFC], ScrollWindowEx
- CWnd [MFC], SendChildNotifyLastMsg
- CWnd [MFC], SendDlgItemMessage
- CWnd [MFC], SendMessage
- CWnd [MFC], SendMessageToDescendants
- CWnd [MFC], SendNotifyMessage
- CWnd [MFC], SetActiveWindow
- CWnd [MFC], SetCapture
- CWnd [MFC], SetCaretPos
- CWnd [MFC], SetClipboardViewer
- CWnd [MFC], SetDlgCtrlID
- CWnd [MFC], SetDlgItemInt
- CWnd [MFC], SetDlgItemText
- CWnd [MFC], SetFocus
- CWnd [MFC], SetFont
- CWnd [MFC], SetForegroundWindow
- CWnd [MFC], SetIcon
- CWnd [MFC], SetLayeredWindowAttributes
- CWnd [MFC], SetMenu
- CWnd [MFC], SetOwner
- CWnd [MFC], SetParent
- CWnd [MFC], SetProperty
- CWnd [MFC], SetRedraw
- CWnd [MFC], SetScrollInfo
- CWnd [MFC], SetScrollPos
- CWnd [MFC], SetScrollRange
- CWnd [MFC], SetTimer
- CWnd [MFC], SetWindowContextHelpId
- CWnd [MFC], SetWindowPlacement
- CWnd [MFC], SetWindowPos
- CWnd [MFC], SetWindowRgn
- CWnd [MFC], SetWindowText
- CWnd [MFC], ShowCaret
- CWnd [MFC], ShowOwnedPopups
- CWnd [MFC], ShowScrollBar
- CWnd [MFC], ShowWindow
- CWnd [MFC], SubclassDlgItem
- CWnd [MFC], SubclassWindow
- CWnd [MFC], UnlockWindowUpdate
- CWnd [MFC], UnsubclassWindow
- CWnd [MFC], UpdateData
- CWnd [MFC], UpdateDialogControls
- CWnd [MFC], UpdateLayeredWindow
- CWnd [MFC], UpdateWindow
- CWnd [MFC], ValidateRect
- CWnd [MFC], ValidateRgn
- CWnd [MFC], WindowFromPoint
- CWnd [MFC], WinHelp
- CWnd [MFC], Default
- CWnd [MFC], DefWindowProc
- CWnd [MFC], DoDataExchange
- CWnd [MFC], GetCurrentMessage
- CWnd [MFC], InitDynamicLayout
- CWnd [MFC], LoadDynamicLayoutResource
- CWnd [MFC], OnActivate
- CWnd [MFC], OnActivateApp
- CWnd [MFC], OnAppCommand
- CWnd [MFC], OnAskCbFormatName
- CWnd [MFC], OnCancelMode
- CWnd [MFC], OnCaptureChanged
- CWnd [MFC], OnChangeCbChain
- CWnd [MFC], OnChangeUIState
- CWnd [MFC], OnChar
- CWnd [MFC], OnCharToItem
- CWnd [MFC], OnChildActivate
- CWnd [MFC], OnChildNotify
- CWnd [MFC], OnClipboardUpdate
- CWnd [MFC], OnClose
- CWnd [MFC], OnColorizationColorChanged
- CWnd [MFC], OnCommand
- CWnd [MFC], OnCompacting
- CWnd [MFC], OnCompareItem
- CWnd [MFC], OnCompositionChanged
- CWnd [MFC], OnContextMenu
- CWnd [MFC], OnCopyData
- CWnd [MFC], OnCreate
- CWnd [MFC], OnCtlColor
- CWnd [MFC], OnDeadChar
- CWnd [MFC], OnDeleteItem
- CWnd [MFC], OnDestroy
- CWnd [MFC], OnDestroyClipboard
- CWnd [MFC], OnDeviceChange
- CWnd [MFC], OnDevModeChange
- CWnd [MFC], OnDrawClipboard
- CWnd [MFC], OnDrawItem
- CWnd [MFC], OnDropFiles
- CWnd [MFC], OnEnable
- CWnd [MFC], OnEndSession
- CWnd [MFC], OnEnterIdle
- CWnd [MFC], OnEnterMenuLoop
- CWnd [MFC], OnEnterSizeMove
- CWnd [MFC], OnEraseBkgnd
- CWnd [MFC], OnExitMenuLoop
- CWnd [MFC], OnExitSizeMove
- CWnd [MFC], OnFontChange
- CWnd [MFC], OnGetDlgCode
- CWnd [MFC], OnGetMinMaxInfo
- CWnd [MFC], OnHelpInfo
- CWnd [MFC], OnHotKey
- CWnd [MFC], OnHScroll
- CWnd [MFC], OnHScrollClipboard
- CWnd [MFC], OnIconEraseBkgnd
- CWnd [MFC], OnInitMenu
- CWnd [MFC], OnInitMenuPopup
- CWnd [MFC], OnInputDeviceChange
- CWnd [MFC], OnInputLangChange
- CWnd [MFC], OnInputLangChangeRequest
- CWnd [MFC], OnKeyDown
- CWnd [MFC], OnKeyUp
- CWnd [MFC], OnKillFocus
- CWnd [MFC], OnLButtonDblClk
- CWnd [MFC], OnLButtonDown
- CWnd [MFC], OnLButtonUp
- CWnd [MFC], OnMButtonDblClk
- CWnd [MFC], OnMButtonDown
- CWnd [MFC], OnMButtonUp
- CWnd [MFC], OnMDIActivate
- CWnd [MFC], OnMeasureItem
- CWnd [MFC], OnMenuChar
- CWnd [MFC], OnMenuDrag
- CWnd [MFC], OnMenuGetObject
- CWnd [MFC], OnMenuRButtonUp
- CWnd [MFC], OnMenuSelect
- CWnd [MFC], OnMouseActivate
- CWnd [MFC], OnMouseHover
- CWnd [MFC], OnMouseHWheel
- CWnd [MFC], OnMouseLeave
- CWnd [MFC], OnMouseMove
- CWnd [MFC], OnMouseWheel
- CWnd [MFC], OnMove
- CWnd [MFC], OnMoving
- CWnd [MFC], OnNcActivate
- CWnd [MFC], OnNcCalcSize
- CWnd [MFC], OnNcCreate
- CWnd [MFC], OnNcDestroy
- CWnd [MFC], OnNcHitTest
- CWnd [MFC], OnNcLButtonDblClk
- CWnd [MFC], OnNcLButtonDown
- CWnd [MFC], OnNcLButtonUp
- CWnd [MFC], OnNcMButtonDblClk
- CWnd [MFC], OnNcMButtonDown
- CWnd [MFC], OnNcMButtonUp
- CWnd [MFC], OnNcMouseHover
- CWnd [MFC], OnNcMouseLeave
- CWnd [MFC], OnNcMouseMove
- CWnd [MFC], OnNcPaint
- CWnd [MFC], OnNcRButtonDblClk
- CWnd [MFC], OnNcRButtonDown
- CWnd [MFC], OnNcRButtonUp
- CWnd [MFC], OnNcRenderingChanged
- CWnd [MFC], OnNcXButtonDblClk
- CWnd [MFC], OnNcXButtonDown
- CWnd [MFC], OnNcXButtonUp
- CWnd [MFC], OnNextMenu
- CWnd [MFC], OnNotify
- CWnd [MFC], OnNotifyFormat
- CWnd [MFC], OnPaint
- CWnd [MFC], OnPaintClipboard
- CWnd [MFC], OnPaletteChanged
- CWnd [MFC], OnPaletteIsChanging
- CWnd [MFC], OnParentNotify
- CWnd [MFC], OnPowerBroadcast
- CWnd [MFC], OnQueryDragIcon
- CWnd [MFC], OnQueryEndSession
- CWnd [MFC], OnQueryNewPalette
- CWnd [MFC], OnQueryOpen
- CWnd [MFC], OnQueryUIState
- CWnd [MFC], OnRawInput
- CWnd [MFC], OnRButtonDblClk
- CWnd [MFC], OnRButtonDown
- CWnd [MFC], OnRButtonUp
- CWnd [MFC], OnRenderAllFormats
- CWnd [MFC], OnRenderFormat
- CWnd [MFC], OnSessionChange
- CWnd [MFC], OnSetCursor
- CWnd [MFC], OnSetFocus
- CWnd [MFC], OnSettingChange
- CWnd [MFC], OnShowWindow
- CWnd [MFC], OnSize
- CWnd [MFC], OnSizeClipboard
- CWnd [MFC], OnSizing
- CWnd [MFC], OnSpoolerStatus
- CWnd [MFC], OnStyleChanged
- CWnd [MFC], OnStyleChanging
- CWnd [MFC], OnSysChar
- CWnd [MFC], OnSysColorChange
- CWnd [MFC], OnSysCommand
- CWnd [MFC], OnSysDeadChar
- CWnd [MFC], OnSysKeyDown
- CWnd [MFC], OnSysKeyUp
- CWnd [MFC], OnTCard
- CWnd [MFC], OnTimeChange
- CWnd [MFC], OnTimer
- CWnd [MFC], OnTouchInput
- CWnd [MFC], OnTouchInputs
- CWnd [MFC], OnUniChar
- CWnd [MFC], OnUnInitMenuPopup
- CWnd [MFC], OnUpdateUIState
- CWnd [MFC], OnUserChanged
- CWnd [MFC], OnVKeyToItem
- CWnd [MFC], OnVScroll
- CWnd [MFC], OnVScrollClipboard
- CWnd [MFC], OnWindowPosChanged
- CWnd [MFC], OnWindowPosChanging
- CWnd [MFC], OnWinIniChange
- CWnd [MFC], OnWndMsg
- CWnd [MFC], OnXButtonDblClk
- CWnd [MFC], OnXButtonDown
- CWnd [MFC], OnXButtonUp
- CWnd [MFC], PostNcDestroy
- CWnd [MFC], ReflectChildNotify
- CWnd [MFC], ReflectLastMsg
- CWnd [MFC], ResizeDynamicLayout
- CWnd [MFC], WindowProc
- CWnd [MFC], m_hWnd
ms.assetid: 49a832ee-bc34-4126-88b3-bc1d9974f6c4
ms.openlocfilehash: c2ea240ba736c95026b2b6d2af45296245881bab
ms.sourcegitcommit: 7a6116e48c3c11b97371b8ae4ecc23adce1f092d
ms.translationtype: MT
ms.contentlocale: ko-KR
ms.lasthandoff: 04/22/2020
ms.locfileid: "81751717"
---
# <a name="cwnd-class"></a>CWnd 클래스

MFC 라이브러리의 모든 Window 클래스의 기본적인 기능을 제공합니다.

## <a name="syntax"></a>구문

```
class CWnd : public CCmdTarget
```

## <a name="members"></a>멤버

### <a name="public-constructors"></a>Public 생성자

|속성|Description|
|----------|-----------------|
|[CWnd::CWnd](#cwnd)|`CWnd` 개체를 생성합니다.|

### <a name="public-methods"></a>Public 메서드

|속성|Description|
|----------|-----------------|
|[CWnd::accDoDefaultAction](#accdodefaultaction)|개체의 기본 동작을 수행하기 위해 프레임워크에서 호출됩니다.|
|[CWnd::accHitTest](#acchittest)|화면의 지정된 지점에서 자식 요소나 자식 개체를 검색하기 위해 프레임워크에서 호출됩니다.|
|[CWnd::accLocation](#acclocation)|지정된 개체의 현재 화면 위치를 검색하기 위해 프레임워크에서 호출됩니다.|
|[CWnd::accNavigate](#accnavigate)|컨테이너 내에서 다른 사용자 인터페이스 요소로 트래버스하고 가능할 경우 개체를 검색하기 위해 프레임워크에서 호출됩니다.|
|[CWnd::accSelect](#accselect)|선택 영역을 수정하거나 지정된 개체의 키보드 포커스를 이동하기 위해 프레임워크에서 호출됩니다.|
|[CWnd::AnimateWindow](#animatewindow)|연결된 창 개체를 애니메이션합니다.|
|[CWnd::ArrangeIconicWindows](#arrangeiconicwindows)|모든 최소화된(아이콘) 자식 창을 정렬합니다.|
|[CWnd::Attach](#attach)|Windows 핸들을 `CWnd` 개체에 연결합니다.|
|[CWnd::BeginModalState](#beginmodalstate)|프레임 창을 모달로 만들기 위해 이 멤버 함수를 호출합니다.|
|[CWnd::BeginPaint](#beginpaint)|그리기를 위해 `CWnd`를 준비합니다.|
|[CWnd::BindDefaultProperty](#binddefaultproperty)|형식 라이브러리에 표시된 대로 호출하는 개체의 바인딩된 기본 단순 속성을 데이터 소스 컨트롤과 연결된 커서에 바인딩합니다.|
|[CWnd::BindProperty](#bindproperty)|데이터 바인딩된 컨트롤의 커서 바인딩된 속성을 데이터 소스 컨트롤에 바인딩하고 MFC 바인딩 관리자에 해당 관계를 등록합니다.|
|[CWnd::BringWindowToTop](#bringwindowtotop)|`CWnd`를 겹치는 창 스택의 맨 위로 이동합니다.|
|[CWnd::CalcWindowRect](#calcwindowrect)|클라이언트 사각형에서 창 사각형을 계산하기 위해 호출됩니다.|
|[CWnd::CancelToolTips](#canceltooltips)|도구 설명 컨트롤을 사용하지 않도록 설정합니다.|
|[CWnd::CenterWindow](#centerwindow)|부모를 기준으로 창을 가운데 맞춤합니다.|
|[CWnd::ChangeClipboardChain](#changeclipboardchain)|클립보드 뷰어 체인에서 `CWnd`를 제거합니다.|
|[CWnd::CheckDlgButton](#checkdlgbutton)|단추 컨트롤 옆에 확인 표시를 배치하거나 이 컨트롤에서 확인 표시를 제거합니다.|
|[CWnd::CheckRadioButton](#checkradiobutton)|지정된 라디오 단추를 선택하고 지정된 단추 그룹의 다른 모든 라디오 단추에서 확인 표시를 제거합니다.|
|[CWnd::ChildWindowFromPoint](#childwindowfrompoint)|자식 창 중에서 지정된 점이 있는 창(있는 경우)을 확인합니다.|
|[CWnd::ClientToScreen](#clienttoscreen)|디스플레이에서 지정된 점이나 사각형의 클라이언트 좌표를 화면 좌표로 변환합니다.|
|[CWnd::CloseWindow](#closewindow)|창을 최소화합니다.|
|[CWnd::ContinueModal](#continuemodal)|창의 모달 상태를 계속 유지합니다.|
|[CWnd::Create](#create)|`CWnd` 개체와 연결된 자식 창을 만들고 초기화합니다.|
|[CWnd::CreateAccessibleProxy](#createaccessibleproxy)|지정된 개체에 대해 Active Accessibility 프록시를 만듭니다.|
|[CWnd::CreateCaret](#createcaret)|시스템 캐럿에 대한 새 셰이프를 만들고 캐럿의 소유권을 가져옵니다.|
|[CWnd::CreateControl](#createcontrol)|MFC 프로그램에서 `CWnd` 개체로 표현될 ActiveX 컨트롤을 만듭니다.|
|[CWnd::CreateEx](#createex)|겹친 Windows 팝업 또는 자식 창을 만들고 `CWnd` 개체에 연결합니다.|
|[CWnd::CreateGrayCaret](#creategraycaret)|시스템 캐럿에 대한 회색 블록을 만들고 캐럿의 소유권을 가져옵니다.|
|[CWnd::CreateSolidCaret](#createsolidcaret)|시스템 캐럿에 대한 단색 블록을 만들고 캐럿의 소유권을 가져옵니다.|
|[CWnd::DeleteTempMap](#deletetempmap)|`CWinApp` 유휴 시간 처리기에서 자동으로 호출되고 `FromHandle`에서 만든 임시 `CWnd` 개체를 삭제합니다.|
|[CWnd::DestroyWindow](#destroywindow)|연결된 Windows 창을 제거합니다.|
|[CWnd::Detach](#detach)|`CWnd` 개체에서 Windows 핸들을 분리하고 핸들을 반환합니다.|
|[CWnd::DlgDirList](#dlgdirlist)|파일 또는 디렉터리 목록으로 목록 상자를 채웁니다.|
|[CWnd::DlgDirListComboBox](#dlgdirlistcombobox)|파일 또는 디렉터리 목록으로 콤보 상자의 목록 상자를 채웁니다.|
|[CWnd::DlgDirSelect](#dlgdirselect)|목록 상자에서 현재 선택 영역을 검색합니다.|
|[CWnd::DlgDirSelectComboBox](#dlgdirselectcombobox)|콤보 상자의 목록 상자에서 현재 선택 영역을 검색합니다.|
|[CWnd::DragAcceptFiles](#dragacceptfiles)|창에서 끌어온 파일을 수락할지를 나타냅니다.|
|[CWnd::DragDetect](#dragdetect)|마우스를 캡처하고 사용자가 왼쪽 단추를 놓거나, Esc 키를 누르거나, 지정된 점 주위의 끌기 사각형 밖으로 마우스를 이동할 때까지 이동을 추적합니다.|
|[CWnd::DrawAnimatedRects](#drawanimatedrects)|와이어프레임 사각형을 그리고 애니메이션하여 아이콘 열기 또는 창의 최소화나 최대화를 나타냅니다.|
|[CWnd::DrawCaption](#drawcaption)|캡션을 그립니다.|
|[CWnd::DrawMenuBar](#drawmenubar)|메뉴 모음을 다시 그립니다.|
|[CWnd::EnableActiveAccessibility](#enableactiveaccessibility)|사용자 정의 `Active Accessibility` 함수를 사용하도록 설정합니다.|
|[CWnd::EnableDynamicLayout](#enabledynamiclayout)|사용자가 창의 크기를 조정하면 자식 창의 위치 및 크기가 동적으로 조정되도록 합니다.|
|[CWnd::EnableD2DSupport](#enabled2dsupport)|창 `D2D` 지원을 사용하거나 사용하지 않도록 설정합니다. 주 창이 초기화되기 전에 이 메서드를 호출합니다.|
|[CWnd::EnableScrollBar](#enablescrollbar)|스크롤 막대의 화살표 하나 또는 둘 다를 사용하거나 사용하지 않도록 설정합니다.|
|[CWnd::EnableScrollBarCtrl](#enablescrollbarctrl)|형제 스크롤 막대 컨트롤을 사용하거나 사용하지 않도록 설정합니다.|
|[CWnd::EnableToolTips](#enabletooltips)|도구 설명 컨트롤을 사용하도록 설정합니다.|
|[CWnd::EnableTrackingToolTips](#enabletrackingtooltips)|추적 모드에서 도구 설명 컨트롤을 사용하도록 설정합니다.|
|[CWnd::EnableWindow](#enablewindow)|마우스 및 키보드 입력을 사용하거나 사용하지 않도록 설정합니다.|
|[CWnd::EndModalLoop](#endmodalloop)|창의 모달 상태를 종료합니다.|
|[CWnd::EndModalState](#endmodalstate)|프레임 창을 모달에서 모덜리스로 변경하려면 이 멤버 함수를 호출합니다.|
|[CWnd::EndPaint](#endpaint)|그리기의 끝을 표시합니다.|
|[CWnd::ExecuteDlgInit](#executedlginit)|대화 상자 리소스를 시작합니다.|
|[CWnd::FilterToolTipMessage](#filtertooltipmessage)|대화 상자에서 컨트롤과 연결된 제목 또는 텍스트를 검색합니다.|
|[CWnd::FindWindow](#findwindow)|창 이름 및 창 클래스로 식별되는 창의 핸들을 반환합니다.|
|[CWnd::FindWindowEx](#findwindowex)|창 이름 및 창 클래스로 식별되는 창의 핸들을 반환합니다.|
|[CWnd::FlashWindow](#flashwindow)|창을 한 번 깜박입니다.|
|[CWnd::FlashWindowEx](#flashwindowex)|추가 기능으로 창을 깜박입니다.|
|[CWnd::FromHandle](#fromhandle)|창에 핸들을 지정한 경우 `CWnd` 개체에 대한 포인터를 반환합니다. `CWnd` 개체가 핸들에 연결되지 않은 경우 임시 `CWnd` 개체를 만들어 연결합니다.|
|[CWnd::FromHandlePermanent](#fromhandlepermanent)|창에 핸들을 지정한 경우 `CWnd` 개체에 대한 포인터를 반환합니다.|
|[CWnd::get_accChild](#get_accchild)|지정된 자식의 `IDispatch` 인터페이스 주소를 검색하기 위해 프레임워크에서 호출됩니다.|
|[CWnd::get_accChildCount](#get_accchildcount)|이 개체에 속하는 자식 수를 검색하기 위해 프레임워크에서 호출됩니다.|
|[CWnd::get_accDefaultAction](#get_accdefaultaction)|개체의 기본 작업을 설명하는 문자열을 검색하기 위해 프레임워크에서 호출됩니다.|
|[CWnd::get_accDescription](#get_accdescription)|지정한 개체의 모양을 설명하는 문자열을 검색하기 위해 프레임워크에서 호출됩니다.|
|[CWnd::get_accFocus](#get_accfocus)|키보드 포커스가 있는 개체를 검색하기 위해 프레임워크에서 호출됩니다.|
|[CWnd::get_accHelp](#get_acchelp)|개체의 **도움말** 속성 문자열을 검색하는 프레임워크에서 호출합니다.|
|[CWnd::get_accHelpTopic](#get_acchelptopic)|지정된 개체와 연결된 `WinHelp` 파일의 전체 경로와 해당 파일 내의 해당 항목의 식별자를 검색하기 위해 프레임워크에서 호출됩니다.|
|[CWnd::get_accKeyboardShortcut](#get_acckeyboardshortcut)|지정된 개체의 바로 가기 키 또는 선택키를 검색하기 위해 프레임워크에서 호출됩니다.|
|[CWnd::get_accName](#get_accname)|지정된 개체의 이름을 검색하기 위해 프레임워크에서 호출됩니다.|
|[CWnd::get_accParent](#get_accparent)|개체 부모의 `IDispatch` 인터페이스를 검색하기 위해 프레임워크에서 호출됩니다.|
|[CWnd::get_accRole](#get_accrole)|지정된 개체의 역할을 설명하는 정보를 검색하기 위해 프레임워크에서 호출됩니다.|
|[CWnd::get_accSelection](#get_accselection)|이 개체의 선택된 자식 개체를 검색하기 위해 프레임워크에서 호출됩니다.|
|[CWnd::get_accState](#get_accstate)|지정된 개체의 현재 상태를 검색하기 위해 프레임워크에서 호출됩니다.|
|[CWnd::get_accValue](#get_accvalue)|지정된 개체의 값을 검색하기 위해 프레임워크에서 호출됩니다.|
|[CWnd::GetActiveWindow](#getactivewindow)|활성 창을 검색합니다.|
|[CWnd::GetAncestor](#getancestor)|지정된 창의 상위 창 개체를 검색합니다.|
|[CWnd::GetCapture](#getcapture)|마우스 캡처가 있는 `CWnd`를 검색합니다.|
|[CWnd::GetCaretPos](#getcaretpos)|캐럿 현재 위치의 클라이언트 좌표를 검색합니다.|
|[CWnd::GetCheckedRadioButton](#getcheckedradiobutton)|단추 그룹에서 현재 선택된 라디오 단추의 ID를 반환합니다.|
|[CWnd::GetClientRect](#getclientrect)|`CWnd` 클라이언트 영역의 차원을 가져옵니다.|
|[CWnd::GetClipboardOwner](#getclipboardowner)|클립보드의 현재 소유자에 대한 포인터를 검색합니다.|
|[CWnd::GetClipboardViewer](#getclipboardviewer)|클립보드 뷰어 체인에서 첫 번째 창에 대한 포인터를 검색합니다.|
|[CWnd::GetControlUnknown](#getcontrolunknown)|알 수 없는 ActiveX 컨트롤에 대한 포인터를 검색합니다.|
|[CWnd::GetDC](#getdc)|클라이언트 영역에 대한 디스플레이 컨텍스트를 검색합니다.|
|[CWnd::GetDCEx](#getdcex)|클라이언트 영역에 대한 디스플레이 컨텍스트를 검색하고 그리는 동안 클리핑을 사용하도록 설정합니다.|
|[CWnd::GetDCRenderTarget](#getdcrendertarget)|`CWnd` 창에 대한 DC(디바이스 컨텍스트) 렌더링 대상을 검색합니다.|
|[CWnd::GetDescendantWindow](#getdescendantwindow)|모든 하위 창을 검색하고 지정된 ID를 사용하는 창을 반환합니다.|
|[CWnd::GetDesktopWindow](#getdesktopwindow)|Windows 바탕 화면 창을 검색합니다.|
|[CWnd::GetDlgCtrlID](#getdlgctrlid)|`CWnd`가 자식 창인 경우 이 함수를 호출하면 해당 ID 값이 반환됩니다.|
|[CWnd::GetDlgItem](#getdlgitem)|지정된 대화 상자에서 지정된 ID를 사용하여 컨트롤을 검색합니다.|
|[CWnd::GetDlgItemInt](#getdlgitemint)|지정된 대화 상자에서 컨트롤의 텍스트를 정수 값으로 변환합니다.|
|[CWnd::GetDlgItemText](#getdlgitemtext)|컨트롤에 연결된 텍스트 또는 캡션을 검색합니다.|
|[CWnd::GetDSCCursor](#getdsccursor)|데이터 소스 컨트롤의 데이터 소스, 사용자 이름, 암호 및 SQL 속성으로 정의된 기본 커서에 대한 포인터를 검색합니다.|
|[CWnd::GetDynamicLayout](#getdynamiclayout)|동적 레이아웃 관리자 개체에 대한 포인터를 검색합니다.|
|[CWnd::GetExStyle](#getexstyle)|창의 확장된 스타일을 반환합니다.|
|[CWnd::GetFocus](#getfocus)|현재 입력 포커스가 있는 `CWnd`를 검색합니다.|
|[CWnd::GetFont](#getfont)|현재 글꼴을 검색합니다.|
|[CWnd::GetForegroundWindow](#getforegroundwindow)|전경 창(사용자가 현재 작업 중인 최상위 창)에 대한 포인터를 반환합니다.|
|[CWnd::GetIcon](#geticon)|아이콘에 대한 핸들을 검색합니다.|
|[CWnd::GetLastActivePopup](#getlastactivepopup)|`CWnd`가 소유한 팝업 창이 가장 최근에 활성화되었는지 확인합니다.|
|[CWnd::GetLayeredWindowAttributes](#getlayeredwindowattributes)|계층적 창의 불투명도 및 투명도 색상 키를 검색합니다.|
|[CWnd::GetMenu](#getmenu)|지정된 메뉴에 대한 포인터를 검색합니다.|
|[CWnd::GetNextDlgGroupItem](#getnextdlggroupitem)|컨트롤 그룹 내에서 다음(또는 이전) 컨트롤을 검색합니다.|
|[CWnd::GetNextDlgTabItem](#getnextdlgtabitem)|지정된 컨트롤을 따르거나 앞에 오는 [WS_TABSTOP](styles-used-by-mfc.md#window-styles) 스타일로 첫 번째 컨트롤을 검색합니다.|
|[CWnd::GetNextWindow](#getnextwindow)|창 관리자의 목록에서 다음(또는 이전) 창을 반환합니다.|
|[CWnd::GetOleControlSite](#getolecontrolsite)|지정된 ActiveX 컨트롤에 대한 사용자 지정 사이트를 검색합니다.|
|[CWnd::GetOpenClipboardWindow](#getopenclipboardwindow)|현재 클립보드가 열려 있는 창에 대한 포인터를 검색합니다.|
|[CWnd::GetOwner](#getowner)|`CWnd`의 소유자에 대한 포인터를 검색합니다.|
|[CWnd::GetParent](#getparent)|`CWnd`의 부모 창(있는 경우)을 검색합니다.|
|[CWnd::GetParentFrame](#getparentframe)|`CWnd` 개체의 부모 프레임 창을 검색합니다.|
|[CWnd::GetParentOwner](#getparentowner)|자식 창의 부모 창에 대한 포인터를 반환합니다.|
|[CWnd::GetProperty](#getproperty)|ActiveX 컨트롤 속성을 검색합니다.|
|[CWnd::GetRenderTarget](#getrendertarget)|이 창에 연결된 렌더링 대상을 가져옵니다.|
|[CWnd::GetSafeHwnd](#getsafehwnd)|`m_hWnd` **이** 포인터가 NULL인 경우 반환 합니다 .|
|[CWnd::GetSafeOwner](#getsafeowner)|지정된 창에 대한 안전한 소유자를 검색합니다.|
|[CWnd::GetScrollBarCtrl](#getscrollbarctrl)|형제 스크롤 막대 컨트롤을 반환합니다.|
|[CWnd::GetScrollBarInfo](#getscrollbarinfo)|지정한 스크롤 막대에 대한 정보를 검색합니다.|
|[CWnd::GetScrollInfo](#getscrollinfo)|`SCROLLINFO` 구조체에서 스크롤 막대에 대해 유지 관리하는 정보를 검색합니다.|
|[CWnd::GetScrollLimit](#getscrolllimit)|스크롤 막대의 한계를 검색합니다.|
|[CWnd::GetScrollPos](#getscrollpos)|스크롤 상자의 현재 위치를 검색합니다.|
|[CWnd::GetScrollRange](#getscrollrange)|지정된 스크롤 막대에 대해 현재 최소 및 최대 스크롤 막대 위치를 복사합니다.|
|[CWnd::GetStyle](#getstyle)|현재 창 스타일을 반환합니다.|
|[CWnd::GetSystemMenu](#getsystemmenu)|애플리케이션에서 복사 및 수정을 위해 컨트롤 메뉴에 액세스할 수 있도록 합니다.|
|[CWnd::GetTitleBarInfo](#gettitlebarinfo)|지정된 제목 표시줄에 대한 정보를 검색합니다.|
|[CWnd::GetTopLevelFrame](#gettoplevelframe)|창의 최상위 프레임 창을 검색합니다.|
|[CWnd::GetTopLevelOwner](#gettoplevelowner)|최상위 창을 검색합니다.|
|[CWnd::GetTopLevelParent](#gettoplevelparent)|창의 최상위 부모를 검색합니다.|
|[CWnd::GetTopWindow](#gettopwindow)|`CWnd`에 속하는 첫 번째 자식 창을 반환합니다.|
|[CWnd::GetUpdateRect](#getupdaterect)|`CWnd` 업데이트 영역을 완전히 둘러싸는 가장 작은 사각형의 좌표를 검색합니다.|
|[CWnd::GetUpdateRgn](#getupdatergn)|`CWnd` 업데이트 영역을 검색합니다.|
|[CWnd::GetWindow](#getwindow)|이 창에 대해 지정된 관계를 갖는 창을 반환합니다.|
|[CWnd::GetWindowContextHelpId](#getwindowcontexthelpid)|도움말 컨텍스트 식별자를 검색합니다.|
|[CWnd::GetWindowDC](#getwindowdc)|캡션 표시줄, 메뉴 및 스크롤 막대를 포함하여 전체 창에 대한 디스플레이 컨텍스트를 검색합니다.|
|[CWnd::GetWindowedChildCount](#getwindowedchildcount)|연결된 자식 창의 수를 반환합니다.|
|[CWnd::GetWindowInfo](#getwindowinfo)|창에 대한 정보를 반환합니다.|
|[CWnd::GetWindowlessChildCount](#getwindowlesschildcount)|연결된 창 없는 자식 창의 수를 반환합니다.|
|[CWnd::GetWindowPlacement](#getwindowplacement)|창의 표시 상태와 일반(복원됨), 최소화 및 최대화 위치를 검색합니다.|
|[CWnd::GetWindowRect](#getwindowrect)|`CWnd`의 화면 좌표를 가져옵니다.|
|[CWnd::GetWindowRgn](#getwindowrgn)|창의 창 영역에 대한 복사본을 검색합니다.|
|[CWnd::GetWindowText](#getwindowtext)|창 텍스트 또는 캡션 제목(있는 경우)을 반환합니다.|
|[CWnd::GetWindowTextLength](#getwindowtextlength)|창의 텍스트 또는 캡션 제목의 길이를 반환합니다.|
|[CWnd::HideCaret](#hidecaret)|디스플레이 화면에서 제거하여 캐럿을 숨깁니다.|
|[CWnd::HiliteMenuItem](#hilitemenuitem)|최상위(메뉴 모음) 메뉴 항목에서 강조 표시하거나 강조 표시를 제거합니다.|
|[CWnd::HtmlHelp](#htmlhelp)|HTMLHelp 애플리케이션을 시작하기 위해 호출됩니다.|
|[CWnd::Invalidate](#invalidate)|전체 클라이언트 영역을 무효화합니다.|
|[CWnd::InvalidateRect](#invalidaterect)|현재 업데이트 영역에 지정된 사각형을 추가하여 해당 사각형 내에서 클라이언트 영역을 무효화합니다.|
|[CWnd::InvalidateRgn](#invalidatergn)|현재 업데이트 영역에 지정된 영역을 추가하여 해당 영역 내에서 클라이언트 영역을 무효화합니다.|
|[CWnd::InvokeHelper](#invokehelper)|ActiveX 컨트롤 메서드나 속성을 호출합니다.|
|[CWnd::IsChild](#ischild)|`CWnd`가 자식 창인지 지정된 창의 다른 직계 하위 항목인지를 나타냅니다.|
|[CWnd::IsD2DSupportEnabled](#isd2dsupportenabled)|D2D 지원이 활성화되어 있는지 여부를 결정합니다.|
|[CWnd::IsDialogMessage](#isdialogmessage)|지정된 메시지가 모덜리스 대화 상자용인지 확인하고 그럴 경우 처리합니다.|
|[CWnd::IsDlgButtonChecked](#isdlgbuttonchecked)|단추 컨트롤이 선택되어 있는지 여부를 확인합니다.|
|[CWnd::IsDynamicLayoutEnabled](#isdynamiclayoutenabled)|이 창에서 동적 레이아웃이 사용되는지 여부를 확인합니다. 동적 레이아웃이 사용되는 경우 사용자가 부모 창의 크기를 조정하면 자식 창의 위치 및 크기가 변경될 수 있습니다.|
|[CWnd::IsIconic](#isiconic)|`CWnd`가 최소화(아이콘)되었는지 여부를 확인합니다.|
|[CWnd::IsTouchWindow](#istouchwindow)|`CWnd`가 터치를 지원하는지 여부를 지정합니다.|
|[CWnd::IsWindowEnabled](#iswindowenabled)|마우스 및 키보드 입력에 창을 사용할 수 있는지 여부를 확인합니다.|
|[CWnd::IsWindowVisible](#iswindowvisible)|창이 표시되는지 여부를 확인합니다.|
|[CWnd::IsZoomed](#iszoomed)|`CWnd`이 최대화되었는지 여부를 확인합니다.|
|[CWnd::KillTimer](#killtimer)|시스템 타이머를 종료합니다.|
|[CWnd::LockWindowUpdate](#lockwindowupdate)|지정된 창에서 그리기를 사용하지 않거나 다시 사용하도록 설정합니다.|
|[CWnd::MapWindowPoints](#mapwindowpoints)|`CWnd`의 좌표 공간에서 다른 창의 좌표 공간으로 점 집합(맵)을 변환합니다.|
|[CWnd::MessageBox](#messagebox)|애플리케이션에서 제공하는 메시지 및 캡션을 포함하는 창을 만들고 표시합니다.|
|[CWnd::ModifyStyle](#modifystyle)|현재 창 스타일을 수정합니다.|
|[CWnd::ModifyStyleEx](#modifystyleex)|창의 확장된 스타일을 수정합니다.|
|[CWnd::MoveWindow](#movewindow)|`CWnd`의 위치 및 크기를 변경합니다.|
|[CWnd::NotifyWinEvent](#notifywinevent)|미리 정의된 이벤트가 발생한 시스템에 신호를 보냅니다.|
|[CWnd::OnAmbientProperty](#onambientproperty)|앰비언트 속성 값을 구현합니다.|
|[CWnd::OnDrawIconicThumbnailOrLivePreview](#ondrawiconicthumbnailorlivepreview)|Windows 7 탭 미리 보기 또는 애플리케이션 피킹(Peeking)용 클라이언트에 표시할 비트맵을 가져와야 하는 경우 프레임워크에서 호출됩니다.|
|[CWnd::OnHelp](#onhelp)|현재 컨텍스트를 사용하여 애플리케이션 내에서 F1 도움말을 처리합니다.|
|[CWnd::OnHelpFinder](#onhelpfinder)|ID_HELP_FINDER 및 ID_DEFAULT_HELP 명령을 처리합니다.|
|[CWnd::OnHelpIndex](#onhelpindex)|ID_HELP_INDEX 명령을 처리하고 기본 도움말 항목을 제공합니다.|
|[CWnd::OnHelpUsing](#onhelpusing)|ID_HELP_USING 명령을 처리합니다.|
|[CWnd::OnToolHitTest](#ontoolhittest)|점이 지정된 도구의 경계 사각형에 있는지 여부를 확인하고 도구에 대한 정보를 검색합니다.|
|[CWnd::OpenClipboard](#openclipboard)|클립보드를 엽니다. 다른 응용 프로그램은 Windows [CloseClipboard](/windows/win32/api/winuser/nf-winuser-closeclipboard) 함수가 호출될 때까지 클립보드를 수정할 수 없습니다.|
|[CWnd::PaintWindowlessControls](#paintwindowlesscontrols)|컨트롤 컨테이너에서 창 없는 컨트롤을 그립니다.|
|[CWnd::PostMessage](#postmessage)|애플리케이션 큐에 메시지를 배치한 다음 창에서 메시지를 처리할 때까지 기다리지 않고 반환합니다.|
|[CWnd::PreCreateWindow](#precreatewindow)|이 `CWnd` 개체에 연결된 Windows 창을 만들기 전에 호출됩니다.|
|[CWnd::PreSubclassWindow](#presubclasswindow)|[하위 클래스창이](#subclasswindow) 호출되기 전에 다른 필요한 하위 분류가 발생할 수 있습니다.|
|[CWnd::PreTranslateMessage](#pretranslatemessage)|`TranslateMessage` 및 `DispatchMessage` Windows 함수로 디스패치되기 전에 `CWinApp`에서 창 메시지를 필터링하는 데 사용됩니다.|
|[CWnd::Print](#print)|지정된 디바이스 컨텍스트에서 현재 창을 그립니다.|
|[CWnd::PrintClient](#printclient)|지정된 디바이스 컨텍스트(일반적으로 프린터 디바이스 컨텍스트)에서 창을 그립니다.|
|[CWnd::PrintWindow](#printwindow)|지정된 디바이스 컨텍스트(일반적으로 프린터 DC)에 시각적 창을 복사합니다.|
|[CWnd::RedrawWindow](#redrawwindow)|클라이언트 영역에서 지정된 사각형 또는 영역을 업데이트합니다.|
|[CWnd::RegisterTouchWindow](#registertouchwindow)|창 Windows Touch 지원을 등록/등록 취소합니다.|
|[CWnd::ReleaseDC](#releasedc)|클라이언트 및 창 디바이스 컨텍스트를 해제하여 다른 애플리케이션에서 사용하도록 확보합니다.|
|[CWnd::RepositionBars](#repositionbars)|클라이언트 영역에서 컨트롤 막대의 위치를 변경합니다.|
|[CWnd::RunModalLoop](#runmodalloop)|모달 상태에 있는 창에 대한 메시지를 검색, 변환 또는 디스패치합니다.|
|[CWnd::ScreenToClient](#screentoclient)|디스플레이에 있는 지정된 점 또는 사각형의 화면 좌표를 클라이언트 좌표로 변환합니다.|
|[CWnd::ScrollWindow](#scrollwindow)|클라이언트 영역의 내용을 스크롤합니다.|
|[CWnd::ScrollWindowEx](#scrollwindowex)|클라이언트 영역의 내용을 스크롤합니다. `ScrollWindow`와 유사하며 추가 기능을 제공합니다.|
|[CWnd::SendChildNotifyLastMsg](#sendchildnotifylastmsg)|부모 창에서 자식 창에 알림 메시지를 제공하여 자식 창에서 작업을 처리할 수 있도록 합니다.|
|[CWnd::SendDlgItemMessage](#senddlgitemmessage)|지정된 컨트롤에 메시지를 보냅니다.|
|[CWnd::SendMessage](#sendmessage)|`CWnd` 개체에 메시지를 보내고 메시지를 처리할 때까지 반환하지 않습니다.|
|[CWnd::SendMessageToDescendants](#sendmessagetodescendants)|창의 모든 하위 창에 메시지를 보냅니다.|
|[CWnd::SendNotifyMessage](#sendnotifymessage)|호출 스레드에서 창을 만들었는지 여부에 따라 지정된 메시지를 창에 보내고 최대한 빨리 반환합니다.|
|[CWnd::SetActiveWindow](#setactivewindow)|창을 활성화합니다.|
|[CWnd::SetCapture](#setcapture)|모든 후속 마우스 입력을 `CWnd`로 보내도록 합니다.|
|[CWnd::SetCaretPos](#setcaretpos)|지정된 위치로 캐럿을 이동합니다.|
|[CWnd::SetClipboardViewer](#setclipboardviewer)|클립보드의 내용이 변경될 때마다 알림을 받는 창 체인에 `CWnd`를 추가합니다.|
|[CWnd::SetDlgCtrlID](#setdlgctrlid)|창(대화 상자의 컨트롤만이 아니라 모든 자식 창이 될 수 있음)에 대해 창 또는 컨트롤 ID를 설정합니다.|
|[CWnd::SetDlgItemInt](#setdlgitemint)|컨트롤의 텍스트를 정수 값을 나타내는 문자열로 설정합니다.|
|[CWnd::SetDlgItemText](#setdlgitemtext)|지정된 대화 상자에서 컨트롤의 캡션 또는 텍스트를 설정합니다.|
|[CWnd::SetFocus](#setfocus)|입력 포커스를 클레임합니다.|
|[CWnd::SetFont](#setfont)|현재 글꼴을 설정합니다.|
|[CWnd::SetForegroundWindow](#setforegroundwindow)|창을 만든 스레드를 전경으로 전환하고 창을 활성화합니다.|
|[CWnd::SetIcon](#seticon)|핸들을 특정 아이콘으로 설정합니다.|
|[CWnd::SetLayeredWindowAttributes](#setlayeredwindowattributes)|계층적 창의 불투명도 및 투명도 색상 키를 설정합니다.|
|[CWnd::SetMenu](#setmenu)|메뉴를 지정된 메뉴로 설정합니다.|
|[CWnd::SetOwner](#setowner)|`CWnd`의 소유자를 변경합니다.|
|[CWnd::SetParent](#setparent)|부모 창을 변경합니다.|
|[CWnd::SetProperty](#setproperty)|ActiveX 컨트롤 속성을 설정합니다.|
|[CWnd::SetRedraw](#setredraw)|`CWnd`의 변경 내용을 다시 그리도록 허용하거나 변경 내용을 다시 그릴 수 없도록 방지합니다.|
|[CWnd::SetScrollInfo](#setscrollinfo)|스크롤 막대에 대한 정보를 설정합니다.|
|[CWnd::SetScrollPos](#setscrollpos)|스크롤 상자의 현재 위치를 설정하고, 지정된 경우 새 위치를 반영하도록 스크롤 막대를 다시 그립니다.|
|[CWnd::SetScrollRange](#setscrollrange)|지정된 스크롤 막대에 대한 최소 및 최대 위치 값을 설정합니다.|
|[CWnd::SetTimer](#settimer)|트리거될 때 [WM_TIMER](#ontimer) 메시지를 보내는 시스템 타이머를 설치합니다.|
|[CWnd::SetWindowContextHelpId](#setwindowcontexthelpid)|도움말 컨텍스트 식별자를 설정합니다.|
|[CWnd::SetWindowPlacement](#setwindowplacement)|창의 표시 상태와 일반(복원됨), 최소화 및 최대화 위치를 설정합니다.|
|[CWnd::SetWindowPos](#setwindowpos)|자식, 팝업 및 최상위 창의 크기, 위치 및 순서를 변경합니다.|
|[CWnd::SetWindowRgn](#setwindowrgn)|창의 영역을 설정합니다.|
|[CWnd::SetWindowText](#setwindowtext)|창 텍스트 또는 캡션 제목(있는 경우)을 지정된 텍스트로 설정합니다.|
|[CWnd::ShowCaret](#showcaret)|캐럿의 현재 위치에서 디스플레이의 캐럿을 표시합니다. 표시되면 캐럿이 자동으로 깜박이기 시작합니다.|
|[CWnd::ShowOwnedPopups](#showownedpopups)|창이 소유한 모든 팝업 창을 표시하거나 숨깁니다.|
|[CWnd::ShowScrollBar](#showscrollbar)|스크롤 막대를 표시하거나 숨깁니다.|
|[CWnd::ShowWindow](#showwindow)|창을 표시하거나 숨깁니다.|
|[CWnd::SubclassDlgItem](#subclassdlgitem)|Windows 컨트롤을 `CWnd` 개체에 연결하고 `CWnd`의 메시지 맵을 통해 메시지를 라우팅하도록 합니다.|
|[CWnd::SubclassWindow](#subclasswindow)|창을 `CWnd` 개체에 연결하고 `CWnd`의 메시지 맵을 통해 메시지를 라우팅하도록 합니다.|
|[CWnd::UnlockWindowUpdate](#unlockwindowupdate)|`CWnd::LockWindowUpdate`로 잠긴 창의 잠금을 해제합니다.|
|[CWnd::UnsubclassWindow](#unsubclasswindow)|`CWnd` 개체에서 창을 분리|
|[CWnd::UpdateData](#updatedata)|대화 상자에서 데이터를 초기화하거나 검색합니다.|
|[CWnd::UpdateDialogControls](#updatedialogcontrols)|대화 상자 단추 및 다른 컨트롤의 상태를 업데이트하기 위해 호출합니다.|
|[CWnd::UpdateLayeredWindow](#updatelayeredwindow)|계층적 창의 위치, 크기, 모양, 내용 및 투명도를 업데이트합니다.|
|[CWnd::UpdateWindow](#updatewindow)|클라이언트 영역을 업데이트합니다.|
|[CWnd::ValidateRect](#validaterect)|현재 업데이트 영역에서 사각형을 제거하여 지정된 사각형 내에서 클라이언트 영역의 유효성을 검색합니다.|
|[CWnd::ValidateRgn](#validatergn)|현재 업데이트 영역에서 영역을 제거하여 지정된 영역 내에서 클라이언트 영역의 유효성을 검사합니다.|
|[CWnd::WindowFromPoint](#windowfrompoint)|지정된 점을 포함하는 창을 식별합니다.|
|[CWnd::WinHelp](#winhelp)|WinHelp 애플리케이션을 시작하기 위해 호출됩니다.|

### <a name="protected-methods"></a>Protected 메서드

|속성|Description|
|----------|-----------------|
|[CWnd::Default](#default)|기본 창 프로시저를 호출하여 애플리케이션에서 처리하지 않는 모든 창 메시지에 대해 기본 처리를 제공합니다.|
|[CWnd::DefWindowProc](#defwindowproc)|기본 창 프로시저를 호출하여 애플리케이션에서 처리하지 않는 모든 창 메시지에 대해 기본 처리를 제공합니다.|
|[CWnd::DoDataExchange](#dodataexchange)|대화 상자 데이터 교환 및 유효성 검사용입니다. `UpdateData`에 의해 호출됩니다.|
|[CWnd::GetCurrentMessage](#getcurrentmessage)|이 창에서 현재 처리 중인 메시지에 대한 포인터를 반환합니다. `On` *메시지* 메시지 처리기 멤버 함수에서 만 호출 해야 합니다.|
|[CWnd::InitDynamicLayout](#initdynamiclayout)|창에 대한 동적 레이아웃을 초기화하기 위해 프레임워크에서 호출됩니다.|
|[CWnd::LoadDynamicLayoutResource](#loaddynamiclayoutresource)|리소스 파일에서 동적 레이아웃 정보를 로드합니다.|
|[CWnd::에 활성화](#onactivate)|`CWnd`가 활성화되거나 비활성화되고 있을 때 호출됩니다.|
|[CWnd::OnActivateApp](#onactivateapp)|애플리케이션이 활성화되거나 비활성화되려고 할 때 호출됩니다.|
|[CWnd::OnAppCommand](#onappcommand)|사용자가 애플리케이션 명령 이벤트를 생성할 때 호출됩니다.|
|[CWnd::OnAskCbFormatName](#onaskcbformatname)|클립보드 소유자가 클립보드 내용을 표시할 때 클립보드 뷰어 애플리케이션에서 호출됩니다.|
|[CWnd::OnCancelMode](#oncancelmode)|`CWnd`에서 마우스 캡처와 같은 내부 모드를 취소할 수 있도록 허용하기 위해 호출됩니다.|
|[CWnd::OnCaptureChanged](#oncapturechanged)|마우스 캡처가 손실되고 있는 창에 메시지를 보냅니다.|
|[CWnd::OnChangeCbChain](#onchangecbchain)|지정된 창이 체인에서 제거됨을 알립니다.|
|[CWnd::OnChangeUIState](#onchangeuistate)|UI(사용자 인터페이스) 상태를 변경해야 할 때 호출됩니다.|
|[CWnd::OnChar](#onchar)|키 입력이 시스템 문자 이외의 문자로 변환될 때 호출됩니다.|
|[CWnd::OnCharToItem](#onchartoitem)|[WM_CHAR](#onchar) 메시지에 대한 응답으로 [LBS_WANTKEYBOARDINPUT](../../mfc/reference/styles-used-by-mfc.md#list-box-styles) 스타일이 있는 자식 목록 상자에 의해 호출됩니다.|
|[CWnd::OnChildActivate](#onchildactivate)|`CWnd`의 크기 또는 위치가 변경되거나 `CWnd`가 활성화될 때마다 MDI(다중 문서 인터페이스) 자식 창에 대해 호출됩니다.|
|[CWnd::OnChildNotify](#onchildnotify)|알림 컨트롤이 컨트롤 알림에 응답할 수 있는 기회를 제공하기 위해 부모 창에서 호출됩니다.|
|[CWnd::OnClipboardUpdate](#onclipboardupdate)|클립보드의 내용이 변경될 때 호출됩니다.|
|[CWnd::OnClose](#onclose)|`CWnd`를 닫아야 한다는 신호로 호출됩니다.|
|[CWnd::OnColorizationColorChanged](#oncolorizationcolorchanged)|비클라이언트 영역에 대한 렌더링 정책이 변경된 경우 호출됩니다.|
|[CWnd::OnCommand](#oncommand)|사용자가 명령을 선택할 때 호출됩니다.|
|[CWnd::OnCompacting](#oncompacting)|Windows에서 시스템 메모리 부족을 감지할 때 호출됩니다.|
|[CWnd::OnCompareItem](#oncompareitem)|정렬된 소유자 그리기 자식 콤보 상자 또는 목록 상자에서 새 항목의 상대 위치를 확인하기 위해 호출됩니다.|
|[CWnd::OnCompositionChanged](#oncompositionchanged)|DWM(바탕 화면 창 관리자) 컴퍼지션을 사용하거나 사용하지 않도록 설정할 때 모든 최상위 창에 대해 호출됩니다.|
|[CWnd::OnContextMenu](#oncontextmenu)|사용자가 창에서 오른쪽 마우스 단추를 클릭할 때 호출됩니다.|
|[CWnd::OnCopyData](#oncopydata)|애플리케이션 간에 데이터를 복사합니다.|
|[CWnd::OnCreate](#oncreate)|창 만들기의 일부로 호출됩니다.|
|[CWnd::OnCtlColor](#onctlcolor)|컨트롤을 그리려고 할 때 `CWnd`가 컨트롤의 부모인 경우 호출됩니다.|
|[CWnd::OnDeadChar](#ondeadchar)|키 입력이 비시스템 데드 문자(예: 악센트 문자)로 변환될 때 호출됩니다.|
|[CWnd::OnDeleteItem](#ondeleteitem)|소유자 그리기 자식 목록 상자 또는 콤보 상자가 제거되거나 컨트롤에서 항목이 제거될 때 호출됩니다.|
|[CWnd::OnDestroy](#ondestroy)|`CWnd`가 제거되고 있을 때 호출됩니다.|
|[CWnd::OnDestroyClipboard](#ondestroyclipboard)|Windows 빈 클립보드 함수에 대한 호출을 통해 [클립보드가](/windows/win32/api/winuser/nf-winuser-emptyclipboard) 비워지는 경우 호출됩니다.|
|[CWnd::OnDeviceChange](#ondevicechange)|디바이스 또는 컴퓨터의 하드웨어 구성 변경을 애플리케이션 또는 디바이스 드라이버에 알립니다.|
|[CWnd::OnDevModeChange](#ondevmodechange)|사용자가 디바이스 모드 설정을 변경할 때 모든 최상위 창에 대해 호출됩니다.|
|[CWnd::OnDrawClipboard](#ondrawclipboard)|클립보드의 내용이 변경될 때 호출됩니다.|
|[CWnd::OnDrawItem](#ondrawitem)|소유자 그리기 자식 단추 컨트롤, 콤보 상자 컨트롤, 목록 상자 컨트롤 또는 메뉴의 시각적 측면을 그려야 할 때 호출됩니다.|
|[CWnd::OnDropFiles](#ondropfiles)|삭제된 파일의 수신자로 자신을 등록한 창에 대해 사용자가 왼쪽 마우스 단추를 놓을 때 호출됩니다.|
|[CWnd::OnEnable](#onenable)|`CWnd`가 사용하거나 사용하지 않도록 설정될 때 호출됩니다.|
|[CWnd::OnEndSession](#onendsession)|세션이 종료되고 있을 때 호출됩니다.|
|[CWnd::OnEnterIdle](#onenteridle)|모달 대화 상자 또는 메뉴가 유휴 상태로 전환하고 있음을 애플리케이션의 주 창 프로시저에 알리기 위해 호출됩니다.|
|[CWnd::OnEnterMenuLoop](#onentermenuloop)|메뉴 모달 루프가 시작된 경우 호출됩니다.|
|[CWnd::OnEnterSizeMove](#onentersizemove)|영향을 받는 창이 모달 루프의 이동 또는 크기 조정을 시작한 후 호출됩니다.|
|[CWnd::OnEraseBkgnd](#onerasebkgnd)|창 배경을 지워야 하는 경우 호출됩니다.|
|[CWnd::OnExitMenuLoop](#onexitmenuloop)|메뉴 모달 루프가 종료된 경우 호출됩니다.|
|[CWnd::OnExitSizeMove](#onexitsizemove)|영향을 받는 창이 모달 루프의 이동 또는 코기 조정을 종료한 후 호출됩니다.|
|[CWnd::OnFontChange](#onfontchange)|글꼴 리소스 풀이 변경될 때 호출됩니다.|
|[CWnd::OnGetDlgCode](#ongetdlgcode)|컨트롤에서 화살표 키 및 Tab 키 입력 자체를 처리할 수 있도록 컨트롤에 대해 호출됩니다.|
|[CWnd::OnGetMinMaxInfo](#ongetminmaxinfo)|Windows에서 최대화된 위치 또는 크기, 최소 또는 최대 추적 크기를 알고 있어야 할 때마다 호출됩니다.|
|[CWnd::OnHelpInfo](#onhelpinfo)|사용자가 F1 키를 누를 때 프레임워크에서 호출됩니다.|
|[CWnd::OnHotKey](#onhotkey)|사용자가 시스템 차원 바로 가기 키를 누를 때 호출됩니다.|
|[CWnd::OnHScroll](#onhscroll)|사용자가 `CWnd`의 가로 스크롤 막대를 클릭할 때 호출됩니다.|
|[CWnd::OnHScrollClipboard](#onhscrollclipboard)|클립보드 소유자가 클립보드 이미지를 스크롤하고, 적절한 섹션을 무효화하고, 스크롤 막대 값을 업데이트해야 하는 경우 호출됩니다.|
|[CWnd::OnIconEraseBkgnd](#oniconerasebkgnd)|`CWnd`가 최소화(아이콘)되고 아이콘을 그리기 전에 아이콘의 배경을 채워야 하는 경우 호출됩니다.|
|[CWnd::OnInitMenu](#oninitmenu)|메뉴가 활성화되려고 할 때 호출됩니다.|
|[CWnd::OnInitMenuPopup](#oninitmenupopup)|팝업 메뉴가 활성화되려고 할 때 호출됩니다.|
|[CWnd::OnInputDeviceChange](#oninputdevicechange)|시스템에서 I/O 디바이스를 추가하거나 제거할 때 호출됩니다.|
|[CWnd::OnInputLangChange](#oninputlangchange)|애플리케이션의 입력 언어가 변경된 후 호출됩니다.|
|[CWnd::OnInputLangChangeRequest](#oninputlangchangerequest)|사용자가 새 입력 언어를 선택할 때 호출됩니다.|
|[CWnd::OnKeyDown](#onkeydown)|시스템 키 이외의 키를 누를 때 호출됩니다.|
|[CWnd::OnKeyUp](#onkeyup)|시스템 키 이외의 키를 놓을 때 호출됩니다.|
|[CWnd::OnKillFocus](#onkillfocus)|`CWnd`가 입력 포커스를 잃기 직전에 호출됩니다.|
|[CWnd::OnLButtonDblClk](#onlbuttondblclk)|사용자가 왼쪽 마우스 단추를 두 번 클릭할 때 호출됩니다.|
|[CWnd::OnLButtonDown](#onlbuttondown)|사용자가 왼쪽 마우스 단추를 누를 때 호출됩니다.|
|[CWnd::OnLButtonUp](#onlbuttonup)|사용자가 왼쪽 마우스 단추를 놓을 때 호출됩니다.|
|[CWnd::OnMButtonDblClk](#onmbuttondblclk)|사용자가 마우스 가운데 단추를 두 번 클릭할 때 호출됩니다.|
|[CWnd::OnMButtonDown](#onmbuttondown)|사용자가 마우스 가운데 단추를 누를 때 호출됩니다.|
|[CWnd::OnMButtonUp](#onmbuttonup)|사용자가 마우스 가운데 단추를 놓을 때 호출됩니다.|
|[CWnd::OnMDIActivate](#onmdiactivate)|MDI 자식 창이 활성화되거나 비활성화될 때 호출됩니다.|
|[CWnd::OnMeasureItem](#onmeasureitem)|컨트롤을 만들 때 소유자 그리기 자식 콤보 상자, 목록 상자 또는 메뉴 항목에 대해 호출됩니다. `CWnd`가 Windows에 컨트롤의 크기를 알립니다.|
|[CWnd::OnMenuChar](#onmenuchar)|사용자가 현재 메뉴의 미리 정의된 니모닉과 일치하지 않는 메뉴 니모닉 문자를 누를 때 호출됩니다.|
|[CWnd::OnMenuDrag](#onmenudrag)|사용자가 메뉴 항목을 끌기 시작하면 호출됩니다.|
|[CWnd::OnMenuGetObject](#onmenugetobject)|마우스 커서를 메뉴 항목으로 가져가거나 항목의 가운데에서 항목의 위 또는 아래로 이동할 때 호출됩니다.|
|[CWnd::OnMenuRButtonUp](#onmenurbuttonup)|커서가 메뉴 항목 위에 있는 동안 사용자가 마우스 오른쪽 단추를 놓을 때 호출됩니다.|
|[CWnd::OnMenuSelect](#onmenuselect)|사용자가 메뉴 항목을 선택할 때 호출됩니다.|
|[CWnd::OnMouseActivate](#onmouseactivate)|커서가 비활성 창에 있을 때 사용자가 마우스 단추를 누르면 호출됩니다.|
|[CWnd::OnMouseHover](#onmousehover)|[TrackMouseEvent에](/windows/win32/api/winuser/nf-winuser-trackmouseevent)대한 사전 호출에 지정된 기간 동안 커서가 창의 클라이언트 영역 위로 마우스를 가져가면 호출됩니다.|
|[CWnd::OnMouseHWheel](#onmousehwheel)|현재 창이 DWM(바탕 화면 창 관리자)에 의해 구성되고 해당 창이 최대화되면 호출됩니다.|
|[CWnd::OnMouseLeave](#onmouseleave)|커서가 [TrackMouseEvent](/windows/win32/api/winuser/nf-winuser-trackmouseevent)에 대한 사전 호출에 지정된 창의 클라이언트 영역을 떠날 때 호출됩니다.|
|[CWnd::OnMouseMove](#onmousemove)|마우스 커서가 이동할 때 호출됩니다.|
|[CWnd::OnMouseWheel](#onmousewheel)|사용자가 마우스 휠을 회전할 때 호출됩니다. Windows NT 4.0 메시지 처리를 사용합니다.|
|[CWnd::OnMove](#onmove)|`CWnd`의 위치가 변경된 후 호출됩니다.|
|[CWnd::OnMoving](#onmoving)|사용자가 `CWnd` 개체를 이동하고 있음을 나타냅니다.|
|[CWnd::OnNcActivate](#onncactivate)|활성 또는 비활성 상태를 나타내려면 비클라이언트 영역을 변경해야 하는 경우 호출됩니다.|
|[CWnd::OnNcCalcSize](#onnccalcsize)|클라이언트 영역의 크기 및 위치를 계산해야 하는 경우 호출됩니다.|
|[CWnd::OnNcCreate](#onnccreate)|클라이언트가 아닌 영역을 만들 때 [OnCreate](#oncreate) 이전에 호출됩니다.|
|[CWnd::OnNcDestroy](#onncdestroy)|비클라이언트 영역을 제거할 때 호출됩니다.|
|[CWnd::OnNcHitTest](#onnchittest)|`CWnd`가 커서를 포함하거나 `SetCapture`를 사용하여 마우스 입력을 캡처한 경우 마우스가 이동할 때마다 Windows에서 호출됩니다.|
|[CWnd::OnNcLButtonDblClk](#onnclbuttondblclk)|커서가 `CWnd`의 비클라이언트 영역 내에 있는 동안 사용자가 왼쪽 마우스 단추를 두 번 클릭하면 호출됩니다.|
|[CWnd::OnNcLButtonDown](#onnclbuttondown)|커서가 `CWnd`의 비클라이언트 영역 내에 있는 동안 사용자가 왼쪽 마우스 단추를 누르면 호출됩니다.|
|[CWnd::OnNcLButtonUp](#onnclbuttonup)|커서가 `CWnd`의 비클라이언트 영역 내에 있는 동안 사용자가 왼쪽 마우스 단추를 놓으면 호출됩니다.|
|[CWnd::OnNcMButtonDblClk](#onncmbuttondblclk)|커서가 `CWnd`의 비클라이언트 영역 내에 있는 동안 사용자가 가운데 마우스 단추를 두 번 클릭하면 호출됩니다.|
|[CWnd::OnNcMButtonDown](#onncmbuttondown)|커서가 `CWnd`의 비클라이언트 영역 내에 있는 동안 사용자가 가운데 마우스 단추를 누르면 호출됩니다.|
|[CWnd::OnNcMButtonUp](#onncmbuttonup)|커서가 `CWnd`의 비클라이언트 영역 내에 있는 동안 사용자가 가운데 마우스 단추를 놓으면 호출됩니다.|
|[CWnd::OnNcMouseHover](#onncmousehover)|[TrackMouseEvent](/windows/win32/api/winuser/nf-winuser-trackmouseevent)에 대한 사전 호출에 지정된 기간 동안 커서가 창의 비클라이언트 영역 위로 마우스를 가져가면 호출됩니다.|
|[CWnd::OnNcMouseLeave](#onncmouseleave)|프레임워크는 [커서가 TrackMouseEvent](/windows/win32/api/winuser/nf-winuser-trackmouseevent)에 대한 사전 호출에 지정된 창의 비클라이언트 영역을 떠날 때 이 멤버 함수를 호출합니다.|
|[CWnd::OnNcMouseMove](#onncmousemove)|커서가 `CWnd`의 비클라이언트 영역 내에서 이동할 때 호출됩니다.|
|[CWnd::OnNcPaint](#onncpaint)|비클라이언트 영역을 그려야 하는 경우 호출됩니다.|
|[CWnd::OnNcRButtonDblClk](#onncrbuttondblclk)|커서가 `CWnd`의 비클라이언트 영역 내에 있는 동안 사용자가 오른쪽 마우스 단추를 두 번 클릭하면 호출됩니다.|
|[CWnd::OnNcRButtonDown](#onncrbuttondown)|커서가 `CWnd`의 비클라이언트 영역 내에 있는 동안 사용자가 오른쪽 마우스 단추를 누르면 호출됩니다.|
|[CWnd::OnNcRButtonUp](#onncrbuttonup)|커서가 `CWnd`의 비클라이언트 영역 내에 있는 동안 사용자가 오른쪽 마우스 단추를 놓으면 호출됩니다.|
|[CWnd::OnNcRenderingChanged](#onncrenderingchanged)|비클라이언트 영역에 대한 렌더링 정책이 변경된 경우 호출됩니다.|
|[CWnd::OnNcXButtonDblClk](#onncxbuttondblclk)|커서가 창의 비클라이언트 영역에 있는 동안 사용자가 XBUTTON1 또는 XBUTTON2를 두 번 클릭하면 호출됩니다.|
|[CWnd::OnNcXButtonDown](#onncxbuttondown)|커서가 창의 비클라이언트 영역에 있는 동안 사용자가 마우스의 XBUTTON1 또는 XBUTTON2를 누르면 호출됩니다.|
|[CWnd::OnNcXButtonUp](#onncxbuttonup)|커서가 창의 비클라이언트 영역에 있는 동안 사용자가 XBUTTON1 또는 XBUTTON2를 놓으면 호출됩니다.|
|[CWnd::OnNextMenu](#onnextmenu)|오른쪽 또는 왼쪽 화살표 키를 사용하여 메뉴 모음과 시스템 메뉴 간을 전환할 때 호출됩니다.|
|[CWnd::OnNotify](#onnotify)|해당 컨트롤 중 하나에서 이벤트가 발생했거나 컨트롤에 정보가 필요함을 부모 창에 알리기 위해 프레임워크에서 호출됩니다.|
|[CWnd::OnNotifyFormat](#onnotifyformat)|현재 창에서 WM_NOTIFY 알림 메시지의 ANSI 또는 유니코드 구조를 허용할지 여부를 결정하기 위해 호출됩니다.|
|[CWnd::OnPaint](#onpaint)|창의 일부를 다시 그리기 위해 호출됩니다.|
|[CWnd::OnPaintClipboard](#onpaintclipboard)|클립보드 뷰어의 클라이언트 영역을 다시 그려야 할 때 호출됩니다.|
|[CWnd::OnPaletteChanged](#onpalettechanged)|색상표를 사용하는 창에서 논리 팔레트를 나타내고 해당 클라이언트 영역을 업데이트하도록 허용하기 위해 호출됩니다.|
|[CWnd::OnPaletteIsChanging](#onpaletteischanging)|애플리케이션에서 논리 팔레트를 나타내려고 하는 경우 다른 애플리케이션에 알립니다.|
|[CWnd::OnParentNotify](#onparentnotify)|자식 창을 만들거나 제거할 때 또는 커서가 자식 창 위에 있는 동안 사용자가 마우스 단추를 클릭할 때 호출됩니다.|
|[CWnd::OnPowerBroadcast](#onpowerbroadcast)|전원 관리 이벤트가 발생할 때 호출됩니다.|
|[CWnd::OnQueryDragIcon](#onquerydragicon)|사용자가 최소화(아이콘)된 `CWnd`를 끌려고 할 때 호출됩니다.|
|[CWnd::OnQueryEndSession](#onqueryendsession)|사용자가 Windows 세션을 종료하도록 선택할 때 호출됩니다.|
|[CWnd::OnQueryNewPalette](#onquerynewpalette)|입력 포커스를 수신하려고 함을 `CWnd`에 알립니다.|
|[CWnd::OnQueryOpen](#onqueryopen)|`CWnd`가 아이콘이고 사용자가 아이콘을 열도록 요청할 때 호출됩니다.|
|[CWnd::OnQueryUIState](#onqueryuistate)|창에 대한 UI(사용자 인터페이스) 상태를 검색 하기 위해 호출됩니다.|
|[CWnd::OnRawInput](#onrawinput)|현재 창에서 원시 입력을 가져올 때 호출됩니다.|
|[CWnd::OnRButtonDblClk](#onrbuttondblclk)|사용자가 오른쪽 마우스 단추를 두 번 클릭할 때 호출됩니다.|
|[CWnd::OnRButtonDown](#onrbuttondown)|사용자가 오른쪽 마우스 단추를 누를 때 호출됩니다.|
|[CWnd::OnRButtonUp](#onrbuttonup)|사용자가 오른쪽 마우스 단추를 놓을 때 호출됩니다.|
|[CWnd::OnRenderAllFormats](#onrenderallformats)|소유자 애플리케이션이 제거되고 있고 모든 형식을 렌더링해야 하는 경우 호출됩니다.|
|[CWnd::OnRenderFormat](#onrenderformat)|렌더링이 지연된 특정 형식을 렌더링해야 하는 경우 클립보드 소유자에 대해 호출됩니다.|
|[CWnd::OnSessionChange](#onsessionchange)|세션 상태의 변경을 애플리케이션에 알리기 위해 호출됩니다.|
|[CWnd::OnSetCursor](#onsetcursor)|마우스 입력이 캡처되지 않고 마우스로 창 내에서 커서를 이동하는 경우 호출됩니다.|
|[CWnd::OnSetFocus](#onsetfocus)|`CWnd`가 입력 포커스를 얻은 후 호출됩니다.|
|[CWnd::OnSettingChange](#onsettingchange)|Win32 `SystemParametersInfo` 함수가 시스템 차원 설정을 변경할 때 호출됩니다.|
|[CWnd::OnShowWindow](#onshowwindow)|`CWnd`을 숨기거나 표시해야 할 때 호출됩니다.|
|[CWnd::OnSize](#onsize)|`CWnd`의 크기가 변경된 후 호출됩니다.|
|[CWnd::OnSizeClipboard](#onsizeclipboard)|클립보드 뷰어 창의 클라이언트 영역 크기가 변경된 경우 호출됩니다.|
|[CWnd::OnSizing](#onsizing)|사용자가 사각형의 크기를 조정하고 있음을 나타냅니다.|
|[CWnd::OnSpoolerStatus](#onspoolerstatus)|인쇄 관리자 큐에서 작업이 추가되거나 제거될 때마다 인쇄 관리자에서 호출됩니다.|
|[CWnd::OnStyleChanged](#onstylechanged)|[SetWindowLong](/windows/win32/api/winuser/nf-winuser-setwindowlongw) Windows 함수가 창 스타일 중 하나 이상을 변경했음을 나타냅니다.|
|[CWnd::OnStyleChanging](#onstylechanging)|[SetWindowLong](/windows/win32/api/winuser/nf-winuser-setwindowlongw) Windows 함수가 창 의 스타일 중 하나 이상을 변경하려고 했음을 나타냅니다.|
|[CWnd::OnSysChar](#onsyschar)|키 입력이 시스템 문자로 변환될 때 호출됩니다.|
|[CWnd::OnSysColorChange](#onsyscolorchange)|시스템 색 설정이 변경될 때 모든 최상위 창에 대해 호출됩니다.|
|[CWnd::OnSysCommand](#onsyscommand)|사용자가 컨트롤 메뉴에서 명령을 선택할 때 또는 사용자가 최대화 또는 최소화 단추를 선택할 때 호출됩니다.|
|[CWnd::OnSysDeadChar](#onsysdeadchar)|키 입력이 시스템 데드 문자(예: 악센트 문자)로 변환될 때 호출됩니다.|
|[CWnd::OnSysKeyDown](#onsyskeydown)|사용자가 Alt 키를 누르고 있다가 다른 키를 누를 때 호출됩니다.|
|[CWnd::OnSysKeyUp](#onsyskeyup)|사용자가 Alt 키를 누른 상태에서 눌렀던 키를 놓을 때 호출됩니다.|
|[CWnd::OnTCard](#ontcard)|사용자가 만들 수 있는 단추를 클릭하면 호출됩니다.|
|[CWnd::OnTimeChange](#ontimechange)|시스템 시간이 변경된 후 모든 최상위 창에 대해 호출됩니다.|
|[CWnd::OnTimer](#ontimer)|[SetTimer에](#settimer)지정된 각 간격 다음에 호출됩니다.|
|[CWnd::OnTouchInput](#ontouchinput)|Windows Touch에서 단일 입력을 처리합니다.|
|[CWnd::OnTouchInputs](#ontouchinputs)|Windows Touch에서 입력을 처리합니다.|
|[CWnd::OnUniChar](#onunichar)|키를 누를 때 호출됩니다. 즉, 현재 창에는 키보드 포커스가 있고 [WM_KEYDOWN](/windows/win32/inputdev/wm-keydown) 메시지가 [TranslateMessage](/windows/win32/api/winuser/nf-winuser-translatemessage) 기능으로 변환됩니다.|
|[CWnd::OnUnInitMenuPopup](#onuninitmenupopup)|드롭다운 메뉴나 하위 메뉴가 제거될 때 호출됩니다.|
|[CWnd::OnUpdateUIState](#onupdateuistate)|지정된 창 및 모든 자식 창에 대한 UI(사용자 인터페이스) 상태를 변경하기 위해 호출됩니다.|
|[CWnd::OnUserChanged](#onuserchanged)|사용자가 로그온하거나 로드오프한 후에 호출됩니다.|
|[CWnd::OnVKeyToItem](#onvkeytoitem)|WM_KEYDOWN 메시지에 대한 응답으로 `CWnd` 소유한 목록 [WM_KEYDOWN](#onkeydown) 상자에 의해 호출됩니다.|
|[CWnd::OnVScroll](#onvscroll)|사용자가 창의 세로 스크롤 막대를 클릭할 때 호출됩니다.|
|[CWnd::OnVScrollClipboard](#onvscrollclipboard)|소유자가 클립보드 이미지를 스크롤하고, 적절한 섹션을 무효화하고, 스크롤 막대 값을 업데이트해야 하는 경우 호출됩니다.|
|[CWnd::OnWindowPosChanged](#onwindowposchanged)|[SetWindowPos](#setwindowpos) 또는 다른 창 관리 함수에 대한 호출의 결과로 크기, 위치 또는 Z 순서가 변경된 경우 호출됩니다.|
|[CWnd::OnWindowPosChanging](#onwindowposchanging)|[SetWindowPos](#setwindowpos) 또는 다른 창 관리 함수에 대한 호출의 결과로 크기, 위치 또는 Z 순서가 변경될 때 호출됩니다.|
|[CWnd::OnWinIniChange](#onwininichange)|Windows 초기화 파일 WIN.INI가 변경된 후 모든 최상위 창에 대해 호출됩니다.|
|[CWnd::OnWndMsg](#onwndmsg)|Windows 메시지가 처리되었는지를 나타냅니다.|
|[CWnd::OnXButtonDblClk](#onxbuttondblclk)|커서가 창의 클라이언트 영역에 있는 동안 사용자가 XBUTTON1 또는 XBUTTON2를 두 번 클릭하면 호출됩니다.|
|[CWnd::OnXButtonDown](#onxbuttondown)|커서가 창의 클라이언트 영역에 있는 동안 사용자가 XBUTTON1 또는 XBUTTON2를 누르면 호출됩니다.|
|[CWnd::OnXButtonUp](#onxbuttonup)|커서가 창의 클라이언트 영역에 있는 동안 사용자가 XBUTTON1 또는 XBUTTON2를 놓으면 호출됩니다.|
|[CWnd::PostNcDestroy](#postncdestroy)|이 가상 함수는 창이 소멸된 후 기본 [OnNcDestroy](#onncdestroy) 함수에 의해 호출됩니다.|
|[CWnd::ReflectChildNotify](#reflectchildnotify)|소스에 대한 메시지를 반영하는 도우미 함수입니다.|
|[CWnd::ReflectLastMsg](#reflectlastmsg)|자식 창에 대한 마지막 메시지를 반영합니다.|
|[CWnd::ResizeDynamicLayout](#resizedynamiclayout)|창에 대해 동적 레이아웃을 사용하도록 설정한 경우 창 크기가 변경되면 자식 창의 레이아웃을 조정하기 위해 프레임워크에서 호출됩니다.|
|[CWnd::WindowProc](#windowproc)|`CWnd`에 창 프로시저를 제공합니다. 기본값은 메시지 맵을 통해 메시지를 디스패치합니다.|

### <a name="public-operators"></a>Public 연산자

|속성|Description|
|----------|-----------------|
|[CWnd::operator HWND](#operator_hwnd)|창에 대한 핸들을 가져오려면 호출합니다.|
|[CWnd::연산자 !=](#operator_neq)|에서 핸들이 [m_hWnd](#m_hwnd)창이 같지 않은지 확인합니다.|
|[CWnd::operator ==](#operator_eq_eq)|의 핸들이 [m_hWnd](#m_hwnd)창과 동일한지 확인합니다.|

### <a name="public-data-members"></a>공용 데이터 멤버

|속성|Description|
|----------|-----------------|
|[CWnd::m_hWnd](#m_hwnd)|이에 `CWnd`연결된 HWND를 나타냅니다.|

## <a name="remarks"></a>설명

`CWnd` 개체는 Windows 창과 다르지만 둘이 밀접하게 연결되어 있습니다. `CWnd` 개체는 `CWnd` 생성자 및 소멸자로 만들거나 제거합니다. 반면에 Windows 창은 `Create` 구성원 함수에 의해 만들어지고 가상 소멸자에 의해 소멸되는 `CWnd` Windows 내부의 데이터 구조입니다. [DestroyWindow](#destroywindow) 함수는 개체를 손상시키지 않고 Windows 창을 삭제합니다.

클래스와 메시지 맵 메커니즘은 `CWnd` `WndProc` 함수를 숨깁니다. 들어오는 Windows 알림 메시지는 메시지 맵을 통해 적절한 `CWnd` **메시지 켜기**<em>멤버</em> 기능으로 자동으로 라우팅됩니다. 파생 된 클래스에서 멤버의 특정 메시지를 처리 하려면 **메시지 켜기**<em>멤버</em> 함수를 재정의 합니다.

또한 `CWnd` 클래스를 사용하여 애플리케이션에 대한 Windows 자식 창을 만들 수 있습니다. `CWnd`에서 클래스를 파생시킨 다음 파생 클래스에 멤버 변수를 추가하여 애플리케이션 관련 데이터를 저장합니다. 파생 클래스에서 메시지 처리기 멤버 함수 및 메시지 맵을 구현하여 메시지가 창에 전달될 때 수행되는 작업을 지정합니다.

자식 창은 두 단계로 만듭니다. 먼저 `CWnd` 생성자 호출하여 개체를 `CWnd` 생성한 다음 create member 함수를 호출하여 자식 창을 만들고 개체에 [Create](#create) `CWnd` 연결합니다.

사용자가 자식 창을 종료하면 `CWnd` 개체를 제거하거나 `DestroyWindow` 멤버 함수를 호출하여 창을 제거하고 해당 데이터 구조를 제거합니다.

Microsoft Foundation Class 라이브러리 내에서 추가 클래스를 `CWnd`에서 파생시켜 특정 창 형식을 제공합니다. [CFrameWnd,](../../mfc/reference/cframewnd-class.md) [CMDIFrameWnd,](../../mfc/reference/cmdiframewnd-class.md) [CMDIChildWnd,](../../mfc/reference/cmdichildwnd-class.md) [CView](../../mfc/reference/cview-class.md)및 [CDialog를](../../mfc/reference/cdialog-class.md)포함한 많은 클래스는 추가 파생을 위해 설계되었습니다. [CButton과](../../mfc/reference/cbutton-class.md)같은 `CWnd`에서 파생된 제어 클래스를 직접 사용하거나 클래스의 추가 파생에 사용할 수 있습니다.

사용에 `CWnd`대한 자세한 내용은 [프레임 창](../../mfc/frame-windows.md) 및 창 [개체를](../../mfc/window-objects.md)참조하십시오.

## <a name="inheritance-hierarchy"></a>상속 계층 구조

[CObject](../../mfc/reference/cobject-class.md)

[CCmdTarget](../../mfc/reference/ccmdtarget-class.md)

`CWnd`

## <a name="requirements"></a>요구 사항

**헤더:** afxwin.h

## <a name="cwndaccdodefaultaction"></a><a name="accdodefaultaction"></a>CWnd::accDoDefaultAction

개체의 기본 동작을 수행하기 위해 프레임워크에서 호출됩니다.

```
virtual HRESULT accDoDefaultAction(VARIANT varChild);
```

### <a name="parameters"></a>매개 변수

*바차일드*<br/>
호출할 기본 작업이 개체의 작업인지 아니면 개체의 자식 요소 중 하나인지 지정합니다. 이 매개 변수는 CHILDID_SELF(개체의 기본 작업을 수행하기 위해) 또는 자식 ID(개체의 자식 요소 중 하나의 기본 동작을 수행)일 수 있습니다.

### <a name="return-value"></a>Return Value

성공 시 S_OK 반환하고 실패시 COM 오류 코드를 반환합니다. [IAccessible::accDoDefaultAction](/windows/win32/api/oleacc/nf-oleacc-iaccessible-accdodefaultaction) Windows SDK에서 **반환 값을** 참조하십시오.

### <a name="remarks"></a>설명

이 기능은 MFC의 [활성 접근성](/windows/win32/WinAuto/microsoft-active-accessibility) 지원의 일부입니다.

`CWnd`-derived 클래스에서 이 함수를 재정의하여 오브젝트의 기본 작업을 수행합니다. 자세한 내용은 Windows SDK에서 [IAccessible::accDoDefaultAction을](/windows/win32/api/oleacc/nf-oleacc-iaccessible-accdodefaultaction) 참조하십시오.

## <a name="cwndacchittest"></a><a name="acchittest"></a>CWnd:::accHitTest

화면의 지정된 지점에서 자식 요소나 자식 개체를 검색하기 위해 프레임워크에서 호출됩니다.

```
virtual HRESULT accHitTest(
    long xLeft,
    long yTop,
    VARIANT* pvarChild);
```

### <a name="parameters"></a>매개 변수

*x왼쪽*<br/>
(화면 단위에서) 테스트 할 점의 X 좌표.

*yTop*<br/>
(화면 단위에서) 테스트 할 점의 Y 좌표.

*pvarChild*<br/>
*xLeft* 및 *yTop에*의해 지정된 지점에서 개체를 식별하는 정보를 수신합니다. [I액세스::accHitTest](/windows/win32/api/oleacc/nf-oleacc-iaccessible-acchittest) 에서 windows SDK에서 *pvarID를* 참조하십시오.

### <a name="return-value"></a>Return Value

성공 시 S_OK 반환하고 실패시 COM 오류 코드를 반환합니다. Windows **Return Values** SDK에서 `IAccessible::accHitTest` 반환 값을 참조하십시오.

### <a name="remarks"></a>설명

이 기능은 MFC의 [활성 접근성](/windows/win32/WinAuto/microsoft-active-accessibility) 지원의 일부입니다.

창이 없는 사용자 `CWnd`인터페이스 요소(MFC가 처리하는 창 없는 ActiveX 컨트롤 제외)가 있는 경우 파생 클래스에서 이 함수를 재정의합니다.

자세한 내용은 Windows SDK에서 [IAccessible::accHitTest를](/windows/win32/api/oleacc/nf-oleacc-iaccessible-acchittest) 참조하십시오.

## <a name="cwndacclocation"></a><a name="acclocation"></a>CWnd::acclocation

지정된 개체의 현재 화면 위치를 검색하기 위해 프레임워크에서 호출됩니다.

```
virtual HRESULT accLocation(
    long* pxLeft,
    long* pyTop,
    long* pcxWidth,
    long* pcyHeight,
    VARIANT varChild);
```

### <a name="parameters"></a>매개 변수

*pxLeft*<br/>
화면 단위에서 오브젝트의 왼쪽 위 모서리의 x 좌표를 받습니다.

*파이 탑*<br/>
화면 단위에서 오브젝트의 왼쪽 위 모서리의 y 좌표를 받습니다.

*pcx폭*<br/>
화면 단위에서 개체의 너비를 받습니다.

*pcy높이*<br/>
화면 단위에서 개체의 높이를 받습니다.

*바차일드*<br/>
검색할 위치가 개체의 위치인지 아니면 개체의 자식 요소 중 하나인지 지정합니다. 이 매개 변수는 CHILDID_SELF(개체에 대한 정보를 얻기 위해) 또는 자식 ID(개체의 자식 요소에 대한 정보를 얻기 위해)일 수 있습니다.

### <a name="return-value"></a>Return Value

성공 시 S_OK 반환하고 실패시 COM 오류 코드를 반환합니다. Windows **Return Values** SDK에서 `IAccessible::accLocation` 반환 값을 참조하십시오.

### <a name="remarks"></a>설명

창이 없는 사용자 `CWnd`인터페이스 요소(MFC가 처리하는 창 없는 ActiveX 컨트롤 제외)가 있는 경우 파생 클래스에서 이 함수를 재정의합니다.

자세한 내용은 Windows `IAccessible::accLocation` SDK를 참조하십시오.

## <a name="cwndaccnavigate"></a><a name="accnavigate"></a>CWnd::accNavigate

컨테이너 내에서 다른 사용자 인터페이스 요소로 트래버스하고 가능할 경우 개체를 검색하기 위해 프레임워크에서 호출됩니다.

```
virtual HRESULT accNavigate(
    long navDir,
    VARIANT varStart,
    VARIANT* pvarEndUpAt);
```

### <a name="parameters"></a>매개 변수

*나디르 (것)나디*<br/>
탐색 방향을 지정합니다. [IAccessible::acc에서](/windows/win32/api/oleacc/nf-oleacc-iaccessible-accnavigate) windows SDK에서 *navDir를* 참조하십시오.

*바스타트*<br/>
시작 개체를 지정합니다. Windows SDK에서 `IAccessible::accNavigate` *varStart를* 참조하십시오.

*프바엔드업*<br/>
대상 사용자 인터페이스 개체에 대한 정보를 수신합니다. 윈도우 SDK에서 `IAccessible::accNavigate` *pvarEnd를* 참조하십시오.

### <a name="return-value"></a>Return Value

성공 시 S_OK 반환하고 실패시 COM 오류 코드를 반환합니다. Windows **Return Values** SDK에서 `IAccessible::accNavigate` 반환 값을 참조하십시오.

### <a name="remarks"></a>설명

이 기능은 MFC의 [활성 접근성](/windows/win32/WinAuto/microsoft-active-accessibility) 지원의 일부입니다.

창이 없는 사용자 `CWnd`인터페이스 요소(MFC가 처리하는 창 없는 ActiveX 컨트롤 제외)가 있는 경우 파생 클래스에서 이 함수를 재정의합니다.

자세한 내용은 [IAccessible::acc탐색](/windows/win32/api/oleacc/nf-oleacc-iaccessible-accnavigate) 을 참조하십시오.

## <a name="cwndaccselect"></a><a name="accselect"></a>CWnd::accSelect

선택 영역을 수정하거나 지정된 개체의 키보드 포커스를 이동하기 위해 프레임워크에서 호출됩니다.

```
virtual HRESULT accSelect(
    long flagsSelect,
    VARIANT varChild);
```

### <a name="parameters"></a>매개 변수

*플래그 선택*<br/>
현재 선택 영역 또는 포커스를 변경하는 방법을 지정합니다. [I액세스::acc에서](/windows/win32/api/oleacc/nf-oleacc-iaccessible-accselect) Windows SDK에서 *선택*

*바차일드*<br/>
선택할 객체를 지정합니다. 이 매개 변수는 CHILDID_SELF(개체 자체를 선택) 또는 자식 ID(개체의 자식 중 하나를 선택)일 수 있습니다.

### <a name="return-value"></a>Return Value

성공 시 S_OK 반환하고 실패시 COM 오류 코드를 반환합니다. Windows **Return Values** SDK에서 `IAccessible::accSelect` 반환 값을 참조하십시오.

### <a name="remarks"></a>설명

이 기능은 MFC의 [활성 접근성](/windows/win32/WinAuto/microsoft-active-accessibility) 지원의 일부입니다.

창이 없는 사용자 `CWnd`인터페이스 요소(MFC가 처리하는 창 없는 ActiveX 컨트롤 제외)가 있는 경우 파생 클래스에서 이 함수를 재정의합니다.

자세한 내용은 Windows SDK에서 [IAccessible::accSelect를](/windows/win32/api/oleacc/nf-oleacc-iaccessible-accselect) 참조하십시오.

## <a name="cwndanimatewindow"></a><a name="animatewindow"></a>CWnd::애니메이션창

창을 표시하거나 숨길 때 특수 효과를 생성합니다.

```
BOOL AnimateWindow(
    DWORD dwTime,
    DWORD dwFlags);
```

### <a name="parameters"></a>매개 변수

*dw시간*<br/>
애니메이션을 재생하는 데 걸리는 시간을 밀리초 단위로 지정합니다. 일반적으로 애니메이션을 재생하는 데 200밀리초가 걸립니다.

*dwFlags*<br/>
애니메이션 유형을 지정합니다. 가능한 값의 전체 목록은 [AnimateWindow](/windows/win32/api/winuser/nf-winuser-animatewindow)를 참조하십시오.

### <a name="return-value"></a>Return Value

함수가 성공하면 0이 아니고 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

이 멤버 함수는 Windows SDK에 설명된 대로 [기능 AnimateWindow의](/windows/win32/api/winuser/nf-winuser-animatewindow)기능을 에뮬레이트합니다.

## <a name="cwndarrangeiconicwindows"></a><a name="arrangeiconicwindows"></a>CWnd::어레인지아이콘윈도우

모든 최소화된(아이콘) 자식 창을 정렬합니다.

```
UINT ArrangeIconicWindows();
```

### <a name="return-value"></a>Return Value

함수가 성공한 경우 아이콘의 한 행의 높이입니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

이 멤버 기능은 전체 화면을 덮는 바탕 화면 창에 아이콘을 정렬합니다. [GetDesktopWindow](#getdesktopwindow) 멤버 함수는 데스크톱 창 개체에 대한 포인터를 검색합니다.

MDI 클라이언트 창에서 상징적인 MDI 자식 창을 정렬하려면 [CMDIFrameWnd::MDIIcon정렬을](../../mfc/reference/cmdiframewnd-class.md#mdiiconarrange)호출합니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#66](../../mfc/reference/codesnippet/cpp/cwnd-class_1.cpp)]

## <a name="cwndattach"></a><a name="attach"></a>CWnd::연결

개체에 Windows 창을 `CWnd` 연결합니다.

```
BOOL Attach(HWND hWndNew);
```

### <a name="parameters"></a>매개 변수

*hWndNew*<br/>
Windows 창에 대한 핸들을 지정합니다.

### <a name="return-value"></a>Return Value

성공하면 0이 아니고, 그렇지 않으면 0입니다.

### <a name="example"></a>예제

이 예제에서는 연결 및 분리를 사용하여 MDI 클라이언트 창에 매핑하는 방법을 보여 주었습니다.

[!code-cpp[NVC_MFCWindowing#67](../../mfc/reference/codesnippet/cpp/cwnd-class_2.h)]

[!code-cpp[NVC_MFCWindowing#68](../../mfc/reference/codesnippet/cpp/cwnd-class_3.cpp)]

[!code-cpp[NVC_MFCWindowing#69](../../mfc/reference/codesnippet/cpp/cwnd-class_4.cpp)]

## <a name="cwndbeginmodalstate"></a><a name="beginmodalstate"></a>CWnd::시작모달스테이트

프레임 창을 모달로 만들기 위해 이 멤버 함수를 호출합니다.

```
virtual void BeginModalState();
```

## <a name="cwndbeginpaint"></a><a name="beginpaint"></a>CWnd::시작 페인트

`CWnd` 페인팅을 준비하고 `PAINTSTRUCT` 데이터 구조를 그림에 대한 정보로 채웁니다.

```
CDC* BeginPaint(LPPAINTSTRUCT lpPaint);
```

### <a name="parameters"></a>매개 변수

*lpPaint*<br/>
페인팅 정보를 수신하는 [PAINTSTRUCT](/windows/win32/api/winuser/ns-winuser-paintstruct) 구조를 가리킵니다.

### <a name="return-value"></a>Return Value

에 대한 장치 `CWnd`컨텍스트를 식별합니다. 포인터는 일시적일 수 있으며 [EndPaint](#endpaint)의 범위를 벗어나저장해서는 안 됩니다.

### <a name="remarks"></a>설명

페인트 구조에는 업데이트 영역을 완전히 둘러싸는 가장 작은 사각형이 있는 RECT 데이터 구조와 배경이 지워졌는지 여부를 지정하는 플래그가 포함되어 있습니다.

업데이트 영역은 [무효화,](#invalidate) [무효화Rect](#invalidaterect)또는 [무효화Rgn](#invalidatergn) 멤버 함수및 클라이언트 영역에 영향을 주는 다른 작업의 크기, 이동, 생성, 스크롤 또는 수행한 후 시스템에 의해 설정됩니다. 업데이트 영역이 지워짐으로 표시되면 `BeginPaint` [WM_ONERASEBKGND](#onerasebkgnd) 메시지를 보냅니다.

WM_PAINT 메시지에 `BeginPaint` 대한 응답을 제외하고 멤버 [WM_PAINT](#onpaint) 함수를 호출하지 마십시오. `BeginPaint` 멤버 함수에 대한 각 호출에는 [EndPaint](#endpaint) 멤버 함수에 대한 일치하는 호출이 있어야 합니다. 페인팅할 영역에 있는 경우 `BeginPaint` 멤버 함수는 자동으로 캐리트가 지워지지 않도록 캐시를 숨깁니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#70](../../mfc/reference/codesnippet/cpp/cwnd-class_5.cpp)]

## <a name="cwndbinddefaultproperty"></a><a name="binddefaultproperty"></a>CWnd::바인딩기본 속성

형식 라이브러리에 표시된 것처럼 호출 하는 개체의 기본 단순 바인딩된 속성(예: 편집 컨트롤)을 DataSource, UserName, 암호 및 데이터 원본 컨트롤의 SQL 속성에 의해 정의 된 기본 커서에 바인딩합니다.

```cpp
void BindDefaultProperty(
    DISPID dwDispID,
    VARTYPE vtProp,
    LPCTSTR szFieldName,
    CWnd* pDSCWnd);
```

### <a name="parameters"></a>매개 변수

*dwDispID*<br/>
데이터 원본 컨트롤에 바인딩할 데이터 바인딩된 컨트롤에서 속성의 DISPID를 지정합니다.

*vtProp*<br/>
바인딩할 속성의 유형(예: VT_BSTR, VT_VARIANT 등)을 지정합니다.

*szField네임*<br/>
속성이 바인딩될 데이터 원본 컨트롤에서 제공하는 커서에서 열의 이름을 지정합니다.

*pDSCWnd*<br/>
속성이 바인딩될 데이터 원본 컨트롤을 호스트하는 창을 가리킵니다. DCS 호스트 창의 리소스 ID를 `GetDlgItem` 사용하여 이 포인터를 검색합니다.

### <a name="remarks"></a>설명

이 `CWnd` 함수를 호출하는 개체는 데이터 바인딩된 컨트롤이어야 합니다.

### <a name="example"></a>예제

`BindDefaultProperty`다음 컨텍스트에서 사용할 수 있습니다.

[!code-cpp[NVC_MFC_AxDataBinding#2](../../mfc/reference/codesnippet/cpp/cwnd-class_7.cpp)]

## <a name="cwndbindproperty"></a><a name="bindproperty"></a>CWnd::바인드속성

데이터 바인딩 컨트롤(예: 그리드 컨트롤)에 커서 바인딩 속성을 데이터 원본 컨트롤에 바인딩하고 MFC 바인딩 관리자와의 관계를 등록합니다.

```cpp
void BindProperty(
    DISPID dwDispId,
    CWnd* pWndDSC);
```

### <a name="parameters"></a>매개 변수

*dwDispId*<br/>
데이터 원본 컨트롤에 바인딩할 데이터 바인딩된 컨트롤에서 속성의 DISPID를 지정합니다.

*pWndDSC*<br/>
속성이 바인딩될 데이터 원본 컨트롤을 호스트하는 창을 가리킵니다. DCS 호스트 창의 리소스 ID를 `GetDlgItem` 사용하여 이 포인터를 검색합니다.

### <a name="remarks"></a>설명

이 `CWnd` 함수를 호출하는 개체는 데이터 바인딩된 컨트롤이어야 합니다.

### <a name="example"></a>예제

`BindProperty`다음 컨텍스트에서 사용할 수 있습니다.

[!code-cpp[NVC_MFC_AxDataBinding#4](../../mfc/reference/codesnippet/cpp/cwnd-class_9.cpp)]

## <a name="cwndbringwindowtotop"></a><a name="bringwindowtotop"></a>CWnd::BringWindowToTop

`CWnd`를 겹치는 창 스택의 맨 위로 이동합니다.

```cpp
void BringWindowToTop();
```

### <a name="remarks"></a>설명

또한 `BringWindowToTop`은 팝업, 최상위 및 MDI 자식 창을 활성화합니다. `BringWindowToTop` 멤버 함수를 사용하여 겹치는 창에 의해 부분적으로 또는 완전히 가려진 모든 창을 발견해야 합니다.

이 함수는 Win32 [BringWindowToTop](/windows/win32/api/winuser/nf-winuser-bringwindowtotop) 함수를 호출합니다. [SetWindowPos](#setwindowpos) 함수를 호출하여 Z 순서로 창의 위치를 변경합니다. `BringWindowToTop` 함수는 창 스타일을 변경하여 최상위 창으로 만들지 않습니다. 자세한 내용은 [HWND_TOP HWND_TOPMOST 차이점을](https://devblogs.microsoft.com/oldnewthing/?p=33263) 참조하십시오.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#71](../../mfc/reference/codesnippet/cpp/cwnd-class_10.cpp)]

## <a name="cwndcalcwindowrect"></a><a name="calcwindowrect"></a>CWnd::CalcWindowRect

지정된 클라이언트 사각형을 포함할 수 있는 창 사각형을 계산합니다.

```
virtual void CalcWindowRect(
    LPRECT lpClientRect,
    UINT nAdjustType = adjustBorder);
```

### <a name="parameters"></a>매개 변수

*lp클라이언트렉트*<br/>
【인, 아웃】 사각형 구조에 대한 포인터입니다. 입력시 이 구조에는 클라이언트 사각형이 포함됩니다. 메서드가 완료되면 이 구조체에는 지정된 클라이언트 사각형을 포함할 수 있는 창 사각형이 포함됩니다.

*n조정유형*<br/>
【인】 WS_EX_CLIENTEDGE `CWnd::adjustBorder` 스타일없이 창 좌표를 계산하는 데 사용합니다. 그렇지 않으면 `CWnd::adjustOutside`을 사용합니다.

### <a name="remarks"></a>설명

계산된 창 사각형의 크기에는 메뉴 막대에 대한 공간이 포함되지 않습니다.

사용 제한 에 대한 자세한 내용은 [adjustWindowRectEx](/windows/win32/api/winuser/nf-winuser-adjustwindowrectex)를 참조하십시오.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#72](../../mfc/reference/codesnippet/cpp/cwnd-class_11.cpp)]

## <a name="cwndcanceltooltips"></a><a name="canceltooltips"></a>CWnd::취소도구 팁

도구 설명이 현재 표시되는 경우 이 멤버 함수를 호출하여 화면에서 도구 팁을 제거합니다.

```
static void PASCAL CancelToolTips(BOOL bKeys = FALSE);
```

### <a name="parameters"></a>매개 변수

*b키*<br/>
TRUE 키를 누를 때 도구 팁을 취소하고 상태 표시줄 텍스트를 기본값으로 설정합니다. 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

> [!NOTE]
> 이 멤버 함수를 사용하면 코드에서 관리하는 도구 팁에는 영향을 주지 않습니다. [CWnd::EnableToolTips에서](#enabletooltips)관리하는 도구 팁 컨트롤에만 영향을 줍니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#73](../../mfc/reference/codesnippet/cpp/cwnd-class_12.cpp)]

## <a name="cwndcenterwindow"></a><a name="centerwindow"></a>CWnd::센터윈도우

부모를 기준으로 창을 가운데 맞춤합니다.

```cpp
void CenterWindow(CWnd* pAlternateOwner = NULL);
```

### <a name="parameters"></a>매개 변수

*pAlternate소유자*<br/>
부모 창 이 아닌 가운데가 될 대체 창에 대한 포인터입니다.

### <a name="remarks"></a>설명

일반적으로 [CDialog::OnInitDialog에서](../../mfc/reference/cdialog-class.md#oninitdialog) 응용 프로그램의 주 창을 기준으로 대화 상자를 중심으로 합니다. 기본적으로 함수는 상위 창을 기준으로 자식 창을 중심으로 하고 소유자를 기준으로 팝업 창을 입력합니다. 팝업 창이 소유되지 않은 경우 화면을 기준으로 중앙에 배치됩니다. 소유자 또는 부모가 아닌 특정 창을 기준으로 창을 가운데에 두려면 *pAlternateOwner* 매개 변수를 유효한 창으로 설정할 수 있습니다. 화면을 기준으로 중심을 강제하려면 [CWnd::GetDesktopWindow에서](#getdesktopwindow) 반환된 값을 *대체 소유자로*전달합니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#74](../../mfc/reference/codesnippet/cpp/cwnd-class_13.cpp)]

## <a name="cwndchangeclipboardchain"></a><a name="changeclipboardchain"></a>CWnd::변경 클립보드 체인

클립보드 `CWnd` 뷰어 체인에서 제거하고 *hWndNext가* 지정한 창을 체인의 `CWnd` 상위 하위 항목으로 만듭니다.

```
BOOL ChangeClipboardChain(HWND hWndNext);
```

### <a name="parameters"></a>매개 변수

*hWnd다음*<br/>
클립보드 뷰어 `CWnd` 체인에 따르는 창을 식별합니다.

### <a name="return-value"></a>Return Value

성공하면 0이 아니고, 그렇지 않으면 0입니다.

## <a name="cwndcheckdlgbutton"></a><a name="checkdlgbutton"></a>CWnd::체크Dlg버튼

단추를 선택(옆에 확인 표시 배치)하거나 단추를 지우거나(확인 표시를 제거) 3상태 단추의 상태를 변경합니다.

```cpp
void CheckDlgButton(
    int nIDButton,
    UINT nCheck);
```

### <a name="parameters"></a>매개 변수

*니드 버튼*<br/>
수정할 단추를 지정합니다.

*nCheck*<br/>
취할 작업을 지정합니다. *nCheck가* 0이 아닌 `CheckDlgButton` 경우 멤버 함수는 단추 옆에 확인 표시를 배치합니다. 0이면 확인 표시가 제거됩니다. 세 상태 단추의 경우 *nCheck가* 2인 경우 단추 상태가 확정되지 않습니다.

### <a name="remarks"></a>설명

함수는 `CheckDlgButton` 지정된 단추에 [BM_SETCHECK](/windows/win32/Controls/bm-setcheck) 메시지를 보냅니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#75](../../mfc/reference/codesnippet/cpp/cwnd-class_14.cpp)]

## <a name="cwndcheckradiobutton"></a><a name="checkradiobutton"></a>CWnd::체크라디오 버튼

그룹에서 지정된 라디오 단추를 선택하고 그룹의 다른 모든 라디오 단추를 선택(확인 표시를 제거)합니다.

```cpp
void CheckRadioButton(
    int nIDFirstButton,
    int nIDLastButton,
    int nIDCheckButton);
```

### <a name="parameters"></a>매개 변수

*니드퍼스트버튼*<br/>
그룹의 첫 번째 라디오 단추의 정수 식별자를 지정합니다.

*니드라스트 버튼*<br/>
그룹의 마지막 라디오 단추의 정수 식별자를 지정합니다.

*니드 체크 버튼*<br/>
확인할 라디오 단추의 정수 식별자를 지정합니다.

### <a name="remarks"></a>설명

이 `CheckRadioButton` 함수는 지정된 라디오 단추에 [BM_SETCHECK](/windows/win32/Controls/bm-setcheck) 메시지를 보냅니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#76](../../mfc/reference/codesnippet/cpp/cwnd-class_15.cpp)]

## <a name="cwndchildwindowfrompoint"></a><a name="childwindowfrompoint"></a>CWnd::차일드윈도우From포인트

지정된 점을 `CWnd` 포함하는 하위 창(있는 경우)을 결정합니다.

```
CWnd* ChildWindowFromPoint(POINT point) const;

CWnd* ChildWindowFromPoint(
    POINT point,
    UINT nFlags) const;
```

### <a name="parameters"></a>매개 변수

*지점*<br/>
테스트할 점의 클라이언트 좌표를 지정합니다.

*Nflags*<br/>
건너뛸 하위 창을 지정합니다. 이 매개 변수는 다음 값의 조합일 수 있습니다.

|값|의미|
|-----------|-------------|
|CWP_ALL|자식 창을 건너뛰지 마십시오.|
|CWP_SKIPINVISIBLE|보이지 않는 자식 창 건너뛰기|
|CWP_SKIPDISABLED|비활성화된 자식 창 건너뛰기|
|CWP_SKIPTRANSPARENT|투명 한 자식 창 건너뛰기|

### <a name="return-value"></a>Return Value

점을 포함하는 하위 창을 식별합니다. 지정된 지점이 클라이언트 영역 외부에 있는 경우 NULL입니다. 포인트가 클라이언트 영역 내에 있지만 자식 창 내에 `CWnd` 포함되지 않은 경우 반환됩니다.

이 멤버 함수는 지정된 점을 포함하는 숨겨진 또는 비활성화된 자식 창을 반환합니다.

두 개 이상의 창에는 지정된 점이 포함될 수 있습니다. 그러나 이 함수는 `CWnd`포인트가 포함된 첫 번째 창의 *만 반환합니다.

※ `CWnd`반품 된 것은 일시적일 수 있으며 나중에 사용할 수 있도록 보관하지 않아야합니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#77](../../mfc/reference/codesnippet/cpp/cwnd-class_16.cpp)]

## <a name="cwndclienttoscreen"></a><a name="clienttoscreen"></a>CWnd::클라이언트토스크린

디스플레이에서 지정된 점이나 사각형의 클라이언트 좌표를 화면 좌표로 변환합니다.

```cpp
void ClientToScreen(LPPOINT lpPoint) const;  void ClientToScreen(LPRECT lpRect) const;
```

### <a name="parameters"></a>매개 변수

*lpPoint*<br/>
변환할 클라이언트 좌표를 포함하는 [POINT 구조](/windows/win32/api/windef/ns-windef-point) 또는 `CPoint` 개체를 가리킵니다.

*Lprect*<br/>
변환할 클라이언트 좌표를 `CRect` 포함하는 [RECT 구조](/windows/win32/api/windef/ns-windef-rect) 또는 개체를 가리킵니다.

### <a name="remarks"></a>설명

멤버 `ClientToScreen` 함수는 새 화면 좌표를 `RECT` 계산하기 `CPoint` `CRect` 위해 *lpPoint* 또는 *lpRect에* 의해 가리키는 `POINT` 또는 구조또는 객체의 클라이언트 좌표를 사용합니다. 그런 다음 구조의 좌표를 새 좌표로 바꿉습니다. 새 화면 좌표는 시스템 디스플레이의 왼쪽 위 모서리를 기준으로 합니다.

멤버 `ClientToScreen` 함수는 지정된 점 또는 사각형이 클라이언트 좌표에 있다고 가정합니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#78](../../mfc/reference/codesnippet/cpp/cwnd-class_17.cpp)]

## <a name="cwndclosewindow"></a><a name="closewindow"></a>CWnd::닫기 창

창을 최소화합니다.

```cpp
void CloseWindow();
```

### <a name="remarks"></a>설명

이 멤버 함수는 Windows SDK에 설명된 대로 [CloseWindow](/windows/win32/api/winuser/nf-winuser-closewindow)기능의 기능을 에뮬레이트합니다.

## <a name="cwndcontinuemodal"></a><a name="continuemodal"></a>CWnd::계속모달

이 멤버 함수는 [RunModalLoop에서](#runmodalloop) 호출하여 모달 상태를 종료해야 하는 시기를 결정합니다.

```
virtual BOOL ContinueModal();
```

### <a name="return-value"></a>Return Value

모달 루프를 계속하려면 영하지 않습니다. [EndModalLoop가](#endmodalloop) 호출될 때 0입니다.

### <a name="remarks"></a>설명

기본적으로 호출될 때까지 `EndModalLoop` 0이 아닌 값을 반환합니다.

## <a name="cwndcreate"></a><a name="create"></a>CWnd::만들기

지정된 자식 창을 만들고 [CWnd](../../mfc/reference/cwnd-class.md) 개체에 연결합니다.

```
virtual BOOL Create(
    LPCTSTR lpszClassName,
    LPCTSTR lpszWindowName,
    DWORD dwStyle,
    Const RECT& rect,
    CWnd* pParentWnd,
    UINT nID,
    CCreateContext* pContext = NULL);
```

### <a name="parameters"></a>매개 변수

*lpszClassName*<br/>
【인】 등록된 시스템 창 클래스의 이름을 포함하는 null-terminated 문자열에 대한 포인터; 또는 미리 정의된 시스템 창 클래스의 이름입니다.

*lpszWindowName*<br/>
【인】 창 표시 이름을 포함 하는 null-종료 된 문자열에 대 한 포인터; 그렇지 않으면 창 표시 이름에 대해 NULL입니다.

*dwStyle*<br/>
【인】 [창 스타일의](styles-used-by-mfc.md#window-styles)비트 조합(OR) WS_POPUP 옵션이 잘못된 스타일이 아닙니다.

*rect*<br/>
【인】 상위 창의 왼쪽 위 모서리를 기준으로 한 창의 크기 및 위치입니다.

*pParentWnd*<br/>
【인】 부모 창에 대한 포인터입니다.

*nID*<br/>
【인】 창의 ID입니다.

*pContext*<br/>
【인】 응용 프로그램에 대한 문서 보기 아키텍처를 사용자 지정하는 데 사용되는 [CCreateContext](../../mfc/reference/ccreatecontext-structure.md) 구조에 대한 포인터입니다.

### <a name="return-value"></a>Return Value

메서드가 성공한 경우 TRUE입니다. 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

> [!WARNING]
> `CWnd::PreCreateWindow`이제 메뉴가 NULL이고 스타일에 `CREATESTRUCT` WS_CHILD 포함되어 있는 경우 해당 매개 변수의 hMenu 멤버를 **이** 포인터에 할당합니다. 적절한 기능을 위해 대화 상자 컨트롤에 NULL이 아닌 ID가 있는지 확인합니다.
>
> 이 변경 은 관리 /네이티브 interop 시나리오에서 충돌을 해결 합니다. 문제의 개발자에게 `CWnd::Create` 경고하는 TRACE 문입니다.

[AfxRegisterWndClass](../../mfc/reference/application-information-and-management.md#afxregisterwndclass) 함수를 사용하여 창 클래스를 등록합니다. 사용자 정의 창 클래스는 등록된 모듈에서 사용할 수 있습니다.

[CWnd::OnCreate](#oncreate) 메서드는 메서드가 `Create` 반환하기 전에, 창이 표시되기 전에 호출됩니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#79](../../mfc/reference/codesnippet/cpp/cwnd-class_18.cpp)]

## <a name="cwndcreateaccessibleproxy"></a><a name="createaccessibleproxy"></a>CWnd::액세스 가능한 프록시 만들기

지정된 개체에 대해 Active Accessibility 프록시를 만듭니다.

```
virtual HRESULT CreateAccessibleProxy(
    WPARAM wParam,
    LPARAM lParam,
    LRESULT* pResult);
```

### <a name="parameters"></a>매개 변수

*wParam*<br/>
활성 내게 필요한 옵션 프록시에서 액세스하는 개체를 식별합니다. 다음 값 중 하나가 될 수 있습니다.

|값|의미|
|-----------|-------------|
|OBJID_CLIENT|창의 클라이언트 영역을 나타냅니다.|

*lParam*<br/>
추가 메시지 종속 정보를 제공합니다.

*p결과*<br/>
결과 코드를 저장하는 LRESULT에 대한 포인터입니다.

### <a name="remarks"></a>설명

지정된 개체에 대해 Active Accessibility 프록시를 만듭니다.

## <a name="cwndcreatecaret"></a><a name="createcaret"></a>CWnd::만들기 Caret

시스템 캐리트에 대한 새 모양을 만들고 카포트의 소유권을 주장합니다.

```cpp
void CreateCaret(CBitmap* pBitmap);
```

### <a name="parameters"></a>매개 변수

*pBitmap*<br/>
캐번 모양을 정의하는 비트맵을 식별합니다.

### <a name="remarks"></a>설명

비트맵은 이전에 [CBitmap::CreateBitmap](../../mfc/reference/cbitmap-class.md#createbitmap) 멤버 함수, [CreateDIBitmap](/windows/win32/api/wingdi/nf-wingdi-createdibitmap) Windows 함수 또는 [CBitmap::LoadBitmap](../../mfc/reference/cbitmap-class.md#loadbitmap) 멤버 함수에 의해 만들어졌어야 합니다.

`CreateCaret`어떤 창이 캐리트소유인지에 관계없이 이전 의 캐리트 쉐이프를 자동으로 파기합니다. 일단 생성되면, 카어는 처음에 숨겨져 있습니다. 카를트(caret)를 표시하려면 [ShowCaret](#showcaret) 멤버 함수를 호출해야 합니다.

시스템 케어는 공유 리소스입니다. `CWnd`입력 포커스가 있거나 활성화된 경우에만 케어를 만들어야 합니다. 입력 포커스를 잃거나 비활성 상태가 되기 전에 카릿을 파괴해야 합니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#80](../../mfc/reference/codesnippet/cpp/cwnd-class_19.cpp)]

## <a name="cwndcreatecontrol"></a><a name="createcontrol"></a>CWnd::만들기 제어

이 멤버 함수를 사용하여 개체에 의해 MFC 프로그램에 표시될 `CWnd` ActiveX 컨트롤을 만듭니다.

```
BOOL CreateControl(
    LPCTSTR pszClass,
    LPCTSTR pszWindowName,
    DWORD dwStyle,
    const RECT& rect,
    CWnd* pParentWnd,
    UINT nID,
    CFile* pPersist = NULL,
    BOOL bStorage = FALSE,
    BSTR bstrLicKey = NULL);

BOOL CreateControl(
    REFCLSID clsid,
    LPCTSTR pszWindowName,
    DWORD dwStyle,
    const RECT& rect,
    CWnd* pParentWnd,
    UINT nID,
    CFile* pPersist = NULL,
    BOOL bStorage = FALSE,
    BSTR bstrLicKey = NULL);

BOOL CreateControl(
    REFCLSID clsid,
    LPCTSTR pszWindowName,
    DWORD dwStyle,
    const POINT* ppt,
    const SIZE* psize,
    CWnd* pParentWnd,
    UINT nID,
    CFile* pPersist = NULL,
    BOOL bStorage = FALSE,
    BSTR bstrLicKey = NULL);
```

### <a name="parameters"></a>매개 변수

*pszClass*<br/>
이 문자열은 클래스에 대한 OLE "짧은 이름"(ProgID)을 포함할 수 있습니다(예: "CIRC3. Circ3Ctrl.1". 이름은 컨트롤에 의해 등록된 이름과 일치해야 합니다. 또는, 문자열은 "{9DBAFCCF-592F-101B-85CE-00608CEC297B}"와 같은 중괄호에 포함된 CLSID의 문자열 형태를 포함할 수 있습니다. 두 경우 `CreateControl` 모두 문자열을 해당 클래스 ID로 변환합니다.

*psz창이름*<br/>
컨트롤에 표시할 텍스트에 대한 포인터입니다. 컨트롤의 캡션 또는 텍스트 속성(있는 경우)의 값을 설정합니다. NULL이면 컨트롤의 캡션 또는 텍스트 속성이 변경되지 않습니다.

*dwStyle*<br/>
윈도우 스타일. 사용 가능한 스타일은 비고 아래에 나열됩니다.

*rect*<br/>
컨트롤의 크기와 위치를 지정합니다. [CRect](../../atl-mfc-shared/reference/crect-class.md) 개체 또는 [RECT 구조일](/windows/win32/api/windef/ns-windef-rect)수 있습니다.

*Ppt*<br/>
컨트롤의 왼쪽 위 `CPoint` 모서리를 포함하는 POINT [구조](/windows/win32/api/windef/ns-windef-point) 또는 개체를 가리킵니다.

*pSize*<br/>
컨트롤의 [SIZE](/windows/win32/api/windef/ns-windef-size) 크기를 포함하는 `CSize` SIZE 구조 또는 개체를 가리킵니다.

*pParentWnd*<br/>
컨트롤의 상위 창을 지정합니다. NULL이 아니어야 합니다.

*nID*<br/>
컨트롤의 ID를 지정합니다.

*pPersist*<br/>
컨트롤에 대한 영구 상태를 포함하는 [CFile에](../../mfc/reference/cfile-class.md) 대한 포인터입니다. 기본값은 NULL로, 컨트롤이 영구 저장소에서 상태를 복원하지 않고 자체를 초기화한다는 것을 나타냅니다. NULL이 아닌 경우 스트림 또는 `CFile`저장소의 형태로 컨트롤의 영구 데이터를 포함하는 -derived 개체에 대한 포인터여야 합니다. 이 데이터는 클라이언트의 이전 활성화에 저장되었을 수 있습니다. 다른 `CFile` 데이터를 포함할 수 있지만 호출 시 영구 데이터의 첫 번째 바이트로 설정된 읽기 `CreateControl`쓰기 포인터가 있어야 합니다.

*b스토리지*<br/>
*pPersist의* 데이터를 IStorage 또는 IStream 데이터로 해석해야 하는지 여부를 나타냅니다. *pPersist의* 데이터가 저장소인 경우 *bStorage는* TRUE여야 합니다. *pPersist의* 데이터가 스트림인 경우 *bStorage는* FALSE여야 합니다. 기본값은 FALSE입니다.

*블스트릭키*<br/>
옵션 라이센스 키 데이터. 이 데이터는 런타임 라이센스 키가 필요한 컨트롤을 만드는 데만 필요합니다. 컨트롤이 라이선스를 지원하는 경우 성공하려면 컨트롤을 만들 수 있는 라이센스 키를 제공해야 합니다. 기본값은 NULL입니다.

*clsid*<br/>
컨트롤의 고유 클래스 ID입니다.

### <a name="return-value"></a>Return Value

성공하면 0이 아니고, 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

`CreateControl`[는 CWnd::Create](#create) 함수의 직접 아날로그이며, 이 `CWnd`함수는 에 대한 창을 만듭니다. `CreateControl`을 사용하면 일반 창 대신 ActiveX 컨트롤이 만들어집니다.

Windows *dwStyle* 플래그의 하위 집합만 `CreateControl`지원됩니다.

- WS_VISIBLE 처음에 표시되는 창을 만듭니다. 일반 창과 같이 컨트롤을 즉시 표시하려면 필수입니다.

- WS_DISABLED 처음에 사용하지 않도록 설정한 창을 만듭니다. 비활성화된 창은 사용자로부터 입력을 받을 수 없습니다. 컨트롤에 Enabled 속성이 있는 경우 설정할 수 있습니다.

- WS_BORDER 줄 테두리가 있는 창을 만듭니다. 컨트롤에 BorderStyle 속성이 있는 경우 설정할 수 있습니다.

- WS_GROUP 컨트롤 그룹의 첫 번째 컨트롤을 지정합니다. 사용자는 방향 키를 사용하여 그룹의 한 컨트롤에서 다음 컨트롤로 키보드 포커스를 변경할 수 있습니다. 첫 번째 컨트롤 이후에 WS_GROUP 스타일로 정의된 모든 컨트롤은 동일한 그룹에 속합니다. WS_GROUP 스타일로 다음 컨트롤은 그룹을 종료하고 다음 그룹을 시작합니다.

- WS_TABSTOP 사용자가 TAB 키를 누를 때 키보드 포커스를 받을 수 있는 컨트롤을 지정합니다. TAB 키를 누르면 키보드 초점이 WS_TABSTOP 스타일의 다음 컨트롤로 변경됩니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#81](../../mfc/reference/codesnippet/cpp/cwnd-class_20.h)]

## <a name="cwndcreateex"></a><a name="createex"></a>CWnd::CreateEx

지정된 창을 만들고 개체에 `CWnd` 연결합니다.

```
virtual BOOL CreateEx(
    DWORD dwExStyle,
    LPCTSTR lpszClassName,
    LPCTSTR lpszWindowName,
    DWORD dwStyle,
    int x,
    int y,
    int nWidth,
    int nHeight,
    HWND hWndParent,
    HMENU nIDorHMenu,
    LPVOID lpParam = NULL);

virtual BOOL CreateEx(
    DWORD dwExStyle,
    LPCTSTR lpszClassName,
    LPCTSTR lpszWindowName,
    DWORD dwStyle,
    const RECT& rect,
    CWnd* pParentWnd,
    UINT nID,
    LPVOID lpParam = NULL);
```

### <a name="parameters"></a>매개 변수

*dwExStyle*<br/>
[확장 창 스타일의](../../mfc/reference/styles-used-by-mfc.md#extended-window-styles)비트 조합(OR) 그렇지 않으면 기본 확장 창 스타일에 대해 NULL입니다.

*lpszClassName*<br/>
등록된 시스템 창 클래스의 이름을 포함하는 null-terminated 문자열에 대한 포인터; 또는 미리 정의된 시스템 창 클래스의 이름입니다.

*lpszWindowName*<br/>
창 표시 이름을 포함 하는 null-종료 된 문자열에 대 한 포인터; 그렇지 않으면 창 표시 이름에 대해 NULL입니다.

*dwStyle*<br/>
[윈도우 스타일의](styles-used-by-mfc.md#window-styles)비트 조합 (OR) ; 그렇지 않으면 기본 창 스타일에 대해 NULL입니다.

*x*<br/>
화면 의 왼쪽 또는 상위 창에서 창의 초기 수평 거리입니다.

*Y*<br/>
화면 위또는 상위 창에서 창의 초기 수직 거리입니다.

*nWidth*<br/>
창의 너비(픽셀 단위)입니다.

*nHeight*<br/>
창의 높이(픽셀 단위)입니다.

*hwndParent*<br/>
하위 창의 경우 부모 창에 대한 핸들입니다. 그렇지 않으면 창에 소유자가 있는 경우 소유자 창의 핸들입니다.

*니도르H메뉴*<br/>
자식 창의 경우 창 ID; 그렇지 않으면 창에 대한 메뉴의 ID입니다.

*lpParam*<br/>
[cWnd::OnCreate](#oncreate) 메서드에 전달 되는 사용자 데이터에 대 한 포인터 *lpCreateParams* 필드에 메서드를 만듭니다.

*rect*<br/>
화면 또는 상위 창을 기준으로 한 창의 크기와 위치입니다.

*pParentWnd*<br/>
자식 창의 경우 부모 창에 대한 포인터입니다. 그렇지 않으면 창에 소유자가 있는 경우 소유자 창에 대한 포인터를 지정합니다.

*nID*<br/>
자식 창의 경우 창 ID; 그렇지 않으면 창에 대한 메뉴의 ID입니다.

### <a name="return-value"></a>Return Value

메서드가 성공한 경우 TRUE입니다. 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

> [!WARNING]
> `CWnd::PreCreateWindow`이제 메뉴가 NULL이고 스타일에 `CREATESTRUCT` WS_CHILD 포함되어 있는 경우 해당 매개 변수의 hMenu 멤버를 **이** 포인터에 할당합니다. 적절한 기능을 위해 대화 상자 컨트롤에 NULL이 아닌 ID가 있는지 확인합니다.
>
> 이 변경 은 관리 /네이티브 interop 시나리오에서 충돌을 해결 합니다. 문제의 `TRACE` 개발자에게 `CWnd::Create` 경고하는 명령문입니다.

기본 확장 창 스타일은 WS_EX_LEFT. 기본 창 스타일은 WS_OVERLAPPED.

[AfxRegisterWndClass](../../mfc/reference/application-information-and-management.md#afxregisterwndclass) 함수를 사용하여 창 클래스를 등록합니다. 사용자 정의 창 클래스는 등록된 모듈에서 사용할 수 있습니다.

자식 창의 차원은 상위 창의 클라이언트 영역의 왼쪽 위 모서리를 기준으로 합니다. 최상위 창의 차원은 화면의 왼쪽 위 모서리를 기준으로 합니다.

[CWnd::OnCreate](#oncreate) 메서드는 메서드가 `CreateEx` 반환하기 전에, 창이 표시되기 전에 호출됩니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#82](../../mfc/reference/codesnippet/cpp/cwnd-class_21.cpp)]

## <a name="cwndcreategraycaret"></a><a name="creategraycaret"></a>CWnd::만들기그레이케어

시스템 캐리트에 대한 회색 사각형을 만들고 캐리트의 소유권을 주장합니다.

```cpp
void CreateGrayCaret(
    int nWidth,
    int nHeight);
```

### <a name="parameters"></a>매개 변수

*nWidth*<br/>
(논리 단위로) 카어트의 너비를 지정합니다. 이 매개변수가 0이면 너비가 시스템 정의 창 테두리 폭으로 설정됩니다.

*nHeight*<br/>
(논리 단위로) 카터의 높이를 지정합니다. 이 매개변수가 0이면 높이가 시스템 정의 창 테두리 높이로 설정됩니다.

### <a name="remarks"></a>설명

캐번 모양은 선 또는 블록일 수 있습니다.

*매개변수 nWidth* 및 *nHeight는* 캐번의 너비와 높이(논리 단위)를 지정합니다. 정확한 너비와 높이(픽셀)는 매핑 모드에 따라 달라집니다.

시스템의 창 테두리 너비 또는 높이는 [getSystemMetrics](/windows/win32/api/winuser/nf-winuser-getsystemmetrics) Windows 함수에서 SM_CXBORDER 및 SM_CYBORDER 인덱스를 통해 검색할 수 있습니다. 창 테두리 너비 또는 높이를 사용하면 고해상도 디스플레이에서 캐리트가 표시됩니다.

멤버 `CreateGrayCaret` 함수는 어떤 창이 카포트를 소유하든 이전 캐리트 셰이프(있는 경우)를 자동으로 파기합니다. 일단 생성되면, 카어는 처음에 숨겨져 있습니다. 카를트(caret)를 표시하려면 [ShowCaret](#showcaret) 멤버 함수를 호출해야 합니다.

시스템 케어는 공유 리소스입니다. `CWnd`입력 포커스가 있거나 활성화된 경우에만 케어를 만들어야 합니다. 입력 포커스를 잃거나 비활성 상태가 되기 전에 카릿을 파괴해야 합니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#83](../../mfc/reference/codesnippet/cpp/cwnd-class_22.cpp)]

## <a name="cwndcreatesolidcaret"></a><a name="createsolidcaret"></a>CWnd::만들기솔리드케어

시스템 캐리트에 대한 견고한 사각형을 만들고 캐리트의 소유권을 주장합니다.

```cpp
void CreateSolidCaret(
    int nWidth,
    int nHeight);
```

### <a name="parameters"></a>매개 변수

*nWidth*<br/>
(논리 단위로) 카어트의 너비를 지정합니다. 이 매개변수가 0이면 너비가 시스템 정의 창 테두리 폭으로 설정됩니다.

*nHeight*<br/>
(논리 단위로) 카터의 높이를 지정합니다. 이 매개변수가 0이면 높이가 시스템 정의 창 테두리 높이로 설정됩니다.

### <a name="remarks"></a>설명

캐번 모양은 선 또는 블록일 수 있습니다.

*매개변수 nWidth* 및 *nHeight는* 캐번의 너비와 높이(논리 단위)를 지정합니다. 정확한 너비와 높이(픽셀)는 매핑 모드에 따라 달라집니다.

시스템의 창 테두리 너비 또는 높이는 [getSystemMetrics](/windows/win32/api/winuser/nf-winuser-getsystemmetrics) Windows 함수에서 SM_CXBORDER 및 SM_CYBORDER 인덱스를 통해 검색할 수 있습니다. 창 테두리 너비 또는 높이를 사용하면 고해상도 디스플레이에서 캐리트가 표시됩니다.

멤버 `CreateSolidCaret` 함수는 어떤 창이 카포트를 소유하든 이전 캐리트 셰이프(있는 경우)를 자동으로 파기합니다. 일단 생성되면, 카어는 처음에 숨겨져 있습니다. 카를트(caret)를 표시하려면 [ShowCaret](#showcaret) 멤버 함수를 호출해야 합니다.

시스템 케어는 공유 리소스입니다. `CWnd`입력 포커스가 있거나 활성화된 경우에만 케어를 만들어야 합니다. 입력 포커스를 잃거나 비활성 상태가 되기 전에 카릿을 파괴해야 합니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#84](../../mfc/reference/codesnippet/cpp/cwnd-class_23.cpp)]

## <a name="cwndcwnd"></a><a name="cwnd"></a>CWnd::: CWnd

`CWnd` 개체를 생성합니다.

```
CWnd();
```

### <a name="remarks"></a>설명

[CreateEx](#createex) [또는](#create) Create 멤버 함수가 호출될 때까지 Windows 창이 만들어지고 첨부되지 않습니다.

## <a name="cwnddefault"></a><a name="default"></a>CWnd::D

기본 창 프로시저를 호출합니다.

```
LRESULT Default();
```

### <a name="return-value"></a>Return Value

보낸 메시지에 따라 다릅니다.

### <a name="remarks"></a>설명

기본 창 프로시저는 응용 프로그램이 처리하지 않는 모든 창 메시지에 대한 기본 처리를 제공합니다. 이 멤버 함수는 모든 메시지가 처리되도록 합니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#85](../../mfc/reference/codesnippet/cpp/cwnd-class_24.cpp)]

## <a name="cwnddefwindowproc"></a><a name="defwindowproc"></a>CWnd::D프윈도우프로크

응용 프로그램이 처리하지 않는 모든 창 메시지에 대한 기본 처리를 제공하는 기본 창 프로시저를 호출합니다.

```
virtual LRESULT DefWindowProc(
    UINT message,
    WPARAM wParam,
    LPARAM lParam);
```

### <a name="parameters"></a>매개 변수

*message*<br/>
처리할 Windows 메시지를 지정합니다.

*wParam*<br/>
추가 메시지 종속 정보를 지정합니다.

*lParam*<br/>
추가 메시지 종속 정보를 지정합니다.

### <a name="return-value"></a>Return Value

보낸 메시지에 따라 다릅니다.

### <a name="remarks"></a>설명

이 멤버 함수는 모든 메시지가 처리되도록 합니다. 창 프로시저에서 받은 매개 변수와 동일한 매개 변수로 호출해야 합니다.

## <a name="cwnddeletetempmap"></a><a name="deletetempmap"></a>CWnd::DeleteTempMap

개체의 유휴 시간 처리기에 `CWinApp` 의해 자동으로 호출됩니다.

```
static void PASCAL DeleteTempMap();
```

### <a name="remarks"></a>설명

멤버 함수에 `CWnd` 의해 생성된 `FromHandle` 임시 개체를 삭제합니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#86](../../mfc/reference/codesnippet/cpp/cwnd-class_25.cpp)]

## <a name="cwnddestroywindow"></a><a name="destroywindow"></a>CWnd::D에스트로이윈도우

개체에 연결된 Windows 창을 `CWnd` 삭제합니다.

```
virtual BOOL DestroyWindow();
```

### <a name="return-value"></a>Return Value

창이 소멸된 경우 0이 아닙니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

멤버 `DestroyWindow` 함수는 창에 적절한 메시지를 보내 비활성화하고 입력 포커스를 제거합니다. 또한 창 의 메뉴를 삭제하고, 응용 프로그램 큐를 플러시하고, 뛰어난 타이머를 제거하고, 클립보드 소유권을 `CWnd` 제거하고, 뷰어 체인의 맨 위에 있는 경우 클립보드 뷰어 체인을 분리합니다. [WM_DESTROY](#ondestroy) [WM_NCDESTROY](#onncdestroy) 메시지를 창으로 보냅니다. 개체를 `CWnd` 파괴하지 않습니다.

`DestroyWindow`은 정리를 수행하기 위한 장소 홀더입니다. 가상 `DestroyWindow` 함수이기 때문에 클래스 보기에서 `CWnd`파생된 모든 클래스에 표시됩니다. 그러나 `CWnd`-derived 클래스에서 이 함수를 재정의하더라도 반드시 `DestroyWindow` 호출되는 것은 아닙니다. MFC 코드에서 호출되지 않으면 `DestroyWindow` 호출하려는 경우 자체 코드에서 명시적으로 호출해야 합니다.

예를 들어 `DestroyWindow` `CView`-derived 클래스에서 재정의했다고 가정합니다. MFC 소스 코드는 `DestroyWindow` `CFrameWnd`-derived 클래스에서 호출되지 않으므로 명시적으로 `DestroyWindow` 호출하지 않는 한 재정의되지 않습니다.

창이 모든 창의 부모인 경우 상위 창이 소멸되면 이러한 하위 창이 자동으로 소멸됩니다. 멤버 `DestroyWindow` 함수는 자식 창을 먼저 파괴한 다음 창 자체를 파괴합니다.

멤버 `DestroyWindow` 함수는 [CDialog::Create에서](../../mfc/reference/cdialog-class.md#create)만든 모덜리스 대화 상자도 삭제합니다.

소멸되는 것이 자식 창이고 [WS_EX_NOPARENTNOTIFY](../../mfc/reference/styles-used-by-mfc.md#extended-window-styles) 스타일 집합이 없는 경우 WM_PARENTNOTIFY 메시지가 부모에게 전송됩니다. [WM_PARENTNOTIFY](/windows/win32/inputmsg/wm-parentnotify) `CWnd`

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#87](../../mfc/reference/codesnippet/cpp/cwnd-class_26.cpp)]

## <a name="cwnddetach"></a><a name="detach"></a>CWnd::D에타치

`CWnd` 개체에서 Windows 핸들을 분리하고 핸들을 반환합니다.

```
HWND Detach();
```

### <a name="return-value"></a>Return Value

Windows 개체에 대한 HWND입니다.

### <a name="example"></a>예제

  [CWnd::연결에](#attach)대한 예제를 참조하십시오.

## <a name="cwnddlgdirlist"></a><a name="dlgdirlist"></a>CWnd::DlgDirList

파일 또는 디렉터리 목록으로 목록 상자를 채웁니다.

```
int DlgDirList(
    LPTSTR lpPathSpec,
    int nIDListBox,
    int nIDStaticPath,
    UINT nFileType);
```

### <a name="parameters"></a>매개 변수

*lp패스스펙*<br/>
경로 또는 파일 이름을 포함하는 null 종료 된 문자열을 가리킵니다. `DlgDirList`수정 사항을 포함할 수 있을 만큼 길어야 하는 이 문자열을 수정합니다. 자세한 내용은 다음 "비고" 섹션을 참조하십시오.

*니디리스트박스*<br/>
목록 상자의 식별자를 지정합니다. *nIDListBox가* 0이면 `DlgDirList` 목록 상자가 존재하지 않으며 채우기를 시도하지 않는다고 가정합니다.

*니디드스테틱 패스*<br/>
현재 드라이브 및 디렉터리를 표시하는 데 사용되는 정적 텍스트 컨트롤의 식별자를 지정합니다. *nIDStaticPath가* 0이면 `DlgDirList` 이러한 텍스트 컨트롤이 없다고 가정합니다.

*n파일타입*<br/>
표시할 파일의 속성을 지정합니다. 다음 값의 조합일 수 있습니다.

- 추가 특성없이 데이터 파일을 읽기 - 쓰기를 DDL_READWRITE.

- 읽기 전용 파일을 DDL_READONLY.

- 숨겨진 파일을 DDL_HIDDEN.

- 시스템 파일을 DDL_SYSTEM.

- DDL_DIRECTORY 디렉토리.

- DDL_ARCHIVE 아카이브.

- DDL_POSTMSGS LB_DIR 플래그입니다. LB_DIR 플래그가 설정된 경우 Windows는 응용 `DlgDirList` 프로그램의 큐에 생성된 메시지를 배치합니다. 그렇지 않으면 대화 상자 프로시저로 직접 전송됩니다.

- DDL_DRIVES 드라이브. DDL_DRIVES 플래그가 설정되면 DDL_EXCLUSIVE 플래그가 자동으로 설정됩니다. 따라서 드라이브와 파일이 포함된 디렉터리 목록을 만들려면 `DlgDirList` DDL_DRIVES 플래그가 설정된 경우 한 번, 나머지 목록에 대한 플래그를 한 번 호출해야 합니다.

- DDL_EXCLUSIVE 배타적 비트입니다. 단독 비트가 설정된 경우 지정된 형식의 파일만 나열됩니다. 그렇지 않으면 일반 파일과 지정된 형식의 파일이 나열됩니다.

### <a name="return-value"></a>Return Value

함수가 성공하면 0이 아니고 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

`DlgDirList`[LB_RESETCONTENT](/windows/win32/Controls/lb-resetcontent) 및 [LB_DIR](/windows/win32/Controls/lb-dir) 메시지를 목록 상자로 보냅니다. *nIDListBox에서* 지정한 목록 상자에 *lpPathSpec에*의해 지정된 경로와 일치하는 모든 파일의 이름을 채웁니다.

*lpPathSpec* 매개 변수에는 다음과 같은 형식이 있습니다.

`[drive:] [ [\u]directory[\idirectory]...\u] [filename]`

이 예제에서 *드라이브는* 드라이브 문자이고 *디렉터리이름은* 유효한 디렉터리 이름이며 *파일 이름은* 하나 이상의 와일드카드를 포함해야 하는 유효한 파일 이름입니다. 와일드카드는 물음표(?)이며, 이는 모든 문자와 일치하는 문자와 별표()를 **?**<strong>\*</strong>의미하며, 이는 임의의 수의 문자와 일치함을 의미합니다.

*lpPathSpec에*대해 0길이 문자열을 지정하거나 디렉터리 이름만 지정하지만 파일 사양을 포함하지 않는 경우 문자열이 "로\*변경됩니다. \*".

*lpPathSpec에* 드라이브 및/또는 디렉터리 이름이 포함된 경우 목록 상자가 채워지기 전에 현재 드라이브 및 디렉터리가 지정된 드라이브 및 디렉터리로 변경됩니다. *nIDStaticPath로* 식별된 텍스트 컨트롤도 새 드라이브 및/또는 디렉터리 이름으로 업데이트됩니다.

목록 상자가 채워진 후 *lpPathSpec은* 경로의 드라이브 및/또는 디렉터리 부분을 제거하여 업데이트됩니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#88](../../mfc/reference/codesnippet/cpp/cwnd-class_27.cpp)]

## <a name="cwnddlgdirlistcombobox"></a><a name="dlgdirlistcombobox"></a>CWnd::DlgdirListComboBox

파일 또는 디렉터리 목록으로 콤보 상자의 목록 상자를 채웁니다.

```
int DlgDirListComboBox(
    LPTSTR lpPathSpec,
    int nIDComboBox,
    int nIDStaticPath,
    UINT nFileType);
```

### <a name="parameters"></a>매개 변수

*lp패스스펙*<br/>
경로 또는 파일 이름을 포함하는 null 종료 된 문자열을 가리킵니다. `DlgDirListComboBox`이 문자열을 수정하므로 이 데이터는 문자열 리터럴 형식이 아니어야 합니다. 다음 "비고" 절을 참조하십시오.

*니드콤보박스*<br/>
대화 상자에서 콤보 상자의 식별자를 지정합니다. *nIDComboBox가* 0이면 `DlgDirListComboBox` 콤보 상자가 존재하지 않으며 콤보 상자를 채우려고 시도하지 않는다고 가정합니다.

*니디드스테틱 패스*<br/>
현재 드라이브 및 디렉터리를 표시하는 데 사용되는 정적 텍스트 컨트롤의 식별자를 지정합니다. *nIDStaticPath가* 0이면 `DlgDirListComboBox` 이러한 텍스트 컨트롤이 없다고 가정합니다.

*n파일타입*<br/>
표시할 파일의 DOS 파일 특성을 지정합니다. 다음 값의 조합일 수 있습니다.

- 추가 특성없이 데이터 파일을 읽기 - 쓰기를 DDL_READWRITE.

- 읽기 전용 파일을 DDL_READONLY.

- 숨겨진 파일을 DDL_HIDDEN.

- 시스템 파일을 DDL_SYSTEM.

- DDL_DIRECTORY 디렉토리.

- DDL_ARCHIVE 아카이브.

- DDL_POSTMSGS CB_DIR 플래그입니다. CB_DIR 플래그가 설정된 경우 Windows는 응용 `DlgDirListComboBox` 프로그램의 큐에 생성된 메시지를 배치합니다. 그렇지 않으면 대화 상자 프로시저로 직접 전송됩니다.

- DDL_DRIVES 드라이브. DDL_DRIVES 플래그가 설정되면 DDL_EXCLUSIVE 플래그가 자동으로 설정됩니다. 따라서 드라이브와 파일이 포함된 디렉터리 목록을 만들려면 `DlgDirListComboBox` DDL_DRIVES 플래그가 설정된 경우 한 번, 나머지 목록에 대한 플래그를 한 번 호출해야 합니다.

- DDL_EXCLUSIVE 배타적 비트입니다. 단독 비트가 설정된 경우 지정된 형식의 파일만 나열됩니다. 그렇지 않으면 일반 파일과 지정된 형식의 파일이 나열됩니다.

### <a name="return-value"></a>Return Value

함수의 결과를 지정합니다. 목록이 만들어진 경우, 심지어 빈 목록이 만들어지면 그것은 영하지 않습니다. 0 return 값은 입력 문자열에 유효한 검색 경로가 포함되어 있지 않음을 의미합니다.

### <a name="remarks"></a>설명

`DlgDirListComboBox`[CB_RESETCONTENT](/windows/win32/Controls/cb-resetcontent) 및 [CB_DIR](/windows/win32/Controls/cb-dir) 메시지를 콤보 상자로 보냅니다. *nIDComboBox에서* 지정한 콤보 상자의 목록 상자에 *lpPathSpec에*의해 지정된 경로와 일치하는 모든 파일의 이름을 채웁니다.

*lpPathSpec* 매개 변수에는 다음과 같은 형식이 있습니다.

`[drive:] [ [\u]directory[\idirectory]...\u] [filename]`

이 예제에서 *드라이브는* 드라이브 문자이고 *디렉터리이름은* 유효한 디렉터리 이름이며 *파일 이름은* 하나 이상의 와일드카드를 포함해야 하는 유효한 파일 이름입니다. 와일드카드는 물음표(?)이며, 이는 모든 문자와 일치하는 문자와 별표(별표)를 **?**<strong>\*</strong>의미하며, 이는 임의의 수의 문자와 일치함을 의미합니다.

*lpPathSpec에*대해 길이 0길이 문자열을 지정하면 현재 디렉토리가 사용되고 *lpPathSpec이* 수정되지 않습니다. 디렉터리 이름만 지정하지만 파일 사양을 포함하지 않으면 문자열이 ""로\*변경됩니다.

*lpPathSpec에* 드라이브 및/또는 디렉터리 이름이 포함된 경우 목록 상자가 채워지기 전에 현재 드라이브 및 디렉터리가 지정된 드라이브 및 디렉터리로 변경됩니다. *nIDStaticPath로* 식별된 텍스트 컨트롤도 새 드라이브 및/또는 디렉터리 이름으로 업데이트됩니다.

콤보 상자 목록 상자가 채워진 후 *lpPathSpec은* 경로의 드라이브 및/또는 디렉터리 부분을 제거하여 업데이트됩니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#89](../../mfc/reference/codesnippet/cpp/cwnd-class_28.cpp)]

## <a name="cwnddlgdirselect"></a><a name="dlgdirselect"></a>CWnd::DlgDirSelect

목록 상자에서 현재 선택 영역을 검색합니다.

```
BOOL DlgDirSelect(
    LPTSTR lpString,
    int nIDListBox);
```

### <a name="parameters"></a>매개 변수

*lpString*<br/>
목록 상자에서 현재 선택 영역을 수신할 버퍼를 가리킵니다.

*니디리스트박스*<br/>
대화 상자에서 목록 상자의 정수 ID를 지정합니다.

### <a name="return-value"></a>Return Value

성공하면 0이 아니고, 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

목록 상자가 [DlgDirList](#dlgdirlist) 멤버 함수에 의해 채워졌으며 선택 영역이 드라이브 문자, 파일 또는 디렉터리 이름이라고 가정합니다.

멤버 `DlgDirSelect` 함수는 *lpString에서*제공하는 버퍼에 선택 항목을 복사합니다. 선택 항목이 없는 경우 *lpString은* 변경되지 않습니다.

`DlgDirSelect`[LB_GETCURSEL](/windows/win32/Controls/lb-getcursel) 및 [LB_GETTEXT](/windows/win32/Controls/lb-gettext) 메시지를 목록 상자로 보냅니다.

목록 상자에서 두 개 이상의 파일 이름을 반환할 수 없습니다. 목록 상자는 다중 선택 목록 상자일 수 없습니다.

## <a name="cwnddlgdirselectcombobox"></a><a name="dlgdirselectcombobox"></a>CWnd::Dlg디어셀렉실렉실렉션콤보박스

콤보 상자의 목록 상자에서 현재 선택 영역을 검색합니다.

```
BOOL DlgDirSelectComboBox(
    LPTSTR lpString,
    int nIDComboBox);
```

### <a name="parameters"></a>매개 변수

*lpString*<br/>
선택한 경로를 수신할 버퍼를 가리킵니다.

*니드콤보박스*<br/>
대화 상자에서 콤보 상자의 정수 ID를 지정합니다.

### <a name="return-value"></a>Return Value

성공하면 0이 아니고, 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

목록 상자가 [DlgDirListComboBox](#dlgdirlistcombobox) 멤버 함수에 의해 채워졌으며 선택 영역이 드라이브 문자, 파일 또는 디렉터리 이름이라고 가정합니다.

멤버 `DlgDirSelectComboBox` 함수는 선택 영역을 지정된 버퍼에 복사합니다. 선택 항목이 없는 경우 버퍼의 내용은 변경되지 않습니다.

`DlgDirSelectComboBox`[CB_GETCURSEL](/windows/win32/Controls/cb-getcursel) 및 [CB_GETLBTEXT](/windows/win32/Controls/cb-getlbtext) 메시지를 콤보 상자로 보냅니다.

콤보 상자에서 두 개 이상의 파일 이름을 반환할 수 없습니다.

## <a name="cwnddodataexchange"></a><a name="dodataexchange"></a>CWnd::DoDataExchange

대화 상자 데이터를 교환하고 유효성을 검사하기 위해 프레임워크에서 호출합니다.

```
virtual void DoDataExchange(CDataExchange* pDX);
```

### <a name="parameters"></a>매개 변수

*pDX*<br/>
`CDataExchange` 개체에 대한 포인터입니다.

### <a name="remarks"></a>설명

이 함수를 직접 호출하지 마십시오. [UpdateData](#updatedata) 멤버 함수에서 호출합니다. 대화 `UpdateData` 상자의 컨트롤을 초기화하거나 대화 상자에서 데이터를 검색하기 위해 호출합니다.

[CDialog에서](../../mfc/reference/cdialog-class.md)응용 프로그램 별 대화 상자 클래스를 파생 하는 경우 프레임 워크의 자동 데이터 교환 및 유효성 검사를 사용 하려는 경우이 멤버 함수를 재정의 해야 합니다. 가변 추가 마법사는 대화 상자 데이터 교환(DDX) 및 유효성 검사(DDV) 전역 함수 호출의 원하는 "데이터 맵"을 포함하는 이 멤버 함수의 재정의된 버전을 작성합니다.

이 멤버 함수의 재정의된 버전을 자동으로 생성하려면 먼저 대화 상자 편집기를 사용하여 대화 상자 리소스를 만든 다음 응용 프로그램별 대화 상자 클래스를 파생시됩니다. 그런 다음 변수 추가 마법사를 사용하여 변수, 데이터 및 유효성 검사 범위를 새 대화 상자의 다양한 컨트롤과 연결합니다. 그런 다음 마법사는 데이터 `DoDataExchange`맵을 포함하는 재정의된 을 씁니다. 다음은 가변 추가 마법사에서 생성된 DDX/DDV 코드 블록의 예입니다.

[!code-cpp[NVC_MFCWindowing#90](../../mfc/reference/codesnippet/cpp/cwnd-class_29.cpp)]

재정의된 멤버 함수는 `DoDataExchange` 원본 파일의 매크로 문 앞에 있어야 합니다.

대화 상자 데이터 교환 및 유효성 검사에 대한 자세한 내용은 양식 및 [대화 상자 데이터 교환 및 유효성 검사에서](../../mfc/dialog-data-exchange-and-validation.md) [데이터 표시 및 조작을](../../data/odbc/displaying-and-manipulating-data-in-a-form.md) 참조하십시오. 변수 추가 마법사에서 생성된 DDX_ 및 DDV_ 매크로에 대한 설명은 [기술 참고 26을](../../mfc/tn026-ddx-and-ddv-routines.md)참조하십시오.

## <a name="cwnddragacceptfiles"></a><a name="dragacceptfiles"></a>CWnd::D래그수락파일

응용 프로그램의 `CWnd` [CWinApp::InitInstance](../../mfc/reference/cwinapp-class.md#initinstance) 함수에서 포인터를 사용하여 창 내에서 이 멤버 함수를 호출하여 창이 Windows 파일 관리자 또는 파일 탐색기에서 삭제된 파일을 수락함을 나타냅니다.

```cpp
void DragAcceptFiles(BOOL bAccept = TRUE);
```

### <a name="parameters"></a>매개 변수

*BAccept*<br/>
끌어들인 파일이 허용되는지 여부를 나타내는 플래그입니다.

### <a name="remarks"></a>설명

true로 설정된 `DragAcceptFiles` *bAccept* 매개 변수를 사용하여 호출하는 창만 Windows 메시지를 WM_DROPFILES 처리할 수 있는 것으로 식별되었습니다. 예를 들어 MDI 응용 프로그램에서 `CMDIFrameWnd` 함수 호출에서 `DragAcceptFiles` 창 포인터가 사용되는 `CMDIFrameWnd` 경우 창만 WM_DROPFILES 메시지를 가져옵니다. 이 메시지가 열려 `CMDIChildWnd` 있는 모든 창으로 전송되지는 않습니다. `CMDIChildWnd` 이 메시지를 받으려면 창 포인터를 `DragAcceptFiles` `CMDIChildWnd` 사용하여 호출해야 합니다.

끌려간 파일 수신을 중단하려면 *bAccept를* FALSE로 설정한 멤버 함수를 호출합니다.

## <a name="cwnddragdetect"></a><a name="dragdetect"></a>CWnd::D래그니드

마우스를 캡처하고 사용자가 왼쪽 단추를 놓거나, Esc 키를 누르거나, 지정된 점 주위의 끌기 사각형 밖으로 마우스를 이동할 때까지 이동을 추적합니다.

```
BOOL DragDetect(POINT pt) const;
```

### <a name="parameters"></a>매개 변수

*pt*<br/>
화면 좌표에서 마우스의 초기 위치입니다. 이 함수는 이 점을 사용하여 드래그 사각형의 좌표를 결정합니다.

### <a name="return-value"></a>Return Value

사용자가 왼쪽 단추를 누를 때 드래그 사각형 외부로 마우스를 이동한 경우 반환 값은 0이 아닙니다.

사용자가 왼쪽 단추를 누를 때 드래그 사각형 외부로 마우스를 이동하지 않은 경우 반환 값은 0입니다.

### <a name="remarks"></a>설명

이 멤버 함수는 Windows SDK에 설명된 대로 [DragDetect](/windows/win32/api/winuser/nf-winuser-dragdetect)함수의 기능을 에뮬레이트합니다.

## <a name="cwnddrawanimatedrects"></a><a name="drawanimatedrects"></a>CWnd::Draw애니메이션 렉트

와이어프레임 사각형을 그리고 애니메이션하여 아이콘 열기 또는 창의 최소화나 최대화를 나타냅니다.

```
BOOL DrawAnimatedRects(
    int idAni,
    CONST RECT* lprcFrom,
    CONST RECT* lprcTo);
```

### <a name="parameters"></a>매개 변수

*idAni*<br/>
애니메이션 유형을 지정합니다. IDANI_CAPTION 지정하면 창 캡션이 *lprcFrom에서* 지정한 위치에서 *lprcTo에*의해 지정된 위치로 애니메이션됩니다. 이 효과는 창을 최소화하거나 최대화하는 것과 유사합니다.

*lprcFrom*<br/>
아이콘 또는 최소화된 창의 위치와 크기를 지정하는 [RECT](/windows/win32/api/windef/ns-windef-rect) 구조에 대한 포인터입니다.

*lprcTo*<br/>
복원된 창의 위치와 크기를 지정하는 [RECT](/windows/win32/api/windef/ns-windef-rect) 구조에 대한 포인터

### <a name="return-value"></a>Return Value

함수가 성공하면 0이 아니고 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

이 멤버 함수는 Windows SDK에 설명된 대로 [DrawAnimatedRects](/windows/win32/api/winuser/nf-winuser-drawanimatedrects)함수의 기능을 에뮬레이트합니다.

## <a name="cwnddrawcaption"></a><a name="drawcaption"></a>CWnd::D로캡션

창 캡션을 그립니다.

```
BOOL DrawCaption(
    CDC* pDC,
    LPCRECT lprc,
    UINT uFlags);
```

### <a name="parameters"></a>매개 변수

*pDC*<br/>
디바이스 컨텍스트에 대한 포인터입니다. 이 함수는 이 장치 컨텍스트에 창 캡션을 그립니다.

*lprc*<br/>
창 캡션에 대한 경계 사각형을 지정하는 RECT 구조에 대한 포인터입니다.

*uFlags*<br/>
도면 옵션을 지정합니다. 전체 값 목록은 [DrawCaption](/windows/win32/api/winuser/nf-winuser-drawcaption)을 참조하십시오.

### <a name="return-value"></a>Return Value

함수가 성공하면 0이 아니고 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

이 멤버 함수는 Windows SDK에 설명된 대로 [DrawCaption](/windows/win32/api/winuser/nf-winuser-drawcaption)함수의 기능을 에뮬레이트합니다.

## <a name="cwnddrawmenubar"></a><a name="drawmenubar"></a>CWnd::D로메뉴바

메뉴 모음을 다시 그립니다.

```cpp
void DrawMenuBar();
```

### <a name="remarks"></a>설명

Windows에서 창을 만든 후 메뉴 모음이 변경된 경우 이 함수를 호출하여 변경된 메뉴 모음을 그립니다.

### <a name="example"></a>예제

  [CWnd::GetMenu에](#getmenu)대한 예제를 참조하십시오.

## <a name="cwndenableactiveaccessibility"></a><a name="enableactiveaccessibility"></a>CWnd::활성화활성 접근성

사용자 정의 활성 접근성 기능을 활성화합니다.

```cpp
void EnableActiveAccessibility();
```

### <a name="remarks"></a>설명

MFC의 기본 활성 접근성 지원은 ActiveX 컨트롤을 포함한 표준 창 및 컨트롤에 충분합니다. 그러나 `CWnd`-derived 클래스에 창이 지정되지 않은 사용자 인터페이스 요소가 포함되어 있는 경우 MFC는 해당 요소에 대해 알 수 없습니다. 이 경우 클래스의 적절한 [Active Accessibility 멤버 함수를](/windows/win32/winauto/sdk-components) 재정의해야 하며 `EnableActiveAccessibility` 클래스의 생성자에서 호출해야 합니다.

## <a name="cwndenabledynamiclayout"></a><a name="enabledynamiclayout"></a>CWnd::인에이블 동적 레이아웃

동적 레이아웃 관리자를 사용하거나 사용하지 않도록 설정합니다. 동적 레이아웃을 사용하는 경우 사용자가 창의 크기를 조정하면 자식 창의 위치 및 크기가 동적으로 조정됩니다.

```cpp
void EnableDynamicLayout(BOOL bEnable = TRUE);
```

### <a name="parameters"></a>매개 변수

*bEnable*<br/>
동적 레이아웃을 사용하도록 설정하려면 TRUE이고, 동적 레이아웃을 사용하지 않도록 설정하려면 FALSE입니다.

### <a name="remarks"></a>설명

동적 레이아웃을 사용하도록 설정하려는 경우 이 메서드를 호출하기만 하면 됩니다. 창의 컨트롤이 크기 변경에 대응하는 방식을 지정하는 동적 레이아웃 정보도 제공해야 합니다. 리소스 편집기에서 또는 프로그래밍 방식으로 각 컨트롤에 대해 이 정보를 지정할 수 있습니다. [동적 레이아웃](../../mfc/dynamic-layout.md)을 참조하십시오.

## <a name="cwndenabled2dsupport"></a><a name="enabled2dsupport"></a>CWnd::인에이블D2D지원

D2D 지원을 사용하거나 사용하지 않도록 설정합니다. 주 창이 초기화되기 전에 이 메서드를 호출합니다.

```cpp
void EnableD2DSupport(
    BOOL bEnable = TRUE,
    BOOL bUseDCRenderTarget = FALSE);
```

### <a name="parameters"></a>매개 변수

*bEnable*<br/>
D2D 지원을 설정 또는 해제할지를 지정합니다.

*bUseDC렌더대상*<br/>
DC(디바이스 컨텍스트) 렌더 대상, CDCRenderTarget을 사용할지를 지정합니다. FALSE이면 CHwndRenderTarget이 사용됩니다.

## <a name="cwndenablescrollbar"></a><a name="enablescrollbar"></a>CWnd::사용스크롤바

스크롤 막대의 화살표 하나 또는 둘 다를 사용하거나 사용하지 않도록 설정합니다.

```
BOOL EnableScrollBar(
    int nSBFlags,
    UINT nArrowFlags = ESB_ENABLE_BOTH);
```

### <a name="parameters"></a>매개 변수

*nSBFlags*<br/>
스크롤 막대 유형을 지정합니다. 다음 값 중 하나가 될 수 있습니다.

- SB_BOTH 창과 연결된 가로 및 세로 스크롤 막대의 화살표를 활성화하거나 사용하지 않도록 설정합니다.

- SB_HORZ 창과 연결된 가로 스크롤 막대의 화살표를 활성화하거나 사용하지 않도록 설정합니다.

- SB_VERT 창과 연결된 세로 스크롤 막대의 화살표를 활성화하거나 사용하지 않도록 설정합니다.

*nArrowFlags*<br/>
스크롤 막대 화살표가 활성화또는 비활성화되는지 여부와 활성화 또는 비활성화된 화살표를 지정합니다. 다음 값 중 하나가 될 수 있습니다.

- ESB_ENABLE_BOTH 스크롤 막대의 두 화살표(기본값)를 모두 활성화합니다.

- ESB_DISABLE_LTUP 가로 스크롤 막대의 왼쪽 화살표 또는 세로 스크롤 막대의 위쪽 화살표를 사용하지 않도록 설정합니다.

- ESB_DISABLE_RTDN 가로 스크롤 막대의 오른쪽 화살표 또는 세로 스크롤 막대의 아래쪽 화살표를 사용하지 않도록 설정합니다.

- ESB_DISABLE_BOTH 스크롤 막대의 두 화살표를 모두 사용하지 않도록 설정합니다.

### <a name="return-value"></a>Return Value

화살표가 활성화되거나 지정된 대로 비활성화된 경우 0이 아닙니다. 그렇지 않으면 화살표가 이미 요청된 상태에 있거나 오류가 발생했음을 나타내는 0입니다.

## <a name="cwndenablescrollbarctrl"></a><a name="enablescrollbarctrl"></a>CWnd::인에이블스크롤바Ctrl

이 창의 스크롤 막대를 활성화하거나 사용하지 않도록 설정합니다.

```cpp
void EnableScrollBarCtrl(
    int nBar,
    BOOL bEnable = TRUE);
```

### <a name="parameters"></a>매개 변수

*nBar*<br/>
스크롤 막대 식별자입니다.

*bEnable*<br/>
스크롤 막대를 활성화할지 비활성화할지 여부를 지정합니다.

### <a name="remarks"></a>설명

창에 형제 스크롤 막대 컨트롤이 있는 경우 해당 스크롤 막대가 사용됩니다. 그렇지 않으면 창의 자체 스크롤 막대가 사용됩니다.

## <a name="cwndenabletooltips"></a><a name="enabletooltips"></a>CWnd::인에이블툴팁

지정된 창에 대한 도구 팁을 활성화합니다.

```
BOOL EnableToolTips(BOOL bEnable = TRUE);
```

### <a name="parameters"></a>매개 변수

*bEnable*<br/>
공구 팁 컨트롤을 사용하도록 설정하거나 사용하지 않도록 설정할지 여부를 지정합니다. TRUE를 사용하면 컨트롤이 활성화됩니다. FALSE는 컨트롤을 사용하지 않도록 설정합니다.

### <a name="return-value"></a>Return Value

TRUE 도구 설명이 활성화되어 있는 경우 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

[OnToolHitTest를](#ontoolhittest) 재정의하여 창에 [대한 TOOLINFO](/windows/win32/api/commctrl/ns-commctrl-tttoolinfoa) 구조체 또는 구조체를 제공합니다.

> [!NOTE]
> [CToolBar와](../../mfc/reference/ctoolbar-class.md)같은 일부 창은 [OnToolHitTest](#ontoolhittest)의 기본 제공 구현을 제공합니다.

이 구조에 대한 자세한 내용은 Windows SDK의 [TOOLINFO를](/windows/win32/api/commctrl/ns-commctrl-tttoolinfoa) 참조하십시오.

부모 `EnableToolTips` 창에서 파생되지 않는 한 호출만으로는 자식 컨트롤에 `CFrameWnd`대한 도구 팁을 표시하기에 충분하지 않습니다. 이는 TTN_NEEDTEXT `CFrameWnd` 알림에 대한 기본 처리기를 제공하기 때문입니다. 부모 창이 `CFrameWnd`에서 파생되지 않은 경우( 즉, 대화 상자 또는 양식 보기인 경우 TTN_NEEDTEXT 도구 설명 팁 알림에 대한 처리기를 제공하지 않는 한 자식 컨트롤에 대한 도구 설명이 올바르게 표시되지 않습니다. [도구 팁을](../../mfc/tool-tips-in-windows-not-derived-from-cframewnd.md)참조하십시오.

창에 `EnableToolTips` 제공된 기본 도구 팁에는 해당 텍스트와 연관된 텍스트가 없습니다. 도구 설명이 표시될 텍스트를 검색하려면 도구 설명 창이 표시되기 직전에 TTN_NEEDTEXT 알림이 도구 설명 컨트롤의 상위 창으로 전송됩니다. `TOOLTIPTEXT` 구조의 *pszText* 멤버에 일부 값을 할당하는 이 메시지에 대한 처리기가 없는 경우 도구 설명에 대한 텍스트가 표시되지 않습니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#91](../../mfc/reference/codesnippet/cpp/cwnd-class_30.cpp)]

[!code-cpp[NVC_MFCWindowing#92](../../mfc/reference/codesnippet/cpp/cwnd-class_31.cpp)]

## <a name="cwndenabletrackingtooltips"></a><a name="enabletrackingtooltips"></a>CWnd::인에이블트래킹툴팁

추적 도구 설명을 사용하거나 사용하지 않도록 설정합니다.

```
BOOL EnableTrackingToolTips(BOOL bEnable = TRUE);
```

### <a name="parameters"></a>매개 변수

*bEnable*<br/>
추적 도구 팁을 사용하도록 설정하거나 사용하지 않도록 설정할 지 여부를 지정합니다. 이 매개 변수가 TRUE이면 추적 도구 설명이 활성화됩니다. 이 매개 변수가 FALSE이면 추적 도구 설명이 비활성화됩니다.

### <a name="return-value"></a>Return Value

멤버 함수가 `EnableWindow` 호출되기 전의 상태를 나타냅니다. 창이 이전에 비활성화된 경우 반환 값은 0이 아닙니다. 창이 이전에 활성화되거나 오류가 발생한 경우 반환 값은 0입니다.

### <a name="remarks"></a>설명

추적 도구 팁은 화면에 동적으로 배치할 수 있는 도구 팁 창입니다. 위치를 빠르게 업데이트하면 공구 팁 창이 원활하게 이동하거나 "추적"하는 것처럼 보입니다. 이 기능은 포인터가 움직일 때 포인터의 위치를 따라가기 위해 도구 설명 텍스트가 필요한 경우에 유용할 수 있습니다.

## <a name="cwndenablewindow"></a><a name="enablewindow"></a>CWnd::인에이블윈도우

마우스 및 키보드 입력을 사용하거나 사용하지 않도록 설정합니다.

```
BOOL EnableWindow(BOOL bEnable = TRUE);
```

### <a name="parameters"></a>매개 변수

*bEnable*<br/>
지정된 창을 활성화할지 비활성화할지 여부를 지정합니다. 이 매개 변수가 TRUE이면 창이 활성화됩니다. 이 매개 변수가 FALSE이면 창이 비활성화됩니다.

### <a name="return-value"></a>Return Value

멤버 함수가 `EnableWindow` 호출되기 전의 상태를 나타냅니다. 창이 이전에 비활성화된 경우 반환 값은 0이 아닙니다. 창이 이전에 활성화되거나 오류가 발생한 경우 반환 값은 0입니다.

### <a name="remarks"></a>설명

입력을 사용하지 않도록 설정하면 마우스 클릭 및 키 입력과 같은 입력이 무시됩니다. 입력을 사용하도록 설정하면 창은 모든 입력을 처리합니다.

활성화된 상태가 변경되면 이 함수가 반환되기 전에 [WM_ENABLE](#onenable) 메시지가 전송됩니다.

사용하지 않도록 설정하면 모든 자식 창이 암시적으로 비활성화되지만 메시지로 전송되지는 WM_ENABLE.

창을 활성화하려면 먼저 창을 활성화해야 합니다. 예를 들어 응용 프로그램에서 모덜리스 대화 상자를 표시하고 기본 창을 사용하지 않도록 설정한 경우 대화 상자가 삭제되기 전에 주 창을 사용하도록 설정해야 합니다. 그렇지 않으면 다른 창에서 입력 포커스를 얻고 활성화됩니다. 자식 창이 비활성화된 경우 Windows에서 마우스 메시지를 받을 창을 결정하려고 할 때 무시됩니다.

기본적으로 창이 생성될 때 활성화됩니다. 응용 프로그램은 [Create](#create) 또는 [CreateEx](#createex) 멤버 함수에서 WS_DISABLED 스타일을 지정하여 처음에 비활성화된 창을 만들 수 있습니다. 창을 만든 후 응용 프로그램은 멤버 함수를 `EnableWindow` 사용하여 창을 사용하거나 사용하지 않도록 설정할 수도 있습니다.

응용 프로그램은 이 기능을 사용하여 대화 상자에서 컨트롤을 활성화하거나 비활성화할 수 있습니다. 비활성화된 컨트롤은 입력 포커스를 수신할 수 없으며 사용자가 액세스할 수 없습니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#93](../../mfc/reference/codesnippet/cpp/cwnd-class_32.cpp)]

## <a name="cwndendmodalloop"></a><a name="endmodalloop"></a>CWnd::엔드모달루프

에 대한 호출을 `RunModalLoop`종료합니다.

```
virtual void EndModalLoop(int nResult);
```

### <a name="parameters"></a>매개 변수

*nResult*<br/>
[RunModalLoop의](#runmodalloop)호출자에게 반환할 값을 포함합니다.

### <a name="remarks"></a>설명

*nResult* 매개 변수는 에서 `RunModalLoop`반환 값으로 전파됩니다.

## <a name="cwndendmodalstate"></a><a name="endmodalstate"></a>CWnd::엔드모달스테이트

프레임 창을 모달에서 모덜리스로 변경하려면 이 멤버 함수를 호출합니다.

```
virtual void EndModalState();
```

## <a name="cwndendpaint"></a><a name="endpaint"></a>CWnd::엔드페인트

지정된 창에서 페인팅의 끝을 표시합니다.

```cpp
void EndPaint(LPPAINTSTRUCT lpPaint);
```

### <a name="parameters"></a>매개 변수

*lpPaint*<br/>
[BeginPaint](#beginpaint) 멤버 함수에서 검색한 페인팅 정보가 포함된 [PAINTSTRUCT](/windows/win32/api/winuser/ns-winuser-paintstruct) 구조를 가리킵니다.

### <a name="remarks"></a>설명

`EndPaint` 멤버 함수에 대한 각 호출마다 멤버 함수가 필요하지만 페인팅이 완료된 후에만 멤버 함수가 `BeginPaint` 필요합니다.

멤버 함수에 의해 카포트가 `BeginPaint` `EndPaint` 숨김이 있으면 화면에 캐리트가 복원됩니다.

### <a name="example"></a>예제

  [CWnd::BeginPaint](#beginpaint)에 대한 예제를 참조하십시오.

## <a name="cwndexecutedlginit"></a><a name="executedlginit"></a>CWnd::실행DlgInit

대화 상자 리소스를 시작합니다.

```
BOOL ExecuteDlgInit(LPCTSTR lpszResourceName);
BOOL ExecuteDlgInit(LPVOID lpResource);
```

### <a name="parameters"></a>매개 변수

*lpszResourceName*<br/>
리소스 이름을 지정하는 null 종료된 문자열에 대한 포인터입니다.

*lpResource*<br/>
리소스에 대한 포인터입니다.

### <a name="return-value"></a>Return Value

대화 상자 리소스가 실행되는 경우 TRUE입니다. 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

`ExecuteDlgInit`을 사용한다. 이렇게 하려면 `ExecuteDlgInit` 을 호출하여 `AfxFindResourceHandle`리소스 핸들을 찾습니다. MFC 응용 프로그램이 공유 DLL(MFCx0[U][D]를 사용하지 않는 경우. DLL)는 `AfxFindResourceHandle` 실행 에 대한 현재 리소스 핸들을 반환하는 [AfxGetResourceHandle을](application-information-and-management.md#afxgetresourcehandle)호출합니다. MFCx0[U][D]를 사용하는 MFC 응용 프로그램의 경우. DLL은 `AfxFindResourceHandle` 올바른 `CDynLinkLibrary` 리소스 핸들을 찾는 공유 및 MFC 확장 DLL의 개체 목록을 트래버스합니다.

## <a name="cwndfiltertooltipmessage"></a><a name="filtertooltipmessage"></a>CWnd::필터도구팁메시지

도구 팁 메시지를 표시 하기 위해 프레임 워크에 의해 호출 됩니다.

```cpp
void FilterToolTipMessage(MSG* pMsg);
```

### <a name="parameters"></a>매개 변수

*pMsg*<br/>
도구 팁 메시지에 대한 포인터입니다.

### <a name="remarks"></a>설명

대부분의 MFC 응용 프로그램에서이 메서드는 [PreTranslateMessage](#pretranslatemessage) 및 [EnableToolTips의](#enabletooltips)프레임 워크에서 호출 됩니다., 그리고 직접 호출 할 필요가 없습니다.

그러나 특정 응용 프로그램(예: 일부 ActiveX 컨트롤)에서는 이러한 메서드가 프레임워크에서 호출되지 않을 수 있으므로 FilterToolTipMessage를 직접 호출해야 합니다. 자세한 내용은 [도구 만들기 팁을](../../mfc/methods-of-creating-tool-tips.md)참조하세요.

## <a name="cwndfindwindow"></a><a name="findwindow"></a>CWnd::찾기 창

`CWnd` *lpszClassName에* 의해 주어진 창 클래스와 창 이름 또는 제목이 *lpszWindowName에*의해 지정된 최상위 수준을 반환합니다.

```
static CWnd* PASCAL FindWindow(
    LPCTSTR lpszClassName,
    LPCTSTR lpszWindowName);
```

### <a name="parameters"></a>매개 변수

*lpszClassName*<br/>
창의 클래스 이름(구조)을 지정하는 null 종료된 문자열을 `WNDCLASS` 가리킵니다. *lpClassName이* NULL이면 모든 클래스 이름이 일치합니다.

*lpszWindowName*<br/>
창 이름(창 제목)을 지정하는 null 종료된 문자열을 가리킵니다. *lpWindowName이* NULL이면 모든 창 이름이 일치합니다.

### <a name="return-value"></a>Return Value

지정된 클래스 이름과 창 이름이 있는 창을 식별합니다. 이러한 창이 없는 경우 NULL입니다.

※ `CWnd`일시적일 수 있으며 나중에 사용할 수 없습니다.

### <a name="remarks"></a>설명

이 함수는 자식 창을 검색하지 않습니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#94](../../mfc/reference/codesnippet/cpp/cwnd-class_33.cpp)]

## <a name="cwndfindwindowex"></a><a name="findwindowex"></a>CWnd::찾기 윈도우 엑스

클래스 이름과 창 이름이 지정된 문자열과 일치하는 창 개체를 검색합니다.

```
static CWnd* FindWindowEx(
    HWND hwndParent,
    HWND hwndChildAfter,
    LPCTSTR lpszClass,
    LPCTSTR lpszWindow);
```

### <a name="parameters"></a>매개 변수

*hwndParent*<br/>
하위 창을 검색할 상위 창을 처리합니다.

*hwndChild 애프터*<br/>
자식 창에 핸들을 보입니다. 검색은 Z 순서의 다음 자식 창으로 시작됩니다. 자식 창은 하위 창이 아니라 하위 창이 아닌 *hwndParent의*직접 자식 창이어야 합니다.

*lpszClass*<br/>
[RegisterClass](/windows/win32/api/winuser/nf-winuser-registerclassw) 또는 [RegisterClassEx에](/windows/win32/api/winuser/nf-winuser-registerclassexw)대한 이전 호출에서 만든 클래스 이름 또는 클래스 원자를 지정하는 null-terminated 문자열에 대한 포인터 .

*lpsz윈도우*<br/>
창 이름(창의 제목)을 지정하는 null 종료된 문자열에 대한 포인터입니다. 이 매개 변수가 NULL이면 모든 창 이름이 일치합니다.

### <a name="return-value"></a>Return Value

함수가 성공하면 반환 값은 지정된 클래스 및 창 이름을 갖는 창 개체에 대한 포인터입니다. 함수가 실패하면 반환 값은 NULL입니다.

### <a name="remarks"></a>설명

이 멤버 함수는 Windows SDK에 설명된 대로 [FindWindowEx](/windows/win32/api/winuser/nf-winuser-findwindowexw)함수의 기능을 에뮬레이트합니다.

## <a name="cwndflashwindow"></a><a name="flashwindow"></a>CWnd::플래시 윈도우

지정된 창을 한 번 깜박입니다.

```
BOOL FlashWindow(BOOL bInvert);
```

### <a name="parameters"></a>매개 변수

*b 반전*<br/>
을 `CWnd` 플래시할지 원래 상태로 반환할지 여부를 지정합니다. `CWnd` *bInvert가* TRUE인 경우 한 상태에서 다른 상태로 깜박입니다. *bInvert가* FALSE이면 창이 원래 상태(활성 또는 비활성)로 반환됩니다.

### <a name="return-value"></a>Return Value

`FlashWindow` 멤버 함수에 대한 호출 전에 창이 활성 상태인 경우 Nonzero; 그렇지 않으면 0.

### <a name="remarks"></a>설명

연속적인 깜박임의 경우 시스템 타이머를 `FlashWindow`만들고 반복적으로 호출합니다. 제목 표시줄의 `CWnd` `CWnd` 모양을 비활성 상태에서 활성 상태로 변경하거나 그 반대로 변경하는 방법을 깜박입니다. 비활성 제목 표시줄이 활성 제목 표시줄로 변경되며 활성 제목 표시줄이 비활성 제목 표시줄로 변경됩니다.

일반적으로 사용자에게 주의가 필요하지만 현재 입력 포커스가 없다는 것을 알리기 위해 창이 깜박입니다.

*bInvert* 매개 변수는 창이 입력 포커스를 얻고 더 이상 깜박일 때만 FALSE여야 합니다. 입력 포커스를 얻기 위해 기다리는 동안 연속 호출에서 TRUE여야 합니다.

이 함수는 최소화된 창에 대해 항상 0이 아닌 것을 반환합니다. 창이 최소화되면 단순히 `FlashWindow` 창의 아이콘이 깜박입니다. b최소화된 창에 대해 *Invert는* 무시됩니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#95](../../mfc/reference/codesnippet/cpp/cwnd-class_34.cpp)]

## <a name="cwndflashwindowex"></a><a name="flashwindowex"></a>CWnd::플래시 윈도우 엑스

지정된 창을 깜박입니다.

```
BOOL FlashWindowEx(
    DWORD dwFlags,
    UINT uCount,
    DWORD dwTimeout);
```

### <a name="parameters"></a>매개 변수

*dwFlags*<br/>
플래시 상태를 지정합니다. 전체 값 목록은 [FLASHWINFO](/windows/win32/api/winuser/ns-winuser-flashwinfo) 구조를 참조하십시오.

*uCount*<br/>
창을 깜박이는 횟수를 지정합니다.

*dw타임아웃*<br/>
창이 깜박이는 속도를 밀리초 단위로 지정합니다. *dwTimeout이* 0이면 함수는 기본 커서 깜박임 속도를 사용합니다.

### <a name="return-value"></a>Return Value

반환 값은 함수를 호출하기 전에 창의 `FlashWindowEx` 상태를 지정합니다. 호출 전에 창 캡션이 활성으로 그려진 경우 반환 값은 0이 아닙니다. 그렇지 않으면 반환 값이 0입니다.

### <a name="remarks"></a>설명

이 메서드는 Windows SDK에 설명된 대로 [FlashWindowEx](/windows/win32/api/winuser/nf-winuser-flashwindowex)기능의 기능을 에뮬레이트합니다.

## <a name="cwndfromhandle"></a><a name="fromhandle"></a>CWnd::에서 핸들

창에 핸들을 지정한 경우 `CWnd` 개체에 대한 포인터를 반환합니다. `CWnd` 개체가 핸들에 연결되지 않은 경우 임시 `CWnd` 개체를 만들어 연결합니다.

```
static CWnd* PASCAL FromHandle(HWND hWnd);
```

### <a name="parameters"></a>매개 변수

*Hwnd*<br/>
창창의 HWND입니다.

### <a name="return-value"></a>Return Value

창에 핸들을 지정한 경우 `CWnd` 개체에 대한 포인터를 반환합니다. `CWnd` 개체가 핸들에 연결되지 않은 경우 임시 `CWnd` 개체를 만들어 연결합니다.

해당 포인터는 임시적이며, 나중에 사용하려고 저장하면 안됩니다.

## <a name="cwndfromhandlepermanent"></a><a name="fromhandlepermanent"></a>CWnd::에서핸들 영구

창에 핸들을 지정한 경우 `CWnd` 개체에 대한 포인터를 반환합니다.

```
static CWnd* PASCAL FromHandlePermanent(HWND hWnd);
```

### <a name="parameters"></a>매개 변수

*Hwnd*<br/>
창창의 HWND입니다.

### <a name="return-value"></a>Return Value

`CWnd` 개체에 대한 포인터입니다.

### <a name="remarks"></a>설명

개체가 `CWnd` 핸들에 연결되지 않은 경우 NULL이 반환됩니다.

이 함수는 [FromHandle과](#fromhandle)달리 임시 개체를 만들지 않습니다.

## <a name="cwndget_accchild"></a><a name="get_accchild"></a>CWnd::get_accChild

지정된 자식의 `IDispatch` 인터페이스 주소를 검색하기 위해 프레임워크에서 호출됩니다.

```
virtual HRESULT get_accChild(
    VARIANT varChild,
    IDispatch** ppdispChild);
```

### <a name="parameters"></a>매개 변수

*바차일드*<br/>
`IDispatch` 인터페이스를 검색할 자식을 식별합니다.

*ppdispChild*<br/>
자식 개체의 `IDispatch` 인터페이스 의 주소를 받습니다.

### <a name="return-value"></a>Return Value

성공 시 S_OK 반환하고 실패시 COM 오류 코드를 반환합니다. Windows SDK에서 [iAccessible::get_accChild](/windows/win32/api/oleacc/nf-oleacc-iaccessible-get_accchild) 반환 **값을** 참조하십시오.

### <a name="remarks"></a>설명

이 기능은 MFC의 [활성 접근성](/windows/win32/WinAuto/microsoft-active-accessibility) 지원의 일부입니다.

창이 없는 사용자 `CWnd`인터페이스 요소(MFC가 처리하는 창 없는 ActiveX 컨트롤 제외)가 있는 경우 파생 클래스에서 이 함수를 재정의합니다.

자세한 내용은 Windows SDK에서 [IAccessible:get_accChild](/windows/win32/api/oleacc/nf-oleacc-iaccessible-get_accchild) 을 참조하십시오.

## <a name="cwndget_accchildcount"></a><a name="get_accchildcount"></a>CWnd::get_accChildCount

이 개체에 속하는 자식 수를 검색하기 위해 프레임워크에서 호출됩니다.

```
virtual HRESULT get_accChildCount(long* pcountChildren);
```

### <a name="parameters"></a>매개 변수

*포카운트어린이*<br/>
자식 수를 받습니다.

### <a name="return-value"></a>Return Value

성공 시 S_OK 반환하고 실패시 COM 오류 코드를 반환합니다. Windows SDK에서 [iAccessible::get_accChildCount](/windows/win32/api/oleacc/nf-oleacc-iaccessible-get_accchildcount) **반환 값을** 참조하십시오.

### <a name="remarks"></a>설명

이 기능은 MFC의 [활성 접근성](/windows/win32/WinAuto/microsoft-active-accessibility) 지원의 일부입니다.

창이 없는 사용자 `CWnd`인터페이스 요소(MFC가 처리하는 창 없는 ActiveX 컨트롤 제외)가 있는 경우 파생 클래스에서 이 함수를 재정의합니다. 기본 클래스 버전을 호출한 다음 창이 없는 자식 요소를 추가합니다.

자세한 내용은 Windows SDK에서 [IAccessible:get_accChildCount](/windows/win32/api/oleacc/nf-oleacc-iaccessible-get_accchildcount) 를 참조하십시오.

## <a name="cwndget_accdefaultaction"></a><a name="get_accdefaultaction"></a>CWnd::get_accDefaultAction

개체의 기본 작업을 설명하는 문자열을 검색하기 위해 프레임워크에서 호출됩니다.

```
virtual HRESULT get_accDefaultAction(
    VARIANT varChild,
    BSTR* pszDefaultAction);
```

### <a name="parameters"></a>매개 변수

*바차일드*<br/>
검색할 기본 작업이 개체의 작업인지 아니면 개체의 자식 요소 중 하나인지 지정합니다. 이 매개 변수는 CHILDID_SELF(개체에 대한 정보를 얻기 위해) 또는 자식 ID(개체의 자식 요소에 대한 정보를 얻기 위해)일 수 있습니다.

*pszDefaultAction*<br/>
지정된 개체에 대한 기본 작업을 설명하는 지역화된 문자열을 수신하는 BSTR의 주소 또는 이 개체에 기본 작업이 없는 경우 NULL입니다.

### <a name="return-value"></a>Return Value

성공 시 S_OK 반환하고 실패시 COM 오류 코드를 반환합니다. Windows SDK에서 [iAccessible::get_accDefaultAction](/windows/win32/api/oleacc/nf-oleacc-iaccessible-get_accdefaultaction) 반환 **값을** 참조하십시오.

### <a name="remarks"></a>설명

이 기능은 MFC의 [활성 접근성](/windows/win32/WinAuto/microsoft-active-accessibility) 지원의 일부입니다.

`CWnd`-derived 클래스에서 이 함수를 재정의하여 개체의 기본 동작을 설명합니다.

자세한 내용은 Windows SDK에서 [IAccessible:get_accDefaultAction](/windows/win32/api/oleacc/nf-oleacc-iaccessible-get_accdefaultaction) 를 참조하십시오.

## <a name="cwndget_accdescription"></a><a name="get_accdescription"></a>CWnd::get_accDescription

지정한 개체의 모양을 설명하는 문자열을 검색하기 위해 프레임워크에서 호출됩니다.

```
virtual HRESULT get_accDescription(
    VARIANT varChild,
    BSTR* pszDescription);
```

### <a name="parameters"></a>매개 변수

*바차일드*<br/>
검색할 설명이 개체의 설명인지 아니면 개체의 자식 요소 중 하나인지 지정합니다. 이 매개 변수는 CHILDID_SELF(개체에 대한 정보를 얻기 위해) 또는 자식 ID(개체의 자식 요소에 대한 정보를 얻기 위해)일 수 있습니다.

*pszDescription*<br/>
지정된 개체를 설명하는 지역화된 문자열을 수신하는 BSTR의 주소 또는 이 개체에 사용할 수 있는 설명이 없는 경우 NULL입니다.

### <a name="return-value"></a>Return Value

성공 시 S_OK 반환하고 실패시 COM 오류 코드를 반환합니다. Windows SDK에서 [iAccessible::get_accDescription](/windows/win32/api/oleacc/nf-oleacc-iaccessible-get_accdescription) **반환 값을** 참조하십시오.

### <a name="remarks"></a>설명

이 기능은 MFC의 [활성 접근성](/windows/win32/WinAuto/microsoft-active-accessibility) 지원의 일부입니다.

`CWnd`-derived 클래스에서 이 함수를 재정의하여 개체를 설명합니다. 기본 클래스 버전을 호출하고 설명을 추가합니다.

자세한 내용은 Windows SDK에서 [IAccessible:get_accDescription](/windows/win32/api/oleacc/nf-oleacc-iaccessible-get_accdescription) 를 참조하십시오.

## <a name="cwndget_accfocus"></a><a name="get_accfocus"></a>CWnd::get_accFocus

키보드 포커스가 있는 개체를 검색하기 위해 프레임워크에서 호출됩니다.

```
virtual HRESULT get_accFocus(VARIANT* pvarChild);
```

### <a name="parameters"></a>매개 변수

*pvarChild*<br/>
포커스가 있는 개체에 대한 정보를 수신합니다. [I액세스::get_accFocus](/windows/win32/api/oleacc/nf-oleacc-iaccessible-get_accfocus) Windows SDK에서 *pvarID를* 참조하십시오.

### <a name="return-value"></a>Return Value

성공 시 S_OK 반환하고 실패시 COM 오류 코드를 반환합니다. Windows **Return Values** SDK에서 `IAccessible::get_accFocus` 반환 값을 참조하십시오.

### <a name="remarks"></a>설명

이 기능은 MFC의 [활성 접근성](/windows/win32/WinAuto/microsoft-active-accessibility) 지원의 일부입니다.

창이 없는 사용자 `CWnd`인터페이스 요소(MFC가 처리하는 창 없는 ActiveX 컨트롤 제외)가 있는 경우 파생 클래스에서 이 함수를 재정의합니다.

자세한 내용은 Windows SDK에서 [IAccessible::get_accFocus](/windows/win32/api/oleacc/nf-oleacc-iaccessible-get_accfocus) 를 참조하십시오.

## <a name="cwndget_acchelp"></a><a name="get_acchelp"></a>CWnd::get_accHelp

개체의 **도움말** 속성 문자열을 검색하는 프레임워크에서 호출합니다.

```
virtual HRESULT get_accHelp(
    VARIANT varChild,
    BSTR* pszHelp);
```

### <a name="parameters"></a>매개 변수

*바차일드*<br/>
검색할 도움말 정보가 개체의 도움말 정보인지 아니면 개체의 자식 요소 중 하나인지 지정합니다. 이 매개 변수는 CHILDID_SELF(개체에 대한 정보를 얻기 위해) 또는 자식 ID(개체의 자식 요소에 대한 정보를 얻기 위해)일 수 있습니다.

*pszHelp*<br/>
지정된 개체에 대한 도움말 정보를 포함하는 지역화된 문자열을 수신하는 BSTR의 주소 또는 도움말 정보를 사용할 수 없는 경우 NULL입니다.

### <a name="return-value"></a>Return Value

성공 시 S_OK 반환하고 실패시 COM 오류 코드를 반환합니다. Windows SDK에서 [iAccessible::get_accHelp](/windows/win32/api/oleacc/nf-oleacc-iaccessible-get_acchelp) **반환 값을** 참조하십시오.

### <a name="remarks"></a>설명

이 기능은 MFC의 [활성 접근성](/windows/win32/WinAuto/microsoft-active-accessibility) 지원의 일부입니다.

`CWnd`-derived 클래스에서 이 함수를 재정의하여 개체에 대한 도움말 텍스트를 제공합니다.

자세한 내용은 Windows SDK에서 [IAccessible:get_accHelp](/windows/win32/api/oleacc/nf-oleacc-iaccessible-get_acchelp) 을 참조하십시오.

## <a name="cwndget_acchelptopic"></a><a name="get_acchelptopic"></a>CWnd::get_accHelpTopic

지정된 개체와 연결된 **WinHelp** 파일의 전체 경로및 해당 파일 내의 해당 토픽의 식별자를 검색하기 위해 프레임워크에서 호출합니다.

```
virtual HRESULT get_accHelpTopic(
    BSTR* pszHelpFile,
    VARIANT varChild,
    long* pidTopic);
```

### <a name="parameters"></a>매개 변수

*psz도움말파일*<br/>
지정된 개체와 연결된 `WinHelp` 파일의 전체 경로를 수신하는 BSTR의 주소(있는 경우)입니다.

*바차일드*<br/>
검색할 도움말 항목이 개체의 항목인지 아니면 개체의 자식 요소 중 하나인지 지정합니다. 이 매개 변수는 CHILDID_SELF(개체에 대한 도움말 항목을 가져오려면) 또는 자식 ID(개체의 자식 요소 중 하나에 대한 도움말 항목을 가져오기)일 수 있습니다.

*pidTopic*<br/>
지정된 개체와 연결된 도움말 파일 항목을 식별합니다. [I액세스::get_accHelpTopic](/windows/win32/api/oleacc/nf-oleacc-iaccessible-get_acchelptopic) Windows SDK에서 *pidTopic를* 참조하십시오.

### <a name="return-value"></a>Return Value

성공 시 S_OK 반환하고 실패시 COM 오류 코드를 반환합니다. Windows **Return Values** SDK에서 `IAccessible::get_accHelpTopic` 반환 값을 참조하십시오.

### <a name="remarks"></a>설명

이 기능은 MFC의 [활성 접근성](/windows/win32/WinAuto/microsoft-active-accessibility) 지원의 일부입니다.

`CWnd`-derived 클래스에서 이 함수를 재정의하여 개체에 대한 도움말 정보를 제공합니다.

자세한 내용은 Windows SDK에서 [IAccessible:get_accHelpTopic](/windows/win32/api/oleacc/nf-oleacc-iaccessible-get_acchelptopic) 을 참조하십시오.

## <a name="cwndget_acckeyboardshortcut"></a><a name="get_acckeyboardshortcut"></a>CWnd:get_accKeyboardShortcut

지정된 개체의 바로 가기 키 또는 선택키를 검색하기 위해 프레임워크에서 호출됩니다.

```
virtual HRESULT get_accKeyboardShortcut(
    VARIANT varChild,
    BSTR* pszKeyboardShortcut);
```

### <a name="parameters"></a>매개 변수

*바차일드*<br/>
검색할 키보드 단축키가 개체의 바로 가기 키인지 아니면 개체의 자식 요소 중 하나인지 지정합니다. 이 매개 변수는 CHILDID_SELF(개체에 대한 정보를 얻기 위해) 또는 자식 ID(개체의 자식 요소에 대한 정보를 얻기 위해)일 수 있습니다.

*psz키보드 바로 가기*<br/>
키보드 단축을 식별하는 지역화된 문자열을 수신하는 BSTR의 주소 또는 지정된 개체와 연결된 키보드 단축커가 없는 경우 NULL입니다.

### <a name="return-value"></a>Return Value

성공 시 S_OK 반환하고 실패시 COM 오류 코드를 반환합니다. Windows SDK에서 [iAccessible::get_accKeyboardShortcut](/windows/win32/api/oleacc/nf-oleacc-iaccessible-get_acckeyboardshortcut) 반환 **값을** 참조하십시오.

### <a name="remarks"></a>설명

이 기능은 MFC의 [활성 접근성](/windows/win32/WinAuto/microsoft-active-accessibility) 지원의 일부입니다.

`CWnd`-derived 클래스에서 이 함수를 재정의하여 개체의 키보드 단축을 식별합니다.

자세한 내용은 Windows SDK에서 [IAccessible::get_accKeyboardShortcut](/windows/win32/api/oleacc/nf-oleacc-iaccessible-get_acckeyboardshortcut) 를 참조하십시오.

## <a name="cwndget_accname"></a><a name="get_accname"></a>CWnd::get_accName

지정된 개체의 이름을 검색하기 위해 프레임워크에서 호출됩니다.

```
virtual HRESULT get_accName(
    VARIANT varChild,
    BSTR* pszName);
```

### <a name="parameters"></a>매개 변수

*바차일드*<br/>
검색할 이름이 개체의 이름인지 아니면 개체의 자식 요소 중 하나인지 지정합니다. 이 매개 변수는 CHILDID_SELF(개체에 대한 정보를 얻기 위해) 또는 자식 ID(개체의 자식 요소에 대한 정보를 얻기 위해)일 수 있습니다.

*pszName*<br/>
지정된 개체의 이름을 포함하는 문자열을 받는 BSTR의 주소입니다.

### <a name="return-value"></a>Return Value

성공 시 S_OK 반환하고 실패시 COM 오류 코드를 반환합니다. Windows SDK에서 [iAccessible::get_accName](/windows/win32/api/oleacc/nf-oleacc-iaccessible-get_accname) **반환 값을** 참조하십시오.

### <a name="remarks"></a>설명

이 기능은 MFC의 [활성 접근성](/windows/win32/WinAuto/microsoft-active-accessibility) 지원의 일부입니다.

`CWnd`-derived 클래스에서 이 함수를 재정의하여 개체 이름을 반환합니다.

자세한 내용은 Windows SDK에서 [IAccessible:get_accName](/windows/win32/api/oleacc/nf-oleacc-iaccessible-get_accname) 를 참조하십시오.

## <a name="cwndget_accparent"></a><a name="get_accparent"></a>CWnd::get_accParent

개체 부모의 `IDispatch` 인터페이스를 검색하기 위해 프레임워크에서 호출됩니다.

```
virtual HRESULT get_accParent(IDispatch** ppdispParent);
```

### <a name="parameters"></a>매개 변수

*ppdisp부모*<br/>
상위 개체 `IDispatch` 인터페이스의 주소를 받습니다. 변수는 부모가 없거나 자식이 부모에 액세스할 수 없는 경우 NULL로 설정됩니다.

### <a name="return-value"></a>Return Value

성공 시 S_OK 반환하고 실패시 COM 오류 코드를 반환합니다. Windows SDK에서 [iAccessible::get_accParent](/windows/win32/api/oleacc/nf-oleacc-iaccessible-get_accparent) **반환 값을** 참조하십시오.

### <a name="remarks"></a>설명

이 기능은 MFC의 [활성 접근성](/windows/win32/WinAuto/microsoft-active-accessibility) 지원의 일부입니다.

대부분의 경우 이 함수를 재정의할 필요가 없습니다.

자세한 내용은 Windows SDK에서 [IAccessible:get_accParent](/windows/win32/api/oleacc/nf-oleacc-iaccessible-get_accparent) 을 참조하십시오.

## <a name="cwndget_accrole"></a><a name="get_accrole"></a>CWnd::get_accRole

지정된 개체의 역할을 설명하는 정보를 검색하기 위해 프레임워크에서 호출됩니다.

```
virtual HRESULT get_accRole(
    VARIANT varChild,
    VARIANT* pvarRole);
```

### <a name="parameters"></a>매개 변수

*바차일드*<br/>
검색할 역할 정보가 개체의 역할 정보인지 아니면 개체의 자식 요소 중 하나인지 지정합니다. 이 매개 변수는 CHILDID_SELF(개체에 대한 정보를 얻기 위해) 또는 자식 ID(개체의 자식 요소에 대한 정보를 얻기 위해)일 수 있습니다.

*프바 롤*<br/>
역할 정보를 받습니다. [I액세스::get_accRole](/windows/win32/api/oleacc/nf-oleacc-iaccessible-get_accrole) Windows SDK에서 *pvarRole을* 참조하십시오.

### <a name="return-value"></a>Return Value

성공 시 S_OK 반환하고 실패시 COM 오류 코드를 반환합니다. Windows **Return Values** SDK에서 `IAccessible::get_accRole` 반환 값을 참조하십시오.

### <a name="remarks"></a>설명

이 기능은 MFC의 [활성 접근성](/windows/win32/WinAuto/microsoft-active-accessibility) 지원의 일부입니다.

창이 없는 사용자 `CWnd`인터페이스 요소(MFC가 처리하는 창 없는 ActiveX 컨트롤 제외)가 있는 경우 파생 클래스에서 이 함수를 재정의합니다.

자세한 내용은 Windows SDK에서 [IAccessible::get_accRole](/windows/win32/api/oleacc/nf-oleacc-iaccessible-get_accrole) 를 참조하십시오.

## <a name="cwndget_accselection"></a><a name="get_accselection"></a>CWnd::get_accSelection

이 개체의 선택된 자식 개체를 검색하기 위해 프레임워크에서 호출됩니다.

```
virtual HRESULT get_accSelection(VARIANT* pvarChildren);
```

### <a name="parameters"></a>매개 변수

*pvar어린이*<br/>
선택한 자식에 대한 정보를 받습니다. [I액세스::get_accSelection](/windows/win32/api/oleacc/nf-oleacc-iaccessible-get_accselection) Windows SDK에서 *pvarChildren를* 참조하십시오.

### <a name="return-value"></a>Return Value

성공 시 S_OK 반환하고 실패시 COM 오류 코드를 반환합니다. Windows **Return Values** SDK에서 `IAccessible::get_accSelection` 반환 값을 참조하십시오.

### <a name="remarks"></a>설명

이 기능은 MFC의 [활성 접근성](/windows/win32/WinAuto/microsoft-active-accessibility) 지원의 일부입니다.

창이 없는 사용자 `CWnd`인터페이스 요소(MFC가 처리하는 창 없는 ActiveX 컨트롤 제외)가 있는 경우 파생 클래스에서 이 함수를 재정의합니다.

자세한 내용은 Windows SDK에서 [IAccessible::get_accSelection](/windows/win32/api/oleacc/nf-oleacc-iaccessible-get_accselection) 를 참조하십시오.

## <a name="cwndget_accstate"></a><a name="get_accstate"></a>CWnd::get_accState

지정된 개체의 현재 상태를 검색하기 위해 프레임워크에서 호출됩니다.

```
virtual HRESULT get_accState(
    VARIANT varChild,
    VARIANT* pvarState);
```

### <a name="parameters"></a>매개 변수

*바차일드*<br/>
검색할 상태 정보가 개체의 상태 정보인지 아니면 개체의 자식 요소 중 하나인지 지정합니다. 이 매개 변수는 CHILDID_SELF(개체에 대한 정보를 얻기 위해) 또는 자식 ID(개체의 자식 요소에 대한 정보를 얻기 위해)일 수 있습니다.

*pvar스테이트*<br/>
개체의 상태에 대한 정보를 받습니다. [I액세스::get_accState](/windows/win32/api/oleacc/nf-oleacc-iaccessible-get_accstate) Windows SDK에서 *pvarState를* 참조하십시오.

### <a name="return-value"></a>Return Value

성공 시 S_OK 반환하고 실패시 COM 오류 코드를 반환합니다. Windows **Return Values** SDK에서 `IAccessible::get_accState` 반환 값을 참조하십시오.

### <a name="remarks"></a>설명

이 기능은 MFC의 [활성 접근성](/windows/win32/WinAuto/microsoft-active-accessibility) 지원의 일부입니다.

창이 없는 사용자 `CWnd`인터페이스 요소(MFC가 처리하는 창 없는 ActiveX 컨트롤 제외)가 있는 경우 파생 클래스에서 이 함수를 재정의합니다.

자세한 내용은 Windows SDK에서 [IAccessible:get_accState](/windows/win32/api/oleacc/nf-oleacc-iaccessible-get_accstate) 을 참조하십시오.

## <a name="cwndget_accvalue"></a><a name="get_accvalue"></a>CWnd::get_accValue

지정된 개체의 값을 검색하기 위해 프레임워크에서 호출됩니다.

```
virtual HRESULT get_accValue(
    VARIANT varChild,
    BSTR* pszValue);
```

### <a name="parameters"></a>매개 변수

*바차일드*<br/>
검색할 값 정보가 개체의 값 정보인지 아니면 개체의 자식 요소 중 하나인지 지정합니다. 이 매개 변수는 CHILDID_SELF(개체에 대한 정보를 얻기 위해) 또는 자식 ID(개체의 자식 요소에 대한 정보를 얻기 위해)일 수 있습니다.

*psz값*<br/>
개체의 현재 값을 포함하는 지역화된 문자열을 받는 BSTR의 주소입니다.

### <a name="return-value"></a>Return Value

성공 시 S_OK 반환하고 실패시 COM 오류 코드를 반환합니다. Windows SDK에서 [iAccessible::get_accValue](/windows/win32/api/oleacc/nf-oleacc-iaccessible-get_accvalue) **반환 값을** 참조하십시오.

### <a name="remarks"></a>설명

이 기능은 MFC의 [활성 접근성](/windows/win32/WinAuto/microsoft-active-accessibility) 지원의 일부입니다.

창이 없는 사용자 `CWnd`인터페이스 요소(MFC가 처리하는 창 없는 ActiveX 컨트롤 제외)가 있는 경우 파생 클래스에서 이 함수를 재정의합니다.

자세한 내용은 Windows SDK에서 [IAccessible:get_accValue](/windows/win32/api/oleacc/nf-oleacc-iaccessible-get_accvalue) 를 참조하십시오.

## <a name="cwndgetactivewindow"></a><a name="getactivewindow"></a>CWnd::GetActiveWindow

활성 창에 대한 포인터를 검색합니다.

```
static CWnd* PASCAL GetActiveWindow();
```

### <a name="return-value"></a>Return Value

호출 시 활성 창이 없는 경우 활성 창 또는 NULL입니다. 해당 포인터는 임시적이며, 나중에 사용하려고 저장하면 안됩니다.

### <a name="remarks"></a>설명

활성 창은 현재 입력 포커스가 있는 창이거나 [SetActiveWindow](#setactivewindow) 멤버 함수에서 명시적으로 활성화된 창입니다.

## <a name="cwndgetancestor"></a><a name="getancestor"></a>CWnd::GetAncestor

지정된 창의 상위 창 개체를 검색합니다.

```
CWnd* GetAncestor(UINT gaFlags) const;
```

### <a name="parameters"></a>매개 변수

*가플래그*<br/>
검색할 상위 항목을 지정합니다. 가능한 값의 전체 목록은 [GetAncestor](/windows/win32/api/winuser/nf-winuser-getancestor)를 참조하십시오.

### <a name="return-value"></a>Return Value

함수가 성공하면 반환 값은 상위 창 개체에 대한 포인터입니다. 함수가 실패하면 반환 값은 NULL입니다.

### <a name="remarks"></a>설명

이 멤버 함수는 Windows SDK에 설명된 대로 [GetAncestor](/windows/win32/api/winuser/nf-winuser-getancestor)함수의 기능을 에뮬레이트합니다.

## <a name="cwndgetcapture"></a><a name="getcapture"></a>CWnd::GetCapture

마우스 캡처가 있는 창을 검색합니다.

```
static CWnd* PASCAL GetCapture();
```

### <a name="return-value"></a>Return Value

마우스 캡처가 있는 창을 식별합니다. 창에 마우스 캡처가 없는 경우 NULL입니다.

반환 값은 일시적일 수 있으며 나중에 사용할 수 없습니다.

### <a name="remarks"></a>설명

지정된 시간에 하나의 창만 마우스 캡처가 있습니다. [창은 SetCapture](#setcapture) 멤버 함수가 호출될 때 마우스 캡처를 수신합니다. 이 창은 커서가 테두리 내에 있는지 여부에 관계없이 마우스 입력을 받습니다.

## <a name="cwndgetcaretpos"></a><a name="getcaretpos"></a>CWnd::GetCaretPos

클라이언트 좌표를 캐리트의 현재 위치의 좌표를 검색하고 `CPoint`로 반환합니다.

```
static CPoint PASCAL GetCaretPos();
```

### <a name="return-value"></a>Return Value

카포트 위치의 좌표를 포함하는 [CPoint](../../atl-mfc-shared/reference/cpoint-class.md) 개체입니다.

### <a name="remarks"></a>설명

카포트 위치는 창의 클라이언트 좌표에 `CWnd` 지정됩니다.

## <a name="cwndgetcheckedradiobutton"></a><a name="getcheckedradiobutton"></a>CWnd::GetChecked라디오 버튼

지정된 그룹에서 현재 확인된 라디오 단추의 ID를 검색합니다.

```
int GetCheckedRadioButton(
    int nIDFirstButton,
    int nIDLastButton);
```

### <a name="parameters"></a>매개 변수

*니드퍼스트버튼*<br/>
그룹의 첫 번째 라디오 단추의 정수 식별자를 지정합니다.

*니드라스트 버튼*<br/>
그룹의 마지막 라디오 단추의 정수 식별자를 지정합니다.

### <a name="return-value"></a>Return Value

선택된 라디오 단추의 ID또는 선택되지 않은 경우 0입니다.

## <a name="cwndgetclientrect"></a><a name="getclientrect"></a>CWnd::GetClientRect

`CWnd` 클라이언트 영역의 클라이언트 좌표를 *lpRect로*가리키는 구조로 복사합니다.

```cpp
void GetClientRect(LPRECT lpRect) const;
```

### <a name="parameters"></a>매개 변수

*Lprect*<br/>
클라이언트 좌표를 수신할 `CRect` [RECT 구조](/windows/win32/api/windef/ns-windef-rect) 또는 개체를 가리킵니다. `left` 및 `top` 멤버는 0이 됩니다. `right` 및 `bottom` 멤버에는 창의 너비와 높이가 포함됩니다.

### <a name="remarks"></a>설명

클라이언트 좌표는 클라이언트 영역의 왼쪽 위 및 오른쪽 아래 모서리를 지정합니다. 클라이언트 좌표는 `CWnd` 클라이언트 영역의 왼쪽 위 모서리를 기준으로 하므로 왼쪽 위 모서리의 좌표는 (0,0)입니다.

### <a name="example"></a>예제

  [CWnd::IsIconic에](#isiconic)대한 예제를 참조하십시오.

## <a name="cwndgetclipboardowner"></a><a name="getclipboardowner"></a>CWnd::GetClipboard소유자

클립보드의 현재 소유자를 검색합니다.

```
static CWnd* PASCAL GetClipboardOwner();
```

### <a name="return-value"></a>Return Value

함수가 성공하면 클립보드를 소유하는 창을 식별합니다. 그렇지 않으면 NULL입니다.

반환된 포인터는 일시적일 수 있으며 나중에 사용할 수 없습니다.

### <a name="remarks"></a>설명

클립보드에는 현재 소유되어 있지 않더라도 데이터가 포함될 수 있습니다.

## <a name="cwndgetclipboardviewer"></a><a name="getclipboardviewer"></a>CWnd::GetClipboard뷰어

클립보드 뷰어 체인의 첫 번째 창을 검색합니다.

```
static CWnd* PASCAL GetClipboardViewer();
```

### <a name="return-value"></a>Return Value

성공하면 클립보드를 표시할 책임이 있는 창을 현재 식별합니다. 그렇지 않으면 NULL(예: 뷰어가 없는 경우).

반환된 포인터는 일시적일 수 있으며 나중에 사용할 수 없습니다.

## <a name="cwndgetcontrolunknown"></a><a name="getcontrolunknown"></a>CWnd::GetControl알 수 없음

이 멤버 함수를 호출하여 알 수 없는 OLE 컨트롤에 대한 포인터를 검색합니다.

```
LPUNKNOWN GetControlUnknown();
```

### <a name="return-value"></a>Return Value

이 `CWnd` 개체로 표시되는 OLE 컨트롤의 [IUnknown](/windows/win32/api/unknwn/nn-unknwn-iunknown) 인터페이스에 대한 포인터입니다. 이 개체가 OLE 컨트롤을 나타내지 않으면 반환 값은 NULL입니다.

### <a name="remarks"></a>설명

이 `IUnknown` 포인터를 해제해서는 안 됩니다. 일반적으로 컨트롤의 특정 인터페이스를 가져오는 데 사용 됩니다.

반환되는 `GetControlUnknown` 인터페이스 포인터는 참조 로 계산되지 않습니다. 이전에 [IUnknown:AddRef를](/windows/win32/api/unknwn/nf-unknwn-iunknown-addref) 호출하지 않는 한 포인터에서 [IUnknown::Release를](/windows/win32/api/unknwn/nf-unknwn-iunknown-release) 호출하지 마십시오.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#96](../../mfc/reference/codesnippet/cpp/cwnd-class_35.cpp)]

## <a name="cwndgetcurrentmessage"></a><a name="getcurrentmessage"></a>CWnd::GetCurrentMessage

이 창에서 현재 처리 중인 메시지에 대한 포인터를 반환합니다. **On**<em>Message</em> 메시지-처리기 멤버 함수에서만 호출해야 합니다.

```
static const MSG* PASCAL GetCurrentMessage();
```

### <a name="return-value"></a>Return Value

창이 현재 처리 중인 메시지를 포함하는 [MSG](/windows/win32/api/winuser/ns-winuser-msg) 구조에 대한 포인터를 반환합니다. **On**<em>Message</em> 처리기에서 만 호출해야 합니다.

### <a name="example"></a>예제

  [CMDIFrameWnd::MDICascade에](../../mfc/reference/cmdiframewnd-class.md#mdicascade)대한 예제를 참조하십시오.

## <a name="cwndgetdc"></a><a name="getdc"></a>CWnd::GetDC

`CWnd`에 대해 지정된 클래스 스타일에 따라 클라이언트 영역에 대한 공통, 클래스 또는 개인 장치 컨텍스트에 대한 포인터를 검색합니다.

```
CDC* GetDC();
```

### <a name="return-value"></a>Return Value

성공하면 클라이언트 영역에 `CWnd` 대한 장치 컨텍스트를 식별합니다. 그렇지 않으면 반환 값은 NULL입니다. 해당 포인터는 임시적이며, 나중에 사용하려고 저장하면 안됩니다.

### <a name="remarks"></a>설명

일반적인 장치 컨텍스트의 `GetDC` 경우 검색할 때마다 컨텍스트에 기본 특성을 할당합니다. 클래스 및 개인 컨텍스트의 경우 이전에 할당된 특성은 변경되지 않은 `GetDC` 상태로 둡습니다. 디바이스 컨텍스트는 클라이언트 영역에서 그리는 후속 그래픽 디바이스 인터페이스(GDI) 함수에서 사용될 수 있다.

장치 컨텍스트가 창 클래스에 속하지 않는 한 그림 이후에 컨텍스트를 해제하려면 [ReleaseDC](#releasedc) 멤버 함수를 호출해야 합니다.

클래스에 `CWnd` 속하는 장치 컨텍스트는 클래스가 `GetDC` 등록될 때 CS_CLASSDC, CS_OWNDC 또는 CS_PARENTDC `WNDCLASS` 구조체의 스타일로 지정된 경우 멤버 함수에 의해 반환됩니다.

## <a name="cwndgetdcex"></a><a name="getdcex"></a>CWnd::GetDCEx

창에 대 한 장치 컨텍스트의 핸들을 검색 합니다. `CWnd`

```
CDC* GetDCEx(
    CRgn* prgnClip,
    DWORD flags);
```

### <a name="parameters"></a>매개 변수

*prgn클립*<br/>
클라이언트 창의 가시 영역과 결합될 수 있는 클리핑 영역을 식별합니다.

*플래그*<br/>
다음 사전 설정 값 중 하나를 가질 수 있습니다.

- DCX_CACHE OWNDC 또는 CLASSDC 창이 아닌 캐시에서 장치 컨텍스트를 반환합니다. CS_OWNDC 및 CS_CLASSDC 재정의합니다.

- DCX_CLIPCHILDREN 창 아래에 있는 모든 자식 `CWnd` 창의 표시되는 영역을 제외합니다.

- DCX_CLIPSIBLINGS 창 위에 있는 모든 형제 `CWnd` 창의 표시되는 영역을 제외합니다.

- DCX_EXCLUDERGN 반환된 장치 컨텍스트의 가시 영역에서 *prgnClip으로* 식별된 클리핑 영역을 제외합니다.

- DCX_INTERSECTRGN 반환된 장치 컨텍스트의 가시 영역 내에서 *prgnClip으로* 식별된 클리핑 영역을 교차합니다.

- DCX_LOCKWINDOWUPDATE 이 창을 제외하는 `LockWindowUpdate` 호출이 적용되는 경우에도 그리기를 허용합니다. 이 값은 추적 중에 드로잉에 사용됩니다.

- DCX_PARENTCLIP 부모 창의 표시되는 영역을 사용하고 부모 창의 WS_CLIPCHILDREN 및 WS_PARENTDC 스타일 비트를 무시합니다. 이 값은 장치 컨텍스트의 원점이 `CWnd` 창의 왼쪽 위 모서리로 설정합니다.

- DCX_WINDOW 클라이언트 사각형이 아닌 창 사각형에 해당하는 장치 컨텍스트를 반환합니다.

### <a name="return-value"></a>Return Value

함수가 성공한 경우 지정된 창에 대한 장치 컨텍스트입니다. 그렇지 않으면 NULL.

### <a name="remarks"></a>설명

장치 컨텍스트는 후속 GDI 함수에서 클라이언트 영역에 그리는 데 사용할 수 있습니다.

[GetDC](/windows/win32/api/winuser/nf-winuser-getdc) 함수의 확장자인 이 함수는 응용 프로그램에서 창에 대한 장치 컨텍스트가 잘린 방법과 인지 여부를 보다 세밀하게 제어할 수 있도록 합니다.

장치 컨텍스트가 창 클래스에 속하지 않는 한 그리기 후 컨텍스트를 해제하려면 [ReleaseDC](/windows/win32/api/winuser/nf-winuser-releasedc) 함수를 호출해야 합니다. 지정된 시간에 5개의 일반적인 장치 컨텍스트만 사용할 수 있기 때문에 장치 컨텍스트를 해제하지 않으면 다른 응용 프로그램이 장치 컨텍스트에 액세스하지 못할 수 있습니다.

캐시된 장치 컨텍스트를 가져오려면 응용 프로그램에서 [DCX_CACHE](/windows/win32/api/winuser/nf-winuser-getdcex)지정해야 합니다. DCX_CACHE 지정되지 않고 창이 CS_OWNDC [CS_CLASSDC](/windows/win32/api/winuser/ns-winuser-wndclassw)않으면 이 함수는 NULL을 반환합니다.

클래스가 등록될 때 CS_CLASSDC, [CS_OWNDC](/windows/win32/api/winuser/ns-winuser-wndclassw)또는 [CS_PARENTDC](/windows/win32/api/winuser/ns-winuser-wndclassw) 스타일이 [WNDCLASS](/windows/win32/api/winuser/ns-winuser-wndclassw) 구조에 지정된 경우 특수 특성이 있는 장치 컨텍스트가 [GetDCEx](/windows/win32/api/winuser/nf-winuser-getdcex) 함수에 의해 반환됩니다.

이러한 특성에 대한 자세한 내용은 Windows `WNDCLASS` SDK의 구조 설명을 참조하십시오.

## <a name="cwndgetdcrendertarget"></a><a name="getdcrendertarget"></a>CWnd::GetDC렌더대상

`CWnd` 창에 대한 DC(디바이스 컨텍스트) 렌더링 대상을 검색합니다.

```
CDCRenderTarget* GetDCRenderTarget();
```

### <a name="return-value"></a>Return Value

함수가 성공하면 지정된 창에 대한 디바이스 컨텍스트 렌더링 대상이고, 그렇지 않으면 NULL입니다.

### <a name="remarks"></a>설명

## <a name="cwndgetdescendantwindow"></a><a name="getdescendantwindow"></a>CWnd::GetdescendantWindow

지정된 ID에 의해 지정된 하위 창을 찾으려면 이 멤버 함수를 호출합니다.

```
CWnd* GetDescendantWindow(
    int nID,
    BOOL bOnlyPerm = FALSE) const;
```

### <a name="parameters"></a>매개 변수

*nID*<br/>
검색할 컨트롤 또는 자식 창의 식별자를 지정합니다.

*bOnlyPerm*<br/>
반환할 창이 일시적일 수 있는지 여부를 지정합니다. TRUE인 경우 영구 창만 반환할 수 있습니다. FALSE이면 함수가 임시 창을 반환할 수 있습니다. 임시 창에 대한 자세한 내용은 [기술 참고 3을](../../mfc/tn003-mapping-of-windows-handles-to-objects.md)참조하십시오.

### <a name="return-value"></a>Return Value

하위 창이 `CWnd` 없는 경우 개체 또는 NULL에 대한 포인터입니다.

### <a name="remarks"></a>설명

이 멤버 함수는 자식 자식인 창뿐만 아니라 자식 창의 전체 트리를 검색합니다.

## <a name="cwndgetdesktopwindow"></a><a name="getdesktopwindow"></a>CWnd::GetDesktop창

Windows 데스크톱 창을 반환합니다.

```
static CWnd* PASCAL GetDesktopWindow();
```

### <a name="return-value"></a>Return Value

Windows 데스크톱 창을 식별합니다. 이 포인터는 일시적일 수 있으며 나중에 사용할 수 없습니다.

### <a name="remarks"></a>설명

바탕 화면 창은 전체 화면을 덮고 모든 아이콘과 다른 창이 그려진 영역입니다.

## <a name="cwndgetdlgctrlid"></a><a name="getdlgctrlid"></a>CWnd::GetDlgCtrlID

대화 상자의 컨트롤뿐만 아니라 모든 자식 창에 대한 창 또는 컨트롤 ID 값을 반환합니다.

```
int GetDlgCtrlID() const;
```

### <a name="return-value"></a>Return Value

함수가 `CWnd` 성공한 경우 자식 창의 숫자 식별자입니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

최상위 창에는 ID 값이 없으므로 최상위 창인 경우 `CWnd` 이 함수의 반환 값이 잘못됩니다.

### <a name="example"></a>예제

  [CWnd::OnCtlColor](#onctlcolor)에 대한 예제를 참조하십시오.

## <a name="cwndgetdlgitem"></a><a name="getdlgitem"></a>CWnd::GetDlg항목

대화 상자 또는 다른 창에서 지정된 컨트롤 또는 자식 창에 대한 포인터를 검색합니다.

```
CWnd* GetDlgItem(int nID) const;

void GetDlgItem(
    int nID,
    HWND* phWnd) const;
```

### <a name="parameters"></a>매개 변수

*nID*<br/>
검색할 컨트롤 또는 자식 창의 식별자를 지정합니다.

*phwnd*<br/>
자식 창에 대한 포인터입니다.

### <a name="return-value"></a>Return Value

지정된 컨트롤 또는 자식 창에 대한 포인터입니다. *nID* 매개 변수에 의해 주어진 정수 ID를 가진 컨트롤이 없으면 값은 NULL입니다.

반환된 포인터는 일시적일 수 있으며 나중에 사용할 수 없습니다.

### <a name="remarks"></a>설명

반환된 포인터는 일반적으로 *nID로*식별된 컨트롤 유형으로 캐스팅됩니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#97](../../mfc/reference/codesnippet/cpp/cwnd-class_36.cpp)]

## <a name="cwndgetdlgitemint"></a><a name="getdlgitemint"></a>CWnd::GetDlgItemInt

*nID로*식별된 컨트롤의 텍스트를 검색합니다.

```
UINT GetDlgItemInt(
    int nID,
    BOOL* lpTrans = NULL,
    BOOL bSigned = TRUE) const;
```

### <a name="parameters"></a>매개 변수

*nID*<br/>
번역할 대화 상자 컨트롤의 정수 식별자를 지정합니다.

*lpTrans*<br/>
번역된 플래그를 받을 부울 변수를 가리킵니다.

*b 서명됨*<br/>
검색할 값이 서명되었는지 여부를 지정합니다.

### <a name="return-value"></a>Return Value

대화 상자 항목 텍스트의 번역된 값을 지정합니다. 0은 유효한 반환 값이므로 오류를 감지하는 데 *lpTrans를* 사용해야 합니다. 서명된 반환 값이 필요한 경우 **int** 유형으로 캐스팅합니다.

함수는 번역된 숫자가 INT_MAX(서명된 번호의 경우) 또는 UINT_MAX(서명되지 않은 경우)보다 큰 경우 0을 반환합니다.

비숫자 문자가 발생하고 위의 최대값을 초과하는 등의 오류가 `GetDlgItemInt` 발생하면 *lpTrans가*가리키는 위치에 0을 복사합니다. 오류가 없는 경우 *lpTrans는* 영하지 않은 값을 받습니다. *lpTrans가* NULL이면 오류에 대해 경고하지 `GetDlgItemInt` 않습니다.

### <a name="remarks"></a>설명

텍스트 의 시작 부분에 여분의 공백을 제거하고 소수 자릿수를 변환하여 지정된 대화 상자에서 지정된 컨트롤의 텍스트를 정수 값으로 변환합니다. 텍스트의 끝에 도달하거나 숫자가 없는 문자가 발생하면 번역이 중지됩니다.

*bSigned가* TRUE인 `GetDlgItemInt` 경우 텍스트 의 시작 부분에서 빼기 기호(-)를 확인하고 텍스트를 서명된 번호로 변환합니다. 그렇지 않으면 서명되지 않은 값을 만듭니다.

컨트롤에 [WM_GETTEXT](/windows/win32/winmsg/wm-gettext) 메시지를 보냅니다.

## <a name="cwndgetdlgitemtext"></a><a name="getdlgitemtext"></a>CWnd::GetDlg항목텍스트

이 멤버 함수를 호출하여 대화 상자에서 컨트롤과 연결된 제목 또는 텍스트를 검색합니다.

```
int GetDlgItemText(
    int nID,
    LPTSTR lpStr,
    int nMaxCount) const;

int GetDlgItemText(
    int nID,
    CString& rString) const;
```

### <a name="parameters"></a>매개 변수

*nID*<br/>
제목을 검색할 컨트롤의 정수 식별자를 지정합니다.

*lpStr*<br/>
버퍼를 가리키면 컨트롤의 제목이나 텍스트를 받을 수 있습니다.

*n맥스카운트*<br/>
*lpStr에*복사할 문자열의 최대 길이(문자)를 지정합니다. 문자열이 *nMaxCount보다*길면 잘립니다.

*rString*<br/>
[CString](../../atl-mfc-shared/reference/cstringt-class.md)에 대한 참조입니다.

### <a name="return-value"></a>Return Value

null 문자 종료를 포함하지 않고 버퍼에 복사된 실제 문자 수를 지정합니다. 텍스트가 복사되지 않으면 값이 0입니다.

### <a name="remarks"></a>설명

멤버 `GetDlgItemText` 함수는 *텍스트를 lpStr이* 가리키는 위치에 복사하고 복사한 바이트 수의 수를 반환합니다.

## <a name="cwndgetdsccursor"></a><a name="getdsccursor"></a>CWnd::GetDSC커서

이 멤버 함수를 호출하여 데이터 원본 컨트롤의 DataSource, UserName, 암호 및 SQL 속성에 의해 정의된 기본 커서에 대한 포인터를 검색합니다.

```
IUnknown* GetDSCCursor();
```

### <a name="return-value"></a>Return Value

데이터 원본 컨트롤에 의해 정의된 커서에 대한 포인터입니다. MFC는 `AddRef` 포인터호출을 처리합니다.

### <a name="remarks"></a>설명

반환된 포인터를 사용하여 데이터 바인딩된 그리드 컨트롤과 같은 복잡한 데이터 바인딩 컨트롤의 ICursor 속성을 설정합니다. 첫 번째 바인딩된 컨트롤이 커서를 요청할 때까지 데이터 원본 컨트롤이 활성화되지 않습니다. 이 문제는 MFC 바인딩 `GetDSCCursor` 관리자에 대한 호출또는 암시적으로 명시적으로 발생할 수 있습니다. 두 경우 모두 반환된 포인터를 `GetDSCCursor` `Release` `IUnknown`호출한 다음 에 호출하여 데이터 원본 컨트롤이 활성화되도록 강제할 수 있습니다. 활성화하면 데이터 원본 컨트롤이 기본 데이터 원본에 연결을 시도합니다. 반환된 포인터는 다음 컨텍스트에서 사용될 수 있습니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFC_AxDataBinding#5](../../mfc/reference/codesnippet/cpp/cwnd-class_37.cpp)]

## <a name="cwndgetdynamiclayout"></a><a name="getdynamiclayout"></a>CWnd::GetDynamic레이아웃

동적 레이아웃 관리자 개체에 대한 포인터를 검색합니다.

```
CMFCDynamicLayout* GetDynamicLayout();
```

### <a name="return-value"></a>Return Value

동적 레이아웃 관리자 개체이거나 동적 레이아웃을 사용하지 않을 경우 NULL입니다.

### <a name="remarks"></a>설명

창 개체는 반환된 포인터의 수명을 소유하고 관리하므로 개체에 액세스할 때만 사용해야 합니다. 포인터를 삭제하거나 포인터를 영구적으로 저장하지 마세요.

## <a name="cwndgetexstyle"></a><a name="getexstyle"></a>CWnd::GetEx스타일

창의 확장된 스타일을 반환합니다.

```
DWORD GetExStyle() const;
```

### <a name="return-value"></a>Return Value

창의 확장 된 스타일입니다. MFC에서 사용되는 확장 창 스타일에 대한 자세한 내용은 [확장 창 스타일을](../../mfc/reference/styles-used-by-mfc.md#extended-window-styles)참조하십시오.

## <a name="cwndgetfocus"></a><a name="getfocus"></a>CWnd::Get포커스

현재 입력 포커스가 `CWnd` 있는 포인터를 검색합니다.

```
static CWnd* PASCAL GetFocus();
```

### <a name="return-value"></a>Return Value

포커스 창이 없는 경우 현재 포커스가 있는 창 또는 NULL에 대한 포인터입니다.

해당 포인터는 임시적이며, 나중에 사용하려고 저장하면 안됩니다.

## <a name="cwndgetfont"></a><a name="getfont"></a>CWnd::GetFont

WM_GETFONT 메시지를 창으로 보내 현재 글꼴을 검색합니다.

```
CFont* GetFont() const;
```

### <a name="return-value"></a>Return Value

창의 현재 글꼴에 연결된 [CFont](../../mfc/reference/cfont-class.md) 개체에 대한 포인터입니다.

### <a name="remarks"></a>설명

창에서 WM_GETFONT 메시지를 처리하지 않으면 이 메서드는 영향을 주지 않습니다. WM_GETFONT 메시지에 대한 메시지 `CWnd` 처리기를 포함하는 미리 정의된 창 클래스에 연결되기 때문에 이 메시지를 처리에서 파생하는 많은 MFC 클래스입니다. 이 메서드를 사용 하려면 파생 `CWnd` 된 클래스 WM_GETFONT 메시지에 대 한 메서드 처리기를 정의 해야 합니다.

## <a name="cwndgetforegroundwindow"></a><a name="getforegroundwindow"></a>CWnd::GetForeground창

포그라운드 창(사용자가 현재 작업 중인 창)에 대한 포인터를 반환합니다.

```
static CWnd* PASCAL GetForegroundWindow();
```

### <a name="return-value"></a>Return Value

전경 창에 대한 포인터입니다. 임시 `CWnd` 개체일 수 있습니다.

### <a name="remarks"></a>설명

전경 창은 최상위 창(프레임 창 또는 대화 상자)에만 적용됩니다.

## <a name="cwndgeticon"></a><a name="geticon"></a>CWnd::GetIcon

이 멤버 함수를 호출하여 핸들을 큰(32x32) 또는 *핸들을 bBigIcon으로*표시된 작은(16x16) 아이콘으로 가져옵니다.

```
HICON GetIcon(BOOL bBigIcon) const;
```

### <a name="parameters"></a>매개 변수

*bBigIcon*<br/>
TRUE인 경우 32픽셀 x 32픽셀 아이콘을 지정합니다. FALSE인 경우 16픽셀 x 16픽셀 아이콘을 지정합니다.

### <a name="return-value"></a>Return Value

아이콘에 대한 핸들입니다. 실패하면 NULL을 반환합니다.

## <a name="cwndgetlastactivepopup"></a><a name="getlastactivepopup"></a>CWnd::GetLastActivePopup

`CWnd`가 소유한 팝업 창이 가장 최근에 활성화되었는지 확인합니다.

```
CWnd* GetLastActivePopup() const;
```

### <a name="return-value"></a>Return Value

가장 최근에 활성 팝업 창을 식별합니다. 다음 조건 중 한 가지가 충족되면 반환 값은 창 자체가 됩니다.

- 창 자체가 가장 최근에 활성화되었습니다.

- 창에 팝업 창이 없습니다.

- 창은 최상위 창이 아니거나 다른 창이 소유합니다.

해당 포인터는 임시적이며, 나중에 사용하려고 저장하면 안됩니다.

### <a name="example"></a>예제

  [CWnd::FindWindow](#findwindow)에 대한 예제를 참조하십시오.

## <a name="cwndgetlayeredwindowattributes"></a><a name="getlayeredwindowattributes"></a>CWnd::GetLayered창속성

계층적 창의 불투명도 및 투명도 색상 키를 검색합니다.

```
BOOL GetLayeredWindowAttributes(
    COLORREF* pcrKey,
    BYTE* pbAlpha,
    DWORD* pdwFlags) const;
```

### <a name="parameters"></a>매개 변수

*pcrKey*<br/>
계층화된 창을 작성할 때 사용할 투명도 색상 키를 받는 COLORREF 값에 대한 포인터입니다. 이 색상의 창으로 그려진 모든 픽셀은 투명합니다. 인수가 필요하지 않은 경우 NULL이 될 수 있습니다.

*pbAlpha*<br/>
계층화된 창의 불투명도를 설명하는 데 사용되는 알파 값을 받는 BYTE에 대한 포인터입니다. *pbAlpha에서* 참조하는 변수가 0이면 창이 완전히 투명해지요. *pbAlpha에서* 참조하는 변수가 255이면 창이 불투명합니다. 인수가 필요하지 않은 경우 NULL이 될 수 있습니다.

*pdwFlags*<br/>
계층 화 플래그를 받는 DWORD에 대한 포인터입니다. 인수가 필요하지 않은 경우 NULL이 될 수 있습니다. 가능한 값의 전체 목록은 [GetLayered창 속성](/windows/win32/api/winuser/nf-winuser-getlayeredwindowattributes)을 참조하십시오.

### <a name="return-value"></a>Return Value

함수가 성공하면 0이 아니고 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

이 멤버 함수는 Windows SDK에 설명된 대로 [GetLayeredWindowAttributes](/windows/win32/api/winuser/nf-winuser-getlayeredwindowattributes)함수의 기능을 에뮬레이트합니다.

## <a name="cwndgetmenu"></a><a name="getmenu"></a>CWnd::GetMenu

이 창의 메뉴에 대한 포인터를 검색합니다.

```
CMenu* GetMenu() const;
```

### <a name="return-value"></a>Return Value

메뉴를 식별합니다. 메뉴가 없는 `CWnd` 경우 값은 NULL입니다. 자식 창인 경우 `CWnd` 반환 값이 정의되지 않습니다.

반환된 포인터는 일시적일 수 있으며 나중에 사용할 수 없습니다.

### <a name="remarks"></a>설명

이 함수는 메뉴가 없으므로 자식 창에 사용해서는 안 됩니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#98](../../mfc/reference/codesnippet/cpp/cwnd-class_38.cpp)]

## <a name="cwndgetmenubarinfo"></a><a name="getmenubarinfo"></a>CWnd::GetMenuBarInfo

지정된 메뉴 모음에 대한 정보를 검색합니다.

```
BOOL GetMenuBarInfo(
    LONG idObject,
    LONG idItem,
    PMENUBARINFO pmbi) const;
```

### <a name="parameters"></a>매개 변수

*idObject*<br/>
메뉴 개체를 지정합니다. 가능한 값 목록은 [GetMenuBarInfo](/windows/win32/api/winuser/nf-winuser-getmenubarinfo)를 참조하십시오.

*id항목*<br/>
정보를 검색할 항목을 지정합니다. 이 매개 변수가 0이면 함수는 메뉴 자체에 대한 정보를 검색합니다. 이 매개 변수가 1이면 함수는 메뉴의 첫 번째 항목에 대한 정보를 검색합니다.

*pmbi*<br/>
정보를 수신하는 [MENUBARINFO](/windows/win32/api/winuser/ns-winuser-menubarinfo) 구조에 대한 포인터입니다.

### <a name="return-value"></a>Return Value

함수가 성공하면 0이 아니고 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

이 멤버 함수는 Windows SDK에 설명된 대로 [GetMenuBarInfo](/windows/win32/api/winuser/nf-winuser-getmenubarinfo)함수의 기능을 에뮬레이트합니다.

## <a name="cwndgetnextdlggroupitem"></a><a name="getnextdlggroupitem"></a>CWnd::GetNextDlg그룹항목

대화 상자의 컨트롤 그룹 내에서 이전 또는 다음 컨트롤을 검색합니다.

```
CWnd* GetNextDlgGroupItem(
    CWnd* pWndCtl,
    BOOL bPrevious = FALSE) const;

COleControlSiteOrWnd* GetNextDlgGroupItem(
    COleControlSiteOrWnd* pCurSiteOrWnd = NULL) const;
```

### <a name="parameters"></a>매개 변수

*pWndCtl*<br/>
검색의 시작점으로 사용할 컨트롤을 식별합니다.

*b이전*<br/>
대화 상자에서 컨트롤 그룹을 검색하는 방법을 지정합니다. TRUE이면 함수는 그룹의 이전 컨트롤을 검색합니다. FALSE이면 그룹의 다음 컨트롤을 검색합니다.

*pCurSiteOrWnd*<br/>
컨트롤을 `COleControlSiteOrWnd` 식별합니다. 에 대한 `COleControlSiteOrWnd`자세한 내용은 **비고를**참조하십시오.

### <a name="return-value"></a>Return Value

멤버 함수가 성공한 경우 그룹의 이전 또는 다음 컨트롤에 대한 포인터입니다.

반환된 포인터는 일시적일 수 있으며 나중에 사용할 수 없습니다.

### <a name="remarks"></a>설명

컨트롤 그룹은 [WS_GROUP](styles-used-by-mfc.md#window-styles) 스타일로 만든 컨트롤로 시작하여 WS_GROUP 스타일로 작성되지 않은 마지막 컨트롤로 끝납니다.

기본적으로 멤버 `GetNextDlgGroupItem` 함수는 그룹의 다음 컨트롤에 대한 포인터를 반환합니다. *pWndCtl이* 그룹의 첫 번째 컨트롤을 식별하고 *b이전이* TRUE인 `GetNextDlgGroupItem` 경우 그룹의 마지막 컨트롤에 대한 포인터를 반환합니다.

> [!NOTE]
> MFC는 창 없는 ActiveX 컨트롤, 표준 ActiveX 컨트롤 및 창을 지원하므로 HWND만 으로만 컨트롤을 참조하면 더 이상 충분하지 않습니다. 개체에는 `COleControlSiteOrWnd` 다음과 같이 개체를 창이 있는 ActiveX 컨트롤, 창 없는 ActiveX 컨트롤 또는 창으로 식별하는 정보가 포함됩니다.

|컨트롤 또는 창 유형|정보 식별|
|----------------------------|-----------------------------|
|윈도우 액티브X 컨트롤|HWND를 포함 하 고 [COleControlSite](../../mfc/reference/colecontrolsite-class.md) 개체를 연결 합니다. 의 `m_hWnd` `COleControlSiteOrWnd` 멤버는 컨트롤의 HWND로 설정되고 `m_pSite` 멤버는 컨트롤의 을 `COleControlSite`가리킵니다.|
|윈도우리스 액티브X 컨트롤|HWND가 포함되어 있지 않습니다. `m_pSite` 컨트롤의 `COleControlSiteOrWnd` `COleControlSite`가리키고 `m_hWnd` 멤버는 NULL입니다.|
|표준 창|HWND만 들어 있습니다. 멤버가 `m_hWnd` `COleControlSiteOrWnd` 창의 HWND로 설정되고 `m_pSite` 멤버가 NULL입니다.|

## <a name="cwndgetnextdlgtabitem"></a><a name="getnextdlgtabitem"></a>CWnd::GetNextDlgTabItem

[WS_TABSTOP](styles-used-by-mfc.md#window-styles) 스타일로 만들어졌으며 지정된 컨트롤 앞에 오거나 따르는 첫 번째 컨트롤에 대한 포인터를 검색합니다.

```
CWnd* GetNextDlgTabItem(
    CWnd* pWndCtl,
    BOOL bPrevious = FALSE) const;

COleControlSiteOrWnd* GetNextDlgTabItem(
    COleControlSiteOrWnd* pCurSiteOrWnd,
    BOOL bPrevious) const;
```

### <a name="parameters"></a>매개 변수

*pWndCtl*<br/>
검색의 시작점으로 사용할 컨트롤을 식별합니다.

*pCurSiteOrWnd*<br/>
컨트롤을 `COleControlSiteOrWnd` 식별합니다. 자세한 `COleControlSiteOrWnd`내용은 [CWnd::GetNextDlgGroupItem](#getnextdlggroupitem)을 참조하십시오.

*b이전*<br/>
함수가 대화 상자를 검색하는 방법을 지정합니다. TRUE이면 함수는 대화 상자에서 이전 컨트롤을 검색합니다. FALSE이면 다음 컨트롤을 검색합니다.

### <a name="return-value"></a>Return Value

멤버 함수가 성공한 경우 WS_TABSTOP 스타일이 있는 이전 또는 다음 컨트롤에 대한 포인터입니다.

반환된 포인터는 일시적일 수 있으며 나중에 사용할 수 없습니다.

자세한 `COleControlSiteOrWnd`내용은 [CWnd::GetNextDlgGroupItem](#getnextdlggroupitem)을 참조하십시오.

## <a name="cwndgetnextwindow"></a><a name="getnextwindow"></a>CWnd::GetNextWindow

창 관리자 목록에서 다음(또는 이전) 창을 검색합니다.

```
CWnd* GetNextWindow(UINT nFlag = GW_HWNDNEXT) const;
```

### <a name="parameters"></a>매개 변수

*n 플래그*<br/>
함수가 다음 창또는 이전 창에 대한 포인터를 반환할지 여부를 지정합니다. GW_HWNDNEXT 창 관리자 목록의 `CWnd` 개체 다음에 오는 창을 반환하거나 창 관리자 목록의 이전 창을 반환하는 GW_HWNDPREV 수 있습니다.

### <a name="return-value"></a>Return Value

멤버 함수가 성공한 경우 창 관리자 목록에서 다음(또는 이전) 창을 식별합니다.

반환된 포인터는 일시적일 수 있으며 나중에 사용할 수 없습니다.

### <a name="remarks"></a>설명

창 관리자의 목록에는 모든 최상위 창, 연결된 하위 창 및 하위 창의 자식 창에 대한 항목이 포함되어 있습니다.

최상위 `CWnd` 창인 경우 함수는 다음(또는 이전) 최상위 창을 검색합니다. 자식 창인 경우 `CWnd` 함수는 다음(또는 이전) 자식 창을 검색합니다.

## <a name="cwndgetolecontrolsite"></a><a name="getolecontrolsite"></a>CWnd::Getole제어사이트

지정된 ActiveX 컨트롤에 대한 사용자 지정 사이트를 검색합니다.

```
COleControlSite* GetOleControlSite(UINT idControl) const;
```

### <a name="parameters"></a>매개 변수

*idControl*<br/>
ActiveX 컨트롤의 ID입니다.

## <a name="cwndgetopenclipboardwindow"></a><a name="getopenclipboardwindow"></a>CWnd::GetOpen클립보드창

현재 클립보드가 열려 있는 창의 핸들을 검색합니다.

```
static CWnd* PASCAL GetOpenClipboardWindow();
```

### <a name="return-value"></a>Return Value

함수가 성공하면 현재 클립보드가 열려 있는 창의 핸들입니다. 그렇지 않으면 NULL.

## <a name="cwndgetowner"></a><a name="getowner"></a>CWnd::GetOwner

창 소유자에 대한 포인터를 검색합니다.

```
CWnd* GetOwner() const;
```

### <a name="return-value"></a>Return Value

`CWnd` 개체에 대한 포인터입니다.

### <a name="remarks"></a>설명

창에 소유자가 없는 경우 부모 창 개체에 대한 포인터가 기본적으로 반환됩니다. 소유자와 소유 자 간의 관계는 몇 가지 중요한 측면에서 부모-자식 측면과 다릅니다. 예를 들어 부모가 있는 창은 부모 창의 클라이언트 영역에 국한됩니다. 소유한 창은 바탕 화면의 모든 위치에서 그릴 수 있습니다.

이 함수의 소유권 개념은 [GetWindow의](/windows/win32/api/winuser/nf-winuser-getwindow)소유권 개념과 다릅니다.

## <a name="cwndgetparent"></a><a name="getparent"></a>CWnd::GetParent

이 함수를 호출하여 자식 창의 부모 창(있는 경우)에 대한 포인터를 가져옵니다.

```
CWnd* GetParent() const;
```

### <a name="return-value"></a>Return Value

Windows SDK의 [GetParent에서](/windows/win32/api/winuser/nf-winuser-getparent) 반환 값 섹션을 참조하십시오.

### <a name="remarks"></a>설명

함수는 `GetParent` 바로 부모에 대한 포인터를 반환합니다(있는 경우). 반대로 [GetParentOwner](#getparentowner) 함수는 자식 창이 아닌 가장 즉각적인 부모 또는 소유자 창에 대한 포인터를 반환합니다(WS_CHILD 스타일이 없습니다). 하위 창 내에 하위 창이 `GetParent` `GetParentOwner` 있고 다른 결과를 반환하는 경우.

## <a name="cwndgetparentframe"></a><a name="getparentframe"></a>CWnd::GetParentFrame

이 멤버 함수를 호출하여 부모 프레임 창을 검색합니다.

```
CFrameWnd* GetParentFrame() const;
```

### <a name="return-value"></a>Return Value

성공하면 프레임 창에 대한 포인터입니다. 그렇지 않으면 NULL.

### <a name="remarks"></a>설명

멤버 함수는 [CFrameWnd(또는](../../mfc/reference/cframewnd-class.md) 파생 클래스) 개체가 발견될 때까지 부모 체인을 검색합니다.

## <a name="cwndgetparentowner"></a><a name="getparentowner"></a>CWnd::GetParent소유자

이 멤버 함수를 호출하여 자식 창의 부모 창 또는 소유자 창에 대한 포인터를 가져옵니다.

```
CWnd* GetParentOwner() const;
```

### <a name="return-value"></a>Return Value

`CWnd` 개체에 대한 포인터입니다. `CWnd` 개체가 핸들에 연결되지 않은 경우 임시 `CWnd` 개체를 만들어 연결합니다. 해당 포인터는 임시적이며, 나중에 사용하려고 저장하면 안됩니다.

### <a name="remarks"></a>설명

`GetParentOwner`하위 창이 아닌 가장 즉각적인 부모 또는 소유자 창에 대한 포인터를 반환합니다(WS_CHILD 스타일이 없습니다). 현재 소유자 창은 [SetOwner](#setowner)로 설정할 수 있습니다. 기본적으로 창의 부모는 해당 소유자입니다.

반대로 [GetParent](#getparent) 함수는 하위 창인지 여부에 관계없이 바로 부모에 대한 포인터를 반환합니다. 하위 창 내에 하위 창이 `GetParent` `GetParentOwner` 있고 다른 결과를 반환하는 경우.

## <a name="cwndgetproperty"></a><a name="getproperty"></a>CWnd::GetProperty

*dwDispID에*의해 지정된 ActiveX 컨트롤 속성을 얻으려면 이 멤버 함수를 호출합니다.

```cpp
void GetProperty(
    DISPID dwDispID,
    VARTYPE vtProp,
    void* pvProp)const;
```

### <a name="parameters"></a>매개 변수

*dwDispID*<br/>
검색할 속성을 식별합니다.

*vtProp*<br/>
검색할 속성의 형식을 지정합니다. 가능한 값에 대해서는 [COleDispatchDriver::InvokeHelper](../../mfc/reference/coledispatchdriver-class.md#invokehelper)의 설명 섹션을 참조하세요.

*pvProp*<br/>
속성 값을 받을 변수의 주소입니다. *vtProp*에서 지정한 형식과 일치해야 합니다.

### <a name="remarks"></a>설명

`GetProperty`*pvProp을*통해 값을 반환합니다.

> [!NOTE]
> 이 함수는 ActiveX `CWnd` 컨트롤을 나타내는 개체에서만 호출해야 합니다.

ActiveX 제어 컨테이너를 사용하여 이 멤버 함수를 사용하는 자세한 내용은 [ActiveX 제어 컨테이너: ActiveX 제어 컨테이너에서 ActiveX 컨트롤 프로그래밍](../../mfc/programming-activex-controls-in-a-activex-control-container.md)문서 입니다.

## <a name="cwndgetrendertarget"></a><a name="getrendertarget"></a>CWnd::GetRenderTarget

이 창에 연결된 렌더링 대상을 가져옵니다.

```
CHwndRenderTarget* GetRenderTarget();
```

### <a name="return-value"></a>Return Value

렌더링 대상 또는 NULL에 대한 포인터입니다.

## <a name="cwndgetsafehwnd"></a><a name="getsafehwnd"></a>CWnd::GetSafeHwnd

`m_hWnd` **이** 포인터가 NULL인 경우 반환 합니다 .

```
HWND GetSafeHwnd() const;
```

### <a name="return-value"></a>Return Value

창에 대한 창 핸들을 반환합니다. 창에 `CWnd` 연결되지 않았거나 NULL 포인터와 함께 사용되는 경우 `CWnd` NULL을 반환합니다.

### <a name="example"></a>예제

  [CWnd::SubclassWindow에](#subclasswindow)대 한 예제를 참조 합니다.

## <a name="cwndgetsafeowner"></a><a name="getsafeowner"></a>CWnd::GetSafe소유자

이 멤버 함수를 호출하여 대화 상자 또는 기타 모달 창에 사용해야 하는 소유자 창을 검색합니다.

```
static CWnd* GetSafeOwner(
    CWnd* pParent = NULL,
    HWND* pWndTop = NULL);
```

### <a name="parameters"></a>매개 변수

*pParent*<br/>
상위 `CWnd` 창에 대한 포인터입니다. NULL일 수 있습니다.

*pWndTop*<br/>
현재 맨 위에 있는 창에 대한 포인터입니다. NULL일 수 있습니다.

### <a name="return-value"></a>Return Value

지정된 창의 안전 소유자에 대한 포인터입니다.

### <a name="remarks"></a>설명

안전 소유자는 *pParent의*첫 번째 비하위 부모 창입니다. *pParent가* NULL인 경우 스레드의 주 [창(AfxGetMainWnd를](../../mfc/reference/application-information-and-management.md#afxgetmainwnd)통해 검색)이 소유자를 찾는 데 사용됩니다.

> [!NOTE]
> 프레임워크 자체는 이 함수를 사용하여 소유자가 지정되지 않은 대화 상자 및 속성 시트에 대한 올바른 소유자 창을 결정합니다.

## <a name="cwndgetscrollbarctrl"></a><a name="getscrollbarctrl"></a>CWnd::GetScrollBarCtrl

지정된 형제 스크롤 막대 또는 스플리터 창에 대한 포인터를 얻으려면 이 멤버 함수를 호출합니다.

```
virtual CScrollBar* GetScrollBarCtrl(int nBar) const;
```

### <a name="parameters"></a>매개 변수

*nBar*<br/>
스크롤 막대의 유형을 지정합니다. 매개 변수는 다음 값 중 하나를 취할 수 있습니다.

- SB_HORZ 가로 스크롤 막대의 위치를 검색합니다.

- SB_VERT 세로 스크롤 막대의 위치를 검색합니다.

### <a name="return-value"></a>Return Value

형제 스크롤 막대 컨트롤 또는 NULL이 없는 경우.

### <a name="remarks"></a>설명

이 멤버 함수는 창을 만드는 동안 WS_HSCROLL 또는 WS_VSCROLL 비트가 설정될 때 생성된 스크롤 막대에서 작동하지 않습니다. 이 `CWnd` 함수의 구현은 NULL을 반환합니다. 와 같은 `CView`파생 클래스는 설명된 기능을 구현합니다.

## <a name="cwndgetscrollbarinfo"></a><a name="getscrollbarinfo"></a>CWnd::GetScrollBarInfo

지정한 스크롤 막대에 대한 정보를 검색합니다.

```
BOOL GetScrollBarInfo(
    LONG idObject,
    PSCROLLBARINFO psbi) const;
```

### <a name="parameters"></a>매개 변수

*idObject*<br/>
메뉴 개체를 지정합니다. 가능한 값 목록은 [GetScrollBarInfo](/windows/win32/api/winuser/nf-winuser-getscrollbarinfo)를 참조하십시오.

*psbi*<br/>
정보를 수신하는 [SCROLLBARINFO](/windows/win32/api/winuser/ns-winuser-scrollbarinfo) 구조에 대한 포인터입니다.

### <a name="return-value"></a>Return Value

함수가 성공하면 0이 아니고 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

이 멤버 함수는 Windows SDK에 설명된 대로 [GetScrollBarInfo](/windows/win32/api/winuser/nf-winuser-getscrollbarinfo)함수의 기능을 에뮬레이트합니다.

## <a name="cwndgetscrollinfo"></a><a name="getscrollinfo"></a>CWnd::GetScrollInfo

이 멤버 함수를 호출하여 `SCROLLINFO` 구조가 스크롤 막대에 대해 유지 관리하는 정보를 검색합니다.

```
BOOL GetScrollInfo(
    int nBar,
    LPSCROLLINFO lpScrollInfo,
    UINT nMask = SIF_ALL);
```

### <a name="parameters"></a>매개 변수

*nBar*<br/>
스크롤 막대가 컨트롤인지 창의 비클라이언트 영역의 일부인지 지정합니다. 클라이언트가 아닌 영역의 일부인 경우 *nBar는* 스크롤 막대가 가로, 세로 또는 둘 다에 배치되어 있는지 여부도 나타냅니다. 다음 중 하나여야 합니다.

- SB_CTL 스크롤 막대 컨트롤에 대한 매개 변수를 검색합니다. 데이터 `m_hWnd` 멤버는 스크롤 막대 컨트롤의 핸들이어야 합니다.

- SB_HORZ 창의 표준 가로 스크롤 막대에 대한 매개 변수를 검색합니다.

- SB_VERT 창의 표준 세로 스크롤 막대에 대한 매개 변수를 검색합니다.

*lp스크롤정보*<br/>
[SCROLLINFO](/windows/win32/api/winuser/ns-winuser-scrollinfo) 구조에 대한 포인터입니다. 이 구조에 대한 자세한 내용은 Windows SDK를 참조하십시오.

*nMask*<br/>
검색할 스크롤 막대 매개 변수를 지정합니다. 기본값은 SIF_PAGE, SIF_POS, SIF_TRACKPOS 및 SIF_RANGE 조합을 지정합니다. `SCROLLINFO` *nMask* 값에 대한 자세한 내용은 을 참조하십시오.

### <a name="return-value"></a>Return Value

메시지가 값을 검색한 경우 반환은 TRUE입니다. 그렇지 않으면 FALSE입니다.

### <a name="remarks"></a>설명

`GetScrollInfo`응용 프로그램에서 32비트 스크롤 위치를 사용할 수 있습니다.

[SCROLLINFO](/windows/win32/api/winuser/ns-winuser-scrollinfo) 구조에는 최소 및 최대 스크롤 위치, 페이지 크기 및 스크롤 상자(엄지 손가락)의 위치를 비롯한 스크롤 막대에 대한 정보가 포함되어 있습니다. 구조 `SCROLLINFO` 기본값 변경에 대한 자세한 내용은 Windows SDK의 구조 항목을 참조하십시오.

스크롤 막대 위치를 나타내는 MFC Windows 메시지 처리기, [CWnd::OnH스크롤](#onhscroll) 및 [CWnd::OnVScroll은](#onvscroll)16비트의 위치 데이터만 제공합니다. `GetScrollInfo`32비트의 `SetScrollInfo` 스크롤 막대 위치 데이터를 제공합니다. 따라서 응용 프로그램은 `GetScrollInfo` 처리 중 `CWnd::OnHScroll` `CWnd::OnVScroll` 하나를 호출하거나 32비트 스크롤 막대 위치 데이터를 가져올 수 있습니다.

## <a name="cwndgetscrolllimit"></a><a name="getscrolllimit"></a>CWnd::Get스크롤 제한

이 멤버 함수를 호출하여 스크롤 막대의 최대 스크롤 위치를 검색합니다.

```
int GetScrollLimit(int nBar);
```

### <a name="parameters"></a>매개 변수

*nBar*<br/>
스크롤 막대의 유형을 지정합니다. 매개 변수는 다음 값 중 하나를 취할 수 있습니다.

- SB_HORZ 가로 스크롤 막대의 스크롤 제한을 검색합니다.

- SB_VERT 세로 스크롤 막대의 스크롤 제한을 검색합니다.

### <a name="return-value"></a>Return Value

성공한 경우 스크롤 막대의 최대 위치를 지정합니다. 그렇지 않으면 0.

## <a name="cwndgetscrollpos"></a><a name="getscrollpos"></a>CWnd::Get스크롤포스

스크롤 막대의 스크롤 상자의 현재 위치를 검색합니다.

```
int GetScrollPos(int nBar) const;
```

### <a name="parameters"></a>매개 변수

*nBar*<br/>
검사할 스크롤 막대를 지정합니다. 매개 변수는 다음 값 중 하나를 취할 수 있습니다.

- SB_HORZ 가로 스크롤 막대의 위치를 검색합니다.

- SB_VERT 세로 스크롤 막대의 위치를 검색합니다.

### <a name="return-value"></a>Return Value

성공한 경우 스크롤 막대에서 스크롤 상자의 현재 위치를 지정합니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

현재 위치는 현재 스크롤 범위에 종속된 상대 값입니다. 예를 들어 스크롤 범위가 50~100이고 스크롤 상자가 막대 중간에 있는 경우 현재 위치는 75입니다.

## <a name="cwndgetscrollrange"></a><a name="getscrollrange"></a>CWnd::Get스크롤 레인지

지정된 스크롤 막대의 현재 최소 및 최대 스크롤 막대 위치를 *lpMinPos* 및 *lpMaxPos에서*지정한 위치로 복사합니다.

```cpp
void GetScrollRange(
    int nBar,
    LPINT lpMinPos,
    LPINT lpMaxPos) const;
```

### <a name="parameters"></a>매개 변수

*nBar*<br/>
검사할 스크롤 막대를 지정합니다. 매개 변수는 다음 값 중 하나를 취할 수 있습니다.

- SB_HORZ 가로 스크롤 막대의 위치를 검색합니다.

- SB_VERT 세로 스크롤 막대의 위치를 검색합니다.

*lpMinpos*<br/>
최소 위치를 수신하는 정수 변수를 가리킵니다.

*lp맥스포스*<br/>
최대 위치를 수신하는 정수 변수를 가리킵니다.

### <a name="remarks"></a>설명

스크롤 `CWnd` 막대가 없는 경우 `GetScrollRange` 멤버 함수는 *0을 lpMinPos* 및 *lpMaxPos에*복사합니다.

표준 스크롤 막대의 기본 범위는 0에서 100사이입니다. 스크롤 막대 컨트롤의 기본 범위는 비어 있습니다(두 값 모두 0).

## <a name="cwndgetstyle"></a><a name="getstyle"></a>CWnd::Get스타일

현재 창 스타일을 반환합니다.

```
DWORD GetStyle() const;
```

### <a name="return-value"></a>Return Value

창의 스타일입니다. MFC에 사용되는 창 스타일에 대한 자세한 내용은 [창 스타일을](styles-used-by-mfc.md#window-styles)참조하십시오.

## <a name="cwndgetsystemmenu"></a><a name="getsystemmenu"></a>CWnd::GetSystem메뉴

애플리케이션에서 복사 및 수정을 위해 컨트롤 메뉴에 액세스할 수 있도록 합니다.

```
CMenu* GetSystemMenu(BOOL bRevert) const;
```

### <a name="parameters"></a>매개 변수

*b되반전*<br/>
수행할 작업을 지정합니다. *bRevert가* FALSE이면 현재 사용 중인 컨트롤 메뉴의 복사본에 핸들을 `GetSystemMenu` 반환합니다. 이 복사본은 처음에는 컨트롤 메뉴와 동일하지만 수정할 수 있습니다. *bRevert가* TRUE이면 `GetSystemMenu` 컨트롤 메뉴를 다시 기본 상태로 재설정합니다. 이전, 아마도 수정, 제어 메뉴(있는 경우)는 소멸됩니다. 이 경우 반환 값은 정의되지 않습니다.

### <a name="return-value"></a>Return Value

*bRevert가* FALSE인 경우 컨트롤 메뉴의 복사본을 식별합니다. *bRevert가* TRUE이면 반환 값이 정의되지 않습니다.

반환된 포인터는 일시적일 수 있으며 나중에 사용할 수 없습니다.

### <a name="remarks"></a>설명

컨트롤 메뉴의 자체 `GetSystemMenu` 복사본을 만드는 데 사용하지 않는 모든 창은 표준 컨트롤 메뉴를 받습니다.

멤버 함수에서 반환되는 포인터는 [CMenu::AppendMenu,](../../mfc/reference/cmenu-class.md#appendmenu) [CMenu::InsertMenu](../../mfc/reference/cmenu-class.md#insertmenu)및 [CMenu::수정메뉴](../../mfc/reference/cmenu-class.md#modifymenu) 함수를 사용하여 컨트롤 메뉴를 변경할 수 있습니다. `GetSystemMenu`

컨트롤 메뉴에는 처음에 SC_CLOSE, SC_MOVE 및 SC_SIZE 같은 다양한 ID 값으로 식별된 항목이 포함되어 있습니다. 컨트롤 메뉴의 항목은 [WM_SYSCOMMAND](#onsyscommand) 메시지를 생성합니다. 미리 정의된 모든 컨트롤 메뉴 항목에는 ID 번호가 0xF000보다 큽습니다. 응용 프로그램이 컨트롤 메뉴에 항목을 추가하는 경우 F000 미만의 ID 번호를 사용해야 합니다.

Windows에서 표준 제어 메뉴에서 항목을 자동으로 사용할 수 없게 만들 수 있습니다. `CWnd`메뉴가 표시되기 전에 전송되는 [WM_INITMENU](#oninitmenu) 메시지에 응답하여 자체 선택 또는 가용성을 수행할 수 있습니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#99](../../mfc/reference/codesnippet/cpp/cwnd-class_39.cpp)]

## <a name="cwndgettitlebarinfo"></a><a name="gettitlebarinfo"></a>CWnd::GetTitleBarInfo

지정된 제목 표시줄에 대한 정보를 검색합니다.

```
BOOL GetTitleBarInfo(PTITLEBARINFO pti) const;
```

### <a name="parameters"></a>매개 변수

*Pti*<br/>
정보를 받는 [TITLEBARINFO](/windows/win32/api/winuser/ns-winuser-titlebarinfo) 구조에 대한 포인터입니다.

### <a name="remarks"></a>설명

이 멤버 함수는 Windows SDK에 설명된 대로 [GetTitleBarInfo](/windows/win32/api/winuser/nf-winuser-gettitlebarinfo)함수의 기능을 에뮬레이트합니다.

## <a name="cwndgettoplevelframe"></a><a name="gettoplevelframe"></a>CWnd::GetTopLevelFrame

이 멤버 함수를 호출하여 창의 최상위 프레임 창(있는 경우)을 검색합니다.

```
CFrameWnd* GetTopLevelFrame() const;
```

### <a name="return-value"></a>Return Value

창의 최상위 프레임 창을 식별합니다.

반환된 포인터는 일시적일 수 있으며 나중에 사용할 수 없습니다.

### <a name="remarks"></a>설명

연결된 `CWnd` 창이 없거나 최상위 부모가 [CFrameWnd](../../mfc/reference/cframewnd-class.md)-파생 개체가 아닌 경우 이 함수는 NULL을 반환합니다.

## <a name="cwndgettoplevelowner"></a><a name="gettoplevelowner"></a>CWnd::GetTopLevel 소유자

이 멤버 함수를 호출하여 최상위 창을 검색합니다.

```
CWnd* GetTopLevelOwner() const;
```

### <a name="return-value"></a>Return Value

최상위 창을 식별합니다. 반환된 포인터는 일시적일 수 있으며 나중에 사용할 수 없습니다.

### <a name="remarks"></a>설명

최상위 창은 데스크톱의 자식인 창입니다. 연결된 `CWnd` 창이 없는 경우 이 함수는 NULL을 반환합니다.

## <a name="cwndgettoplevelparent"></a><a name="gettoplevelparent"></a>CWnd::GetTopLevel부모

이 멤버 함수를 호출하여 창의 최상위 부모를 검색합니다.

```
CWnd* GetTopLevelParent() const;
```

### <a name="return-value"></a>Return Value

창의 최상위 상위 창을 식별합니다.

반환된 포인터는 일시적일 수 있으며 나중에 사용할 수 없습니다.

### <a name="remarks"></a>설명

`GetTopLevelParent`[GetTopLevelFrame](#gettoplevelframe) 및 [GetTopLevel소유자와](#gettoplevelowner)유사합니다. 그러나 현재 소유자 창으로 설정된 값을 무시합니다.

## <a name="cwndgettopwindow"></a><a name="gettopwindow"></a>CWnd::GetTopWindow

에 속하는 최상위 하위 창을 검색합니다. `CWnd`

```
CWnd* GetTopWindow() const;
```

### <a name="return-value"></a>Return Value

`CWnd` 연결된 하위 창 목록에서 최상위 하위 창을 식별합니다. 자식 창이 없으면 값은 NULL입니다.

반환된 포인터는 일시적일 수 있으며 나중에 사용할 수 없습니다.

### <a name="remarks"></a>설명

자식이 없는 경우 `CWnd` 이 함수는 NULL을 반환합니다.

## <a name="cwndgetupdaterect"></a><a name="getupdaterect"></a>CWnd::GetUpdateRect

업데이트 영역을 완전히 둘러싸는 가장 작은 사각형의 좌표를 검색합니다.

```
BOOL GetUpdateRect(
    LPRECT lpRect,
    BOOL bErase = FALSE);
```

### <a name="parameters"></a>매개 변수

*Lprect*<br/>
업데이트 영역을 둘러싸는 `CRect` 업데이트의 클라이언트 좌표를 수신하는 개체 또는 [RECT 구조를](/windows/win32/api/windef/ns-windef-rect) 가리킵니다.

이 매개 변수를 NULL로 설정하여 업데이트 `CWnd`영역이 내에 있는지 여부를 확인합니다. *lpRect가* NULL이면 `GetUpdateRect` 멤버 함수는 업데이트 영역이 있는 경우 0이 아닌 경우 반환하고 그렇지 않은 경우 0을 반환합니다. 이렇게 하면 WM_PAINT 메시지가 잘못된 영역에서 발생했는지 여부를 확인할 수 있습니다. Windows 버전 3.0 이상에서 이 매개 변수를 NULL로 설정하지 마십시오.

*bErase*<br/>
업데이트 영역의 배경을 지울지 여부를 지정합니다.

### <a name="return-value"></a>Return Value

업데이트 영역의 상태를 지정합니다. 업데이트 영역이 비어 있지 않으면 값이 0이 아닙니다. 그렇지 않으면 0.

*lpRect* 매개 변수가 NULL로 설정된 경우 업데이트 영역이 있는 경우 반환 값이 0이 아닙니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

CS_OWNDC `CWnd` 스타일로 만들어졌고 매핑 모드가 MM_TEXT 않은 `GetUpdateRect` 경우 멤버 함수는 논리적 좌표로 사각형을 제공합니다. 그렇지 `GetUpdateRect` 않으면 클라이언트 좌표에 사각형을 제공합니다. 업데이트 영역이 없는 `GetUpdateRect` 경우 사각형을 비우도록 설정합니다(모든 좌표를 0으로 설정).

*bErase* 매개 변수는 `GetUpdateRect` 업데이트 영역의 배경을 지울지 여부를 지정합니다. *bErase가* TRUE이고 업데이트 영역이 비어 있지 않으면 배경이 지워집니다. 배경을 지우려면 `GetUpdateRect` [WM_ERASEBKGND](#onerasebkgnd) 메시지를 보냅니다.

[BeginPaint](#beginpaint) 멤버 함수에서 검색한 업데이트 사각형은 멤버 함수에서 `GetUpdateRect` 검색한 것과 동일합니다.

멤버 `BeginPaint` 함수는 업데이트 영역의 유효성을 자동으로 `GetUpdateRect` 검사하므로 호출 직후에 수행된 모든 호출은 빈 업데이트 영역을 `BeginPaint` 검색합니다.

## <a name="cwndgetupdatergn"></a><a name="getupdatergn"></a>CWnd::GetUpdateRgn

*pRgn으로*식별된 영역으로 업데이트 영역을 검색합니다.

```
int GetUpdateRgn(
    CRgn* pRgn,
    BOOL bErase = FALSE);
```

### <a name="parameters"></a>매개 변수

*pRgn*<br/>
업데이트 영역을 식별합니다.

*bErase*<br/>
배경이 지워지고 자식 창의 클라이언트가 아닌 영역이 그려질지 여부를 지정합니다. 값이 FALSE이면 도면이 수행되지 않습니다.

### <a name="return-value"></a>Return Value

결과 영역의 유형을 나타내는 짧은 정수 플래그를 지정합니다. 값은 다음 중 하나를 취할 수 있습니다.

- SIMPLEREGION 이 지역에겹치는 테두리가 없습니다.

- COMPLEXREGION 이 지역은 겹치는 테두리를 가미합니다.

- NULLREGION 영역이 비어 있습니다.

- 오류 영역이 생성되지 않았습니다.

### <a name="remarks"></a>설명

이 영역의 좌표는 왼쪽 위 모서리(클라이언트 좌표)를 기준으로 합니다.

[BeginPaint](#beginpaint) 멤버 함수는 업데이트 영역의 유효성을 자동으로 `GetUpdateRgn` 검사하므로 호출 `BeginPaint` 직후에 수행된 모든 호출은 빈 업데이트 영역을 검색합니다.

## <a name="cwndgetwindow"></a><a name="getwindow"></a>CWnd::GetWindow

요청된 창에 대한 포인터를 반환하거나 NULL이 없는 경우 NULL을 반환합니다.

```
CWnd* GetWindow(UINT nCmd) const;
```

### <a name="parameters"></a>매개 변수

*nCmd*<br/>
반환된 창과 `CWnd` 의 관계를 지정합니다. 다음 값 중 하나를 취할 수 있습니다.

- GW_CHILD 첫 `CWnd` 번째 하위 창을 식별합니다.

- GW_HWNDFIRST `CWnd` 자식 창인 경우 첫 번째 형제 창을 반환합니다. 그렇지 않으면 목록의 첫 번째 최상위 창을 반환합니다.

- GW_HWNDLAST자식 창인 경우 `CWnd` 마지막 형제 창을 반환합니다. 그렇지 않으면 목록의 마지막 최상위 창을 반환합니다.

- GW_HWNDNEXT 창 관리자 의 목록에 다음 창을 반환 합니다.

- GW_HWNDPREV 창 관리자 의 목록에서 이전 창을 반환합니다.

- GW_OWNER 소유자를 `CWnd` 식별합니다.

### <a name="return-value"></a>Return Value

반환된 포인터는 일시적일 수 있으며 나중에 사용할 수 없습니다.

## <a name="cwndgetwindowcontexthelpid"></a><a name="getwindowcontexthelpid"></a>CWnd::GetWindow컨텍스트도움말Id

이 멤버 함수를 호출하여 창과 연결된 도움말 컨텍스트 식별자(있는 경우)를 검색합니다.

```
DWORD GetWindowContextHelpId() const;
```

### <a name="return-value"></a>Return Value

도움말 컨텍스트 식별자입니다. 창에 없는 경우 0을 반환합니다.

## <a name="cwndgetwindowedchildcount"></a><a name="getwindowedchildcount"></a>CWnd::GetWindowedChildCount

이 멤버 함수를 호출하여 연결된 자식 창 수를 검색합니다.

```
long GetWindowedChildCount();
```

### <a name="return-value"></a>Return Value

개체와 연결된 자식 `CWnd` 창의 수입니다.

## <a name="cwndgetwindowdc"></a><a name="getwindowdc"></a>CWnd::GetWindowDC

캡션 막대, 메뉴 및 스크롤 막대를 포함하여 전체 창에 대한 표시 컨텍스트를 검색합니다.

```
CDC* GetWindowDC();
```

### <a name="return-value"></a>Return Value

함수가 성공하면 지정된 창에 대한 표시 컨텍스트를 식별합니다. 그렇지 않으면 NULL.

반환된 포인터는 일시적일 수 있으며 나중에 사용할 수 없습니다. [ReleaseDC는](#releasedc) 에 대한 각 성공적인 `GetWindowDC`호출에 대해 한 번 호출해야 합니다.

### <a name="remarks"></a>설명

창 표시 컨텍스트는 컨텍스트의 `CWnd`원점이 클라이언트 영역 `CWnd` 대신 왼쪽 위 모서리이므로 에서 아무 곳이나 페인팅을 허용합니다.

기본 특성은 컨텍스트를 검색할 때마다 표시 컨텍스트에 할당됩니다. 이전 속성이 손실됩니다.

`GetWindowDC`는 비클라이언트 영역 내의 `CWnd` 특수 페인팅 효과에 사용됩니다. 창의 클라이언트가 아닌 영역에서 페인팅하는 것은 권장되지 않습니다.

[GetSystemMetrics](/windows/win32/api/winuser/nf-winuser-getsystemmetrics) Windows 함수는 캡션 막대, 메뉴 및 스크롤 막대와 같은 비클라이언트 영역의 다양한 부분의 크기를 검색하는 데 사용할 수 있습니다.

페인팅이 완료되면 [ReleaseDC](#releasedc) 멤버 함수를 호출하여 표시 컨텍스트를 해제해야 합니다. 디스플레이 컨텍스트를 해제하지 않으면 동시에 열 수 있는 장치 컨텍스트 수의 제한으로 인해 응용 프로그램에서 요청한 페인팅에 심각한 영향을 미칩니다.

## <a name="cwndgetwindowinfo"></a><a name="getwindowinfo"></a>CWnd::GetWindowInfo

창에 대한 정보를 검색합니다.

```
BOOL GetWindowInfo(PWINDOWINFO pwi) const;
```

### <a name="parameters"></a>매개 변수

*Pwi*<br/>
[WINDOWINFO](/windows/win32/api/winuser/ns-winuser-windowinfo) 구조에 대한 포인터입니다.

### <a name="remarks"></a>설명

이 멤버 함수는 Windows SDK에 설명된 대로 [GetWindowInfo](/windows/win32/api/winuser/nf-winuser-getwindowinfo)함수의 기능을 에뮬레이트합니다.

## <a name="cwndgetwindowlesschildcount"></a><a name="getwindowlesschildcount"></a>CWnd::GetWindowlessChildCount

연결된 창 없는 자식 창 의 수를 검색합니다.

```
long GetWindowlessChildCount();
```

### <a name="return-value"></a>Return Value

`CWnd` 개체와 연결된 창 없는 자식 창의 수입니다.

## <a name="cwndgetwindowplacement"></a><a name="getwindowplacement"></a>CWnd::GetWindow배치

창의 표시 상태와 일반(복원됨), 최소화 및 최대화 위치를 검색합니다.

```
BOOL GetWindowPlacement(WINDOWPLACEMENT* lpwndpl) const;
```

### <a name="parameters"></a>매개 변수

*lpwndpl*<br/>
표시 상태 `WINDOWPLACEMENT` 및 위치 정보를 수신하는 구조를 가리킵니다.

### <a name="return-value"></a>Return Value

함수가 성공하면 0이 아니고 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

이 `flags` 함수에서 검색하는 [WINDOWPLACEMENT](/windows/win32/api/winuser/ns-winuser-windowplacement) 구조의 멤버는 항상 0입니다. 최대화되면 `CWnd` 멤버가 `showCmd` `WINDOWPLACEMENT` SW_SHOWMAXIMIZED. 창이 최소화되면 SW_SHOWMINIMIZED. 그렇지 않으면 SW_SHOWNORMAL.

## <a name="cwndgetwindowrect"></a><a name="getwindowrect"></a>CWnd::GetWindowRect

`CWnd` *lpRect로*가리키는 구조에 개체의 경계 사각형의 치수를 복사합니다.

```cpp
void GetWindowRect(LPRECT lpRect) const;
```

### <a name="parameters"></a>매개 변수

*Lprect*<br/>
왼쪽 상단 `CRect` 및 오른쪽 아래 모서리의 화면 좌표를 받는 오브젝트 또는 [RECT 구조를](/windows/win32/api/windef/ns-windef-rect) 가리킵니다.

### <a name="remarks"></a>설명

치수는 화면 의 왼쪽 위 모서리를 기준으로 화면 좌표에 지정됩니다. 캡션, 테두리 및 스크롤 막대(있는 경우)의 치수가 포함됩니다.

## <a name="cwndgetwindowrgn"></a><a name="getwindowrgn"></a>CWnd::GetWindowRgn

이 멤버 함수를 호출하여 창의 창 영역을 가져옵니다.

```
int GetWindowRgn(HRGN hRgn)const;
```

### <a name="parameters"></a>매개 변수

*hRgn*<br/>
창 영역에 대한 핸들입니다.

### <a name="return-value"></a>Return Value

반환 값은 함수가 가져오는 영역의 형식을 지정합니다. 다음 값 중 하나일 수 있습니다.

- NULLREGION 영역이 비어 있습니다.

- SIMPLEREGION 영역은 단일 사각형입니다.

- COMPLEXREGION 영역이 두 개 이상의 사각형입니다.

- 오류 오류가 발생했습니다. 지역은 영향을 받지 않습니다.

### <a name="remarks"></a>설명

창 영역은 운영 체제에서 그리기를 허용하는 창 내의 영역을 결정합니다. 운영 체제는 창 영역 외부에 있는 창의 일부를 표시 하지 않습니다.

창 창 영역의 좌표는 창의 클라이언트 영역이 아니라 창의 왼쪽 위 모서리를 기준으로 합니다.

창의 창 영역을 설정하려면 [CWnd::SetWindowRgn](#setwindowrgn)을 호출합니다.

## <a name="cwndgetwindowtext"></a><a name="getwindowtext"></a>CWnd::GetWindow텍스트

`CWnd` 캡션 제목(있는 경우)을 *lpszStringBuf가* 가리키는 버퍼에 복사하거나 대상 문자열 *rString에*복사합니다.

```
int GetWindowText(
    LPTSTR lpszStringBuf,
    int nMaxCount) const;

void GetWindowText(
    CString& rString) const;
```

### <a name="parameters"></a>매개 변수

*lpsz스트링부프*<br/>
창 제목의 복사된 문자열을 받을 버퍼를 가리킵니다.

*n맥스카운트*<br/>
null 문자 종료를 포함하여 버퍼에 복사할 최대 문자 수를 지정합니다. 문자열이 *nMaxCount에*지정된 문자 수보다 길면 잘립니다.

*rString*<br/>
창 제목의 복사된 문자열을 수신하는 [CString](../../atl-mfc-shared/reference/cstringt-class.md) 개체입니다.

### <a name="return-value"></a>Return Value

문자로 복사된 문자열의 길이를 지정하고 null 문자 종료를 포함하지 않습니다. 캡션이 없거나 `CWnd` 캡션이 비어 있는 경우 0입니다.

### <a name="remarks"></a>설명

개체가 `CWnd` 컨트롤인 경우 `GetWindowText` 멤버 함수는 캡션을 복사하는 대신 컨트롤 내에서 텍스트를 복사합니다.

이 멤버 함수를 사용하면 [WM_GETTEXT](/windows/win32/winmsg/wm-gettext) 메시지가 `CWnd` 개체에 전송됩니다.

### <a name="example"></a>예제

  [CWnd::SetWindowText](#setwindowtext)에 대한 예제를 참조하십시오.

## <a name="cwndgetwindowtextlength"></a><a name="getwindowtextlength"></a>CWnd::GetWindow텍스트길이

개체 캡션 제목의 `CWnd` 길이를 반환합니다.

```
int GetWindowTextLength() const;
```

### <a name="return-value"></a>Return Value

null 종료 문자를 포함하지 않고 문자길이를 지정합니다. 이러한 텍스트가 없는 경우 값은 0입니다.

### <a name="remarks"></a>설명

컨트롤인 경우 `CWnd` `GetWindowTextLength` 멤버 함수는 캡션 대신 컨트롤 내에서 텍스트의 길이를 반환합니다.

이 멤버 함수를 사용하면 [WM_GETTEXTLENGTH](/windows/win32/winmsg/wm-gettextlength) 메시지가 `CWnd` 개체에 전송됩니다.

### <a name="example"></a>예제

  [CWnd::SetWindowText](#setwindowtext)에 대한 예제를 참조하십시오.

## <a name="cwndhidecaret"></a><a name="hidecaret"></a>CWnd::숨은 돌

디스플레이 화면에서 제거하여 캐럿을 숨깁니다.

```cpp
void HideCaret();
```

### <a name="remarks"></a>설명

캐릿은 더 이상 표시되지 않지만 [ShowCaret](#showcaret) 멤버 함수를 사용하여 다시 표시할 수 있습니다. 캐리트숨기지는 현재 모양을 파괴하지 않습니다.

숨기기는 누적됩니다. 연속으로 `HideCaret` 다섯 번 호출된 경우 `ShowCaret` 멤버 함수를 5번 호출해야 합니다.

## <a name="cwndhilitemenuitem"></a><a name="hilitemenuitem"></a>CWnd::HiliteMenu항목

최상위(메뉴 모음) 메뉴 항목에서 강조 표시를 강조 표시하거나 제거합니다.

```
BOOL HiliteMenuItem(
    CMenu* pMenu,
    UINT nIDHiliteItem,
    UINT nHilite);
```

### <a name="parameters"></a>매개 변수

*p메뉴*<br/>
강조 표시할 항목이 포함된 최상위 메뉴를 식별합니다.

*니디힐라이트아이템*<br/>
*nHilite* 매개 변수의 값에 따라 강조 표시할 메뉴 항목을 지정합니다.

*n힐라이트*<br/>
메뉴 항목이 강조 표시되어 있는지 또는 강조 표시가 제거되었는지 지정합니다. MF_BYCOMMAND 또는 MF_BYPOSITION MF_HILITE 또는 MF_UNHILITE 조합일 수 있습니다. 비트OR 연산자를 사용하여 값을 결합할 수 있습니다. 이러한 값에는 다음과 같은 의미가 있습니다.

- MF_BYCOMMAND *nIDHiliteItem을* 메뉴 항목 ID(기본 해석)로 해석합니다.

- MF_BYPOSITION *nIDHiliteItem을* 메뉴 항목의 0 기반 오프셋으로 해석합니다.

- MF_HILITE 항목을 강조 표시됩니다. 이 값이 지정되지 않으면 항목에서 강조 표시가 제거됩니다.

- MF_UNHILITE 항목에서 강조 표시를 제거합니다.

### <a name="return-value"></a>Return Value

메뉴 항목이 강조 표시되었는지 여부를 지정합니다. 항목이 강조 표시된 경우 0이 아닙니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

MF_HILITE 및 MF_UNHILITE 플래그는 이 멤버 함수에서만 사용할 수 있습니다. [CMenu::수정](../../mfc/reference/cmenu-class.md#modifymenu) 메뉴 멤버 함수와 함께 사용할 수 없습니다.

## <a name="cwndhtmlhelp"></a><a name="htmlhelp"></a>CWnd::Html도움말

HTMLHelp 응용 프로그램을 호출하려면 이 멤버 함수를 호출합니다.

```
virtual void HtmlHelp(
    DWORD_PTR dwData,
    UINT nCmd = 0x000F);
```

### <a name="parameters"></a>매개 변수

*dwData*<br/>
추가 데이터를 지정합니다. 사용되는 값은 *nCmd* 매개 변수의 값에 따라 다릅니다.

*nCmd*<br/>
요청한 도움말의 형식을 지정합니다. 가능한 값 목록과 *dwData* 매개 변수에 미치는 영향은 Windows SDK의 HTML 도움말 API 참조에 설명된 *uCommand* 매개 변수를 참조하십시오.

### <a name="remarks"></a>설명

자세한 내용은 [CWinApp::HtmlHelp를](../../mfc/reference/cwinapp-class.md#htmlhelp) 참조하십시오.

## <a name="cwndinitdynamiclayout"></a><a name="initdynamiclayout"></a>CWnd::InitDynamic레이아웃

창의 동적 레이아웃을 초기화하기 위해 프레임워크에서 호출됩니다.

```cpp
void InitDynamicLayout();
```

### <a name="remarks"></a>설명

이 메서드를 직접 호출하지 마세요.

## <a name="cwndinvalidate"></a><a name="invalidate"></a>CWnd::무효화

의 `CWnd`전체 클라이언트 영역을 무효화합니다.

```cpp
void Invalidate(BOOL bErase = TRUE);
```

### <a name="parameters"></a>매개 변수

*bErase*<br/>
업데이트 영역 내의 배경을 지울지 여부를 지정합니다.

### <a name="remarks"></a>설명

클라이언트 영역은 다음 [WM_PAINT](#onpaint) 메시지가 발생할 때 페인팅으로 표시됩니다. [유효성 검사Rect](#validaterect) 또는 [ValidateRgn](#validatergn) 멤버 함수에 의해 WM_PAINT 메시지가 발생 하기 전에 이 지역의 유효성을 검사할 수도 있습니다.

*bErase* 매개 변수는 업데이트 영역이 처리될 때 업데이트 영역 내의 배경을 지울지 여부를 지정합니다. *bErase가* TRUE이면 [BeginPaint](#beginpaint) 멤버 함수가 호출될 때 배경이 지워집니다. *bErase가* FALSE이면 배경은 변경되지 않습니다. *bErase가* 업데이트 영역의 모든 부분에 TRUE인 경우 지정된 부분뿐만 아니라 전체 영역의 배경이 지워집니다.

Windows는 [WM_PAINT](#onpaint) 업데이트 영역이 `CWnd` 비어 있지 않고 해당 창에 대한 응용 프로그램 큐에 다른 메시지가 없을 때마다 WM_PAINT 메시지를 보냅니다.

### <a name="example"></a>예제

  [CWnd::UpdateWindow](#updatewindow)에 대한 예제를 참조하십시오.

## <a name="cwndinvalidaterect"></a><a name="invalidaterect"></a>CWnd::무효화

업데이트 영역에 해당 사각형을 추가하여 지정된 사각형 내의 클라이언트 `CWnd` 영역을 무효화합니다.

```cpp
void InvalidateRect(
    LPCRECT lpRect,
    BOOL bErase = TRUE);
```

### <a name="parameters"></a>매개 변수

*Lprect*<br/>
업데이트 영역에 `CRect` 추가할 사각형(클라이언트 좌표)이 포함된 개체 또는 [RECT 구조를](/windows/win32/api/windef/ns-windef-rect) 가리킵니다. *lpRect가* NULL이면 전체 클라이언트 영역이 리전에 추가됩니다.

*bErase*<br/>
업데이트 영역 내의 배경을 지울지 여부를 지정합니다.

### <a name="remarks"></a>설명

무효화된 사각형은 업데이트 영역의 다른 모든 영역과 함께 다음 [WM_PAINT](#onpaint) 메시지가 전송될 때 페인팅으로 표시됩니다. 무효화된 영역은 다음 WM_PAINT 호출이 발생할 때 또는 [ValidateRect](#validaterect) 또는 [ValidateRgn](#validatergn) 멤버 함수에 의해 영역의 유효성이 검사될 때까지 영역이 처리될 때까지 업데이트 영역에 누적됩니다.

*bErase* 매개 변수는 업데이트 영역이 처리될 때 업데이트 영역 내의 배경을 지울지 여부를 지정합니다. *bErase가* TRUE이면 [BeginPaint](#beginpaint) 멤버 함수가 호출될 때 배경이 지워집니다. *bErase가* FALSE이면 배경은 변경되지 않습니다. *bErase가* 업데이트 영역의 모든 부분에 TRUE인 경우 지정된 부분뿐만 아니라 전체 영역의 배경이 지워집니다.

Windows는 [WM_PAINT](#onpaint) 업데이트 영역이 `CWnd` 비어 있지 않고 해당 창에 대한 응용 프로그램 큐에 다른 메시지가 없을 때마다 WM_PAINT 메시지를 보냅니다.

## <a name="cwndinvalidatergn"></a><a name="invalidatergn"></a>CWnd::무효화Rgn

`CWnd`의 현재 업데이트 영역에 추가하여 지정된 영역 내의 클라이언트 영역을 무효화합니다.

```cpp
void InvalidateRgn(
    CRgn* pRgn,
    BOOL bErase = TRUE);
```

### <a name="parameters"></a>매개 변수

*pRgn*<br/>
업데이트 영역에 추가할 영역을 식별하는 [CRgn](../../mfc/reference/crgn-class.md) 개체에 대한 포인터입니다. 이 지역에는 클라이언트 좌표가 있는 것으로 가정됩니다. 이 매개 변수가 NULL이면 전체 클라이언트 영역이 업데이트 영역에 추가됩니다.

*bErase*<br/>
업데이트 영역 내의 배경을 지울지 여부를 지정합니다.

### <a name="remarks"></a>설명

업데이트 영역의 다른 모든 영역과 함께 무효화된 영역은 [WM_PAINT](#onpaint) 메시지가 다음에 전송될 때 페인팅으로 표시됩니다. 무효화된 영역은 WM_PAINT 메시지가 다음에 전송될 때 또는 [ValidateRect](#validaterect) 또는 [ValidateRgn](#validatergn) 멤버 함수에 의해 영역의 유효성이 검사될 때까지 영역이 처리될 때까지 업데이트 영역에 누적됩니다.

*bErase* 매개 변수는 업데이트 영역이 처리될 때 업데이트 영역 내의 배경을 지울지 여부를 지정합니다. *bErase가* TRUE이면 [BeginPaint](#beginpaint) 멤버 함수가 호출될 때 배경이 지워집니다. *bErase가* FALSE이면 배경은 변경되지 않습니다. *bErase가* 업데이트 영역의 모든 부분에 TRUE인 경우 지정된 부분뿐만 아니라 전체 영역의 배경이 지워집니다.

Windows는 [WM_PAINT](#onpaint) 업데이트 영역이 `CWnd` 비어 있지 않고 해당 창에 대한 응용 프로그램 큐에 다른 메시지가 없을 때마다 WM_PAINT 메시지를 보냅니다.

지정된 영역은 지역 함수 중 하나에 의해 이전에 만들어졌어야 합니다.

## <a name="cwndinvokehelper"></a><a name="invokehelper"></a>CWnd::호출 도우미

wFlags에 의해 지정된 컨텍스트에서 *dwDispID에*의해 지정된 ActiveX Control 메서드 또는 속성을 호출하려면 이 멤버 함수를 *호출합니다.*

```cpp
void AFX_CDECL InvokeHelper(
    DISPID dwDispID,
    WORD wFlags,
    VARTYPE vtRet,
    void* pvRet,
    const BYTE* pbParamInfo,
    ... );
```

### <a name="parameters"></a>매개 변수

*dwDispID*<br/>
호출할 메서드 또는 속성을 식별합니다.

*wFlags*<br/>
에 대한 호출의 컨텍스트를 `IDispatch::Invoke`설명하는 플래그입니다.

*vtRet*<br/>
반환 값 형식을 지정합니다. 가능한 값에 대해서는 [COleDispatchDriver::InvokeHelper](../../mfc/reference/coledispatchdriver-class.md#invokehelper)의 설명 섹션을 참조하세요.

*pvRet*<br/>
속성 값 또는 반환 값을 받을 변수의 주소입니다. *vtRet*.

*pbParamInfo*<br/>
*pbParamInfo*다음 매개 변수의 형식을 지정 하는 바이트의 null-종료 된 문자열에 대 한 포인터 . 가능한 값은 에 대한 `COleDispatchDriver::InvokeHelper`설명 섹션을 참조하십시오.

*...*<br/>
매개 변수 목록, *pbParamInfo에*지정된 형식의 .

### <a name="remarks"></a>설명

*pbParamInfo* 매개 변수는 메서드 또는 속성에 전달 된 매개 변수의 형식을 지정 합니다. 인수의 변수 목록은 구문 선언에서 *...* 로 표시됩니다.

이 함수는 매개 변수를 VARIANTARG 값으로 변환한 다음 ActiveX 컨트롤에서 메서드를 `IDispatch::Invoke` 호출합니다. `IDispatch::Invoke` 호출에 실패하면 이 함수가 예외를 throw합니다. 반환되는 `IDispatch::Invoke` SCODE(상태 코드)가 DISP_E_EXCEPTION 경우 이 함수는 [COleException](../../mfc/reference/coleexception-class.md) 개체를 throw하고 그렇지 않으면 [COleDispatchException을](../../mfc/reference/coledispatchexception-class.md)throw합니다.

> [!NOTE]
> 이 함수는 ActiveX `CWnd` 컨트롤을 나타내는 개체에서만 호출해야 합니다.

ActiveX 제어 컨테이너를 사용하여 이 멤버 함수를 사용하는 자세한 내용은 [ActiveX 제어 컨테이너: ActiveX 제어 컨테이너에서 ActiveX 컨트롤 프로그래밍](../../mfc/programming-activex-controls-in-a-activex-control-container.md)문서 입니다.

## <a name="cwndischild"></a><a name="ischild"></a>CWnd::아이

*pWnd에* 의해 지정된 창이 하위 창인지 아니면 `CWnd`의 다른 직접 하위 창인지 를 나타냅니다.

```
BOOL IsChild(const CWnd* pWnd) const;
```

### <a name="parameters"></a>매개 변수

*pWnd*<br/>
테스트할 창을 식별합니다.

### <a name="return-value"></a>Return Value

함수의 결과를 지정합니다. *pWnd로* 식별된 창이 의 자식 창인 `CWnd`경우 값이 0이 아닙니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

하위 창은 `CWnd` `CWnd` 개체가 원래 팝업 창에서 하위 창으로 연결되는 부모 창 체인에 있는 경우의 직접 하위 창입니다.

## <a name="cwndisd2dsupportenabled"></a><a name="isd2dsupportenabled"></a>CWnd::IsD2D지원 지원

D2D 지원이 활성화되어 있는지 여부를 결정합니다.

```
BOOL IsD2DSupportEnabled();
```

### <a name="return-value"></a>Return Value

TRUE 기능이 활성화되어 있는 경우 그렇지 않으면 거짓.

## <a name="cwndisdialogmessage"></a><a name="isdialogmessage"></a>CWnd::IsDialogMessage

이 멤버 함수를 호출하여 지정된 메시지가 모덜리스 대화 상자용인지 여부를 확인합니다. 이 경우 이 함수는 메시지를 처리합니다.

```
BOOL IsDialogMessage(LPMSG lpMsg);
```

### <a name="parameters"></a>매개 변수

*lpMsg*<br/>
확인할 메시지가 포함된 [MSG](/windows/win32/api/winuser/ns-winuser-msg) 구조를 가리킵니다.

### <a name="return-value"></a>Return Value

멤버 함수가 지정된 메시지를 처리했는지 여부를 지정합니다. 메시지가 처리된 경우 0이 아닙니다. 그렇지 않으면 0. 반환이 0이면 기본 클래스의 [CWnd::P reTranslateMessage](#pretranslatemessage) 멤버 함수를 호출하여 메시지를 처리합니다. `CWnd::PreTranslateMessage` 멤버 함수의 재정의에서 코드는 다음과 같습니다.

[!code-cpp[NVC_MFCWindowing#100](../../mfc/reference/codesnippet/cpp/cwnd-class_40.cpp)]

### <a name="remarks"></a>설명

함수가 `IsDialogMessage` 메시지를 처리하면 키보드 메시지를 확인하고 해당 대화 상자에 대한 선택 명령으로 변환합니다. 예를 들어 TAB 키는 다음 컨트롤 또는 컨트롤 그룹을 선택하고 DOWN ARROW 키는 그룹의 다음 컨트롤을 선택합니다.

이미 처리되었기 때문에 `IsDialogMessage` [TranslateMessage](/windows/win32/api/winuser/nf-winuser-translatemessage) 또는 [DispatchMessage](/windows/win32/api/winuser/nf-winuser-dispatchmessage) Windows 함수에 의해 처리된 메시지를 전달해서는 안 됩니다.

## <a name="cwndisdlgbuttonchecked"></a><a name="isdlgbuttonchecked"></a>CWnd::IsDlgButton 확인

단추 컨트롤 옆에 확인 표시가 있는지 여부를 확인합니다.

```
UINT IsDlgButtonChecked(int nIDButton) const;
```

### <a name="parameters"></a>매개 변수

*니드 버튼*<br/>
단추 컨트롤의 정수 식별자를 지정합니다.

### <a name="return-value"></a>Return Value

지정된 컨트롤을 선택하면 0이 아닌 경우, 0을 선택하지 않은 경우. 라디오 버튼과 확인란만 선택할 수 있습니다. 세 상태 단추의 경우 단추가 확정되지 않은 경우 반환 값은 2가 될 수 있습니다. 이 멤버 함수는 푸시 버튼에 대해 0을 반환합니다.

### <a name="remarks"></a>설명

단추를 세 가지 상태 컨트롤인 경우 멤버 함수는 흐리게 지정, 검사 또는 둘 중 어느 쪽인지 여부를 결정합니다.

## <a name="cwndisdynamiclayoutenabled"></a><a name="isdynamiclayoutenabled"></a>CWnd::IsDynamic레이아웃 사용 가능

이 창에서 동적 레이아웃이 사용되는지 여부를 확인합니다. 동적 레이아웃이 사용되는 경우 사용자가 부모 창의 크기를 조정하면 자식 창의 위치 및 크기가 변경될 수 있습니다.

```
BOOL IsDynamicLayoutEnabled() const;
```

### <a name="return-value"></a>Return Value

동적 레이아웃을 사용하면 TRUE이고, 그렇지 않으면 FALSE입니다.

### <a name="remarks"></a>설명

## <a name="cwndisiconic"></a><a name="isiconic"></a>CWnd::이아이코닉

최소화(아이코닉)인지 여부를 `CWnd` 지정합니다.

```
BOOL IsIconic() const;
```

### <a name="return-value"></a>Return Value

최소화된 `CWnd` 경우 비영점; 그렇지 않으면 0.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#101](../../mfc/reference/codesnippet/cpp/cwnd-class_41.cpp)]

## <a name="cwndistouchwindow"></a><a name="istouchwindow"></a>CWnd::이스터치윈도우

`CWnd`가 터치를 지원하는지 여부를 지정합니다.

```
BOOL IsTouchWindow() const;
```

### <a name="return-value"></a>Return Value

TRUE `CWnd` 터치 지원이 있는 경우; 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

## <a name="cwndiswindowenabled"></a><a name="iswindowenabled"></a>CWnd::IsWindow사용

마우스 및 `CWnd` 키보드 입력에 대해 사용할 수 있는지 여부를 지정합니다.

```
BOOL IsWindowEnabled() const;
```

### <a name="return-value"></a>Return Value

비영이 `CWnd` 활성화된 경우; 그렇지 않으면 0.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#102](../../mfc/reference/codesnippet/cpp/cwnd-class_42.cpp)]

## <a name="cwndiswindowvisible"></a><a name="iswindowvisible"></a>CWnd::이윈도우 가시

지정된 창의 가시성 상태를 결정합니다.

```
BOOL IsWindowVisible() const;
```

### <a name="return-value"></a>Return Value

비영이 `CWnd` 표시되는 [경우(WS_VISIBLE](styles-used-by-mfc.md#window-styles) 스타일 비트가 설정되어 있고 부모 창이 표시됩니다). 반환 값은 WS_VISIBLE 스타일 비트의 상태를 반영하므로 다른 창에 `CWnd` 의해 완전히 가려져 있더라도 반환 값이 0이 아닐 수 있습니다.

### <a name="remarks"></a>설명

창에는 WS_VISIBLE 스타일 비트로 표시된 가시성 상태가 있습니다. 이 스타일 비트가 [ShowWindow](#showwindow) 멤버 함수에 대한 호출로 설정되면 창이 표시되고 창에 대한 후속 드로잉이 창에 스타일 비트가 설정되어 있는 한 표시됩니다.

창이 다른 창으로 덮여 있거나 부모 창으로 잘린 경우 WS_VISIBLE 스타일이 있는 창에 대한 드로잉은 표시되지 않습니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#103](../../mfc/reference/codesnippet/cpp/cwnd-class_43.cpp)]

## <a name="cwndiszoomed"></a><a name="iszoomed"></a>CWnd::축소

최대화되었는지 `CWnd` 여부를 결정합니다.

```
BOOL IsZoomed() const;
```

### <a name="return-value"></a>Return Value

최대화되는 `CWnd` 경우 비영점; 그렇지 않으면 0.

## <a name="cwndkilltimer"></a><a name="killtimer"></a>CWnd::킬타이머

이전 호출에서 *nIDEvent로* 식별된 타이머 이벤트를 `SetTimer`제거합니다.

```
BOOL KillTimer(UINT_PTR nIDEvent);
```

### <a name="parameters"></a>매개 변수

*니디이벤트*<br/>
[SetTimer에](#settimer)전달된 타이머 이벤트의 값입니다.

### <a name="return-value"></a>Return Value

함수의 결과를 지정합니다. 이벤트가 죽은 경우 값은 0이 아닙니다. 멤버 함수가 `KillTimer` 지정된 타이머 이벤트를 찾을 수 없는 경우 0입니다.

### <a name="remarks"></a>설명

타이머와 연결된 보류 중인 [WM_TIMER](#ontimer) 메시지는 메시지 큐에서 제거되지 않습니다.

### <a name="example"></a>예제

  [CWnd::SetTimer에](#settimer)대한 예제를 참조하십시오.

## <a name="cwndloaddynamiclayoutresource"></a><a name="loaddynamiclayoutresource"></a>CWnd::로드 역학 레이아웃 리소스

프레임워크에서 호출되어 리소스 파일에서 동적 레이아웃 정보를 로드합니다.

```
BOOL LoadDynamicLayoutResource(LPCTSTR lpszResourceName);
```

### <a name="parameters"></a>매개 변수

*lpszResourceName*<br/>
이 창에 대한 필요한 동적 레이아웃 정보가 포함된 리소스의 이름입니다.

### <a name="return-value"></a>Return Value

함수가 성공하는 경우 0이 아닙니다. 오류가 발생하는 경우 0입니다.

### <a name="remarks"></a>설명

이 메서드를 직접 호출하지 마세요.

## <a name="cwndlockwindowupdate"></a><a name="lockwindowupdate"></a>CWnd::잠금 창업데이트

지정된 창에서 그리기를 비활성화합니다.

```
BOOL LockWindowUpdate();
```

### <a name="return-value"></a>Return Value

함수가 성공하는 경우 0이 아닙니다. 오류가 발생하거나 함수를 `LockWindowUpdate` 사용하여 다른 창을 잠그는 경우 0입니다.

### <a name="remarks"></a>설명

잠긴 창을 이동할 수 없습니다. 한 번에 하나의 창만 잠글 수 있습니다. 로 잠긴 `LockWindowUpdate`창의 잠금을 해제하려면 [UnlockWindowUpdate](#unlockwindowupdate)를 호출합니다.

잠긴 창(또는 잠긴 자식 창)이 있는 응용 프로그램이 [GetDC,](/windows/win32/api/winuser/nf-winuser-getdc) [GetDCEx](/windows/win32/api/winuser/nf-winuser-getdcex) 또는 [BeginPaint](/windows/win32/api/winuser/nf-winuser-beginpaint) Windows 함수를 호출하는 경우 호출된 함수는 가시 영역이 비어 있는 장치 컨텍스트를 반환합니다. 이 문제는 응용 프로그램이 `UnlockWindowUpdate` 멤버 함수를 호출하여 창잠금을 해제할 때까지 발생합니다.

창 업데이트가 잠겨 있는 동안 시스템은 잠긴 창과 연결된 장치 컨텍스트에 대한 드로잉 작업의 경계 사각형을 추적합니다. 그리기를 다시 활성화하면 잠긴 창과 자식 창에서 이 경계 사각형이 무효화되어 최종 [WM_PAINT](/windows/win32/gdi/wm-paint) 메시지가 화면을 업데이트하도록 합니다. 창 업데이트가 잠겨 있는 동안 도면이 발생하지 않으면 영역이 무효화되지 않습니다.

멤버 `LockWindowUpdate` 함수는 지정된 창을 보이지 않게 하지 않으며 [WS_VISIBLE](styles-used-by-mfc.md#window-styles) 스타일 비트를 지우지 않습니다.

## <a name="cwndm_hwnd"></a><a name="m_hwnd"></a>CWnd::m_hWnd

이 `CWnd`에 연결된 Windows 창의 핸들입니다.

```
HWND m_hWnd;
```

### <a name="remarks"></a>설명

`m_hWnd` 데이터 멤버는 HWND 형식의 공용 변수입니다.

## <a name="cwndmapwindowpoints"></a><a name="mapwindowpoints"></a>CWnd::맵윈도우포인트

`CWnd`의 좌표 공간에서 다른 창의 좌표 공간으로 점 집합(맵)을 변환합니다.

```cpp
void MapWindowPoints(
    CWnd* pwndTo,
    LPRECT lpRect) const;

void MapWindowPoints(
    CWnd* pwndTo,
    LPPOINT lpPoint,
    UINT nCount) const;
```

### <a name="parameters"></a>매개 변수

*pwndTo*<br/>
변환되는 점을 식별하는 창을 식별합니다. 이 매개변수가 NULL이면 점이 화면 좌표로 변환됩니다.

*Lprect*<br/>
변환할 점이 있는 사각형을 지정합니다. 이 함수의 첫 번째 버전은 Windows 3.1 이상에서만 사용할 수 있습니다.

*lpPoint*<br/>
변환할 점 집합을 포함하는 [POINT 구조의](/windows/win32/api/windef/ns-windef-point) 배열에 대한 포인터입니다.

*nCount*<br/>
*lpPoint로*가리키는 `POINT` 배열의 구조 수를 지정합니다.

## <a name="cwndmessagebox"></a><a name="messagebox"></a>CWnd::메시지 상자

응용 프로그램에서 제공한 메시지 및 캡션과 [메시지 상자 스타일](../../mfc/reference/styles-used-by-mfc.md#message-box-styles) 목록에 설명된 미리 정의된 아이콘 및 푸시버튼의 조합이 포함된 창을 만들고 표시합니다.

```
int MessageBox(
    LPCTSTR lpszText,
    LPCTSTR lpszCaption = NULL,
    UINT nType = MB_OK);
```

### <a name="parameters"></a>매개 변수

*lpszText*<br/>
표시할 `CString` 메시지를 포함하는 개체 또는 null 종료 문자열을 가리킵니다.

*lpszCaption*<br/>
메시지 상자 `CString` 캡션에 사용할 개체 또는 null 종료 문자열을 가리킵니다. *lpszCaption이* NULL이면 기본 캡션 "오류"가 사용됩니다.

*nType*<br/>
메시지 상자의 내용과 동작을 지정합니다.

### <a name="return-value"></a>Return Value

이 메서드는 Windows SDK에 정의된 [대로 MessageBox](/windows/win32/api/winuser/nf-winuser-messagebox) 기능을 사용합니다. 이 메서드는 이 함수를 호출하는 결과를 반환합니다.

### <a name="remarks"></a>설명

이 멤버 함수 대신 전역 함수 [AfxMessageBox를](../../mfc/reference/cstring-formatting-and-message-box-display.md#afxmessagebox) 사용하여 응용 프로그램에서 메시지 상자를 구현합니다.

다음은 메시지 상자에서 사용할 수 있는 다양한 시스템 아이콘을 보여 주며,

|||
|-|-|
|![중지 &#40;x&#41; 아이콘](../../mfc/reference/media/vc364f1.gif "중지 &#40;x&#41; 아이콘")|MB_ICONHAND, MB_ICONSTOP 및 MB_ICONERROR|
|![도움말 &#40;?&#41; 아이콘](../../mfc/reference/media/vc364f2.gif "도움말 &#40;?&#41; 아이콘")|MB_ICONQUESTION|
|![중요 &#40;&#33;&#41; 아이콘](../../mfc/reference/media/vc364f3.gif "중요 &#40;&#33;&#41; 아이콘")|MB_ICONEXCLAMATION 및 MB_ICONWARNING|
|![정보 &#40;i&#41; 아이콘](../../mfc/reference/media/vc364f4.gif "정보 &#40;i&#41; 아이콘")|MB_ICONASTERISK 및 MB_ICONINFORMATION|

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#104](../../mfc/reference/codesnippet/cpp/cwnd-class_44.cpp)]

## <a name="cwndmodifystyle"></a><a name="modifystyle"></a>CWnd::수정 스타일

이 멤버 함수를 호출하여 창 스타일을 수정합니다.

```
BOOL ModifyStyle(
    DWORD dwRemove,
    DWORD dwAdd,
    UINT nFlags = 0);
```

### <a name="parameters"></a>매개 변수

*dwRemove*<br/>
스타일을 수정하는 동안 제거할 창 스타일을 지정합니다.

*dwAdd*<br/>
스타일을 수정하는 동안 추가할 창 스타일을 지정합니다.

*nFlags*<br/>
[플래그를 SetWindowPos로](#setwindowpos)전달하거나 호출하지 않아야 하는 경우 `SetWindowPos` 0으로 전달합니다. 기본값은 0입니다. 미리 설정된 플래그 목록은 비고 섹션을 참조하십시오.

### <a name="return-value"></a>Return Value

스타일이 성공적으로 수정된 경우 0이 아닌 경우 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

추가하거나 제거할 스타일은 비트별 OR(&#124;) 연산자를 사용하여 결합할 수 있습니다. 사용 가능한 창 스타일에 대한 자세한 내용은 Windows SDK의 [창 스타일](/windows/win32/winmsg/window-styles) 및 [Create창](/windows/win32/api/winuser/nf-winuser-createwindoww) 항목을 참조하십시오.

*nFlags가* 영하지 `ModifyStyle` 않은 경우 Windows API 함수 [SetWindowPos를](/windows/win32/api/winuser/nf-winuser-setwindowpos) 호출하고 *nFlags를* 다음 네 개의 미리 설정된 플래그와 결합하여 창을 다시 그립니다.

- SWP_NOSIZE 현재 크기를 유지합니다.

- SWP_NOMOVE 현재 위치를 유지합니다.

- SWP_NOZORDER 현재 Z 순서를 유지합니다.

- SWP_NOACTIVATE 창을 활성화하지 않습니다.

창의 확장 스타일을 수정하려면 [스타일텍스 수정을](#modifystyleex)참조하십시오.

> [!NOTE]
> 특정 컨트롤의 일부 스타일(예: 편집 컨트롤의 ES_READONLY `ModifyStyle` 스타일)의 경우 컨트롤이 특별한 내부 처리를 수행해야 할 수 있으므로 스타일을 제대로 변경하지 못할 수 있습니다. 이러한 경우 스타일을 변경하는 해당 메시지를 사용할 수 있습니다(언급된 예에서 EM_SETREADONLY).

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#105](../../mfc/reference/codesnippet/cpp/cwnd-class_45.cpp)]

## <a name="cwndmodifystyleex"></a><a name="modifystyleex"></a>CWnd::수정스타일엑스

이 멤버 함수를 호출하여 창의 확장 스타일을 수정합니다.

```
BOOL ModifyStyleEx(
    DWORD dwRemove,
    DWORD dwAdd,
    UINT nFlags = 0);
```

### <a name="parameters"></a>매개 변수

*dwRemove*<br/>
스타일을 수정하는 동안 제거할 확장 스타일을 지정합니다.

*dwAdd*<br/>
스타일을 수정하는 동안 추가할 확장 스타일을 지정합니다.

*nFlags*<br/>
[플래그를 SetWindowPos로](#setwindowpos)전달하거나 호출하지 않아야 하는 경우 `SetWindowPos` 0으로 전달합니다. 기본값은 0입니다. 미리 설정된 플래그 목록은 비고 섹션을 참조하십시오.

### <a name="return-value"></a>Return Value

스타일이 성공적으로 수정된 경우 0이 아닌 경우 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

추가하거나 제거할 스타일은 비트별 OR(&#124;) 연산자를 사용하여 결합할 수 있습니다. 사용 가능한 확장 스타일에 대한 자세한 내용은 이 설명서의 [확장 창 스타일](../../mfc/reference/styles-used-by-mfc.md#extended-window-styles) 및 Windows SDK의 [CreateWindowEx](/windows/win32/api/winuser/nf-winuser-createwindowexw) 항목을 참조하십시오.

*nFlags가* 영하지 `ModifyStyleEx` 않은 경우 Windows API 함수 [SetWindowPos를](/windows/win32/api/winuser/nf-winuser-setwindowpos) 호출하고 *nFlags를* 다음 네 개의 미리 설정된 플래그와 결합하여 창을 다시 그립니다.

- SWP_NOSIZE 현재 크기를 유지합니다.

- SWP_NOMOVE 현재 위치를 유지합니다.

- SWP_NOZORDER 현재 Z 순서를 유지합니다.

- SWP_NOACTIVATE 창을 활성화하지 않습니다.

일반 창 스타일을 사용하여 창을 수정하려면 [스타일 수정을](#modifystyle)참조하십시오.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#106](../../mfc/reference/codesnippet/cpp/cwnd-class_46.cpp)]

## <a name="cwndmovewindow"></a><a name="movewindow"></a>CWnd::이동 창

위치와 치수를 변경합니다.

```cpp
void MoveWindow(
    int x,
    int y,
    int nWidth,
    int nHeight,
    BOOL bRepaint = TRUE);

void MoveWindow(
    LPCRECT lpRect,
    BOOL bRepaint = TRUE);
```

### <a name="parameters"></a>매개 변수

*x*<br/>
`CWnd`의 왼쪽의 새 위치를 지정합니다.

*Y*<br/>
의 맨 위에 새 위치를 지정합니다. `CWnd`

*nWidth*<br/>
의 새 너비를 지정합니다. `CWnd`

*nHeight*<br/>
의 새 높이를 지정합니다. `CWnd`

*bRepaint*<br/>
다시 칠할지 `CWnd` 여부를 지정합니다. TRUE인 `CWnd` 경우 [OnPaint](#onpaint) 메시지 처리기에서 평소와 같이 [WM_PAINT](/windows/win32/gdi/wm-paint) 메시지를 받습니다. 이 매개 변수가 FALSE이면 어떤 종류의 재도장도 발생하지 않습니다. 이는 클라이언트 영역, 비클라이언트 영역(제목 및 스크롤 막대 포함) 및 `CWnd`이동의 결과로 발견된 상위 창의 모든 부분에 적용됩니다. 이 매개 변수가 FALSE이면 응용 프로그램은 다시 그려야 하는 상위 창 및 상위 창의 `CWnd` 일부를 명시적으로 무효화하거나 다시 그려야 합니다.

*Lprect*<br/>
새 크기와 위치를 지정하는 [CRect](../../atl-mfc-shared/reference/crect-class.md) 개체 또는 [RECT 구조입니다.](/windows/win32/api/windef/ns-windef-rect)

### <a name="remarks"></a>설명

최상위 `CWnd` 개체의 경우 *x* 및 *y* 매개변수는 화면의 왼쪽 위 모서리를 기준으로 합니다. 자식 `CWnd` 개체의 경우 부모 창의 클라이언트 영역의 왼쪽 위 모서리를 기준으로 합니다.

함수는 `MoveWindow` [WM_GETMINMAXINFO](#ongetminmaxinfo) 메시지를 보냅니다. 이 메시지를 `CWnd` 처리하면 가능한 가장 크고 작은 창에 대한 기본값을 수정할 수 있습니다. `MoveWindow` 멤버 함수에 대한 매개 변수가 이러한 값을 초과하면 WM_GETMINMAXINFO 처리기의 최소 값 또는 최대 값으로 값을 대체할 수 있습니다.

### <a name="example"></a>예제

  [CWnd::ClientToScreen](#clienttoscreen)에 대한 예제를 참조하십시오.

## <a name="cwndnotifywinevent"></a><a name="notifywinevent"></a>CWnd::알림윈 이벤트

미리 정의된 이벤트가 발생한 시스템에 신호를 보냅니다. 클라이언트 응용 프로그램이 이벤트에 대한 후크 함수를 등록한 경우 시스템은 클라이언트의 후크 함수를 호출합니다.

```cpp
void NotifyWinEvent(
    DWORD event,
    LONG idObjectType,
    LONG idObject);
```

### <a name="parameters"></a>매개 변수

*event*<br/>
발생한 이벤트를 지정합니다. 이 값은 이벤트 [상수](/windows/win32/WinAuto/event-constants)중 하나여야 합니다.

*idObjectType*<br/>
이벤트를 생성한 개체의 종류를 식별합니다. 이 값은 미리 정의된 [개체 식별자](/windows/win32/WinAuto/object-identifiers) 또는 사용자 지정 개체 ID 값 중 하나입니다.

*idObject*<br/>
이벤트가 개체 또는 개체의 자식 요소에 의해 생성되었는지 여부를 식별합니다. 이 값이 CHILDID_SELF 경우 개체 자체에 의해 이벤트가 생성되었습니다. 그렇지 않은 경우 이 값은 이벤트를 생성한 요소의 자식 ID입니다.

### <a name="remarks"></a>설명

이 멤버 함수는 Windows SDK에 설명된 대로 [NotifyWinEvent](/windows/win32/api/winuser/nf-winuser-notifywinevent)함수의 기능을 에뮬레이트합니다.

## <a name="cwndonactivate"></a><a name="onactivate"></a>CWnd::에 활성화

프레임워크는 개체가 활성화되거나 비활성화될 때 이 멤버 함수를 `CWnd` 호출합니다.

```
afx_msg void OnActivate(
    UINT nState,
    CWnd* pWndOther,
    BOOL bMinimized);
```

### <a name="parameters"></a>매개 변수

*nState*<br/>
활성화 또는 비활성화 `CWnd` 중인지 여부를 지정합니다. 다음 값 중 하나일 수 있습니다.

- WA_INACTIVE 창이 비활성화되고 있습니다.

- WA_ACTIVE 창이 마우스 클릭 이외의 다른 방법을 통해 활성화되고 있습니다(예: 키보드 인터페이스를 사용하여 창을 선택).

- WA_CLICKACTIVE 창이 마우스 클릭으로 활성화되고 있습니다.

*pWnd기타*<br/>
활성화 `CWnd` 또는 비활성화되는 포인터입니다. 포인터는 NULL일 수 있으며 일시적일 수 있습니다.

*b최소화*<br/>
활성화 또는 `CWnd` 비활성화되는 최소화된 상태를 지정합니다. TRUE 값은 창이 최소화했음을 나타냅니다.

TRUE이면 `CWnd` 활성화되고 있습니다. 그렇지 않으면 비활성화됩니다.

### <a name="remarks"></a>설명

개체가 `CWnd` 마우스 클릭으로 활성화되면 [OnMouseActivate](#onmouseactivate) 멤버 기능 호출도 수신됩니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonactivateapp"></a><a name="onactivateapp"></a>CWnd::에 온 활성화 앱

프레임워크는 이 멤버 함수를 활성화 중인 작업의 모든 최상위 창과 비활성화되는 작업의 모든 최상위 창에 호출합니다.

```
afx_msg void OnActivateApp(
    BOOL bActive,
    DWORD dwThreadID);
```

### <a name="parameters"></a>매개 변수

*b활성*<br/>
활성화 또는 비활성화 `CWnd` 중인지 여부를 지정합니다. TRUE는 `CWnd` 활성화됨을 의미합니다. FALSE는 `CWnd` 비활성화됨을 의미합니다.

*dwThreadID*<br/>
스레드 ID의 값을 지정합니다. *bActive가* TRUE인 경우 *dwThreadID는* 비활성화되는 `CWnd` 스레드를 식별하는 것입니다. *bActive가* FALSE인 경우 *dwThreadID는* 활성화되는 `CWnd` 스레드를 식별합니다.

### <a name="remarks"></a>설명

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonambientproperty"></a><a name="onambientproperty"></a>CWnd::온앰비언트 프로퍼티

프레임워크는 이 멤버 함수를 호출하여 OLE 컨트롤이 포함된 창에서 주변 속성 값을 가져옵니다.

```
virtual BOOL OnAmbientProperty(
    COleControlSite* pSite,
    DISPID dispid,
    VARIANT* pvar);
```

### <a name="parameters"></a>매개 변수

*p사이트*<br/>
앰비언트 속성을 요청한 컨트롤의 사이트에 대한 포인터입니다.

*디스피드 (것)들*<br/>
요청된 주변 속성의 디스패치 ID입니다.

*pvar*<br/>
주변 속성의 값이 `VARIANT` 반환되는 호출자 할당 된 구조에 대한 포인터입니다.

### <a name="return-value"></a>Return Value

주변 속성이 지원되는 경우 TRUE입니다. 그렇지 않은 경우 false.

### <a name="remarks"></a>설명

이 함수를 재정의하여 OLE 컨트롤 컨테이너에서 반환하는 기본 주변 속성 값을 해당 컨트롤로 변경합니다. 재정의 함수에서 처리되지 않는 모든 주변 속성 요청은 기본 클래스 구현으로 전달되어야 합니다.

## <a name="cwndonappcommand"></a><a name="onappcommand"></a>CWnd::OnAppCommand

프레임워크는 사용자가 응용 프로그램 명령 이벤트를 생성할 때 이 멤버 함수를 호출합니다. 이러한 이벤트는 사용자가 응용 프로그램 명령 단추를 클릭하거나 응용 프로그램 명령 키를 입력할 때 발생합니다.

```
afx_msg void OnAppCommand(
    CWnd* pWnd,
    UINT nCmd,
    UINT nDevice,
    UINT nKey);
```

### <a name="parameters"></a>매개 변수

|매개 변수|Description|
|---------------|-----------------|
|*pWnd*|【인】 사용자가 명령 `CWnd` 단추를 클릭하거나 명령 키를 누른 창을 나타내는 개체에 대한 포인터입니다. 이 창은 메시지를 받는 창의 자식 창일 수 있습니다.|
|*nCmd*|【인】 응용 프로그램 명령을 나타냅니다. 가능한 값 목록은 [WM_APPCOMMAND](/windows/win32/inputdev/wm-appcommand) *lParam* 매개 변수의 *cmd* 섹션 아래의 명령을 참조하십시오.|
|*n장치*|【인】 입력 이벤트를 생성한 입력 장치입니다. 가능한 값 목록은 [WM_APPCOMMAND](/windows/win32/inputdev/wm-appcommand) *lParam* 매개 변수의 *uDevice* 섹션 아래의 장치를 참조하십시오.|
|*nKey*|【인】 CTRL 키 또는 왼쪽 마우스 단추와 같이 다운된 가상 키를 나타냅니다. 가능한 값 목록은 [WM_APPCOMMAND](/windows/win32/inputdev/wm-appcommand) *lParam* 매개 변수의 *dwKeys* 섹션 아래의 키를 참조하십시오. 자세한 내용은 [마우스 입력 정보에서](/windows/win32/inputdev/about-mouse-input)"메시지 매개 변수" 부제목을 참조하십시오.|

### <a name="remarks"></a>설명

이 메서드는 Windows SDK에 설명 된 [WM_APPCOMMAND](/windows/win32/inputdev/wm-appcommand) 알림을 받습니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonaskcbformatname"></a><a name="onaskcbformatname"></a>CWnd::OnAskCb형식 이름

프레임워크는 클립보드 형식에 대한 데이터 핸들이 CF_OWNERDISPLAY(즉, 클립보드 소유자가 클립보드 내용을 표시할 때)에 대한 데이터 핸들이 있을 때 이 멤버 함수를 호출합니다.

```
afx_msg void OnAskCbFormatName(
    UINT nMaxCount,
    LPTSTR lpszString);
```

### <a name="parameters"></a>매개 변수

*n맥스카운트*<br/>
복사할 최대 바이트 수를 지정합니다.

*lpszString*<br/>
형식 이름의 복사본을 저장할 버퍼를 가리킵니다.

### <a name="remarks"></a>설명

클립보드 소유자는 해당 형식의 이름을 제공해야 합니다.

이 멤버 함수를 재정의하고 지정된 최대 바이트 수를 초과하지 않고 CF_OWNERDISPLAY 형식의 이름을 지정된 버퍼에 복사합니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndoncancelmode"></a><a name="oncancelmode"></a>CWnd::온캔스 모드

프레임워크는 이 멤버 함수를 호출하여 내부 모드를 취소하라는 정보를 제공합니다. `CWnd`

```
afx_msg void OnCancelMode();
```

### <a name="remarks"></a>설명

개체에 `CWnd` 포커스가 있으면 `OnCancelMode` 대화 상자 또는 메시지 상자가 표시될 때 해당 멤버 함수가 호출됩니다. 이렇게 하면 `CWnd` 마우스 캡처와 같은 모드를 취소할 수 있습니다.

기본 구현은 [ReleaseCapture](/windows/win32/api/winuser/nf-winuser-releasecapture) Windows 함수를 호출하여 응답합니다. 파생 클래스에서 이 멤버 함수를 재정의하여 다른 모드를 처리합니다.

## <a name="cwndoncapturechanged"></a><a name="oncapturechanged"></a>CWnd::에 캡처 변경

프레임워크는 이 멤버 함수를 호출하여 마우스 캡처가 손실되는 창에 알립니다.

```
afx_msg void OnCaptureChanged(CWnd* pWnd);
```

### <a name="parameters"></a>매개 변수

*pWnd*<br/>
창에 대한 포인터로 마우스 캡처

### <a name="remarks"></a>설명

창은 [ReleaseCapture](/windows/win32/api/winuser/nf-winuser-releasecapture) 자체를 호출하는 경우에도 이 메시지를 수신합니다. 응용 프로그램은 이 메시지에 대한 응답으로 마우스 캡처를 설정하려고 시도해서는 안 됩니다. 이 메시지가 수신되면 필요한 경우 새 마우스 캡처 상태를 반영하기 위해 창이 자체적으로 다시 그려야 합니다.

Windows 기능에 대한 자세한 내용은 `ReleaseCapture` Windows SDK를 참조하십시오.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonchangecbchain"></a><a name="onchangecbchain"></a>CWnd::온체인Cb체인

프레임워크는 Clipboard-Viewer 체인의 각 창에 대해 이 멤버 함수를 호출하여 창이 체인에서 제거되고 있음을 알립니다.

```
afx_msg void OnChangeCbChain(
    HWND hWndRemove,
    HWND hWndAfter);
```

### <a name="parameters"></a>매개 변수

*hWndRemove*<br/>
클립보드 뷰어 체인에서 제거되는 창 핸들을 지정합니다.

*hWnd 후*<br/>
클립보드 뷰어 체인에서 제거되는 창 다음에 오는 창 핸들을 지정합니다.

### <a name="remarks"></a>설명

`CWnd` 호출을 `OnChangeCbChain` 받는 각 개체는 [SendMessage](/windows/win32/api/winuser/nf-winuser-sendmessage) Windows 함수를 사용하여 클립보드 뷰어 체인의 다음 창(반환된 `SetClipboardViewer`핸들)의 다음 창으로 [WM_CHANGECBCHAIN 메시지를](/windows/win32/dataxchg/wm-changecbchain) 보내야 합니다. *hWndRemove체인의* 다음 창인 경우 *hWndAfter에* 의해 지정된 창이 다음 창이 되고 클립보드 메시지가 전달됩니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonchangeuistate"></a><a name="onchangeuistate"></a>CWnd::온체인지UI스테이트

UI(사용자 인터페이스) 상태를 변경해야 할 때 호출됩니다.

```
afx_msg void OnChangeUIState(
    UINT nAction,
    UINT nUIElement);
```

### <a name="parameters"></a>매개 변수

*nAction*<br/>
수행할 작업을 지정합니다. 다음 값 중 하나를 사용할 수 있습니다.

- UIS_CLEAR ui 상태 *요소(nUIElement에*의해 지정)는 숨춰져야 합니다.

- UIS_INITIALIZE *ui Element에*의해 지정 된 UI 상태 요소 마지막 입력 이벤트에 따라 변경 해야 합니다. 자세한 내용은 [WM_CHANGEUISTATE](/windows/win32/menurc/wm-changeuistate) **의 비고** 섹션을 참조하십시오.

- UIS_SET ui 상태 *요소(nUIElement로*지정)가 표시되어야 합니다.

*누이 엘리먼트*<br/>
영향을 받는 UI 상태 요소 또는 컨트롤의 스타일을 지정합니다. 다음 값 중 하나를 사용할 수 있습니다.

- UISF_HIDEACCEL 키보드 가속기.

- UISF_HIDEFOCUS 초점 지표.

- windows XP UISF_ACTIVE: 컨트롤은 활성 컨트롤에 사용되는 스타일로 그려야 합니다.

### <a name="remarks"></a>설명

이 멤버 함수는 Windows SDK에 설명된 대로 [WM_CHANGEUISTATE](/windows/win32/menurc/wm-changeuistate) 메시지의 기능을 에뮬레이트합니다.

## <a name="cwndonchar"></a><a name="onchar"></a>CWnd::온차

프레임워크는 키 입력이 비시스템 문자로 변환될 때 이 멤버 함수를 호출합니다.

```
afx_msg void OnChar(
    UINT nChar,
    UINT nRepCnt,
    UINT nFlags);
```

### <a name="parameters"></a>매개 변수

*Nchar*<br/>
키의 문자 코드 값을 포함 합니다.

*nRepCnt*<br/>
반복 횟수가 포함되며 사용자가 키를 보류할 때 키 입력이 반복되는 횟수입니다.

*nFlags*<br/>
다음 목록과 같이 검색 코드, 키 전환 코드, 이전 키 상태 및 컨텍스트 코드가 포함됩니다.

|값|의미|
|-----------|-------------|
|0-15|반복 횟수를 지정합니다. 이 값은 사용자가 키를 누르고 있는 결과로 키 입력이 반복되는 횟수입니다.|
|16-23|검색 코드를 지정합니다. 값은 원래 장비 제조업체(OEM)에 따라 다릅니다.|
|24|키가 향상된 101키 또는 102키 키보드에 나타나는 오른쪽 ALT 및 CTRL 키와 같은 확장 키인지 여부를 지정합니다. 확장 키인 경우 값은 1입니다. 그렇지 않으면 0입니다.|
|25-28|Windows에서 내부적으로 사용됩니다.|
|29|컨텍스트 코드를 지정합니다. 값을 누르고 있는 동안 ALT 키가 누를 경우 값은 1입니다. 그렇지 않으면 값은 0입니다.|
|30|이전 키 상태를 지정합니다. 메시지가 전송되기 전에 키가 다운된 경우 값이 1이거나 키가 위인 경우 0입니다.|
|31|전환 상태를 지정합니다. 키가 해제되는 경우 값은 1이거나 키를 누를 경우 0입니다.|

### <a name="remarks"></a>설명

이 함수는 [OnKeyUp](#onkeyup) 멤버 함수 앞에 호출되고 [OnKeyDown](#onkeydown) 멤버 함수가 호출된 후에 호출됩니다. `OnChar`에는 누르거나 놓는 키보드 키의 값이 포함되어 있습니다.

키를 누르고 `OnChar` 생성된 호출 간에 일대일 대응이 반드시 있을 필요는 없으므로 *nFlags의* 정보는 일반적으로 응용 프로그램에 유용하지 않습니다. *nFlags의* 정보는 `OnKeyUp` 에 대한 호출 앞에 오는 멤버 `OnKeyDown` 함수 또는 멤버 함수에 `OnChar`대한 최신 호출에만 적용됩니다.

IBM 향상된 101키 및 102키 키보드의 경우 향상된 키는 올바른 ALT이며 키보드의 기본 섹션에 있는 올바른 CTRL 키입니다. 숫자 키패드의 왼쪽에 있는 클러스터의 INS, DEL, HOME, END, 페이지 업, 페이지 다운 및 화살표 키; 및 숫자 키패드의 슬래시(/) 및 ENTER 키입니다. 일부 다른 키보드는 *nFlags에서*확장 키 비트를 지원할 수 있습니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonchartoitem"></a><a name="onchartoitem"></a>CWnd::OnCharToItem

[LBS_WANTKEYBOARDINPUT](../../mfc/reference/styles-used-by-mfc.md#list-box-styles) 스타일이 있는 목록 상자가 [소유자에게 WM_CHAR](#onchar) 메시지에 대한 응답으로 [WM_CHARTOITEM](/windows/win32/Controls/wm-chartoitem) 메시지를 보낼 때 호출됩니다.

```
afx_msg int OnCharToItem(
    UINT nChar,
    CListBox* pListBox,
    UINT nIndex);
```

### <a name="parameters"></a>매개 변수

*Nchar*<br/>
사용자가 누른 키 값을 지정합니다.

*pListBox*<br/>
목록 상자에 대한 포인터를 지정합니다. 일시적일 수 있습니다.

*nIndex*<br/>
현재 카포트 위치를 지정합니다.

### <a name="return-value"></a>Return Value

프레임워크는 이 멤버 함수를 호출하여 응용 프로그램이 호출에 응답하여 수행한 작업을 지정합니다. 반환 값 -2는 응용 프로그램이 항목을 선택하는 모든 측면을 처리하고 목록 상자에서 추가 작업을 원하지 않음을 나타냅니다. 반환 값 -1은 목록 상자가 키 입력에 대한 응답으로 기본 작업을 수행해야 함을 나타냅니다. 반환 값이 0 이상이면 목록 상자에 있는 항목의 0기반 인덱스를 지정하고 목록 상자에서 지정된 항목의 키 입력에 대한 기본 작업을 수행해야 함을 나타냅니다.

### <a name="remarks"></a>설명

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonchildactivate"></a><a name="onchildactivate"></a>CWnd::온차일드 활성화

개체가 `CWnd` 여러 문서 인터페이스(MDI) 자식 `OnChildActivate` 창인 경우 사용자가 창의 제목 표시줄을 클릭하거나 창이 활성화, 이동 또는 크기가 조정될 때 프레임워크에서 호출됩니다.

```
afx_msg void OnChildActivate();
```

## <a name="cwndonchildnotify"></a><a name="onchildnotify"></a>CWnd::에차일드 알림

이 멤버 함수는 이 창에 적용되는 알림 메시지를 받을 때 이 창의 부모 창에서 호출됩니다.

```
virtual BOOL OnChildNotify(
    UINT message,
    WPARAM wParam,
    LPARAM lParam,
    LRESULT* pResult);
```

### <a name="parameters"></a>매개 변수

*message*<br/>
부모 창으로 전송된 Windows 메시지 번호입니다.

*wParam*<br/>
메시지와 연결된 *wparam입니다.*

*lParam*<br/>
메시지와 연결된 *lparam입니다.*

*pL결과*<br/>
부모의 창 프로시저에서 반환할 값에 대한 포인터입니다. 반환 값이 예상되지 않으면 이 포인터는 NULL이 됩니다.

### <a name="return-value"></a>Return Value

이 창이 부모에게 전송된 메시지를 처리하는 경우 Nonzero; 그렇지 않으면 0.

### <a name="remarks"></a>설명

이 멤버 함수를 직접 호출하지 마십시오.

이 멤버 함수의 기본 구현은 부모가 메시지를 처리해야 한다는 0을 반환합니다.

이 멤버 함수를 재정의하여 컨트롤이 알림 메시지에 응답하는 방식을 확장합니다.

## <a name="cwndonclipboardupdate"></a><a name="onclipboardupdate"></a>CWnd::온클립보드 업데이트

프레임워크는 클립보드의 내용이 변경되면 이 멤버 함수를 호출합니다.

```
afx_msg void OnClipboardUpdate();
```

## <a name="cwndonclose"></a><a name="onclose"></a>CWnd::온클로즈

프레임워크는 이 멤버 함수를 `CWnd` 또는 응용 프로그램이 종료한다는 신호로 호출합니다.

```
afx_msg void OnClose();
```

### <a name="remarks"></a>설명

기본 구현은 `DestroyWindow`을 호출합니다.

## <a name="cwndoncolorizationcolorchanged"></a><a name="oncolorizationcolorchanged"></a>CWnd::온컬러화색상 변경

프레임워크는 비클라이언트 영역에 대한 렌더링 정책이 변경되면 이 멤버를 호출합니다.

```
afx_msg void OnColorizationColorChanged(
    DWORD dwColorizationColor,
    BOOL bOpacity);
```

### <a name="parameters"></a>매개 변수

|매개 변수|Description|
|---------------|-----------------|
|*dw색화색상*|【인】 새 색상 색상을 지정합니다.<br /><br /> 색상 형식은 0xARRGGBB 양식의 헥사데임의 수이며, 여기서 네 가지 구성 요소 각각은 0x00에서 0xFF사이입니다. AA 구성 요소는 알파 값이고 RR은 빨간색이고 GG는 녹색이고 BB는 파란색입니다.|
|*bOpacity*|【인】 새 색상이 불투명도와 혼합된 경우 TRUE; false가 아닌 경우.|

### <a name="remarks"></a>설명

이 메서드는 Windows SDK에 설명 된 [WM_DWMNCRENDERINGCHANGED](/windows/win32/dwm/wm-dwmcolorizationcolorchanged) 알림 메시지를 받습니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndoncommand"></a><a name="oncommand"></a>CWnd::온 커맨드

프레임워크는 사용자가 메뉴에서 항목을 선택하거나, 자식 컨트롤이 알림 메시지를 보낼 때 또는 가속기 키 입력이 번역될 때 이 멤버 함수를 호출합니다.

```
virtual BOOL OnCommand(
    WPARAM wParam,
    LPARAM lParam);
```

### <a name="parameters"></a>매개 변수

*wParam*<br/>
*wParam의* 낮은 순서 단어는 메뉴 항목, 컨트롤 또는 가속기의 명령 ID를 식별합니다. *wParam의* 높은 순서 단어는 메시지가 컨트롤에서 온 경우 알림 메시지를 지정합니다. 메시지가 액셀러레이터에서 온 경우 높은 순서의 단어는 1입니다. 메시지가 메뉴에서 온 경우 높은 순서의 단어는 0입니다.

*lParam*<br/>
메시지가 컨트롤에서 온 경우 메시지를 보내는 컨트롤을 식별합니다. 그렇지 않으면 *lParam은* 0입니다.

### <a name="return-value"></a>Return Value

응용 프로그램은 이 메시지를 처리하는 경우 0이 아닌 것을 반환합니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

`OnCommand`은 제어 알림 및 ON_COMMAND 항목에 대한 메시지 맵을 처리하고 적절한 멤버 함수를 호출합니다.

파생 클래스에서 이 멤버 함수를 재정의하여 [WM_COMMAND](/windows/win32/menurc/wm-command) 메시지를 처리합니다. 재정의는 기본 클래스가 `OnCommand` 호출되지 않는 한 메시지 맵을 처리하지 않습니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndoncompacting"></a><a name="oncompacting"></a>CWnd::온콤다짐

Windows에서 30초에서 60초 간격으로 시스템 시간의 12.5% 이상이 메모리 압축에 소요되는 것을 감지하면 프레임워크는 모든 최상위 창에 대해 이 멤버 함수를 호출합니다.

```
afx_msg void OnCompacting(UINT nCpuTime);
```

### <a name="parameters"></a>매개 변수

*nCpu시간*<br/>
Windows에서 현재 메모리를 압축하여 다른 작업을 수행하는 데 소비한 CPU 시간의 비율을 지정합니다. 예를 들어 8000h는 메모리 압축에 소요된 CPU 시간의 50%를 나타냅니다.

### <a name="remarks"></a>설명

시스템 메모리가 부족했음을 나타냅니다.

개체가 `CWnd` 이 호출을 수신하면 응용 프로그램의 현재 작업 수준과 Windows에서 실행 중인 총 응용 프로그램 수를 고려하여 가능한 한 많은 메모리를 확보해야 합니다. 응용 프로그램은 Windows 함수를 호출하여 실행 중인 응용 프로그램의 수를 확인할 수 있습니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndoncompareitem"></a><a name="oncompareitem"></a>CWnd::에 비교 항목

프레임워크는 이 멤버 함수를 호출하여 자식 정렬된 소유자-그리기 콤보 또는 목록 상자에서 새 항목의 상대적 위치를 지정합니다.

```
afx_msg int OnCompareItem(
    int nIDCtl,
    LPCOMPAREITEMSTRUCT lpCompareItemStruct);
```

### <a name="parameters"></a>매개 변수

*니디크틀*<br/>
WM_COMPAREITEM 메시지를 보낸 컨트롤의 식별자입니다.

*lpCompareItemStruct*<br/>
콤보 또는 목록 상자에 있는 두 항목에 대한 식별자 및 응용 프로그램에서 제공한 데이터를 포함하는 [COMPAREITEMSTRUCT](/windows/win32/api/winuser/ns-winuser-compareitemstruct) 데이터 구조에 대한 긴 포인터가 포함되어 있습니다.

### <a name="return-value"></a>Return Value

두 항목의 상대 위치를 나타냅니다. 다음 값 중 어느 값일 수도 있습니다.

|값|의미|
|-----------|-------------|
|-1|항목 1 항목 2 전에 정렬합니다.|
|0|항목 1과 항목 2는 동일하게 정렬됩니다.|
|1|항목 1 항목 2 후 정렬 합니다.|

### <a name="remarks"></a>설명

[CBS_SORT](../../mfc/reference/styles-used-by-mfc.md#combo-box-styles) 또는 [LBS_SORT](../../mfc/reference/styles-used-by-mfc.md#list-box-styles) 스타일로 콤보 또는 목록 상자를 만든 경우 Windows는 응용 프로그램이 새 항목을 추가할 때마다 콤보 상자 또는 목록 상자 소유자에게 WM_COMPAREITEM 메시지를 보냅니다.

콤보 또는 목록 상자에 있는 두 개의 `COMPAREITEMSTRUCT` 항목은 *lpCompareItemStruct에*의해 가리키는 구조로 개혁됩니다. `OnCompareItem`는 다른 항목 앞에 표시해야 하는 항목을 나타내는 값을 반환해야 합니다. 일반적으로 Windows는 새 항목에 대한 정확한 위치를 결정할 때까지 이 호출을 여러 번 합니다.

구조체의 `hwndItem` `COMPAREITEMSTRUCT` 멤버가 [CListBox](../../mfc/reference/clistbox-class.md) 또는 [CComboBox](../../mfc/reference/ccombobox-class.md) 개체에 `CompareItem` 속하는 경우 해당 클래스의 가상 함수가 호출됩니다. 항목 비교를 수행하려면 `CListBox` `CComboBox` 파생 또는 `CComboBox::CompareItem` `CListBox::CompareItem` 클래스에서 재정의합니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndoncompositionchanged"></a><a name="oncompositionchanged"></a>CWnd::에 구성변경

DWM(데스크톱 창 관리자) 컴포지션을 사용 하거나 사용 하지 않도록 설정 하는 경우 프레임 워크는 모든 최상위 창에 대 한이 멤버 함수를 호출 합니다.

```
afx_msg void OnCompositionChanged();
```

### <a name="remarks"></a>설명

이 메서드는 Windows SDK에 설명 된 [WM_DWMCOMPOSITIONCHANGED](/windows/win32/dwm/wm-dwmcompositionchanged) 알림을 받습니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndoncontextmenu"></a><a name="oncontextmenu"></a>CWnd::컨텍스트 메뉴

사용자가 창에서 오른쪽 마우스 단추(마우스 오른쪽 클릭)를 클릭하면 프레임워크에서 호출됩니다.

```
afx_msg void OnContextMenu(
    CWnd* pWnd,
    CPoint pos);
```

### <a name="parameters"></a>매개 변수

*pWnd*<br/>
사용자가 마우스를 마우스 오른쪽 단추로 클릭한 창에 핸들을 맞습니다. 메시지를 받는 창의 자식 창일 수 있습니다. 이 메시지 처리에 대한 자세한 내용은 설명 섹션을 참조하십시오.

*Pos*<br/>
마우스 클릭 시 커서의 위치( 화면 좌표)입니다.

### <a name="remarks"></a>설명

[TrackPopupMenu를](../../mfc/reference/cmenu-class.md#trackpopupmenu)사용하여 컨텍스트 메뉴를 표시하여 이 메시지를 처리할 수 있습니다.

컨텍스트 메뉴를 표시하지 않으면 이 메시지를 [DefWindowProc](#defwindowproc) 함수에 전달해야 합니다. 창이 하위 창인 `DefWindowProc` 경우 부모에게 메시지를 보냅니다. 그렇지 `DefWindowProc` 않으면 지정된 위치가 창의 캡션에 있는 경우 기본 컨텍스트 메뉴가 표시됩니다.

## <a name="cwndoncopydata"></a><a name="oncopydata"></a>CWnd::온카피데이터

이 멤버 함수는 프레임워크에서 한 응용 프로그램에서 다른 응용 프로그램으로 데이터를 복사하는 것을 호출합니다.

```
afx_msg BOOL OnCopyData(
    CWnd* pWnd,
    COPYDATASTRUCT* pCopyDataStruct);
```

### <a name="parameters"></a>매개 변수

*pWnd*<br/>
데이터를 보내는 `CWnd` 개체에 대한 포인터입니다.

*pCopyDataStruct*<br/>
전송되는 데이터를 포함하는 [COPYDATASTRUCT](/windows/win32/api/winuser/ns-winuser-copydatastruct) 구조에 대한 포인터입니다.

### <a name="return-value"></a>Return Value

수신 응용 프로그램이 데이터를 성공적으로 수락하면 TRUE를 반환합니다. 그렇지 않으면 FALSE를 반환합니다.

### <a name="remarks"></a>설명

전달되는 데이터는 데이터를 수신하는 응용 프로그램에서 액세스할 수 없는 개체에 대한 포인터 또는 기타 참조를 포함해서는 안 됩니다.

데이터가 복사되는 동안 전송 프로세스의 다른 스레드에서 데이터를 변경하면 안 됩니다.

수신 응용 프로그램은 데이터 읽기 전용을 고려해야 합니다. 매개 변수 *pCopyDataStruct가* 가리키는 구조는 데이터 전송 중에만 유효합니다. 그러나 수신 응용 프로그램은 구조와 연결된 메모리를 해제해서는 안 됩니다.

이 함수가 반환된 후 수신 응용 프로그램이 데이터에 액세스해야 하는 경우 수신된 데이터를 로컬 버퍼에 복사해야 합니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndoncreate"></a><a name="oncreate"></a>CWnd::에서 만들기

프레임워크는 응용 프로그램이 [Create](#create) 또는 CreateEx 멤버 함수를 호출하여 Windows 창을 만들것을 요청할 때 이 멤버 [함수를](#createex) 호출합니다.

```
afx_msg int OnCreate(LPCREATESTRUCT lpCreateStruct);
```

### <a name="parameters"></a>매개 변수

*lp창조*<br/>
생성되는 개체에 대한 정보가 포함된 `CWnd` [CREATESTRUCT](/windows/win32/api/winuser/ns-winuser-createstructw) 구조를 가리킵니다.

### <a name="return-value"></a>Return Value

`OnCreate``CWnd` 개체 만들기를 계속하려면 0을 반환해야 합니다. 응용 프로그램이 -1을 반환하면 창이 소멸됩니다.

### <a name="remarks"></a>설명

개체는 `CWnd` 창이 만들어지지만 표시되기 전에 이 호출을 수신합니다. `OnCreate`또는 `CreateEx` 멤버 `Create` 함수가 반환되기 전에 호출됩니다.

파생 클래스의 필요한 초기화를 수행하려면 이 멤버 함수를 재정의합니다.

구조에는 `CREATESTRUCT` 창을 만드는 데 사용되는 매개 변수의 복사본이 포함되어 있습니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonctlcolor"></a><a name="onctlcolor"></a>CWnd::온CtlColor

자식 컨트롤이 그려질 때 프레임워크는 이 멤버 함수를 호출합니다.

```
afx_msg HBRUSH OnCtlColor(
    CDC* pDC,
    CWnd* pWnd,
    UINT nCtlColor);
```

### <a name="parameters"></a>매개 변수

*pDC*<br/>
자식 창에 대한 표시 컨텍스트에 대한 포인터를 포함합니다. 일시적일 수 있습니다.

*pWnd*<br/>
색상을 묻는 컨트롤에 대한 포인터가 포함되어 있습니다. 일시적일 수 있습니다.

*nCtlColor*<br/>
컨트롤 의 형식을 지정 하는 다음 값 중 하나를 포함 합니다.

- CTLCOLOR_BTN 버튼 컨트롤

- CTLCOLOR_DLG 대화 상자

- CTLCOLOR_EDIT 편집 컨트롤

- CTLCOLOR_LISTBOX 목록 상자 컨트롤

- CTLCOLOR_MSGBOX 메시지 상자

- CTLCOLOR_SCROLLBAR 스크롤 막대 컨트롤

- CTLCOLOR_STATIC 정적 제어

### <a name="return-value"></a>Return Value

`OnCtlColor`컨트롤 배경을 페인팅하는 데 사용할 브러시에 핸들을 반환해야 합니다.

### <a name="remarks"></a>설명

대부분의 컨트롤은 올바른 색상을 사용하여 컨트롤을 그리기 위해 *pDC를* 준비하기 위해 부모(일반적으로 대화 상자)에 이 메시지를 보냅니다.

텍스트 색상을 변경하려면 원하는 `SetTextColor` 빨간색, 녹색 및 파란색(RGB) 값을 사용하여 멤버 함수를 호출합니다.

한 줄 편집 컨트롤의 배경 색을 변경하려면 CTLCOLOR_EDIT 및 CTLCOLOR_MSGBOX 메시지 코드 모두에서 브러시 핸들을 설정하고 CTLCOLOR_EDIT 코드에 대한 응답으로 [CDC:SetBkColor](../../mfc/reference/cdc-class.md#setbkcolor) 함수를 호출합니다.

`OnCtlColor`드롭다운 목록 상자는 실제로 창의 자식이 아닌 콤보 상자의 자식이므로 드롭다운 콤보 상자의 목록 상자가 호출되지 않습니다. 드롭다운 목록 상자의 색상을 변경하려면 `CComboBox` `OnCtlColor` `nCtlColor` 매개 변수에서 CTLCOLOR_LISTBOX 검사하는 재정의를 사용하여 을 만듭니다. 이 처리기에서 `SetBkColor` 멤버 함수를 사용하여 텍스트의 배경색을 설정해야 합니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다. 대화 상자 클래스에 다음 메서드를 추가하려면 Visual Studio 속성 창을 사용하여 WM_CTLCOLOR 위한 메시지 처리기를 추가합니다. 또는 메시지 맵에 ON_WM_CTLCOLOR() 항목을 수동으로 추가할 수 있습니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#107](../../mfc/reference/codesnippet/cpp/cwnd-class_47.cpp)]

## <a name="cwndondeadchar"></a><a name="ondeadchar"></a>CWnd::온데드차

[프레임워크는 OnKeyUp](#onkeyup) 멤버 함수와 [OnKeyDown](#onkeydown) 멤버 함수가 호출될 때 이 멤버 함수를 호출합니다.

```
afx_msg void OnDeadChar(
    UINT nChar,
    UINT nRepCnt,
    UINT nFlags);
```

### <a name="parameters"></a>매개 변수

*Nchar*<br/>
데드 키 문자 값을 지정합니다.

*nRepCnt*<br/>
반복 횟수를 지정합니다.

*nFlags*<br/>
다음 목록과 같이 검색 코드, 키 전환 코드, 이전 키 상태 및 컨텍스트 코드를 지정합니다.

|값|Description|
|-----------|-----------------|
|0-7|스캔 코드(OEM 종속 값). 높은 순서 단어의 낮은 바이트입니다.|
|8|함수 키 또는 숫자 키패드의 키와 같은 확장 키(확장 키인 경우 1, 그렇지 않으면 0).|
|9-10|사용되지 않습니다.|
|11-12|Windows에서 내부적으로 사용됩니다.|
|13|컨텍스트 코드(키를 누른 상태에서 ALT 키가 누수된 경우 1, 그렇지 않으면 0).|
|14|이전 키 상태(1키가 호출 되기 전에 다운된 경우, 키가 위태인 경우 0).|
|15|전환 상태(키가 해제되는 경우 1, 키를 누르는 경우 0).|

### <a name="remarks"></a>설명

이 멤버 함수는 죽은 키의 문자 값을 지정하는 데 사용할 수 있습니다. 데드 키는 움라우트(이중 점) 문자와 같은 키로, 다른 문자와 결합되어 복합 문자를 형성합니다. 예를 들어 움라우트-O 문자는 죽은 키, 움라우트 및 O 키로 구성됩니다.

응용 프로그램은 `OnDeadChar` 일반적으로 누른 각 키에 대한 사용자 피드백을 제공하는 데 사용합니다. 예를 들어 응용 프로그램은 캐런을 이동하지 않고 현재 문자 위치에 악센트를 표시할 수 있습니다.

키 누른 것과 `OnDeadChar` 호출 간에 일대일 대응이 반드시 있는 것은 아니므로 *nFlags의* 정보는 일반적으로 응용 프로그램에 유용하지 않습니다. *nFlags의* 정보는 `OnDeadChar` [OnKeyUp](#onkeyup) 멤버 함수 또는 호출 앞에 오는 [OnKeyDown](#onkeydown) 멤버 함수에 대한 최신 호출에만 적용됩니다.

IBM 향상된 101키 및 102키 키보드의 경우 향상된 키는 올바른 ALT이며 키보드의 기본 섹션에 있는 올바른 CTRL 키입니다. 숫자 키패드의 왼쪽에 있는 클러스터의 INS, DEL, HOME, END, 페이지 업, 페이지 다운 및 화살표 키; 및 숫자 키패드의 슬래시(/) 및 ENTER 키입니다. 일부 다른 키보드는 *nFlags에서*확장 키 비트를 지원할 수 있습니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndondeleteitem"></a><a name="ondeleteitem"></a>CWnd::에 삭제 항목

프레임워크는 이 멤버 함수를 호출하여 소유자-그리기 목록 상자 또는 콤보 상자의 소유자에게 목록 상자 또는 콤보 상자가 파괴되었거나 [CComboBox::DeleteString](../../mfc/reference/ccombobox-class.md#deletestring), [CListBox::DeleteString](../../mfc/reference/clistbox-class.md#deletestring), [CComboBox::ResetContent:](../../mfc/reference/ccombobox-class.md#resetcontent)또는 [CListBox:ResetContent에](../../mfc/reference/clistbox-class.md#resetcontent)의해 항목이 제거되었음을 알립니다.

```
afx_msg void OnDeleteItem(
    int nIDCtl,
    LPDELETEITEMSTRUCT lpDeleteItemStruct);
```

### <a name="parameters"></a>매개 변수

*니디크틀*<br/>
WM_DELETEITEM 메시지를 보낸 컨트롤의 식별자입니다.

*lpDeleteItemStruct*<br/>
삭제된 목록 상자 항목에 대한 정보가 포함된 [DELETEITEMSTRUCT](/windows/win32/api/winuser/ns-winuser-deleteitemstruct) 데이터 구조에 대한 긴 포인터를 지정합니다.

### <a name="remarks"></a>설명

구조체의 멤버가 `hwndItem` `DELETEITEMSTRUCT` 콤보 상자 또는 목록 상자에 `DeleteItem` 속하는 경우 해당 클래스의 가상 함수가 호출됩니다. 적절한 컨트롤 `DeleteItem` 클래스의 멤버 함수를 재정의하여 항목별 데이터를 삭제합니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndondestroy"></a><a name="ondestroy"></a>CWnd::온Destroy

프레임워크는 이 멤버 함수를 `CWnd` 호출하여 개체가 소멸되고 있음을 알립니다.

```
afx_msg void OnDestroy();
```

### <a name="remarks"></a>설명

`OnDestroy`은 화면에서 `CWnd` 개체를 제거한 후에 호출됩니다.

`OnDestroy`먼저 `CWnd` 파괴되는 것에 대해, 그리고 그들이 `CWnd` 파괴되는 자식 창문을 위해 호출됩니다. 실행 하는 동안 `OnDestroy` 모든 자식 창이 여전히 존재 한다고 가정할 수 있습니다.

소멸되는 `CWnd` 객체가 Clipboard-Viewer 체인의 일부인 `OnDestroy` `CWnd` [경우(SetClipboardViewer](#setclipboardviewer) 멤버 함수를 호출하여 설정됨) 함수에서 반환하기 전에 [ChangeClipboardChain](#changeclipboardchain) 멤버 함수를 호출하여 클립보드 뷰어 체인에서 자신을 제거해야 합니다.

## <a name="cwndondestroyclipboard"></a><a name="ondestroyclipboard"></a>CWnd::온Destroy클립보드

Windows 보드가 [EmptyClipboard](/windows/win32/api/winuser/nf-winuser-emptyclipboard) Windows 함수에 대한 호출을 통해 비워지면 프레임워크는 클립보드 소유자에 대해 이 멤버 함수를 호출합니다.

```
afx_msg void OnDestroyClipboard();
```

## <a name="cwndondevicechange"></a><a name="ondevicechange"></a>CWnd:::온디바이스 체인지

프레임워크는 이 멤버 함수를 호출하여 응용 프로그램 또는 장치 드라이버에게 장치 또는 컴퓨터의 하드웨어 구성에 대한 변경 사항을 알립니다.

```
afx_msg BOOL OnDeviceChange(
    UINT nEventType,
    DWORD_PTR dwData);
```

### <a name="parameters"></a>매개 변수

*n이벤트유형*<br/>
이벤트 유형입니다. 사용 가능한 값에 대한 설명은 비고 섹션을 참조하십시오.

*dwData*<br/>
이벤트 별 데이터를 포함하는 구조의 주소입니다. 그 의미는 주어진 이벤트에 따라 달라집니다.

### <a name="remarks"></a>설명

꺼내기 및 잠금과 같은 소프트웨어 제어 기능을 제공하는 장치의 경우 운영 체제는 일반적으로 응용 프로그램 및 장치 드라이버가 장치 사용을 정상적으로 종료할 수 있도록 DBT_DEVICEREMOVEPENDING 메시지를 보냅니다.

운영 체제가 장치를 강제로 제거하는 경우 DBT_DEVICEQUERYREMOVE 메시지를 보내지 않을 수 있습니다.

*nEvent* 매개 변수는 다음 값 중 하나일 수 있습니다.

- [DBT_DEVICEARRIVAL](/windows/win32/DevIO/dbt-devicearrival) 장치가 삽입되어 이제 사용할 수 있습니다.

- [DBT_DEVICEQUERYREMOVE](/windows/win32/DevIO/dbt-devicequeryremove) 장치를 제거할 수 있는 권한이 요청됩니다. 모든 응용 프로그램은 이 요청을 거부하고 제거를 취소할 수 있습니다.

- [DBT_DEVICEQUERYREMOVEFAILED](/windows/win32/DevIO/dbt-devicequeryremovefailed) 장치 제거 요청이 취소되었습니다.

- [DBT_DEVICEREMOVEPENDING](/windows/win32/DevIO/dbt-deviceremovepending) 장치가 제거될 예정입니다. 거부할 수 없습니다.

- [DBT_DEVICEREMOVECOMPLETE](/windows/win32/DevIO/dbt-deviceremovecomplete) 장치가 제거되었습니다.

- [DBT_DEVICETYPESPECIFIC](/windows/win32/DevIO/dbt-devicetypespecific) 장치별 이벤트입니다.

- [DBT_CONFIGCHANGED](/windows/win32/DevIO/dbt-configchanged) 현재 구성이 변경되었습니다.

- 장치 노드가 변경된 DBT_DEVNODES_CHANGED.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndondevmodechange"></a><a name="ondevmodechange"></a>CWnd::온데브모드체인지

프레임워크는 사용자가 장치 모드 설정을 `CWnd` 변경할 때 모든 최상위 개체에 대해 이 멤버 함수를 호출합니다.

```
afx_msg void OnDevModeChange(LPTSTR lpDeviceName);
```

### <a name="parameters"></a>매개 변수

*lpDeviceName*<br/>
Windows 초기화 파일 WIN에 지정된 장치 이름을 가리킵니다. Ini.

### <a name="remarks"></a>설명

WM_DEVMODECHANGE 메시지를 처리하는 응용 프로그램은 장치 모드 설정을 다시 초기화할 수 있습니다. Windows `ExtDeviceMode` 함수를 사용하여 장치 설정을 저장하고 복원하는 응용 프로그램은 일반적으로 이 기능을 처리하지 않습니다.

사용자가 제어판에서 기본 프린터를 변경할 때 이 함수가 호출되지 않습니다. 이 경우 함수가 `OnWinIniChange` 호출됩니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndondrawclipboard"></a><a name="ondrawclipboard"></a>CWnd::온드로우 클립보드

프레임워크는 클립보드의 내용이 변경될 때 클립보드 뷰어 체인의 각 창에 대해 이 멤버 함수를 호출합니다.

```
afx_msg void OnDrawClipboard();
```

### <a name="remarks"></a>설명

[SetClipboardViewer](#setclipboardviewer) 멤버 함수를 호출하여 클립보드 뷰어 체인에 참여한 응용 프로그램만 이 호출에 응답해야 합니다.

호출을 `OnDrawClipboard` 받는 각 창은 [SendMessage](/windows/win32/api/winuser/nf-winuser-sendmessage) Windows 함수를 호출하여 [WM_DRAWCLIPBOARD](/windows/win32/dataxchg/wm-drawclipboard) 메시지를 클립보드 뷰어 체인의 다음 창에 전달해야 합니다. 다음 창의 핸들은 [SetClipboardViewer](#setclipboardviewer) 멤버 함수에 의해 반환됩니다. [OnChangeCbChain](#onchangecbchain) 멤버 함수 호출에 대한 응답으로 수정할 수 있습니다.

## <a name="cwndondrawiconicthumbnailorlivepreview"></a><a name="ondrawiconicthumbnailorlivepreview"></a>CWnd::에그리기상징적엄지네일OrLive미리보기

Windows 7 탭 미리 보기 또는 애플리케이션 피킹(Peeking)용 클라이언트에 표시할 비트맵을 가져와야 하는 경우 프레임워크에서 호출됩니다.

```
virtual void OnDrawIconicThumbnailOrLivePreview(
    CDC& dc,
    CRect rect,
    CSize szRequiredThumbnailSize,
    BOOL bIsThumbnail,
    BOOL& bAlphaChannelSet);
```

### <a name="parameters"></a>매개 변수

*Dc*<br/>
장치 컨텍스트를 지정합니다.

*rect*<br/>
렌더링할 영역의 경계 사각형을 지정합니다.

*szRequired엄지네일크기*<br/>
대상 축소판의 크기를 지정합니다. *bIsThumbnail이* FALSE인 경우 무시해야 합니다.

*비스썸네일*<br/>
이 메서드가 상징적인 축소판 또는 라이브 미리 보기(엿보기)에 대해 호출되는지 여부를 지정합니다.

*b알파채널세트*<br/>
【아웃】 구현이 *DC에서*선택한 비트맵의 알파 채널을 초기화하는 경우 TRUE로 설정합니다.

### <a name="remarks"></a>설명

파생 클래스에서 이 메서드를 재정의하고 지정된 장치 컨텍스트를 그려 서 축소판 그림 및 엿보기를 사용자 지정합니다. *bThumbnailTRUE* 인 경우 *szRequired엄지섬네일크기는* 무시할 수 있습니다. 이 경우 전체 크기의 비트맵(즉, 전체 클라이언트 영역을 포함하는 비트맵)을 그립니다. 장치 *컨텍스트(dc)에는*선택한 32비트 비트맵이 함께 제공됩니다. 기본 구현은 PRF_CLIENT, PRF_CHILDREN 및 PRF_NONCLIENT 플래그를 사용하여 이 창에 WM_PRINT 보냅니다.

## <a name="cwndondrawitem"></a><a name="ondrawitem"></a>CWnd::에 그리기 항목

프레임워크는 컨트롤 또는 메뉴의 시각적 측면이 변경될 때 소유자-그리기 단추 컨트롤, 콤보 상자 컨트롤, 목록 상자 컨트롤 또는 메뉴의 소유자에 대해 이 멤버 함수를 호출합니다.

```
afx_msg void OnDrawItem(
    int nIDCtl,
    LPDRAWITEMSTRUCT lpDrawItemStruct);
```

### <a name="parameters"></a>매개 변수

*니디크틀*<br/>
WM_DRAWITEM 메시지를 보낸 컨트롤의 식별자를 포함합니다. 메뉴가 메시지를 보낸 경우 *nIDCtl에는* 0이 포함됩니다.

*lpDrawItemStruct*<br/>
그릴 항목과 필요한 도면 유형에 대한 정보가 포함된 `DRAWITEMSTRUCT` 데이터 구조에 대한 긴 포인터를 지정합니다.

### <a name="remarks"></a>설명

`itemAction` [DRAWITEMSTRUCT](/windows/win32/api/winuser/ns-winuser-drawitemstruct) 구조의 멤버는 수행할 드로잉 작업을 정의합니다. 이 멤버의 데이터를 사용하면 컨트롤소유자가 필요한 그리기 작업을 결정할 수 있습니다.

이 메시지를 처리에서 반환하기 전에 응용 프로그램은 `DRAWITEMSTRUCT` 구조체의 *hDC* 멤버로 식별된 장치 컨텍스트가 기본 상태로 복원되도록 해야 합니다.

멤버가 `hwndItem` [CButton](../../mfc/reference/cbutton-class.md), [CMenu](../../mfc/reference/cmenu-class.md), [CListBox](../../mfc/reference/clistbox-class.md)또는 [CComboBox](../../mfc/reference/ccombobox-class.md) 개체에 속하는 경우 해당 클래스의 `DrawItem` 가상 함수가 호출됩니다. 항목을 그리려면 `DrawItem` 적절한 컨트롤 클래스의 멤버 함수를 재정의합니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndondropfiles"></a><a name="ondropfiles"></a>CWnd::온드롭 파일

프레임워크는 사용자가 삭제된 파일의 수신자로 자신을 등록한 창 위에 왼쪽 마우스 단추를 해제할 때 이 멤버 함수를 호출합니다.

```
afx_msg void OnDropFiles(HDROP hDropInfo);
```

### <a name="parameters"></a>매개 변수

*hDropInfo*<br/>
삭제된 파일을 설명하는 내부 데이터 구조에 대한 포인터입니다. 이 핸들은 `DragFinish`에서 `DragQueryFile` `DragQueryPoint` 삭제된 파일에 대한 정보를 검색하는 데 사용됩니다.

### <a name="remarks"></a>설명

일반적으로 파생 클래스는 삭제된 파일을 지원하도록 설계되며 창 생성 중에 자체적으로 등록됩니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonenable"></a><a name="onenable"></a>CWnd::에 사용 가능

프레임워크는 응용 프로그램이 개체의 사용 가능한 상태를 `CWnd` 변경할 때 이 멤버 함수를 호출합니다.

```
afx_msg void OnEnable(BOOL bEnable);
```

### <a name="parameters"></a>매개 변수

*bEnable*<br/>
개체가 `CWnd` 활성화되었는지 비활성화되었는지 여부를 지정합니다. 이 매개 변수는 `CWnd` TRUE가 활성화된 경우입니다. `CWnd` 비활성화된 경우 FALSE입니다.

### <a name="remarks"></a>설명

`OnEnable`[EnableWindow](#enablewindow) 멤버 함수가 반환되기 전에 호출되지만 창 사용 [상태(스타일 비트)가 변경된](styles-used-by-mfc.md#window-styles) 후WS_DISABLED

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonendsession"></a><a name="onendsession"></a>CWnd::온엔드세션

프레임워크는 `CWnd` [개체가 OnQueryEndEndSession](#onqueryendsession) 함수 함수 호출에서 비영값을 반환한 후 이 멤버 함수를 호출합니다.

```
afx_msg void OnEndSession(BOOL bEnding);
```

### <a name="parameters"></a>매개 변수

*구 부리*<br/>
세션이 종료되는지 여부를 지정합니다. 세션이 종료되는 경우 true입니다. 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

호출은 `OnEndSession` 세션이 `CWnd` 실제로 종료되는지 여부를 개체에 알립니다.

*bEndingTRUE인* 경우 모든 응용 프로그램이 이 호출을 처리에서 반환된 후 Windows는 언제든지 종료할 수 있습니다. 따라서 응용 프로그램이 내에서 `OnEndSession`종료에 필요한 모든 작업을 수행하도록 합니다.

세션이 종료될 때 [DestroyWindow](#destroywindow) 멤버 함수 또는 [PostQuitMessage](/windows/win32/api/winuser/nf-winuser-postquitmessage) Windows 함수를 호출할 필요가 없습니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonenteridle"></a><a name="onenteridle"></a>CWnd::온엔터유들

프레임워크는 이 멤버 함수를 호출하여 모달 대화 상자 또는 메뉴가 유휴 상태가 되고 있음을 응용 프로그램의 기본 창 프로시저에 알립니다.

```
afx_msg void OnEnterIdle(
    UINT nWhy,
    CWnd* pWho);
```

### <a name="parameters"></a>매개 변수

*n왜*<br/>
메시지가 대화 상자의 결과인지 아니면 표시되는 메뉴인지 지정합니다. 이 매개 변수는 다음 값 중 하나일 수 있습니다.

- MSGF_DIALOGBOX 대화 상자가 표시되어 시스템이 유휴 상태입니다.

- MSGF_MENU 메뉴가 표시되어 시스템이 유휴 상태입니다.

*p누구*<br/>
대화 *상자(nWhy가* MSGF_DIALOGBOX 경우) 또는 표시된 메뉴가 포함된 *창(nWhy가* MSGF_MENU 경우)에 대한 포인터를 지정합니다. 이 포인터는 일시적일 수 있으며 나중에 사용할 수 없습니다.

### <a name="remarks"></a>설명

모달 대화 상자 또는 메뉴는 하나 이상의 이전 메시지를 처리한 후 큐에서 대기 중인 메시지가 없을 때 유휴 상태가 됩니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonentermenuloop"></a><a name="onentermenuloop"></a>CWnd:::온엔터메뉴루프

프레임워크는 메뉴 모달 루프를 입력할 때 이 멤버 함수를 호출합니다.

```
afx_msg void OnEnterMenuLoop(BOOL bIsTrackPopupMenu);
```

### <a name="parameters"></a>매개 변수

*비스트랙팝메뉴*<br/>
관련된 메뉴가 팝업 메뉴인지 여부를 지정합니다. 함수가 성공하면 0이 아닌 값을 가집니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonentersizemove"></a><a name="onentersizemove"></a>CWnd::온엔터사이즈무브

프레임워크는 영향을 받는 창이 이동 또는 크기 조정 모달 루프에 들어간 후 이 멤버 함수를 한 번 호출합니다.

```
afx_msg void OnEnterSizeMove();
```

### <a name="remarks"></a>설명

이 메서드는 Windows SDK에 설명 된 [WM_ENTERSIZEMOVE](/windows/win32/winmsg/wm-entersizemove) 알림을 받습니다.

사용자가 창의 제목 표시줄 또는 크기 조정 테두리를 클릭하거나 창이 [WM_SYSCOMMAND](/windows/win32/menurc/wm-syscommand) 메시지를 [CWnd::DefWindowProc](#defwindowproc) 함수로 전달하고 해당 메시지의 *wParam* 매개 변수를 통해 SC_MOVE 또는 SC_SIZE 지정할 때 창이 이동 또는 크기 조정 모달 루프를 입력합니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonerasebkgnd"></a><a name="onerasebkgnd"></a>CWnd::온라세브브크nd

프레임워크는 `CWnd` 개체 배경을 지워야 할 때(예: 크기를 조정하는 경우) 이 멤버 함수를 호출합니다.

```
afx_msg BOOL OnEraseBkgnd(CDC* pDC);
```

### <a name="parameters"></a>매개 변수

*pDC*<br/>
장치 컨텍스트 개체를 지정합니다.

### <a name="return-value"></a>Return Value

배경을 지우면 영하지 않습니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

그림에 대해 무효화된 영역을 준비하기 위해 호출됩니다.

기본 구현은 window 클래스 구조의 멤버가 지정한 창 클래스 배경 브러시를 `hbrBackground` 사용하여 배경을 지웁니다.

멤버가 `hbrBackground` NULL이면 재정의된 버전의 `OnEraseBkgnd` 배경색이 지워집니다. 버전은 먼저 브러시에 대한 `CWnd` [UnrealizeObject를](/windows/win32/api/wingdi/nf-wingdi-unrealizeobject) 호출한 다음 브러시를 선택하여 의도한 브러시의 원점이 좌표와 정렬되어야 합니다.

재정의된 `OnEraseBkgnd` 메시지를 처리하고 배경을 지우는 경우 WM_ERASEBKGND 대한 응답으로 비영도를 반환해야 합니다. 이는 더 이상 지워질 필요가 없음을 나타냅니다. 0을 반환하면 창이 지워져야 하는 것으로 표시됩니다. 일반적으로 이는 `fErase` `PAINTSTRUCT` 구조의 구성원이 TRUE임을 의미합니다.

Windows는 배경이 MM_TEXT 매핑 모드로 계산된다고 가정합니다. 장치 컨텍스트가 다른 매핑 모드를 사용하는 경우 지워진 영역이 클라이언트 영역의 표시되는 부분에 없을 수 있습니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonexitmenuloop"></a><a name="onexitmenuloop"></a>CWnd:::에 엑시트 메뉴 루프

프레임워크는 메뉴 모달 루프가 종료될 때 이 멤버 함수를 호출합니다.

```
afx_msg void OnExitMenuLoop(BOOL bIsTrackPopupMenu);
```

### <a name="parameters"></a>매개 변수

*비스트랙팝메뉴*<br/>
관련된 메뉴가 팝업 메뉴인지 여부를 지정합니다. 함수가 성공하면 0이 아닌 값을 가집니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonexitsizemove"></a><a name="onexitsizemove"></a>CWnd::에 엑시트 사이즈 이동

프레임워크는 영향을 받는 창이 이동 또는 크기 조정 모달 루프를 종료한 후 이 멤버 함수를 한 번 호출합니다.

```
afx_msg void OnExitSizeMove();
```

### <a name="remarks"></a>설명

이 메서드는 Windows SDK에 설명 된 [WM_EXITSIZEMOVE](/windows/win32/winmsg/wm-exitsizemove) 알림을 받습니다.

사용자가 창의 제목 표시줄 또는 크기 조정 테두리를 클릭하거나 창이 [WM_SYSCOMMAND](/windows/win32/menurc/wm-syscommand) 메시지를 [CWnd::DefWindowProc](#defwindowproc) 함수로 전달하고 해당 메시지의 *wParam* 매개 변수를 통해 SC_MOVE 또는 SC_SIZE 지정할 때 창이 이동 또는 크기 조정 모달 루프를 입력합니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonfontchange"></a><a name="onfontchange"></a>CWnd::온폰트체인지

시스템의 모든 최상위 창은 응용 `OnFontChange` 프로그램이 글꼴 리소스 풀을 변경한 후 프레임워크에서 호출을 받습니다.

```
afx_msg void OnFontChange();
```

### <a name="remarks"></a>설명

시스템에서 글꼴을 추가하거나 제거하는 응용 프로그램(예: [AddFontResource](/windows/win32/api/wingdi/nf-wingdi-addfontresourcew) 또는 [RemoveFontResource](/windows/win32/api/wingdi/nf-wingdi-removefontresourcew) Windows 함수를 통해)은 [WM_FONTCHANGE](/windows/win32/gdi/wm-fontchange) 메시지를 모든 최상위 창에 보내야 합니다.

이 메시지를 보내려면 *HWND_BROADCAST hWnd* 매개 변수를 설정한 [SendMessage](/windows/win32/api/winuser/nf-winuser-sendmessage) Windows 함수를 사용합니다.

## <a name="cwndongetdlgcode"></a><a name="ongetdlgcode"></a>CWnd::OnGetDlgCode

컨트롤에서 화살표 키 및 Tab 키 입력 자체를 처리할 수 있도록 컨트롤에 대해 호출됩니다.

```
afx_msg UINT OnGetDlgCode();
```

### <a name="return-value"></a>Return Value

응용 프로그램 프로세스에 입력하는 유형의 다음 값 중 하나 이상은 다음과 같은 값입니다.

- DLGC_BUTTON 버튼(제네릭)입니다.

- 기본 푸시 버튼이 DLGC_DEFPUSHBUTTON.

- DLGC_HASSETSEL EM_SETSEL 메시지.

- DLGC_UNDEFPUSHBUTTON 기본 푸시 버튼 처리가 없습니다. 응용 프로그램은 DLGC_BUTTON 이 플래그를 사용하여 단추 입력을 처리하지만 기본 푸시 버튼 처리를 위해 시스템에 의존함을 나타낼 수 있습니다.

- DLGC_RADIOBUTTON 라디오 버튼.

- DLGC_STATIC 정적 제어.

- DLGC_WANTALLKEYS 모든 키보드 입력.

- DLGC_WANTARROWS 화살표 키입니다.

- DLGC_WANTCHARS WM_CHAR 메시지.

- 모든 키보드 입력을 DLGC_WANTMESSAGE. 응용 프로그램은 이 메시지를 컨트롤에 전달합니다.

- 탭 키를 DLGC_WANTTAB.

### <a name="remarks"></a>설명

일반적으로 Windows는 컨트롤에 대한 모든 화살표 키 `CWnd` 및 TAB 키 입력을 처리합니다. 컨트롤을 `CWnd` `OnGetDlgCode`재정의하여 특정 유형의 입력을 선택하여 자체를 처리할 수 있습니다.

미리 `OnGetDlgCode` 정의된 컨트롤 클래스의 기본 함수는 각 클래스에 적합한 코드를 반환합니다.

## <a name="cwndongetminmaxinfo"></a><a name="ongetminmaxinfo"></a>CWnd::온겟민맥스정보

프레임워크는 Windows에서 최대화된 위치 또는 차원 또는 최소 또는 최대 추적 크기를 알아야 할 때마다 이 멤버 함수를 호출합니다.

```
afx_msg void OnGetMinMaxInfo(MINMAXINFO* lpMMI);
```

### <a name="parameters"></a>매개 변수

*lpMMI*<br/>
창의 `MINMAXINFO` 최대화된 크기 및 위치및 최소 및 최대 추적 크기에 대한 정보가 포함된 구조를 가리킵니다. 이 구조에 대한 자세한 내용은 [MINMAXINFO](/windows/win32/api/winuser/ns-winuser-minmaxinfo) 구조를 참조하십시오.

### <a name="remarks"></a>설명

최대화된 크기는 테두리가 완전히 확장될 때 창의 크기입니다. 창의 최대 추적 크기는 테두리를 사용하여 창 크기를 조정하여 얻을 수 있는 가장 큰 창 크기입니다. 창의 최소 추적 크기는 테두리를 사용하여 창 크기를 조정하여 얻을 수 있는 가장 작은 창 크기입니다.

Windows는 다양한 위치 및 치수에 대한 기본값을 지정하는 점 배열을 채웁니다. 응용 프로그램은 에서 `OnGetMinMaxInfo`이러한 값을 변경할 수 있습니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonhelp"></a><a name="onhelp"></a>CWnd::에 도움

현재 컨텍스트를 사용하여 애플리케이션 내에서 F1 도움말을 처리합니다.

```
afx_msg void OnHelp();
```

### <a name="remarks"></a>설명

자세한 내용은 [CWinApp::OnHelp를](../../mfc/reference/cwinapp-class.md#onhelp) 참조하십시오.

## <a name="cwndonhelpfinder"></a><a name="onhelpfinder"></a>CWnd::온헬프 파인더

ID_HELP_FINDER 및 ID_DEFAULT_HELP 명령을 처리합니다.

```
afx_msg void OnHelpFinder();
```

### <a name="remarks"></a>설명

자세한 내용은 [CWinApp::OnHelpFinder를](../../mfc/reference/cwinapp-class.md#onhelpfinder) 참조하십시오.

## <a name="cwndonhelpindex"></a><a name="onhelpindex"></a>CWnd::OnHelpIndex

ID_HELP_INDEX 명령을 처리하고 기본 도움말 항목을 제공합니다.

```
afx_msg void OnHelpIndex();
```

### <a name="remarks"></a>설명

자세한 내용은 [CWinApp::OnHelpIndex를](../../mfc/reference/cwinapp-class.md#onhelpindex) 참조하십시오.

## <a name="cwndonhelpinfo"></a><a name="onhelpinfo"></a>CWnd:::온헬프정보

사용자가 F1 키를 누를 때 프레임워크에서 호출됩니다.

```
afx_msg BOOL OnHelpInfo(HELPINFO* lpHelpInfo);
```

### <a name="parameters"></a>매개 변수

*lpHelpInfo*<br/>
도움말이 요청되는 메뉴 항목, 제어, 대화 상자 또는 창에 대한 정보가 포함된 [HELPINFO](/windows/win32/api/winuser/ns-winuser-helpinfo) 구조에 대한 포인터입니다.

### <a name="return-value"></a>Return Value

창에 키보드 포커스가 있거나 창 내에서 메뉴가 활성화되어 있는 경우 TRUE를 반환합니다. 키보드 포커스가 없는 창이 있으면 FALSE를 반환합니다.

### <a name="remarks"></a>설명

F1을 누를 때 메뉴가 활성화되면 메뉴와 연결된 WM_HELP 창으로 전송됩니다. 그렇지 않으면 WM_HELP 키보드 포커스가 있는 창으로 전송됩니다. 키보드 포커스가 없는 창이 없는 경우 WM_HELP 현재 활성 창으로 전송됩니다.

## <a name="cwndonhelpusing"></a><a name="onhelpusing"></a>CWnd::에 도움 사용

ID_HELP_USING 명령을 처리합니다.

```
afx_msg void OnHelpUsing();
```

### <a name="remarks"></a>설명

자세한 내용은 [CWinApp::OnHelp사용을](../../mfc/reference/cwinapp-class.md#onhelpusing) 참조하십시오.

## <a name="cwndonhotkey"></a><a name="onhotkey"></a>CWnd::OnHotKey

프레임워크는 사용자가 시스템 전체 핫 키를 누를 때 이 멤버 함수를 호출합니다.

```
afx_msg void OnHotKey(
    UINT nHotKeyId,
    UINT nKey1,
    UINT nKey2);
```

### <a name="parameters"></a>매개 변수

|매개 변수|Description|
|---------------|-----------------|
|*nHotKeyId*|【인】 메시지를 생성한 바로 키에 대한 식별자입니다. 시스템 정의 단축키로 메시지가 생성된 경우 이 매개 변수는 다음 값 중 하나가 됩니다.<br /><br /> - IDHOT_SNAPDESKTOP - 스냅 데스크톱 핫 키를 눌렀습니다.<br />- IDHOT_SNAPWINDOW - 스냅 창 단축 키를 눌렀습니다.|
|*nKey1*|【인】 *nKey2* 매개 변수에 의해 지정된 키와 함께 누른 키를 나타내는 플래그의 비트 조합(OR)입니다. 사용 가능한 값은<br /><br /> - MOD_ALT - ALT 키가 모두 유지되었습니다.<br />- MOD_CONTROL - CTRL 키가 모두 유지되었습니다.<br />- MOD_SHIFT - SHIFT 키가 모두 유지되었습니다.<br />- MOD_WIN - WINDOWS 키가 모두 유지되었습니다. 이러한 키는 Microsoft Windows 로고로 레이블이 지정되어 있습니다.|
|*nKey2*|【인】 단축키의 가상 키 코드입니다.|

### <a name="remarks"></a>설명

이 메서드는 Windows SDK에 설명 된 [WM_HOTKEY](/windows/win32/inputdev/wm-hotkey) 알림을 받습니다. 이 메시지는 핫 키를 등록한 스레드와 연결된 메시지 큐의 맨 위에 배치됩니다. [RegisterHotKey](/windows/win32/api/winuser/nf-winuser-registerhotkey) 기능을 사용하여 시스템 전체 단축키를 등록합니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonhscroll"></a><a name="onhscroll"></a>CWnd::온스크롤

사용자가 창의 가로 스크롤 막대를 클릭할 때 프레임워크는 이 멤버 함수를 호출합니다.

```
afx_msg void OnHScroll(
    UINT nSBCode,
    UINT nPos,
    CScrollBar* pScrollBar);
```

### <a name="parameters"></a>매개 변수

*nSBCode*<br/>
사용자의 스크롤 요청을 나타내는 스크롤 바로 표시 줄 코드를 지정합니다. 이 매개 변수는 다음 중 하나일 수 있습니다.

- SB_LEFT 왼쪽으로 스크롤합니다.

- SB_ENDSCROLL 끝 스크롤입니다.

- SB_LINELEFT 스크롤 왼쪽.

- 오른쪽으로 스크롤할 SB_LINERIGHT 있습니다.

- SB_PAGELEFT 한 페이지를 왼쪽으로 스크롤합니다.

- SB_PAGERIGHT 한 페이지를 오른쪽으로 스크롤합니다.

- SB_RIGHT 맨 오른쪽으로 스크롤합니다.

- SB_THUMBPOSITION 절대 위치로 스크롤합니다. 현재 위치는 *nPos* 매개 변수에 의해 지정됩니다.

- SB_THUMBTRACK 스크롤 상자를 지정된 위치로 드래그합니다. 현재 위치는 *nPos* 매개 변수에 의해 지정됩니다.

*Npos*<br/>
스크롤 바발 코드가 SB_THUMBPOSITION 또는 SB_THUMBTRACK 스크롤 상자 위치를 지정합니다. 그렇지 않으면 사용되지 않습니다. 초기 스크롤 범위에 따라 *nPos는* 음수일 수 있으며 필요한 경우 **int로** 캐스팅해야 합니다.

*p스크롤바*<br/>
스크롤 메시지가 스크롤 막대 컨트롤에서 온 경우 컨트롤에 대한 포인터가 포함되어 있습니다. 사용자가 창의 스크롤 막대를 클릭한 경우 이 매개 변수는 NULL입니다. 해당 포인터는 임시적이며, 나중에 사용하려고 저장하면 안됩니다.

### <a name="remarks"></a>설명

SB_THUMBTRACK 스크롤 바코드는 일반적으로 스크롤 상자를 드래그하는 동안 피드백을 제공하는 응용 프로그램에서 사용됩니다.

응용 프로그램이 스크롤 막대에 의해 제어되는 내용을 스크롤하는 경우 [SetScrollPos](#setscrollpos) 멤버 함수를 사용 하 여 스크롤 상자의 위치도 재설정 해야 합니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#108](../../mfc/reference/codesnippet/cpp/cwnd-class_48.cpp)]

## <a name="cwndonhscrollclipboard"></a><a name="onhscrollclipboard"></a>CWnd::OnH스크롤 클립보드

클립보드 데이터의 `OnHScrollClipboard` `CF_OWNERDISPLAY` 형식이 있고 클립보드 뷰어의 가로 스크롤 막대에 이벤트가 있을 때 클립보드 소유자의 멤버 함수가 클립보드 뷰어에서 호출됩니다.

```
afx_msg void OnHScrollClipboard(
    CWnd* pClipAppWnd,
    UINT nSBCode,
    UINT nPos);
```

### <a name="parameters"></a>매개 변수

*pClipAppWnd*<br/>
클립보드 뷰어 창에 대한 포인터를 지정합니다. 해당 포인터는 임시적이며, 나중에 사용하려고 저장하면 안됩니다.

*nSBCode*<br/>
낮은 차수 단어에서 다음 스크롤 바코드 중 하나를 지정합니다.

- SB_BOTTOM 스크롤하여 오른쪽으로 스크롤합니다.

- SB_ENDSCROLL 끝 스크롤입니다.

- SB_LINEDOWN 한 줄 아래로 스크롤합니다.

- SB_LINEUP 한 줄을 스크롤합니다.

- SB_PAGEDOWN 한 페이지 아래로 스크롤합니다.

- SB_PAGEUP 한 페이지를 위로 스크롤합니다.

- SB_THUMBPOSITION 절대 위치로 스크롤합니다. 현재 위치는 *nPos에*제공됩니다.

- SB_TOP 왼쪽 상단으로 스크롤합니다.

*Npos*<br/>
스크롤 바코드SB_THUMBPOSITION 스크롤 바코드인 경우 스크롤 상자 위치를 포함합니다. 그렇지 않으면 사용되지 않습니다.

### <a name="remarks"></a>설명

소유자는 클립보드 이미지를 스크롤하고, 적절한 섹션을 무효화하고, 스크롤 막대 값을 업데이트해야 합니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndoniconerasebkgnd"></a><a name="oniconerasebkgnd"></a>CWnd::OniconEraseBkgnd

아이콘을 페인팅하기 전에 아이콘의 배경을 `CWnd` 채워야 할 때 프레임워크는 최소화된(상징적) 개체에 대해 이 멤버 함수를 호출합니다.

```
afx_msg void OnIconEraseBkgnd(CDC* pDC);
```

### <a name="parameters"></a>매개 변수

*pDC*<br/>
아이콘의 장치 컨텍스트 개체를 지정합니다. 일시적일 수 있으며 나중에 사용할 수 있습니다.

### <a name="remarks"></a>설명

`CWnd`클래스 아이콘이 창 기본 구현에 대해 정의된 경우에만 이 호출을 수신합니다. 그렇지 않으면 [OnEraseBkgnd가](#onerasebkgnd) 호출됩니다.

[DefWindowProc](#defwindowproc) 멤버 함수는 상위 창의 배경 브러시로 아이콘 배경을 채웁니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndoninitmenu"></a><a name="oninitmenu"></a>CWnd:::온니니트 메뉴

메뉴가 활성화될 때 프레임워크는 이 멤버 함수를 호출합니다.

```
afx_msg void OnInitMenu(CMenu* pMenu);
```

### <a name="parameters"></a>매개 변수

*p메뉴*<br/>
초기화할 메뉴를 지정합니다. 일시적일 수 있으며 나중에 사용할 수 있습니다.

### <a name="remarks"></a>설명

`OnInitMenu`사용자가 메뉴 모음에서 항목을 클릭하거나 메뉴 키를 누를 때 호출됩니다. 이 멤버 함수를 재정의하여 메뉴가 표시되기 전에 수정합니다.

`OnInitMenu`메뉴에 처음 액세스할 때(예: 사용자가 메뉴 모음에서 항목을 클릭하는 경우) 한 번만 호출됩니다. 이 메서드는 메뉴 항목에 대 한 정보를 제공 하지 않습니다. 사용자가 메뉴 내의 항목으로 이동하면(예: 여러 메뉴 항목에서 마우스를 이동하여) 함수가 다시 호출되지 않습니다. 사용자가 메뉴에서 종료(예: 응용 프로그램 클라이언트 영역을 클릭하여) 나중에 메뉴 모음에서 항목을 클릭하면 함수가 다시 호출됩니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndoninitmenupopup"></a><a name="oninitmenupopup"></a>CWnd:::OninitMenuPopup

프레임워크는 팝업 메뉴가 활성화될 때 이 멤버 함수를 호출합니다.

```
afx_msg void OnInitMenuPopup(
    CMenu* pPopupMenu,
    UINT nIndex,
    BOOL bSysMenu);
```

### <a name="parameters"></a>매개 변수

*강아지 메뉴*<br/>
팝업 메뉴의 메뉴 개체를 지정합니다. 일시적일 수 있으며 나중에 사용할 수 있습니다.

*nIndex*<br/>
주 메뉴에서 팝업 메뉴의 인덱스를 지정합니다.

*bSysMenu*<br/>
팝업 메뉴가 컨트롤 메뉴인 경우 TRUE입니다. 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

이렇게 하면 응용 프로그램이 전체 메뉴를 변경하지 않고 팝업 메뉴를 표시하기 전에 수정할 수 있습니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndoninputdevicechange"></a><a name="oninputdevicechange"></a>CWnd::온입력장치 변경

프레임워크는 I/O 장치가 시스템에서 추가되거나 제거될 때 이 멤버 함수를 호출합니다.

```
afx_msg void OnInputDeviceChange(unsigned short uFlag);
```

### <a name="parameters"></a>매개 변수

|매개 변수|Description|
|---------------|-----------------|
|*uFlag*|【인】 이 플래그에는 다음 값이 포함될 수 있습니다.<br /><br /> - GIDC_ARRIVAL - 새 장치가 시스템에 추가되었습니다.<br />- GIDC_REMOVAL - 장치가 시스템에서 제거되었습니다.|

### <a name="remarks"></a>설명

이 메서드는 Windows SDK에 설명 된 [WM_INPUT_DEVICE_CHANGE](/windows/win32/inputdev/wm-input-device-change) 알림을 받습니다. 는 일반 입력 장치 메시지입니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndoninputlangchange"></a><a name="oninputlangchange"></a>CWnd::온입력랭체인지

프레임워크는 응용 프로그램의 입력 언어가 변경된 후 영향을 받는 최상위 창에 대해 이 멤버를 호출합니다.

```
afx_msg void OnInputLangChange(
    UINT nCharSet,
    UINT nLocaleId);
```

### <a name="parameters"></a>매개 변수

|매개 변수|Description|
|---------------|-----------------|
|*nCharSet*|【인】 새 로캘의 문자 집합입니다. 자세한 내용은 [LOGFONT](/windows/win32/api/wingdi/ns-wingdi-logfontw) 구조의 *lfCharSet* 매개 변수를 참조하십시오.|
|*n 로컬리드*|【인】 입력 로캘 식별자입니다. 자세한 내용은 [언어 식별자 상수 및 문자열을](/windows/win32/Intl/language-identifier-constants-and-strings)참조하십시오.|

### <a name="remarks"></a>설명

이 메서드는 Windows SDK에 설명 된 [WM_INPUTLANGCHANGE](/windows/win32/winmsg/wm-inputlangchange) 알림 메시지를 받습니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndoninputlangchangerequest"></a><a name="oninputlangchangerequest"></a>CWnd::온입력랭변경요청

프레임워크는 사용자가 새 입력 언어를 선택할 때 포커스가 있는 창에 대해 이 멤버를 호출합니다.

```
afx_msg void OnInputLangChangeRequest(
    UINT nFlags,
    UINT nLocaleId);
```

### <a name="parameters"></a>매개 변수

|매개 변수|Description|
|---------------|-----------------|
|*nFlags*|【인】 새 로캘을 나타내는 비트(OR) 플래그 조합은 설치된 로캘 목록에서 이전 또는 다음 로캘에서 선택되었거나 새 입력 로캘의 키보드 레이아웃을 시스템 문자 집합과 함께 사용할 수 있음입니다.<br /><br /> 가능한 값은 INPUTLANGCHANGE_BACKWARD, INPUTLANGCHANGE_FORWARD 및 INPUTLANGCHANGE_SYSCHARSET.|
|*n 로컬리드*|【인】 입력 로캘 식별자입니다. 자세한 내용은 [언어 식별자 상수 및 문자열을](/windows/win32/Intl/language-identifier-constants-and-strings)참조하십시오.|

### <a name="remarks"></a>설명

이 메서드는 Windows SDK에 설명 된 [WM_INPUTLANGCHANGEREQUEST](/windows/win32/winmsg/wm-inputlangchangerequest) 알림 메시지를 받습니다. 이 메시지는 사용자가 키보드 제어판 응용 프로그램에 지정된 단축키또는 시스템 작업 표시줄의 표시기에서 새 입력 언어를 선택할 때 게시됩니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonkeydown"></a><a name="onkeydown"></a>CWnd::온키다운

프레임워크는 비시스템 키를 누를 때 이 멤버 함수를 호출합니다.

```
afx_msg void OnKeyDown(
    UINT nChar,
    UINT nRepCnt,
    UINT nFlags);
```

### <a name="parameters"></a>매개 변수

*Nchar*<br/>
지정된 키의 가상 키 코드를 지정합니다. 표준 가상 키 코드 목록은 Winuser.h를 참조하십시오.

*nRepCnt*<br/>
반복 횟수(사용자가 키를 누르고 있는 결과로 키 입력이 반복되는 횟수).

*nFlags*<br/>
다음 목록과 같이 검색 코드, 키 전환 코드, 이전 키 상태 및 컨텍스트 코드를 지정합니다.

|값|Description|
|-----------|-----------------|
|0-7|스캔 코드(OEM 종속 값).|
|8|함수 키 또는 숫자 키패드의 키와 같은 확장 키(확장 키인 경우 1).|
|9-10|사용되지 않습니다.|
|11-12|Windows에서 내부적으로 사용됩니다.|
|13|컨텍스트 코드(키를 누른 상태에서 ALT 키가 누수된 경우 1, 그렇지 않으면 0).|
|14|이전 키 상태(1키가 호출 되기 전에 다운된 경우, 키가 위태인 경우 0).|
|15|전환 상태(키가 해제되는 경우 1, 키를 누르는 경우 0).|

WM_KEYDOWN 메시지의 경우 키 전환 비트(비트 15)는 0이고 컨텍스트 코드 비트(비트 13)는 0입니다.

### <a name="remarks"></a>설명

비시스템 키는 ALT 키를 누르지 않을 때 누르거나 입력 포커스가 있을 `CWnd` 때 누르는 키보드 키입니다.

자동 반복으로 인해 `OnKeyDown` [OnKeyUp](#onkeyup) 멤버 함수 호출이 수행되기 전에 두 개 이상의 호출이 발생할 수 있습니다. 이전 키 상태를 나타내는 비트를 사용하여 호출이 `OnKeyDown` 첫 번째 다운 전환인지 반복다운 전환인지 여부를 확인할 수 있습니다.

IBM 향상된 101키 및 102키 키보드의 경우 향상된 키는 올바른 ALT이며 키보드의 기본 섹션에 있는 올바른 CTRL 키입니다. 숫자 키패드의 왼쪽에 있는 클러스터의 INS, DEL, HOME, END, 페이지 업, 페이지 다운 및 화살표 키; 및 숫자 키패드의 슬래시(/) 및 ENTER 키입니다. 일부 다른 키보드는 *nFlags에서*확장 키 비트를 지원할 수 있습니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonkeyup"></a><a name="onkeyup"></a>CWnd::OnKeyUp

프레임워크는 비시스템 키가 해제될 때 이 멤버 함수를 호출합니다.

```
afx_msg void OnKeyUp(
    UINT nChar,
    UINT nRepCnt,
    UINT nFlags);
```

### <a name="parameters"></a>매개 변수

*Nchar*<br/>
지정된 키의 가상 키 코드를 지정합니다. 표준 가상 키 코드 목록은 Winuser.h를 참조하십시오.

*nRepCnt*<br/>
반복 횟수(사용자가 키를 누르고 있는 결과로 키 입력이 반복되는 횟수).

*nFlags*<br/>
다음 목록과 같이 검색 코드, 키 전환 코드, 이전 키 상태 및 컨텍스트 코드를 지정합니다.

|값|Description|
|-----------|-----------------|
|0-7|스캔 코드(OEM 종속 값). 높은 순서 단어의 낮은 바이트입니다.|
|8|함수 키 또는 숫자 키패드의 키와 같은 확장 키(확장 키인 경우 1, 그렇지 않으면 0).|
|9-10|사용되지 않습니다.|
|11-12|Windows에서 내부적으로 사용됩니다.|
|13|컨텍스트 코드(키를 누른 상태에서 ALT 키가 누수된 경우 1, 그렇지 않으면 0).|
|14|이전 키 상태(1키가 호출 되기 전에 다운된 경우, 키가 위태인 경우 0).|
|15|전환 상태(키가 해제되는 경우 1, 키를 누르는 경우 0).|

WM_KEYUP 메시지의 경우 키 전환 비트(비트 15)는 1이고 컨텍스트 코드 비트(비트 13)는 0입니다.

### <a name="remarks"></a>설명

비시스템 키는 ALT 키를 누르지 않을 때 누르는 키보드 키 또는 입력 `CWnd` 포커스가 있을 때 누른 키보드 키입니다.

IBM 향상된 101키 및 102키 키보드의 경우 향상된 키는 올바른 ALT이며 키보드의 기본 섹션에 있는 올바른 CTRL 키입니다. 숫자 키패드의 왼쪽에 있는 클러스터의 INS, DEL, HOME, END, 페이지 업, 페이지 다운 및 화살표 키; 및 숫자 키패드의 슬래시(/) 및 ENTER 키입니다. 일부 다른 키보드는 *nFlags에서*확장 키 비트를 지원할 수 있습니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonkillfocus"></a><a name="onkillfocus"></a>CWnd:::온킬 포커스

프레임워크는 입력 포커스를 잃기 직전에 이 멤버 함수를 호출합니다.

```
afx_msg void OnKillFocus(CWnd* pNewWnd);
```

### <a name="parameters"></a>매개 변수

*pNewWnd*<br/>
입력 포커스를 수신하는 창에 대한 포인터를 지정합니다(NULL이거나 일시적일 수 있음).

### <a name="remarks"></a>설명

개체가 `CWnd` 캐리트 표시 되는 경우, 이 시점에서 카 를 파괴 한다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonlbuttondblclk"></a><a name="onlbuttondblclk"></a>CWnd::OnLButtonDblk

사용자가 왼쪽 마우스 단추를 두 번 클릭할 때 프레임워크는 이 멤버 함수를 호출합니다.

```
afx_msg void OnLButtonDblClk(
    UINT nFlags,
    CPoint point);
```

### <a name="parameters"></a>매개 변수

*nFlags*<br/>
다양한 가상 키가 다운되었는지 여부를 나타냅니다. 이 매개 변수는 다음 값의 조합일 수 있습니다.

- MK_CONTROL CTRL 키가 다운된 경우 설정합니다.

- MK_LBUTTON 왼쪽 마우스 버튼이 다운된 경우 설정합니다.

- MK_MBUTTON 가운데 마우스 버튼이 다운된 경우 설정합니다.

- 오른쪽 마우스 버튼이 다운되면 MK_RBUTTON 설정합니다.

- shift 키가 다운된 경우 MK_SHIFT 설정합니다.

*지점*<br/>
커서의 x- 및 y 좌표를 지정합니다. 이러한 좌표는 항상 창의 왼쪽 위 모서리를 기준으로 합니다.

### <a name="remarks"></a>설명

[CS_DBLCLKS WNDCLASS](/windows/win32/api/winuser/ns-winuser-wndclassw) 스타일이 있는 창에만 전화가 걸려옵니다. `OnLButtonDblClk` 이것은 마이크로소프트 파운데이션 클래스 창에 대 한 기본값입니다. 사용자가 `OnLButtonDblClk` 시스템의 두 번 클릭 시간 제한 내에서 왼쪽 마우스 버튼을 다시 누르고 놓으면 Windows가 호출됩니다. 왼쪽 마우스 단추를 두 번 클릭하면 [실제로 WM_LBUTTONDOWN,](#onlbuttondown) [WM_LBUTTONUP](#onlbuttonup) 메시지, WM_LBUTTONDBLCLK 호출 및 단추를 해제할 때 다른 WM_LBUTTONUP 메시지의 네 가지 이벤트가 생성됩니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonlbuttondown"></a><a name="onlbuttondown"></a>CWnd::OnLButtonDown

사용자가 왼쪽 마우스 단추를 누를 때 프레임워크는 이 멤버 함수를 호출합니다.

```
afx_msg void OnLButtonDown(
    UINT nFlags,
    CPoint point);
```

### <a name="parameters"></a>매개 변수

*nFlags*<br/>
다양한 가상 키가 다운되었는지 여부를 나타냅니다. 이 매개 변수는 다음 값의 조합일 수 있습니다.

- MK_CONTROL CTRL 키가 다운된 경우 설정합니다.

- MK_LBUTTON 왼쪽 마우스 버튼이 다운된 경우 설정합니다.

- MK_MBUTTON 가운데 마우스 버튼이 다운된 경우 설정합니다.

- 오른쪽 마우스 버튼이 다운되면 MK_RBUTTON 설정합니다.

- shift 키가 다운된 경우 MK_SHIFT 설정합니다.

*지점*<br/>
커서의 x- 및 y 좌표를 지정합니다. 이러한 좌표는 항상 창의 왼쪽 위 모서리를 기준으로 합니다.

### <a name="remarks"></a>설명

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonlbuttonup"></a><a name="onlbuttonup"></a>CWnd::OnLButtonUp

프레임워크는 사용자가 왼쪽 마우스 단추를 해제할 때 이 멤버 함수를 호출합니다.

```
afx_msg void OnLButtonUp(
    UINT nFlags,
    CPoint point);
```

### <a name="parameters"></a>매개 변수

*nFlags*<br/>
다양한 가상 키가 다운되었는지 여부를 나타냅니다. 이 매개 변수는 다음 값의 조합일 수 있습니다.

- MK_CONTROL CTRL 키가 다운된 경우 설정합니다.

- MK_MBUTTON 가운데 마우스 버튼이 다운된 경우 설정합니다.

- 오른쪽 마우스 버튼이 다운되면 MK_RBUTTON 설정합니다.

- shift 키가 다운된 경우 MK_SHIFT 설정합니다.

*지점*<br/>
커서의 x- 및 y 좌표를 지정합니다. 이러한 좌표는 항상 창의 왼쪽 위 모서리를 기준으로 합니다.

### <a name="remarks"></a>설명

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonmbuttondblclk"></a><a name="onmbuttondblclk"></a>CWnd::OnMButtonDblk

프레임워크는 사용자가 가운데 마우스 단추를 두 번 클릭할 때 이 멤버 함수를 호출합니다.

```
afx_msg void OnMButtonDblClk(
    UINT nFlags,
    CPoint point);
```

### <a name="parameters"></a>매개 변수

*nFlags*<br/>
다양한 가상 키가 다운되었는지 여부를 나타냅니다. 이 매개 변수는 다음 값의 조합일 수 있습니다.

- MK_CONTROL CTRL 키가 다운된 경우 설정합니다.

- MK_LBUTTON 왼쪽 마우스 버튼이 다운된 경우 설정합니다.

- MK_MBUTTON 가운데 마우스 버튼이 다운된 경우 설정합니다.

- 오른쪽 마우스 버튼이 다운되면 MK_RBUTTON 설정합니다.

- shift 키가 다운된 경우 MK_SHIFT 설정합니다.

*지점*<br/>
커서의 x- 및 y 좌표를 지정합니다. 이러한 좌표는 항상 창의 왼쪽 위 모서리를 기준으로 합니다.

### <a name="remarks"></a>설명

[CS_DBLCLKS WNDCLASS](/windows/win32/api/winuser/ns-winuser-wndclassw) 스타일이 있는 창에만 전화가 걸려옵니다. `OnMButtonDblClk` 모든 Microsoft 파운데이션 클래스 창에 대한 기본값입니다. Windows는 사용자가 `OnMButtonDblClk` 시스템의 두 번 클릭 시간 제한 내에서 가운데 마우스 단추를 다시 누르고 해제할 때 호출을 생성합니다. 가운데 마우스 단추를 두 번 클릭하면 [실제로 WM_MBUTTONDOWN](#onmbuttondown) 및 [WM_MBUTTONUP](#onmbuttonup) 메시지, WM_MBUTTONDBLCLK 호출 및 다른 WM_MBUTTONUP 메시지의 네 가지 이벤트가 생성됩니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonmbuttondown"></a><a name="onmbuttondown"></a>CWnd::온엠버튼다운

프레임워크는 사용자가 가운데 마우스 단추를 누를 때 이 멤버 함수를 호출합니다.

```
afx_msg void OnMButtonDown(
    UINT nFlags,
    CPoint point);
```

### <a name="parameters"></a>매개 변수

*nFlags*<br/>
다양한 가상 키가 다운되었는지 여부를 나타냅니다. 이 매개 변수는 다음 값의 조합일 수 있습니다.

- MK_CONTROL CTRL 키가 다운된 경우 설정합니다.

- MK_LBUTTON 왼쪽 마우스 버튼이 다운된 경우 설정합니다.

- MK_MBUTTON 가운데 마우스 버튼이 다운된 경우 설정합니다.

- 오른쪽 마우스 버튼이 다운되면 MK_RBUTTON 설정합니다.

- shift 키가 다운된 경우 MK_SHIFT 설정합니다.

*지점*<br/>
커서의 x- 및 y 좌표를 지정합니다. 이러한 좌표는 항상 창의 왼쪽 위 모서리를 기준으로 합니다.

### <a name="remarks"></a>설명

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonmbuttonup"></a><a name="onmbuttonup"></a>CWnd::OnMButtonUp

프레임워크는 사용자가 중간 마우스 단추를 해제할 때 이 멤버 함수를 호출합니다.

```
afx_msg void OnMButtonUp(
    UINT nFlags,
    CPoint point);
```

### <a name="parameters"></a>매개 변수

*nFlags*<br/>
다양한 가상 키가 다운되었는지 여부를 나타냅니다. 이 매개 변수는 다음 값의 조합일 수 있습니다.

- MK_CONTROL CTRL 키가 다운된 경우 설정합니다.

- MK_LBUTTON 왼쪽 마우스 버튼이 다운된 경우 설정합니다.

- 오른쪽 마우스 버튼이 다운되면 MK_RBUTTON 설정합니다.

- shift 키가 다운된 경우 MK_SHIFT 설정합니다.

*지점*<br/>
커서의 x- 및 y 좌표를 지정합니다. 이러한 좌표는 항상 창의 왼쪽 위 모서리를 기준으로 합니다.

### <a name="remarks"></a>설명

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonmdiactivate"></a><a name="onmdiactivate"></a>CWnd::OnMDIActivate

프레임워크는 비활성화되고 자식 창이 활성화되는 자식 창에 대해 이 멤버 함수를 호출합니다.

```
afx_msg void OnMDIActivate(
    BOOL bActivate,
    CWnd* pActivateWnd,
    CWnd* pDeactivateWnd);
```

### <a name="parameters"></a>매개 변수

*bActivate*<br/>
true 는 자녀가 활성화되고 있는 경우이고 비활성화되는 경우 FALSE입니다.

*pActivateWnd*<br/>
활성화할 MDI 자식 창에 대한 포인터가 포함되어 있습니다. MDI 자식 창에서 수신하면 *pActivateWnd에는* 활성화중인 자식 창에 대한 포인터가 포함되어 있습니다. 이 포인터는 일시적일 수 있으며 나중에 사용할 수 없습니다.

*pDeactivateWnd*<br/>
비활성화되는 MDI 자식 창에 대한 포인터를 포함합니다. 이 포인터는 일시적일 수 있으며 나중에 사용할 수 없습니다.

### <a name="remarks"></a>설명

MDI 자식 창은 MDI 프레임 창과 독립적으로 활성화됩니다. 프레임이 활성화되면 `OnMDIActivate` 호출로 마지막으로 활성화된 자식 창은 활성 창 프레임 및 캡션 막대를 그리는 `OnMDIActivate` [WM_NCACTIVATE](#onncactivate) 메시지를 수신하지만 다른 호출은 받지 않습니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonmeasureitem"></a><a name="onmeasureitem"></a>CWnd:::온측정 항목

프레임워크는 컨트롤을 만들 때 소유자-그리기 단추, 콤보 상자, 목록 상자 또는 메뉴 항목의 소유자에 대 한 프레임 워크에 의해이 멤버 함수를 호출 합니다.

```
afx_msg void OnMeasureItem(
    int nIDCtl, LPMEASUREITEMSTRUCT lpMeasureItemStruct);
```

### <a name="parameters"></a>매개 변수

*니디크틀*<br/>
컨트롤의 ID입니다.

*lpMeasureItemStruct*<br/>
소유자-그리기 컨트롤의 차원을 포함 하는 [MEASUREITEMStruCT](/windows/win32/api/winuser/ns-winuser-measureitemstruct) 데이터 구조를 가리킵니다.

### <a name="remarks"></a>설명

이 멤버 함수를 재정의하고 `MEASUREITEMSTRUCT` *lpMeasureItemStruct및* 반환에 의해 가리키는 데이터 구조를 채웁니다. 이렇게 하면 Windows에 컨트롤 의 차원을 알리고 Windows에서 컨트롤과의 사용자 상호 작용을 올바르게 처리할 수 있습니다.

목록 상자 또는 콤보 상자가 [LBS_OWNERDRAWVARIABLE](../../mfc/reference/styles-used-by-mfc.md#list-box-styles) 또는 [CBS_OWNERDRAWVARIABLE](../../mfc/reference/styles-used-by-mfc.md#combo-box-styles) 스타일로 만들어지면 프레임워크는 컨트롤의 각 항목에 대해 이 함수를 호출합니다. 그렇지 않으면 이 함수를 한 번 호출합니다.

Windows는 WM_INITDIALOG 메시지를 `OnMeasureItem` 보내기 전에 ownerdrawFIXED 스타일로 만든 콤보 상자 및 목록 [WM_INITDIALOG](/windows/win32/dlgbox/wm-initdialog) 상자의 소유자에 대 한 호출을 시작 합니다. 따라서 소유자가 이 호출을 수신할 때 Windows는 컨트롤에 사용된 글꼴의 높이와 너비를 아직 결정하지 않았습니다. 이러한 값을 필요로 하는 함수 호출 및 계산은 응용 프로그램 또는 라이브러리의 주요 함수에서 발생해야 합니다.

측정중인 항목이 `CMenu`의 `CListBox` 또는 `CComboBox` 개체인 `MeasureItem` 경우 해당 클래스의 가상 함수가 호출됩니다. 적절한 컨트롤 `MeasureItem` 클래스의 멤버 함수를 재정의하여 각 항목의 크기를 계산하고 설정합니다.

`OnMeasureItem`컨트롤의 클래스가 런타임에 만들어지거나 LBS_OWNERDRAWVARIABLE 또는 CBS_OWNERDRAWVARIABLE 스타일로 만들어진 경우에만 호출됩니다. 대화 상자 편집기에서 `OnMeasureItem` 컨트롤을 만든 경우 호출되지 않습니다. [이는 WM_MEASUREITEM](/windows/win32/Controls/wm-measureitem) 메시지가 컨트롤의 만들기 프로세스 초기에 전송되므로 이 메시지가 전송됩니다. 을 사용하여 하위 `DDX_Control`클래스를 사용하는 `SubclassDlgItem`경우 하위 `SubclassWindow`클래스는 일반적으로 생성 프로세스 후에 발생합니다. 따라서 MFC가 ON_WM_MEASUREITEM_REFLECT 구현하는 [WM_MEASUREITEM](/windows/win32/Controls/wm-measureitem) 데 사용하는 메커니즘인 `OnChildNotify` 컨트롤 함수에서 WM_MEASUREITEM 메시지를 처리할 수 있는 방법은 없습니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonmenuchar"></a><a name="onmenuchar"></a>CWnd::온메뉴차

프레임워크는 사용자가 현재 메뉴에서 미리 정의된 니모닉과 일치하지 않는 메뉴 니모닉 문자를 누를 때 이 멤버 함수를 호출합니다.

```
afx_msg LRESULT OnMenuChar(
    UINT nChar,
    UINT nFlags,
    CMenu* pMenu);
```

### <a name="parameters"></a>매개 변수

*Nchar*<br/>
빌드 설정에 따라 사용자가 누른 ANSI 또는 유니코드 문자를 지정합니다.

*nFlags*<br/>
메뉴가 팝업 메뉴인 경우 MF_POPUP 플래그를 포함합니다. 메뉴가 컨트롤 메뉴인 경우 MF_SYSMENU 플래그가 포함되어 있습니다.

*p메뉴*<br/>
선택한 `CMenu`에 대한 포인터가 포함되어 있습니다. 포인터는 일시적일 수 있으며 저장해서는 안 됩니다.

### <a name="return-value"></a>Return Value

return 값의 높은 순서 단어에는 다음 명령 코드 중 하나가 포함되어야 합니다.

|값|설명|
|-----------|-----------------|
|0|Windows에서 사용자가 누른 문자를 삭제하도록 지시하고 시스템 스피커에서 짧은 비프음을 만듭니다.|
|1|Windows에 현재 메뉴를 닫으라고 지시합니다.|
|2|반환 값의 낮은 순서 단어에는 특정 항목의 항목 번호가 포함되어 있음을 Windows에 알립니다. 이 항목은 Windows에서 선택합니다.|

높은 순서의 단어가 0 또는 1을 포함하는 경우 낮은 순서의 단어는 무시됩니다. 가속기(바로 가기) 키가 메뉴에 배치된 비트맵을 선택하는 데 사용될 때 응용 프로그램은 이 메시지를 처리해야 합니다.

### <a name="remarks"></a>설명

메뉴를 소유하는 `CWnd` 것으로 전송됩니다. `OnMenuChar`키가 니모닉 문자와 일치하지 않더라도 사용자가 ALT 및 기타 키를 누를 때도 호출됩니다. 이 경우 `CWnd` *pMenu는* 에서 소유한 메뉴를 가리키고 *nFlags는* 0입니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonmenudrag"></a><a name="onmenudrag"></a>CWnd::온메뉴드래그

프레임워크는 사용자가 메뉴 항목을 끌기 시작할 때 현재 끌어서 놓기 메뉴의 이 멤버 함수를 호출합니다.

```
afx_msg UINT OnMenuDrag(
    UINT nPos,
    CMenu* pMenu);
```

### <a name="parameters"></a>매개 변수

|매개 변수|Description|
|---------------|-----------------|
|*Npos*|【인】 끌기 작업이 시작될 때 메뉴 항목의 인덱스 위치입니다.|
|*p메뉴*|【인】 메뉴 항목이 포함된 [CMenu](../../mfc/reference/cmenu-class.md) 개체에 대한 포인터입니다.|

### <a name="return-value"></a>Return Value

|Return Value|의미|
|------------------|-------------|
|MND_CONTINUE|메뉴는 활성 상태로 유지되어야 합니다. 마우스가 해제되면 무시해야 합니다.|
|MND_ENDMENU|메뉴가 종료되어야 합니다.|

### <a name="remarks"></a>설명

이 메서드는 Windows SDK에 설명 된 [WM_MENUDRAG](/windows/win32/menurc/wm-menudrag) 알림을 받습니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonmenugetobject"></a><a name="onmenugetobject"></a>CWnd::OnMenuGetObject

프레임워크는 마우스 커서가 메뉴 항목을 입력하거나 항목의 중앙에서 항목의 위쪽 또는 아래쪽으로 이동할 때 현재 끌어서 놓기 메뉴의 이 멤버 함수를 호출합니다.

```
afx_msg UINT OnMenuGetObject(MENUGETOBJECTINFO* pMenuGetObjectInfo);
```

### <a name="parameters"></a>매개 변수

|매개 변수|Description|
|---------------|-----------------|
|*p메뉴*|【인】 MOUSE 커서가 있는 끌어서 놓기 메뉴에 대한 정보가 포함된 [MENUGETOBJECTINFO](/windows/win32/api/winuser/ns-winuser-menugetobjectinfo) 구조에 대한 포인터입니다.|

### <a name="return-value"></a>Return Value

|Return Value|의미|
|------------------|-------------|
|MNGO_NOERROR|끌어 놓기 및 끌기 작업을 지원하는 인터페이스 `pvObj` 포인터는 [MENUGETOBJECTINFO](/windows/win32/api/winuser/ns-winuser-menugetobjectinfo) 구조의 멤버에서 반환됩니다. 현재 [IDropTarget](/windows/win32/api/oleidl/nn-oleidl-idroptarget) 인터페이스만 지원됩니다.|
|MNGO_NOINTERFACE|드롭 앤 드래그 인터페이스가 지원되지 않습니다.|

### <a name="remarks"></a>설명

이 메서드는 Windows SDK에 설명 된 [WM_MENUGETOBJECT](/windows/win32/menurc/wm-menugetobject) 알림을 받습니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonmenurbuttonup"></a><a name="onmenurbuttonup"></a>CWnd::온메뉴버튼업

커서가 메뉴 항목에 있는 동안 사용자가 오른쪽 마우스 단추를 해제할 때 프레임워크는 이 멤버 함수를 호출합니다.

```
afx_msg void OnMenuRButtonUp(
    UINT nPos,
    CMenu* pMenu);
```

### <a name="parameters"></a>매개 변수

|매개 변수|Description|
|---------------|-----------------|
|*Npos*|【인】 오른쪽 마우스 버튼이 해제되었을 때 메뉴 항목의 인덱스 위치입니다.|
|*p메뉴*|【인】 메뉴 항목이 포함된 [CMenu](../../mfc/reference/cmenu-class.md) 개체에 대한 포인터입니다.|

### <a name="remarks"></a>설명

이 메서드는 Windows SDK에 설명 된 [WM_MENURBUTTONUP](/windows/win32/menurc/wm-menurbuttonup) 알림을 받습니다. [WM_MENURBUTTONUP](/windows/win32/menurc/wm-menurbuttonup) 메시지를 사용하면 응용 프로그램이 메시지에 지정된 메뉴 항목에 대한 상황에 맞는 메뉴를 제공할 수 있습니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonmenuselect"></a><a name="onmenuselect"></a>CWnd::온메뉴 선택

개체가 `CWnd` 메뉴와 `OnMenuSelect` 연결되어 있는 경우 사용자가 메뉴 항목을 선택할 때 프레임워크에서 호출됩니다.

```
afx_msg void OnMenuSelect(
    UINT nItemID,
    UINT nFlags,
    HMENU hSysMenu);
```

### <a name="parameters"></a>매개 변수

*n항목 ID*<br/>
선택한 항목을 식별합니다. 선택한 항목이 메뉴 항목인 경우 *nItemID에는* 메뉴 항목 ID가 포함됩니다. 선택한 항목에 팝업 메뉴가 포함된 경우 *nItemID에는* 팝업 메뉴 인덱스가 포함되고 *hSysMenu에는* 기본(클릭온) 메뉴의 핸들이 포함됩니다.

*nFlags*<br/>
다음 메뉴 플래그의 조합을 포함합니다.

- MF_BITMAP 항목은 비트맵입니다.

- MF_CHECKED 항목이 선택됩니다.

- MF_DISABLED 항목을 사용할 수 없습니다.

- MF_GRAYED 아이템이 흐리게 표시됩니다.

- MF_MOUSESELECT 항목을 마우스로 선택했습니다.

- MF_OWNERDRAW 아이템은 소유자-뽑기 아이템입니다.

- MF_POPUP 항목에 팝업 메뉴가 포함되어 있습니다.

- MF_SEPARATOR 아이템은 메뉴 항목 구분 기호입니다.

- MF_SYSMENU 항목은 컨트롤 메뉴에 포함되어 있습니다.

*hSysMenu*<br/>
*nFlags에* MF_SYSMENU 포함된 경우 메시지와 연결된 메뉴를 식별합니다. *nFlags에* MF_POPUP 포함된 경우 주 메뉴의 핸들을 식별합니다. *nFlags에* MF_SYSMENU MF_POPUP 포함되어 있지 않으면 사용되지 않습니다.

### <a name="remarks"></a>설명

nFlags0xFFff를 포함 하 고 *hSysMenu* 0 포함 하는 경우, 사용자가 ESC 키를 누르거나 메뉴 외부를 클릭 하기 때문에 Windows 메뉴를 닫았다. *nFlags*

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonmouseactivate"></a><a name="onmouseactivate"></a>CWnd::온마우스 활성화

프레임워크는 커서가 비활성 창에 있고 사용자가 마우스 단추를 누를 때 이 멤버 함수를 호출합니다.

```
afx_msg int OnMouseActivate(
    CWnd* pDesktopWnd,
    UINT nHitTest,
    UINT message);
```

### <a name="parameters"></a>매개 변수

*pDesktopWnd*<br/>
활성화되는 창의 최상위 상위 창에 대한 포인터를 지정합니다. 포인터는 일시적일 수 있으며 저장해서는 안 됩니다.

*n히트 테스트*<br/>
[적중 테스트](#onnchittest) 영역 코드를 지정합니다. 적중 테스트는 커서의 위치를 결정하는 테스트입니다.

*message*<br/>
마우스 메시지 번호를 지정합니다.

### <a name="return-value"></a>Return Value

를 활성화할지 `CWnd` 여부와 마우스 이벤트를 삭제할지 여부를 지정합니다. 다음 값 중 하나여야 합니다.

- MA_ACTIVATE `CWnd` 개체를 활성화합니다.

- MA_NOACTIVATE 개체를 `CWnd` 활성화하지 마십시오.

- MA_ACTIVATEANDEAT `CWnd` 오브젝트를 활성화하고 마우스 이벤트를 삭제합니다.

- MA_NOACTIVATEANDEAT 개체를 `CWnd` 활성화하고 마우스 이벤트를 삭제하지 마십시오.

### <a name="remarks"></a>설명

기본 구현은 처리가 발생하기 전에 이 메시지를 부모 창에 전달합니다. 부모 창이 TRUE를 반환하면 처리가 중지됩니다.

개별 적중 테스트 영역 코드에 대한 설명은 [OnNcHitTest](#onnchittest) 멤버 함수를 참조하십시오.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCAxCtl#9](../../mfc/reference/codesnippet/cpp/cwnd-class_49.cpp)]

## <a name="cwndonmousehover"></a><a name="onmousehover"></a>CWnd::에 마우스 호버

[프레임워크는 Cursor가 TrackMouseEvent](/windows/win32/api/winuser/nf-winuser-trackmouseevent)에 대한 사전 호출에 지정된 기간 동안 창의 클라이언트 영역 위로 마우스를 가져가면 이 멤버 함수를 호출합니다.

```
afx_msg void OnMouseHover(
    UINT nFlags,
    CPoint point);
```

### <a name="parameters"></a>매개 변수

|매개 변수|Description|
|---------------|-----------------|
|*nFlags*|【인】 누른 수정자 키를 나타내는 플래그의 비트 조합(OR)입니다. 예를 들어 MK_CONTROL 플래그는 CTRL 키가 눌리고 있음을 나타냅니다.|
|*지점*|【인】 클라이언트 영역의 왼쪽 위 모서리를 기준으로 커서의 *x* 및 *y* 좌표를 지정하는 [CPoint](../../atl-mfc-shared/reference/cpoint-class.md) 개체입니다.|

### <a name="remarks"></a>설명

이 메서드는 Windows SDK에 설명 된 [WM_MOUSEHOVER](/windows/win32/inputdev/wm-mousehover) 알림을 받습니다.

*nFlags* 매개 변수는 다음 표에 나열된 수정자 키의 조합일 수 있습니다. 자세한 내용은 [마우스 입력 정보를](/windows/win32/inputdev/about-mouse-input)참조하십시오.

|수정자 키|Description|
|------------------|-----------------|
|MK_CONTROL|CTRL 키를 누른다.|
|MK_LBUTTON|마우스 왼쪽 단추가 눌러져 있습니다.|
|MK_MBUTTON|마우스 가운데 단추가 눌러져 있습니다.|
|MK_RBUTTON|마우스 오른쪽 단추가 눌러져 있습니다.|
|MK_SHIFT|SHIFT 키를 누른 다.|
|MK_XBUTTON1|마이크로소프트 IntelliMouse의 XBUTTON1 마우스 버튼을 누른.|
|MK_XBUTTON2|마이크로소프트 인텔리마우스의 XBUTTON2 마우스 버튼을 누른다.|

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonmousehwheel"></a><a name="onmousehwheel"></a>CWnd::에 마우스 HWheel

현재 창이 DWM(데스크톱 창 관리자)에 의해 구성되고 해당 창이 최대화될 때 프레임워크는 이 멤버를 호출합니다.

```
afx_msg void OnMouseHWheel(
    UINT nFlags,
    short zDelta,
    CPoint pt);
```

### <a name="parameters"></a>매개 변수

|매개 변수|Description|
|---------------|-----------------|
|*nFlags*|【인】 누른 수정자 키를 나타내는 플래그의 비트 조합(OR)입니다. 예를 들어 MK_CONTROL 플래그는 CTRL 키가 눌리고 있음을 나타냅니다.<br /><br /> 플래그 목록은 [마우스 입력 에](/windows/win32/inputdev/about-mouse-input)있는 "메시지 매개 변수" 부제목을 참조하십시오.|
|*z델타*|【인】 휠이 회전하는 거리를 나타내며, 120인 WHEEL_DELTA 배수 또는 분할로 표현됩니다. 양수 값은 휠이 오른쪽으로 회전했음을 나타냅니다. 음수 값은 휠이 왼쪽으로 회전했음을 나타냅니다.|
|*pt*|【인】 클라이언트 영역의 왼쪽 위 모서리를 기준으로 커서의 *x* 및 *y* 좌표를 지정하는 [CPoint](../../atl-mfc-shared/reference/cpoint-class.md) 개체입니다.|

### <a name="remarks"></a>설명

이 메서드는 Windows SDK에 설명 된 [WM_MOUSEHWHEEL](/windows/win32/inputdev/wm-mousehwheel) 알림 메시지를 받습니다. 이 메시지는 마우스의 가로 스크롤 휠이 기울어지거나 회전할 때 포커스가 있는 창으로 전송됩니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonmouseleave"></a><a name="onmouseleave"></a>CWnd::에 마우스 리브

프레임워크는 커서가 [TrackMouseEvent](/windows/win32/api/winuser/nf-winuser-trackmouseevent)에 대한 사전 호출에 지정된 창의 클라이언트 영역을 떠날 때 이 멤버 함수를 호출합니다.

```
afx_msg void OnMouseLeave();
```

### <a name="remarks"></a>설명

이 메서드는 Windows SDK에 설명 된 [WM_MOUSELEAVE](/windows/win32/inputdev/wm-mouseleave) 알림을 받습니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonmousemove"></a><a name="onmousemove"></a>CWnd:::온마우스 무브

프레임워크는 마우스 커서가 이동할 때 이 멤버 함수를 호출합니다.

```
afx_msg void OnMouseMove(
    UINT nFlags,
    CPoint point);
```

### <a name="parameters"></a>매개 변수

*nFlags*<br/>
다양한 가상 키가 다운되었는지 여부를 나타냅니다. 이 매개 변수는 다음 값의 조합일 수 있습니다.

- MK_CONTROL CTRL 키가 다운된 경우 설정합니다.

- MK_LBUTTON 왼쪽 마우스 버튼이 다운된 경우 설정합니다.

- MK_MBUTTON 가운데 마우스 버튼이 다운된 경우 설정합니다.

- 오른쪽 마우스 버튼이 다운되면 MK_RBUTTON 설정합니다.

- shift 키가 다운된 경우 MK_SHIFT 설정합니다.

*지점*<br/>
커서의 x- 및 y 좌표를 지정합니다. 이러한 좌표는 항상 창의 왼쪽 위 모서리를 기준으로 합니다.

### <a name="remarks"></a>설명

마우스가 캡처되지 않으면 WM_MOUSEMOVE 메시지가 마우스 `CWnd` 커서 아래의 개체에서 수신됩니다. 그렇지 않으면 메시지가 마우스를 캡처한 창으로 이동합니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonmousewheel"></a><a name="onmousewheel"></a>CWnd::에마우스 휠

프레임워크는 사용자가 마우스 휠을 회전하고 휠의 다음 노치를 만날 때 이 멤버 함수를 호출합니다.

```
afx_msg BOOL OnMouseWheel(
    UINT nFlags,
    short zDelta,
    CPoint pt);
```

### <a name="parameters"></a>매개 변수

*nFlags*<br/>
다양한 가상 키가 다운되었는지 여부를 나타냅니다. 이 매개 변수는 다음 값의 조합일 수 있습니다.

- MK_CONTROL CTRL 키가 다운된 경우 설정합니다.

- MK_LBUTTON 왼쪽 마우스 버튼이 다운된 경우 설정합니다.

- MK_MBUTTON 가운데 마우스 버튼이 다운된 경우 설정합니다.

- 오른쪽 마우스 버튼이 다운되면 MK_RBUTTON 설정합니다.

- shift 키가 다운된 경우 MK_SHIFT 설정합니다.

*z델타*<br/>
회전된 거리를 나타냅니다. *zDelta* 값은 120인 WHEEL_DELTA 배수 또는 분할로 표현됩니다. 값이 0보다 작은 값은 뒤로 회전(사용자를 향하여)을 나타내고 값이 0보다 큰 값은 앞으로 회전하는 것을 나타냅니다(사용자로부터 멀어지기). 사용자는 마우스 소프트웨어에서 휠 설정을 변경하여 이 응답을 되돌릴 수 있습니다. 이 매개 변수에 대한 자세한 내용은 설명(설명)을 참조하십시오.

*pt*<br/>
커서의 x- 및 y 좌표를 지정합니다. 이러한 좌표는 항상 화면의 왼쪽 위 모서리를 기준으로 합니다.

### <a name="return-value"></a>Return Value

마우스 휠 스크롤이 활성화된 경우 0이 아닙니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

재정의하지 않는 `OnMouseWheel` 한 [WM_MOUSEWHEEL](/windows/win32/inputdev/wm-mousewheel)기본값을 호출합니다. Windows는 포커스가 있는 컨트롤 또는 자식 창에 메시지를 자동으로 라우팅합니다. Win32 함수 [DefWindowProc는](/windows/win32/api/winuser/nf-winuser-defwindowprocw) 메시지를 처리하는 창으로 부모 체인위로 전파합니다.

*zDelta* 매개 변수는 120으로 설정된 WHEEL_DELTA 배수입니다. 이 값은 작업을 수행해야 하는 임계값이며 각 델타에 대해 이러한 작업(예: 한 단계 앞으로 스크롤)이 발생해야 합니다.

WHEEL_DELTA 노치가 없는 자유롭게 회전하는 휠과 같이 더 미세한 해상도의 휠을 허용하도록 120으로 설정되었습니다. 더 미세한 해상도휠은 회전당 더 많은 메시지를 보내지만 각 메시지에는 델타 값이 더 작습니다. 이러한 휠을 사용하려면 WHEEL_DELTA 도달할 때까지 들어오는 *zDelta* 값을 추가하거나(지정된 델타 회전에 대해 동일한 응답을 받도록) 더 빈번한 메시지에 대한 응답으로 부분 선을 스크롤합니다. 스크롤 세분성을 선택하고 WHEEL_DELTA 도달할 때까지 델타를 누적할 수도 있습니다.

이 멤버 함수를 재정의하여 고유한 마우스 휠 스크롤 동작을 제공합니다.

> [!NOTE]
> `OnMouseWheel`은 Windows NT 4.0 및 이후 버전에 대한 메시지를 처리합니다. 윈도우 95/98 또는 Windows NT 3.51 메시지 처리의 경우 [등록된 MouseWheel](#onregisteredmousewheel)을 사용합니다.

## <a name="cwndonmove"></a><a name="onmove"></a>CWnd::: 이동 중

프레임워크는 개체를 이동한 `CWnd` 후 이 멤버 함수를 호출합니다.

```
afx_msg void OnMove(
    int x,
    int y);
```

### <a name="parameters"></a>매개 변수

*x*<br/>
클라이언트 영역의 왼쪽 위 모서리의 새 x 좌표 위치를 지정합니다. 이 새 위치는 겹치고 팝업 창에 대한 화면 좌표와 자식 창에 대한 부모-클라이언트 좌표에 제공됩니다.

*Y*<br/>
클라이언트 영역의 왼쪽 위 모서리의 새 y 좌표 위치를 지정합니다. 이 새 위치는 겹치고 팝업 창에 대한 화면 좌표와 자식 창에 대한 부모-클라이언트 좌표에 제공됩니다.

### <a name="remarks"></a>설명

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonmoving"></a><a name="onmoving"></a>CWnd::이동 중

프레임워크는 사용자가 개체를 이동하는 동안 `CWnd` 이 멤버 함수를 호출합니다.

```
afx_msg void OnMoving(
    UINT nSide,
    LPRECT lpRect);
```

### <a name="parameters"></a>매개 변수

*nSide*<br/>
이동할 창의 모서리입니다.

*Lprect*<br/>
항목의 좌표를 포함하는 [CRect](../../atl-mfc-shared/reference/crect-class.md) 또는 [RECT 구조의](/windows/win32/api/windef/ns-windef-rect) 주소입니다.

### <a name="remarks"></a>설명

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonncactivate"></a><a name="onncactivate"></a>CWnd::OnNcActivate

프레임워크는 비클라이언트 영역을 활성 또는 비활성 상태를 나타내기 위해 변경해야 할 때 이 멤버 함수를 호출합니다.

```
afx_msg BOOL OnNcActivate(BOOL bActive);
```

### <a name="parameters"></a>매개 변수

*b활성*<br/>
활성 또는 비활성 상태를 나타내기 위해 캡션 표시줄 또는 아이콘을 변경해야 하는 시기를 지정합니다. 활성 캡션 또는 아이콘을 그릴 경우 *bActive* 매개 변수는 TRUE입니다. 비활성 캡션 또는 아이콘에 대한 FALSE입니다.

### <a name="return-value"></a>Return Value

Windows에서 기본 처리를 진행해야 하는 경우 0이 아닙니다. 0을 사용하여 캡션 표시줄 또는 아이콘이 비활성화되지 않도록 합니다.

### <a name="remarks"></a>설명

기본 구현에서는 *bActive가* TRUE인 경우 활성 색상으로 제목 표시줄 과 제목 표시줄 텍스트를 그리고 *bActive가* FALSE인 경우 비활성 색상으로 그립니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonnccalcsize"></a><a name="onnccalcsize"></a>CWnd::OnNcCalcsize

프레임워크는 클라이언트 영역의 크기와 위치를 계산해야 할 때 이 멤버 함수를 호출합니다.

```
afx_msg void OnNcCalcSize(
    BOOL bCalcValidRects,
    NCCALCSIZE_PARAMS* lpncsp);
```

### <a name="parameters"></a>매개 변수

*bCalcValidRects*<br/>
응용 프로그램에서 유효한 정보가 포함된 클라이언트 영역의 일부를 지정할지 여부를 지정합니다. Windows는 유효한 정보를 새 클라이언트 영역 내의 지정된 영역으로 복사합니다. 이 매개 변수가 TRUE이면 응용 프로그램은 클라이언트 영역의 어느 부분이 유효한지 지정해야 합니다.

*lpncsp*<br/>
응용 프로그램에서 [NCCALCSIZE_PARAMS](/windows/win32/api/winuser/ns-winuser-nccalcsize_params) `CWnd` 사각형의 새 크기와 위치(클라이언트 영역, 테두리, 캡션, 스크롤 막대 등)를 계산하는 데 사용할 수 있는 정보가 포함된 NCCALCSIZE_PARAMS 데이터 구조를 가리킵니다.

### <a name="remarks"></a>설명

이 메시지를 처리하면 응용 프로그램은 창의 크기 나 위치가 변경될 때 창의 클라이언트 영역의 내용을 제어할 수 있습니다.

*bCalcValidRects*값에 관계없이 `rgrc` `NCCALCSIZE_PARAMS` 구조체의 구조 멤버가 지정한 배열의 첫 번째 사각형에는 창의 좌표가 포함됩니다. 자식 창의 좌표는 부모 창의 클라이언트 영역을 기준으로 합니다. 최상위 창의 경우 좌표는 화면 좌표입니다. 응용 프로그램은 클라이언트 `rgrc[0]` 영역의 크기와 위치를 반영하도록 사각형을 수정해야 합니다.

`rgrc[1]` 및 `rgrc[2]` 사각형은 *bCalcValidRects가* TRUE인 경우에만 유효합니다. 이 경우 사각형은 `rgrc[1]` 이동하거나 크기를 조정하기 전에 창의 좌표를 포함합니다. 사각형에는 `rgrc[2]` 창을 이동하기 전에 창의 클라이언트 영역의 좌표가 포함됩니다. 모든 좌표는 상위 창 또는 화면을 기준으로 합니다.

기본 구현은 창 특성(스크롤 막대, 메뉴 등)을 기반으로 클라이언트 영역의 크기를 계산하고 결과를 *lpncsp로*배치합니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonnccreate"></a><a name="onnccreate"></a>CWnd::OnNcCreate

프레임워크는 개체를 처음 만들 [WM_CREATE](#oncreate) 때 WM_CREATE `CWnd` 메시지 이전에 이 멤버 함수를 호출합니다.

```
afx_msg BOOL OnNcCreate(LPCREATESTRUCT lpCreateStruct);
```

### <a name="parameters"></a>매개 변수

*lp창조*<br/>
에 대한 `CWnd` [CREATESTRUCT](/windows/win32/api/winuser/ns-winuser-createstructw) 데이터 구조를 가리킵니다.

### <a name="return-value"></a>Return Value

비클라이언트 영역이 생성되는 경우 0이 아닙니다. 오류가 발생하면 0입니다. 이 `Create` 경우 함수가 **오류를** 반환합니다.

### <a name="remarks"></a>설명

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonncdestroy"></a><a name="onncdestroy"></a>CWnd::OnNcDestroy

비클라이언트 영역이 소멸될 때 프레임워크에서 호출되며 Windows 창이 소멸될 때 호출되는 마지막 멤버 함수입니다.

```
afx_msg void OnNcDestroy();
```

### <a name="remarks"></a>설명

기본 구현은 몇 가지 정리를 수행한 다음 가상 멤버 함수 [PostNcDestroy를](#postncdestroy)호출합니다.

이 `PostNcDestroy` **작업 삭제와** 같이 사용자 고유의 정리를 수행하려는 경우 재정의합니다. 재정의하는 `OnNcDestroy`경우 창에 `OnNcDestroy` 내부적으로 할당된 메모리가 해제되도록 기본 클래스에서 호출해야 합니다.

## <a name="cwndonnchittest"></a><a name="onnchittest"></a>CWnd::OnNcHitTest

프레임워크는 마우스를 이동할 `CWnd` 때마다 커서(또는 [SetCapture](#setcapture) `CWnd` 멤버 함수를 사용하여 마우스 입력을 캡처한 개체)가 포함된 개체에 대해 이 멤버 함수를 호출합니다.

```
afx_msg LRESULT OnNcHitTest(CPoint point);
```

### <a name="parameters"></a>매개 변수

*지점*<br/>
커서의 x- 및 y 좌표를 포함합니다. 이러한 좌표는 항상 화면 좌표입니다.

### <a name="return-value"></a>Return Value

마우스 히트 테스트 수거 값 중 하나입니다. 값 목록은 [WM_NCHITTEST](/windows/win32/inputdev/wm-nchittest) 를 참조하십시오.

### <a name="remarks"></a>설명

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonnclbuttondblclk"></a><a name="onnclbuttondblclk"></a>CWnd::OnNcLButtonDblk

커서가 `CWnd`의 비클라이언트 영역 내에 있는 동안 사용자가 왼쪽 마우스 단추를 두 번 클릭할 때 프레임워크는 이 멤버 함수를 호출합니다.

```
afx_msg void OnNcLButtonDblClk(
    UINT nHitTest,
    CPoint point);
```

### <a name="parameters"></a>매개 변수

*n히트 테스트*<br/>
[적중 테스트 코드를](#onnchittest)지정합니다. 적중 테스트는 커서의 위치를 결정하는 테스트입니다. 값 목록은 [WM_NCHITTEST](/windows/win32/inputdev/wm-nchittest) 를 참조하십시오.

*지점*<br/>
커서 위치의 x 및 y 화면 좌표를 포함하는 `CPoint` 개체를 지정합니다. 이러한 좌표는 항상 화면의 왼쪽 위 모서리를 기준으로 합니다.

### <a name="remarks"></a>설명

적절한 경우 [WM_SYSCOMMAND](#onsyscommand) 메시지가 전송됩니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonnclbuttondown"></a><a name="onnclbuttondown"></a>CWnd::OnNcLButtonDown

커서가 개체의 비클라이언트 영역 내에 있는 동안 사용자가 왼쪽 마우스 단추를 누를 때 프레임워크는 이 멤버 함수를 호출합니다. `CWnd`

```
afx_msg void OnNcLButtonDown(
    UINT nHitTest,
    CPoint point);
```

### <a name="parameters"></a>매개 변수

*n히트 테스트*<br/>
[적중 테스트 코드를](#onnchittest)지정합니다. 적중 테스트는 커서의 위치를 결정하는 테스트입니다. 값 목록은 [WM_NCHITTEST](/windows/win32/inputdev/wm-nchittest) 를 참조하십시오.

*지점*<br/>
커서 위치의 x 및 y 화면 좌표를 포함하는 `CPoint` 개체를 지정합니다. 이러한 좌표는 항상 화면의 왼쪽 위 모서리를 기준으로 합니다.

### <a name="remarks"></a>설명

적절한 경우 [WM_SYSCOMMAND](#onsyscommand) 전송됩니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달된 매개 변수는 메시지를 받을 때 프레임워크에서 받은 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우 해당 구현은 함수에 제공하는 매개 변수가 아니라 메시지와 함께 전달된 매개 변수를 사용합니다.

## <a name="cwndonnclbuttonup"></a><a name="onnclbuttonup"></a>CWnd::OnNcLButtonUp

커서가 클라이언트가 아닌 영역 내에 있는 동안 사용자가 왼쪽 마우스 단추를 해제할 때 프레임워크는 이 멤버 함수를 호출합니다.

```
afx_msg void OnNcLButtonUp(
    UINT nHitTest,
    CPoint point);
```

### <a name="parameters"></a>매개 변수

*n히트 테스트*<br/>
[적중 테스트 코드를](#onnchittest)지정합니다. 적중 테스트는 커서의 위치를 결정하는 테스트입니다. 값 목록은 [WM_NCHITTEST](/windows/win32/inputdev/wm-nchittest) 를 참조하십시오.

*지점*<br/>
커서 위치의 x 및 y 화면 좌표를 포함하는 `CPoint` 개체를 지정합니다. 이러한 좌표는 항상 화면의 왼쪽 위 모서리를 기준으로 합니다.

### <a name="remarks"></a>설명

적절한 경우 [WM_SYSCOMMAND](#onsyscommand) 전송됩니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonncmbuttondblclk"></a><a name="onncmbuttondblclk"></a>CWnd::OnNcMButtonDblk

커서가 클라이언트가 아닌 영역 내에 있는 동안 사용자가 가운데 마우스 단추를 두 번 클릭할 때 프레임워크는 이 멤버 함수를 호출합니다.

```
afx_msg void OnNcMButtonDblClk(
    UINT nHitTest,
    CPoint point);
```

### <a name="parameters"></a>매개 변수

*n히트 테스트*<br/>
[적중 테스트 코드를](#onnchittest)지정합니다. 적중 테스트는 커서의 위치를 결정하는 테스트입니다.

*지점*<br/>
커서 위치의 x 및 y 화면 좌표를 포함하는 `CPoint` 개체를 지정합니다. 이러한 좌표는 항상 화면의 왼쪽 위 모서리를 기준으로 합니다.

### <a name="remarks"></a>설명

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonncmbuttondown"></a><a name="onncmbuttondown"></a>CWnd::OnNcMButtonDown

커서가 클라이언트가 아닌 영역 내에 있는 동안 사용자가 가운데 마우스 단추를 누를 때 프레임워크는 이 멤버 함수를 호출합니다.

```
afx_msg void OnNcMButtonDown(
    UINT nHitTest,
    CPoint point);
```

### <a name="parameters"></a>매개 변수

*n히트 테스트*<br/>
[적중 테스트 코드를](#onnchittest)지정합니다. 적중 테스트는 커서의 위치를 결정하는 테스트입니다.

*지점*<br/>
커서 위치의 x 및 y 화면 좌표를 포함하는 `CPoint` 개체를 지정합니다. 이러한 좌표는 항상 화면의 왼쪽 위 모서리를 기준으로 합니다.

### <a name="remarks"></a>설명

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonncmbuttonup"></a><a name="onncmbuttonup"></a>CWnd::OnNcMButtonUp

커서가 클라이언트가 아닌 영역 내에 있는 동안 사용자가 가운데 마우스 단추를 해제할 때 프레임워크는 이 멤버 함수를 호출합니다.

```
afx_msg void OnNcMButtonUp(
    UINT nHitTest,
    CPoint point);
```

### <a name="parameters"></a>매개 변수

*n히트 테스트*<br/>
[적중 테스트 코드를](#onnchittest)지정합니다. 적중 테스트는 커서의 위치를 결정하는 테스트입니다.

*지점*<br/>
커서 위치의 x 및 y 화면 좌표를 포함하는 `CPoint` 개체를 지정합니다. 이러한 좌표는 항상 화면의 왼쪽 위 모서리를 기준으로 합니다.

### <a name="remarks"></a>설명

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonncmousehover"></a><a name="onncmousehover"></a>CWnd::OnNc마우스 호버

[프레임워크는 Cursor가 TrackMouseEvent](/windows/win32/api/winuser/nf-winuser-trackmouseevent)에 대한 사전 호출에 지정된 기간 동안 창의 비클라이언트 영역 위로 마우스를 가져가면 이 멤버 함수를 호출합니다.

```
afx_msg void OnNcMouseHover(
    UINT nHitTest,
    CPoint point);
```

### <a name="parameters"></a>매개 변수

|매개 변수|Description|
|---------------|-----------------|
|*n히트 테스트*|【인】 [cWnd::DefWindowProc](#defwindowproc) 함수에서 반환된 적중 테스트 값은 [WM_NCHITTEST](/windows/win32/inputdev/wm-nchittest) 메시지를 처리한 결과입니다.|
|*지점*|【인】 화면의 왼쪽 위 모서리를 기준으로 커서의 *x* 및 *y* 좌표를 지정하는 [CPoint](../../atl-mfc-shared/reference/cpoint-class.md) 개체입니다.|

### <a name="remarks"></a>설명

이 메서드는 Windows SDK에 설명 된 [WM_NCMOUSEHOVER](/windows/win32/inputdev/wm-ncmousehover) 알림을 받습니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonncmouseleave"></a><a name="onncmouseleave"></a>CWnd::OnNcMouseLeave

프레임워크는 [커서가 TrackMouseEvent](/windows/win32/api/winuser/nf-winuser-trackmouseevent)에 대한 사전 호출에 지정된 창의 비클라이언트 영역을 떠날 때 이 멤버 함수를 호출합니다.

```
afx_msg void OnNcMouseLeave();
```

### <a name="remarks"></a>설명

이 메서드는 Windows SDK에 설명 된 [WM_NCMOUSELEAVE](/windows/win32/inputdev/wm-ncmouseleave) 알림을 받습니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonncmousemove"></a><a name="onncmousemove"></a>CWnd::OnNcMouseMove

프레임워크는 커서가 아닌 클라이언트 영역 내에서 이동할 때 이 멤버 함수를 호출합니다.

```
afx_msg void OnNcMouseMove(
    UINT nHitTest,
    CPoint point);
```

### <a name="parameters"></a>매개 변수

*n히트 테스트*<br/>
[적중 테스트 코드를](#onnchittest)지정합니다. 적중 테스트는 커서의 위치를 결정하는 테스트입니다.

*지점*<br/>
커서 위치의 x 및 y 화면 좌표를 포함하는 `CPoint` 개체를 지정합니다. 이러한 좌표는 항상 화면의 왼쪽 위 모서리를 기준으로 합니다.

### <a name="remarks"></a>설명

적절한 경우 [WM_SYSCOMMAND](#onsyscommand) 메시지가 전송됩니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonncpaint"></a><a name="onncpaint"></a>CWnd::온씨페인트

프레임워크는 비클라이언트 영역을 그려야 할 때 이 멤버 함수를 호출합니다.

```
afx_msg void OnNcPaint();
```

### <a name="remarks"></a>설명

기본 구현은 창 프레임을 그립니다.

응용 프로그램은 이 호출을 재정의하고 자체 사용자 지정 창 프레임을 페인트할 수 있습니다. 프레임의 모양이 변경된 경우에도 클리핑 영역은 항상 직사각형입니다.

## <a name="cwndonncrbuttondblclk"></a><a name="onncrbuttondblclk"></a>CWnd::OnNcRButtonDblk

커서가 `CWnd`의 비클라이언트 영역 내에 있는 동안 사용자가 오른쪽 마우스 단추를 두 번 클릭할 때 프레임워크는 이 멤버 함수를 호출합니다.

```
afx_msg void OnNcRButtonDblClk(
    UINT nHitTest,
    CPoint point);
```

### <a name="parameters"></a>매개 변수

*n히트 테스트*<br/>
[적중 테스트 코드를](#onnchittest)지정합니다. 적중 테스트는 커서의 위치를 결정하는 테스트입니다.

*지점*<br/>
커서 위치의 x 및 y 화면 좌표를 포함하는 `CPoint` 개체를 지정합니다. 이러한 좌표는 항상 화면의 왼쪽 위 모서리를 기준으로 합니다.

### <a name="remarks"></a>설명

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonncrbuttondown"></a><a name="onncrbuttondown"></a>CWnd::OnNcR 버튼 다운

커서가 클라이언트가 아닌 영역 내에 있는 동안 사용자가 오른쪽 마우스 단추를 누를 때 프레임워크는 이 멤버 함수를 호출합니다.

```
afx_msg void OnNcRButtonDown(
    UINT nHitTest,
    CPoint point);
```

### <a name="parameters"></a>매개 변수

*n히트 테스트*<br/>
[적중 테스트 코드를](#onnchittest)지정합니다. 적중 테스트는 커서의 위치를 결정하는 테스트입니다.

*지점*<br/>
커서 위치의 x 및 y 화면 좌표를 포함하는 `CPoint` 개체를 지정합니다. 이러한 좌표는 항상 화면의 왼쪽 위 모서리를 기준으로 합니다.

### <a name="remarks"></a>설명

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonncrbuttonup"></a><a name="onncrbuttonup"></a>CWnd::OnNcRButtonUp

커서가 클라이언트가 아닌 영역 내에 있는 동안 사용자가 오른쪽 마우스 단추를 해제할 때 프레임워크는 이 멤버 함수를 호출합니다.

```
afx_msg void OnNcRButtonUp(
    UINT nHitTest,
    CPoint point);
```

### <a name="parameters"></a>매개 변수

*n히트 테스트*<br/>
[적중 테스트 코드를](#onnchittest)지정합니다. 적중 테스트는 커서의 위치를 결정하는 테스트입니다.

*지점*<br/>
커서 위치의 x 및 y 화면 좌표를 포함하는 `CPoint` 개체를 지정합니다. 이러한 좌표는 항상 화면의 왼쪽 위 모서리를 기준으로 합니다.

### <a name="remarks"></a>설명

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonncrenderingchanged"></a><a name="onncrenderingchanged"></a>CWnd::OnNcRendering 변경

프레임워크는 비클라이언트 영역에 대한 렌더링 정책이 변경되면 이 멤버를 호출합니다.

```
afx_msg void OnNcRenderingChanged(BOOL bIsRendering);
```

### <a name="parameters"></a>매개 변수

|매개 변수|Description|
|---------------|-----------------|
|*비스 렌더링*|【인】 TRUE 경우 DWM(데스크톱 창 관리자) 렌더링이 창의 비클라이언트 영역에 대해 활성화되어 있습니다. false 렌더링을 사용할 수 없습니다.|

### <a name="remarks"></a>설명

이 메서드는 Windows SDK에 설명 된 [WM_DWMNCRENDERINGCHANGED](/windows/win32/dwm/wm-dwmncrenderingchanged) 알림을 받습니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonncxbuttondblclk"></a><a name="onncxbuttondblclk"></a>CWnd::OnNcXButtonDblk

커서가 창의 비클라이언트 영역에 있는 동안 사용자가 XBUTTON1 또는 XBUTTON2를 두 번 클릭할 때 프레임워크는 이 멤버 함수를 호출합니다.

```cpp
void OnNcXButtonDblClk(
    short nHitTest,
    UINT nButton,
    CPoint point);
```

### <a name="parameters"></a>매개 변수

|매개 변수|Description|
|---------------|-----------------|
|*n히트 테스트*|【인】 [cWnd::DefWindowProc](#defwindowproc) 함수에서 반환된 적중 테스트 값은 [WM_NCHITTEST](/windows/win32/inputdev/wm-nchittest) 메시지를 처리한 결과입니다.|
|*nButton*|【인】 첫 번째 Microsoft Intellimouse X 버튼을 두 번 클릭한 경우 XBUTTON1 값, 두 번째 X 단추를 두 번 클릭하면 XBUTTON2의 값입니다.|
|*지점*|【인】 클라이언트 영역의 왼쪽 위 모서리를 기준으로 커서의 *x* 및 *y* 좌표를 지정하는 [CPoint](../../atl-mfc-shared/reference/cpoint-class.md) 개체입니다.|

### <a name="remarks"></a>설명

이 메서드는 Windows SDK에 설명 된 [WM_XBUTTONDBLCLK](/windows/win32/inputdev/wm-xbuttondblclk) 알림을 받습니다. 이 메시지는 커서가 포함된 창에 게시됩니다. 창에서 마우스를 캡처한 경우 이 메시지가 게시되지 않습니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonncxbuttondown"></a><a name="onncxbuttondown"></a>CWnd::OnNcX 버튼 다운

커서가 창의 비클라이언트 영역에 있는 동안 사용자가 마우스의 XBUTTON1 또는 XBUTTON2를 누를 때 프레임워크는 이 멤버 함수를 호출합니다.

```
afx_msg void OnNcXButtonDown(
    short nHitTest,
    UINT nButton,
    CPoint point);
```

### <a name="parameters"></a>매개 변수

|매개 변수|Description|
|---------------|-----------------|
|*n히트 테스트*|【인】 [cWnd::DefWindowProc](#defwindowproc) 함수에서 반환된 적중 테스트 값은 [WM_NCHITTEST](/windows/win32/inputdev/wm-nchittest) 메시지를 처리한 결과입니다.|
|*nButton*|【인】 첫 번째 마우스 X 버튼을 누르면 XBUTTON1 값, 두 번째 X 버튼을 누르면 XBUTTON2값입니다.|
|*지점*|【인】 화면의 왼쪽 위 모서리를 기준으로 커서의 *x* 및 *y* 좌표를 지정하는 [CPoint](../../atl-mfc-shared/reference/cpoint-class.md) 개체입니다.|

### <a name="remarks"></a>설명

이 메서드는 Windows SDK에 설명 된 [WM_NCXBUTTONDOWN](/windows/win32/inputdev/wm-ncxbuttondown) 알림을 받습니다. 이 메시지는 커서가 포함된 창에 게시됩니다. 창에서 마우스를 캡처한 경우 이 메시지가 게시되지 않습니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonncxbuttonup"></a><a name="onncxbuttonup"></a>CWnd::OnNcXButtonUp

커서가 창의 비클라이언트 영역에 있는 동안 사용자가 마우스의 XBUTTON1 또는 XBUTTON2를 해제할 때 프레임워크는 이 멤버 함수를 호출합니다.

```
afx_msg void OnNcXButtonUp(
    short nHitTest,
    UINT nButton,
    CPoint point);
```

### <a name="parameters"></a>매개 변수

|매개 변수|Description|
|---------------|-----------------|
|*n히트 테스트*|【인】 [cWnd::DefWindowProc](#defwindowproc) 함수에서 반환된 적중 테스트 값은 [WM_NCHITTEST](/windows/win32/inputdev/wm-nchittest) 메시지를 처리한 결과입니다.|
|*nButton*|【인】 첫 번째 마우스 X 버튼이 해제된 경우 XBUTTON1 값, 두 번째 X 버튼이 해제된 경우 XBUTTON2값입니다.|
|*지점*|【인】 화면의 왼쪽 위 모서리를 기준으로 커서의 *x* 및 *y* 좌표를 지정하는 [CPoint](../../atl-mfc-shared/reference/cpoint-class.md) 개체입니다.|

### <a name="remarks"></a>설명

이 메서드는 Windows SDK에 설명 된 [WM_NCXBUTTONUP](/windows/win32/inputdev/wm-ncxbuttonup) 알림을 받습니다. 이 메시지는 커서가 포함된 창에 게시됩니다. 창에서 마우스를 캡처한 경우 이 메시지가 게시되지 않습니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonnextmenu"></a><a name="onnextmenu"></a>CWnd::다음 메뉴

오른쪽 또는 왼쪽 화살표 키가 메뉴 모음과 시스템 메뉴 사이를 전환하는 데 사용되는 경우 프레임워크는 이 멤버 함수를 호출합니다.

```
afx_msg void OnNextMenu(
    UINT nKey,
    LPMDINEXTMENU lpMdiNextMenu);
```

### <a name="parameters"></a>매개 변수

|매개 변수|Description|
|---------------|-----------------|
|*nKey*|【인】 누른 수정자 키를 나타내는 플래그의 비트 조합(OR)입니다. 예를 들어 MK_CONTROL 플래그는 CTRL 키가 눌리고 있음을 나타냅니다.<br /><br /> 플래그 목록은 [마우스 입력 에](/windows/win32/inputdev/about-mouse-input)있는 "메시지 매개 변수" 부제목을 참조하십시오.|
|*lpMdi다음메뉴*|【인】 활성화할 메뉴에 대한 정보가 포함된 [MDINEXTMENU](/windows/win32/api/winuser/ns-winuser-mdinextmenu) 구조에 대한 포인터입니다.|

### <a name="remarks"></a>설명

이 메서드는 Windows SDK에 설명 된 [WM_UNINITMENUPOPUP](/windows/win32/menurc/wm-uninitmenupopup) 알림을 받습니다. 이 메시지에 대한 응답으로 응용 프로그램은 `hmenuNext` [MDINEXTMENU](/windows/win32/api/winuser/ns-winuser-mdinextmenu) 구조의 멤버를 설정하여 전환할 메뉴를 `hwndNext` 지정하고 멤버는 메뉴 알림 메시지를 받을 창을 지정할 수 있습니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonnotify"></a><a name="onnotify"></a>CWnd::에 통보

프레임워크는 이 멤버 함수를 호출하여 컨트롤에서 이벤트가 발생했거나 컨트롤에 일종의 정보가 필요하다는 것을 컨트롤의 상위 창에 알립니다.

```
virtual BOOL OnNotify(
    WPARAM wParam,
    LPARAM lParam,
    LRESULT* pResult);
```

### <a name="parameters"></a>매개 변수

*wParam*<br/>
메시지가 컨트롤에서 온 경우 메시지를 보내는 컨트롤을 식별합니다. 그렇지 않으면 *wParam은* 0입니다.

*lParam*<br/>
알림 코드 및`NMHDR`추가 정보를 포함하는 알림 메시지 () 구조에 대한 포인터입니다. 일부 알림 메시지의 경우 이 매개 변수는 `NMHDR` 구조가 첫 번째 멤버로 있는 더 큰 구조를 가리킵니다.

*p결과*<br/>
메시지가 처리되는 경우 결과 코드를 저장할 LRESULT 변수를 포인터로 지정합니다.

### <a name="return-value"></a>Return Value

응용 프로그램은 이 메시지를 처리하는 경우 0이 아닌 것을 반환합니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

`OnNotify`을 사용하여 제어 알림을 위해 메시지 맵을 처리합니다.

파생 클래스에서 이 멤버 함수를 재정의하여 WM_NOTIFY 메시지를 처리합니다. 재정의는 기본 클래스가 `OnNotify` 호출되지 않는 한 메시지 맵을 처리하지 않습니다.

WM_NOTIFY 메시지에 대한 자세한 내용은 기술 참고 61(TN061), [ON_NOTIFY 및 WM_NOTIFY 메시지를](../../mfc/tn061-on-notify-and-wm-notify-messages.md)참조하십시오. 또한 [컨트롤 항목](../../mfc/controls-mfc.md)및 TN062, [Windows 컨트롤에 대한 메시지 반사에](../../mfc/tn062-message-reflection-for-windows-controls.md)설명된 관련 항목에 대해서도 관심이 있을 수 있습니다.

## <a name="cwndonnotifyformat"></a><a name="onnotifyformat"></a>CWnd::에 알림 형식

프레임워크는 이 멤버 함수를 호출하여 현재 창이 WM_NOTIFY 알림 메시지에서 ANSI 또는 유니코드 구조를 허용하는지 확인합니다.

```
afx_msg UINT OnNotifyFormat(
    CWnd* pWnd,
    UINT nCommand);
```

### <a name="parameters"></a>매개 변수

|매개 변수|Description|
|---------------|-----------------|
|*pWnd*|【인】 [WM_NOTIFY](/windows/win32/controls/wm-notify) 메시지를 `CWnd` 보내는 창을 나타내는 개체에 대한 포인터입니다.<br /><br /> 이 매개 변수는 *nCommand* 매개 변수가 NF_QUERY 경우 컨트롤에 대한 포인터이거나 *nCommand가* NF_REQUERY 경우 컨트롤의 부모 창에 대한 포인터입니다.|
|*n 명령*|【인】 WM_NOTIFY 메시지를 전문으로 하는 명령 값입니다. 사용 가능한 값은<br /><br /> - NF_QUERY -<br />     이 메시지는 anSI 또는 유니코드 구조를 WM_NOTIFY 메시지에 사용해야 하는지 여부를 결정하는 쿼리입니다. 이 메시지는 컨트롤을 만드는 동안 및 이 메시지의 NF_REQUERY 형식에 대한 응답으로 컨트롤에서 부모 창으로 전송됩니다.<br />- NF_REQUERY -<br />     메시지는 이 메시지의 NF_QUERY 형식을 부모 창으로 보내는 컨트롤에 대한 요청입니다. 이 요청은 부모 창에서 전송되며 WM_NOTIFY 메시지에 사용할 구조 유형에 대해 부모를 다시 쿼리하도록 컨트롤에 요청합니다. *nCommand* 매개 변수가 NF_REQUERY 경우 반환 값은 다시 쿼리 작업의 결과입니다.|

### <a name="return-value"></a>Return Value

|반환 값|의미|
|------------------|-------------|
|NFR_ANSI|ANSI 구조는 컨트롤에서 보낸 WM_NOTIFY 메시지에 사용해야 합니다.|
|NFR_UNICODE|유니코드 구조는 컨트롤에서 보낸 WM_NOTIFY 메시지에 사용해야 합니다.|
|0|오류가 발생했습니다.|

### <a name="remarks"></a>설명

이 메서드는 Windows SDK에 설명 된 [WM_NOTIFYFORMAT](/windows/win32/Controls/wm-notifyformat) 알림을 받습니다. WM_NOTIFY 메시지는 공통 컨트롤에서 부모 창으로, 부모 창에서 공통 컨트롤로 전송됩니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonpaint"></a><a name="onpaint"></a>CWnd::온페인트

Windows 또는 응용 프로그램이 응용 프로그램 창의 일부를 다시 그리도록 요청할 때 프레임워크는 이 멤버 함수를 호출합니다.

```
afx_msg void OnPaint();
```

### <a name="remarks"></a>설명

[WM_PAINT](/windows/win32/gdi/the-wm-paint-message) 업데이트 [창](#updatewindow) 또는 다시 [그리기 창](#redrawwindow) 멤버 함수를 호출 할 때 전송 됩니다.

창은 RDW_INTERNALPAINT 플래그가 설정된 멤버 함수를 `RedrawWindow` 호출한 결과로 내부 페인트 메시지를 받을 수 있습니다. 이 경우 창에 업데이트 영역이 없을 수 있습니다. 응용 프로그램은 [GetUpdateRect](#getupdaterect) 멤버 함수를 호출하여 창에 업데이트 영역이 있는지 확인해야 합니다. 0을 반환하는 경우 `GetUpdateRect` 응용 프로그램은 [BeginPaint](#beginpaint) 및 [EndPaint](#endpaint) 멤버 함수를 호출해서는 안 됩니다.

WM_PAINT 메시지가 잘못된 영역과 RDW_INTERNALPAINT 플래그 집합이 설정된 `RedrawWindow` 멤버 함수 호출로 인해 발생할 수 있으므로 각 WM_PAINT 메시지에 대한 내부 데이터 구조를 확인하여 필요한 내부 다시 그리기 또는 업데이트를 확인하는 것은 응용 프로그램의 책임입니다.

내부 WM_PAINT 메시지는 Windows에서 한 번만 전송됩니다. `UpdateWindow` 멤버 함수에서 내부 WM_PAINT 메시지를 창으로 보낸 후 창이 무효화되거나 RDW_INTERNALPAINT 플래그 가 설정된 `RedrawWindow` 멤버 함수가 다시 호출될 때까지 더 이상 WM_PAINT 메시지가 전송되거나 게시되지 않습니다.

문서/뷰 응용 프로그램에서 이미지 렌더링에 대한 자세한 내용은 [CView::OnDraw](../../mfc/reference/cview-class.md#ondraw)를 참조하십시오.

사용에 `WM_Paint`대한 자세한 내용은 Windows SDK의 다음 항목을 참조하십시오.

- [WM_PAINT 메시지](/windows/win32/gdi/the-wm-paint-message)

- [WM_PAINT 메시지 사용](/windows/win32/gdi/using-the-wm-paint-message)

## <a name="cwndonpaintclipboard"></a><a name="onpaintclipboard"></a>CWnd::온페인트 클립보드

클립보드 소유자가 클립보드 뷰어에 데이터를 CF_OWNERDISPLAY 형식으로 배치하고 클립보드 뷰어의 클라이언트 영역을 다시 페인팅해야 할 때 클립보드 소유자의 `OnPaintClipboard` 멤버 함수가 클립보드 뷰어에 의해 호출됩니다.

```
afx_msg void OnPaintClipboard(
    CWnd* pClipAppWnd,
    HGLOBAL hPaintStruct);
```

### <a name="parameters"></a>매개 변수

*pClipAppWnd*<br/>
클립보드 애플리케이션 창에 대한 포인터를 지정합니다. 해당 포인터는 임시적이며, 나중에 사용하려고 저장하면 안됩니다.

*hPaintStruct*<br/>
그릴 클라이언트 영역을 정의하는 [PAINTSTRUCT](/windows/win32/api/winuser/ns-winuser-paintstruct) 데이터 구조를 식별합니다.

### <a name="remarks"></a>설명

전체 클라이언트 영역 또는 일부만 다시 페인팅해야 하는지 여부를 확인하려면 클립보드 소유자는 `rcpaint` `PAINTSTRUCT` 구조체의 멤버에 지정된 드로잉 영역의 치수를 가장 최근의 [OnSizeClipboard](#onsizeclipboard) 멤버 함수 호출에 지정된 치수와 비교해야 합니다.

`OnPaintClipboard`[전역 잠금](/windows/win32/api/winbase/nf-winbase-globallock) Windows 함수를 사용하여 데이터 구조가 포함된 메모리를 `PAINTSTRUCT` 잠그고 종료하기 전에 [GlobalUnlock](/windows/win32/api/winbase/nf-winbase-globalunlock) Windows 함수를 사용하여 해당 메모리의 잠금을 해제해야 합니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonpalettechanged"></a><a name="onpalettechanged"></a>CWnd::온팔레트 변경

프레임워크는 입력 포커스가 있는 창이 논리적 팔레트를 실현한 후 모든 최상위 창에 대해 이 멤버 함수를 호출하여 시스템 팔레트를 변경합니다.

```
afx_msg void OnPaletteChanged(CWnd* pFocusWnd);
```

### <a name="parameters"></a>매개 변수

*p포커스Wnd*<br/>
시스템 팔레트가 변경된 창에 대한 포인터를 지정합니다. 포인터는 일시적일 수 있으며 저장해서는 안 됩니다.

### <a name="remarks"></a>설명

이 호출을 사용하면 색상 팔레트를 사용하는 입력 포커스가 없는 창을 사용하여 논리적 팔레트를 실현하고 클라이언트 영역을 업데이트할 수 있습니다.

멤버 `OnPaletteChanged` 함수는 시스템 팔레트를 변경하고 WM_PALETTECHANGED 메시지를 전송한 창을 포함하여 모든 최상위 및 중첩된 창에 대해 호출됩니다. 자식 창에서 색상 팔레트를 사용하는 경우 이 메시지가 전달되어야 합니다.

무한 루프를 피하기 위해 *pFocusWnd* 자체에 대한 포인터가 포함되어 있지 않다고 판단하지 않는 한 창은 해당 팔레트를 인식하지 못합니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonpaletteischanging"></a><a name="onpaletteischanging"></a>CWnd::온팔레트변경

프레임워크는 이 멤버 함수를 호출하여 응용 프로그램이 논리적 팔레트를 실현할 것임을 응용 프로그램에 알립니다.

```
afx_msg void OnPaletteIsChanging(CWnd* pRealizeWnd);
```

### <a name="parameters"></a>매개 변수

*pRealizeWnd*<br/>
논리적 팔레트를 실현하려고 하는 창을 지정합니다.

### <a name="remarks"></a>설명

이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonparentnotify"></a><a name="onparentnotify"></a>CWnd::에 부모 통지

부모의 `OnParentNotify` 멤버 함수는 하위 창이 만들어지거나 소멸될 때 또는 커서가 자식 창 위에 있는 동안 사용자가 마우스 단추를 클릭할 때 프레임워크에서 호출됩니다.

```
afx_msg void OnParentNotify(
    UINT message,
    LPARAM lParam);
```

### <a name="parameters"></a>매개 변수

*message*<br/>
부모에게 알림되는 이벤트와 하위 창의 식별자를 지정합니다. 이벤트는 *메시지의*낮은 순서 단어입니다. 이벤트가 WM_CREATE 또는 WM_DESTROY 경우 높은 순서의 *메시지* 단어는 자식 창의 식별자입니다. 그렇지 않으면 높은 순서의 단어가 정의되지 않습니다. 이벤트(메시지의 낮은 순서 *message*단어)는 다음 값 중 어느 중 일 수 있습니다.

- WM_CREATE 자식 창이 만들어지고 있습니다.

- WM_DESTROY 자식 창이 파괴되고 있습니다.

- WM_LBUTTONDOWN 사용자가 자식 창 위에 마우스 커서를 놓고 왼쪽 마우스 단추를 클릭했습니다.

- WM_MBUTTONDOWN 사용자가 자식 창 위에 마우스 커서를 놓고 가운데 마우스 단추를 클릭했습니다.

- WM_RBUTTONDOWN 사용자가 자식 창 위에 마우스 커서를 놓고 오른쪽 마우스 단추를 클릭했습니다.

*lParam*<br/>
*메시지의* 이벤트(낮은 차순 단어)가 WM_CREATE 또는 WM_DESTROY *경우 lParam은* 자식 창의 창 핸들을 지정합니다. 그렇지 않으면 *lParam커서의* x 및 y 좌표가 포함됩니다. x 좌표는 낮은 순서의 단어이고 y 좌표는 높은 순서의 단어에 있습니다.

### <a name="remarks"></a>설명

자식 창이 만들어지는 경우 `OnParentNotify` 시스템은 창을 만드는 [멤버 만들기](#create) 함수 바로 앞에서 호출합니다. 자식 창이 소멸되면 모든 처리가 수행되어 창을 파괴하기 전에 시스템이 호출됩니다. `OnParentNotify`

`OnParentNotify`최상위 창을 포함하여 하위 창의 모든 상위 창에 대해 호출됩니다.

[WS_EX_NOPARENTNOTIFY](../../mfc/reference/styles-used-by-mfc.md#extended-window-styles) 스타일이 있는 자식 창을 제외한 모든 자식 창은 이 메시지를 부모 창으로 보냅니다. 기본적으로 대화 상자의 자식 창은 [CreateEx](#createex) 멤버 함수를 호출하여 이 스타일 없이 자식 창을 만들지 않는 한 WS_EX_NOPARENTNOTIFY 스타일을 갖습니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonpowerbroadcast"></a><a name="onpowerbroadcast"></a>CWnd::온파워브로드캐스트

프레임워크는 전원 관리 이벤트가 발생할 때 이 멤버 함수를 호출합니다.

```
afx_msg UINT OnPowerBroadcast(
    UINT nPowerEvent,
    UINT nEventData);
```

### <a name="parameters"></a>매개 변수

|매개 변수|Description|
|---------------|-----------------|
|*n파워 이벤트*|【인】 전원 관리 이벤트입니다.|
|*n이벤트데이터*|【인】 이벤트별 데이터입니다.|

### <a name="return-value"></a>Return Value

이벤트가 요청인 경우 TRUE를 반환하여 요청을 부여하거나 요청을 거부하도록 BROADCAST_QUERY_DENY.

### <a name="remarks"></a>설명

이 메서드는 Windows SDK에 설명 된 [WM_POWERBROADCAST](/windows/win32/Power/wm-powerbroadcast) 메시지를 받습니다.

*nPowerEvent* 매개 변수는 배터리 전원이 부족하거나, 전원 상태가 변경되었거나, 작업 일시 중단 권한이 요청또는 거부됨, 이벤트 후 작업이 자동으로 재개됨, 시스템이 작동을 일시 중단하거나, 일시 중단 후 작업이 다시 시작되는 등의 이벤트를 지정합니다. *nEventData* 매개 변수는 일반적으로 사용 되지 않습니다. 자세한 내용은 [WM_POWERBROADCAST](/windows/win32/Power/wm-powerbroadcast) 메시지의 *wParam* 및 *lParam* 매개 변수를 참조하십시오.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonquerydragicon"></a><a name="onquerydragicon"></a>CWnd::OnQueryDragicon

프레임워크는 해당 클래스에 정의된 아이콘이 없는 최소화된(상징적) 창으로 이 멤버 함수를 호출합니다.

```
afx_msg HCURSOR OnQueryDragIcon();
```

### <a name="return-value"></a>Return Value

낮은 순서 단어의 커서 또는 아이콘 핸들을 포함하는 doubleword 값입니다. 커서 또는 아이콘은 디스플레이 드라이버의 해상도와 호환되어야 합니다. 응용 프로그램이 NULL을 반환하면 시스템이 기본 커서를 표시합니다. 기본 반환 값은 NULL입니다.

### <a name="remarks"></a>설명

사용자가 최소화된 창을 드래그하는 동안 표시할 커서를 얻기 위해 이 호출을 합니다. 응용 프로그램이 아이콘 또는 커서의 핸들을 반환하면 시스템에서 아이콘또는 커서의 핸들을 흑백으로 변환합니다. 응용 프로그램이 핸들을 반환하는 경우 핸들은 디스플레이 드라이버의 해상도와 호환되는 흑백 커서 또는 아이콘을 식별해야 합니다. 응용 프로그램은 CWinApp을 호출 할 수 있습니다 [::LoadCursor](../../mfc/reference/cwinapp-class.md#loadcursor) 또는 [CWinApp ::LoadIcon](../../mfc/reference/cwinapp-class.md#loadicon) 멤버는 실행 파일의 리소스에서 커서 또는 아이콘을로드하고이 핸들을 얻기 위해 함수.

## <a name="cwndonqueryendsession"></a><a name="onqueryendsession"></a>CWnd::OnQueryEndSession세션

프레임워크는 사용자가 Windows 세션을 종료하도록 선택하거나 응용 프로그램이 [ExitWindows](/windows/win32/api/winuser/nf-winuser-exitwindows) Windows 함수를 호출할 때 이 멤버 함수를 호출합니다.

```
afx_msg BOOL OnQueryEndSession();
```

### <a name="return-value"></a>Return Value

응용 프로그램을 편리하게 종료할 수 있는 경우 Nonzero; 그렇지 않으면 0.

### <a name="remarks"></a>설명

응용 프로그램이 0을 반환하면 Windows 세션이 종료되지 않습니다. Windows는 `OnQueryEndSession` 한 응용 프로그램이 0을 반환하는 즉시 호출을 중지하고 이미 0이 아닌 응용 프로그램에 대해 false의 매개 변수 값을 가진 [WM_ENDSESSION](#onendsession) 메시지를 보냅니다.

## <a name="cwndonquerynewpalette"></a><a name="onquerynewpalette"></a>CWnd::OnQuery뉴팔레트

프레임워크는 `CWnd` 개체가 입력 포커스를 수신하려고 할 때 이 `CWnd` 멤버 함수를 호출하여 포커스를 받을 때 논리적 팔레트를 실현할 수 있는 기회를 제공합니다.

```
afx_msg BOOL OnQueryNewPalette();
```

### <a name="return-value"></a>Return Value

비영이 `CWnd` 논리적 팔레트를 실현하는 경우; 그렇지 않으면 0.

## <a name="cwndonqueryopen"></a><a name="onqueryopen"></a>CWnd::온쿼리 열기

`CWnd` 프레임워크는 개체가 최소화되고 사용자가 미리 최소화된 크기와 `CWnd` 위치로 복원할 것을 요청할 때 이 멤버 함수를 호출합니다.

```
afx_msg BOOL OnQueryOpen();
```

### <a name="return-value"></a>Return Value

아이콘을 열 수 있는 경우 0이 아닌 경우 또는 0을 사용하여 아이콘이 열리지 않도록 합니다.

### <a name="remarks"></a>설명

에서 `OnQueryOpen`동안 `CWnd` 활성화 또는 포커스 변경을 일으킬 수 있는 작업을 수행 하지 않아야 합니다(예: 대화 상자 만들기).

## <a name="cwndonqueryuistate"></a><a name="onqueryuistate"></a>CWnd::온쿼리UI상태

창에 대한 UI(사용자 인터페이스) 상태를 검색 하기 위해 호출됩니다.

```
afx_msg UINT OnQueryUIState();
```

### <a name="return-value"></a>Return Value

포커스 표시기와 키보드 가속기가 표시되는 경우 반환 값은 NULL입니다. 그렇지 않으면 반환 값은 다음 값 중 하나 이상이 될 수 있습니다.

- UISF_HIDEFOCUS 포커스 표시등이 숨겨져 있습니다.

- UISF_HIDEACCEL 키보드 가속기가 숨겨져 있습니다.

- windows XP UISF_ACTIVE: 컨트롤은 활성 컨트롤에 사용되는 스타일로 그려야 합니다.

### <a name="remarks"></a>설명

이 멤버 함수는 Windows SDK에 설명된 대로 [WM_QUERYUISTATE](/windows/win32/menurc/wm-queryuistate) 메시지의 기능을 에뮬레이트합니다.

## <a name="cwndonrawinput"></a><a name="onrawinput"></a>CWnd::에로우 입력

프레임워크는 현재 창이 원시 입력을 받을 때 이 멤버 함수를 호출합니다.

```
afx_msg void OnRawInput(
    UINT nInputCode,
    HRAWINPUT hRawInput);
```

### <a name="parameters"></a>매개 변수

|매개 변수|Description|
|---------------|-----------------|
|*n입력 코드*|【인】 응용 프로그램이 포그라운드에 있는 동안 입력이 발생했는지 여부를 나타내는 입력 코드입니다. 두 경우 모두 응용 프로그램은 [CWnd::DefWindowProc를](#defwindowproc) 호출하여 시스템이 정리를 수행할 수 있도록 해야 합니다.<br /><br /> 이 매개 변수는 다음 값 중 하나일 수 있습니다.<br /><br /> - RIM_INPUT - 응용 프로그램이 전경에 있는 동안 입력이 발생했습니다.<br />- RIM_INPUTSINK - 응용 프로그램이 전경에 없는 동안 입력이 발생했습니다.|
|*hRaw입력*|【인】 장치의 원시 입력을 포함하는 [RAWINPUT](/windows/win32/api/winuser/ns-winuser-rawinput) 구조를 처리합니다.|

### <a name="remarks"></a>설명

이 메서드는 Windows SDK에 설명 된 [WM_INPUT](/windows/win32/inputdev/wm-appcommand) 알림을 받습니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonrbuttondblclk"></a><a name="onrbuttondblclk"></a>CWnd::OnRButtonDblk

사용자가 오른쪽 마우스 단추를 두 번 클릭하면 프레임워크가 이 멤버 함수를 호출합니다.

```
afx_msg void OnRButtonDblClk(
    UINT nFlags,
    CPoint point);
```

### <a name="parameters"></a>매개 변수

*nFlags*<br/>
다양한 가상 키가 다운되었는지 여부를 나타냅니다. 이 매개 변수는 다음 값의 조합일 수 있습니다.

- ctRL 키가 다운된 경우 MK_CONTROL 설정합니다.

- MK_LBUTTON 왼쪽 마우스 버튼이 다운된 경우 설정합니다.

- 가운데 마우스 버튼이 다운된 경우 MK_MBUTTON 설정합니다.

- 오른쪽 마우스 버튼이 다운된 경우 MK_RBUTTON 설정합니다.

- shift 키가 다운된 경우 MK_SHIFT 설정합니다.

*지점*<br/>
커서의 x 및 y 좌표를 지정합니다. 이러한 좌표는 항상 창의 왼쪽 위 모서리를 기준으로 합니다.

### <a name="remarks"></a>설명

[CS_DBLCLKS WNDCLASS](/windows/win32/api/winuser/ns-winuser-wndclassw) 스타일이 있는 창만 전화를 받을 `OnRButtonDblClk` 수 있습니다. Microsoft 재단 클래스 라이브러리 내의 창에 대한 기본값입니다. 사용자가 `OnRButtonDblClk` 시스템의 두 번 클릭 시간 제한 내에서 오른쪽 마우스 버튼을 누르고 다시 누르면 Windows가 호출됩니다. 오른쪽 마우스 단추를 두 번 클릭하면 [실제로 WM_RBUTTONDOWN](#onrbuttondown) 및 [WM_RBUTTONUP](#onrbuttonup) 메시지, `OnRButtonDblClk` 호출 및 단추를 해제할 때 다른 WM_RBUTTONUP 메시지의 네 가지 이벤트가 생성됩니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonrbuttondown"></a><a name="onrbuttondown"></a>CWnd:::온버튼다운

사용자가 오른쪽 마우스 단추를 누를 때 프레임워크는 이 멤버 함수를 호출합니다.

```
afx_msg void OnRButtonDown(
    UINT nFlags,
    CPoint point);
```

### <a name="parameters"></a>매개 변수

*nFlags*<br/>
다양한 가상 키가 다운되었는지 여부를 나타냅니다. 이 매개 변수는 다음 값의 조합일 수 있습니다.

- ctRL 키가 다운된 경우 MK_CONTROL 설정합니다.

- MK_LBUTTON 왼쪽 마우스 버튼이 다운된 경우 설정합니다.

- 가운데 마우스 버튼이 다운된 경우 MK_MBUTTON 설정합니다.

- 오른쪽 마우스 버튼이 다운된 경우 MK_RBUTTON 설정합니다.

- shift 키가 다운된 경우 MK_SHIFT 설정합니다.

*지점*<br/>
커서의 x 및 y 좌표를 지정합니다. 이러한 좌표는 항상 창의 왼쪽 위 모서리를 기준으로 합니다.

### <a name="remarks"></a>설명

이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonrbuttonup"></a><a name="onrbuttonup"></a>CWnd::OnRButtonUp

사용자가 오른쪽 마우스 단추를 해제할 때 프레임워크는 이 멤버 함수를 호출합니다.

```
afx_msg void OnRButtonUp(
    UINT nFlags,
    CPoint point);
```

### <a name="parameters"></a>매개 변수

*nFlags*<br/>
다양한 가상 키가 다운되었는지 여부를 나타냅니다. 이 매개 변수는 다음 값의 조합일 수 있습니다.

- ctRL 키가 다운된 경우 MK_CONTROL 설정합니다.

- MK_LBUTTON 왼쪽 마우스 버튼이 다운된 경우 설정합니다.

- 가운데 마우스 버튼이 다운된 경우 MK_MBUTTON 설정합니다.

- shift 키가 다운된 경우 MK_SHIFT 설정합니다.

*지점*<br/>
커서의 x 및 y 좌표를 지정합니다. 이러한 좌표는 항상 창의 왼쪽 위 모서리를 기준으로 합니다.

### <a name="remarks"></a>설명

이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonregisteredmousewheel"></a><a name="onregisteredmousewheel"></a>CWnd::등록 된 마우스 휠

프레임워크는 사용자가 마우스 휠을 회전하고 휠의 다음 노치를 만날 때 이 멤버 함수를 호출합니다.

```
afx_msg LRESULT OnRegisteredMouseWheel(
    WPARAM wParam,
    LPARAM lParam);
```

### <a name="parameters"></a>매개 변수

*wParam*<br/>
포인터의 수평 위치입니다.

*lParam*<br/>
포인터의 수직 위치입니다.

### <a name="return-value"></a>Return Value

이 시점에서 중요하지 않습니다. 항상 0입니다.

### <a name="remarks"></a>설명

재정의하지 않는 `OnRegisteredMouseWheel` 한 메시지를 적절한 창(포커스가 있는 부모 창)으로 라우팅하고 해당 창에 대한 [WM_MOUSEWHEEL](/windows/win32/inputdev/wm-mousewheel) 처리기를 호출합니다.

이 멤버 함수를 재정의하여 사용자 고유의 메시지 라우팅을 제공하거나 마우스 휠 스크롤 동작을 변경합니다.

> [!NOTE]
> `OnRegisteredMouseWheel`윈도우 95/98 및 윈도우 NT 3.51에 대한 메시지를 처리합니다. Windows NT 4.0 메시지 처리의 경우 [OnMouseWheel](#onmousewheel)을 사용합니다.

## <a name="cwndonrenderallformats"></a><a name="onrenderallformats"></a>CWnd::온렌더올포맷

소유자 응용 프로그램이 `OnRenderAllFormats` 소멸될 때 클립보드 소유자의 멤버 함수는 프레임워크에서 호출됩니다.

```
afx_msg void OnRenderAllFormats();
```

### <a name="remarks"></a>설명

클립보드 소유자는 [SetClipboardData](/windows/win32/api/winuser/nf-winuser-setclipboarddata) Windows 함수를 호출하여 각 형식에 대한 데이터 핸들을 생성하고 클립보드에 전달할 수 있는 모든 형식으로 데이터를 렌더링해야 합니다. 이렇게 하면 데이터를 렌더링한 응용 프로그램이 소멸되더라도 Clipboard에 유효한 데이터가 포함됩니다. 응용 프로그램은 [SetClipboardData Windows](/windows/win32/api/winuser/nf-winuser-setclipboarddata) 함수를 호출하기 전에 [OpenClipboard](#openclipboard) 멤버 함수를 호출하고 나중에 [CloseClipboard](/windows/win32/api/winuser/nf-winuser-closeclipboard) Windows 함수를 호출해야 합니다.

## <a name="cwndonrenderformat"></a><a name="onrenderformat"></a>CWnd::온렌더 형식

렌더링이 지연된 `OnRenderFormat` 특정 형식을 렌더링해야 하는 경우 클립보드 소유자의 멤버 함수가 프레임워크에서 호출됩니다.

```
afx_msg void OnRenderFormat(UINT nFormat);
```

### <a name="parameters"></a>매개 변수

*nFormat*<br/>
클립보드 형식을 지정합니다.

### <a name="remarks"></a>설명

수신기는 해당 형식으로 데이터를 렌더링하고 [SetClipboardData](/windows/win32/api/winuser/nf-winuser-setclipboarddata) Windows 함수를 호출하여 클립보드에 전달해야 합니다.

`OpenClipboard` 에서 멤버 함수 또는 `CloseClipboard` Windows 함수를 `OnRenderFormat`호출하지 마십시오.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonsessionchange"></a><a name="onsessionchange"></a>CWnd::OnSessionChange

프레임워크는 이 멤버 함수를 호출하여 세션 상태의 변경 사항을 응용 프로그램에 알립니다.

```
afx_msg void OnSessionChange(
    UINT nSessionState,
    UINT nId);
```

### <a name="parameters"></a>매개 변수

|매개 변수|Description|
|---------------|-----------------|
|*n세션 상태*|【인】 상태 코드는 세션 상태 변경을 설명합니다.|
|*Nid*|【인】 세션 식별자입니다.|

### <a name="remarks"></a>설명

이 메서드는 Windows SDK에 설명 된 [WM_WTSSESSION_CHANGE](/windows/win32/TermServ/wm-wtssession-change) 알림을 받습니다.

*nSessionState* 매개 변수는 세션이 콘솔 또는 원격 터미널에서 연결또는 연결이 끊어지거나, 사용자가 로그온 또는 오프하거나, 세션이 잠겨 있거나 잠금 해제되었거나, 세션이 원격 제어 상태로 변경되었음을 지정합니다. 자세한 내용은 [WM_WTSSESSION_CHANGE](/windows/win32/TermServ/wm-wtssession-change) 메시지의 *wParam* 매개 변수를 참조하십시오.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonsetcursor"></a><a name="onsetcursor"></a>CWnd::온셋커서

프레임워크는 마우스 입력이 캡처되지 않고 마우스가 개체 내에서 커서 `CWnd` 이동을 일으키는 경우 이 멤버 함수를 호출합니다.

```
afx_msg BOOL OnSetCursor(
    CWnd* pWnd,
    UINT nHitTest,
    UINT message);
```

### <a name="parameters"></a>매개 변수

*pWnd*<br/>
커서가 포함된 창에 대한 포인터를 지정합니다. 해당 포인터는 임시적이며, 나중에 사용하려고 저장하면 안됩니다.

*n히트 테스트*<br/>
[적중 테스트](#onnchittest) 영역 코드를 지정합니다. 적중 테스트는 커서의 위치를 결정합니다.

*message*<br/>
마우스 메시지 번호를 지정합니다.

### <a name="return-value"></a>Return Value

Nonzero는 추가 처리를 중단하거나 0을 계속합니다.

### <a name="remarks"></a>설명

기본 구현은 처리하기 전에 `OnSetCursor` 부모 창을 호출합니다. 부모 창이 TRUE를 반환하면 추가 처리가 중지됩니다. 상위 창을 호출하면 부모 창이 하위 창에서 커서 의 설정을 제어할 수 있습니다.

기본 구현은 커서가 클라이언트 영역에 없는 경우 화살표로 설정하거나 등록된 클래스 커서(있는 경우)로 설정합니다.

*nHitTest가* HTERROR이고 *메시지가* 마우스 단추 아래로 메시지인 경우 멤버 함수가 `MessageBeep` 호출됩니다.

*메뉴* 모드로 들어갈 `CWnd` 때 메시지 매개 변수는 0입니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonsetfocus"></a><a name="onsetfocus"></a>CWnd::온셋 포커스

프레임워크는 입력 포커스를 얻은 후 이 멤버 함수를 호출합니다.

```
afx_msg void OnSetFocus(CWnd* pOldWnd);
```

### <a name="parameters"></a>매개 변수

*올드Wnd*<br/>
입력 `CWnd` 포커스를 잃는 개체를 포함합니다(NULL일 수 있음). 해당 포인터는 임시적이며, 나중에 사용하려고 저장하면 안됩니다.

### <a name="remarks"></a>설명

카를 러를 `CWnd` 표시하려면 이 시점에서 적절한 카를트 함수를 호출해야 합니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonsettingchange"></a><a name="onsettingchange"></a>CWnd:::온세팅변경

Win32 `OnSettingChange` SystemParametersInfo 함수가 시스템 전체 설정을 변경할 때 프레임워크는 모든 최상위 창을 요구합니다.

```
afx_msg void OnSettingChange(
    UINT uFlags,
    LPCTSTR lpszSection);
```

### <a name="parameters"></a>매개 변수

*uFlags*<br/>
시스템에서 `SystemParametersInfo` 호출의 결과로 메시지를 보낼 때 이 매개 변수는 변경된 시스템 매개 변수를 나타내는 플래그입니다. 값 목록은 Windows SDK의 [SystemParametersInfo를](/windows/win32/api/winuser/nf-winuser-systemparametersinfow) 참조하십시오. 응용 프로그램이 메시지를 보낼 때 이 매개 변수는 0이어야 합니다.

*lpszSection*<br/>
변경된 섹션의 이름을 지정하는 문자열을 가리킵니다. (문자열에는 섹션 이름을 둘러싸는 대괄호가 포함되지 않습니다.)

### <a name="remarks"></a>설명

응용 프로그램은 시스템 매개 변수를 변경할 때 모든 최상위 창에 메시지를 보내야 하며 사용자가 제어판을 통해 설정을 변경하면 Windows에서 메시지를 보냅니다.

ON_WM_SETTINGCHANGE 메시지는 ON_WM_WININICHANGE 메시지와 유사하며 다음과 같은 차이가 있습니다.

- Windows NT 4.0 이상 또는 Windows 95/98에서 실행할 때 ON_WM_SETTINGCHANGE 사용합니다.

- Windows NT 3.51 이상 실행 시 ON_WININICHANGE 사용합니다. 이제 이 메시지는 더 이상 사용되지 않습니다.

메시지 맵에는 이러한 매크로 중 하나만 있어야 합니다. Windows 95/98 및 Windows NT 4.0모두에서 작동하는 프로그램을 작성하려면 ON_WM_SETTINGCHANGE 대한 처리기를 작성합니다. Windows NT 3.51에서 처리기는 `OnSettingChange` *uFlags에* 의해 호출되고 항상 0이 됩니다.

## <a name="cwndonshowwindow"></a><a name="onshowwindow"></a>CWnd::OnShowWindow

프레임워크는 개체가 숨주거나 표시될 때 이 멤버 함수를 `CWnd` 호출합니다.

```
afx_msg void OnShowWindow(
    BOOL bShow,
    UINT nStatus);
```

### <a name="parameters"></a>매개 변수

*bShow*<br/>
창이 표시되는지 여부를 지정합니다. 창이 표시되는 경우 TRUE입니다. 창이 숨겨져 있는 경우 FALSE입니다.

*n 상태*<br/>
표시되는 창의 상태를 지정합니다. 멤버 함수 호출로 인해 메시지가 `ShowWindow` 전송되는 경우 0입니다. 그렇지 않으면 *nStatus는* 다음 중 하나입니다.

- SW_PARENTCLOSING 부모 창이 닫히거나(상징적으로 설정됨) 팝업 창이 숨겨져 있습니다.

- SW_PARENTOPENING 부모 창이 열리거나(표시중) 팝업 창이 표시됩니다.

### <a name="remarks"></a>설명

`ShowWindow` 멤버 함수가 호출될 때, 겹친 창이 최대화또는 복원되거나, 겹치거나 팝업 창이 닫히거나(상징적으로 만들어) 열리거나(화면에 표시) 창이 숨겨지거나 표시됩니다. 겹친 창이 닫히면 해당 창과 연결된 모든 팝업 창이 숨겨지게 됩니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonsize"></a><a name="onsize"></a>CWnd::에 사이즈

프레임워크는 창의 크기가 변경된 후 이 멤버 함수를 호출합니다.

```
afx_msg void OnSize(
    UINT nType,
    int cx,
    int cy);
```

### <a name="parameters"></a>매개 변수

*nType*<br/>
요청된 크기 조정 유형을 지정합니다. 이 매개 변수는 다음 값 중 하나일 수 있습니다.

- SIZE_MAXIMIZED 창이 최대화되었습니다.

- SIZE_MINIMIZED 창이 최소화되었습니다.

- SIZE_RESTORED 창의 크기가 조정되었지만 SIZE_MINIMIZED SIZE_MAXIMIZED 적용되지 않습니다.

- SIZE_MAXHIDE 메시지는 다른 창이 최대화되면 모든 팝업 창으로 전송됩니다.

- SIZE_MAXSHOW 다른 창이 이전 크기로 복원되면 모든 팝업 창으로 메시지가 전송됩니다.

*Cx*<br/>
클라이언트 영역의 새 너비를 지정합니다.

*Cy*<br/>
클라이언트 영역의 새 높이를 지정합니다.

### <a name="remarks"></a>설명

에서 자식 창에 대해 [SetScrollPos](#setscrollpos) 또는 [MoveWindow](#movewindow) `bRedraw` 멤버 `SetScrollPos` 함수가 호출되는 경우 `MoveWindow` `CWnd` `OnSize`의 매개 변수또는 0이 아닌 매개 변수가 다시 그려지도록 해야 합니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#109](../../mfc/reference/codesnippet/cpp/cwnd-class_50.cpp)]

## <a name="cwndonsizeclipboard"></a><a name="onsizeclipboard"></a>CWnd::온사이즈 클립보드

클립보드 뷰어에 `OnSizeClipboard` `CF_OWNERDISPLAY` 속성이 포함된 데이터와 클립보드 뷰어 창의 클라이언트 영역 크기가 변경된 경우 클립보드 소유자의 멤버 함수가 클립보드 뷰어에서 호출됩니다.

```
afx_msg void OnSizeClipboard(
    CWnd* pClipAppWnd,
    HGLOBAL hRect);
```

### <a name="parameters"></a>매개 변수

*pClipAppWnd*<br/>
클립보드 응용 프로그램 창을 식별합니다. 포인터는 일시적일 수 있으며 저장해서는 안 됩니다.

*hRect*<br/>
전역 메모리 개체를 식별합니다. 메모리 개체에는 클립보드 소유자가 페인트할 영역을 지정하는 RECT 데이터 구조가 포함되어 있습니다.

### <a name="remarks"></a>설명

멤버 `OnSizeClipboard` 함수는 클립보드 응용 프로그램이 소멸되거나 최소화될 때 새 크기로 null 사각형(0,0,0,0)으로 호출됩니다. 이렇게 하면 클립보드 소유자가 디스플레이 리소스를 확보할 수 있습니다.

내에서 `OnSizeClipboard`응용 프로그램은 [GlobalLock](/windows/win32/api/winbase/nf-winbase-globallock) Windows 함수를 사용하여 RECT 데이터 구조를 포함하는 메모리를 잠그어야 합니다. 응용 프로그램이 제어를 생성하거나 반환하기 전에 [GlobalUnlock](/windows/win32/api/winbase/nf-winbase-globalunlock) Windows 함수를 통해 해당 메모리의 잠금을 해제합니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonsizing"></a><a name="onsizing"></a>CWnd::온사이징

프레임워크는 사용자가 사각형크기를 조정하고 있음을 나타내기 위해 이 멤버 함수를 호출합니다.

```
afx_msg void OnSizing(
    UINT nSide,
    LPRECT lpRect);
```

### <a name="parameters"></a>매개 변수

*nSide*<br/>
이동할 창의 모서리입니다.

*Lprect*<br/>
항목의 좌표를 포함하는 [CRect](../../atl-mfc-shared/reference/crect-class.md) 또는 [RECT 구조의](/windows/win32/api/windef/ns-windef-rect) 주소입니다.

### <a name="remarks"></a>설명

이 메시지를 처리하면 응용 프로그램은 끌기 사각형의 크기와 위치를 모니터링하고 필요한 경우 크기 또는 위치를 변경할 수 있습니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#110](../../mfc/reference/codesnippet/cpp/cwnd-class_51.cpp)]

## <a name="cwndonspoolerstatus"></a><a name="onspoolerstatus"></a>CWnd::온스풀러 상태

프레임워크는 작업이 인쇄 관리자 큐에 추가되거나 제거될 때마다 Print Manager에서 이 멤버 함수를 호출합니다.

```
afx_msg void OnSpoolerStatus(
    UINT nStatus,
    UINT nJobs);
```

### <a name="parameters"></a>매개 변수

*n 상태*<br/>
SP_JOBSTATUS 플래그를 지정합니다.

*n잡스*<br/>
인쇄 관리자 큐에 남아 있는 작업 수를 지정합니다.

### <a name="remarks"></a>설명

이 호출은 정보 정보를 위한 용도로만 사용됩니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonstylechanged"></a><a name="onstylechanged"></a>CWnd::온스타일 변경

[프레임워크는 SetWindowLong](/windows/win32/api/winuser/nf-winuser-setwindowlongw) 함수가 창 스타일 중 하나 이상을 변경한 후 이 멤버 함수를 호출합니다.

```
afx_msg void OnStyleChanged(
    int nStyleType,
    LPSTYLESTRUCT lpStyleStruct);
```

### <a name="parameters"></a>매개 변수

*n스타일타입*<br/>
창의 확장 스타일 또는 확장되지 않은 스타일이 변경되었는지 여부를 지정합니다. 이 매개 변수는 다음 값의 조합일 수 있습니다.

- GWL_EXSTYLE 창의 확장 된 스타일이 변경 되었습니다.

- GWL_STYLE 창의 확장되지 않은 스타일이 변경되었습니다.

*lp스타일스트럭터*<br/>
창에 대한 새 스타일이 포함된 [STYLESTRUCT](/windows/win32/api/winuser/ns-winuser-stylestruct) 구조를 가리킵니다. 응용 프로그램은 스타일을 검사할 수 있지만 스타일을 변경할 수는 없습니다.

### <a name="remarks"></a>설명

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonstylechanging"></a><a name="onstylechanging"></a>CWnd::온스타일 변경

[프레임워크는 SetWindowLong](/windows/win32/api/winuser/nf-winuser-setwindowlongw) 함수가 창의 스타일 중 하나 이상을 변경하려고 할 때 이 멤버 함수를 호출합니다.

```
afx_msg void OnStyleChanging(
    int nStyleType,
    LPSTYLESTRUCT lpStyleStruct);
```

### <a name="parameters"></a>매개 변수

*n스타일타입*<br/>
창의 확장 스타일 또는 확장되지 않은 스타일이 변경되었는지 여부를 지정합니다. 이 매개 변수는 다음 값의 조합일 수 있습니다.

- GWL_EXSTYLE 창의 확장 된 스타일이 변경 되었습니다.

- GWL_STYLE 창의 확장되지 않은 스타일이 변경되었습니다.

*lp스타일스트럭터*<br/>
창에 대한 새 스타일이 포함된 [STYLESTRUCT](/windows/win32/api/winuser/ns-winuser-stylestruct) 구조를 가리킵니다. 응용 프로그램은 스타일을 검사하고 변경할 수 있습니다.

### <a name="remarks"></a>설명

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonsyschar"></a><a name="onsyschar"></a>CWnd::온시차르

프레임워크는 입력 포커스가 `CWnd` 있고 [WM_SYSKEYUP](#onsyskeyup) 및 [WM_SYSKEYDOWN](#onsyskeydown) 메시지가 번역되는 경우 이 멤버 함수를 호출합니다.

```
afx_msg void OnSysChar(
    UINT nChar,
    UINT nRepCnt,
    UINT nFlags);
```

### <a name="parameters"></a>매개 변수

*Nchar*<br/>
컨트롤 메뉴 키의 ASCII 문자 키 코드를 지정합니다.

*nRepCnt*<br/>
반복 횟수를 지정합니다(사용자가 키를 누르고 있는 결과로 키 입력이 반복되는 횟수).

*nFlags*<br/>
*nFlags* 매개 변수에는 다음 값이 있을 수 있습니다.

|값|의미|
|-----------|-------------|
|0-15|반복 횟수를 지정합니다. 이 값은 사용자가 키를 누르고 있는 결과로 키 입력이 반복되는 횟수입니다.|
|16-23|검색 코드를 지정합니다. 값은 원래 장비 제조업체(OEM)에 따라 다릅니다.|
|24|키가 향상된 101키 또는 102키 키보드에 나타나는 오른쪽 ALT 및 CTRL 키와 같은 확장 키인지 여부를 지정합니다. 확장 키인 경우 값은 1입니다. 그렇지 않으면 0입니다.|
|25-28|Windows에서 내부적으로 사용됩니다.|
|29|컨텍스트 코드를 지정합니다. 값을 누르고 있는 동안 ALT 키가 누를 경우 값은 1입니다. 그렇지 않으면 값은 0입니다.|
|30|이전 키 상태를 지정합니다. 메시지가 전송되기 전에 키가 다운된 경우 값이 1이거나 키가 위인 경우 0입니다.|
|31|전환 상태를 지정합니다. 키가 해제되는 경우 값은 1이거나 키를 누를 경우 0입니다.|

### <a name="remarks"></a>설명

컨트롤 메뉴 키의 가상 키 코드를 지정합니다. (표준 가상 키 코드 목록은 Winuser.h 를 참조하십시오.)

컨텍스트 코드가 0이면 WM_SYSCHAR [WM_SYSCHAR](/windows/win32/menurc/wm-syschar) 메시지를 [TranslateAccelerator](/windows/win32/api/winuser/nf-winuser-translateacceleratorw) Windows 함수에 전달할 수 있으며, 이 메시지는 시스템 문자 키 대신 일반 키 메시지인 것처럼 처리됩니다. 이렇게 하면 활성 창에 입력 포커스가 없는 경우에도 액셀러레이터 키를 활성 창과 함께 사용할 수 있습니다.

IBM 향상된 101키 및 102키 키보드의 경우 향상된 키는 올바른 ALT이며 키보드의 기본 섹션에 있는 올바른 CTRL 키입니다. 숫자 키패드의 왼쪽에 있는 클러스터의 INS, DEL, HOME, END, 페이지 업, 페이지 다운 및 화살표 키; 및 숫자 키패드의 슬래시(/) 및 ENTER 키입니다. 일부 다른 키보드는 *nFlags에서*확장 키 비트를 지원할 수 있습니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonsyscolorchange"></a><a name="onsyscolorchange"></a>CWnd::OnsysColor체인지

프레임워크는 시스템 색상 설정에서 변경될 때 모든 최상위 창에 대해 이 멤버 함수를 호출합니다.

```
afx_msg void OnSysColorChange();
```

### <a name="remarks"></a>설명

Windows에서 `OnSysColorChange` 시스템 색상 변경의 영향을 받는 모든 창을 호출합니다.

기존 시스템 색상을 사용하는 브러시가 있는 응용 프로그램은 해당 브러시를 삭제하고 새 시스템 색상으로 다시 만들어야 합니다.

## <a name="cwndonsyscommand"></a><a name="onsyscommand"></a>CWnd::OnsysCommand

프레임워크는 사용자가 제어 메뉴에서 명령을 선택하거나 최대화 또는 최소화 단추를 선택할 때 이 멤버 함수를 호출합니다.

```
afx_msg void OnSysCommand(
    UINT nID,
    LPARAM lParam);
```

### <a name="parameters"></a>매개 변수

*nID*<br/>
요청된 시스템 명령의 유형을 지정합니다. 이 매개 변수는 다음 값 중 하나일 수 있습니다.

- SC_CLOSE 개체를 `CWnd` 닫습니다.

- SC_HOTKEY 응용 `CWnd` 프로그램 지정 단축키와 연결된 개체를 활성화합니다. *lParam의* 낮은 순서 단어는 활성화할 창의 HWND를 식별합니다.

- SC_HSCROLL 가로로 스크롤합니다.

- SC_KEYMENU 키 입력을 통해 메뉴를 검색합니다.

- SC_MAXIMIZE(또는 SC_ZOOM) `CWnd` 개체를 최대화합니다.

- SC_MINIMIZE(또는 SC_ICON) `CWnd` 개체를 최소화합니다.

- SC_MOUSEMENU 마우스 클릭으로 메뉴를 검색합니다.

- SC_MOVE 오브젝트를 이동합니다. `CWnd`

- SC_NEXTWINDOW 다음 창으로 이동합니다.

- SC_PREVWINDOW 이전 창으로 이동합니다.

- SC_RESTORE 창을 정상 위치 및 크기로 복원합니다.

- SC_SCREENSAVE SYSTEM의 [부팅] 섹션에 지정된 화면 보호기 응용 프로그램을 실행합니다. INI 파일입니다.

- SC_SIZE 개체의 크기를 조정합니다. `CWnd`

- SC_TASKLIST 실행하거나 Windows 작업 관리자 응용 프로그램을 활성화합니다.

- SC_VSCROLL 세로로 스크롤합니다.

*lParam*<br/>
컨트롤 메뉴 명령을 마우스로 선택한 경우 *lParam에는* 커서 좌표가 포함됩니다. 하위 순서 단어는 x 좌표를 포함하고 고차 단어에는 y 좌표가 포함됩니다. 그렇지 않으면 이 매개 변수가 사용되지 않습니다.

- SC_HOTKEY 응용 프로그램 지정 단축키와 연결된 창을 활성화합니다. *lParam의* 낮은 순서 단어는 활성화할 창을 식별합니다.

- SC_SCREENSAVE 제어판의 데스크톱 섹션에 지정된 화면 저장 응용 프로그램을 실행합니다.

### <a name="remarks"></a>설명

기본적으로 이전 `OnSysCommand` 표에 지정된 미리 정의된 작업에 대한 컨트롤 메뉴 요청을 수행합니다.

WM_SYSCOMMAND 메시지에서 *nID* 매개 변수의 네 개의 낮은 순서 비트는 Windows에서 내부적으로 사용됩니다. 응용 프로그램이 *nID*값을 테스트할 때 올바른 결과를 얻으려면 bitwise-AND 연산자를 사용하여 값 0xFFF0을 *nID* 값과 결합해야 합니다.

컨트롤 메뉴의 메뉴 항목은 `GetSystemMenu`" `AppendMenu` `InsertMenu`및 `ModifyMenu` 멤버 함수로 수정할 수 있습니다. 컨트롤 메뉴를 수정하는 응용 프로그램은 WM_SYSCOMMAND 메시지를 처리해야 하며 응용 프로그램에서 처리하지 `OnSysCommand`않는 WM_SYSCOMMAND 메시지는 에 전달되어야 합니다. 응용 프로그램에서 추가한 모든 명령 값은 응용 프로그램에서 처리해야 하며 `OnSysCommand`에 전달할 수 없습니다.

응용 프로그램은 WM_SYSCOMMAND 메시지를 에 전달하여 언제든지 모든 `OnSysCommand`시스템 명령을 수행할 수 있습니다.

컨트롤 메뉴에서 항목을 선택하도록 정의된 가속기(바로 가기) 키 입력은 호출로 `OnSysCommand` 변환됩니다. 다른 모든 가속기 키 입력은 [WM_COMMAND](#oncommand) 메시지로 변환됩니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonsysdeadchar"></a><a name="onsysdeadchar"></a>CWnd::온시스데드차

`CWnd` [프레임워크는 OnSysKeyUp 또는 OnSysKeyDown](#onsyskeyup) 멤버 함수가 호출될 때 개체에 입력 [포커스가](#onsyskeydown) 있는 경우 이 멤버 함수를 호출합니다.

```
afx_msg void OnSysDeadChar(
    UINT nChar,
    UINT nRepCnt,
    UINT nFlags);
```

### <a name="parameters"></a>매개 변수

*Nchar*<br/>
데드 키 문자 값을 지정합니다.

*nRepCnt*<br/>
반복 횟수를 지정합니다.

*nFlags*<br/>
다음 목록과 같이 검색 코드, 키 전환 코드, 이전 키 상태 및 컨텍스트 코드를 지정합니다.

|값|의미|
|-----------|-------------|
|0-7|스캔 코드(OEM 종속 값). 높은 순서 단어의 낮은 바이트입니다.|
|8|함수 키 또는 숫자 키패드의 키와 같은 확장 키(확장 키인 경우 1, 그렇지 않으면 0).|
|9-10|사용되지 않습니다.|
|11-12|Windows에서 내부적으로 사용됩니다.|
|13|컨텍스트 코드(키를 누른 상태에서 ALT 키가 누수된 경우 1, 그렇지 않으면 0).|
|14|이전 키 상태(1키가 호출 되기 전에 다운된 경우, 키가 위태인 경우 0).|
|15|전환 상태(키가 해제되는 경우 1, 키를 누르는 경우 0).|

### <a name="remarks"></a>설명

죽은 키의 문자 값을 지정합니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonsyskeydown"></a><a name="onsyskeydown"></a>CWnd::온시스키다운

개체에 `CWnd` 입력 포커스가 있는 `OnSysKeyDown` 경우 사용자가 ALT 키를 누른 다음 다른 키를 누를 때 멤버 함수가 프레임워크에서 호출됩니다.

```
afx_msg void OnSysKeyDown(
    UINT nChar,
    UINT nRepCnt,
    UINT nFlags);
```

### <a name="parameters"></a>매개 변수

*Nchar*<br/>
누는 키의 가상 키 코드를 지정합니다. 표준 가상 키 코드 목록은 Winuser.h를 참조하십시오.

*nRepCnt*<br/>
반복 횟수를 지정합니다.

*nFlags*<br/>
다음 목록과 같이 검색 코드, 키 전환 코드, 이전 키 상태 및 컨텍스트 코드를 지정합니다.

|값|의미|
|-----------|-------------|
|0-7|스캔 코드(OEM 종속 값). 높은 순서 단어의 낮은 바이트입니다.|
|8|함수 키 또는 숫자 키패드의 키와 같은 확장 키(확장 키인 경우 1, 그렇지 않으면 0).|
|9-10|사용되지 않습니다.|
|11-12|Windows에서 내부적으로 사용됩니다.|
|13|컨텍스트 코드(1ALT 키가 누른 상태에서 키를 누른 경우, 0)|
|14|이전 키 상태(메시지를 보내기 전에 키가 다운된 경우 1, 키가 위업인 경우 0).|
|15|전환 상태(키가 해제되는 경우 1, 키를 누르는 경우 0).|

호출의 경우 `OnSysKeyDown` 키 전환 비트(비트 15)는 0입니다. 컨텍스트 코드 비트(비트 13)는 키를 누른 동안 ALT 키가 다운된 경우 1입니다. 입력 포커스가 없는 창이 없기 때문에 메시지가 활성 창으로 전송되는 경우 0입니다.

### <a name="remarks"></a>설명

현재 입력 포커스가 있는 창이 없는 `OnSysKeyDown` 경우 활성 창의 멤버 함수가 호출됩니다. 메시지를 `CWnd` 수신하는 개체는 에서 컨텍스트 코드를 확인하여 이러한 `nFlags`두 컨텍스트를 구분할 수 있습니다.

컨텍스트 코드가 0이면 수신된 WM_SYSKEYDOWN `OnSysKeyDown` 메시지가 [TranslateAccelerator](/windows/win32/api/winuser/nf-winuser-translateacceleratorw) Windows 함수로 전달될 수 있으며, 이 함수는 시스템 키 메시지 대신 일반 키 메시지인 것처럼 처리됩니다. 이렇게 하면 활성 창에 입력 포커스가 없는 경우에도 액셀러레이터 키를 활성 창과 함께 사용할 수 있습니다.

자동 반복으로 인해 [WM_SYSKEYUP](#onsyskeyup) `OnSysKeyDown` 메시지가 수신되기 전에 두 개 이상의 호출이 발생할 수 있습니다. 이전 키 상태(비트 14)를 사용하여 호출이 `OnSysKeyDown` 첫 번째 다운 전환 또는 반복 다운 전환을 나타내는지 여부를 확인할 수 있습니다.

IBM 향상된 101키 및 102키 키보드의 경우 향상된 키는 올바른 ALT이며 키보드의 기본 섹션에 있는 올바른 CTRL 키입니다. 숫자 키패드의 왼쪽에 있는 클러스터의 INS, DEL, HOME, END, 페이지 업, 페이지 다운 및 화살표 키; 및 숫자 키패드의 슬래시(/) 및 ENTER 키입니다. 일부 다른 키보드는 *nFlags에서*확장 키 비트를 지원할 수 있습니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonsyskeyup"></a><a name="onsyskeyup"></a>CWnd::OnsysKeyUp

개체에 `CWnd` 포커스가 있는 `OnSysKeyUp` 경우 사용자가 ALT 키를 누른 상태에서 누른 키를 해제할 때 프레임워크에서 멤버 함수가 호출됩니다.

```
afx_msg void OnSysKeyUp(
    UINT nChar,
    UINT nRepCnt,
    UINT nFlags);
```

### <a name="parameters"></a>매개 변수

*Nchar*<br/>
누는 키의 가상 키 코드를 지정합니다. 표준 가상 키 코드 목록은 Winuser.h를 참조하십시오.

*nRepCnt*<br/>
반복 횟수를 지정합니다.

*nFlags*<br/>
다음 목록과 같이 검색 코드, 키 전환 코드, 이전 키 상태 및 컨텍스트 코드를 지정합니다.

|값|의미|
|-----------|-------------|
|0-7|스캔 코드(OEM 종속 값). 높은 순서 단어의 낮은 바이트입니다.|
|8|함수 키 또는 숫자 키패드의 키와 같은 확장 키(확장 키인 경우 1, 그렇지 않으면 0).|
|9-10|사용되지 않습니다.|
|11-12|Windows에서 내부적으로 사용됩니다.|
|13|컨텍스트 코드(1ALT 키가 누른 상태에서 키를 누른 경우, 0)|
|14|이전 키 상태(메시지를 보내기 전에 키가 다운된 경우 1, 키가 위업인 경우 0).|
|15|전환 상태(키가 해제되는 경우 1, 키를 누르는 경우 0).|

통화의 경우 `OnSysKeyUp` 키 전환 비트(비트 15)는 1입니다. 컨텍스트 코드 비트(비트 13)는 키를 누른 동안 ALT 키가 다운된 경우 1입니다. 입력 포커스가 없는 창이 없기 때문에 메시지가 활성 창으로 전송되는 경우 0입니다.

### <a name="remarks"></a>설명

현재 입력 포커스가 있는 창이 없는 `OnSysKeyUp` 경우 활성 창의 멤버 함수가 호출됩니다. 호출을 받는 개체는 `CWnd` *nFlags*에서 컨텍스트 코드를 확인 하여 이러한 두 컨텍스트를 구분할 수 있습니다.

컨텍스트 코드가 0이면 수신된 WM_SYSKEYUP `OnSysKeyUp` 메시지가 [TranslateAccelerator](/windows/win32/api/winuser/nf-winuser-translateacceleratorw) Windows 함수로 전달될 수 있으며, 이 함수는 시스템 키 메시지 대신 일반 키 메시지인 것처럼 처리됩니다. 이렇게 하면 활성 창에 입력 포커스가 없는 경우에도 가속기(바로 가기) 키를 활성 창과 함께 사용할 수 있습니다.

IBM 향상된 101키 및 102키 키보드의 경우 향상된 키는 올바른 ALT이며 키보드의 기본 섹션에 있는 올바른 CTRL 키입니다. 숫자 키패드의 왼쪽에 있는 클러스터의 INS, DEL, HOME, END, 페이지 업, 페이지 다운 및 화살표 키; 및 숫자 키패드의 슬래시(/) 및 ENTER 키입니다. 일부 다른 키보드는 *nFlags에서*확장 키 비트를 지원할 수 있습니다.

미국 이외의 고급 102키 키보드의 경우 올바른 ALT 키가 CTRL+ALT 키 조합으로 처리됩니다. 다음은 사용자가 이 키를 누르고 해제할 때 발생하는 메시지 및 호출 의 순서를 보여 주며 있습니다.

|시퀀스|함수 액세스|전달된 메시지|
|--------------|-----------------------|--------------------|
|1.|[WM_KEYDOWN](#onkeydown)|VK_CONTROL|
|2.|[WM_KEYDOWN](#onkeydown)|VK_MENU|
|3.|[WM_KEYUP](#onkeyup)|VK_CONTROL|
|4.|[WM_SYSKEYUP](/windows/win32/inputdev/wm-syskeyup)|VK_MENU|

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndontcard"></a><a name="ontcard"></a>CWnd::온카드

프레임워크는 사용자가 작성 가능한 단추를 클릭할 때 이 멤버 함수를 호출합니다.

```
afx_msg void OnTCard(
    UINT idAction,
    DWORD dwActionData);
```

### <a name="parameters"></a>매개 변수

*idAction*<br/>
사용자가 수행한 작업을 나타냅니다. 이 매개 변수는 다음 값 중 하나일 수 있습니다.

- IDABORT 사용자가 작성 가능한 중단 단추를 클릭했습니다.

- IDCANCEL 사용자가 작성 가능한 취소 단추를 클릭했습니다.

- IDCLOSE 사용자가 교육 카드를 닫았습니다.

- ID도움말 사용자가 작성 가능한 Windows 도움말 단추를 클릭했습니다.

- IDIGNORE 사용자가 작성 가능한 무시 단추를 클릭했습니다.

- IDOK 사용자가 작성 가능한 확인 단추를 클릭했습니다.

- IDNO 사용자가 작성 할 수없는 단추를 클릭했습니다.

- IDRETRY 사용자가 작성 가능한 재시도 단추를 클릭했습니다.

- HELP_TCARD_DATA 사용자가 작성 가능한 단추를 클릭했습니다. *dwActionData* 매개 변수에는 도움말 작성자가 지정한 긴 정수가 포함되어 있습니다.

- HELP_TCARD_NEXT 사용자가 작성 가능한 다음 단추를 클릭했습니다.

- HELP_TCARD_OTHER_CALLER 다른 응용 프로그램이 교육 카드를 요청했습니다.

- IDYES 사용자가 작성 가능한 예 단추를 클릭했습니다.

*dwActionData*<br/>
*idAction이* HELP_TCARD_DATA 지정하는 경우 이 매개 변수는 도움말 작성자가 지정한 긴 정수입니다. 그렇지 않으면 이 매개 변수는 0입니다.

### <a name="remarks"></a>설명

이 함수는 응용 프로그램이 Windows 도움말을 통해 교육 카드를 시작한 경우에만 호출됩니다. 응용 프로그램은 [WinHelp](../../mfc/reference/cwinapp-class.md#winhelp) 함수에 대한 호출에서 HELP_TCARD 명령을 지정하여 교육 카드를 시작합니다.

## <a name="cwndontimechange"></a><a name="ontimechange"></a>CWnd::온타임체인지

프레임워크는 시스템 시간이 변경된 후 이 멤버 함수를 호출합니다.

```
afx_msg void OnTimeChange();
```

### <a name="remarks"></a>설명

시스템 시간을 변경하는 모든 응용 프로그램이 이 메시지를 모든 최상위 창으로 보냅니다. WM_TIMECHANGE 메시지를 모든 최상위 창으로 보내려면 응용 프로그램은 *hwnd* 매개 변수를 HWND_BROADCAST 설정된 [SendMessage](/windows/win32/api/winuser/nf-winuser-sendmessage) Windows 함수를 사용할 수 있습니다.

## <a name="cwndontimer"></a><a name="ontimer"></a>CWnd::온타이머

프레임워크는 타이머를 설치하는 데 사용되는 [SetTimer](#settimer) 멤버 함수에 지정된 각 간격 이후에 이 멤버 함수를 호출합니다.

```
afx_msg void OnTimer(UINT_PTR nIDEvent);
```

### <a name="parameters"></a>매개 변수

*니디이벤트*<br/>
타이머의 식별자를 지정합니다.

### <a name="remarks"></a>설명

[DispatchMessage](/windows/win32/api/winuser/nf-winuser-dispatchmessage) Windows 함수는 응용 프로그램의 메시지 큐에 다른 메시지가 없을 때 [WM_TIMER](/windows/win32/winmsg/wm-timer) 메시지를 보냅니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

### <a name="example"></a>예제

  [CWnd::SetTimer의](#settimer)예제를 참조하십시오.

## <a name="cwndontoolhittest"></a><a name="ontoolhittest"></a>CWnd::OnToolHitTest

프레임워크는 이 멤버 함수를 호출하여 지정된 도구의 경계 사각형에 점이 있는지 확인합니다.

```
virtual INT_PTR OnToolHitTest(
    CPoint point,
    TOOLINFO* pTI) const;
```

### <a name="parameters"></a>매개 변수

*지점*<br/>
커서의 x- 및 y 좌표를 지정합니다. 이러한 좌표는 항상 창의 왼쪽 위 모서리를 기준으로 합니다.

*Pti*<br/>
[TOOLINFO](/windows/win32/api/commctrl/ns-commctrl-tttoolinfoa) 구조에 대한 포인터입니다. 다음 구조 값은 기본적으로 설정됩니다.

- *창에 핸들을 hwnd*  =  `m_hWnd`

- *하위*  =  `(UINT)hWndChild` 창에 대한 uId 핸들

- *uFlags* &#124;= TTF_IDISHWND 도구 핸들

- *lpszText* = 지정된 창에 표시할 문자열에 대한 LPSTR_TEXTCALLBACK 포인터

### <a name="return-value"></a>Return Value

도구 설명 컨트롤이 발견되면 창 컨트롤 ID입니다. 도구 설명 컨트롤을 찾을 수 없는 경우 -1입니다.

### <a name="remarks"></a>설명

점이 사각형에 있으면 도구에 대한 정보를 검색합니다.

도구 설명이 연결된 영역이 단추가 아닌 `OnToolHitTest` 경우 구조 플래그를 TTF_NOTBUTTON TTF_CENTERTIP 설정합니다.

기본값에서 제공하는 것과 다른 정보를 제공하기 위해 재정의합니다. `OnToolHitTest`

구조에 대한 자세한 내용은 Windows SDK에서 [TOOLINFO를](/windows/win32/api/commctrl/ns-commctrl-tttoolinfoa)참조하십시오.

## <a name="cwndontouchinput"></a><a name="ontouchinput"></a>CWnd::온터치 입력

Windows Touch에서 단일 입력을 처리합니다.

```
virtual BOOL OnTouchInput(
    CPoint pt,
    int nInputNumber,
    int nInputsCount,
    PTOUCHINPUT pInput);
```

### <a name="parameters"></a>매개 변수

*pt*<br/>
클라이언트 좌표에서 화면이 터치된 지점을 가리킵니다.

*n입력번호*<br/>
터치 입력 수입니다.

*n입력카운트*<br/>
총 터치 입력 수입니다.

*p입력*<br/>
터치 입력 구조에 대한 포인터입니다.

### <a name="return-value"></a>Return Value

TRUE 응용 프로그램이 윈도우 터치 입력을 처리하는 경우; 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

## <a name="cwndontouchinputs"></a><a name="ontouchinputs"></a>CWnd::OnTouch입력

Windows 터치에서 입력을 처리합니다.

```
virtual BOOL OnTouchInputs(
    UINT nInputsCount,
    PTOUCHINPUT pInputs);
```

### <a name="parameters"></a>매개 변수

*n입력카운트*<br/>
총 Windows 터치 입력 수입니다.

*p입력*<br/>
터치 입력의 배열.

### <a name="return-value"></a>Return Value

TRUE 응용 프로그램이 윈도우 터치 입력을 처리하는 경우; 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

## <a name="cwndonunichar"></a><a name="onunichar"></a>CWnd::OnUniChar

프레임워크는 키를 누를 때 이 멤버 함수를 호출합니다. 즉, 현재 창에는 키보드 포커스가 있고 [WM_KEYDOWN](/windows/win32/inputdev/wm-keydown) 메시지가 [TranslateMessage](/windows/win32/api/winuser/nf-winuser-translatemessage) 기능으로 변환됩니다.

```
afx_msg void OnUniChar(
    UINT nChar,
    UINT nRepCnt,
    UINT nFlags);
```

### <a name="parameters"></a>매개 변수

|매개 변수|Description|
|---------------|-----------------|
|*Nchar*|【인】 누른 키의 문자 코드를 지정합니다.|
|*nRepCnt*|【인】 현재 메시지에 대한 반복 수를 지정합니다. 이 값은 사용자가 키를 누를 때 키 입력이 자동으로 반복되는 횟수입니다. 키 입력이 충분히 오래 유지되면 여러 메시지가 전송됩니다. 그러나 반복 수는 누적되지 않습니다.|
|*nFlags*|【인】 다음 표와 같이 검색 코드, 확장 키, 컨텍스트 코드, 이전 키 상태 및 전환 상태를 지정하는 플래그는 다음과 같습니다.<br /><br /> **0-7:** 검색 코드를 지정합니다. 값은 원래 장비 제조업체(OEM)에 따라 다릅니다.<br /><br /> **8:** 향상된 101 키 또는 102키 키보드에 나타나는 오른쪽 ALT 및 CTRL 키와 같은 확장 키를 지정합니다. 키가 확장 키인 경우 플래그는 1입니다. 그렇지 않으면 0입니다.<br /><br /> **9-12:**  Windows에서 내부적으로 사용됩니다.<br /><br /> **13:**  컨텍스트 코드를 지정합니다. 키를 누를 때 ALT 키가 누른 경우 플래그는 1입니다. 그렇지 않으면 값은 0입니다.<br /><br /> **14:**  이전 키 상태를 지정합니다. 메시지를 보내기 전에 키가 다운된 경우 플래그는 1, 키가 켜진 경우 0입니다.<br /><br /> **15:**  전환 상태를 지정합니다. 키가 해제되는 경우 플래그는 1, 키를 누르는 경우 0입니다.|

### <a name="remarks"></a>설명

이 메서드는 Windows SDK에 설명 된 [WM_UNICHAR](/windows/win32/inputdev/wm-unichar) 알림을 받습니다. [WM_UNICHAR](/windows/win32/inputdev/wm-unichar) 메시지는 ANSI 창에 유니코드 문자를 보내거나 우편으로 보내도록 설계되었습니다. [WM_CHAR](/windows/win32/inputdev/wm-char) 메시지와 동일하지만 유니코드 변환 형식-32 인코딩(UTF-32)을 사용하는 반면 [WM_CHAR](/windows/win32/inputdev/wm-char) 메시지는 UTF-16을 사용합니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonuninitmenupopup"></a><a name="onuninitmenupopup"></a>CWnd:::OnUnInitMenuPopup

드롭다운 메뉴 또는 하위 메뉴가 소멸되었을 때 프레임워크는 이 멤버 함수를 호출합니다.

```
afx_msg void OnUnInitMenuPopup(
    CMenu* pPopupMenu,
    UINT nFlags);
```

### <a name="parameters"></a>매개 변수

|매개 변수|Description|
|---------------|-----------------|
|*p메뉴*|【인】 메뉴 또는 하위 메뉴를 나타내는 [CMenu](../../mfc/reference/cmenu-class.md) 개체에 대한 포인터입니다.|
|*nFlags*|【인】 파괴된 메뉴입니다. 현재는 MF_SYSMENU 창 메뉴일 수 있습니다.|

### <a name="remarks"></a>설명

이 메서드는 Windows SDK에 설명 된 [WM_UNINITMENUPOPUP](/windows/win32/menurc/wm-uninitmenupopup) 알림을 받습니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonupdateuistate"></a><a name="onupdateuistate"></a>CWnd::OnUpdateUI스테이트

지정된 창 및 모든 자식 창에 대한 UI(사용자 인터페이스) 상태를 변경하기 위해 호출됩니다.

```
afx_msg void OnUpdateUIState(
    UINT nAction,
    UINT nUIElement);
```

### <a name="parameters"></a>매개 변수

*nAction*<br/>
수행할 작업을 지정합니다. 다음 값 중 하나를 사용할 수 있습니다.

- UIS_CLEAR ui 상태 *요소(nUIElement에*의해 지정)는 숨춰져야 합니다.

- UIS_INITIALIZE *ui Element에*의해 지정 된 UI 상태 요소 마지막 입력 이벤트에 따라 변경 해야 합니다. 자세한 내용은 [WM_UPDATEISTATE](/windows/win32/menurc/wm-updateuistate) **의 비고** 섹션을 참조하십시오.

- UIS_SET ui 상태 *요소(nUIElement로*지정)가 표시되어야 합니다.

*누이 엘리먼트*<br/>
영향을 받는 UI 상태 요소 또는 컨트롤의 스타일을 지정합니다. 다음 값 중 하나를 사용할 수 있습니다.

- UISF_HIDEACCEL 키보드 가속기.

- UISF_HIDEFOCUS 초점 지표.

- windows XP UISF_ACTIVE: 컨트롤은 활성 컨트롤에 사용되는 스타일로 그려야 합니다.

### <a name="remarks"></a>설명

이 멤버 함수는 Windows SDK에 설명된 대로 [WM_UPDATEUISTATE](/windows/win32/menurc/wm-updateuistate) 메시지의 기능을 에뮬레이트합니다.

## <a name="cwndonuserchanged"></a><a name="onuserchanged"></a>CWnd::OnUser변경

프레임워크는 사용자가 로그온 또는 로그오프한 후 모든 창에 대해 이 멤버를 호출합니다.

```
afx_msg void OnUserChanged();
```

### <a name="remarks"></a>설명

이 메서드는 Windows SDK에 설명 된 [WM_USERCHANGED](/windows/win32/winmsg/wm-userchanged) 알림 메시지를 받습니다. 사용자가 로그온하거나 꺼지면 운영 체제가 사용자별 설정을 업데이트합니다. 시스템은 설정을 업데이트한 직후 이 메시지를 보냅니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonvkeytoitem"></a><a name="onvkeytoitem"></a>CWnd::OnVKeyToItem

개체가 `CWnd` [LBS_WANTKEYBOARDINPUT](../../mfc/reference/styles-used-by-mfc.md#list-box-styles) 스타일로 목록 상자를 소유하는 경우 목록 상자는 WM_KEYDOWN 메시지에 대한 응답으로 WM_VKEYTOITEM 메시지를 보냅니다.

```
afx_msg int OnVKeyToItem(
    UINT nKey,
    CListBox* pListBox,
    UINT nIndex);
```

### <a name="parameters"></a>매개 변수

*nKey*<br/>
사용자가 누른 키의 가상 키 코드를 지정합니다. 표준 가상 키 코드 목록은 Winuser.h를 참조하십시오.

*pListBox*<br/>
목록 상자에 대한 포인터를 지정합니다. 해당 포인터는 임시적이며, 나중에 사용하려고 저장하면 안됩니다.

*nIndex*<br/>
현재 카포트 위치를 지정합니다.

### <a name="return-value"></a>Return Value

응용 프로그램이 메시지에 대한 응답으로 수행한 작업을 지정합니다. 반환 값 -2는 응용 프로그램이 항목을 선택하는 모든 측면을 처리했음을 나타내며 목록 상자에서 추가 작업이 필요하지 않음을 나타냅니다. 반환 값 -1은 목록 상자가 키 입력에 대한 응답으로 기본 작업을 수행해야 함을 나타냅니다. 반환 값이 0 이상이면 목록 상자에 있는 항목의 0기반 인덱스를 지정하고 목록 상자에서 지정된 항목의 키 입력에 대한 기본 작업을 수행해야 함을 나타냅니다.

### <a name="remarks"></a>설명

이 멤버 함수는 [LBS_HASSTRINGS](../../mfc/reference/styles-used-by-mfc.md#list-box-styles) 스타일이 있는 목록 상자에 대해서만 프레임워크에서 호출됩니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonvscroll"></a><a name="onvscroll"></a>CWnd::온비스크롤

사용자가 창의 세로 스크롤 막대를 클릭할 때 프레임워크는 이 멤버 함수를 호출합니다.

```
afx_msg void OnVScroll(
    UINT nSBCode,
    UINT nPos,
    CScrollBar* pScrollBar);
```

### <a name="parameters"></a>매개 변수

*nSBCode*<br/>
사용자의 스크롤 요청을 나타내는 스크롤 바로 표시 줄 코드를 지정합니다. 이 매개 변수는 다음 중 하나일 수 있습니다.

- SB_BOTTOM 아래로 스크롤합니다.

- SB_ENDSCROLL 끝 스크롤입니다.

- SB_LINEDOWN 한 줄 아래로 스크롤합니다.

- SB_LINEUP 한 줄을 스크롤합니다.

- SB_PAGEDOWN 한 페이지 아래로 스크롤합니다.

- SB_PAGEUP 한 페이지를 위로 스크롤합니다.

- SB_THUMBPOSITION 절대 위치로 스크롤합니다. 현재 위치는 *nPos에*제공됩니다.

- SB_THUMBTRACK 스크롤 상자를 지정된 위치로 드래그합니다. 현재 위치는 *nPos에*제공됩니다.

- SB_TOP 위로 스크롤합니다.

*Npos*<br/>
스크롤 바코드가 SB_THUMBPOSITION 또는 SB_THUMBTRACK 경우 현재 스크롤 상자 위치를 포함합니다. 그렇지 않으면 사용되지 않습니다. 초기 스크롤 범위에 따라 *nPos는* 음수일 수 있으며 필요한 경우 **int로** 캐스팅해야 합니다.

*p스크롤바*<br/>
스크롤 메시지가 스크롤 막대 컨트롤에서 온 경우 컨트롤에 대한 포인터가 포함되어 있습니다. 사용자가 창의 스크롤 막대를 클릭한 경우 이 매개 변수는 NULL입니다. 해당 포인터는 임시적이며, 나중에 사용하려고 저장하면 안됩니다.

### <a name="remarks"></a>설명

`OnVScroll`일반적으로 스크롤 상자를 드래그하는 동안 피드백을 제공하는 응용 프로그램에서 사용됩니다.

개체의 내용을 스크롤하는 경우 `OnVScroll` [SetScrollPos](#setscrollpos) 멤버 함수를 통해 스크롤 상자의 위치도 재설정해야 합니다. `CWnd`

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonvscrollclipboard"></a><a name="onvscrollclipboard"></a>CWnd::OnV스크롤 클립보드

클립보드 데이터의 CF_OWNERDISPLAY `OnVScrollClipboard` 형식이 있고 클립보드 뷰어의 세로 스크롤 막대에 이벤트가 있을 때 클립보드 소유자의 멤버 함수가 클립보드 뷰어에서 호출됩니다.

```
afx_msg void OnVScrollClipboard(
    CWnd* pClipAppWnd,
    UINT nSBCode,
    UINT nPos);
```

### <a name="parameters"></a>매개 변수

*pClipAppWnd*<br/>
클립보드 뷰어 창에 대한 포인터를 지정합니다. 해당 포인터는 임시적이며, 나중에 사용하려고 저장하면 안됩니다.

*nSBCode*<br/>
다음 스크롤 막대 값 중 하나를 지정합니다.

- SB_BOTTOM 아래로 스크롤합니다.

- SB_ENDSCROLL 끝 스크롤입니다.

- SB_LINEDOWN 한 줄 아래로 스크롤합니다.

- SB_LINEUP 한 줄을 스크롤합니다.

- SB_PAGEDOWN 한 페이지 아래로 스크롤합니다.

- SB_PAGEUP 한 페이지를 위로 스크롤합니다.

- SB_THUMBPOSITION 절대 위치로 스크롤합니다. 현재 위치는 *nPos에*제공됩니다.

- SB_TOP 위로 스크롤합니다.

*Npos*<br/>
스크롤 바코드SB_THUMBPOSITION 스크롤 바코드인 경우 스크롤 상자 위치를 포함합니다. 그렇지 않으면 *nPos가* 사용되지 않습니다.

### <a name="remarks"></a>설명

소유자는 클립보드 이미지를 스크롤하고, 적절한 섹션을 무효화하고, 스크롤 막대 값을 업데이트해야 합니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonwindowmaximizedchanged"></a><a name="onwindowmaximizedchanged"></a>CWnd::온윈도우최대화변경

프레임워크는 현재 창이 최대화되고 창이 DWM(데스크톱 창 관리자)에 의해 구성될 때 이 멤버를 호출합니다.

```
afx_msg void OnWindowMaximizedChanged(BOOL bIsMaximized);
```

### <a name="parameters"></a>매개 변수

|매개 변수|Description|
|---------------|-----------------|
|*비스 극대화*|【인】 현재 창이 최대화되면 TRUE이고 그렇지 않은 경우 FALSE입니다.|

### <a name="remarks"></a>설명

이 메서드는 Windows SDK에 설명 된 [WM_DWMWINDOWMAXIMIZEDCHANGE](/windows/win32/dwm/wm-dwmwindowmaximizedchange) 알림 메시지를 받습니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonwindowposchanged"></a><a name="onwindowposchanged"></a>CWnd::에 윈도우 포스 변경

[프레임워크는 SetWindowPos](/windows/win32/api/winuser/nf-winuser-setwindowpos) 멤버 함수 또는 다른 창 관리 함수에 대한 호출의 결과로 크기, 위치 또는 Z 순서가 변경된 경우 이 멤버 함수를 호출합니다.

```
afx_msg void OnWindowPosChanged(WINDOWPOS* lpwndpos);
```

### <a name="parameters"></a>매개 변수

*lpwndpos*<br/>
창의 새 크기와 위치에 대한 정보가 포함된 [WINDOWPOS](/windows/win32/api/winuser/ns-winuser-windowpos) 데이터 구조를 가리킵니다.

### <a name="remarks"></a>설명

기본 구현은 [WM_SIZE](/windows/win32/winmsg/wm-size) 및 [WM_MOVE](/windows/win32/winmsg/wm-move) 메시지를 창으로 보냅니다. 응용 프로그램이 기본 클래스를 호출하지 `OnWindowPosChanged` 않고 호출을 처리하는 경우 이러한 메시지는 전송되지 않습니다. 기본 클래스를 호출하지 않고 호출 하는 동안 `OnWindowPosChanged` 이동 또는 크기 변경 처리를 수행 하는 것이 더 효율적입니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonwindowposchanging"></a><a name="onwindowposchanging"></a>CWnd::에 윈도우 포스 변경

[프레임워크는 SetWindowPos](/windows/win32/api/winuser/nf-winuser-setwindowpos) 멤버 함수 또는 다른 창 관리 함수에 대한 호출의 결과로 크기, 위치 또는 Z 순서가 변경될 때 이 멤버 함수를 호출합니다.

```
afx_msg void OnWindowPosChanging(WINDOWPOS* lpwndpos);
```

### <a name="parameters"></a>매개 변수

*lpwndpos*<br/>
창의 `WINDOWPOS` 새 크기 및 위치에 대한 정보가 포함된 데이터 구조를 가리킵니다.

### <a name="remarks"></a>설명

응용 프로그램은 [WINDOWPOS](/windows/win32/api/winuser/ns-winuser-windowpos) 구조의 **플래그** 멤버에서 적절한 비트를 설정하거나 지워 창의 변경을 방지할 수 있습니다.

[WS_OVERLAPPED](styles-used-by-mfc.md#window-styles) 또는 [WS_THICKFRAME](styles-used-by-mfc.md#window-styles) 스타일이 있는 창의 경우 기본 구현은 [WM_GETMINMAXINFO](/windows/win32/winmsg/wm-getminmaxinfo) 메시지를 창으로 보냅니다. 이는 창의 새 크기와 위치를 확인하고 CS_BYTEALIGNCLIENT 및 CS_BYTEALIGN 클라이언트 스타일을 적용하기 위해 수행됩니다. 응용 프로그램은 기본 클래스를 호출하지 않음으로써 이 기능을 재정의할 수 있습니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonwininichange"></a><a name="onwininichange"></a>CWnd::온위니체인지

Windows 초기화 파일 WIN을 변경한 후 프레임워크는 이 멤버 함수를 호출합니다. Ini.

```
afx_msg void OnWinIniChange(LPCTSTR lpszSection);
```

### <a name="parameters"></a>매개 변수

*lpszSection*<br/>
변경된 섹션의 이름을 지정하는 문자열을 가리킵니다. (문자열에는 섹션 이름을 둘러싸는 대괄호가 포함되지 않습니다.)

### <a name="remarks"></a>설명

[SystemParametersInfo](/windows/win32/api/winuser/nf-winuser-systemparametersinfow) Windows 함수는 응용 프로그램이 WIN의 설정을 변경하는 기능을 사용한 후 호출합니다. `OnWinIniChange` INI 파일입니다.

WM_WININICHANGE 메시지를 모든 최상위 창으로 보내려면 응용 프로그램에서 *hwnd* 매개 변수를 HWND_BROADCAST 설정된 [SendMessage](/windows/win32/api/winuser/nf-winuser-sendmessage) Windows 함수를 사용할 수 있습니다.

응용 프로그램이 WIN에서 여러 섹션을 변경하는 경우. INI를 동시에 응용 프로그램은 *lpszSection이* NULL로 설정된 하나의 WM_WININICHANGE 메시지를 보내야 합니다. 그렇지 않으면 응용 프로그램이 WIN을 변경할 때마다 WM_WININICHANGE 보내야 합니다. Ini.

응용 프로그램이 `OnWinIniChange` *lpszSection이* NULL로 설정된 호출을 수신하는 경우 응용 프로그램은 WIN의 모든 섹션을 확인해야 합니다. 응용 프로그램에 영향을 주는 INI입니다.

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonwndmsg"></a><a name="onwndmsg"></a>CWnd::OnWndMsg

이 멤버 함수는 `WindowProc`을 통해 호출하거나 메시지 리플렉션 중에 호출됩니다.

```
virtual BOOL OnWndMsg(
    UINT message,
    WPARAM wParam,
    LPARAM lParam,
    LRESULT* pResult);
```

### <a name="parameters"></a>매개 변수

*message*<br/>
보낼 메시지를 지정합니다.

*wParam*<br/>
추가 메시지 종속 정보를 지정합니다.

*lParam*<br/>
추가 메시지 종속 정보를 지정합니다.

*p결과*<br/>
[WindowProc](#windowproc)의 반환 값입니다. 메시지에 따라 다릅니다. NULL일 수 있습니다.

### <a name="return-value"></a>Return Value

TRUE 메시지가 처리된 경우 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

`OnWndMsg`은 메시지 유형을 결정하고 적절한 프레임워크 함수(예: [WM_COMMAND OnCommand)를](#oncommand) 호출하거나 메시지 맵에서 적절한 메시지를 찾습니다.

메시지 리플렉션에 대한 자세한 내용은 [반사된 메시지 처리를](../../mfc/handling-reflected-messages.md)참조하십시오.

## <a name="cwndonxbuttondblclk"></a><a name="onxbuttondblclk"></a>CWnd::OnXButtonDblk

커서가 창의 클라이언트 영역에 있는 동안 사용자가 XBUTTON1 또는 XBUTTON2를 두 번 클릭할 때 프레임워크는 이 멤버 함수를 호출합니다.

```
afx_msg void OnXButtonDblClk(
    UINT nFlags,
    UINT nButton,
    CPoint point);
```

### <a name="parameters"></a>매개 변수

|매개 변수|Description|
|---------------|-----------------|
|*nFlags*|【인】 누른 수정자 키를 나타내는 플래그의 비트 조합(OR)입니다. 예를 들어 MK_CONTROL 플래그는 CTRL 키가 눌리고 있음을 나타냅니다.|
|*nButton*|【인】 첫 번째 Microsoft Intellimouse X 버튼을 두 번 클릭한 경우 XBUTTON1 값, 두 번째 X 단추를 두 번 클릭하면 XBUTTON2의 값입니다.|
|*지점*|【인】 클라이언트 영역의 왼쪽 위 모서리를 기준으로 커서의 *x* 및 *y* 좌표를 지정하는 [CPoint](../../atl-mfc-shared/reference/cpoint-class.md) 개체입니다.|

### <a name="remarks"></a>설명

이 메서드는 Windows SDK에 설명 된 [WM_XBUTTONDBLCLK](/windows/win32/inputdev/wm-xbuttondblclk) 알림을 받습니다. 마우스가 캡처되지 않으면 메시지가 커서 아래의 창에 게시됩니다. 그렇지 않으면 메시지가 마우스를 캡처한 창에 게시됩니다.

*nFlags* 매개 변수는 다음 표에 나열된 수정자 키의 조합일 수 있습니다. 자세한 내용은 [마우스 입력 정보를](/windows/win32/inputdev/about-mouse-input)참조하십시오.

|수정자 키|Description|
|------------------|-----------------|
|MK_CONTROL|CTRL 키를 누른다.|
|MK_LBUTTON|마우스 왼쪽 단추가 눌러져 있습니다.|
|MK_MBUTTON|마우스 가운데 단추가 눌러져 있습니다.|
|MK_RBUTTON|마우스 오른쪽 단추가 눌러져 있습니다.|
|MK_SHIFT|SHIFT 키를 누른 다.|
|MK_XBUTTON1|마이크로소프트 IntelliMouse의 XBUTTON1 마우스 버튼을 누른.|
|MK_XBUTTON2|마이크로소프트 인텔리마우스의 XBUTTON2 마우스 버튼을 누른다.|

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonxbuttondown"></a><a name="onxbuttondown"></a>CWnd::OnXButtonDown

커서가 창의 클라이언트 영역에 있는 동안 사용자가 XBUTTON1 또는 XBUTTON2를 누를 때 프레임워크는 이 멤버 함수를 호출합니다.

```
afx_msg void OnXButtonDown(
    UINT nFlags,
    UINT nButton,
    CPoint point);
```

### <a name="parameters"></a>매개 변수

|매개 변수|Description|
|---------------|-----------------|
|*nFlags*|【인】 누른 수정자 키를 나타내는 플래그의 비트 조합(OR)입니다. 예를 들어 MK_CONTROL 플래그는 CTRL 키가 눌리고 있음을 나타냅니다.|
|*nButton*|【인】 첫 번째 Microsoft Intellimouse X 단추를 클릭한 경우 XBUTTON1 값, 또는 두 번째 X 단추를 클릭한 경우 XBUTTON2의 값입니다.|
|*지점*|【인】 클라이언트 영역의 왼쪽 위 모서리를 기준으로 커서의 *x* 및 *y* 좌표를 지정하는 [CPoint](../../atl-mfc-shared/reference/cpoint-class.md) 개체입니다.|

### <a name="remarks"></a>설명

이 메서드는 Windows SDK에 설명 된 [WM_XBUTTONDOWN](/windows/win32/inputdev/wm-xbuttondown) 알림을 받습니다. 마우스가 캡처되지 않으면 메시지가 커서 아래의 창에 게시됩니다. 그렇지 않으면 메시지가 마우스를 캡처한 창에 게시됩니다.

*nFlags* 매개 변수는 다음 표에 나열된 수정자 키의 조합일 수 있습니다. 자세한 내용은 [마우스 입력 정보를](/windows/win32/inputdev/about-mouse-input)참조하십시오.

|수정자 키|Description|
|------------------|-----------------|
|MK_CONTROL|CTRL 키를 누른다.|
|MK_LBUTTON|마우스 왼쪽 단추가 눌러져 있습니다.|
|MK_MBUTTON|마우스 가운데 단추가 눌러져 있습니다.|
|MK_RBUTTON|마우스 오른쪽 단추가 눌러져 있습니다.|
|MK_SHIFT|SHIFT 키를 누른 다.|
|MK_XBUTTON1|마이크로소프트 IntelliMouse의 XBUTTON1 마우스 버튼을 누른.|
|MK_XBUTTON2|마이크로소프트 인텔리마우스의 XBUTTON2 마우스 버튼을 누른다.|

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndonxbuttonup"></a><a name="onxbuttonup"></a>CWnd::OnXButtonUp

커서가 창의 클라이언트 영역에 있는 동안 사용자가 XBUTTON1 또는 XBUTTON2를 해제할 때 프레임워크는 이 멤버 함수를 호출합니다.

```
afx_msg void OnXButtonUp(
    UINT nFlags,
    UINT nButton,
    CPoint point);
```

### <a name="parameters"></a>매개 변수

|매개 변수|Description|
|---------------|-----------------|
|*nFlags*|【인】 누른 수정자 키를 나타내는 플래그의 비트 조합(OR)입니다. 예를 들어 MK_CONTROL 플래그는 CTRL 키가 눌리고 있음을 나타냅니다.|
|*nButton*|【인】 첫 번째 Microsoft Intellimouse X 버튼을 두 번 클릭한 경우 XBUTTON1 값, 두 번째 X 버튼을 두 번 클릭한 경우 XBUTTON2값입니다.|
|*지점*|【인】 클라이언트 영역의 왼쪽 위 모서리를 기준으로 커서의 *x* 및 *y* 좌표를 지정하는 [CPoint](../../atl-mfc-shared/reference/cpoint-class.md) 개체입니다.|

### <a name="remarks"></a>설명

이 메서드는 Windows SDK에 설명 된 [WM_XBUTTONUP](/windows/win32/inputdev/wm-xbuttonup) 알림을 받습니다. 마우스가 캡처되지 않으면 메시지가 커서 아래의 창에 게시됩니다. 그렇지 않으면 메시지가 마우스를 캡처한 창에 게시됩니다.

*nFlags* 매개 변수는 다음 표에 나열된 수정자 키의 조합일 수 있습니다. 자세한 내용은 [마우스 입력 정보를](/windows/win32/inputdev/about-mouse-input)참조하십시오.

|수정자 키|Description|
|------------------|-----------------|
|MK_CONTROL|CTRL 키를 누른다.|
|MK_LBUTTON|마우스 왼쪽 단추가 눌러져 있습니다.|
|MK_MBUTTON|마우스 가운데 단추가 눌러져 있습니다.|
|MK_RBUTTON|마우스 오른쪽 단추가 눌러져 있습니다.|
|MK_SHIFT|SHIFT 키를 누른 다.|
|MK_XBUTTON1|마이크로소프트 IntelliMouse의 XBUTTON1 마우스 버튼을 누른.|
|MK_XBUTTON2|마이크로소프트 인텔리마우스의 XBUTTON2 마우스 버튼을 누른다.|

> [!NOTE]
> 이 멤버 함수는 애플리케이션에서 Windows 메시지를 처리할 수 있도록 프레임워크에서 호출됩니다. 함수에 전달되는 매개 변수는 해당 메시지가 수신되었을 때 프레임워크에서 받았던 매개 변수를 반영합니다. 이 함수의 기본 클래스 구현을 호출하는 경우, 해당 구현은 해당 함수에 공급한 매개변수가 아닌, 메시지와 함께 전달 받았던 원래의 매개 변수를 사용할 것입니다.

## <a name="cwndopenclipboard"></a><a name="openclipboard"></a>CWnd::오픈 클립보드

클립보드를 엽니다.

```
BOOL OpenClipboard();
```

### <a name="return-value"></a>Return Value

을 통해 `CWnd`클립보드를 열거나 다른 응용 프로그램이나 창에 클립보드가 열려 있는 경우 0이 아닌 경우

### <a name="remarks"></a>설명

다른 응용 프로그램은 [CloseClipboard](/windows/win32/api/winuser/nf-winuser-closeclipboard) Windows 함수가 호출될 때까지 클립보드를 수정할 수 없습니다.

현재 `CWnd` 개체는 EmptyClipboard Windows 함수가 호출될 때까지 [클립보드의](/windows/win32/api/winuser/nf-winuser-emptyclipboard) 소유자가 되지 않습니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#111](../../mfc/reference/codesnippet/cpp/cwnd-class_52.cpp)]

## <a name="cwndoperator-hwnd"></a><a name="operator_hwnd"></a>CWnd::연산자 HWND

이 연산자에서 개체에 `CWnd` 대한 핸들을 가져옵니다.

```
operator HWND() const;
```

## <a name="cwndoperator-"></a><a name="operator_neq"></a>CWnd::연산자 !=

두 `CWnd` 개체를 비교하여 [동일한 m_hWnd](#m_hwnd)없는지 확인합니다.

```
BOOL operator!=(const CWnd& wnd) const;
```

### <a name="parameters"></a>매개 변수

*Wnd*<br/>
`CWnd` 개체에 대한 참조입니다.

### <a name="return-value"></a>Return Value

비영점(같음); 그렇지 않으면 0.

## <a name="cwndoperator-"></a><a name="operator_eq_eq"></a>CWnd::연산자 ==

두 `CWnd` 개체를 비교하여 [동일한 m_hWnd](#m_hwnd)있는지 확인합니다.

```
BOOL operator==(const CWnd& wnd) const;
```

### <a name="parameters"></a>매개 변수

*Wnd*<br/>
`CWnd` 개체에 대한 참조입니다.

### <a name="return-value"></a>Return Value

비영점(같음); 그렇지 않으면 0.

## <a name="cwndpaintwindowlesscontrols"></a><a name="paintwindowlesscontrols"></a>CWnd::Paint윈도우리스컨트롤스

컨트롤 컨테이너에서 창 없는 컨트롤을 그립니다.

```
BOOL PaintWindowlessControls(CDC* pDC);
```

### <a name="parameters"></a>매개 변수

*pDC*<br/>
창 없는 컨트롤을 그릴 장치 컨텍스트입니다.

### <a name="return-value"></a>Return Value

컨트롤 컨테이너가 있고 창 없는 컨트롤이 성공적으로 그려지는 경우 TRUE를 반환합니다( 그렇지 않으면 FALSE).

## <a name="cwndpostmessage"></a><a name="postmessage"></a>CWnd::PostMessage

창의 메시지 큐에 메시지를 배치한 다음 해당 창이 메시지를 처리할 때까지 기다리지 않고 반환됩니다.

```
BOOL PostMessage(
    UINT message,
    WPARAM wParam = 0,
    LPARAM lParam = 0);
```

### <a name="parameters"></a>매개 변수

*message*<br/>
게시할 메시지를 지정합니다.

*wParam*<br/>
추가 메시지 정보를 지정합니다. 이 매개 변수의 내용은 게시되는 메시지에 따라 다릅니다.

*lParam*<br/>
추가 메시지 정보를 지정합니다. 이 매개 변수의 내용은 게시되는 메시지에 따라 다릅니다.

### <a name="return-value"></a>Return Value

메시지가 게시되는 경우 0이 아닙니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

메시지 큐의 메시지는 [GetMessage](/windows/win32/api/winuser/nf-winuser-getmessage) 또는 [PeekMessage](/windows/win32/api/winuser/nf-winuser-peekmessagew) Windows 함수에 대한 호출로 검색됩니다.

Windows [PostMessage](/windows/win32/api/winuser/nf-winuser-postmessagew) 함수를 사용하여 다른 응용 프로그램에 액세스할 수 있습니다.

### <a name="example"></a>예제

  [AfxGetMainWnd에](../../mfc/reference/application-information-and-management.md#afxgetmainwnd)대한 예제를 참조하십시오.

## <a name="cwndpostncdestroy"></a><a name="postncdestroy"></a>CWnd::PostNcDestroy

창이 소멸된 후 기본 [OnNcDestroy](#onncdestroy) 멤버 함수에서 호출됩니다.

```
virtual void PostNcDestroy();
```

### <a name="remarks"></a>설명

파생 클래스는 이 포인터삭제와 같은 사용자 지정 정리에 **이** 함수를 사용할 수 있습니다.

## <a name="cwndprecreatewindow"></a><a name="precreatewindow"></a>CWnd::P재현창

이 `CWnd` 개체에 연결된 Windows 창을 만들기 전에 프레임워크에서 호출합니다.

```
virtual BOOL PreCreateWindow(CREATESTRUCT& cs);
```

### <a name="parameters"></a>매개 변수

*cs*<br/>
[생성TRUCT](/windows/win32/api/winuser/ns-winuser-createstructw) 구조입니다.

### <a name="return-value"></a>Return Value

창 만들기를 계속해야 하는 경우 0이 아닙니다. 0을 사용하여 생성 실패를 나타냅니다.

### <a name="remarks"></a>설명

> [!WARNING]
> `CWnd::PreCreateWindow`이제 메뉴가 NULL이고 스타일에 WS_CHILD 포함되어 있는 경우 **이** 포인터에 *cs의* hMenu 멤버를 할당합니다. 적절한 기능을 위해 대화 상자 컨트롤에 NULL이 아닌 ID가 있는지 확인합니다.
>
> 이 변경 은 관리 /네이티브 interop 시나리오에서 충돌을 해결 합니다. 문제의 개발자에게 `CWnd::Create` 경고하는 TRACE 문입니다.

이 함수를 직접 호출하지 마십시오.

이 함수의 기본 구현은 NULL window 클래스 이름을 확인하고 적절한 기본값을 대체합니다. 이 멤버 함수를 재정의하여 창을 작성하기 전에 `CREATESTRUCT` 구조를 수정합니다.

파생된 각 `CWnd` 클래스는 `PreCreateWindow`의 재정의에 자체 기능을 추가합니다. 의도적으로 이러한 `PreCreateWindow` 파생은 문서화되지 않습니다. 각 클래스에 적합한 스타일과 스타일 간의 상호 종속성을 확인하려면 응용 프로그램의 기본 클래스에 대한 MFC 소스 코드를 검사할 수 있습니다. 재정의를 `PreCreateWindow,` 선택하는 경우 응용 프로그램의 기본 클래스에 사용된 스타일이 MFC 소스 코드에서 수집한 정보를 사용하여 필요한 기능을 제공하는지 여부를 확인할 수 있습니다.

창 스타일 변경에 대한 자세한 내용은 [MFC에서 만든 창의 스타일 변경을](../../mfc/changing-the-styles-of-a-window-created-by-mfc.md)참조하십시오.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#112](../../mfc/reference/codesnippet/cpp/cwnd-class_53.cpp)]

## <a name="cwndpresubclasswindow"></a><a name="presubclasswindow"></a>CWnd::Presubclass창

이 멤버 함수는 창이 하위 분류되기 전에 다른 필요한 하위 분류가 발생할 수 있도록 프레임워크에서 호출됩니다.

```
virtual void PreSubclassWindow();
```

### <a name="remarks"></a>설명

이 멤버 함수를 재정의하면 컨트롤의 동적 하위 클래스를 사용할 수 있습니다. 고급 재정의 가능한 고급입니다.

## <a name="cwndpretranslatemessage"></a><a name="pretranslatemessage"></a>CWnd::PreTranslateMessage

창 메시지가 [TranslateMessage](../../mfc/reference/cwinapp-class.md) 및 [DispatchMessage](/windows/win32/api/winuser/nf-winuser-translatemessage) Windows 함수로 디스패치되기 전에 [CWinApp](/windows/win32/api/winuser/nf-winuser-dispatchmessage) 클래스가 이 메시지를 해석하는 데 사용됩니다.

```
virtual BOOL PreTranslateMessage(MSG* pMsg);
```

### <a name="parameters"></a>매개 변수

*pMsg*<br/>
처리할 메시지가 포함된 [MSG](/windows/win32/api/winuser/ns-winuser-msg) 구조를 가리킵니다.

### <a name="return-value"></a>Return Value

메시지가 번역되어 전달되지 않아야 하는 경우 0이 아님을 의미합니다. 메시지가 번역되지 않고 디스패치해야 하는 경우 0입니다.

## <a name="cwndprint"></a><a name="print"></a>CWnd::P린트

이 멤버 함수를 호출하여 프린터 장치 컨텍스트에서 가장 일반적으로 지정된 장치 컨텍스트에서 현재 창을 그립니다.

```cpp
void Print(
    CDC* pDC,
    DWORD dwFlags) const;
```

### <a name="parameters"></a>매개 변수

*pDC*<br/>
디바이스 컨텍스트에 대한 포인터입니다.

*dwFlags*<br/>
도면 옵션을 지정합니다. 이 매개 변수는 다음 플래그 중 하나 이상일 수 있습니다.

- PRF_CHECKVISIBLE 표시되는 경우에만 창을 그립니다.

- PRF_CHILDREN 보이는 모든 자식 창을 그립니다.

- PRF_CLIENT 창의 클라이언트 영역을 그립니다.

- PRF_ERASEBKGND 창을 그리기 전에 배경을 지웁니다.

- PRF_NONCLIENT 창의 비클라이언트 영역을 그립니다.

- PRF_OWNED 소유한 모든 창을 그립니다.

### <a name="remarks"></a>설명

[CWnd::DefWindowProc](#defwindowproc) 함수는 지정된 도면 옵션을 기반으로 이 메시지를 처리합니다.

- PRF_CHECKVISIBLE 지정되어 있고 창이 표시되지 않으면 아무 것도 수행하지 마십시오.

- PRF_NONCLIENT 지정하면 지정된 장치 컨텍스트에서 비클라이언트 영역을 그립니다.

- PRF_ERASEBKGND 지정되면 창에 [WM_ERASEBKGND](/windows/win32/winmsg/wm-erasebkgnd) 메시지를 보냅니다.

- PRF_CLIENT 지정하면 창에 [WM_PRINTCLIENT](/windows/win32/gdi/wm-printclient) 메시지를 보냅니다.

- PRF_CHILDREN 설정된 경우 표시되는 각 자식 창에 [WM_PRINT](/windows/win32/gdi/wm-print) 메시지를 보냅니다.

- PRF_OWNED 설정되면 표시되는 각 창을 WM_PRINT 메시지로 보냅니다.

## <a name="cwndprintclient"></a><a name="printclient"></a>CWnd::P린트 클라이언트

지정된 장치 컨텍스트(일반적으로 프린터 장치 컨텍스트)에서 창을 그리려면 이 멤버 함수를 호출합니다.

```cpp
void PrintClient(
    CDC* pDC,
    DWORD dwFlags) const;
```

### <a name="parameters"></a>매개 변수

*pDC*<br/>
디바이스 컨텍스트에 대한 포인터입니다.

*dwFlags*<br/>
도면 옵션을 지정합니다. 이 매개 변수는 다음 플래그 중 하나 이상일 수 있습니다.

- PRF_CHECKVISIBLE 표시되는 경우에만 창을 그립니다.

- PRF_CHILDREN 보이는 모든 자식 창을 그립니다.

- PRF_CLIENT 창의 클라이언트 영역을 그립니다.

- PRF_ERASEBKGND 창을 그리기 전에 배경을 지웁니다.

- PRF_NONCLIENT 창의 비클라이언트 영역을 그립니다.

- PRF_OWNED 소유한 모든 창을 그립니다.

## <a name="cwndprintwindow"></a><a name="printwindow"></a>CWnd::P린트윈도우

지정된 디바이스 컨텍스트(일반적으로 프린터 DC)에 시각적 창을 복사합니다.

```
BOOL PrintWindow(
    CDC* pDC,
    UINT nFlags) const;
```

### <a name="parameters"></a>매개 변수

*pDC*<br/>
인쇄할 장치 컨텍스트에 대한 포인터입니다.

*nFlags*<br/>
도면 옵션을 지정합니다. 가능한 값 목록은 [PrintWindow](/windows/win32/api/winuser/nf-winuser-printwindow)를 참조하십시오.

### <a name="return-value"></a>Return Value

함수가 성공하면 0이 아니고 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

이 멤버 함수는 Windows SDK에 설명된 대로 [PrintWindow](/windows/win32/api/winuser/nf-winuser-printwindow)기능의 기능을 에뮬레이트합니다.

## <a name="cwndredrawwindow"></a><a name="redrawwindow"></a>CWnd::다시 그리기 창

지정된 창의 클라이언트 영역에서 지정된 사각형 또는 영역을 업데이트합니다.

```
BOOL RedrawWindow(
    LPCRECT lpRectUpdate = NULL,
    CRgn* prgnUpdate = NULL,
    UINT flags = RDW_INVALIDATE | RDW_UPDATENOW | RDW_ERASE);
```

### <a name="parameters"></a>매개 변수

*lpRectUpdate*<br/>
업데이트 사각형의 좌표를 포함하는 [RECT 구조를](/windows/win32/api/windef/ns-windef-rect) 가리킵니다. *prgnUpdate유효한* 지역 핸들을 포함하는 경우 이 매개 변수는 무시됩니다.

*prgnUpdate*<br/>
업데이트 영역을 식별합니다. *prgnUpdate* 및 *lpRectUpdate가* 모두 NULL이면 전체 클라이언트 영역이 업데이트 영역에 추가됩니다.

*플래그*<br/>
다음 플래그는 창을 무효화하는 데 사용됩니다.

- RDW_ERASE 창이 다시 칠될 때 창이 [WM_ERASEBKGND](/windows/win32/winmsg/wm-erasebkgnd) 메시지를 수신하게 합니다. RDW_INVALIDATE 플래그도 지정해야 합니다. 그렇지 않으면 RDW_ERASE 효과가 없습니다.

- RDW_FRAME 업데이트 영역과 교차하는 창의 비클라이언트 영역의 일부가 [WM_NCPAINT](/windows/win32/gdi/wm-ncpaint) 메시지를 수신하도록 합니다. RDW_INVALIDATE 플래그도 지정해야 합니다. 그렇지 않으면 RDW_FRAME 효과가 없습니다.

- RDW_INTERNALPAINT 창에 잘못된 영역이 포함되어 있는지 여부에 관계없이 [WM_PAINT](/windows/win32/gdi/wm-paint) 메시지가 창에 게시됩니다.

- RDW_INVALIDATE *lpRectUpdate* 또는 *prgnUpdate를* 무효화합니다 (하나만 NULL이 아닐 수 있음). 둘 다 NULL이면 전체 창이 무효화됩니다.

다음 플래그는 창의 유효성을 검사하는 데 사용됩니다.

- RDW_NOERASE 보류 중인 WM_ERASEBKGND 메시지를 표시하지 않습니다.

- 보류 중인 WM_NCPAINT 메시지를 RDW_NOFRAME. 이 플래그는 RDW_VALIDATE 함께 사용해야 하며 일반적으로 RDW_NOCHILDREN 함께 사용됩니다. 이 옵션은 창의 일부가 제대로 페인팅되지 않도록 주의해야 합니다.

- RDW_NOINTERNALPAINT 보류 중인 내부 WM_PAINT 메시지를 표시하지 않습니다. 이 플래그는 잘못된 영역으로 인한 WM_PAINT 메시지에는 영향을 주지 않습니다.

- RDW_VALIDATE *lpRectUpdate* 또는 *prgnUpdate의* 유효성을 검사합니다(NULL이 아닐 수 있음). 둘 다 NULL이면 전체 창의 유효성이 검사됩니다. 이 플래그는 내부 WM_PAINT 메시지에는 영향을 주지 않습니다.

다음 플래그는 다시 그리기가 발생할 때 제어합니다. 이러한 비트 중 `RedrawWindow` 하나가 지정되지 않는 한 페인팅은 함수에 의해 수행되지 않습니다.

- RDW_ERASENOW RDW_ALLCHILDREN 및 RDW_NOCHILDREN 플래그에 의해 지정된 대로 영향을 받는 창이 함수가 반환되기 전에 필요한 경우 WM_NCPAINT 및 WM_ERASEBKGND 메시지를 수신하게 합니다. WM_PAINT 메시지는 지연됩니다.

- RDW_UPDATENOW 함수가 반환되기 전에 영향을 받는 창(RDW_ALLCHILDREN 및 RDW_NOCHILDREN 플래그에 의해 지정된 대로)이 필요한 경우 WM_NCPAINT, WM_ERASEBKGND 및 WM_PAINT 메시지를 수신하도록 합니다.

기본적으로 함수의 `RedrawWindow` 영향을 받는 창은 지정된 창에 WS_CLIPCHILDREN 스타일이 있는지 여부에 따라 달라집니다. WS_CLIPCHILDREN 창의 자식 창은 영향을 받지 않습니다. 그러나 WS_CLIPCHILDREN 없는 창은 WS_CLIPCHILDREN 창이 발생할 때까지 재귀적으로 유효성을 검사하거나 무효화됩니다. 다음 플래그는 함수의 영향을 `RedrawWindow` 받는 창을 제어합니다.

- RDW_ALLCHILDREN 다시 그리기 작업에서 자식 창(있는 경우)을 포함합니다.

- RDW_NOCHILDREN 다시 페인팅 작업에서 자식 창(있는 경우)을 제외합니다.

### <a name="return-value"></a>Return Value

창이 성공적으로 다시 그려진 경우 0이 아닙니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

멤버 `RedrawWindow` 함수를 사용하여 데스크톱 창의 일부를 무효화하면 해당 창에 [WM_PAINT](/windows/win32/gdi/wm-paint) 메시지가 수신되지 않습니다. 데스크톱을 다시 그리려면 응용 프로그램에서 [CWnd::ValidateRgn](#validatergn), [CWnd:무효화Rgn,](#invalidatergn) [CWnd:UpdateWindow](#updatewindow)또는 [다시 그리기창을](/windows/win32/api/winuser/nf-winuser-redrawwindow) 사용해야 합니다.

## <a name="cwndreflectchildnotify"></a><a name="reflectchildnotify"></a>CWnd::반사아동 알림

이 메시지 함수는 [OnChildNotify](#onchildnotify)의 프레임워크에서 호출됩니다.

```
BOOL ReflectChildNotify(
    UINT message,
    WPARAM wParam,
    LPARAM lParam,
    LRESULT* pResult);
```

### <a name="parameters"></a>매개 변수

*message*<br/>
메시지가 반영될 지 지정합니다.

*wParam*<br/>
추가 메시지 종속 정보를 지정합니다.

*lParam*<br/>
추가 메시지 종속 정보를 지정합니다.

*p결과*<br/>
상위 창에서 반환할 자식 창에서 생성된 결과입니다. NULL일 수 있습니다.

### <a name="return-value"></a>Return Value

TRUE 메시지가 반영된 경우; 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

소스에 *메시지를* 반영하는 도우미 기능입니다.

반사된 메시지는 [CWnd:OnWndMsg](#onwndmsg) 또는 [CCmdTarget::OnCmdMsg로](../../mfc/reference/ccmdtarget-class.md#oncmdmsg)직접 전송됩니다.

메시지 리플렉션에 대한 자세한 내용은 [반사된 메시지 처리를](../../mfc/handling-reflected-messages.md)참조하십시오.

## <a name="cwndreflectlastmsg"></a><a name="reflectlastmsg"></a>CWnd::반사LastMsg

이 멤버 함수는 하위 창에 마지막 메시지를 반영하기 위해 프레임워크에서 호출됩니다.

```
static BOOL PASCAL ReflectLastMsg(
    HWND hWndChild,
    LRESULT* pResult = NULL);
```

### <a name="parameters"></a>매개 변수

*hWndChild*<br/>
자식 창에 대한 핸들입니다.

*p결과*<br/>
상위 창에서 반환할 자식 창에서 생성된 결과입니다. NULL일 수 있습니다.

### <a name="return-value"></a>Return Value

메시지가 처리된 경우 0이 아닙니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

이 멤버 함수는 *hWndChild로* 식별된 창이 OLE 컨트롤이거나 영구 맵의 창인 경우 [SendChildNotifyLastMsg를](#sendchildnotifylastmsg) 호출합니다.

메시지 리플렉션에 대한 자세한 내용은 [반사된 메시지 처리를](../../mfc/handling-reflected-messages.md)참조하십시오.

## <a name="cwndreleasedc"></a><a name="releasedc"></a>CWnd::릴리스DC

장치 컨텍스트를 해제하여 다른 응용 프로그램에서 사용할 수 있습니다.

```
int ReleaseDC(CDC* pDC);
```

### <a name="parameters"></a>매개 변수

*pDC*<br/>
릴리스할 장치 컨텍스트를 식별합니다.

### <a name="return-value"></a>Return Value

성공하면 0이 아니고, 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

멤버 함수의 `ReleaseDC` 효과는 장치 컨텍스트 유형에 따라 다릅니다.

응용 프로그램은 `ReleaseDC` [GetWindowDC](#getwindowdc) 멤버 함수에 대한 각 호출 및 [GetDC](#getdc) 멤버 함수에 대한 각 호출에 대해 멤버 함수를 호출해야 합니다.

## <a name="cwndrepositionbars"></a><a name="repositionbars"></a>CWnd::재배치 막대

창의 클라이언트 영역에서 컨트롤 막대의 위치를 조정하고 크기를 조정하도록 호출됩니다.

```cpp
void RepositionBars(UINT nIDFirst,
    UINT nIDLast,
    UINT nIDLeftOver,
    UINT nFlag = reposDefault,
    LPRECT lpRectParam = NULL,
    LPCRECT lpRectClient = NULL,
    BOOL bStretch = TRUE) ;
```

### <a name="parameters"></a>매개 변수

*니드퍼스트*<br/>
컨트롤 바 범위의 첫 번째 ID를 재배치하고 크기를 조정합니다.

*니드라스트*<br/>
컨트롤 막대 범위의 마지막 ID를 재배치하고 크기를 조정합니다.

*니드남은*<br/>
클라이언트 영역의 나머지 부분을 채우는 창의 ID를 지정합니다.

*n 플래그*<br/>
다음 값 중 하나가 될 수 있습니다.

- `CWnd::reposDefault`컨트롤 막대의 레이아웃을 수행합니다. *lpRectParam은* 사용되지 않으며 NULL이 될 수 있습니다.

- `CWnd::reposQuery`컨트롤 막대의 레이아웃은 수행되지 않습니다. 대신 *lpRectParam은* 레이아웃이 실제로 수행된 것처럼 클라이언트 영역의 크기로 초기화됩니다.

- `CWnd::reposExtra`*nIDLast의* 클라이언트 영역에 *lpRectParam값을* 추가하고 레이아웃도 수행합니다.

*lpRectParam*<br/>
[RECT 구조를](/windows/win32/api/windef/ns-windef-rect)가리킵니다. 사용량은 *nFlag*의 값에 따라 달라집니다.

*lpRect클라이언트*<br/>
사용 가능한 클라이언트 영역을 포함하는 [RECT 구조를](/windows/win32/api/windef/ns-windef-rect) 가리킵니다. NULL이면 창의 클라이언트 영역이 사용됩니다.

*b스트레치*<br/>
막대를 프레임 크기로 늘려야 하는지 여부를 나타냅니다.

### <a name="remarks"></a>설명

*nIDFirst* 및 *nIDLast* 매개 변수는 클라이언트 영역에서 재배치할 컨트롤 바 ID 범위를 정의합니다. *nIDLeftOver* 매개 변수는 컨트롤 막대로 채워지지 않은 클라이언트 영역의 나머지 부분을 채우기 위해 재배치되고 크기조정되는 자식 창(일반적으로 뷰)의 ID를 지정합니다.

## <a name="cwndrunmodalloop"></a><a name="runmodalloop"></a>CWnd:::런모달루프

[ContinueModal이](#continuemodal) FALSE를 반환할 때까지 이 멤버 함수를 호출하여 메시지를 검색, 번역 또는 디스패치합니다.

```
int RunModalLoop(DWORD dwFlags = 0);
```

### <a name="parameters"></a>매개 변수

*dwFlags*<br/>
보낼 Windows 메시지를 지정합니다. 다음 값 중 하나를 사용할 수 있습니다.

- MLF_NOIDLEMSG 부모에게 [WM_ENTERIDLE](/windows/win32/dlgbox/wm-enteridle) 메시지를 보내지 마십시오.

- MLF_NOKICKIDLE WM_KICKIDLE 메시지를 창으로 보내지 않습니다.

- MLF_SHOWONIDLE 메시지 큐가 유휴 상태가 되면 창을 표시합니다.

### <a name="return-value"></a>Return Value

[endModalLoop](#endmodalloop) 멤버 함수에 전달 된 *nResult* 매개 변수의 값을 지정 하 고 모 달 루프를 종료 하는 데 사용 됩니다.

### <a name="remarks"></a>설명

기본적으로 false `ContinueModal` 후 `EndModalLoop` 호출 됩니다 반환 합니다. *nResult로* 제공된 값을 `EndModalLoop`에 반환합니다.

## <a name="cwndscreentoclient"></a><a name="screentoclient"></a>CWnd::스크린토클라이언트

디스플레이에 있는 지정된 점 또는 사각형의 화면 좌표를 클라이언트 좌표로 변환합니다.

```cpp
void ScreenToClient(LPPOINT lpPoint) const;  void ScreenToClient(LPRECT lpRect) const;
```

### <a name="parameters"></a>매개 변수

*lpPoint*<br/>
변환할 화면 좌표를 포함하는 [CPoint](../../atl-mfc-shared/reference/cpoint-class.md) 개체 또는 [POINT 구조를](/windows/win32/api/windef/ns-windef-point) 가리킵니다.

*Lprect*<br/>
변환할 화면 좌표를 포함하는 [CRect](../../atl-mfc-shared/reference/crect-class.md) 개체 또는 [RECT 구조를](/windows/win32/api/windef/ns-windef-rect) 가리킵니다.

### <a name="remarks"></a>설명

멤버 `ScreenToClient` 함수는 *lpPoint* 또는 *lpRect에* 지정된 화면 좌표를 클라이언트 좌표로 대체합니다. 새 좌표는 클라이언트 영역의 왼쪽 위 `CWnd` 모서리를 기준으로 합니다.

### <a name="example"></a>예제

  [CListCtrl::GetItemRect에](../../mfc/reference/clistctrl-class.md#getitemrect)대한 예제를 참조하십시오.

## <a name="cwndscrollwindow"></a><a name="scrollwindow"></a>CWnd::스크롤 창

현재 `CWnd` 개체의 클라이언트 영역의 내용을 스크롤합니다.

```cpp
void ScrollWindow(
    int xAmount,
    int yAmount,
    LPCRECT lpRect = NULL,
    LPCRECT lpClipRect = NULL);
```

### <a name="parameters"></a>매개 변수

*xAmount*<br/>
장치 단위의 가로 스크롤 양을 지정합니다. 이 매개 변수는 왼쪽으로 스크롤하려면 음수 값이어야 합니다.

*yAmount*<br/>
장치 단위의 세로 스크롤 양을 지정합니다. 이 매개 변수는 위로 스크롤하려면 음수 값이어야 합니다.

*Lprect*<br/>
스크롤할 클라이언트 영역의 일부를 지정하는 [CRect](../../atl-mfc-shared/reference/crect-class.md) 개체 또는 [RECT 구조를](/windows/win32/api/windef/ns-windef-rect) 가리킵니다. *lpRect가* NULL이면 전체 클라이언트 영역이 스크롤됩니다. 커서 사각형이 스크롤 사각형과 교차하면 캐리트가 재배치됩니다.

*lpClipRect*<br/>
스크롤할 `CRect` 클리핑 `RECT` 사각형을 지정하는 개체 또는 구조를 가리킵니다. 이 사각형 내부의 비트만 스크롤됩니다. 이 사각형 외부의 비트는 *lpRect* 사각형에 있더라도 영향을 받지 않습니다. *lpClipRect가* NULL이면 스크롤 사각형에서 클리핑이 수행되지 않습니다.

### <a name="remarks"></a>설명

캐리트가 `CWnd` 스크롤중인 경우 `ScrollWindow` 자동으로 캐리트가 지워지지 않도록 캐시를 숨은 다음 스크롤이 완료된 후 캐리어를 복원합니다. 카포트 위치는 그에 따라 조정됩니다.

멤버 함수에서 `ScrollWindow` 발견된 영역은 다시 그려지지 않지만 현재 `CWnd` 개체의 업데이트 영역으로 결합됩니다. 응용 프로그램은 결국 지역에 다시 페인팅이 필요하다는 WM_PAINT [메시지를](/windows/win32/gdi/wm-paint) 받게 됩니다. 스크롤이 완료되는 동시에 덮여 없는 영역을 다시 그리려면 호출 `ScrollWindow`후 즉시 [UpdateWindow](#updatewindow) 멤버 함수를 호출합니다.

*lpRect가* NULL이면 창의 자식 창의 위치는 *xAmount* 및 *yAmount에*의해 지정된 양으로 오프셋되고 `CWnd` 에 있는 잘못된(도색되지 않은) 영역도 오프셋됩니다. `ScrollWindow`*lpRect가* NULL일 때 더 빠릅니다.

*lpRect가* NULL이 아닌 경우 자식 창의 위치가 변경되지 `CWnd` 않고 잘못된 영역이 오프셋되지 않습니다. *lpRect가* NULL이 아닌 경우 업데이트 문제를 `UpdateWindow` 방지하려면 호출하기 `CWnd` `ScrollWindow`전에 멤버 함수를 호출하여 다시 그리기합니다.

## <a name="cwndscrollwindowex"></a><a name="scrollwindowex"></a>CWnd::스크롤윈도우엑스

창의 클라이언트 영역의 내용을 스크롤합니다.

```
int ScrollWindowEx(
    int dx,
    int dy,
    LPCRECT lpRectScroll,
    LPCRECT lpRectClip,
    CRgn* prgnUpdate,
    LPRECT lpRectUpdate,
    UINT flags);
```

### <a name="parameters"></a>매개 변수

*Dx*<br/>
장치 단위의 가로 스크롤 양을 지정합니다. 이 매개 변수는 왼쪽으로 스크롤할 음수 값이 있어야 합니다.

*Dy*<br/>
장치 단위의 세로 스크롤 양을 지정합니다. 이 매개 변수는 위로 스크롤할 수 있는 음수 값이 있어야 합니다.

*lpRect스크롤*<br/>
스크롤할 클라이언트 영역의 일부를 지정하는 [RECT 구조를](/windows/win32/api/windef/ns-windef-rect) 가리킵니다. 이 매개 변수가 NULL이면 전체 클라이언트 영역이 스크롤됩니다.

*lpRectClip*<br/>
스크롤할 `RECT` 클리핑 사각형을 지정하는 구조를 가리킵니다. 이 구조는 *lpRectScroll로*가리키는 사각형보다 우선합니다. 이 사각형 내부의 비트만 스크롤됩니다. 이 사각형 외부의 비트는 *lpRectScroll* 사각형에 있더라도 영향을 받지 않습니다. 이 매개변수가 NULL이면 스크롤 사각형에서 클리핑이 수행되지 않습니다.

*prgnUpdate*<br/>
스크롤하여 무효화된 영역을 보유하도록 수정된 영역을 식별합니다. 이 매개 변수는 NULL이 될 수 있습니다.

*lpRectUpdate*<br/>
스크롤로 `RECT` 무효화된 사각형의 경계를 받는 구조를 가리킵니다. 이 매개 변수는 NULL이 될 수 있습니다.

*플래그*<br/>
다음 값 중 하나가 될 수 있습니다.

- SW_ERASE SW_INVALIDATE 지정하면 WM_ERASEBKGND [메시지를](/windows/win32/winmsg/wm-erasebkgnd) 창에 전송하여 새로 무효화된 영역을 지웁울 수 있습니다.

- SW_INVALIDATE 스크롤 후 *prgnUpdate로* 식별된 영역을 무효화합니다.

- SW_SCROLLCHILDREN *dx* 및 *dy에*지정된 픽셀 수로 *lpRectScroll로* 가리키는 사각형과 교차하는 모든 자식 창을 스크롤합니다. Windows는 이동하지 않더라도 *lpRectScroll과*교차하는 모든 자식 창에 [WM_MOVE](/windows/win32/winmsg/wm-move) 메시지를 보냅니다. 자식 창을 스크롤하고 커서 사각형이 스크롤 사각형과 교차하면 캐리트가 재배치됩니다.

### <a name="return-value"></a>Return Value

반환 값은 SIMPLEREGION (직사각형 무효화 된 영역), COMPLEXREGION (비직사각형 무효화 된 영역, 겹치는 사각형) 또는 NULLREGION (무효화 된 영역 없음) 함수가 성공한 경우; 그렇지 않으면 반환 값은 ERROR입니다.

### <a name="remarks"></a>설명

이 함수는 몇 가지 추가 기능이 있는 [ScrollWindow](/windows/win32/api/winuser/nf-winuser-scrollwindow) 함수와 유사합니다.

[SW_INVALIDATE](/windows/win32/api/winuser/nf-winuser-scrollwindowex) [및 SW_ERASE](/windows/win32/api/winuser/nf-winuser-scrollwindowex) 지정되지 `ScrollWindowEx` 않은 경우 멤버 함수는 스크롤된 영역을 무효화하지 않습니다. 이러한 플래그 중 하나가 `ScrollWindowEx` 설정된 경우 이 영역을 무효화합니다. 응용 프로그램이 [UpdateWindow](/windows/win32/api/winuser/nf-winuser-updatewindow) 멤버 함수를 호출하거나 [RedrawWindow](/windows/win32/api/winuser/nf-winuser-redrawwindow) 멤버 함수(RDW_UPDATENOW [RDW_UPDATENOW](/windows/win32/api/winuser/nf-winuser-redrawwindow) 또는 [RDW_ERASENOW](/windows/win32/api/winuser/nf-winuser-redrawwindow)지정)를 호출하거나 응용 프로그램 큐에서 [WM_PAINT](/windows/win32/gdi/wm-paint) 메시지를 검색할 때까지 영역이 업데이트되지 않습니다.

창에 [WS_CLIPCHILDREN](/windows/win32/api/winuser/nf-winuser-createwindoww) 스타일이 있는 경우 *prgnUpdate* 및 *lpRectUpdate에서* 지정한 반환된 영역은 업데이트가 필요한 자식 창의 모든 영역을 포함하여 업데이트해야 하는 스크롤된 창의 총 영역을 나타냅니다.

[SW_SCROLLCHILDREN](/windows/win32/api/winuser/nf-winuser-scrollwindowex) 플래그를 지정하면 자식 창의 일부가 스크롤되면 Windows가 화면을 제대로 업데이트하지 않습니다. 원본 사각형 외부에 있는 스크롤된 자식 창의 일부는 지워지지 않으며 새 대상에서 제대로 다시 그려지지 않습니다. [DeferWindowPos](/windows/win32/api/winuser/nf-winuser-deferwindowpos) Windows 함수를 사용하여 *lpRectScroll* 사각형 내에 완전히 놓이지 않는 자식 창을 이동합니다. SW_SCROLLCHILDREN 플래그가 설정되고 카어트 사각형이 스크롤 사각형과 교차하면 커서의 위치가 바쳐지습니다.

모든 입력 및 출력 *좌표(lpRectScroll,* *lpRectClip,* *lpRectUpdate*및 *prgnUpdate)는*창에 CS_OWNDC 또는 CS_CLASSDC 클래스 스타일이 있는지 여부에 관계없이 클라이언트 좌표에 있는 것으로 가정합니다. 필요한 경우 [LPtoDP](/windows/win32/api/wingdi/nf-wingdi-lptodp) 및 [DPtoLP](/windows/win32/api/wingdi/nf-wingdi-dptolp) Windows 함수를 사용하여 논리 좌표로 변환합니다.

## <a name="cwndsendchildnotifylastmsg"></a><a name="sendchildnotifylastmsg"></a>CWnd::센드차일드인신고라스트임스그

이 멤버 함수는 하위 창에서 자식 창에 알림 메시지를 제공 하 고 자식 창에서 작업을 처리할 수 있도록 프레임 워크에서 호출 됩니다.

```
BOOL SendChildNotifyLastMsg(LRESULT* pResult = NULL);
```

### <a name="parameters"></a>매개 변수

*p결과*<br/>
상위 창에서 반환할 자식 창에서 생성된 결과입니다.

### <a name="return-value"></a>Return Value

하위 창이 부모에게 보낸 메시지를 처리한 경우 0이 아닙니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

`SendChildNotifyLastMsg`현재 메시지를 반영하는 메시지인 경우 원본으로 보냅니다.

메시지 리플렉션에 대한 자세한 내용은 [반사된 메시지 처리를](../../mfc/handling-reflected-messages.md)참조하십시오.

## <a name="cwndsenddlgitemmessage"></a><a name="senddlgitemmessage"></a>CWnd::보내기Dlg항목메시지

컨트롤에 메시지를 보냅니다.

```
LRESULT SendDlgItemMessage(
    int nID,
    UINT message,
    WPARAM wParam = 0,
    LPARAM lParam = 0);
```

### <a name="parameters"></a>매개 변수

*nID*<br/>
메시지를 수신할 대화 상자 컨트롤의 식별자를 지정합니다.

*message*<br/>
보낼 메시지를 지정합니다.

*wParam*<br/>
추가 메시지 종속 정보를 지정합니다.

*lParam*<br/>
추가 메시지 종속 정보를 지정합니다.

### <a name="return-value"></a>Return Value

컨트롤의 창 프로시저에서 반환되는 값 또는 컨트롤을 찾을 수 없는 경우 0을 지정합니다.

### <a name="remarks"></a>설명

메시지가 `SendDlgItemMessage` 처리될 때까지 멤버 함수가 반환되지 않습니다.

사용은 `SendDlgItemMessage` 주어진 컨트롤에 *를 `CWnd`가져오고 SendMessage 멤버 함수를 호출하는 것과 [동일합니다.](#sendmessage)

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#113](../../mfc/reference/codesnippet/cpp/cwnd-class_54.cpp)]

## <a name="cwndsendmessage"></a><a name="sendmessage"></a>CWnd::보내기 메시지

지정된 메시지를 이 창으로 보냅니다.

```
LRESULT SendMessage(
    UINT message,
    WPARAM wParam = 0,
    LPARAM lParam = 0);
```

### <a name="parameters"></a>매개 변수

*message*<br/>
보낼 메시지를 지정합니다.

*wParam*<br/>
추가 메시지 종속 정보를 지정합니다.

*lParam*<br/>
추가 메시지 종속 정보를 지정합니다.

### <a name="return-value"></a>Return Value

메시지 처리의 결과입니다. 해당 값은 보낸 메시지에 따라 다릅니다.

### <a name="remarks"></a>설명

멤버 `SendMessage` 함수는 창 프로시저를 직접 호출하며 해당 창 프로시저가 메시지를 처리할 때까지 반환되지 않습니다. 이는 메시지를 창의 메시지 큐에 배치하고 즉시 반환하는 [PostMessage](#postmessage) 멤버 함수와 는 대조적입니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#101](../../mfc/reference/codesnippet/cpp/cwnd-class_41.cpp)]

## <a name="cwndsendmessagetodescendants"></a><a name="sendmessagetodescendants"></a>CWnd::보내기 메시지To후손

지정된 Windows 메시지를 모든 하위 창에 보내려면 이 멤버 함수를 호출합니다.

```cpp
void SendMessageToDescendants(
    UINT message,
    WPARAM wParam = 0,
    LPARAM lParam = 0,
    BOOL bDeep = TRUE,
    BOOL bOnlyPerm = FALSE);
```

### <a name="parameters"></a>매개 변수

*message*<br/>
보낼 메시지를 지정합니다.

*wParam*<br/>
추가 메시지 종속 정보를 지정합니다.

*lParam*<br/>
추가 메시지 종속 정보를 지정합니다.

*bDeep*<br/>
검색할 수준을 지정합니다. TRUE인 경우 모든 자식을 재귀적으로 검색합니다. FALSE인 경우 즉시 자식만 검색합니다.

*bOnlyPerm*<br/>
임시 창에서 메시지를 받을지 여부를 지정합니다. TRUE인 경우 임시 창은 메시지를 받을 수 있습니다. FALSE인 경우 영구 창만 메시지를 받습니다. 임시 창에 대한 자세한 내용은 [기술 참고 3을](../../mfc/tn003-mapping-of-windows-handles-to-objects.md)참조하십시오.

### <a name="remarks"></a>설명

*bDeep가* FALSE이면 메시지는 창의 바로 자식에게만 전송됩니다. 그렇지 않으면 메시지가 모든 하위 창으로 전송됩니다.

*bDeep* 및 *bOnlyPerm이* TRUE인 경우 검색은 임시 창 아래에서 계속됩니다. 이 경우 검색 중에 발생한 영구 창만 메시지를 받습니다. *bDeep가* FALSE이면 메시지는 창의 바로 자식에게만 전송됩니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#114](../../mfc/reference/codesnippet/cpp/cwnd-class_55.cpp)]

## <a name="cwndsendnotifymessage"></a><a name="sendnotifymessage"></a>CWnd:::송신 메시지

지정된 메시지를 창으로 보냅니다.

```
BOOL SendNotifyMessage(
    UINT message,
    WPARAM wParam,
    LPARAM lParam);
```

### <a name="parameters"></a>매개 변수

*message*<br/>
보낼 메시지를 지정합니다.

*wParam*<br/>
추가 메시지 종속 정보를 지정합니다.

*lParam*<br/>
추가 메시지 종속 정보를 지정합니다.

### <a name="return-value"></a>Return Value

함수가 성공하면 0이 아니고 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

호출 스레드에서 창을 만든 경우 `SendNotifyMessage` 창에 대 한 창 프로시저를 호출 하 고 창 프로시저가 메시지를 처리할 때까지 반환 되지 않습니다. 다른 스레드에서 창을 만든 경우 `SendNotifyMessage` 메시지를 창 프로시저에 전달하고 즉시 반환합니다. 창 프로시저가 메시지 처리를 완료할 때까지 기다리지 않습니다.

## <a name="cwndsetactivewindow"></a><a name="setactivewindow"></a>CWnd::설정활성창

활성 `CWnd` 창을 만듭니다.

```
CWnd* SetActiveWindow();
```

### <a name="return-value"></a>Return Value

이전에 활성 상태였던 창입니다.

반환된 포인터는 일시적일 수 있으며 나중에 사용할 수 없습니다.

### <a name="remarks"></a>설명

멤버 `SetActiveWindow` 함수는 응용 프로그램이 임의로 활성 창 및 입력 포커스를 인수할 수 있으므로 주의해야 합니다. 일반적으로 Windows는 모든 정품 인증을 처리합니다.

## <a name="cwndsetcapture"></a><a name="setcapture"></a>CWnd::세트캡처

커서의 위치에 관계없이 모든 후속 `CWnd` 마우스 입력이 현재 개체로 전송됩니다.

```
CWnd* SetCapture();
```

### <a name="return-value"></a>Return Value

이전에 모든 마우스 입력을 받은 창 개체에 대한 포인터입니다. 이러한 창이 없는 경우 NULL입니다. 반환된 포인터는 일시적일 수 있으며 나중에 사용할 수 없습니다.

### <a name="remarks"></a>설명

더 `CWnd` 이상 모든 마우스 입력이 필요하지 않은 경우 다른 창에서 마우스 입력을 받을 수 있도록 응용 프로그램에서 [ReleaseCapture](/windows/win32/api/winuser/nf-winuser-releasecapture) 함수를 호출해야 합니다.

마우스 입력이 캡처되는 동안 WM_NCHITTEST 또는 WM_SETCURSOR 메시지는 활성 창으로 전송되지 않습니다.

## <a name="cwndsetcaretpos"></a><a name="setcaretpos"></a>CWnd::세트케어포스

캐리트의 위치를 설정합니다.

```
static void PASCAL SetCaretPos(POINT point);
```

### <a name="parameters"></a>매개 변수

*지점*<br/>
카포트의 새 x 및 y 좌표(클라이언트 좌표)를 지정합니다.

### <a name="remarks"></a>설명

멤버 `SetCaretPos` 함수는 현재 작업의 창에 의해 소유된 경우에만 caret를 이동합니다. `SetCaretPos`는 카를타가 숨겨져 있는지 여부를 이동합니다.

카포트는 공유 리소스입니다. 창은 캐리트가 소유하지 않은 경우 카를일을 이동해서는 안됩니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#115](../../mfc/reference/codesnippet/cpp/cwnd-class_56.cpp)]

## <a name="cwndsetclipboardviewer"></a><a name="setclipboardviewer"></a>CWnd::세트클립보드 뷰어

클립보드의 콘텐츠가 변경될 때마다 이 창을 WM_DRAWCLIPBOARD 메시지를 통해 알림을 받는 창 체인에 추가합니다.

```
HWND SetClipboardViewer();
```

### <a name="return-value"></a>Return Value

성공하면 클립보드 뷰어 체인의 다음 창에 대한 핸들입니다. 응용 프로그램은 이 핸들을 저장하고(멤버 변수로 저장할 수 있음) Clipboard-Viewer 체인 메시지에 응답할 때 이 핸들을 사용해야 합니다.

### <a name="remarks"></a>설명

Clipboard-뷰어 체인의 일부인 창은 [WM_DRAWCLIPBOARD](#ondrawclipboard) [WM_CHANGECBCHAIN](#onchangecbchain)및 [WM_DESTROY](#ondestroy) 메시지에 응답하고 메시지를 체인의 다음 창으로 전달해야 합니다.

이 멤버 함수는 WM_DRAWCLIPBOARD 메시지를 창에 보냅니다. Clipboard-Viewer 체인의 다음 창에 대한 핸들이 아직 반환되지 않았기 때문에 응용 프로그램은 호출 중에 수신하는 WM_DRAWCLIPBOARD 메시지를 전달하지 않아야 합니다. `SetClipboardViewer`

클립보드 뷰어 체인에서 자신을 제거하려면 응용 프로그램에서 [ChangeClipboardChain](#changeclipboardchain) 멤버 함수를 호출해야 합니다.

## <a name="cwndsetdlgctrlid"></a><a name="setdlgctrlid"></a>CWnd::SetDlgCtrlID

창의 창 ID 또는 컨트롤 ID를 새 값으로 설정합니다.

```
int SetDlgCtrlID(int nID);
```

### <a name="parameters"></a>매개 변수

*nID*<br/>
컨트롤의 식별자를 위해 설정할 새 값입니다.

### <a name="return-value"></a>Return Value

성공한 경우 창의 이전 식별자입니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

창은 대화 상자의 컨트롤뿐만 아니라 모든 자식 창이 될 수 있습니다. 창은 최상위 창이 될 수 없습니다.

## <a name="cwndsetdlgitemint"></a><a name="setdlgitemint"></a>CWnd::SetDlgItemInt

대화 상자에서 지정된 컨트롤의 텍스트를 지정된 정수 값의 문자열 표현으로 설정합니다.

```cpp
void SetDlgItemInt(
    int nID,
    UINT nValue,
    BOOL bSigned = TRUE);
```

### <a name="parameters"></a>매개 변수

*nID*<br/>
변경할 컨트롤의 정수 ID를 지정합니다.

*n값*<br/>
항목 텍스트를 생성하는 데 사용되는 정수 값을 지정합니다.

*b 서명됨*<br/>
정수 값이 서명되었는지 서명되지 않았는지 또는 서명되지 않았는지 지정합니다. 이 매개 변수가 TRUE이면 *nValue가* 서명됩니다. 이 매개 변수가 TRUE이고 *nValue가* 0보다 작은 경우 문자열의 첫 번째 숫자 앞에 빼기 기호가 배치됩니다. 이 매개 변수가 FALSE이면 *nValue가* 서명되지 않습니다.

### <a name="remarks"></a>설명

`SetDlgItemInt`지정된 컨트롤에 [WM_SETTEXT](/windows/win32/winmsg/wm-settext) 메시지를 보냅니다.

### <a name="example"></a>예제

  [CWnd::SetDlgItemText에](#setdlgitemtext)대한 예제를 참조하십시오.

## <a name="cwndsetdlgitemtext"></a><a name="setdlgitemtext"></a>CWnd::SetDlg항목텍스트

창 또는 대화 상자가 소유한 컨트롤의 캡션 또는 텍스트를 설정합니다.

```cpp
void SetDlgItemText(
    int nID,
    LPCTSTR lpszString);
```

### <a name="parameters"></a>매개 변수

*nID*<br/>
텍스트를 설정할 컨트롤을 식별합니다.

*lpszString*<br/>
컨트롤에 복사할 텍스트를 포함하는 [CString](../../atl-mfc-shared/reference/cstringt-class.md) 개체 또는 null 종료된 문자열을 가리킵니다.

### <a name="remarks"></a>설명

`SetDlgItemText`지정된 컨트롤에 [WM_SETTEXT](/windows/win32/winmsg/wm-settext) 메시지를 보냅니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#116](../../mfc/reference/codesnippet/cpp/cwnd-class_57.cpp)]

## <a name="cwndsetforegroundwindow"></a><a name="setforegroundwindow"></a>CWnd:::세포그라운드윈도우

창을 만든 스레드를 전경으로 전환하고 창을 활성화합니다.

```
BOOL SetForegroundWindow();
```

### <a name="return-value"></a>Return Value

함수가 성공하면 0이 아니고 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

키보드 입력은 창으로 이동하며 사용자에 대한 다양한 시각적 신호가 변경됩니다. 전경 창은 사용자가 현재 작업 중인 창입니다. 전경 창은 최상위 창(프레임 창 또는 대화 상자)에만 적용됩니다.

### <a name="example"></a>예제

  [CWnd::FindWindow](#findwindow)에 대한 예제를 참조하십시오.

## <a name="cwndsetfocus"></a><a name="setfocus"></a>CWnd::설정 포커스

입력 포커스를 클레임합니다.

```
CWnd* SetFocus();
```

### <a name="return-value"></a>Return Value

이전에 입력 포커스가 있었던 창 개체에 대한 포인터입니다. 이러한 창이 없는 경우 NULL입니다. 반환된 포인터는 일시적일 수 있으며 저장해서는 안 됩니다.

### <a name="remarks"></a>설명

입력 포커스는 이후의 모든 키보드 입력을 이 창으로 향하게 합니다. 이전에 입력 포커스가 있었던 모든 창은 손실됩니다.

멤버 `SetFocus` 함수는 입력 포커스를 잃는 [WM_KILLFOCUS](/windows/win32/inputdev/wm-killfocus) 메시지를 창에 보내고 입력 포커스를 받는 창에 [WM_SETFOCUS](/windows/win32/inputdev/wm-setfocus) 메시지를 보냅니다. 또한 창 또는 해당 부모를 활성화합니다.

현재 창이 활성 상태이지만 포커스가 없는 경우(즉, 창에 포커스가 없음) 누른 키로 [인해 메시지가 WM_SYSCHAR,](#onsyschar) [WM_SYSKEYDOWN](#onsyskeydown)또는 [WM_SYSKEYUP.](#onsyskeyup)

## <a name="cwndsetfont"></a><a name="setfont"></a>CWnd::세트폰트

지정된 글꼴을 사용하도록 WM_SETFONT 메시지를 창으로 보냅니다.

```cpp
void SetFont(
    CFont* pFont,
    BOOL bRedraw = TRUE);
```

### <a name="parameters"></a>매개 변수

*pFont*<br/>
`CFont` 개체에 대한 포인터입니다.

*bRedraw*<br/>
true 창이 WM_SETFONT 메시지를 처리한 직후에 다시 그릴 수 있습니다. 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

창에서 WM_SETFONT 메시지를 처리하지 않으면 이 메서드는 영향을 주지 않습니다. WM_SETFONT 메시지에 대한 메시지 `CWnd` 처리기를 포함하는 미리 정의된 창 클래스에 연결되기 때문에 이 메시지를 프로세스에서 파생하는 많은 MFC 클래스입니다. 이 메서드를 사용 하려면 파생 `CWnd` 된 클래스 WM_SETFONT 메시지에 대 한 메서드 처리기를 정의 해야 합니다.

## <a name="cwndseticon"></a><a name="seticon"></a>CWnd::Seticon

이 멤버 함수를 호출하여 *hIcon*에서 식별한 핸들을 특정 아이콘으로 설정합니다.

```
HICON SetIcon(
    HICON hIcon,
    BOOL bBigIcon);
```

### <a name="parameters"></a>매개 변수

*hIcon*<br/>
이전 아이콘의 핸들입니다.

*bBigIcon*<br/>
TRUE인 경우 32픽셀 x 32픽셀 아이콘을 지정합니다. FALSE인 경우 16픽셀 x 16픽셀 아이콘을 지정합니다.

### <a name="return-value"></a>Return Value

아이콘에 대한 핸들입니다.

### <a name="remarks"></a>설명

창 클래스가 등록되면 아이콘이 선택됩니다.

### <a name="example"></a>예제

  [CWnd::GetSystemMenu에](#getsystemmenu)대한 예제를 참조하십시오.

## <a name="cwndsetlayeredwindowattributes"></a><a name="setlayeredwindowattributes"></a>CWnd::설정 계층화창속성

계층적 창의 불투명도 및 투명도 색상 키를 설정합니다.

```
BOOL SetLayeredWindowAttributes(
    COLORREF crKey,
    BYTE bAlpha,
    DWORD dwFlags);
```

### <a name="parameters"></a>매개 변수

*crKey*<br/>
계층화된 창을 작성할 때 사용할 투명도 색상 키를 지정하는 COLORREF 값에 대한 포인터입니다. 이 색상의 창으로 그려진 모든 픽셀은 투명합니다. COLORREF를 생성하려면 RGB 매크로를 사용합니다.

*b알파 (주)*<br/>
계층화된 창의 불투명도를 설명하는 데 사용되는 알파 값입니다. 자세한 내용은 `SourceConstantAlpha` [BLENDFUNCTION](/windows/win32/api/wingdi/ns-wingdi-blendfunction) 구조의 멤버를 참조하십시오. *bAlpha가* 0이면 창이 완전히 투명해지요. *bAlpha가* 255이면 창이 불투명합니다.

*dwFlags*<br/>
취할 작업을 지정합니다. 이 매개 변수는 다음 값 중 하나 이상일 수 있습니다. 가능한 값 목록은 [SetLayered창 속성](/windows/win32/api/winuser/nf-winuser-setlayeredwindowattributes)을 참조하십시오.

### <a name="return-value"></a>Return Value

함수가 성공하면 0이 아니고 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

이 멤버 함수는 Windows SDK에 설명된 대로 [SetLayeredWindowAttributes](/windows/win32/api/winuser/nf-winuser-setlayeredwindowattributes)함수의 기능을 에뮬레이트합니다.

## <a name="cwndsetmenu"></a><a name="setmenu"></a>CWnd::세트 메뉴

현재 메뉴를 지정된 메뉴로 설정합니다.

```
BOOL SetMenu(CMenu* pMenu);
```

### <a name="parameters"></a>매개 변수

*p메뉴*<br/>
새 메뉴를 식별합니다. 이 매개 변수가 NULL이면 현재 메뉴가 제거됩니다.

### <a name="return-value"></a>Return Value

메뉴가 변경된 경우 0이 아닙니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

메뉴 변경 을 반영하도록 창을 다시 그려줍니다.

`SetMenu`이전 메뉴를 파괴하지 않습니다. 응용 프로그램은 이 작업을 수행하려면 [CMenu::DestroyMenu](../../mfc/reference/cmenu-class.md#destroymenu) 멤버 함수를 호출해야 합니다.

### <a name="example"></a>예제

  [CMenu::LoadMenu](../../mfc/reference/cmenu-class.md#loadmenu)의 예제를 참조하십시오.

## <a name="cwndsetowner"></a><a name="setowner"></a>CWnd::세트 소유자

현재 창의 소유자를 지정된 창 개체로 설정합니다.

```cpp
void SetOwner(CWnd* pOwnerWnd);
```

### <a name="parameters"></a>매개 변수

*p오너Wnd*<br/>
창 개체의 새 소유자를 식별합니다. 이 매개 변수가 NULL이면 창 개체에 소유자가 없습니다.

### <a name="remarks"></a>설명

이 소유자는 현재 창 개체에서 명령 메시지를 받을 수 있습니다. 기본적으로 현재 창의 상위 는 해당 소유자입니다.

창 계층 구조와 관련이 없는 창 개체 간의 연결을 설정하는 것이 유용한 경우가 많습니다. 예를 들어 [CToolBar는](../../mfc/reference/ctoolbar-class.md) 상위 소유자가 아닌 소유자에게 알림을 보냅니다. 이렇게 하면 도구 모음이 한 창(예: OLE 컨테이너 응용 프로그램 창)의 자식이 될 수 있으며 다른 창(예: 내부 프레임 창)으로 알림을 보낼 수 있습니다. 또한 현재 편집 중에 서버 창이 비활성화되거나 활성화되면 프레임 창이 소유한 모든 창이 숨기거나 표시됩니다. 이 소유권은 `SetOwner`에 대한 호출로 명시적으로 설정됩니다.

이 함수의 소유권 개념은 [GetWindow의](/windows/win32/api/winuser/nf-winuser-getwindow)소유권 개념과 다릅니다.

## <a name="cwndsetparent"></a><a name="setparent"></a>CWnd::설정부모

하위 창의 상위 창을 변경합니다.

```
CWnd* SetParent(CWnd* pWndNewParent);
```

### <a name="parameters"></a>매개 변수

*pWndNewParent*<br/>
새 상위 창을 식별합니다.

### <a name="return-value"></a>Return Value

성공하면 이전 부모 창 개체에 대한 포인터입니다. 반환된 포인터는 일시적일 수 있으며 나중에 사용할 수 없습니다.

### <a name="remarks"></a>설명

하위 창이 표시되면 Windows에서 적절한 다시 그리기 및 다시 그리기를 수행합니다.

## <a name="cwndsetproperty"></a><a name="setproperty"></a>CWnd::설정 속성

*dwDispID에*의해 지정된 OLE 제어 속성을 설정하려면 이 멤버 함수를 호출합니다.

```cpp
void AFX_CDECL SetProperty(
    DISPID dwDispID,
    VARTYPE vtProp, ...);
```

### <a name="parameters"></a>매개 변수

*dwDispID*<br/>
설정할 속성을 확인합니다.

*vtProp*<br/>
설정할 속성의 유형을 지정합니다. 가능한 값에 대해서는 [COleDispatchDriver::InvokeHelper](../../mfc/reference/coledispatchdriver-class.md#invokehelper)의 설명 섹션을 참조하세요.

*...*<br/>
*vtProp에*의해 지정된 형식의 단일 매개 변수입니다.

### <a name="remarks"></a>설명

> [!NOTE]
> 이 함수는 OLE 컨트롤을 `CWnd` 나타내는 개체에서만 호출해야 합니다.

OLE 제어 컨테이너를 사용하여 이 멤버 함수를 사용하는 것에 대한 자세한 내용은 [ActiveX 제어 컨테이너: ActiveX 제어 컨테이너에서 ActiveX 컨트롤 프로그래밍](../../mfc/programming-activex-controls-in-a-activex-control-container.md)문서 문서를 참조하십시오.

## <a name="cwndsetredraw"></a><a name="setredraw"></a>CWnd:::세트 다시 그리기

응용 프로그램은 `SetRedraw` 변경 내용을 다시 그릴 수 있도록 허용하거나 변경 내용이 다시 그려지지 않도록 하기 위해 호출됩니다.

```cpp
void SetRedraw(BOOL bRedraw = TRUE);
```

### <a name="parameters"></a>매개 변수

*bRedraw*<br/>
다시 그리기 플래그의 상태를 지정합니다. 이 매개변수가 TRUE이면 다시 그리기 플래그가 설정됩니다. FALSE이면 플래그가 지워집니다.

### <a name="remarks"></a>설명

이 멤버 함수는 다시 그리기 플래그를 설정하거나 지웁습니다. 다시 그리기 플래그가 지워지는 동안에는 변경 후 내용이 업데이트되지 않으며 다시 그리기 플래그가 설정될 때까지 다시 그려지지 않습니다. 예를 들어 목록 상자에 여러 항목을 추가해야 하는 응용 프로그램은 다시 그리기 플래그를 지우고 항목을 추가한 다음 다시 그리기 플래그를 설정할 수 있습니다. 마지막으로 응용 프로그램은 목록 상자를 다시 그릴 수 있도록 [무효화](#invalidate) 또는 [무효화Rect](#invalidaterect) 멤버 함수를 호출할 수 있습니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#117](../../mfc/reference/codesnippet/cpp/cwnd-class_58.cpp)]

## <a name="cwndsetscrollinfo"></a><a name="setscrollinfo"></a>CWnd::Set스크롤정보

이 멤버 함수를 호출하여 `SCROLLINFO` 구조가 스크롤 막대에 대해 유지 관리하는 정보를 설정합니다.

```
BOOL SetScrollInfo(
    int nBar,
    LPSCROLLINFO lpScrollInfo,
    BOOL bRedraw = TRUE);
```

### <a name="parameters"></a>매개 변수

*nBar*<br/>
스크롤 막대가 컨트롤인지 창의 비클라이언트 영역의 일부인지 지정합니다. 클라이언트가 아닌 영역의 일부인 경우 nBar는 스크롤 막대가 가로, 세로 또는 둘 다에 배치되어 있는지 여부도 나타냅니다. 다음 중 하나여야 합니다.

- SB_CTL 스크롤 막대 컨트롤에 대한 매개 변수를 포함합니다. 데이터 `m_hWnd` 멤버는 스크롤 막대 컨트롤의 핸들이어야 합니다.

- SB_HORZ 창이 가로 스크롤 막대임을 지정합니다.

- SB_VERT 창이 세로 스크롤 막대임을 지정합니다.

*lp스크롤정보*<br/>
[SCROLLINFO](/windows/win32/api/winuser/ns-winuser-scrollinfo) 구조에 대한 포인터입니다. 이 구조에 대한 자세한 내용은 Windows SDK를 참조하십시오.

*bRedraw*<br/>
스크롤 막대를 새 위치를 반영하도록 다시 그려야 하는지 여부를 지정합니다. *bRedraw가* TRUE이면 스크롤 막대가 다시 그려집니다. FALSE이면 다시 그려지지 않습니다. 스크롤 막대는 기본적으로 다시 그려집니다.

### <a name="return-value"></a>Return Value

성공하면 반환은 TRUE입니다. 그렇지 않으면 FALSE입니다.

### <a name="remarks"></a>설명

[SCROLLINFO](/windows/win32/api/winuser/ns-winuser-scrollinfo) 구조에는 최소 및 최대 스크롤 위치, 페이지 크기 및 스크롤 상자(엄지 손가락)의 위치를 비롯한 스크롤 막대에 대한 정보가 포함되어 있습니다. 구조 `SCROLLINFO` 기본값 변경에 대한 자세한 내용은 Windows SDK의 구조 항목을 참조하십시오.

스크롤 막대 위치를 나타내는 MFC Windows 메시지 처리기, [CWnd::OnH스크롤](#onhscroll) 및 [CWnd::OnVScroll은](#onvscroll)16비트의 위치 데이터만 제공합니다. [GetScrollInfo](#getscrollinfo) `SetScrollInfo` 및 스크롤 막대 위치 데이터의 32 비트를 제공합니다. 따라서 응용 프로그램은 `GetScrollInfo` 처리 중 `CWnd::OnHScroll` `CWnd::OnVScroll` 하나를 호출하거나 32비트 스크롤 막대 위치 데이터를 가져올 수 있습니다.

> [!NOTE]
> [CWnd:GetScrollInfo](#getscrollinfo) 응용 프로그램에서 32비트 스크롤 막대 위치를 사용할 수 있습니다.

## <a name="cwndsetscrollpos"></a><a name="setscrollpos"></a>CWnd::Set스크롤포스

스크롤 상자의 현재 위치를 설정하고 요청하면 스크롤 막대를 다시 그려 스크롤 상자의 새 위치를 반영합니다.

```
int SetScrollPos(
    int nBar,
    int nPos,
    BOOL bRedraw = TRUE);
```

### <a name="parameters"></a>매개 변수

*nBar*<br/>
설정할 스크롤 막대를 지정합니다. 이 매개 변수는 다음 중 하나일 수 있습니다.

- SB_HORZ 창의 가로 스크롤 막대에서 스크롤 상자의 위치를 설정합니다.

- SB_VERT 창의 세로 스크롤 막대에서 스크롤 상자의 위치를 설정합니다.

*Npos*<br/>
스크롤 상자의 새 위치를 지정합니다. 스크롤 범위 내에 있어야 합니다.

*bRedraw*<br/>
스크롤 막대를 새 스크롤 상자 위치를 반영하도록 다시 칠할지 여부를 지정합니다. 이 매개 변수가 TRUE이면 스크롤 막대가 다시 그려집니다. FALSE이면 스크롤 막대가 다시 그려지지 않습니다.

### <a name="return-value"></a>Return Value

스크롤 상자의 이전 위치입니다.

### <a name="remarks"></a>설명

*bRedraw를* FALSE로 설정하면 스크롤 막대가 다른 함수에 대한 후속 호출로 다시 그려질 때마다 유용합니다.

## <a name="cwndsetscrollrange"></a><a name="setscrollrange"></a>CWnd::세트스크롤 레인지

지정된 스크롤 막대에 대한 최소 및 최대 위치 값을 설정합니다.

```cpp
void SetScrollRange(
    int nBar,
    int nMinPos,
    int nMaxPos,
    BOOL bRedraw = TRUE);
```

### <a name="parameters"></a>매개 변수

*nBar*<br/>
설정할 스크롤 막대를 지정합니다. 이 매개 변수는 다음 값 중 하나일 수 있습니다.

- SB_HORZ 창의 가로 스크롤 막대 범위를 설정합니다.

- SB_VERT 창의 세로 스크롤 막대 범위를 설정합니다.

*n민포스*<br/>
최소 스크롤 위치를 지정합니다.

*n맥스포스*<br/>
최대 스크롤 위치를 지정합니다.

*bRedraw*<br/>
변경 내용을 반영하도록 스크롤 막대를 다시 그려야 하는지 여부를 지정합니다. *bRedraw가* TRUE이면 스크롤 막대가 다시 그려집니다. FALSE이면 스크롤 막대가 다시 그려지지 않습니다.

### <a name="remarks"></a>설명

표준 스크롤 막대를 숨기거나 표시하는 데도 사용할 수 있습니다.

응용 프로그램은 스크롤 막대 알림 메시지를 처리하는 동안 스크롤 막대를 숨기기 위해 이 함수를 호출해서는 안 됩니다.

즉시 `SetScrollRange` [SetScrollPos](#setscrollpos) 멤버 함수에 대 한 호출을 따르는 경우 `SetScrollPos` 멤버 함수의 *bRedraw* 매개 변수는 0 이어야합니다 스크롤 막대가 두 번 그려지지 않도록 합니다.

표준 스크롤 막대의 기본 범위는 0부터 100사이입니다. 스크롤 막대 컨트롤의 기본 범위는 비어 *있습니다(nMinPos* 및 *nMaxPos* 값은 모두 0). *nMinPos와* *nMaxPos에서* 지정한 값 의 차이는 INT_MAX 초과해서는 안 됩니다.

## <a name="cwndsettimer"></a><a name="settimer"></a>CWnd::설정 타이머

시스템 타이머를 설치합니다.

```
UINT_PTR SetTimer(
    UINT_PTR nIDEvent,
    UINT nElapse,
    void (CALLBACK* lpfnTimer)(HWND,
    UINT,
    UINT_PTR,
    DWORD));
```

### <a name="parameters"></a>매개 변수

*니디이벤트*<br/>
영하가 아닌 타이머 식별자를 지정합니다. 타이머 식별자가 고유하면 `SetTimer`에서 이 동일한 값이 반환됩니다. 그렇지 `SetTimer` 않으면 새 고유 값을 결정하고 반환합니다. NULL 콜백 기능이 있는 창 타이머의 경우 값은 현재 창과 연결된 다른 창 타이머에 대해서만 고유해야 합니다. 콜백 타이머의 경우 값은 모든 프로세스의 모든 타이머에 대해 고유해야 합니다. 따라서 콜백 타이머를 만들 때 반환된 값이 지정한 값과 다를 가능성이 높습니다.

*nElapse*<br/>
시간 시간 값 또는 간격을 밀리초 단위로 지정합니다.

*lpfn타이머*<br/>
[WM_TIMER](/windows/win32/winmsg/wm-timer) 메시지를 처리하는 응용 프로그램에서 `TimerProc` 제공하는 콜백 함수의 주소를 지정합니다. 이 매개 변수가 NULL이면 WM_TIMER 메시지는 응용 프로그램의 메시지 큐에 배치되고 개체에서 `CWnd` 처리됩니다.

### <a name="return-value"></a>Return Value

함수가 성공하면 새 타이머의 타이머 식별자입니다. 이 값은 *nIDEvent* 매개 변수를 통해 전달된 값과 같거나 같지 않을 수 있습니다. 응용 프로그램은 항상 타이머를 죽이기 위해 [KillTimer](#killtimer) 멤버 함수에 반환 값을 전달해야 합니다. 성공하면 영하지 않은; 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

간격 값이 지정되고 간격이 경과할 때마다 시스템은 설치 응용 프로그램의 설치 메시지 큐에 WM_TIMER 메시지를 게시하거나 응용 프로그램 `TimerProc` 정의 콜백 함수에 메시지를 전달합니다.

*lpfnTimer* 콜백 함수의 이름을 `TimerProc`지정할 필요는 없지만 정적으로 선언하고 다음과 같이 정의해야 합니다.

```cpp
void CALLBACK TimerProc(
    HWND hWnd,   // handle of CWnd that called SetTimer
    UINT nMsg,   // WM_TIMER
    UINT_PTR nIDEvent,   // timer identification
    DWORD dwTime    // system time);
```

### <a name="example"></a>예제

이 `CWnd::SetTimer`예제에서는 `CWnd::OnTimer`에서 `CWnd::KillTimer` 및 WM_TIMER 메시지를 처리 합니다. 에서 2초마다 메인 프레임 창에 WM_TIMER 메시지를 보내도록 첫 `OnStartTimer`번째 타이머가 설정됩니다. 이벤트 `OnTimer` 처리기는 주 프레임 창에 대한 WM_TIMER 메시지를 처리합니다. 이 방법을 사용하면 PC 스피커가 2초마다 비프음이 울립니다. 두 번째 타이머는 3.75초마다 콜백 함수에 메시지를 보냅니다. `OnStopTimer`각 타이머 ID를 `CWnd::KillTimer` 호출하여 두 타이머를 중지합니다.

[!code-cpp[NVC_MFCWindowing#118](../../mfc/reference/codesnippet/cpp/cwnd-class_59.cpp)]

## <a name="cwndsetwindowcontexthelpid"></a><a name="setwindowcontexthelpid"></a>CWnd::SetWindow컨텍스트도움말

이 멤버 함수를 호출하여 도움말 컨텍스트 식별자를 지정된 창과 연결합니다.

```
BOOL SetWindowContextHelpId(DWORD dwContextHelpId);
```

### <a name="parameters"></a>매개 변수

*dwContextHelpId*<br/>
도움말 컨텍스트 식별자입니다.

### <a name="return-value"></a>Return Value

함수가 성공하면 0이 아니고 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

자식 창에 도움말 컨텍스트 식별자가 없는 경우 부모 창의 식별자를 상속 합니다. 마찬가지로 소유 된 창에 도움말 컨텍스트 식별자가 없는 경우 소유자 창의 식별자를 상속 합니다. 도움말 컨텍스트 식별자의 이 상속을 사용하면 응용 프로그램에서 대화 상자와 모든 컨트롤에 대해 하나의 식별자만 설정할 수 있습니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#119](../../mfc/reference/codesnippet/cpp/cwnd-class_60.cpp)]

## <a name="cwndsetwindowplacement"></a><a name="setwindowplacement"></a>CWnd::설정창 배치

창의 표시 상태와 일반(복원됨), 최소화 및 최대화 위치를 설정합니다.

```
BOOL SetWindowPlacement(const WINDOWPLACEMENT* lpwndpl);
```

### <a name="parameters"></a>매개 변수

*lpwndpl*<br/>
새 표시 상태 및 위치를 지정하는 [WINDOW 배치](/windows/win32/api/winuser/ns-winuser-windowplacement) 구조를 가리킵니다.

### <a name="return-value"></a>Return Value

함수가 성공하면 0이 아니고 그렇지 않으면 0입니다.

## <a name="cwndsetwindowpos"></a><a name="setwindowpos"></a>CWnd:::세트윈도우포스

자식, 팝업 및 최상위 창의 크기, 위치 및 Z 순서를 변경합니다.

```
BOOL SetWindowPos(
    const CWnd* pWndInsertAfter,
    int x,
    int y,
    int cx,
    int cy,
    UINT nFlags);
```

### <a name="parameters"></a>매개 변수

*pWnd인더*<br/>
Z 순서에서 `CWnd` 이 `CWnd` 개체 앞에 올 오브젝트(보다 높은 개체)를 식별합니다. 이 매개 변수는 a에 `CWnd` 대한 포인터 또는 다음 값 중 하나에 대한 포인터일 수 있습니다.

- `wndBottom`창을 Z 순서의 맨 아래에 배치합니다. 최상위 `CWnd` 창인 경우 창은 최상위 상태를 잃게 됩니다. 시스템은 다른 모든 창의 맨 아래에 창을 배치합니다.

- `wndTop`창을 Z 순서의 맨 위에 배치합니다.

- `wndTopMost`창을 맨 위가 아닌 모든 창 위에 배치합니다. 창이 비활성화된 경우에도 최상위 위치를 유지합니다.

- `wndNoTopMost`창을 맨 위가 아닌 모든 창 의 맨 위(즉, 맨 위 의 모든 창 뒤에) 위로 재배치합니다. 창이 이미 맨 위가 아닌 창인 경우 이 플래그는 영향을 주지 않습니다.

이 매개 변수를 사용하는 방법에 대한 규칙은 이 항목의 "비고" 절을 참조하십시오.

*x*<br/>
창 왼쪽의 새 위치를 지정합니다.

*Y*<br/>
창 상단의 새 위치를 지정합니다.

*Cx*<br/>
창의 새 너비를 지정합니다.

*Cy*<br/>
창의 새 높이를 지정합니다.

*nFlags*<br/>
크기 조정 및 위치 지정 옵션을 지정합니다. 이 매개 변수는 다음 플래그의 조합일 수 있습니다.

- SWP_DRAWFRAME 창 주위에 프레임(창이 생성되었을 때 정의됨)을 그립니다.

- SWP_FRAMECHANGED 창의 크기가 변경되지 않더라도 WM_NCCALCSIZE 메시지를 창으로 보냅니다. 이 플래그를 지정하지 않으면 창의 크기가 변경될 때만 WM_NCCALCSIZE 전송됩니다.

- SWP_HIDEWINDOW 창을 숨깁니다.

- SWP_NOACTIVATE 창을 활성화하지 않습니다. 이 플래그가 설정되지 않은 경우 창이 활성화되고 맨 위 또는 맨 위가 아닌 *그룹(pWndInsertAfter* 매개변수의 설정에 따라 다름)의 맨 위로 이동됩니다.

- SWP_NOCOPYBITS 클라이언트 영역의 전체 내용을 삭제합니다. 이 플래그를 지정하지 않으면 창의 크기를 조정하거나 재배치한 후 클라이언트 영역의 유효한 내용이 저장되고 다시 클라이언트 영역으로 복사됩니다.

- SWP_NOMOVE 현재 위치를 유지합니다(x 및 *y* 매개변수무시). *x*

- SWP_NOOWNERZORDER Z 순서의 소유자 창의 위치를 변경하지 않습니다.

- SWP_NOREDRAW 변경 내용을 다시 그리지 않습니다. 이 플래그를 설정하면 어떤 종류의 재도장도 발생하지 않습니다. 이는 클라이언트 영역, 비클라이언트 영역(제목 및 스크롤 막대 포함) 및 이동된 창의 결과로 발견된 상위 창의 모든 부분에 적용됩니다. 이 플래그를 설정하면 응용 프로그램은 다시 그려야 하는 창 및 상위 창의 일부를 명시적으로 무효화하거나 다시 그려야 합니다.

- SWP_NOREPOSITION SWP_NOOWNERZORDER 동일합니다.

- SWP_NOSENDCHANGING 창이 WM_WINDOWPOSCHANGING 메시지를 받지 못하게 합니다.

- SWP_NOSIZE 현재 크기를 유지합니다(cx 및 *cy* 매개변수무시). *cx*

- SWP_NOZORDER 현재 순서를 *유지합니다(pWndInsertAfter*무시).

- SWP_SHOWWINDOW 창을 표시합니다.

### <a name="return-value"></a>Return Value

함수가 성공하면 0이 아닙니다. 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

Windows는 Z 순서에 따라 화면에 정렬됩니다. Z 순서의 맨 위에 있는 창이 다른 모든 창 위에 순서대로 나타납니다.

자식 창의 모든 좌표는 클라이언트 좌표(부모 창의 클라이언트 영역의 왼쪽 위 모서리에 상대)입니다.

*pWndInsertAfter* 매개 변수를 **&wndTopMost로** 설정하고 SWP_NOZORDER 플래그가 설정되지 않도록 하거나 창의 Z 순서를 설정하여 창이 기존 최상위 창 위에 있도록 창을 Z 순서의 맨 위로 이동할 수 있습니다. 맨 위에 없는 창이 맨 위로 만들어지면 소유 한 창도 맨 위로 만들어집니다. 소유자는 변경되지 않습니다.

맨 위 창은 Z 순서의 맨 **아래(&wndBottom)로**재배치되거나 맨 위가 아닌 창 이후에 위치가 바오면 더 이상 맨 위가 아닙니다. 맨 위 창이 맨 위에 없는 경우 모든 소유자와 소유한 창도 맨 위가 아닌 창으로 만들어집니다.

SWP_NOACTIVATE SWP_NOZORDER 지정되지 않은 경우(즉, 응용 프로그램에서 창을 동시에 활성화하고 지정된 Z 순서로 배치할 것을 요청할 때) *pWndInsertAfter에* 지정된 값은 다음과 같은 경우에만 사용됩니다.

- **&wndTopMost도** **&wndNoTopMost는** *pWndInsertAfter* 매개 변수에 지정되지 않습니다.

- 이 창은 활성 창이 아닙니다.

응용 프로그램은 비활성 창을 Z 순서의 맨 위로 가져오지 않고는 활성화할 수 없습니다. 응용 프로그램은 제한 없이 활성화된 창의 Z 순서를 변경할 수 있습니다.

맨 위가 아닌 창은 맨 위 창을 소유할 수 있지만 그 반대의 경우도 마찬가지입니다. 맨 위 창이 소유한 모든 창(예: 대화 상자)은 소유한 모든 창이 소유자 위에 유지되도록 최상위 창으로 만들어집니다.

Windows 버전 3.1 이상에서는 창을 Z 순서의 맨 위로 이동하고 WS_EX_TOPMOST 스타일을 설정하여 잠글 수 있습니다. 이러한 최상위 창은 비활성화된 경우에도 최상위 위치를 유지합니다. 예를 들어 WinHelp Always On Top 명령을 선택하면 도움말 창이 맨 위에 있고 응용 프로그램으로 돌아갈 때 계속 표시됩니다.

맨 위 창을 `SetWindowPos` 만들려면 *pWndInsertAfter* 매개 변수를&**wndTopMost와**같거나 창을 만들 때 WS_EX_TOPMOST 스타일을 설정합니다.

Z 순서에 WS_EX_TOPMOST 스타일이 있는 창이 포함되어 있는 경우 **&wndTopMost** 값으로 이동한 창은 맨 위가 아닌 모든 창의 맨 위에 있지만 맨 위 창 아래에 배치됩니다. 응용 프로그램이 WS_EX_TOPMOST 비트 없이 비활성 창을 활성화하면 창이 맨 위가 아닌 모든 창 위에 있지만 맨 위 창 아래로 이동합니다.

`SetWindowPos` *pWndInsertAfter* 매개 변수가 **wndBottom에** `CWnd`&맨 위 창인 경우 창이 최상위 상태(WS_EX_TOPMOST 지워집니다)를 잃고 시스템은 창을 Z 순서의 맨 아래에 배치합니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#120](../../mfc/reference/codesnippet/cpp/cwnd-class_61.cpp)]

## <a name="cwndsetwindowrgn"></a><a name="setwindowrgn"></a>CWnd::세트윈도우Rgn

이 멤버 함수를 호출하여 창 영역을 설정합니다.

```
int SetWindowRgn(
    HRGN hRgn,
    BOOL bRedraw);
```

### <a name="parameters"></a>매개 변수

*hRgn*<br/>
지역에 대한 핸들입니다.

*bRedraw*<br/>
TRUE이면 운영 체제는 영역을 설정한 후 창을 다시 그립니다. 그렇지 않으면 그렇지 않습니다. 일반적으로 창이 표시되는 경우 *bRedraw를* TRUE로 설정합니다. TRUE로 설정하면 시스템은 WM_WINDOWPOSCHANGING 보내고 WM_WINDOWPOSCHANGED 메시지를 창으로 보냅니다.

### <a name="return-value"></a>Return Value

함수가 성공하면 반환 값은 0이 아닙니다. 함수가 실패하면 반환 값은 0입니다.

### <a name="remarks"></a>설명

창 창 영역의 좌표는 창의 클라이언트 영역이 아니라 창의 왼쪽 위 모서리를 기준으로 합니다.

에 대한 `SetWindowRgn`호출이 성공하면 운영 체제는 *hRgn을*처리하는 지역에서 지정된 지역을 소유합니다. 운영 체제는 리전의 복사본을 만들지 않으므로 이 지역 핸들을 사용하면 더 이상 함수를 호출하지 않고 이 지역 핸들을 닫지 않습니다.

## <a name="cwndsetwindowtext"></a><a name="setwindowtext"></a>CWnd::SetWindow텍스트

창의 제목을 지정된 텍스트로 설정합니다.

```cpp
void SetWindowText(LPCTSTR lpszString);
```

### <a name="parameters"></a>매개 변수

*lpszString*<br/>
새 제목 또는 컨트롤 텍스트로 사용할 [CString](../../atl-mfc-shared/reference/cstringt-class.md) 개체 또는 null 종료 된 문자열을 가리킵니다.

### <a name="remarks"></a>설명

창이 컨트롤인 경우 컨트롤 내의 텍스트가 설정됩니다.

이 함수를 사용하면 [WM_SETTEXT](/windows/win32/winmsg/wm-settext) 메시지가 이 창으로 전송됩니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#121](../../mfc/reference/codesnippet/cpp/cwnd-class_62.cpp)]

## <a name="cwndshowcaret"></a><a name="showcaret"></a>CWnd::쇼케어

화면에 캐리트의 현재 위치를 표시합니다.

```cpp
void ShowCaret();
```

### <a name="remarks"></a>설명

표시되면 캐럿이 자동으로 깜박이기 시작합니다.

멤버 `ShowCaret` 함수는 현재 셰이프가 있고 두 번 이상 연속적으로 숨김이 없는 경우에만 카릿을 표시합니다. 이 창에서 카를트소유하지 않으면 카포트가 표시되지 않습니다.

카를 을 숨기는 것은 누적됩니다. [HideCaret](#hidecaret) 멤버 함수가 연속적으로 5번 `ShowCaret` 호출된 경우, 5번 호출해야 캐리트 표시합니다.

카포트는 공유 리소스입니다. 창에는 입력 포커스가 있거나 활성 상태인 경우에만 카릿이 표시되어야 합니다.

### <a name="example"></a>예제

  [CWnd::CreateCaret에](#createcaret)대한 예제를 참조하십시오.

## <a name="cwndshowownedpopups"></a><a name="showownedpopups"></a>CWnd::쇼소유팝

이 창이 소유한 모든 팝업 창을 표시하거나 숨깁니다.

```cpp
void ShowOwnedPopups(BOOL bShow = TRUE);
```

### <a name="parameters"></a>매개 변수

*bShow*<br/>
팝업 창을 표시할지 숨길지 여부를 지정합니다. 이 매개 변수가 TRUE이면 숨겨진 모든 팝업 창이 표시됩니다. 이 매개 변수가 FALSE이면 표시되는 모든 팝업 창이 숨겨지도록 됩니다.

### <a name="example"></a>예제

  [CWnd::SetWindowPos에](#setwindowpos)대한 예제를 참조하십시오.

## <a name="cwndshowscrollbar"></a><a name="showscrollbar"></a>CWnd::쇼스크롤바

스크롤 막대를 표시하거나 숨깁니다.

```cpp
void ShowScrollBar(
    UINT nBar,
    BOOL bShow = TRUE);
```

### <a name="parameters"></a>매개 변수

*nBar*<br/>
스크롤 막대가 컨트롤인지 창의 비클라이언트 영역의 일부인지 지정합니다. 클라이언트가 아닌 영역의 일부인 경우 *nBar는* 스크롤 막대가 가로, 세로 또는 둘 다에 배치되어 있는지 여부도 나타냅니다. 다음 중 하나여야 합니다.

- SB_BOTH 창의 가로 및 세로 스크롤 막대를 지정합니다.

- SB_HORZ 창이 가로 스크롤 막대임을 지정합니다.

- SB_VERT 창이 세로 스크롤 막대임을 지정합니다.

*bShow*<br/>
Windows에서 스크롤 막대를 표시하거나 숨길지 여부를 지정합니다. 이 매개 변수가 TRUE이면 스크롤 막대가 표시됩니다. 그렇지 않으면 스크롤 막대가 숨김이 있습니다.

### <a name="remarks"></a>설명

스크롤 막대 알림 `ShowScrollBar` 메시지를 처리하는 동안 응용 프로그램이 스크롤 막대를 숨기기 위해 호출해서는 안 됩니다.

## <a name="cwndshowwindow"></a><a name="showwindow"></a>CWnd::쇼윈도우

창의 가시성 상태를 설정합니다.

```
BOOL ShowWindow(int nCmdShow);
```

### <a name="parameters"></a>매개 변수

*nCmdShow*<br/>
표시 방법을 `CWnd` 지정합니다. 다음 값 중 하나여야 합니다.

- SW_HIDE 이 창을 숨기고 활성화를 다른 창으로 전달합니다.

- SW_MINIMIZE 창을 최소화하고 시스템 목록의 최상위 창을 활성화합니다.

- SW_RESTORE 창을 활성화하고 표시합니다. 창이 최소화되거나 최대화되면 Windows는 창을 원래 크기와 위치로 복원합니다.

- SW_SHOW 창을 활성화하고 현재 크기와 위치에 표시합니다.

- SW_SHOWMAXIMIZED 창을 활성화하고 최대화 된 창으로 표시합니다.

- SW_SHOWMINIMIZED 창을 활성화하고 아이콘으로 표시합니다.

- SW_SHOWMINNOACTIVE 창을 아이콘으로 표시합니다. 현재 활성 상태인 창은 활성 상태로 유지됩니다.

- SW_SHOWNA 창을 현재 상태로 표시합니다. 현재 활성 상태인 창은 활성 상태로 유지됩니다.

- SW_SHOWNOACTIVATE 창을 가장 최근 크기와 위치로 표시합니다. 현재 활성 상태인 창은 활성 상태로 유지됩니다.

- SW_SHOWNORMAL 활성화하고 창을 표시합니다. 창이 최소화되거나 최대화되면 Windows는 창을 원래 크기와 위치로 복원합니다.

### <a name="return-value"></a>Return Value

창이 이전에 표시되면 0이 아닙니다. 0이 `CWnd` 이전에 숨겨져 있는 경우입니다.

### <a name="remarks"></a>설명

`ShowWindow`[CWinApp::m_nCmdShow](../../mfc/reference/cwinapp-class.md#m_ncmdshow)함께 기본 창에 대 한 응용 프로그램 당 한 번만 호출 해야 합니다. 에 대한 `ShowWindow` 후속 호출은 에 의해 `CWinApp::m_nCmdShow`지정된 값 대신 위에 나열된 값 중 하나를 사용해야 합니다.

### <a name="example"></a>예제

  [CWnd::CalcWindowRect에](#calcwindowrect)대한 예제를 참조하십시오.

## <a name="cwndsubclassdlgitem"></a><a name="subclassdlgitem"></a>CWnd::하위 클래스Dlg항목

이 멤버 함수를 호출하여 대화 상자 템플릿에서 만든 컨트롤을 "동적으로 하위 클래스"로 호출하고 이 `CWnd` 개체에 연결합니다.

```
BOOL SubclassDlgItem(
    UINT nID,
    CWnd* pParent);
```

### <a name="parameters"></a>매개 변수

*nID*<br/>
컨트롤의 ID입니다.

*pParent*<br/>
컨트롤의 부모(일반적으로 대화 상자)입니다.

### <a name="return-value"></a>Return Value

함수가 성공하면 0이 아니고 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

컨트롤이 동적으로 하위 클래스가 되면 Windows 메시지는 `CWnd`'메시지 맵'을 통해 라우팅되고 클래스의 첫 번째 클래스에서 `CWnd`메시지 처리기를 호출합니다. 기본 클래스에 전달되는 메시지는 컨트롤의 기본 메시지 처리기로 전달됩니다.

이 멤버 함수는 Windows 컨트롤을 `CWnd` 개체에 연결하고 컨트롤 `WndProc` `AfxWndProc` 및 함수를 대체합니다. 함수는 `GetSuperWndProcAddr` 멤버 `WndProc` 함수에서 반환된 위치에 이전 을 저장합니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#122](../../mfc/reference/codesnippet/cpp/cwnd-class_63.cpp)]

## <a name="cwndsubclasswindow"></a><a name="subclasswindow"></a>CWnd::하위 클래스창

이 멤버 함수를 호출하여 창을 "동적으로 하위 `CWnd` 클래스"로 호출하고 이 개체에 연결합니다.

```
BOOL SubclassWindow(HWND hWnd);
```

### <a name="parameters"></a>매개 변수

*Hwnd*<br/>
창에 대한 핸들입니다.

### <a name="return-value"></a>Return Value

함수가 성공하면 0이 아니고 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

창이 동적으로 하위 클래스가 되면 창 메시지는 `CWnd`'메시지 맵'을 통해 라우팅되고 클래스의 첫 번째 클래스에서 `CWnd`메시지 처리기를 호출합니다. 기본 클래스에 전달 되는 메시지는 창에서 기본 메시지 처리기로 전달 됩니다.

이 멤버 함수는 Windows 컨트롤을 `CWnd` 개체에 연결하고 `WndProc` 창과 `AfxWndProc` 기능을 대체합니다. 함수는 개체의 이전 `WndProc` 포인터를 `CWnd` 저장합니다.

> [!NOTE]
> 이 함수가 호출될 때 창이 MFC 개체에 아직 연결되지 않아야 합니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#123](../../mfc/reference/codesnippet/cpp/cwnd-class_64.cpp)]

## <a name="cwndunlockwindowupdate"></a><a name="unlockwindowupdate"></a>CWnd::잠금 해제창업데이트

이 멤버 함수를 호출하여 잠근 `CWnd::LockWindowUpdate`창의 잠금을 해제합니다.

```cpp
void UnlockWindowUpdate();
```

### <a name="remarks"></a>설명

을 사용하여 `LockWindowUpdate`한 번에 하나의 창만 잠글 수 있습니다. 잠금 창에 대한 자세한 내용은 [CWnd::LockWindowUpdate](#lockwindowupdate) 또는 Win32 기능 [LockWindowUpdate를](/windows/win32/api/winuser/nf-winuser-lockwindowupdate) 참조하십시오.

## <a name="cwndunsubclasswindow"></a><a name="unsubclasswindow"></a>CWnd::하위 클래스창

이 멤버 함수를 `WndProc` 호출하여 원래 값으로 다시 설정하고 개체에서 HWND로 식별된 창을 분리합니다. `CWnd`

```
HWND UnsubclassWindow();
```

### <a name="return-value"></a>Return Value

하위 클래스가 없는 창에 대한 핸들입니다.

### <a name="example"></a>예제

  [CWnd::SubclassWindow에](#subclasswindow)대 한 예제를 참조 합니다.

## <a name="cwndupdatedata"></a><a name="updatedata"></a>CWnd::업데이트데이터

이 멤버 함수를 호출하여 대화 상자에서 데이터를 초기화하거나 대화 상자 데이터를 검색하고 유효성을 검사합니다.

```
BOOL UpdateData(BOOL bSaveAndValidate = TRUE);
```

### <a name="parameters"></a>매개 변수

*bSaveAndValidate*<br/>
대화 상자가 초기화되고 있는지(FALSE) 또는 데이터를 검색하고 있는지 여부를 나타내는 플래그(TRUE)입니다.

### <a name="return-value"></a>Return Value

작업이 성공하면 0이 아닙니다. 그렇지 않으면 0. *bSaveAndValidat*e가 TRUE이면 0이 아닌 반환 값은 데이터가 성공적으로 검증된다는 것을 의미합니다.

### <a name="remarks"></a>설명

`UpdateData` [CDialog::OnInitDialog의](../../mfc/reference/cdialog-class.md#oninitdialog)기본 구현에서 모달 대화 상자를 만들 때 프레임 워크는 자동으로 *bSaveAndValidate* 설정 FALSE로 호출 됩니다. 대화 상자가 표시되기 전에 호출이 발생합니다. [CDialog::OnOK의](../../mfc/reference/cdialog-class.md#onok) 기본 구현은 데이터를 검색하기 위해 true로 설정된 *bSaveAndValidate를* 사용하여 이 멤버 함수를 호출하고 성공하면 대화 상자를 닫습니다. 대화 상자에서 취소 단추를 클릭하면 데이터를 검색하지 않고 대화 상자가 닫힙니다.

## <a name="cwndupdatedialogcontrols"></a><a name="updatedialogcontrols"></a>CWnd::업데이트디아로그컨트롤

이 멤버 함수를 호출하여 [ON_UPDATE_COMMAND_UI](message-map-macros-mfc.md#on_update_command_ui) 콜백 메커니즘을 사용하는 대화 상자 또는 창에서 대화 상자 또는 기타 컨트롤의 상태를 업데이트합니다.

```cpp
void UpdateDialogControls(
    CCmdTarget* pTarget,
    BOOL bDisableIfNoHndler);
```

### <a name="parameters"></a>매개 변수

*p Target*<br/>
응용 프로그램의 기본 프레임 창을 가리키며 업데이트 메시지를 라우팅하는 데 사용됩니다.

*bDisableIfNohndler*<br/>
업데이트 처리기가 없는 컨트롤을 비활성화된 것으로 자동으로 표시해야 하는지 여부를 나타내는 플래그입니다.

### <a name="remarks"></a>설명

자식 컨트롤에 처리기가 없고 *bDisableIfNoHndler가* TRUE인 경우 자식 컨트롤이 비활성화됩니다.

프레임워크는 응용 프로그램의 유휴 처리의 일부로 대화 모음 또는 도구 모음의 컨트롤에 대해 이 멤버 함수를 호출합니다.

## <a name="cwndupdatelayeredwindow"></a><a name="updatelayeredwindow"></a>CWnd::업데이트레이어드 윈도우

계층적 창의 위치, 크기, 모양, 내용 및 투명도를 업데이트합니다.

```
BOOL UpdateLayeredWindow(
    CDC* pDCDst,
    POINT* pptDst,
    SIZE* psize,
    CDC* pDCSrc,
    POINT* pptSrc,
    COLORREF crKey,
    BLENDFUNCTION* pblend,
    DWORD dwFlags);
```

### <a name="parameters"></a>매개 변수

*pDCDst*<br/>
화면의 장치 컨텍스트에 대한 포인터입니다. 창 내용이 업데이트될 때 팔레트 색상 일치에 사용됩니다. *pDCDst가* NULL이면 기본 팔레트가 사용됩니다.

*pDCSrc가* NULL이면 *pDCDst가* NULL이어야 합니다.

*pptDst*<br/>
계층화된 창의 새 화면 위치를 지정하는 `POINT` 구조에 대한 포인터입니다. 현재 위치가 변경되지 않으면 *pptDst가* NULL일 수 있습니다.

*크기 조정*<br/>
계층화된 `SIZE` 창의 새 크기를 지정하는 구조에 대한 포인터입니다. 창의 크기가 변경되지 않으면 *psize가* NULL일 수 있습니다.

*pDCSrc가* NULL이면 *psize는* NULL이어야 합니다.

*pDCSrc*<br/>
계층화된 창을 정의하는 서피스에 대한 DC에 대한 포인터입니다. 창의 모양과 시각적 컨텍스트가 변경되지 않으면 *pDCSrc가* NULL일 수 있습니다.

*pptSrc*<br/>
장치 컨텍스트에서 레이어의 위치를 지정하는 `POINT` 구조에 대한 포인터입니다.

*pDCSrc가* NULL이면 *pptSrc는* NULL이어야 합니다.

*crKey*<br/>
계층화된 창을 작성할 때 사용할 투명도 색상 키를 지정하는 COLORREF 값에 대한 포인터입니다. 이 색상의 창으로 그려진 모든 픽셀은 투명합니다. COLORREF를 생성하려면 RGB 매크로를 사용합니다.

*블렌드 (것)과 블렌드*<br/>
계층화된 창을 작성할 때 사용할 투명도 값을 지정하는 [BLENDFUNCTION](/windows/win32/api/wingdi/ns-wingdi-blendfunction) 구조에 대한 포인터입니다.

*dwFlags*<br/>
취할 작업을 지정합니다. 이 매개 변수는 다음 값 중 하나 이상일 수 있습니다. 가능한 값 목록은 [UpdateLayeredWindow](/windows/win32/api/winuser/nf-winuser-updatelayeredwindow)를 참조하십시오.

### <a name="return-value"></a>Return Value

함수가 성공하면 0이 아니고 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

이 멤버 함수는 Windows SDK에 설명된 대로 [UpdateLayeredWindow](/windows/win32/api/winuser/nf-winuser-updatelayeredwindow)기능의 기능을 에뮬레이트합니다.

## <a name="cwndupdatewindow"></a><a name="updatewindow"></a>CWnd::업데이트 창

업데이트 영역이 비어 있지 않은 경우 [WM_PAINT](/windows/win32/gdi/wm-paint) 메시지를 보내 클라이언트 영역을 업데이트합니다.

```cpp
void UpdateWindow();
```

### <a name="remarks"></a>설명

멤버 `UpdateWindow` 함수는 응용 프로그램 큐를 우회하여 WM_PAINT 메시지를 직접 보냅니다. 업데이트 영역이 비어 있으면 WM_PAINT 전송되지 않습니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCWindowing#124](../../mfc/reference/codesnippet/cpp/cwnd-class_65.cpp)]

## <a name="cwndvalidaterect"></a><a name="validaterect"></a>CWnd::유효성 검사

창의 업데이트 영역에서 사각형을 제거하여 지정된 사각형 내의 클라이언트 영역의 유효성을 검사합니다.

```cpp
void ValidateRect(LPCRECT lpRect);
```

### <a name="parameters"></a>매개 변수

*Lprect*<br/>
업데이트 영역에서 제거할 사각형의 클라이언트 좌표를 포함하는 [CRect](../../atl-mfc-shared/reference/crect-class.md) 개체 또는 [RECT 구조를](/windows/win32/api/windef/ns-windef-rect) 가리킵니다. *lpRect가* NULL이면 전체 창의 유효성이 검사됩니다.

### <a name="remarks"></a>설명

[BeginPaint](#beginpaint) 멤버 함수는 전체 클라이언트 영역의 유효성을 자동으로 검사합니다. 다음에 `ValidateRect` WM_PAINT 생성되기 전에 업데이트 영역의 일부를 유효성을 검사해야 하는 경우 [WM_PAINT](/windows/win32/gdi/wm-paint) [ValidateRgn](#validatergn) 멤버 함수도 호출할 수 없습니다.

Windows는 현재 업데이트 영역의 유효성이 검사될 때까지 WM_PAINT 메시지를 계속 생성합니다.

## <a name="cwndvalidatergn"></a><a name="validatergn"></a>CWnd::유효성 검사

창의 현재 업데이트 영역에서 영역을 제거하여 지정된 영역 내의 클라이언트 영역의 유효성을 검사합니다.

```cpp
void ValidateRgn(CRgn* pRgn);
```

### <a name="parameters"></a>매개 변수

*pRgn*<br/>
업데이트 영역에서 제거할 영역을 정의하는 영역을 식별하는 [CRgn](../../mfc/reference/crgn-class.md) 개체에 대한 포인터입니다. 이 매개 변수가 NULL이면 전체 클라이언트 영역이 제거됩니다.

### <a name="remarks"></a>설명

지정된 영역은 지역 함수에 의해 이전에 만들어졌어야 합니다. 영역 좌표는 클라이언트 좌표로 가정합니다.

[BeginPaint](#beginpaint) 멤버 함수는 전체 클라이언트 영역의 유효성을 자동으로 검사합니다. 다음 [WM_PAINT](/windows/win32/gdi/wm-paint) 메시지가 생성되기 전에 업데이트 영역의 일부를 유효성을 검사해야 하는 경우 [ValidateRect](#validaterect) 또는 `ValidateRgn` 멤버 함수를 호출할 수 없습니다.

## <a name="cwndwindowfrompoint"></a><a name="windowfrompoint"></a>CWnd::창에서부터

지정된 점을 포함하는 창을 검색합니다. *점은* 화면에서 점의 화면 좌표를 지정해야 합니다.

```
static CWnd* PASCAL WindowFromPoint(POINT point);
```

### <a name="parameters"></a>매개 변수

*지점*<br/>
검사할 점을 정의하는 [CPoint](../../atl-mfc-shared/reference/cpoint-class.md) 개체 또는 [POINT](/windows/win32/api/windef/ns-windef-point) 데이터 구조를 지정합니다.

### <a name="return-value"></a>Return Value

점이 있는 창 개체에 대한 포인터입니다. 지정된 지점에 창이 없으면 NULL입니다. 반환된 포인터는 일시적일 수 있으며 나중에 사용할 수 없습니다.

### <a name="remarks"></a>설명

`WindowFromPoint`포인트가 창 내에 있더라도 숨겨진 창이나 비활성화된 창을 검색하지 않습니다. 응용 프로그램은 제한되지 않는 검색을 위해 [ChildWindowFromPoint](#childwindowfrompoint) 멤버 함수를 사용해야 합니다.

## <a name="cwndwindowproc"></a><a name="windowproc"></a>CWnd::창프로크

개체에 대한 `WindowProc`Windows 프로시저() 를 `CWnd` 제공합니다.

```
virtual LRESULT WindowProc(
    UINT message,
    WPARAM wParam,
    LPARAM lParam);
```

### <a name="parameters"></a>매개 변수

*message*<br/>
처리할 Windows 메시지를 지정합니다.

*wParam*<br/>
메시지 처리에 사용되는 추가 정보를 제공합니다. 매개 변수 값은 메시지에 따라 다릅니다.

*lParam*<br/>
메시지 처리에 사용되는 추가 정보를 제공합니다. 매개 변수 값은 메시지에 따라 다릅니다.

### <a name="return-value"></a>Return Value

반환 값은 메시지에 따라 다릅니다.

### <a name="remarks"></a>설명

창의 메시지 맵을 통해 메시지를 보신다.

## <a name="cwndwinhelp"></a><a name="winhelp"></a>CWnd::윈헬프

WinHelp 애플리케이션을 시작하기 위해 호출됩니다.

```
virtual void WinHelp(
    DWORD_PTR dwData,
    UINT nCmd = HELP_CONTEXT);
```

### <a name="parameters"></a>매개 변수

*dwData*<br/>
추가 데이터를 지정합니다. 사용되는 값은 *nCmd* 매개 변수의 값에 따라 다릅니다.

*nCmd*<br/>
요청한 도움말의 형식을 지정합니다. 가능한 값 목록과 *dwData* 매개 변수에 미치는 영향은 Windows SDK의 [WinHelp](/windows/win32/api/winuser/nf-winuser-winhelpw) Windows 함수를 참조하십시오.

### <a name="remarks"></a>설명

자세한 내용은 [CWinApp::WinHelp를](../../mfc/reference/cwinapp-class.md#winhelp) 참조하십시오.

## <a name="cwndregistertouchwindow"></a><a name="registertouchwindow"></a>CWnd::레지스터터치윈도우

Windows 터치 지원을 등록하거나 등록 취소합니다.

```
BOOL RegisterTouchWindow(
    BOOL bRegister = TRUE,
    ULONG ulFlags = 0);
```

### <a name="parameters"></a>매개 변수

*bRegister*<br/>
TRUE는 Windows 터치 지원을 레지스터로 표시합니다. 그렇지 않으면 거짓.

*ulFlags*<br/>
선택적 수정 사항을 지정하는 비트 플래그 집합입니다. 이 필드에는 TWF_FINETOUCH, TWF_WANTPALM 0 또는 다음 값 중 하나가 포함될 수 있습니다.

### <a name="return-value"></a>Return Value

성공하면 TRUE이고, 실패하면 FALSE입니다.

### <a name="remarks"></a>설명

## <a name="cwndresizedynamiclayout"></a><a name="resizedynamiclayout"></a>CWnd::크기 조정동적 레이아웃

창에 대해 동적 레이아웃을 사용하도록 설정한 경우 창 크기가 변경되면 자식 창의 레이아웃을 조정하기 위해 프레임워크에서 호출됩니다.

```
virtual void ResizeDynamicLayout();
```

### <a name="remarks"></a>설명

## <a name="see-also"></a>참조

[CCmdTarget 클래스](../../mfc/reference/ccmdtarget-class.md)<br/>
[계층 구조 차트](../../mfc/hierarchy-chart.md)<br/>
[CFrameWnd 클래스](../../mfc/reference/cframewnd-class.md)<br/>
[CView 클래스](../../mfc/reference/cview-class.md)
