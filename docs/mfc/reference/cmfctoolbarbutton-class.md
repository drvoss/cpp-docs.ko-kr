---
title: CMFCToolBarButton 클래스
ms.date: 11/04/2016
f1_keywords:
- CMFCToolBarButton
- AFXTOOLBARBUTTON/CMFCToolBarButton
- AFXTOOLBARBUTTON/CMFCToolBarButton::CMFCToolBarButton
- AFXTOOLBARBUTTON/CMFCToolBarButton::CanBeDropped
- AFXTOOLBARBUTTON/CMFCToolBarButton::CanBeStored
- AFXTOOLBARBUTTON/CMFCToolBarButton::CanBeStretched
- AFXTOOLBARBUTTON/CMFCToolBarButton::CompareWith
- AFXTOOLBARBUTTON/CMFCToolBarButton::CopyFrom
- AFXTOOLBARBUTTON/CMFCToolBarButton::CreateFromOleData
- AFXTOOLBARBUTTON/CMFCToolBarButton::EnableWindow
- AFXTOOLBARBUTTON/CMFCToolBarButton::ExportToMenuButton
- AFXTOOLBARBUTTON/CMFCToolBarButton::GetClipboardFormat
- AFXTOOLBARBUTTON/CMFCToolBarButton::GetHwnd
- AFXTOOLBARBUTTON/CMFCToolBarButton::GetImage
- AFXTOOLBARBUTTON/CMFCToolBarButton::GetInvalidateRect
- AFXTOOLBARBUTTON/CMFCToolBarButton::GetParentWnd
- AFXTOOLBARBUTTON/CMFCToolBarButton::GetProtectedCommands
- AFXTOOLBARBUTTON/CMFCToolBarButton::GetTextSize
- AFXTOOLBARBUTTON/CMFCToolBarButton::HasFocus
- AFXTOOLBARBUTTON/CMFCToolBarButton::HaveHotBorder
- AFXTOOLBARBUTTON/CMFCToolBarButton::IsDrawImage
- AFXTOOLBARBUTTON/CMFCToolBarButton::IsDrawText
- AFXTOOLBARBUTTON/CMFCToolBarButton::IsDroppedDown
- AFXTOOLBARBUTTON/CMFCToolBarButton::IsEditable
- AFXTOOLBARBUTTON/CMFCToolBarButton::IsExtraSize
- AFXTOOLBARBUTTON/CMFCToolBarButton::IsFirstInGroup
- AFXTOOLBARBUTTON/CMFCToolBarButton::IsHidden
- AFXTOOLBARBUTTON/CMFCToolBarButton::IsHorizontal
- AFXTOOLBARBUTTON/CMFCToolBarButton::IsLastInGroup
- AFXTOOLBARBUTTON/CMFCToolBarButton::IsLocked
- AFXTOOLBARBUTTON/CMFCToolBarButton::IsOwnerOf
- AFXTOOLBARBUTTON/CMFCToolBarButton::IsVisible
- AFXTOOLBARBUTTON/CMFCToolBarButton::IsWindowVisible
- AFXTOOLBARBUTTON/CMFCToolBarButton::NotifyCommand
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnAddToCustomizePage
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnBeforeDrag
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnBeforeDrop
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnCalculateSize
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnCancelMode
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnChangeParentWnd
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnClick
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnClickUp
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnContextHelp
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnCtlColor
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnCustomizeMenu
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnDblClk
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnDraw
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnDrawOnCustomizeList
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnGetCustomToolTipText
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnGlobalFontsChanged
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnMove
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnShow
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnSize
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnToolHitTest
- AFXTOOLBARBUTTON/CMFCToolBarButton::OnUpdateToolTip
- AFXTOOLBARBUTTON/CMFCToolBarButton::PrepareDrag
- AFXTOOLBARBUTTON/CMFCToolBarButton::Rect
- AFXTOOLBARBUTTON/CMFCToolBarButton::ResetImageToDefault
- AFXTOOLBARBUTTON/CMFCToolBarButton::SaveBarState
- AFXTOOLBARBUTTON/CMFCToolBarButton::Serialize
- AFXTOOLBARBUTTON/CMFCToolBarButton::SetACCData
- AFXTOOLBARBUTTON/CMFCToolBarButton::SetClipboardFormatName
- AFXTOOLBARBUTTON/CMFCToolBarButton::SetImage
- AFXTOOLBARBUTTON/CMFCToolBarButton::SetProtectedCommands
- AFXTOOLBARBUTTON/CMFCToolBarButton::SetRadio
- AFXTOOLBARBUTTON/CMFCToolBarButton::SetRect
- AFXTOOLBARBUTTON/CMFCToolBarButton::SetStyle
- AFXTOOLBARBUTTON/CMFCToolBarButton::SetVisible
- AFXTOOLBARBUTTON/CMFCToolBarButton::Show
- AFXTOOLBARBUTTON/CMFCToolBarButton::m_bImage
- AFXTOOLBARBUTTON/CMFCToolBarButton::m_bText
- AFXTOOLBARBUTTON/CMFCToolBarButton::m_bTextBelow
- AFXTOOLBARBUTTON/CMFCToolBarButton::m_bUserButton
- AFXTOOLBARBUTTON/CMFCToolBarButton::m_bWholeText
- AFXTOOLBARBUTTON/CMFCToolBarButton::m_bWrap
- AFXTOOLBARBUTTON/CMFCToolBarButton::m_bWrapText
- AFXTOOLBARBUTTON/CMFCToolBarButton::m_nID
- AFXTOOLBARBUTTON/CMFCToolBarButton::m_nStyle
- AFXTOOLBARBUTTON/CMFCToolBarButton::m_strText
helpviewer_keywords:
- CMFCToolBarButton [MFC], CMFCToolBarButton
- CMFCToolBarButton [MFC], CanBeDropped
- CMFCToolBarButton [MFC], CanBeStored
- CMFCToolBarButton [MFC], CanBeStretched
- CMFCToolBarButton [MFC], CompareWith
- CMFCToolBarButton [MFC], CopyFrom
- CMFCToolBarButton [MFC], CreateFromOleData
- CMFCToolBarButton [MFC], EnableWindow
- CMFCToolBarButton [MFC], ExportToMenuButton
- CMFCToolBarButton [MFC], GetClipboardFormat
- CMFCToolBarButton [MFC], GetHwnd
- CMFCToolBarButton [MFC], GetImage
- CMFCToolBarButton [MFC], GetInvalidateRect
- CMFCToolBarButton [MFC], GetParentWnd
- CMFCToolBarButton [MFC], GetProtectedCommands
- CMFCToolBarButton [MFC], GetTextSize
- CMFCToolBarButton [MFC], HasFocus
- CMFCToolBarButton [MFC], HaveHotBorder
- CMFCToolBarButton [MFC], IsDrawImage
- CMFCToolBarButton [MFC], IsDrawText
- CMFCToolBarButton [MFC], IsDroppedDown
- CMFCToolBarButton [MFC], IsEditable
- CMFCToolBarButton [MFC], IsExtraSize
- CMFCToolBarButton [MFC], IsFirstInGroup
- CMFCToolBarButton [MFC], IsHidden
- CMFCToolBarButton [MFC], IsHorizontal
- CMFCToolBarButton [MFC], IsLastInGroup
- CMFCToolBarButton [MFC], IsLocked
- CMFCToolBarButton [MFC], IsOwnerOf
- CMFCToolBarButton [MFC], IsVisible
- CMFCToolBarButton [MFC], IsWindowVisible
- CMFCToolBarButton [MFC], NotifyCommand
- CMFCToolBarButton [MFC], OnAddToCustomizePage
- CMFCToolBarButton [MFC], OnBeforeDrag
- CMFCToolBarButton [MFC], OnBeforeDrop
- CMFCToolBarButton [MFC], OnCalculateSize
- CMFCToolBarButton [MFC], OnCancelMode
- CMFCToolBarButton [MFC], OnChangeParentWnd
- CMFCToolBarButton [MFC], OnClick
- CMFCToolBarButton [MFC], OnClickUp
- CMFCToolBarButton [MFC], OnContextHelp
- CMFCToolBarButton [MFC], OnCtlColor
- CMFCToolBarButton [MFC], OnCustomizeMenu
- CMFCToolBarButton [MFC], OnDblClk
- CMFCToolBarButton [MFC], OnDraw
- CMFCToolBarButton [MFC], OnDrawOnCustomizeList
- CMFCToolBarButton [MFC], OnGetCustomToolTipText
- CMFCToolBarButton [MFC], OnGlobalFontsChanged
- CMFCToolBarButton [MFC], OnMove
- CMFCToolBarButton [MFC], OnShow
- CMFCToolBarButton [MFC], OnSize
- CMFCToolBarButton [MFC], OnToolHitTest
- CMFCToolBarButton [MFC], OnUpdateToolTip
- CMFCToolBarButton [MFC], PrepareDrag
- CMFCToolBarButton [MFC], Rect
- CMFCToolBarButton [MFC], ResetImageToDefault
- CMFCToolBarButton [MFC], SaveBarState
- CMFCToolBarButton [MFC], Serialize
- CMFCToolBarButton [MFC], SetACCData
- CMFCToolBarButton [MFC], SetClipboardFormatName
- CMFCToolBarButton [MFC], SetImage
- CMFCToolBarButton [MFC], SetProtectedCommands
- CMFCToolBarButton [MFC], SetRadio
- CMFCToolBarButton [MFC], SetRect
- CMFCToolBarButton [MFC], SetStyle
- CMFCToolBarButton [MFC], SetVisible
- CMFCToolBarButton [MFC], Show
- CMFCToolBarButton [MFC], m_bImage
- CMFCToolBarButton [MFC], m_bText
- CMFCToolBarButton [MFC], m_bTextBelow
- CMFCToolBarButton [MFC], m_bUserButton
- CMFCToolBarButton [MFC], m_bWholeText
- CMFCToolBarButton [MFC], m_bWrap
- CMFCToolBarButton [MFC], m_bWrapText
- CMFCToolBarButton [MFC], m_nID
- CMFCToolBarButton [MFC], m_nStyle
- CMFCToolBarButton [MFC], m_strText
ms.assetid: 8a6ecffb-86b0-4f5c-8211-a9146b463efd
ms.openlocfilehash: 8e96740ba6755f489be07fbf8996cfa8b049314a
ms.sourcegitcommit: 7a6116e48c3c11b97371b8ae4ecc23adce1f092d
ms.translationtype: MT
ms.contentlocale: ko-KR
ms.lasthandoff: 04/22/2020
ms.locfileid: "81754032"
---
# <a name="cmfctoolbarbutton-class"></a>CMFCToolBarButton 클래스

도구 모음에 단추 기능을 제공합니다.

## <a name="syntax"></a>구문

```
class CMFCToolBarButton : public CObject
```

## <a name="members"></a>멤버

### <a name="public-constructors"></a>Public 생성자

|속성|Description|
|----------|-----------------|
|[CMFCToolBar 버튼::CMFC툴바버튼](#cmfctoolbarbutton)|`CMFCToolBarButton` 개체를 생성하고 초기화합니다.|
|`CMFCToolBarButton::~CMFCToolBarButton`|소멸자|

### <a name="public-methods"></a>Public 메서드

|속성|Description|
|----------|-----------------|
|[CMFCToolBar 버튼::수 있습니다.](#canbedropped)|사용자가 사용자 지정 하는 동안 도구 모음 또는 메뉴에 단추를 배치할 수 있는지 여부를 지정 합니다.|
|[CMFCToolBar 단추::수 저장](#canbestored)|단추를 저장할 수 있는지 여부를 지정합니다.|
|[CMFCToolBar 버튼 :: 수 있습니다](#canbestretched)|사용자 지정 하는 동안 단추를 늘릴 수 있는지 여부를 지정 합니다.|
|[CMFCToolBar 단추::비교](#comparewith)|이 인스턴스를 제공된 `CMFCToolBarButton` 개체와 비교합니다.|
|[CMFCToolBar 단추::복사에서](#copyfrom)|다른 도구 모음 단추의 속성을 현재 단추에 복사합니다.|
|[CMFCToolBar 버튼::생성FromOle데이터](#createfromoledata)|제공된 `COleDataObject` `CMFCToolBarButton` 개체에서 개체를 만듭니다.|
|`CMFCToolBarButton::CreateObject`|프레임워크에서 이 클래스 형식의 동적 인스턴스를 만드는 데 사용합니다.|
|[CMFCToolBar 단추::인에이블 윈도우](#enablewindow)|마우스 및 키보드 입력을 사용하거나 사용하지 않도록 설정합니다.|
|[CMFCToolBar 단추::내보내기 메뉴 단추](#exporttomenubutton)|도구 모음 단추에서 메뉴로 텍스트를 복사합니다.|
|[CMFCToolBar 버튼::겟클립보드 형식](#getclipboardformat)|응용 프로그램의 전역 클립보드 형식을 검색합니다.|
|[CMFCToolBar 버튼::GetHwnd](#gethwnd)|도구 모음 단추와 연결된 창 핸들을 검색합니다.|
|[CMFCToolBar 단추::이미지 가져옵니다.](#getimage)|단추의 이미지 인덱스를 검색합니다.|
|[CMFCToolBar 단추::Get무효화렉트](#getinvalidaterect)|다시 그려야 하는 단추의 클라이언트 영역영역을 검색합니다.|
|[CMFCToolBar 버튼::GetParentWnd](#getparentwnd)|단추의 상위 창을 검색합니다.|
|[CMFCToolBar 버튼::보호 명령](#getprotectedcommands)|사용자가 사용자 지정할 수 없는 명령 목록을 검색합니다.|
|[CMFCToolBar 단추::GetText크기](#gettextsize)|단추 텍스트의 크기를 검색합니다.|
|[CMFCToolBar 버튼::하스포커스](#hasfocus)|단추에 현재 입력 포커스가 있는지 여부를 결정합니다.|
|[CMFCToolBar 버튼::하핫보더](#havehotborder)|사용자가 단추를 선택할 때 단추의 테두리가 표시되는지 여부를 결정합니다.|
|[CMFCToolBar 단추::이스드이미지](#isdrawimage)|단추에 이미지가 표시되는지 여부를 결정합니다.|
|[CMFCToolBar 단추::이스드로우텍스트](#isdrawtext)|단추에 텍스트 레이블이 표시되는지 여부를 결정합니다.|
|[CMFCToolBar 단추::삭제](#isdroppeddown)|단추에 하위 메뉴가 표시되는지 여부를 결정합니다.|
|[CMFCToolBar 단추::편집 가능](#iseditable)|단추를 사용자 지정할 수 있는지 여부를 결정합니다.|
|[CMFCToolBar 버튼::이엑스트라사이즈](#isextrasize)|확장된 테두리로 단추를 표시할 수 있는지 여부를 결정합니다.|
|[CMFCToolBar 버튼::IsFirstIn그룹](#isfirstingroup)|단추 그룹의 첫 번째 위치에 단추를 지정합니다.|
|[CMFCToolBar 단추::숨겨져 있습니다.](#ishidden)|단추숨김 여부를 결정합니다.|
|[CMFCToolBar 버튼::수평](#ishorizontal)|단추를 가로 도구 모음에 있는지 여부를 확인합니다.|
|[CMFC툴바버튼::이라스트인그룹](#islastingroup)|단추그룹의 마지막 위치에 있는지 여부를 지정합니다.|
|[CMFCToolBar 단추::잠금](#islocked)|단추가 잠긴(사용자 지정할 수 없는) 도구 모음에 있는지 여부를 확인합니다.|
|[CMFCToolBar 버튼::IsOwnerOf](#isownerof)|단추가 제공된 창 핸들의 소유자인지 여부를 결정합니다.|
|[CMFCToolBar 단추::볼 수 있습니다.](#isvisible)|도구 모음 단추를 볼 수 있는지 여부를 확인합니다.|
|[CMFCToolBar 단추::창가볼 수 있습니다.](#iswindowvisible)|단추의 기본 창 핸들이 표시되는지 여부를 확인합니다.|
|[CMFCToolBar 단추::알림 명령](#notifycommand)|단추에서 [WM_COMMAND](/windows/win32/menurc/wm-command) 메시지를 처리하는지 여부를 지정합니다.|
|[CMFCToolBar 단추::OnAddTo 사용자 지정 페이지](#onaddtocustomizepage)|단추를 **사용자 지정** 대화 상자에 추가 될 때 프레임 워크에 의해 호출 됩니다.|
|[CMFCToolBar 버튼::에 드래그](#onbeforedrag)|단추를 끌 수 있는지 여부를 지정합니다.|
|[CMFCToolBar 버튼::에 드롭](#onbeforedrop)|사용자가 단추를 대상 도구 모음에 놓을 수 있는지 여부를 지정합니다.|
|[CMFCToolBar 버튼::계산크기](#oncalculatesize)|지정된 장치 컨텍스트 및 도킹 상태에 대한 단추 크기를 계산하기 위해 프레임워크에서 호출합니다.|
|[CMFCToolBar 단추::온취소 모드](#oncancelmode)|[WM_CANCELMODE](/windows/win32/winmsg/wm-cancelmode) 메시지를 처리 하는 프레임 워크에 의해 호출 됩니다.|
|[CMFCToolBar 단추::온체인지부모](#onchangeparentwnd)|단추를 새 도구 모음에 삽입할 때 프레임워크에서 호출됩니다.|
|[CMFCToolBar 단추::클릭](#onclick)|사용자가 마우스 단추를 클릭할 때 프레임워크에서 호출됩니다.|
|[CMFCToolBar 버튼::클릭업](#onclickup)|사용자가 마우스 단추를 해제할 때 프레임워크에서 호출됩니다.|
|[CMFCToolBar 단추::온컨텍스트 도움말](#oncontexthelp)|상위 도구 모음에서 WM_HELPHITTEST 메시지를 처리할 때 프레임워크에서 호출합니다.|
|[CMFCToolBar 버튼 :: 온CtlColor](#onctlcolor)|상위 도구 모음에서 WM_CTLCOLOR 메시지를 처리할 때 프레임워크에서 호출합니다.|
|[CMFCToolBar 단추::온 커스터마이즈메뉴](#oncustomizemenu)|응용 프로그램이 상위 도구 모음에 바로 가기 메뉴를 표시할 때 단추에서 제공된 메뉴를 수정할 수 있습니다.|
|[CMFCToolBar 버튼::온드블클크](#ondblclk)|상위 도구 모음에서 [WM_LBUTTONDBLCLK](/windows/win32/inputdev/wm-lbuttondblclk) 메시지를 처리할 때 프레임워크에서 호출합니다.|
|[CMFCToolBar 버튼::온드로우](#ondraw)|지정된 스타일과 옵션을 사용하여 단추를 그리는 프레임워크에서 호출합니다.|
|[CMFCToolBar 단추::온드로우온 커스터즈리스트](#ondrawoncustomizelist)|**사용자 지정 대화** 상자의 명령 창에서 단추를 그리는 프레임워크에서 **호출합니다.**|
|[CMFCToolBar 단추::온겟사용자 정의 도구 팁 텍스트](#ongetcustomtooltiptext)|단추에 대 한 사용자 지정 도구 설명 텍스트를 검색 하는 프레임 워크에 의해 호출 됩니다.|
|[CMFCToolBar 단추::온글로벌폰트 변경](#onglobalfontschanged)|전역 글꼴이 변경된 경우 프레임워크에서 호출합니다.|
|[CMFCToolBar 버튼::이동 중](#onmove)|상위 도구 모음이 이동할 때 프레임워크에서 호출됩니다.|
|[CMFCTool바버튼::온쇼](#onshow)|단추를 표시 하거나 보이지 않는 될 때 프레임 워크에 의해 호출 됩니다.|
|[CMFCTool바버튼:온사이즈](#onsize)|상위 도구 모음의 크기 또는 위치를 변경하고 이 변경작업을 변경하려면 단추크기를 변경해야 하는 경우 프레임워크에서 호출됩니다.|
|[CMFCToolBar 버튼::온툴히트테스트](#ontoolhittest)|상위 도구 모음에서 점이 단추의 경계 사각형에 있는지 여부를 결정해야 하는 경우 프레임워크에서 호출합니다.|
|[CMFCToolBar 단추::업데이트 도구 팁](#onupdatetooltip)|상위 도구 모음이 도구 설명 텍스트를 업데이트할 때 프레임워크에서 호출됩니다.|
|[CMFCToolBar버튼::P레파레드래그](#preparedrag)|단추를 드래그 앤 드롭 작업을 수행 하려고 하는 경우 프레임 워크에 의해 호출 됩니다.|
|[CMFCToolBar 버튼::정사각형](#rect)|단추의 경계 사각형을 검색합니다.|
|[CMFCToolBar 단추::재설정이미지토디폴](#resetimagetodefault)|단추와 연결된 이미지를 기본값으로 설정합니다.|
|[CMFCToolBar 단추::저장 바스테이트](#savebarstate)|도구 모음 단추의 상태를 저장합니다.|
|[CMFCToolBar 단추::직렬화](#serialize)|이 개체를 아카이브에서 읽거나 아카이브에 씁니다. ( [CObject::Serialize](../../mfc/reference/cobject-class.md#serialize)를 재정의합니다.)|
|[CMFCToolBar 버튼::세액데이터](#setaccdata)|제공된 `CAccessibilityData` 개체에 도구 모음 단추의 내게 필요한 옵션 데이터를 채웁니다.|
|[CMFCToolBar 단추::세트 클립보드 형식 이름](#setclipboardformatname)|전역 클립보드 형식의 이름을 바꿉니다.|
|[CMFCToolBar 단추::세트 이미지](#setimage)|단추의 이미지 인덱스를 설정합니다.|
|[CMFCToolBar 단추::보호 명령 설정](#setprotectedcommands)|사용자가 사용자 지정할 수 없는 명령 목록을 설정합니다.|
|[CMFCTool바버튼::셋라디오](#setradio)|단추의 선택된 상태가 변경될 때 프레임워크에서 호출됩니다.|
|[CMFCToolBar 단추::세트렉트](#setrect)|단추의 경계 사각형을 설정합니다.|
|[CMFCToolBar 버튼::세트스타일](#setstyle)|단추의 스타일을 설정합니다.|
|[CMFCToolBar 단추::가시설정](#setvisible)|단추를 볼 수 있는지 여부를 지정합니다.|
|[CMFCToolBar 버튼::표시](#show)|단추를 표시하거나 숨깁니다.|

### <a name="data-members"></a>데이터 멤버

|속성|Description|
|----------|-----------------|
|[CMFCToolBar 버튼:m_bImage](#m_bimage)|단추에 이미지가 표시되는지 여부를 지정합니다.|
|[CMFCToolBar 버튼::m_bText](#m_btext)|단추에 텍스트 레이블이 표시되는지 여부를 지정합니다.|
|[CMFCToolBar 버튼:m_bTextBelow](#m_btextbelow)|단추의 이미지 아래에 텍스트 레이블이 표시되는지 여부를 지정합니다.|
|[CMFCToolBar 버튼:m_bUserButton](#m_buserbutton)|단추에 사용자 정의 이미지가 있는지 여부를 지정합니다.|
|[CMFC툴바버튼:m_bWholeText](#m_bwholetext)|단추에 경계 사각형에 맞지 않는 경우에 전체 텍스트 레이블이 표시되는지 여부를 지정합니다.|
|[CMFCToolBar 버튼:m_bWrap](#m_bwrap)|구분 기호 옆에 있는 단추를 다음 행에 넣을지 여부를 지정합니다.|
|[CMFCToolBar 버튼:m_bWrapText](#m_bwraptext)|다중 줄 텍스트 레이블이 활성화되어 있는지 여부를 지정합니다.|
|[CMFCToolBar 버튼::m_nID](#m_nid)|단추의 명령 ID입니다.|
|[CMFCToolBar 버튼:m_nStyle](#m_nstyle)|단추의 스타일입니다.|
|[CMFC툴바버튼:m_strText](#m_strtext)|단추의 텍스트 레이블입니다.|

## <a name="remarks"></a>설명

`CMFCToolbarButton` 개체는 도구 모음에 있는 컨트롤입니다. 그 동작은 일반 버튼과 유사합니다. 이 개체에 이미지와 텍스트 레이블을 할당할 수 있습니다. 도구 모음 단추에는 명령 ID가 있을 수도 있습니다. 사용자가 도구 모음 단추를 클릭하면 프레임워크는 이 ID가 지정한 명령을 실행합니다.

일반적으로 도구 모음 단추를 사용자 지정할 수 있습니다. 사용자가 도구 모음을 사용자 지정하지 못하도록 두 가지 방법 중 하나로 도구 모음을 잠글 수 있습니다. `bLocked` [CMFCToolBar::LoadToolBar를](../../mfc/reference/cmfctoolbar-class.md#loadtoolbar)호출할 때 플래그를 TRUE로 설정하거나 [CMFCToolBarButton::SetProtectedCommands](#setprotectedcommands) 메서드를 사용하여 보호된 명령의 전역 목록에 개별 단추의 명령 ID를 추가합니다.

`CMFCToolBarButton`개체는 응용 프로그램에서 도구 모음 이미지의 전역 컬렉션에서 이미지를 표시합니다. 이러한 컬렉션은 상위 도구 [모음CMFCToolBar 클래스에서](../../mfc/reference/cmfctoolbar-class.md)유지 관리합니다. 자세한 내용은 [CMFCToolBarImages 클래스를](../../mfc/reference/cmfctoolbarimages-class.md)참조하십시오.

사용자가 도구 모음 단추를 클릭하면 상위 도구 모음에서 마우스 메시지를 처리하고 단추에 적절한 작업을 전달합니다. 단추에 유효한 명령 ID가 있는 경우 상위 도구 모음은 WM_COMMAND 메시지를 부모 프레임으로 보냅니다.

클래스는 `CMFCToolBarButton` [CMFCToolBarMenuButton 클래스,](../../mfc/reference/cmfctoolbarmenubutton-class.md) [CMFCToolBar편집상자 단추 클래스](../../mfc/reference/cmfctoolbareditboxbutton-class.md)및 [CMFCToolBarComboBoxButton 클래스와](../../mfc/reference/cmfctoolbarcomboboxbutton-class.md)같은 다른 도구 모음 단추 클래스의 기본 클래스입니다.

## <a name="example"></a>예제

다음 예제에서는 클래스에서 다양 `CMFCToolBarButton` 한 메서드를 사용 `CMFCToolBarButton` 하 여 개체를 구성 하는 방법을 보여 줍니다. 이 예제에서는 마우스 및 키보드 입력을 활성화하고, 단추의 이미지 인덱스를 설정하고, 단추의 경계 사각형을 설정하고, 단추를 표시하도록 하는 방법을 보여 줍니다. 이 코드 조각은 탭 [컨트롤 샘플의](../../overview/visual-cpp-samples.md)일부입니다.

[!code-cpp[NVC_MFC_TabControl#1](../../mfc/reference/codesnippet/cpp/cmfctoolbarbutton-class_1.cpp)]
[!code-cpp[NVC_MFC_TabControl#2](../../mfc/reference/codesnippet/cpp/cmfctoolbarbutton-class_2.cpp)]

## <a name="inheritance-hierarchy"></a>상속 계층 구조

[CObject](../../mfc/reference/cobject-class.md)

[CMFCToolBarButton](../../mfc/reference/cmfctoolbarbutton-class.md)

## <a name="requirements"></a>요구 사항

**헤더:** afxtoolbarbutton.h

## <a name="cmfctoolbarbuttoncanbedropped"></a><a name="canbedropped"></a>CMFCToolBar 버튼::수 있습니다.

사용자가 사용자 지정 하는 동안 도구 모음 또는 메뉴에 단추를 배치할 수 있는지 여부를 지정 합니다.

```
virtual BOOL CanBeDropped(CMFCToolBar* pToolbar);
```

### <a name="parameters"></a>매개 변수

*pToolbar*<br/>
【인】 하지 않는.

### <a name="return-value"></a>Return Value

이 메서드는 TRUE를 반환합니다.

### <a name="remarks"></a>설명

기본적으로 도구 모음 단추는 모든 사용자 지정 가능한(즉, 비잠금) 도구 모음에 삭제할 수 있습니다.

이 메서드의 기본 구현 TRUE를 반환 합니다. 사용자가 단추의 위치를 변경하지 못하도록 하려면 이 메서드를 재정의하고 FALSE를 반환합니다.

## <a name="cmfctoolbarbuttoncanbestored"></a><a name="canbestored"></a>CMFCToolBar 단추::수 저장

단추를 저장할 수 있는지 여부를 결정합니다.

```
virtual BOOL CanBeStored() const;
```

### <a name="return-value"></a>Return Value

이 메서드는 TRUE를 반환합니다.

### <a name="remarks"></a>설명

프레임워크는 이 메서드를 사용하여 단추를 끌어서 놓기 작업에 참여할 수 있는지 여부를 결정합니다.

기본 구현은 TRUE를 반환합니다. 단추를 끌어서 놓기 작업의 일부로 저장할 수 없는 경우 이 메서드를 재정의합니다. 끌어서 놓기 작업에 대한 자세한 내용은 [OLE 드래그 앤 드롭](../../mfc/drag-and-drop-ole.md)을 참조하십시오.

## <a name="cmfctoolbarbuttoncanbestretched"></a><a name="canbestretched"></a>CMFCToolBar 버튼 :: 수 있습니다

사용자 지정 하는 동안 단추를 늘릴 수 있는지 여부를 지정 합니다.

```
virtual BOOL CanBeStretched() const;
```

### <a name="return-value"></a>Return Value

이 메서드는 FALSE를 반환합니다.

### <a name="remarks"></a>설명

이 메서드는 프레임 워크에서 단추를 사용자 지정 모드에서 확장할 수 있는지 여부를 결정 하는 데 사용 됩니다.

이 메서드의 기본 구현은 FALSE를 반환합니다. 이 메서드를 재정의하여 콤보 상자 또는 슬라이더와 같은 가변 너비 컨트롤에 대해 TRUE를 반환합니다.

사용자 지정 모드에 대한 자세한 내용은 [CMFCToolBar::SetCustomizeMode](../../mfc/reference/cmfctoolbar-class.md#setcustomizemode)을 참조하십시오.

## <a name="cmfctoolbarbuttoncmfctoolbarbutton"></a><a name="cmfctoolbarbutton"></a>CMFCToolBar 버튼::CMFC툴바버튼

`CMFCToolBarButton` 개체를 생성하고 초기화합니다.

```
CMFCToolBarButton(
    UINT uiID,
    int iImage,
    LPCTSTR lpszText=NULL,
    BOOL bUserButton=FALSE,
    BOOL bLocked=FALSE);
```

### <a name="parameters"></a>매개 변수

*uiID*<br/>
【인】 단추의 명령 ID입니다.

*아이 이미지*<br/>
【인】 이미지 컬렉션의 단추의 이미지 인덱스입니다.

*lpszText*<br/>
【인】 단추의 텍스트 레이블입니다. NULL일 수 있습니다.

*b 사용자 버튼*<br/>
【인】 단추를 사용자 정의 여부를 결정 하는 부울 값입니다. 이 매개 변수가 TRUE이면 단추는 사용자 정의됩니다. 그렇지 않으면 단추 이미지가 리소스에서 로드됩니다.

*차단*<br/>
【인】 단추를 사용자 지정할 수 있는지 여부를 결정하는 부울 값입니다. 이 매개 변수가 TRUE이면 단추를 사용자 지정할 수 없습니다. 그렇지 않으면 단추를 사용자 지정할 수 있습니다.

## <a name="cmfctoolbarbuttoncomparewith"></a><a name="comparewith"></a>CMFCToolBar 단추::비교

이 인스턴스를 제공된 `CMFCToolBarButton` 개체와 비교합니다.

```
virtual BOOL CompareWith(const CMFCToolBarButton& other) const;
```

### <a name="parameters"></a>매개 변수

*다른*<br/>
【인】 이 인스턴스와 비교할 개체를 참조합니다.

### <a name="return-value"></a>Return Value

제공된 개체가 이 인스턴스의 값과 같으면 0이 아닙니다. 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

기본 구현은 제공된 개체의 명령 ID가 이 인스턴스의 명령 ID와 동일한지 여부를 결정합니다. 두 `CMFCToolBarButton` 개체가 동일한지 여부를 확인하기 위해 추가 처리를 수행해야 하는 경우 이 메서드를 재정의합니다.

## <a name="cmfctoolbarbuttoncopyfrom"></a><a name="copyfrom"></a>CMFCToolBar 단추::복사에서

다른 도구 모음 단추의 속성을 현재 단추에 복사합니다.

```
virtual void CopyFrom(const CMFCToolBarButton& src);
```

### <a name="parameters"></a>매개 변수

*src*<br/>
【인】 복사할 소스 단추에 대한 참조입니다.

### <a name="remarks"></a>설명

이 메서드를 호출하여 다른 도구 모음 단추를 이 도구 모음 단추에 복사합니다.

## <a name="cmfctoolbarbuttoncreatefromoledata"></a><a name="createfromoledata"></a>CMFCToolBar 버튼::생성FromOle데이터

제공된 `COleDataObject` `CMFCToolBarButton` 개체에서 개체를 만듭니다.

```
static CMFCToolBarButton* __stdcall CreateFromOleData(COleDataObject* pDataObject);
```

### <a name="parameters"></a>매개 변수

*pDataObject*<br/>
【인】 원본 OLE 데이터 개체입니다.

### <a name="return-value"></a>Return Value

만든 `CMFCToolBarButton` 개체입니다.

### <a name="remarks"></a>설명

이 메서드는 프레임 워크에서 다양한 형식으로 데이터 전송을 수행하는 데 사용됩니다. 예를 들어 `CMFCOutlookBarPane::OnDragOver` 메서드는 이 메서드를 사용하여 끌어서 놓기 작업을 수행합니다.

## <a name="cmfctoolbarbuttonenablewindow"></a><a name="enablewindow"></a>CMFCToolBar 단추::인에이블 윈도우

마우스 및 키보드 입력을 사용하거나 사용하지 않도록 설정합니다.

```
virtual void EnableWindow(BOOL bEnable = TRUE);
```

### <a name="parameters"></a>매개 변수

*bEnable*<br/>
【인】 이 매개 변수를 TRUE로 설정하여 입력을 활성화하거나 FALSE로 입력을 사용하지 않도록 설정합니다.

### <a name="remarks"></a>설명

이 메서드는 `EnableWindow` 입력을 활성화 하거나 비활성화 하는 함수를 호출 합니다. 자세한 내용은 Windows SDK의 [인에이블윈도우를](/windows/win32/api/winuser/nf-winuser-enablewindow) 참조하십시오.

## <a name="cmfctoolbarbuttonexporttomenubutton"></a><a name="exporttomenubutton"></a>CMFCToolBar 단추::내보내기 메뉴 단추

도구 모음 단추에서 메뉴로 텍스트를 복사합니다.

```
virtual BOOL ExportToMenuButton(CMFCToolBarMenuButton& menuButton) const;
```

### <a name="parameters"></a>매개 변수

*메뉴 버튼*<br/>
【인】 대상 메뉴 단추에 대한 참조입니다.

### <a name="return-value"></a>Return Value

이 메서드는 TRUE를 반환합니다.

### <a name="remarks"></a>설명

프레임워크는 도구 모음 단추에서 메뉴 단추로 텍스트를 복사하기 위해 이 메서드를 호출합니다. 기본 구현은 단추의 텍스트 레이블을 복사합니다. 텍스트 레이블이 비어 있으면 이 메서드는 단추의 도구 설명 텍스트를 복사합니다.

이 메서드의 기본 구현 TRUE를 반환 합니다. 프레임워크가 [CMFCToolbarButton에서](../../mfc/reference/cmfctoolbarbutton-class.md) 파생된 개체를 메뉴 단추로 변환할 때 추가 작업을 수행하려는 경우 이 메서드를 재정의합니다.

## <a name="cmfctoolbarbuttongetclipboardformat"></a><a name="getclipboardformat"></a>CMFCToolBar 버튼::겟클립보드 형식

응용 프로그램의 전역 클립보드 형식을 검색합니다.

```
static CLIPFORMAT __stdcall GetClipboardFormat();
```

### <a name="return-value"></a>Return Value

응용 프로그램의 전역 클립FORMAT 값입니다.

### <a name="remarks"></a>설명

프레임워크는 이 메서드를 호출하여 OLE 데이터 전송 작업에 대한 클립보드 형식을 검색합니다. 예를 들어 [CMFCToolBarButton::CreateFromOleData](#createfromoledata) 메서드는 이 메서드를 사용하여 원본 OLE 데이터 개체에서 데이터를 복사합니다.

이 메서드는 이 메서드를 처음 호출할 때 전역 CLIPFORMAT 값을 설정합니다. 이 메서드에 대한 모든 후속 호출은 이 값을 반환합니다.

응용 프로그램 간에 끌어서 놓기 작업이 수행되도록 하려면 [CMFCToolBarButton::SetClipboardFormatName](#setclipboardformatname) 메서드를 호출합니다.

MFC의 클립보드에 대한 자세한 내용은 [클립보드를](../../mfc/clipboard.md)참조하십시오.

## <a name="cmfctoolbarbuttongethwnd"></a><a name="gethwnd"></a>CMFCToolBar 버튼::GetHwnd

도구 모음 단추와 연결된 창 핸들을 검색합니다.

```
virtual HWND GetHwnd();
```

### <a name="return-value"></a>Return Value

도구 모음 단추에 연결된 창 핸들이 없는 경우 도구 모음 단추 또는 NULL과 연결된 창 핸들입니다.

### <a name="remarks"></a>설명

이 메서드의 기본 구현NULL을 반환 합니다. 특정 컨트롤의 창 핸들을 반환하려면 이 메서드를 재정의합니다.

## <a name="cmfctoolbarbuttongetimage"></a><a name="getimage"></a>CMFCToolBar 단추::이미지 가져옵니다.

단추의 이미지 인덱스를 검색합니다.

```
int GetImage() const;
```

### <a name="return-value"></a>Return Value

이 단추와 연결된 이미지의 인덱스입니다.

### <a name="remarks"></a>설명

단추에 사용자 정의 이미지가 있는 경우(즉, *bUserButton이* 생성자에서 TRUE인 경우) 반환된 인덱스는 사용자 정의 이미지 컬렉션의 이미지를 [지정합니다(CMFCToolBar::GetUserImages](../../mfc/reference/cmfctoolbar-class.md#getuserimages)참조). 그렇지 않으면 인덱스는 리소스 파일에서 로드된 이미지 컬렉션의 이미지를 [지정합니다(CMFCToolBar::GetImages](../../mfc/reference/cmfctoolbar-class.md#getimages)참조). 리소스 파일에 대한 자세한 내용은 [리소스 파일 작업을](../../windows/working-with-resource-files.md)참조하십시오.

## <a name="cmfctoolbarbuttongetinvalidaterect"></a><a name="getinvalidaterect"></a>CMFCToolBar 단추::Get무효화렉트

다시 그려야 하는 단추의 클라이언트 영역영역을 검색합니다.

```
virtual const CRect GetInvalidateRect() const;
```

### <a name="return-value"></a>Return Value

다시 `CRect` 그려야 하는 영역을 지정하는 개체입니다.

### <a name="remarks"></a>설명

이 메서드의 기본 구현은 전체 클라이언트 영역을 반환합니다. 다른 영역을 다시 그릴 경우 이 메서드를 재정의합니다.

## <a name="cmfctoolbarbuttongetparentwnd"></a><a name="getparentwnd"></a>CMFCToolBar 버튼::GetParentWnd

단추의 상위 창을 검색합니다.

```
CWnd* GetParentWnd() const;
```

### <a name="return-value"></a>Return Value

단추의 상위 창입니다.

## <a name="cmfctoolbarbuttongetprotectedcommands"></a><a name="getprotectedcommands"></a>CMFCToolBar 버튼::보호 명령

사용자가 사용자 지정할 수 없는 명령 목록을 검색합니다.

```
static const CList<UINT,UINT>& GetProtectedCommands();
```

### <a name="return-value"></a>Return Value

보호된 명령 목록입니다.

### <a name="remarks"></a>설명

사용자 지정 모드에서 프레임워크는 보호되는 도구 모음 단추 명령을 비활성화합니다. 사용자는 비활성화된 도구 모음 단추에서 끌어서 놓기 및 편집 작업을 수행할 수 없습니다.

[CMFCToolBarButton::SetProtectedCommands 메서드를](#setprotectedcommands) 사용하여 보호된 명령 목록을 정의합니다.

## <a name="cmfctoolbarbuttongettextsize"></a><a name="gettextsize"></a>CMFCToolBar 단추::GetText크기

단추 텍스트의 크기를 검색합니다.

```
SIZE GetTextSize() const;
```

### <a name="return-value"></a>Return Value

단추 텍스트의 크기(픽셀 단위)를 포함하는 SIZE 개체입니다.

## <a name="cmfctoolbarbuttonhasfocus"></a><a name="hasfocus"></a>CMFCToolBar 버튼::하스포커스

단추에 현재 입력 포커스가 있는지 여부를 결정합니다.

```
virtual BOOL HasFocus() const;
```

### <a name="return-value"></a>Return Value

버튼에 입력 포커스가 있는 경우 0이 아닙니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

이 메서드의 기본 구현 단추에 입력 포커스가 있거나 입력 포커스가 있는 창의 자식 또는 하위 창인 경우 비영도 반환됩니다. 이 함수를 재정의하여 이 동작을 사용자 지정할 수 있습니다.

## <a name="cmfctoolbarbuttonhavehotborder"></a><a name="havehotborder"></a>CMFCToolBar 버튼::하핫보더

사용자가 단추를 선택할 때 단추의 테두리가 표시되는지 여부를 결정합니다.

```
virtual BOOL HaveHotBorder() const;
```

### <a name="return-value"></a>Return Value

이 메서드는 TRUE를 반환합니다.

### <a name="remarks"></a>설명

프레임워크는 이 메서드를 호출하여 사용자가 선택할 때 도구 모음 단추에 테두리를 표시할지 여부를 결정합니다.

기본 구현은 TRUE를 반환합니다. 이 메서드를 재정의하여 이 동작을 사용자 지정할 수 있습니다.

## <a name="cmfctoolbarbuttonisdrawimage"></a><a name="isdrawimage"></a>CMFCToolBar 단추::이스드이미지

단추에 이미지가 표시되는지 여부를 결정합니다.

```
BOOL IsDrawImage() const;
```

### <a name="return-value"></a>Return Value

버튼에 이미지가 표시되는 경우 0이 아닙니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

이 메서드는 도구 모음 단추에 연관된 [이미지(CMFCToolBarButton::GetImage](#getimage) 반환 -1) 또는 [CMFCToolBarButton::m_bImage](#m_bimage) FALSE로 설정된 경우 FALSE를 반환합니다.

## <a name="cmfctoolbarbuttonisdrawtext"></a><a name="isdrawtext"></a>CMFCToolBar 단추::이스드로우텍스트

단추에 텍스트 레이블이 표시되는지 여부를 결정합니다.

```
BOOL IsDrawText() const;
```

### <a name="return-value"></a>Return Value

텍스트 레이블이 표시되는 경우 0이 아닙니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

이 메서드는 도구 모음 단추에 연관된 텍스트 [레이블(CMFCToolBarButton:m_strText](#m_strtext) 비어 있음) 또는 [CMFCToolBarButton:m_bText](#m_btext) FALSE로 설정된 경우 FALSE를 반환합니다.

## <a name="cmfctoolbarbuttonisdroppeddown"></a><a name="isdroppeddown"></a>CMFCToolBar 단추::삭제

단추에 하위 메뉴가 표시되는지 여부를 결정합니다.

```
virtual BOOL IsDroppedDown() const;
```

### <a name="return-value"></a>Return Value

이 메서드는 FALSE를 반환합니다.

### <a name="remarks"></a>설명

이 메서드의 기본 구현은 FALSE를 반환합니다. 컨트롤에 하위 메뉴가 표시되는 경우 TRUE를 반환하려면 이 메서드를 재정의합니다.

## <a name="cmfctoolbarbuttoniseditable"></a><a name="iseditable"></a>CMFCToolBar 단추::편집 가능

단추를 사용자 지정할 수 있는지 여부를 결정합니다.

```
virtual BOOL IsEditable() const;
```

### <a name="return-value"></a>Return Value

사용자가 단추를 사용자 지정할 수 있는 경우 0이 아닙니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

프레임워크는 이 메서드를 호출하여 사용자가 끌어서 놓기 작업을 사용하여 도구 모음 단추를 사용자 지정할 수 있는지 여부를 결정합니다.

단추의 명령 ID가 표준 `IsStandardCommand` 명령(함수를 호출하여 확인할 수 있음) 또는 명령 ID가 보호된 명령 목록에 있는 경우 기본 구현은 FALSE를 반환합니다. 보호된 명령에 대한 자세한 내용은 [CMFCToolBarButton::Getprotected명령](#getprotectedcommands) 및 [CMFCToolBarButton::SetprotectedCommand 을](#setprotectedcommands)참조하십시오.

이 메서드를 재정의하여 동작을 사용자 지정합니다.

## <a name="cmfctoolbarbuttonisextrasize"></a><a name="isextrasize"></a>CMFCToolBar 버튼::이엑스트라사이즈

확장된 테두리로 단추를 표시할 수 있는지 여부를 결정합니다.

```
virtual BOOL IsExtraSize() const;
```

### <a name="return-value"></a>Return Value

도구 모음 단추를 확장된 테두리로 표시할 수 있는 경우 0이 아닙니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

도구 모음 단추의 테두리(예: 둥근 단추)에 대해 여러 스킨이 추가 크기를 사용합니다.

사용자가 이 단추를 한 도구 모음에서 다른 도구 모음으로 이동하면 프레임워크는 [CMFCToolBarButton::OnChangeParentWnd 메서드를 호출합니다.](#onchangeparentwnd) [CMFCToolBarButton::OnChangeParentWnd](#onchangeparentwnd) 메서드는 추가 크기 플래그를 새 상위 도구 모음의 플래그로 설정합니다(자세한 내용은 [CMFCToolBar::IsButtonExtraSize사용 가능)를](../../mfc/reference/cmfctoolbar-class.md#isbuttonextrasizeavailable)참조하십시오.

## <a name="cmfctoolbarbuttonisfirstingroup"></a><a name="isfirstingroup"></a>CMFCToolBar 버튼::IsFirstIn그룹

단추 그룹의 첫 번째 위치에 단추를 지정합니다.

```
virtual BOOL IsFirstInGroup() const;
```

### <a name="return-value"></a>Return Value

TRUE 단추그룹의 첫 번째 단추인 경우 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

이 메서드는 *단추 그룹을* 동일한 행에 배치 하 고 구분 기호 또는 도구 모음의 테두리에 의해 경계가 있는 단추집합으로 정의 합니다. 이 메서드는 도구 모음 단추 사용자 **지정** 단추를 참조 하는 경우 FALSE를 반환 합니다. **사용자 지정** 단추에 대한 자세한 내용은 [CMFCToolBar::GetizeButton](../../mfc/reference/cmfctoolbar-class.md#getcustomizebutton)을 참조하십시오.

[CMFCToolBarButton::IsLastInGroup](#islastingroup) 메서드를 호출하여 단추 그룹의 마지막 위치에 있는지 확인합니다.

## <a name="cmfctoolbarbuttonishidden"></a><a name="ishidden"></a>CMFCToolBar 단추::숨겨져 있습니다.

단추숨김 여부를 결정합니다.

```
BOOL IsHidden() const;
```

### <a name="return-value"></a>Return Value

버튼이 숨겨져 있으면 0이 아닌 경우(보이지 않음); 그렇지 않으면 0.

### <a name="remarks"></a>설명

프레임워크는 도구 모음 이 단추를 표시할지 여부를 결정하기 위해 부모 도구 모음을 늘일 때 이 메서드를 호출합니다.

[CMFCToolBarButton::SetVisible](#setvisible) 메서드를 사용하여 단추를 보이지 않게 설정한 경우 [CMFCToolBarButton::IsVisible를](#isvisible) 사용하여 도구 모음 버튼이 표시되는지 확인합니다.

기본적으로 모든 도구 모음 단추가 표시됩니다. [CMFCToolBarButton::표시](#show) 방법을 사용하여 도구 모음 단추를 숨기거나 표시합니다.

## <a name="cmfctoolbarbuttonishorizontal"></a><a name="ishorizontal"></a>CMFCToolBar 버튼::수평

단추를 가로 도구 모음에 있는지 여부를 확인합니다.

```
BOOL IsHorizontal() const;
```

### <a name="return-value"></a>Return Value

도구 모음 단추가 가로 도구 모음에 있는 경우 0이 아닙니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

프레임워크는 이 메서드를 호출하여 도구 모음 단추의 레이아웃을 결정합니다.

이 메서드는 `m_bHorz` 데이터 멤버를 반환합니다. 데이터 멤버의 `m_bHorz` 기본값은 TRUE입니다. [CMFCToolBarButton::OnDraw](#ondraw) 메서드에 대한 각 호출에서 재설정됩니다.

## <a name="cmfctoolbarbuttonislastingroup"></a><a name="islastingroup"></a>CMFC툴바버튼::이라스트인그룹

단추그룹의 마지막 위치에 있는지 여부를 지정합니다.

```
virtual BOOL IsLastInGroup() const;
```

### <a name="return-value"></a>Return Value

TRUE 단추그룹의 마지막 단추인 경우 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

이 메서드는 *단추 그룹을* 동일한 행에 배치 하 고 구분 기호 또는 도구 모음의 테두리에 의해 경계가 있는 단추 집합으로 정의 합니다.이 메서드는 도구 모음 단추에 상위 도구 모음이 없거나 도구 모음 단추를 **참조** 하는 경우 FALSE를 반환 합니다. **사용자 지정** 단추에 대한 자세한 내용은 [CMFCToolBar::GetizeButton](../../mfc/reference/cmfctoolbar-class.md#getcustomizebutton)을 참조하십시오.

[CMFCToolBarButton::IsFirstInGroup](#isfirstingroup) 메서드를 호출하여 단추 그룹의 첫 번째 위치에 있는지 확인합니다.

## <a name="cmfctoolbarbuttonislocked"></a><a name="islocked"></a>CMFCToolBar 단추::잠금

단추가 잠긴(사용자 지정할 수 없는) 도구 모음에 있는지 여부를 확인합니다.

```
BOOL IsLocked() const;
```

### <a name="return-value"></a>Return Value

버튼이 잠긴 도구 모음에 있는 경우 0이 아닙니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

프레임워크는 이 메서드를 호출하여 사용자가 끌어서 놓기 작업을 사용하여 도구 모음 단추를 사용자 지정할 수 있는지 여부를 결정합니다. [CMFCToolBar::LoadToolBar](../../mfc/reference/cmfctoolbar-class.md#loadtoolbar) 메서드를 사용하여 상위 도구 모음에서 잠긴 특성을 설정합니다. 프레임워크는 이 특성의 값을 상위 도구 모음에 삽입하는 각 도구 모음 [단추(CMFCToolbarButton)의](../../mfc/reference/cmfctoolbarbutton-class.md)생성자에게 전달합니다.

## <a name="cmfctoolbarbuttonisownerof"></a><a name="isownerof"></a>CMFCToolBar 버튼::IsOwnerOf

단추가 제공된 창 핸들의 소유자인지 여부를 결정합니다.

```
virtual BOOL IsOwnerOf(HWND hwnd);
```

### <a name="parameters"></a>매개 변수

*Hwnd*<br/>
【인】 창 핸들입니다.

### <a name="return-value"></a>Return Value

버튼이 제공된 창 핸들의 소유자인 경우 비영; 그렇지 않으면 0.

### <a name="remarks"></a>설명

*hwnd가* 직접 창 핸들을 참조하거나 단추와 연결된 창 핸들의 자식인 경우 이 메서드는 비영을 반환합니다. *hwnd가* NULL인 경우 이 메서드는 0을 반환합니다.

## <a name="cmfctoolbarbuttonisvisible"></a><a name="isvisible"></a>CMFCToolBar 단추::볼 수 있습니다.

도구 모음 단추를 볼 수 있는지 여부를 확인합니다.

```
BOOL IsVisible() const;
```

### <a name="return-value"></a>Return Value

도구 모음 단추가 표시되는 경우 0이 아닙니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

[CMFCToolBarButton::SetVisible](#setvisible) 메서드를 사용하여 도구 모음 단추를 표시하거나 숨길 수 있습니다. [CPane::adjustSizeCMFCToolBarButton::SetVisible를](#setvisible) 호출한 후 상위 도구 모음에서 즉시 메서드를 호출하여 상위 도구 모음의 레이아웃을 다시 계산합니다. [CPane::AdjustSizeImmediate](../../mfc/reference/cpane-class.md#adjustsizeimmediate)

## <a name="cmfctoolbarbuttoniswindowvisible"></a><a name="iswindowvisible"></a>CMFCToolBar 단추::창가볼 수 있습니다.

단추의 기본 창 핸들이 표시되는지 여부를 확인합니다.

```
virtual BOOL IsWindowVisible();
```

### <a name="return-value"></a>Return Value

단추의 기본 창 핸들이 표시되는 경우 0이 아닙니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

이 메서드는 기본 창 핸들의 스타일 특성에 WS_VISIBLE 스타일이 포함된 경우 0이 아닌 메서드를 반환합니다. 이 메서드는 단추의 기본 창 핸들이 NULL인 경우 FALSE를 반환합니다.

## <a name="cmfctoolbarbuttonm_bimage"></a><a name="m_bimage"></a>CMFCToolBar 버튼:m_bImage

단추에 이미지가 표시되는지 여부를 지정합니다.

```
BOOL m_bImage;
```

### <a name="remarks"></a>설명

이 데이터 멤버가 TRUE로 설정된 경우 프레임워크는 도구 모음 단추와 연결된 이미지를 표시합니다. 그렇지 않으면 프레임워크에 이미지가 표시되지 않습니다. 이 멤버는 [CMFCToolBarButton::m_bImage](#m_bimage) 메서드의 반환 값에 영향을 줍니다.

## <a name="cmfctoolbarbuttonm_btext"></a><a name="m_btext"></a>CMFCToolBar 버튼::m_bText

단추에 텍스트 레이블이 표시되는지 여부를 지정합니다.

```
BOOL m_bText;
```

### <a name="remarks"></a>설명

이 데이터 멤버가 TRUE로 설정된 경우 프레임워크는 도구 모음 단추의 텍스트 레이블을 표시합니다. 그렇지 않으면 프레임워크에 텍스트 레이블이 표시되지 않습니다. 이 멤버는 [CMFCToolBarButton::m_bText](#m_btext) 메서드의 반환 값에 영향을 줍니다.

## <a name="cmfctoolbarbuttonm_btextbelow"></a><a name="m_btextbelow"></a>CMFCToolBar 버튼:m_bTextBelow

단추의 이미지 아래에 텍스트 레이블이 표시되는지 여부를 지정합니다.

```
BOOL m_bTextBelow;
```

### <a name="remarks"></a>설명

이 멤버 변수가 TRUE로 설정된 경우 프레임워크는 이미지 아래에 단추의 텍스트를 표시합니다. 이 멤버의 기본값은 FALSE입니다.

## <a name="cmfctoolbarbuttonm_buserbutton"></a><a name="m_buserbutton"></a>CMFCToolBar 버튼:m_bUserButton

단추에 사용자 정의 이미지가 있는지 여부를 지정합니다.

```
BOOL m_bUserButton;
```

### <a name="remarks"></a>설명

이 데이터 멤버는 단추에 연결된 사용자 정의 이미지가 있는 경우 TRUE로 설정됩니다.

## <a name="cmfctoolbarbuttonm_bwholetext"></a><a name="m_bwholetext"></a>CMFC툴바버튼:m_bWholeText

단추에 경계 사각형에 맞지 않는 경우에 전체 텍스트 레이블이 표시되는지 여부를 지정합니다.

```
BOOL m_bWholeText;
```

### <a name="remarks"></a>설명

이 데이터 멤버가 TRUE로 설정된 경우 프레임워크는 단추를 확대하여 전체 텍스트 레이블을 표시합니다. 그렇지 않으면 프레임워크가 텍스트 레이블에 타원(... **...**)을 트렌지하고 추가합니다.

## <a name="cmfctoolbarbuttonm_bwrap"></a><a name="m_bwrap"></a>CMFCToolBar 버튼:m_bWrap

구분 기호 옆에 있는 단추를 다음 행에 넣을지 여부를 지정합니다.

```
BOOL m_bWrap;
```

### <a name="remarks"></a>설명

프레임워크는 도구 모음 단추가 현재 행에 맞지 않거나 레이아웃(예: 행당 특정 도구 모음 단추 수)을 지정할 때 이 데이터 멤버를 TRUE로 설정합니다.

이 데이터 멤버가 TRUE로 설정되고 도구 모음이 가로 또는 부동도킹된 경우 프레임워크에서 이 단추를 다음 행에 배치합니다.

이 데이터 멤버의 기본값은 FALSE입니다.

## <a name="cmfctoolbarbuttonm_bwraptext"></a><a name="m_bwraptext"></a>CMFCToolBar 버튼:m_bWrapText

다중 줄 텍스트 레이블이 활성화되어 있는지 여부를 지정합니다.

```
AFX_IMPORT_DATA static BOOL m_bWrapText;
```

### <a name="remarks"></a>설명

이 정적 멤버 변수가 TRUE이면 프레임워크를 사용하면 모든 도구 모음에서 도구 모음 단추에 여러 줄텍스트 레이블을 표시할 수 있습니다.

이 데이터 멤버의 기본값은 FALSE입니다.

## <a name="cmfctoolbarbuttonm_nid"></a><a name="m_nid"></a>CMFCToolBar 버튼::m_nID

단추의 명령 ID입니다.

```
UINT m_nID;
```

### <a name="remarks"></a>설명

명령 ID -1은 단추가 구분 기호임을 나타냅니다. 모든 버튼 구분 기호는 TBBS_SEPARATOR 스타일을 갖습니다. 버튼 스타일에 대한 자세한 내용은 [CMFCToolBarButton::m_nStyle](#m_nstyle) 를 참조하십시오.

## <a name="cmfctoolbarbuttonm_nstyle"></a><a name="m_nstyle"></a>CMFCToolBar 버튼:m_nStyle

단추의 스타일입니다.

```
UINT m_nStyle;
```

### <a name="remarks"></a>설명

사용 가능한 도구 모음 단추 스타일 목록은 [도구 모음 컨트롤 스타일을](../../mfc/reference/toolbar-control-styles.md) 참조하십시오.

## <a name="cmfctoolbarbuttonm_strtext"></a><a name="m_strtext"></a>CMFC툴바버튼:m_strText

단추의 텍스트 레이블입니다.

```
CString m_strText;
```

### <a name="remarks"></a>설명

이 데이터 멤버에는 단추의 텍스트 레이블이 포함되어 있습니다. 텍스트 레이블은 비어 있을 수 있습니다.

## <a name="cmfctoolbarbuttonnotifycommand"></a><a name="notifycommand"></a>CMFCToolBar 단추::알림 명령

단추에서 [WM_COMMAND](/windows/win32/menurc/wm-command) 메시지를 처리하는지 여부를 지정합니다.

```
virtual BOOL NotifyCommand(int iNotifyCode);
```

### <a name="parameters"></a>매개 변수

*iNotifyCode*<br/>
【인】 명령과 연결된 알림 메시지입니다.

### <a name="return-value"></a>Return Value

이 메서드는 FALSE를 반환합니다.

### <a name="remarks"></a>설명

프레임워크는 부모 창에 [WM_COMMAND](/windows/win32/menurc/wm-command) 메시지를 보내려고 할 때 이 메서드를 호출합니다.

기본적으로 이 메서드는 FALSE를 반환합니다. WM_COMMAND 메시지 또는 FALSE를 처리하여 상위 도구 모음에서 메시지를 처리해야 함을 나타내려면 이 메서드를 재정의하여 TRUE를 반환합니다.

## <a name="cmfctoolbarbuttononaddtocustomizepage"></a><a name="onaddtocustomizepage"></a>CMFCToolBar 단추::OnAddTo 사용자 지정 페이지

단추를 **사용자 지정** 대화 상자에 추가 될 때 프레임 워크에 의해 호출 됩니다.

```
virtual void OnAddToCustomizePage();
```

### <a name="remarks"></a>설명

이 메서드의 기본 구현은 아무 것도 수행하지 않습니다. **사용자 지정** 대화 상자에 단추를 추가할 때 일부 작업을 수행하려는 경우 이 메서드를 재정의합니다.

## <a name="cmfctoolbarbuttononbeforedrag"></a><a name="onbeforedrag"></a>CMFCToolBar 버튼::에 드래그

단추를 끌 수 있는지 여부를 지정합니다.

```
virtual BOOL OnBeforeDrag() const;
```

### <a name="return-value"></a>Return Value

TRUE 단추를 끌 수 있는 경우; 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

사용자가 단추를 끌기 전에 프레임워크는 이 메서드를 호출합니다.

이 메서드의 기본 구현 TRUE를 반환 합니다. 이 메서드를 재정의하여 FALSE를 반환하여 단추의 끌기를 비활성화합니다.

## <a name="cmfctoolbarbuttononbeforedrop"></a><a name="onbeforedrop"></a>CMFCToolBar 버튼::에 드롭

사용자가 단추를 대상 도구 모음에 놓을 수 있는지 여부를 지정합니다.

```
virtual BOOL OnBeforeDrop(CMFCToolBar* pTarget);
```

### <a name="parameters"></a>매개 변수

*p Target*<br/>
【인】 끌어서 놓기 작업의 대상입니다.

### <a name="return-value"></a>Return Value

TRUE 버튼을 제공된 대상 도구 모음에 떨어뜨릴 수 있는 경우; 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

단추를 도구 모음에 삭제하기 전에 프레임워크에서 이 메서드를 호출합니다.

이 메서드의 기본 구현 TRUE를 반환 합니다. 이 메서드를 재정의하여 FALSE를 반환하여 지정된 대상에서 놓기 작업을 사용하지 않도록 설정합니다.

## <a name="cmfctoolbarbuttononcalculatesize"></a><a name="oncalculatesize"></a>CMFCToolBar 버튼::계산크기

지정된 장치 컨텍스트 및 도킹 상태에 대한 단추 크기를 계산하기 위해 프레임워크에서 호출합니다.

```
virtual SIZE OnCalculateSize(
    CDC* pDC,
    const CSize& sizeDefault,
    BOOL bHorz);
```

### <a name="parameters"></a>매개 변수

*pDC*<br/>
【인】 단추를 표시 하는 장치 컨텍스트입니다.

*크기기본값*<br/>
【인】 단추의 기본 크기입니다.

*b호르츠 (주)*<br/>
【인】 상위 도구 모음의 도크 상태입니다. 이 매개변수는 도구 모음이 수평으로 도킹되거나 부동 인 경우 TRUE이거나 도구 모음이 수직으로 도킹된 경우 FALSE입니다.

### <a name="return-value"></a>Return Value

단추의 치수를 픽셀 단위로 포함하는 `SIZE` 구조입니다.

### <a name="remarks"></a>설명

프레임워크는 이 메서드를 호출하여 지정된 장치 컨텍스트 및 도킹 상태에 대한 도구 모음 단추의 크기를 결정합니다.

기본 구현에서는 텍스트 및 이미지 크기(표시되는 경우), 텍스트 및 이미지 위치(이미지의 아래 또는 오른쪽에 있는 텍스트) 및 도구 모음 도크 상태를 고려합니다.

비표준 버튼(예: 편집 상자 단추)의 크기를 제공하려는 경우 이 메서드를 재정의합니다.

## <a name="cmfctoolbarbuttononcancelmode"></a><a name="oncancelmode"></a>CMFCToolBar 단추::온취소 모드

[WM_CANCELMODE](/windows/win32/winmsg/wm-cancelmode) 메시지를 처리 하는 프레임 워크에 의해 호출 됩니다.

```
virtual void OnCancelMode();
```

### <a name="remarks"></a>설명

이 메서드의 기본 구현은 아무 것도 수행하지 않습니다. [WM_CANCELMODE](/windows/win32/winmsg/wm-cancelmode) 메시지를 처리하려는 경우 이 메서드를 재정의합니다.

## <a name="cmfctoolbarbuttononchangeparentwnd"></a><a name="onchangeparentwnd"></a>CMFCToolBar 단추::온체인지부모

단추를 새 도구 모음에 삽입할 때 프레임워크에서 호출됩니다.

```
virtual void OnChangeParentWnd(CWnd* pWndParent);
```

### <a name="parameters"></a>매개 변수

*pWndParent*<br/>
【인】 새 부모 창입니다.

### <a name="remarks"></a>설명

예를 들어 사용자가 한 도구 모음에서 다른 도구 모음으로 단추를 드래그할 때 단추가 도구 모음에 삽입됩니다.

이 메서드의 기본 구현은 아무 것도 수행하지 않습니다.

## <a name="cmfctoolbarbuttononclick"></a><a name="onclick"></a>CMFCToolBar 단추::클릭

사용자가 마우스 단추를 클릭할 때 프레임워크에서 호출됩니다.

```
virtual BOOL OnClick(
    CWnd* pWnd,
    BOOL bDelay=TRUE);
```

### <a name="parameters"></a>매개 변수

*pWnd*<br/>
【인】 도구 모음 단추의 상위 창입니다.

*bDelay*<br/>
【인】 TRUE 메시지가 지연된 처리되어야 하는 경우

### <a name="return-value"></a>Return Value

이 메서드는 FALSE를 반환합니다.

### <a name="remarks"></a>설명

사용자가 도구 모음 단추를 클릭할 때 프레임워크는 이 메서드를 호출합니다.

기본 구현은 아무 것도 수행하지 않으며 FALSE를 반환합니다. 버튼이 클릭 메시지를 처리하는 경우 이 메서드를 재정의하여 0이 아닌 값을 반환합니다.

## <a name="cmfctoolbarbuttononclickup"></a><a name="onclickup"></a>CMFCToolBar 버튼::클릭업

사용자가 마우스 단추를 해제할 때 프레임워크에서 호출됩니다.

```
virtual BOOL OnClickUp();
```

### <a name="return-value"></a>Return Value

이 메서드는 FALSE를 반환합니다.

### <a name="remarks"></a>설명

사용자가 도구 모음 단추를 해제할 때 프레임워크는 이 메서드를 호출합니다.

기본 구현은 아무 것도 수행하지 않으며 FALSE를 반환합니다. 버튼이 클릭 메시지를 처리하는 경우 이 메서드를 재정의하여 0이 아닌 값을 반환합니다.

## <a name="cmfctoolbarbuttononcontexthelp"></a><a name="oncontexthelp"></a>CMFCToolBar 단추::온컨텍스트 도움말

상위 도구 모음에서 WM_HELPHITTEST 메시지를 처리할 때 프레임워크에서 호출합니다.

```
virtual BOOL OnContextHelp(CWnd* pWnd);
```

### <a name="parameters"></a>매개 변수

*pWnd*<br/>
【인】 도구 모음 단추의 상위 창입니다.

### <a name="return-value"></a>Return Value

이 메서드는 FALSE를 반환합니다.

### <a name="remarks"></a>설명

이 메서드의 기본 구현은 아무 것도 수행하지 않으며 FALSE를 반환합니다. 버튼이 도움말 메시지를 처리하는 경우 이 메서드를 재정의하여 zero가 아닌 값을 반환합니다.

WM_HELPHITTEST 메시지에 대한 자세한 내용은 [TN028: 컨텍스트 구분 도움말 지원을](../../mfc/tn028-context-sensitive-help-support.md)참조하십시오.

## <a name="cmfctoolbarbuttononctlcolor"></a><a name="onctlcolor"></a>CMFCToolBar 버튼 :: 온CtlColor

상위 도구 모음에서 WM_CTLCOLOR 메시지를 처리할 때 프레임워크에서 호출합니다.

```
virtual HBRUSH OnCtlColor(
    CDC* pDC,
    UINT nCtlColor);
```

### <a name="parameters"></a>매개 변수

*pDC*<br/>
【인】 단추를 표시 하는 장치 컨텍스트입니다.

*nCtlColor*<br/>
【인】 특정 색상 알림입니다.

### <a name="return-value"></a>Return Value

프레임워크가 단추의 배경을 페인트하는 데 사용하는 브러시 오브젝트에 대한 핸들입니다.

### <a name="remarks"></a>설명

상위 도구 모음에서 Windows 컨트롤이 포함된 도구 모음 단추에 대해 WM_CTLCOLOR 메시지를 처리하는 경우 프레임워크는 이 메서드를 호출합니다. 도구 모음 단추에 창이 없는 경우 프레임워크에서 이 메서드를 호출하지 않습니다.

프레임워크는 도구 모음 프레임워크가 사용자 지정 모드에 있고 도구 모음 단추의 잠금이 해제될 때 이 메서드를 호출합니다. 사용자 지정 모드에 대한 자세한 내용은 [CMFCToolBar::SetCustomizeMode](../../mfc/reference/cmfctoolbar-class.md#setcustomizemode)을 참조하십시오. 잠금 도구 모음 단추에 대한 자세한 내용은 [CMFCToolBarButton::IsLocked](#islocked)을 참조하십시오.

기본 구현은 아무 것도 수행하지 않으며 NULL을 반환합니다.

## <a name="cmfctoolbarbuttononcustomizemenu"></a><a name="oncustomizemenu"></a>CMFCToolBar 단추::온 커스터마이즈메뉴

응용 프로그램이 상위 도구 모음에 바로 가기 메뉴를 표시할 때 단추에서 제공된 메뉴를 수정할 수 있습니다.

```
virtual BOOL OnCustomizeMenu(CMenu* pMenu);
```

### <a name="parameters"></a>매개 변수

*p메뉴*<br/>
【인】 사용자 정의 할 메뉴입니다.

### <a name="return-value"></a>Return Value

이 메서드는 FALSE를 반환합니다.

### <a name="remarks"></a>설명

기본 구현은 아무 것도 수행하지 않으며 FALSE를 반환합니다. 제공된 메뉴의 내용을 수정하려는 경우 이 메서드를 재정의하고 zero가 아닌 값을 반환합니다.

## <a name="cmfctoolbarbuttonondblclk"></a><a name="ondblclk"></a>CMFCToolBar 버튼::온드블클크

상위 도구 모음에서 [WM_LBUTTONDBLCLK](/windows/win32/inputdev/wm-lbuttondblclk) 메시지를 처리할 때 프레임워크에서 호출합니다.

```
virtual void OnDblClk(CWnd* pWnd);
```

### <a name="parameters"></a>매개 변수

*pWnd*<br/>
[in] - 단추의 상위 창입니다.

### <a name="remarks"></a>설명

이 메서드는 부모 `CMFCToolBar::OnLButtonDblClk` 도구 모음에서 [WM_LBUTTONDBLCLK](/windows/win32/inputdev/wm-lbuttondblclk) 메시지를 처리할 때 메서드에서 호출됩니다.

이 메서드의 기본 구현은 아무 것도 수행하지 않습니다.

## <a name="cmfctoolbarbuttonondraw"></a><a name="ondraw"></a>CMFCToolBar 버튼::온드로우

지정된 스타일과 옵션을 사용하여 단추를 그리는 프레임워크에서 호출합니다.

```
virtual void OnDraw(
    CDC* pDC,
    const CRect& rect,
    CMFCToolBarImages* pImages,
    BOOL bHorz=TRUE,
    BOOL bCustomizeMode=FALSE,
    BOOL bHighlight=FALSE,
    BOOL bDrawBorder=TRUE,
    BOOL bGrayDisabledButtons=TRUE);
```

### <a name="parameters"></a>매개 변수

*pDC*<br/>
【인】 단추를 표시 하는 장치 컨텍스트입니다.

*rect*<br/>
【인】 단추의 경계 사각형입니다.

*p 이미지*<br/>
【인】 단추와 연결된 도구 모음 이미지의 컬렉션입니다.

*b호르츠 (주)*<br/>
【인】 상위 도구 모음의 도크 상태입니다. 이 매개 변수는 단추를 가로로 도킹할 때 TRUE이고 단추를 세로로 도킹할 때 FALSE입니다.

*b 사용자 정의 모드*<br/>
【인】 도구 모음이 사용자 지정 모드에 있는지 여부를 지정합니다. 이 매개 변수는 도구 모음이 사용자 지정 모드에 있을 때 TRUE이고 도구 모음이 사용자 지정 모드에 있지 않은 경우 FALSE입니다.

*bHighlight*<br/>
【인】 단추를 강조 표시할지 여부를 지정합니다. 이 매개 변수는 단추가 강조 표시될 때 TRUE이고 단추를 강조 표시하지 않을 때 FALSE입니다.

*b드드로우보더*<br/>
【인】 단추에 테두리를 표시할지 여부를 지정합니다. 이 매개 변수는 단추에 테두리를 표시 해야 하는 경우 TRUE 및 FALSE 단추에 테두리를 표시 하지 않아야 합니다.

*b회색비활성화단추*<br/>
【인】 비활성화된 단추를 음영할지 아니면 비활성화된 이미지 컬렉션을 사용할지 지정합니다. 이 매개 변수는 비활성화된 단추를 그늘에 가두어야 하는 경우 TRUE이고 이 메서드는 비활성화된 이미지 컬렉션을 사용해야 하는 경우 FALSE입니다.

### <a name="remarks"></a>설명

도구 모음 단추 그리기를 사용자 지정하려면 이 메서드를 재정의합니다.

## <a name="cmfctoolbarbuttonondrawoncustomizelist"></a><a name="ondrawoncustomizelist"></a>CMFCToolBar 단추::온드로우온 커스터즈리스트

**사용자 지정 대화** 상자의 명령 창에서 단추를 그리는 프레임워크에서 **호출합니다.**

```
virtual int OnDrawOnCustomizeList(
    CDC* pDC,
    const CRect& rect,
    BOOL bSelected);
```

### <a name="parameters"></a>매개 변수

*pDC*<br/>
【인】 단추를 표시 하는 장치 컨텍스트입니다.

*rect*<br/>
【인】 단추의 경계 사각형입니다.

*b 선택됨*<br/>
【인】 단추를 선택했는지 여부를 지정합니다. 이 매개 변수가 TRUE이면 단추가 선택됩니다. 이 매개 변수가 FALSE이면 단추가 선택되지 않습니다.

### <a name="return-value"></a>Return Value

지정된 장치 컨텍스트의 단추 너비(픽셀 단위)입니다.

### <a name="remarks"></a>설명

이 메서드는 단추를 소유자-그리기 목록 상자에 표시 하려고 할 때 사용자 지정 대화 상자 **(명령** 탭)에 의해 호출 됩니다.

이 메서드의 기본 구현에는 단추의 이미지 및 텍스트 레이블을 사용할 수 있는 경우 표시 됩니다. 단추의 텍스트 레이블을 사용할 수 없는 경우 메서드에 도구 설명 텍스트가 표시됩니다.

사용자 지정 도면을 수행하려면 이 메서드를 재정의합니다.

## <a name="cmfctoolbarbuttonongetcustomtooltiptext"></a><a name="ongetcustomtooltiptext"></a>CMFCToolBar 단추::온겟사용자 정의 도구 팁 텍스트

단추에 대 한 사용자 지정 도구 설명 텍스트를 검색 하는 프레임 워크에 의해 호출 됩니다.

```
virtual BOOL OnGetCustomToolTipText(CString& strToolTip);
```

### <a name="parameters"></a>매개 변수

*스트툴팁*<br/>
【아웃】 사용자 `CString` 지정 도구 설명 텍스트를 받는 개체입니다.

### <a name="return-value"></a>Return Value

이 메서드는 FALSE를 반환합니다.

### <a name="remarks"></a>설명

프레임워크는 도구 모음 단추에 대한 도구 설명이 표시될 때 이 메서드를 호출합니다. 이 메서드가 FALSE를 반환하는 경우 프레임워크는 기본 도구 설명입니다.

기본 구현은 아무 것도 수행하지 않으며 FALSE를 반환합니다. 이 메서드를 재정의하고 영하지 않은 값을 반환하여 도구 모음 단추에 대한 사용자 지정 도구 설명 텍스트를 제공합니다.

## <a name="cmfctoolbarbuttononglobalfontschanged"></a><a name="onglobalfontschanged"></a>CMFCToolBar 단추::온글로벌폰트 변경

전역 글꼴이 변경된 경우 프레임워크에서 호출합니다.

```
virtual void OnGlobalFontsChanged();
```

### <a name="remarks"></a>설명

이 메서드의 기본 구현은 아무 것도 수행하지 않습니다. 단추 텍스트를 표시하는 데 사용되는 글꼴을 업데이트하려면 이 메서드를 재정의합니다.

## <a name="cmfctoolbarbuttononmove"></a><a name="onmove"></a>CMFCToolBar 버튼::이동 중

상위 도구 모음이 이동할 때 프레임워크에서 호출됩니다.

```
virtual void OnMove();
```

### <a name="remarks"></a>설명

이 메서드의 기본 구현은 아무 것도 수행하지 않습니다. 이 메서드를 재정의하여 상위 도구 모음이 이동할 때 단추의 위치를 바이라고 합니다.

## <a name="cmfctoolbarbuttononshow"></a><a name="onshow"></a>CMFCTool바버튼::온쇼

단추를 표시 하거나 보이지 않는 될 때 프레임 워크에 의해 호출 됩니다.

```
virtual void OnShow(BOOL bShow);
```

### <a name="parameters"></a>매개 변수

*bShow*<br/>
【인】 단추를 볼 수 있는지 여부를 지정합니다. 이 매개 변수가 TRUE이면 단추가 표시됩니다. 그렇지 않으면 단추가 표시되지 않습니다.

### <a name="remarks"></a>설명

이 메서드의 기본 구현은 아무 것도 수행하지 않습니다. 이 메서드를 재정의하여 단추의 가시성을 업데이트합니다.

## <a name="cmfctoolbarbuttononsize"></a><a name="onsize"></a>CMFCTool바버튼:온사이즈

상위 도구 모음의 크기 또는 위치를 변경하고 이 변경으로 인해 단추크기가 변경될 때 프레임워크에서 호출됩니다.

```
virtual void OnSize(int iSize);
```

### <a name="parameters"></a>매개 변수

*아이 사이즈*<br/>
【인】 단추의 새 너비입니다.

### <a name="remarks"></a>설명

이 메서드의 기본 구현은 아무 것도 수행하지 않습니다. 상위 도구 모음의 크기 나 위치가 변경될 때 단추크기를 조정하려면 이 메서드를 재정의합니다.

## <a name="cmfctoolbarbuttonontoolhittest"></a><a name="ontoolhittest"></a>CMFCToolBar 버튼::온툴히트테스트

상위 도구 모음에서 점이 단추의 경계 사각형에 있는지 여부를 결정해야 하는 경우 프레임워크에서 호출합니다.

```
virtual BOOL OnToolHitTest(
    const CWnd* pWnd,
    TOOLINFO* pTI);
```

### <a name="parameters"></a>매개 변수

*pWnd*<br/>
【인】 단추의 상위 창입니다. NULL일 수 있습니다.

*Pti*<br/>
【인】 도구 `TOOLINFO` 설명 컨트롤의 도구에 대한 정보가 포함된 구조입니다.

### <a name="return-value"></a>Return Value

단추에서 `OnMenuButtonToolHitTest` 상위 프레임 창에 대한 포인터를 검색할 수 있는 경우의 결과입니다. 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

이 메서드는 부모 창을 유효한 프레임 개체로 변환할 수 있는 경우 다음 메서드 중 하나를 호출합니다.

- [CMDIFrameWndEx::OnMenuButtonToolHitTest](../../mfc/reference/cmdiframewndex-class.md#onmenubuttontoolhittest)

- [CFrameWndEx::온메뉴버튼툴히트테스트](../../mfc/reference/cframewndex-class.md#onmenubuttontoolhittest)

- [COleIPFrameWndEx::OnMenuButtonToolHitTest](../../mfc/reference/coleipframewndex-class.md#onmenubuttontoolhittest)

## <a name="cmfctoolbarbuttononupdatetooltip"></a><a name="onupdatetooltip"></a>CMFCToolBar 단추::업데이트 도구 팁

상위 도구 모음이 도구 설명 텍스트를 업데이트할 때 프레임워크에서 호출됩니다.

```
virtual BOOL OnUpdateToolTip(
    CWnd* pWndParent,
    int iButtonIndex,
    CToolTipCtrl& wndToolTip,
    CString& str);
```

### <a name="parameters"></a>매개 변수

*pWndParent*<br/>
【인】 상위 창입니다.

*아이 버튼 인덱스*<br/>
【인】 상위 단추 컬렉션에서 단추의 0 기반 인덱스입니다.

*wndToolTip*<br/>
【인】 도구 설명 텍스트를 표시하는 컨트롤입니다.

*Str*<br/>
【아웃】 업데이트된 도구 설명 텍스트를 받는 `CString` 개체입니다.

### <a name="return-value"></a>Return Value

이 메서드는 FALSE를 반환합니다.

### <a name="remarks"></a>설명

이 메서드의 기본 구현은 아무 것도 수행하지 않으며 FALSE를 반환합니다. 도구 설명 텍스트 문자열을 제공하는 경우 이 메서드를 재정의하여 zero가 아닌 값을 반환합니다.

## <a name="cmfctoolbarbuttonpreparedrag"></a><a name="preparedrag"></a>CMFCToolBar버튼::P레파레드래그

단추를 드래그 앤 드롭 작업을 수행 하려고 하는 경우 프레임 워크에 의해 호출 됩니다.

```
virtual BOOL PrepareDrag(COleDataSource& srcItem);
```

### <a name="parameters"></a>매개 변수

*src항목*<br/>
【인】 끌어서 놓기 작업에 대한 상태 정보를 저장하는 `COleDataSource` 개체입니다.

### <a name="return-value"></a>Return Value

작업이 성공하면 TRUE입니다. 그렇지 않으면 거짓.

### <a name="remarks"></a>설명

프레임워크는 이 메서드를 호출하여 제공된 `COleDataSource` 개체에 해당 상태를 저장할 도구 모음 단추를 준비합니다. 이 메서드는 자신을 공유 파일에 직렬화 한 다음 [COleDataSource::CacheGlobalData](../../mfc/reference/coledatasource-class.md#cacheglobaldata) 메서드에 해당 파일을 전달 하 여 해당 상태를 저장 합니다. 도구 모음 단추 직렬화에 대한 자세한 내용은 [CMFCToolBarButton::Serialize](#serialize)를 참조하십시오.

이 메서드는 아무 것도 하지 않으며 단추를 저장할 수 없는 경우 [TRUE를 반환합니다(CMFCToolBarButton:CanBeStored](#canbestored) 메서드는 FALSE를 반환합니다). 개체 직렬화 중에 예외가 발생하는 경우 FALSE를 반환합니다.

OLE 끌어서 놓기 작업에 대한 자세한 내용은 [OLE 드래그 앤 드롭](../../mfc/drag-and-drop-ole.md)을 참조하십시오.

## <a name="cmfctoolbarbuttonrect"></a><a name="rect"></a>CMFCToolBar 버튼::정사각형

단추의 경계 사각형을 검색합니다.

```
const CRect& Rect() const;
```

### <a name="return-value"></a>Return Value

단추의 경계 사각형을 포함하는 `CRect` 개체입니다.

## <a name="cmfctoolbarbuttonresetimagetodefault"></a><a name="resetimagetodefault"></a>CMFCToolBar 단추::재설정이미지토디폴

단추와 연결된 이미지를 기본값으로 설정합니다.

```
virtual void ResetImageToDefault();
```

### <a name="remarks"></a>설명

이 메서드는 [CMFCToolBar::GetDefaultImage](../../mfc/reference/cmfctoolbar-class.md#getdefaultimage) 메서드를 사용 하 여 상위 도구 모음에서 기본 이미지를 검색합니다. 단추에 연결된 기본 이미지가 없는 경우 이 메서드는 [CStringT::LoadString](../../atl-mfc-shared/reference/cstringt-class.md#loadstring) 메서드를 사용하여 문자열 리소스에 따라 단추의 텍스트 레이블을 설정합니다. 문자열 리소스에 대한 자세한 내용은 [리소스 파일 작업을](../../windows/working-with-resource-files.md)참조하십시오.

이 메서드는 단추에 사용자 정의 된 이미지가 있는 경우 아무 작업을 수행 하지 않습니다.

## <a name="cmfctoolbarbuttonsavebarstate"></a><a name="savebarstate"></a>CMFCToolBar 단추::저장 바스테이트

도구 모음 단추의 상태를 저장합니다.

```
virtual void SaveBarState();
```

### <a name="remarks"></a>설명

프레임워크는 끌어서 놓기 `CMFCToolBarButton` 작업의 결과로 개체를 만들 때 이 메서드를 호출합니다.

이 메서드의 기본 구현은 아무 것도 수행하지 않습니다. 도구 모음 단추의 상태를 외부 데이터 원본에 저장하려면 이 메서드를 재정의합니다.

## <a name="cmfctoolbarbuttonserialize"></a><a name="serialize"></a>CMFCToolBar 단추::직렬화

이 개체를 아카이브에서 읽거나 아카이브에 씁니다.

```
virtual void Serialize(CArchive& ar);
```

### <a name="parameters"></a>매개 변수

*ar*<br/>
【인】 직렬화할 `CArchive` 개체입니다.

### <a name="remarks"></a>설명

이 메서드는 클립보드 또는 끌어서 놓기 작업과 같은 데이터 전송 프로세스를 지원합니다. 제공된 개체에서 또는 제공된 `CArchive` 개체에 대한 ID, 텍스트 레이블 및 이미지 ID와 같은 단추의 속성을 읽거나 씁니다.

직렬화 예제는 [직렬화: 개체 직렬화를](../../mfc/serialization-serializing-an-object.md)참조하십시오.

## <a name="cmfctoolbarbuttonsetaccdata"></a><a name="setaccdata"></a>CMFCToolBar 버튼::세액데이터

제공된 `CAccessibilityData` 개체에 도구 모음 단추의 내게 필요한 옵션 데이터를 채웁니다.

```
virtual BOOL SetACCData(
    CWnd* pParent,
    CAccessibilityData& data);
```

### <a name="parameters"></a>매개 변수

*pParent*<br/>
【인】 도구 모음 단추의 상위 창입니다.

*데이터*<br/>
【인】 도구 `CAccessibilityData` 모음 단추의 내게 필요한 옵션 데이터로 채워진 개체입니다.

### <a name="return-value"></a>Return Value

이 메서드는 TRUE를 반환합니다.

### <a name="remarks"></a>설명

도구 모음 단추에서 내게 필요한 옵션 데이터를 제공하지 않는 경우 FALSE를 반환하려면 이 메서드를 재정의합니다.

## <a name="cmfctoolbarbuttonsetclipboardformatname"></a><a name="setclipboardformatname"></a>CMFCToolBar 단추::세트 클립보드 형식 이름

전역 클립보드 형식의 이름을 바꿉니다.

```
static void __stdcall SetClipboardFormatName(LPCTSTR lpszName);
```

### <a name="parameters"></a>매개 변수

*lpszName*<br/>
【인】 전역 클립보드 형식의 새 이름입니다. NULL이 될 수 없습니다.

### <a name="remarks"></a>설명

이 방법을 사용하면 여러 응용 프로그램에서 끌어서 놓기 작업이 발생할 수 있습니다. 각 응용 프로그램은 동일한 클립보드 형식 이름을 제공해야 합니다.

프레임 [워크가 CMFCToolBarButton::GetClipboardFormat을](#getclipboardformat)호출하기 전에이 메서드를 호출해야합니다.

## <a name="cmfctoolbarbuttonsetimage"></a><a name="setimage"></a>CMFCToolBar 단추::세트 이미지

단추의 이미지 인덱스를 설정합니다.

```
virtual void SetImage(int iImage);
```

### <a name="parameters"></a>매개 변수

*아이 이미지*<br/>
【인】 도구 모음 이미지 컬렉션의 이미지 인덱스입니다.

### <a name="remarks"></a>설명

도구 모음 단추가 구분 기호인 경우 *iImage는* 구분 기호 단추의 새 너비를 나타냅니다.

*iImage가* 0보다 적으면 이미지 그리기를 비활성화하고 단추의 텍스트 레이블을 그릴 수 있습니다.

## <a name="cmfctoolbarbuttonsetprotectedcommands"></a><a name="setprotectedcommands"></a>CMFCToolBar 단추::보호 명령 설정

사용자가 사용자 지정할 수 없는 명령 목록을 설정합니다.

```
static void SetProtectedCommands(const CList<UINT,UINT>& lstCmds);
```

### <a name="parameters"></a>매개 변수

*lstCmds*<br/>
【인】 보호된 명령 목록입니다.

### <a name="remarks"></a>설명

사용자 지정 모드에서 프레임워크는 보호되는 도구 모음 단추 명령을 비활성화합니다. 사용자는 비활성화된 도구 모음 단추에서 끌어서 놓기 및 편집 작업을 수행할 수 없습니다.

[CMFCToolBarButton::GetProtectedCommand메서드를](#getprotectedcommands) 사용하여 보호된 명령 목록을 검색합니다.

## <a name="cmfctoolbarbuttonsetradio"></a><a name="setradio"></a>CMFCTool바버튼::셋라디오

단추의 선택된 상태가 변경될 때 프레임워크에서 호출됩니다.

```
virtual void SetRadio();
```

### <a name="remarks"></a>설명

이 메서드의 기본 구현은 아무 것도 수행하지 않습니다. 이 메서드를 재정의하여 단추에서 확인된 상태를 변경할 때 사용자 지정 작업을 수행합니다.

## <a name="cmfctoolbarbuttonsetrect"></a><a name="setrect"></a>CMFCToolBar 단추::세트렉트

단추의 경계 사각형을 설정합니다.

```cpp
void SetRect(const CRect rect);
```

### <a name="parameters"></a>매개 변수

*rect*<br/>
【인】 단추의 새 경계 사각형입니다.

### <a name="remarks"></a>설명

이 메서드는 새 경계 사각형을 설정 한 후 [CMFCToolBarButton::OnMove](#onmove) 메서드를 호출 합니다.

## <a name="cmfctoolbarbuttonsetstyle"></a><a name="setstyle"></a>CMFCToolBar 버튼::세트스타일

단추의 스타일을 설정합니다.

```
virtual void SetStyle(UINT nStyle);
```

### <a name="parameters"></a>매개 변수

*nStyle*<br/>
【인】 버튼의 새로운 스타일입니다.

### <a name="remarks"></a>설명

기본 구현은 [CMFCToolBarButton::m_nStyle](#m_nstyle) 데이터 멤버를 *nStyle으로*설정합니다. 스타일 변경을 처리하기 위해 추가 처리를 수행하려는 경우 이 메서드를 재정의합니다. 유효한 스타일 플래그 목록은 [도구 모음 컨트롤 스타일을](toolbar-control-styles.md) 참조하십시오.

## <a name="cmfctoolbarbuttonsetvisible"></a><a name="setvisible"></a>CMFCToolBar 단추::가시설정

단추를 볼 수 있는지 여부를 지정합니다.

```cpp
void SetVisible(BOOL bShow=TRUE);
```

### <a name="parameters"></a>매개 변수

*bShow*<br/>
【인】 단추를 표시하거나 숨길지 여부를 지정하는 부울 값입니다. 이 매개 변수가 TRUE이면 단추가 표시됩니다. 매개 변수가 FALSE이면 단추가 숨김입니다.

### <a name="remarks"></a>설명

이 함수를 사용하여 특정 도구 모음 단추를 숨기거나 표시합니다. [CPane::adjustSize이 메서드를](../../mfc/reference/cpane-class.md#adjustsizeimmediate) 호출 한 후 즉시 메서드를 호출 합니다.

## <a name="cmfctoolbarbuttonshow"></a><a name="show"></a>CMFCToolBar 버튼::표시

단추를 표시하거나 숨깁니다.

```cpp
void Show(BOOL bShow);
```

### <a name="parameters"></a>매개 변수

*bShow*<br/>
【인】 단추를 표시하거나 숨길지 여부를 지정하는 부울 값입니다. 이 매개 변수가 TRUE이면 단추가 표시됩니다. 매개 변수가 FALSE이면 단추가 숨김입니다.

### <a name="remarks"></a>설명

프레임워크는 이 메서드를 호출하여 상위 도구 모음의 크기를 조정할 때 도구 모음 단추의 가시성을 업데이트합니다. 단추를 더 이상 도구 모음의 경계 내에 맞지 않는 경우 프레임워크는 *bShow를* FALSE로 설정하여 이 메서드를 호출합니다. 단추크기를 조정한 후 도구 모음의 경계 에 다시 맞을 때 프레임워크는 *bShow를* TRUE로 설정하여 이 메서드를 호출합니다.

[CMFCToolBarButton::SetVisible](#setvisible) 메서드를 사용하여 단추의 일반적인 가시성을 설정합니다.

이 메서드는 단추의 가시성 상태를 업데이트한 후 [CMFCToolBarButton::OnShow](#onshow) 메서드를 호출합니다.

## <a name="see-also"></a>참조

[계층 구조 차트](../../mfc/hierarchy-chart.md)<br/>
[클래스](../../mfc/reference/mfc-classes.md)<br/>
[CMFC툴바 클래스](../../mfc/reference/cmfctoolbar-class.md)<br/>
[CMFC툴바이미지 클래스](../../mfc/reference/cmfctoolbarimages-class.md)
