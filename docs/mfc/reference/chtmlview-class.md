---
title: CHtmlView 클래스
ms.date: 11/04/2016
f1_keywords:
- CHtmlView
- AFXHTML/CHtmlView
- AFXHTML/CHtmlView::Create
- AFXHTML/CHtmlView::CreateControlSite
- AFXHTML/CHtmlView::ExecFormsCommand
- AFXHTML/CHtmlView::ExecWB
- AFXHTML/CHtmlView::GetAddressBar
- AFXHTML/CHtmlView::GetApplication
- AFXHTML/CHtmlView::GetBusy
- AFXHTML/CHtmlView::GetContainer
- AFXHTML/CHtmlView::GetFullName
- AFXHTML/CHtmlView::GetFullScreen
- AFXHTML/CHtmlView::GetHeight
- AFXHTML/CHtmlView::GetHtmlDocument
- AFXHTML/CHtmlView::GetLeft
- AFXHTML/CHtmlView::GetLocationName
- AFXHTML/CHtmlView::GetLocationURL
- AFXHTML/CHtmlView::GetMenuBar
- AFXHTML/CHtmlView::GetOffline
- AFXHTML/CHtmlView::GetParentBrowser
- AFXHTML/CHtmlView::GetProperty
- AFXHTML/CHtmlView::GetReadyState
- AFXHTML/CHtmlView::GetRegisterAsBrowser
- AFXHTML/CHtmlView::GetRegisterAsDropTarget
- AFXHTML/CHtmlView::GetSilent
- AFXHTML/CHtmlView::GetSource
- AFXHTML/CHtmlView::GetStatusBar
- AFXHTML/CHtmlView::GetTheaterMode
- AFXHTML/CHtmlView::GetToolBar
- AFXHTML/CHtmlView::GetTop
- AFXHTML/CHtmlView::GetTopLevelContainer
- AFXHTML/CHtmlView::GetType
- AFXHTML/CHtmlView::GetVisible
- AFXHTML/CHtmlView::GetWidth
- AFXHTML/CHtmlView::GoBack
- AFXHTML/CHtmlView::GoForward
- AFXHTML/CHtmlView::GoHome
- AFXHTML/CHtmlView::GoSearch
- AFXHTML/CHtmlView::LoadFromResource
- AFXHTML/CHtmlView::Navigate
- AFXHTML/CHtmlView::Navigate2
- AFXHTML/CHtmlView::OnBeforeNavigate2
- AFXHTML/CHtmlView::OnCommandStateChange
- AFXHTML/CHtmlView::OnDocumentComplete
- AFXHTML/CHtmlView::OnDocWindowActivate
- AFXHTML/CHtmlView::OnDownloadBegin
- AFXHTML/CHtmlView::OnDownloadComplete
- AFXHTML/CHtmlView::OnEnableModeless
- AFXHTML/CHtmlView::OnFilterDataObject
- AFXHTML/CHtmlView::OnFrameWindowActivate
- AFXHTML/CHtmlView::OnFullScreen
- AFXHTML/CHtmlView::OnGetDropTarget
- AFXHTML/CHtmlView::OnGetExternal
- AFXHTML/CHtmlView::OnGetHostInfo
- AFXHTML/CHtmlView::OnGetOptionKeyPath
- AFXHTML/CHtmlView::OnHideUI
- AFXHTML/CHtmlView::OnMenuBar
- AFXHTML/CHtmlView::OnNavigateComplete2
- AFXHTML/CHtmlView::OnNavigateError
- AFXHTML/CHtmlView::OnNewWindow2
- AFXHTML/CHtmlView::OnProgressChange
- AFXHTML/CHtmlView::OnPropertyChange
- AFXHTML/CHtmlView::OnQuit
- AFXHTML/CHtmlView::OnResizeBorder
- AFXHTML/CHtmlView::OnShowContextMenu
- AFXHTML/CHtmlView::OnShowUI
- AFXHTML/CHtmlView::OnStatusBar
- AFXHTML/CHtmlView::OnStatusTextChange
- AFXHTML/CHtmlView::OnTheaterMode
- AFXHTML/CHtmlView::OnTitleChange
- AFXHTML/CHtmlView::OnToolBar
- AFXHTML/CHtmlView::OnTranslateAccelerator
- AFXHTML/CHtmlView::OnTranslateUrl
- AFXHTML/CHtmlView::OnUpdateUI
- AFXHTML/CHtmlView::OnVisible
- AFXHTML/CHtmlView::PutProperty
- AFXHTML/CHtmlView::QueryFormsCommand
- AFXHTML/CHtmlView::QueryStatusWB
- AFXHTML/CHtmlView::Refresh
- AFXHTML/CHtmlView::Refresh2
- AFXHTML/CHtmlView::SetAddressBar
- AFXHTML/CHtmlView::SetFullScreen
- AFXHTML/CHtmlView::SetHeight
- AFXHTML/CHtmlView::SetLeft
- AFXHTML/CHtmlView::SetMenuBar
- AFXHTML/CHtmlView::SetOffline
- AFXHTML/CHtmlView::SetRegisterAsBrowser
- AFXHTML/CHtmlView::SetRegisterAsDropTarget
- AFXHTML/CHtmlView::SetSilent
- AFXHTML/CHtmlView::SetStatusBar
- AFXHTML/CHtmlView::SetTheaterMode
- AFXHTML/CHtmlView::SetToolBar
- AFXHTML/CHtmlView::SetTop
- AFXHTML/CHtmlView::SetVisible
- AFXHTML/CHtmlView::SetWidth
- AFXHTML/CHtmlView::Stop
helpviewer_keywords:
- CHtmlView [MFC], Create
- CHtmlView [MFC], CreateControlSite
- CHtmlView [MFC], ExecFormsCommand
- CHtmlView [MFC], ExecWB
- CHtmlView [MFC], GetAddressBar
- CHtmlView [MFC], GetApplication
- CHtmlView [MFC], GetBusy
- CHtmlView [MFC], GetContainer
- CHtmlView [MFC], GetFullName
- CHtmlView [MFC], GetFullScreen
- CHtmlView [MFC], GetHeight
- CHtmlView [MFC], GetHtmlDocument
- CHtmlView [MFC], GetLeft
- CHtmlView [MFC], GetLocationName
- CHtmlView [MFC], GetLocationURL
- CHtmlView [MFC], GetMenuBar
- CHtmlView [MFC], GetOffline
- CHtmlView [MFC], GetParentBrowser
- CHtmlView [MFC], GetProperty
- CHtmlView [MFC], GetReadyState
- CHtmlView [MFC], GetRegisterAsBrowser
- CHtmlView [MFC], GetRegisterAsDropTarget
- CHtmlView [MFC], GetSilent
- CHtmlView [MFC], GetSource
- CHtmlView [MFC], GetStatusBar
- CHtmlView [MFC], GetTheaterMode
- CHtmlView [MFC], GetToolBar
- CHtmlView [MFC], GetTop
- CHtmlView [MFC], GetTopLevelContainer
- CHtmlView [MFC], GetType
- CHtmlView [MFC], GetVisible
- CHtmlView [MFC], GetWidth
- CHtmlView [MFC], GoBack
- CHtmlView [MFC], GoForward
- CHtmlView [MFC], GoHome
- CHtmlView [MFC], GoSearch
- CHtmlView [MFC], LoadFromResource
- CHtmlView [MFC], Navigate
- CHtmlView [MFC], Navigate2
- CHtmlView [MFC], OnBeforeNavigate2
- CHtmlView [MFC], OnCommandStateChange
- CHtmlView [MFC], OnDocumentComplete
- CHtmlView [MFC], OnDocWindowActivate
- CHtmlView [MFC], OnDownloadBegin
- CHtmlView [MFC], OnDownloadComplete
- CHtmlView [MFC], OnEnableModeless
- CHtmlView [MFC], OnFilterDataObject
- CHtmlView [MFC], OnFrameWindowActivate
- CHtmlView [MFC], OnFullScreen
- CHtmlView [MFC], OnGetDropTarget
- CHtmlView [MFC], OnGetExternal
- CHtmlView [MFC], OnGetHostInfo
- CHtmlView [MFC], OnGetOptionKeyPath
- CHtmlView [MFC], OnHideUI
- CHtmlView [MFC], OnMenuBar
- CHtmlView [MFC], OnNavigateComplete2
- CHtmlView [MFC], OnNavigateError
- CHtmlView [MFC], OnNewWindow2
- CHtmlView [MFC], OnProgressChange
- CHtmlView [MFC], OnPropertyChange
- CHtmlView [MFC], OnQuit
- CHtmlView [MFC], OnResizeBorder
- CHtmlView [MFC], OnShowContextMenu
- CHtmlView [MFC], OnShowUI
- CHtmlView [MFC], OnStatusBar
- CHtmlView [MFC], OnStatusTextChange
- CHtmlView [MFC], OnTheaterMode
- CHtmlView [MFC], OnTitleChange
- CHtmlView [MFC], OnToolBar
- CHtmlView [MFC], OnTranslateAccelerator
- CHtmlView [MFC], OnTranslateUrl
- CHtmlView [MFC], OnUpdateUI
- CHtmlView [MFC], OnVisible
- CHtmlView [MFC], PutProperty
- CHtmlView [MFC], QueryFormsCommand
- CHtmlView [MFC], QueryStatusWB
- CHtmlView [MFC], Refresh
- CHtmlView [MFC], Refresh2
- CHtmlView [MFC], SetAddressBar
- CHtmlView [MFC], SetFullScreen
- CHtmlView [MFC], SetHeight
- CHtmlView [MFC], SetLeft
- CHtmlView [MFC], SetMenuBar
- CHtmlView [MFC], SetOffline
- CHtmlView [MFC], SetRegisterAsBrowser
- CHtmlView [MFC], SetRegisterAsDropTarget
- CHtmlView [MFC], SetSilent
- CHtmlView [MFC], SetStatusBar
- CHtmlView [MFC], SetTheaterMode
- CHtmlView [MFC], SetToolBar
- CHtmlView [MFC], SetTop
- CHtmlView [MFC], SetVisible
- CHtmlView [MFC], SetWidth
- CHtmlView [MFC], Stop
ms.assetid: 904976af-73de-4aba-84ac-cfae8e2be09a
ms.openlocfilehash: 9956537dbbbc34c69f6f6a6da8174ab594418386
ms.sourcegitcommit: 7a6116e48c3c11b97371b8ae4ecc23adce1f092d
ms.translationtype: MT
ms.contentlocale: ko-KR
ms.lasthandoff: 04/22/2020
ms.locfileid: "81752665"
---
# <a name="chtmlview-class"></a>CHtmlView 클래스

MFC의 문서/뷰 아키텍처 컨텍스트 내에서 WebBrowser 컨트롤의 기능을 제공합니다.

## <a name="syntax"></a>구문

```
class CHtmlView : public CFormView
```

## <a name="members"></a>멤버

### <a name="public-methods"></a>Public 메서드

|속성|Description|
|----------|-----------------|
|[CHtmlView::만들기](#create)|WebBrowser 컨트롤을 만듭니다.|
|[CHtmlView::만들기제어사이트](#createcontrolsite)|폼에서 컨트롤을 호스트할 컨트롤 사이트 인스턴스를 만드는 데 사용되는 Overridable입니다.|
|[CHtmlView::엑세폼스커맨드](#execformscommand)|`IOleCommandTarget::Exec` 메서드를 사용하여 지정된 명령을 실행합니다.|
|[CHtmlView::ExecWB](#execwb)|명령을 실행합니다.|
|[CHtmlView::겟주소바](#getaddressbar)|Internet Explorer 개체의 주소 표시줄을 표시할지 여부를 결정합니다. WebBrowser 컨트롤은 무시됩니다. Internet Explorer에만 해당합니다.|
|[CHtmlView::Get응용 프로그램](#getapplication)|Internet Explorer 애플리케이션의 현재 인스턴스를 포함하는 애플리케이션을 나타내는 애플리케이션 개체를 검색합니다.|
|[CHtmlView::GetBusy](#getbusy)|다운로드 또는 기타 작업이 계속 진행 중인지 여부를 나타내는 값을 검색합니다.|
|[CHtmlView::GetContainer](#getcontainer)|WebBrowser 컨트롤의 컨테이너를 검색합니다.|
|[CHtmlView::GetFullName](#getfullname)|웹 브라우저에 표시되는 리소스의 전체 이름(경로 포함)을 검색합니다. WebBrowser 컨트롤은 무시됩니다. Internet Explorer에만 해당합니다.|
|[CHtmlView::GetFullScreen](#getfullscreen)|WebBrowser 컨트롤이 전체 화면 모드 또는 표준 창 모드로 작동 중인지를 나타냅니다.|
|[CHtmlView::GetHeight](#getheight)|Internet Explorer 주 창의 높이를 검색합니다.|
|[CHtmlView::GetHtmlDocument](#gethtmldocument)|활성 HTML 문서를 검색합니다.|
|[CHtmlView::GetLeft](#getleft)|Internet Explorer 주 창에서 왼쪽 가장자리의 화면 좌표를 검색합니다.|
|[CHtmlView::GetLocationName](#getlocationname)|현재 WebBrowser에 표시되는 리소스의 이름을 검색합니다.|
|[CHtmlView::GetLocationURL](#getlocationurl)|현재 WebBrowser에 표시되는 리소스의 URL을 검색합니다.|
|[CHtmlView::GetMenuBar](#getmenubar)|메뉴 모음을 표시할지 여부를 결정하는 값을 검색합니다.|
|[CHtmlView::오프라인](#getoffline)|컨트롤이 오프라인 상태인지 여부를 결정하는 값을 검색합니다.|
|[CHtmlView::GetParent브라우저](#getparentbrowser)|`IDispatch` 인터페이스에 대한 포인터를 검색합니다. 자세한 내용은 [Implementing the IDispatch Interface](/previous-versions/windows/desktop/automat/implementing-the-idispatch-interface)을 참조하십시오.|
|[CHtmlView::GetProperty](#getproperty)|지정된 개체와 연결된 속성의 현재 값을 검색합니다.|
|[CHtmlView::겟레디스테이트](#getreadystate)|웹 브라우저 개체의 준비 상태를 검색합니다.|
|[CHtmlView::GetRegisterAsBrowser](#getregisterasbrowser)|WebBrowser 컨트롤이 대상 이름 확인을 위한 최상위 브라우저로 등록되었는지 여부를 나타냅니다.|
|[CHtmlView::GetRegisterAsDropTarget](#getregisterasdroptarget)|WebBrowser 컨트롤이 탐색을 위한 놓기 대상으로 등록되었는지 여부를 나타냅니다.|
|[CHtmlView::GetSilent](#getsilent)|대화 상자를 표시할 수 있는지 여부를 나타냅니다.|
|[CHtmlView::GetSource](#getsource)|웹 페이지의 HTML 소스 코드입니다.|
|[CHtmlView::겟스테이터스바](#getstatusbar)|Internet Explorer의 상태 표시줄을 표시할지 여부를 나타냅니다. WebBrowser 컨트롤은 무시됩니다. Internet Explorer에만 해당합니다.|
|[CHtmlView::겟시어터모드](#gettheatermode)|WebBrowser 컨트롤이 극장 모드인지 여부를 나타냅니다.|
|[CHtmlView::GetToolBar](#gettoolbar)|도구 모음을 표시할지 여부를 결정하는 값을 검색합니다.|
|[CHtmlView::GetTop](#gettop)|Internet Explorer 주 창에서 위쪽 가장자리의 화면 좌표를 검색합니다.|
|[CHtmlView::GetTopLevel컨테이너](#gettoplevelcontainer)|현재 개체가 WebBrowser 컨트롤의 최상위 컨테이너인지 여부를 나타내는 값을 검색합니다.|
|[CHtmlView::GetType](#gettype)|문서 개체의 형식 이름을 검색합니다.|
|[CHtmlView::눈에 보이는](#getvisible)|개체를 표시할지 또는 숨길지를 나타내는 값을 검색합니다.|
|[CHtmlView::GetWidth](#getwidth)|Internet Explorer 주 창의 너비를 검색합니다.|
|[CHtmlView::GoBack](#goback)|기록 목록에서 이전 항목으로 이동합니다.|
|[CHtmlView::전진](#goforward)|기록 목록에서 다음 항목으로 이동합니다.|
|[CHtmlView::GoHome](#gohome)|현재 홈페이지 또는 시작 페이지로 이동합니다.|
|[CHtmlView::고서치](#gosearch)|현재 검색 페이지로 이동합니다.|
|[CHtmlView::로드From리소스](#loadfromresource)|WebBrowser 컨트롤에 리소스를 로드합니다.|
|[CHtmlView::탐색](#navigate)|URL로 식별된 리소스로 이동합니다.|
|[CHtmlView::탐색2](#navigate2)|URL로 식별된 리소스 또는 전체 경로로 식별된 파일로 이동합니다.|
|[CHtmlView::에 이전 탐색2](#onbeforenavigate2)|창 또는 프레임셋 요소에 지정된 WebBrowser에서 이동하기 전에 호출됩니다.|
|[CHtmlView::온커맨드스테이트체인지](#oncommandstatechange)|웹 브라우저 명령의 사용 상태가 변경되었음을 애플리케이션에 알리기 위해 호출됩니다.|
|[CHtmlView::문서 완료](#ondocumentcomplete)|문서가 READYSTATE_COMPLETE 상태에 도달했다는 것을 응용 프로그램에 알리기 위해 호출됩니다.|
|[CHtmlView::온닥윈도우활성화](#ondocwindowactivate)|[IOleInPlaceActiveObject::OnDocWindowActivate](/windows/win32/api/oleidl/nf-oleidl-ioleinplaceactiveobject-ondocwindowactivate)의 Internet Explorer 또는 MSHTML 구현에서 호출되며, 컨테이너의 문서 창이 활성화 또는 비활성화될 경우 활성 현재 위치 개체에 알립니다.|
|[CHtmlView::다운로드 시작](#ondownloadbegin)|이동 작업이 시작되었음을 애플리케이션에 알리기 위해 호출됩니다.|
|[CHtmlView::에 다운로드완료](#ondownloadcomplete)|이동 작업이 중단 또는 실패한 상태로 마치면 호출됩니다.|
|[CHtmlView::에인블모드리스](#onenablemodeless)|컨테이너가 모달 대화 상자를 만들거나 삭제할 때 모덜리스 대화 상자를 사용하거나 사용하지 않도록 설정하기 위해 호출됩니다.|
|[CHtmlView::온필터데이터오브젝트](#onfilterdataobject)|호스트가 Internet Explorer 또는 MSHTML의 데이터 개체를 바꿀 수 있도록 하기 위해 Internet Explorer 또는 MSHTML에 의해 호스트에서 호출됩니다.|
|[CHtmlView::온프레임 윈도우활성화](#onframewindowactivate)|컨테이너의 최상위 프레임 창이 활성화 또는 비활성화될 경우 개체에 알리기 위해 [IOleInPlaceActiveObject::OnFrameWindowActivate](/windows/win32/api/oleidl/nf-oleidl-ioleinplaceactiveobject-onframewindowactivate) 에서 호출됩니다.|
|[CHtmlView::온풀스크린](#onfullscreen)|FullScreen 속성이 변경되면 호출됩니다.|
|[CHtmlView::온겟드롭타겟](#ongetdroptarget)|호스트가 대체 [IDropTarget](/windows/win32/api/oleidl/nn-oleidl-idroptarget)을 제공할 수 있도록 놓기 대상으로 사용되는 경우 Internet Explorer 또는 MSHTML에 의해 호출됩니다.|
|[CHtmlView::온겟외부](#ongetexternal)|호스트의 `IDispatch` 인터페이스를 가져오기 위해 Internet Explorer 또는 MSHTML에 의해 호출됩니다.|
|[CHtmlView::온겟호스트정보](#ongethostinfo)|Internet Explorer 또는 MSHTML 호스트의 UI 기능을 검색합니다.|
|[CHtmlView::온겟옵션키패스](#ongetoptionkeypath)|Internet Explorer 또는 MSHTML이 사용자 기본 설정을 저장하는 레지스트리 키를 반환합니다.|
|[CHtmlView::온히데이](#onhideui)|Internet Explorer 또는 MSHTML이 해당 메뉴 및 도구 모음을 제거하면 호출됩니다.|
|[CHtmlView::온메뉴바](#onmenubar)|MenuBar 속성이 변경되면 호출됩니다.|
|[CHtmlView::OnNavigateComplete2](#onnavigatecomplete2)|창 또는 프레임셋 요소의 하이퍼링크로 이동한 후에 호출됩니다.|
|[CHtmlView::에 대한 오류](#onnavigateerror)|하이퍼링크로 이동이 실패할 경우 프레임워크에 의해 호출됩니다.|
|[CHtmlView::온뉴윈도우2](#onnewwindow2)|리소스를 표시하기 위해 새 창이 만들어지면 호출됩니다.|
|[CHtmlView::진행 중 변경](#onprogresschange)|다운로드 작업 진행률이 업데이트되었음을 애플리케이션에 알리기 위해 호출됩니다.|
|[CHtmlView::온프로퍼체인지](#onpropertychange)|[PutProperty](#putproperty) 메서드가 속성 값을 변경했음을 애플리케이션에 알리기 위해 호출됩니다.|
|[CHtmlView::에퀴드](#onquit)|Internet Explorer 애플리케이션을 끝낼 준비가 되었음을 애플리케이션에 알리기 위해 호출됩니다. (Internet Explorer에만 적용됨)|
|[CHtmlView::온리사이즈보더](#onresizeborder)|[IOleInPlaceActiveObject::ResizeBorder](/windows/win32/api/oleidl/nf-oleidl-ioleinplaceactiveobject-resizeborder)의 Internet Explorer 또는 MSHTML 구현에서 호출되며, 해당 테두리 공간 크기를 조정해야 함을 개체에 경고합니다.|
|[CHtmlView::온쇼컨텍스트메뉴](#onshowcontextmenu)|상황에 맞는 메뉴를 표시하려는 경우 Internet Explorer 또는 MSHTML에 의해 호출됩니다.|
|[CHtmlView::온쇼이](#onshowui)|Internet Explorer 또는 MSHTML이 해당 메뉴 및 도구 모음을 표시하기 전에 호출됩니다.|
|[CHtmlView::온스테이바](#onstatusbar)|StatusBar 속성이 변경되면 호출됩니다.|
|[CHtmlView::상태 텍스트 변경](#onstatustextchange)|애플리케이션에 WebBrowser 컨트롤과 연결된 상태 표시줄의 텍스트가 변경되었음을 알리기 위해 호출됩니다.|
|[CHtmlView::온시어터모드](#ontheatermode)|TheaterMode 속성이 변경되면 호출됩니다.|
|[CHtmlView::온타이틀 체인지](#ontitlechange)|WebBrowser 컨트롤의 문서 제목을 사용할 수 있게 되거나 변경할 경우 애플리케이션에 알리기 위해 호출됩니다.|
|[CHtmlView::온툴바](#ontoolbar)|ToolBar 속성이 변경되면 호출됩니다.|
|[CHtmlView::번역가속기](#ontranslateaccelerator)|컨테이너의 메시지 큐에서 메뉴 바로 가기 키 메시지를 처리하기 위해 [IOleInPlaceActiveObject::TranslateAccelerator](/windows/win32/api/oleidl/nf-oleidl-ioleinplaceactiveobject-translateaccelerator) 또는 [IOleControlSite::TranslateAccelerator](/windows/win32/api/ocidl/nf-ocidl-iolecontrolsite-translateaccelerator) 가 호출될 경우 Internet Explorer 또는 MSHTML에 의해 호출됩니다.|
|[CHtmlView::에 번역Url](#ontranslateurl)|호스트가 로드할 URL을 수정할 수 있도록 하기 위해 Internet Explorer 또는 MSHTML에 의해 호출됩니다.|
|[CHtmlView::OnUpdateUI](#onupdateui)|명령 상태가 변경되었음을 호스트에 알립니다.|
|[CHtmlView::눈에 보이는](#onvisible)|WebBrowser 컨트롤의 창을 표시하거나 숨기면 호출됩니다.|
|[CHtmlView::Putproperty](#putproperty)|지정된 개체와 연결된 속성의 값을 설정합니다.|
|[CHtmlView::쿼리폼명령](#queryformscommand)|사용자 인터페이스 이벤트에 의해 생성되는 하나 이상 명령의 상태를 쿼리합니다.|
|[CHtmlView::쿼리 상태WB](#querystatuswb)|WebBrowser 컨트롤에 의해 처리되는 명령의 상태를 쿼리합니다.|
|[CHtmlView::새로 고침](#refresh)|현재 파일을 다시 로드합니다.|
|[CHtmlView::새로 고침2](#refresh2)|현재 파일을 다시 로드하고 필요에 따라 `pragma:nocache` 헤더가 전송되지 않도록 합니다.|
|[CHtmlView::세트주소바](#setaddressbar)|Internet Explorer 개체의 주소 표시줄을 표시하거나 숨깁니다. WebBrowser 컨트롤은 무시됩니다. Internet Explorer에만 해당합니다.|
|[CHtmlView::설정전체 화면](#setfullscreen)|컨트롤이 전체 화면 모드 또는 표준 창 모드로 작동 중인지를 결정할 값을 설정합니다. WebBrowser 컨트롤은 무시됩니다. Internet Explorer에만 해당합니다.|
|[CHtmlView::설정높이](#setheight)|Internet Explorer 주 창의 높이를 설정합니다.|
|[CHtmlView::설정 왼쪽](#setleft)|Internet Explorer 주 창의 가로 위치를 설정합니다.|
|[CHtmlView::세트메뉴바](#setmenubar)|컨트롤의 메뉴 모음을 표시할지 여부를 결정할 값을 설정합니다. WebBrowser 컨트롤은 무시됩니다. Internet Explorer에만 해당합니다.|
|[CHtmlView::설정 오프라인](#setoffline)|컨트롤이 오프라인 상태인지 여부를 결정할 값을 설정합니다.|
|[CHtmlView::세트레지스터브라우저](#setregisterasbrowser)|WebBrowser 컨트롤이 대상 이름 확인을 위한 최상위 브라우저로 등록되었는지 여부를 나타내는 값을 설정합니다.|
|[CHtmlView::세트레지스터스드롭타겟](#setregisterasdroptarget)|WebBrowser 컨트롤이 탐색을 위한 놓기 대상으로 등록되었는지 여부를 나타내는 값을 설정합니다.|
|[CHtmlView::설정침묵](#setsilent)|컨트롤이 대화 상자를 표시할지 여부를 결정할 값을 설정합니다.|
|[CHtmlView::설정 상태 표시줄](#setstatusbar)|Internet Explorer의 상태 표시줄을 표시할지 여부를 결정할 값을 설정합니다. WebBrowser 컨트롤은 무시됩니다. Internet Explorer에만 해당합니다.|
|[CHtmlView::세트극장 모드](#settheatermode)|WebBrowser 컨트롤이 극장 모드인지 여부를 나타내는 값을 설정합니다.|
|[CHtmlView::SetToolBar](#settoolbar)|컨트롤의 도구 모음을 표시할지 여부를 결정할 값을 설정합니다. WebBrowser 컨트롤은 무시됩니다. Internet Explorer에만 해당합니다.|
|[CHtmlView::셋탑](#settop)|Internet Explorer 주 창의 세로 위치를 설정합니다.|
|[CHtmlView::가시설정](#setvisible)|개체를 표시할지 또는 숨길지를 나타내는 값을 설정합니다.|
|[CHtmlView::세트 폭](#setwidth)|Internet Explorer 주 창의 너비를 설정합니다.|
|[CHtmlView::중지](#stop)|파일 열기를 중지합니다.|

## <a name="remarks"></a>설명

WebBrowser 컨트롤은 사용자가 로컬 파일 시스템과 네트워크의 폴더뿐 아니라 World Wide Web에서 사이트를 찾을 수 있는 창입니다. WebBrowser 컨트롤은 하이퍼링크와 URL(Uniform Resource Locator) 탐색을 지원하고 기록 목록을 유지 관리합니다.

## <a name="using-the-chtmlview-class-in-an-mfc-application"></a>MFC 애플리케이션에서 CHtmlView 클래스 사용

표준 MFC 프레임워크 애플리케이션(SDI 또는 MDI 기반)에서 뷰 개체는 일반적으로 특수한 클래스 집합에서 파생됩니다. 이러한 클래스는 모두 `CView`에서 파생되며, `CView`에서 제공하는 것 이상의 특수 기능을 제공합니다.

애플리케이션의 뷰 클래스가 `CHtmlView` 를 기반으로 하는 경우 뷰에 WebBrowser 컨트롤이 제공됩니다. 이렇게 하면 실제로 애플리케이션이 웹 브라우저가 됩니다. 웹 브라우저 스타일 애플리케이션을 만드는 기본 방법은 MFC 애플리케이션 마법사를 사용하고 `CHtmlView` 를 뷰 클래스로 지정하는 것입니다. MFC 애플리케이션 내에서 WebBrowser 컨트롤을 구현 및 사용하는 방법에 대한 자세한 내용은 [웹 브라우저 스타일 애플리케이션 만들기](../../mfc/reference/creating-a-web-browser-style-mfc-application.md)를 참조하세요.

> [!NOTE]
> WebBrowser ActiveX 컨트롤(및 따라서 `CHtmlView`)은 Internet Explorer 4.0 이상이 설치된 Windows NT 버전 4.0 이상에서 실행되는 프로그램에서만 사용할 수 있습니다.

`CHtmlView` 는 웹(및/또는 HTML 문서)에 액세스하는 애플리케이션용으로 작성되었습니다. 다음 `CHtmlView` 멤버 함수는 Internet Explorer 애플리케이션에만 적용됩니다. 해당 함수는 WebBrowser 컨트롤에서 성공하지만 아무런 가시적 효과가 없습니다.

- [GetAddressBar](#getaddressbar)

- [GetFullName](#getfullname)

- [GetStatusBar](#getstatusbar)

- [SetAddressBar](#setaddressbar)

- [SetFullScreen](#setfullscreen)

- [SetMenuBar](#setmenubar)

- [SetStatusBar](#setstatusbar)

- [SetToolBar](#settoolbar)

## <a name="inheritance-hierarchy"></a>상속 계층 구조

[CObject](../../mfc/reference/cobject-class.md)

[CCmdTarget](../../mfc/reference/ccmdtarget-class.md)

[CWnd](../../mfc/reference/cwnd-class.md)

[CView](../../mfc/reference/cview-class.md)

[CScrollView](../../mfc/reference/cscrollview-class.md)

[CFormView](../../mfc/reference/cformview-class.md)

`CHtmlView`

## <a name="requirements"></a>요구 사항

**헤더:** afxhtml.h

## <a name="chtmlviewcreate"></a><a name="create"></a>CHtmlView::만들기

이 멤버 함수를 호출하여 Internet Explorer 실행 에 대한 WebBrowser 컨트롤 또는 컨테이너를 만듭니다.

```
virtual BOOL Create(
    LPCTSTR lpszClassName,
    LPCTSTR lpszWindowName,
    DWORD dwStyle,
    const RECT& rect,
    CWnd* pParentWnd,
    UINT nID,
    CCreateContext* pContext = NULL);
```

### <a name="parameters"></a>매개 변수

*lpszClassName*<br/>
Windows 클래스의 이름을 지정하는 null 종료된 문자 문자열을 가리킵니다. 클래스 이름은 [AfxRegisterWndClass](../../mfc/reference/application-information-and-management.md#afxregisterwndclass) 글로벌 함수 또는 `RegisterClass` Windows 함수에 등록된 모든 이름일 수 있습니다. NULL인 경우 미리 정의된 기본 [CFrameWnd](../../mfc/reference/cframewnd-class.md) 특성을 사용합니다.

*lpszWindowName*<br/>
창 이름을 나타내는 null 종료된 문자 문자열을 가리킵니다.

*dwStyle*<br/>
창 스타일 특성을 지정합니다. 기본적으로 WS_VISIBLE 및 WS_CHILD Windows 스타일이 설정됩니다.

*rect*<br/>
창의 크기와 위치를 지정하는 [RECT](/windows/win32/api/windef/ns-windef-rect) 구조에 대한 참조입니다. *rectDefault* 값을 사용하면 Windows에서 새 창의 크기와 위치를 지정할 수 있습니다.

*pParentWnd*<br/>
컨트롤의 상위 창에 대한 포인터입니다.

*nID*<br/>
뷰의 ID 번호입니다. 기본적으로 AFX_IDW_PANE_FIRST 설정합니다.

*pContext*<br/>
[CCreateContext에](../../mfc/reference/ccreatecontext-structure.md)대한 포인터 . 기본적으로 NULL입니다.

## <a name="chtmlviewcreatecontrolsite"></a><a name="createcontrolsite"></a>CHtmlView::만들기제어사이트

폼에서 컨트롤을 호스트할 컨트롤 사이트 인스턴스를 만드는 데 사용되는 Overridable입니다.

```
virtual BOOL CreateControlSite(
    COleControlContainer* pContainer,
    COleControlSite** ppSite,
    UINT nID,
    REFCLSID clsid);
```

### <a name="parameters"></a>매개 변수

*pContainer*<br/>
컨트롤을 포함하는 [COleControlContainer](../../mfc/reference/colecontrolcontainer-class.md) 개체에 대한 포인터입니다.

*ppSite*<br/>
[COleControlSite](../../mfc/reference/colecontrolsite-class.md) 개체에 대한 포인터에 대한 포인터로 컨트롤에 대한 사이트를 제공합니다.

*nID*<br/>
호스트할 컨트롤의 식별자입니다.

*clsid*<br/>
호스트할 컨트롤의 CLSID

### <a name="return-value"></a>Return Value

성공에 TRUE를 반환, 실패에 FALSE.

### <a name="remarks"></a>설명

이 멤버 함수를 재정의하여 사용자 고유의 제어 사이트 클래스의 인스턴스를 반환할 수 있습니다.

## <a name="chtmlviewexecformscommand"></a><a name="execformscommand"></a>CHtmlView::엑세폼스커맨드

`IOleCommandTarget::Exec` 메서드를 사용하여 지정된 명령을 실행합니다.

```
HRESULT ExecFormsCommand(
    DWORD dwCommandID,
    VARIANT* pVarIn,
    VARIANT* pVarOut);
```

### <a name="parameters"></a>매개 변수

*dwCommandID*<br/>
실행할 명령입니다. 이 명령은 CMDSETID3_Forms3 그룹에 속해야 합니다.

*pVarIn*<br/>
입력 인수를 `VARIANT` 포함하는 구조체에 대한 포인터입니다. NULL일 수 있습니다.

*pVarOut*<br/>
명령 출력을 수신하는 구조에 `VARIANT` 대한 포인터입니다. NULL일 수 있습니다.

### <a name="return-value"></a>Return Value

표준 HRESULT 값입니다. 가능한 값의 전체 목록은 [IOleCommandTarget::Windows](/windows/win32/api/docobj/nf-docobj-iolecommandtarget-exec) SDK의 Exec을 참조하십시오.

### <a name="remarks"></a>설명

`ExecFormsCommand`[는 IOleCommandTarget::Exec](/windows/win32/api/docobj/nf-docobj-iolecommandtarget-exec) 메서드의 동작을 구현합니다.

## <a name="chtmlviewexecwb"></a><a name="execwb"></a>CHtmlView::ExecWB

WebBrowser 또는 Internet Explorer에서 명령을 실행하려면 이 멤버 함수를 호출합니다.

```cpp
void ExecWB(
    OLECMDID cmdID,
    OLECMDEXECOPT cmdexecopt,
    VARIANT* pvaIn,
    VARIANT* pvaOut);
```

### <a name="parameters"></a>매개 변수

*Cmdid*<br/>
실행할 명령입니다.

*cmdexecopt*<br/>
명령을 실행하기 위해 설정된 옵션입니다.

*pvaIn*<br/>
명령 입력 인수를 지정하는 데 사용되는 변형입니다.

*pvaOut*<br/>
명령 출력 인수를 지정하는 데 사용되는 변형입니다.

### <a name="remarks"></a>설명

[IWebBrowser2::ExecWB](/previous-versions//aa752117\(v=vs.85\)) 윈도우 SDK를 참조하십시오.

## <a name="chtmlviewgetaddressbar"></a><a name="getaddressbar"></a>CHtmlView::겟주소바

이 멤버 함수를 호출하여 Internet Explorer의 주소 표시줄을 검색합니다.

```
BOOL GetAddressBar() const;
```

### <a name="return-value"></a>Return Value

주소 표시줄이 표시되는 경우 0이 아닙니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

인터넷 익스플로러에 적용됩니다. WebBrowser 컨트롤에서 이 호출을 사용하는 경우 오류가 반환되지 않지만 이 호출은 무시됩니다.

## <a name="chtmlviewgetapplication"></a><a name="getapplication"></a>CHtmlView::Get응용 프로그램

이 멤버 함수를 호출하여 WebBrowser 컨트롤이 포함된 응용 프로그램에서 지원하는 자동화 개체를 검색합니다.

```
LPDISPATCH GetApplication() const;
```

### <a name="return-value"></a>Return Value

활성 문서 `IDispatch` 개체의 인터페이스에 대한 포인터입니다. 자세한 내용은 [Implementing the IDispatch Interface](/previous-versions/windows/desktop/automat/implementing-the-idispatch-interface)을 참조하십시오.

### <a name="remarks"></a>설명

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="chtmlviewgetbusy"></a><a name="getbusy"></a>CHtmlView::GetBusy

이 멤버 함수를 호출하여 WebBrowser 컨트롤이 탐색 또는 다운로드 작업에 참여하고 있는지 확인합니다.

```
BOOL GetBusy() const;
```

### <a name="return-value"></a>Return Value

웹 브라우저가 사용 중인 경우 영하지 않습니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="chtmlviewgetcontainer"></a><a name="getcontainer"></a>CHtmlView::GetContainer

이 멤버 함수를 호출하여 웹 브라우저의 컨테이너로 평가하는 개체를 검색합니다.

```
LPDISPATCH GetContainer() const;
```

### <a name="return-value"></a>Return Value

활성 문서 `IDispatch` 개체의 인터페이스에 대한 포인터입니다.

### <a name="remarks"></a>설명

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="chtmlviewgetfullname"></a><a name="getfullname"></a>CHtmlView::GetFullName

이 멤버 함수를 호출하여 Internet Explorer가 현재 표시중인 파일의 전체 경로를 검색합니다.

```
CString GetFullName() const;
```

### <a name="return-value"></a>Return Value

현재 표시된 파일의 경로와 이름을 포함하는 [CString](../../atl-mfc-shared/reference/cstringt-class.md) 개체입니다. 경로 및 파일 이름이 `GetFullName` 없으면 빈 `CString`을 반환합니다.

### <a name="remarks"></a>설명

인터넷 익스플로러에 적용됩니다. WebBrowser 컨트롤에서 이 호출을 사용하는 경우 오류가 반환되지 않지만 이 호출은 무시됩니다.

## <a name="chtmlviewgetfullscreen"></a><a name="getfullscreen"></a>CHtmlView::GetFullScreen

이 멤버 함수를 호출하여 WebBrowser 컨트롤이 전체 화면 모드또는 일반 창 모드에서 작동하는지 확인합니다.

```
BOOL GetFullScreen() const;
```

### <a name="return-value"></a>Return Value

웹 브라우저가 전체 화면 모드에서 작동하는 경우 영하지 않습니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

전체 화면 모드에서는 Internet Explorer 기본 창이 최대화되고 상태 표시줄, 도구 모음, 메뉴 모음 및 제목 표시줄이 숨김이 있습니다.

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="chtmlviewgetheight"></a><a name="getheight"></a>CHtmlView::GetHeight

이 멤버 함수를 호출하여 WebBrowser 컨트롤의 프레임 창의 높이(픽셀 단위)를 검색합니다.

```
long GetHeight() const;
```

### <a name="return-value"></a>Return Value

컨트롤의 프레임 창 높이(픽셀)입니다.

## <a name="chtmlviewgethtmldocument"></a><a name="gethtmldocument"></a>CHtmlView::GetHtmlDocument

이 멤버 함수를 호출하여 활성 문서의 HTML 문서를 검색합니다.

```
LPDISPATCH GetHtmlDocument() const;
```

### <a name="return-value"></a>Return Value

활성 문서 `IDispatch` 개체의 인터페이스에 대한 포인터입니다.

### <a name="remarks"></a>설명

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="chtmlviewgetleft"></a><a name="getleft"></a>CHtmlView::GetLeft

이 멤버 함수를 호출하여 WebBrowser 컨트롤의 내부 왼쪽 가장자리와 컨테이너의 왼쪽 가장자리 사이의 거리를 검색합니다.

```
long GetLeft() const;
```

### <a name="return-value"></a>Return Value

왼쪽 가장자리 거리(픽셀 단위)입니다.

### <a name="remarks"></a>설명

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="chtmlviewgetlocationname"></a><a name="getlocationname"></a>CHtmlView::GetLocationName

이 멤버 함수를 호출하여 WebBrowser에 표시되는 리소스의 이름을 가져옵니다.

```
CString GetLocationName() const;
```

### <a name="return-value"></a>Return Value

현재 WebBrowser에 표시되는 리소스의 이름을 포함하는 [CString](../../atl-mfc-shared/reference/cstringt-class.md) 개체입니다.

### <a name="remarks"></a>설명

리소스가 월드 와이드 웹의 HTML 페이지인 경우 이름은 해당 페이지의 제목입니다. 리소스가 네트워크 또는 로컬 컴퓨터의 폴더 또는 파일인 경우 이름은 UNC 또는 폴더 또는 파일의 전체 경로입니다.

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="chtmlviewgetlocationurl"></a><a name="getlocationurl"></a>CHtmlView::GetLocationURL

이 멤버 함수를 호출하여 WebBrowser 컨트롤이 현재 표시중인 리소스의 URL을 검색합니다.

```
CString GetLocationURL() const;
```

### <a name="return-value"></a>Return Value

현재 WebBrowser에 표시되는 리소스의 URL을 포함하는 [CString](../../atl-mfc-shared/reference/cstringt-class.md) 개체입니다.

### <a name="remarks"></a>설명

리소스가 네트워크 또는 로컬 컴퓨터의 폴더 또는 파일인 경우 이름은 UNC 또는 폴더 또는 파일의 전체 경로입니다.

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="chtmlviewgetmenubar"></a><a name="getmenubar"></a>CHtmlView::GetMenuBar

이 멤버 함수를 호출하여 메뉴 모음이 표시되는지 확인합니다.

```
BOOL GetMenuBar() const;
```

### <a name="return-value"></a>Return Value

메뉴 모음이 표시되는 경우 0이 아닙니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="chtmlviewgetoffline"></a><a name="getoffline"></a>CHtmlView::오프라인

이 멤버 함수를 호출하여 웹 브라우저가 오프라인으로 작동하는지 확인합니다.

```
BOOL GetOffline() const;
```

### <a name="return-value"></a>Return Value

웹 브라우저가 현재 오프라인 인 경우 비영; 그렇지 않으면 0.

### <a name="remarks"></a>설명

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="chtmlviewgetparentbrowser"></a><a name="getparentbrowser"></a>CHtmlView::GetParent브라우저

WebBrowser 컨트롤의 상위 개체에 대한 포인터를 검색하려면 이 멤버 함수를 호출합니다.

```
LPDISPATCH GetParentBrowser() const;
```

### <a name="return-value"></a>Return Value

WebBrowser 컨트롤의 부모인 개체의 `IDispatch` 인터페이스에 대한 포인터입니다.

### <a name="remarks"></a>설명

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="chtmlviewgetproperty"></a><a name="getproperty"></a>CHtmlView::GetProperty

이 멤버 함수를 호출하여 현재 컨트롤과 연결된 속성의 값을 가져옵니다.

```
BOOL GetProperty(
    LPCTSTR lpszProperty,
    CString& strValue);

COleVariant GetProperty(LPCTSTR lpszProperty);
```

### <a name="parameters"></a>매개 변수

*lpszProperty*<br/>
검색할 속성을 포함하는 문자열에 대한 포인터입니다.

*strValue*<br/>
속성의 현재 값을 받는 [CString](../../atl-mfc-shared/reference/cstringt-class.md) 개체에 대 한 참조입니다.

### <a name="return-value"></a>Return Value

첫 번째 버전에서는 성공적으로 완료되면 0이 아닌 버전입니다. 그렇지 않으면 0. 두 번째 버전에서는 [COleVariant](../../mfc/reference/colevariant-class.md) 개체입니다.

### <a name="remarks"></a>설명

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="chtmlviewgetreadystate"></a><a name="getreadystate"></a>CHtmlView::겟레디스테이트

이 멤버 함수를 호출하여 WebBrowser 개체의 준비 상태를 검색합니다.

```
READYSTATE GetReadyState() const;
```

### <a name="return-value"></a>Return Value

Windows SDK에 설명된 대로 [READYSTATE](/previous-versions//aa768362\(v=vs.85\)) 값입니다.

### <a name="remarks"></a>설명

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="chtmlviewgetregisterasbrowser"></a><a name="getregisterasbrowser"></a>CHtmlView::GetRegisterAsBrowser

이 멤버 함수를 호출하여 WebBrowser 개체가 대상 이름 확인을 위한 최상위 브라우저로 등록되어 있는지 확인합니다.

```
BOOL GetRegisterAsBrowser() const;
```

### <a name="return-value"></a>Return Value

브라우저가 최상위 브라우저로 등록된 경우 Nonzero; 그렇지 않으면 0.

### <a name="remarks"></a>설명

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="chtmlviewgetregisterasdroptarget"></a><a name="getregisterasdroptarget"></a>CHtmlView::GetRegisterAsDropTarget

이 멤버 함수를 호출하여 WebBrowser 컨트롤이 탐색을 위한 삭제 대상으로 등록되어 있는지 확인합니다.

```
BOOL GetRegisterAsDropTarget() const;
```

### <a name="return-value"></a>Return Value

브라우저가 드롭 대상으로 등록된 경우 비영점; 그렇지 않으면 0.

### <a name="remarks"></a>설명

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="chtmlviewgetsilent"></a><a name="getsilent"></a>CHtmlView::GetSilent

이 멤버 함수를 호출하여 대화 상자를 WebBrowser 컨트롤에 표시할 수 있는지 여부를 확인합니다.

```
BOOL GetSilent() const;
```

### <a name="return-value"></a>Return Value

WebBrowser 컨트롤에서 대화 상자를 표시할 수 없는 경우 0이 아닙니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="chtmlviewgetsource"></a><a name="getsource"></a>CHtmlView::GetSource

이 멤버 함수를 호출하여 웹 페이지의 HTML 소스 코드를 검색합니다.

```
BOOL GetSource(CString& strRef);
```

### <a name="return-value"></a>Return Value

성공하는 경우 0이 아니고, 그렇지 않으면 0입니다.

### <a name="parameters"></a>매개 변수

*refString*<br/>
소스 코드를 보유하는 [CString입니다.](../../atl-mfc-shared/reference/cstringt-class.md)

### <a name="remarks"></a>설명

이 함수는 소스 코드가 `CString`에서 반환되는 것을 제외하고 Internet Explorer의 "소스 보기" 명령과 동일합니다.

## <a name="chtmlviewgetstatusbar"></a><a name="getstatusbar"></a>CHtmlView::겟스테이터스바

이 멤버 함수를 호출하여 WebBrowser 컨트롤에 상태 표시줄이 표시되는지 여부를 확인합니다.

```
BOOL GetStatusBar() const;
```

### <a name="return-value"></a>Return Value

상태 표시줄을 표시할 수 있는 경우 0이 아닙니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

인터넷 익스플로러에 적용됩니다. WebBrowser 컨트롤에서 이 호출을 사용하는 경우 오류가 반환되지 않지만 이 호출은 무시됩니다.

## <a name="chtmlviewgettheatermode"></a><a name="gettheatermode"></a>CHtmlView::겟시어터모드

이 멤버 함수를 호출하여 웹 브라우저가 극장 모드에 있는지 확인합니다.

```
BOOL GetTheaterMode() const;
```

### <a name="return-value"></a>Return Value

웹 브라우저가 극장 모드에 있는 경우 영하지 않습니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

웹 브라우저가 극장 모드에 있으면 브라우저 주 창이 전체 화면을 채우고, 최소한의 탐색 도구 세트가 있는 도구 모음이 나타나고 상태 표시줄이 화면의 오른쪽 상단 모서리에 나타납니다.

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="chtmlviewgettoolbar"></a><a name="gettoolbar"></a>CHtmlView::GetToolBar

이 멤버 함수를 호출하여 도구 모음이 표시되는지 확인합니다.

```
int GetToolBar() const;
```

### <a name="return-value"></a>Return Value

도구 모음이 표시되는지 여부를 나타내는 값입니다. 도구 모음이 표시되는 경우 영하지 않음; 그렇지 않으면 0.

## <a name="chtmlviewgettop"></a><a name="gettop"></a>CHtmlView::GetTop

이 멤버 함수를 호출하여 WebBrowser 컨트롤의 기본 창의 위쪽 가장자리의 화면 좌표를 검색합니다.

```
long GetTop() const;
```

### <a name="return-value"></a>Return Value

주 창의 위쪽 가장자리의 화면 좌표를 받는 변수의 주소입니다.

### <a name="remarks"></a>설명

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="chtmlviewgettoplevelcontainer"></a><a name="gettoplevelcontainer"></a>CHtmlView::GetTopLevel컨테이너

이 멤버 함수를 호출하여 Internet Explorer가 WebBrowser 컨트롤의 최상위 컨테이너인지 확인합니다.

```
BOOL GetTopLevelContainer() const;
```

### <a name="return-value"></a>Return Value

비영점은 최상위 컨테이너입니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="chtmlviewgettype"></a><a name="gettype"></a>CHtmlView::GetType

포함된 활성 문서의 형식 이름을 검색하려면 이 멤버 함수를 호출합니다.

```
CString GetType() const;
```

### <a name="return-value"></a>Return Value

포함된 활성 문서의 형식 이름을 포함하는 [CString](../../atl-mfc-shared/reference/cstringt-class.md) 개체입니다.

### <a name="remarks"></a>설명

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="chtmlviewgetvisible"></a><a name="getvisible"></a>CHtmlView::눈에 보이는

포함된 개체가 표시되는지 확인하려면 이 멤버 함수를 호출합니다.

```
BOOL GetVisible() const;
```

### <a name="return-value"></a>Return Value

개체가 표시되는 경우 0이 아닙니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="chtmlviewgetwidth"></a><a name="getwidth"></a>CHtmlView::GetWidth

Internet Explorer 주 창의 너비를 검색합니다.

```
long GetWidth() const;
```

### <a name="return-value"></a>Return Value

창의 현재 너비(픽셀)입니다.

## <a name="chtmlviewgoback"></a><a name="goback"></a>CHtmlView::GoBack

기록 목록에서 한 항목을 뒤로 이동합니다.

```cpp
void GoBack();
```

### <a name="remarks"></a>설명

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="chtmlviewgoforward"></a><a name="goforward"></a>CHtmlView::전진

기록 목록에서 하나의 항목을 앞으로 이동합니다.

```cpp
void GoForward();
```

## <a name="chtmlviewgohome"></a><a name="gohome"></a>CHtmlView::고홈

Internet Explorer 인터넷 옵션 대화 상자 또는 제어판을 통해 액세스하는 인터넷 속성 대화 상자에서 지정된 현재 홈페이지 또는 시작 페이지로 이동합니다.

```cpp
void GoHome();
```

### <a name="remarks"></a>설명

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="chtmlviewgosearch"></a><a name="gosearch"></a>CHtmlView::고서치

제어판에서 액세스한 Internet Explorer 인터넷 옵션 대화 상자 또는 인터넷 속성 대화 상자에 지정된 대로 현재 검색 페이지로 이동합니다.

```cpp
void GoSearch();
```

### <a name="remarks"></a>설명

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="chtmlviewloadfromresource"></a><a name="loadfromresource"></a>CHtmlView::로드From리소스

지정된 리소스를 WebBrowser 컨트롤에 로드하려면 이 멤버 함수를 호출합니다.

```
BOOL LoadFromResource(LPCTSTR lpszResource);
BOOL LoadFromResource(UINT nRes);
```

### <a name="parameters"></a>매개 변수

*lpszResource*<br/>
로드할 리소스의 이름이 포함된 문자열에 대한 포인터입니다.

*nRes*<br/>
로드할 리소스의 이름을 포함하는 버퍼의 ID입니다.

### <a name="return-value"></a>Return Value

성공하는 경우 0이 아니고, 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="chtmlviewnavigate"></a><a name="navigate"></a>CHtmlView::탐색

이 멤버 함수를 호출하여 URL로 식별된 리소스로 이동합니다.

```cpp
void Navigate(
    LPCTSTR URL,
    DWORD dwFlags = 0,
    LPCTSTR lpszTargetFrameName = NULL,
    LPCTSTR lpszHeaders = NULL,
    LPVOID lpvPostData = NULL,
    DWORD dwPostDataLen = 0);
```

### <a name="parameters"></a>매개 변수

*URL*<br/>
탐색할 URL또는 표시할 파일의 전체 경로가 포함된 호출자가 할당한 문자열입니다.

*dwFlags*<br/>
기록 목록에 리소스를 추가할지, 캐시에서 읽거나 쓸지 여부, 새 창에 리소스를 표시할지 여부를 지정하는 변수의 플래그입니다. 변수는 [BrowserNavConstants](/previous-versions/windows/internet-explorer/ie-developer/platform-apis/aa768360\(v=vs.85\)) 열거형에 의해 정의된 값의 조합일 수 있습니다.

*lpszTargetFrameName*<br/>
리소스를 표시할 프레임의 이름이 포함된 문자열에 대한 포인터입니다.

*lpszHeaders*<br/>
서버에 보낼 HTTP 헤더를 지정하는 값에 대한 포인터입니다. 이러한 헤더는 기본 Internet Explorer 헤더에 추가됩니다. 헤더는 서버에 필요한 작업, 서버에 전달되는 데이터 유형 또는 상태 코드와 같은 항목을 지정할 수 있습니다. *URL이* HTTP URL이 아닌 경우 이 매개 변수는 무시됩니다.

*lpvPostData*<br/>
HTTP POST 트랜잭션으로 보낼 데이터에 대한 포인터입니다. 예를 들어 POST 트랜잭션은 HTML 양식으로 수집된 데이터를 전송하는 데 사용됩니다. 이 매개 변수가 사후 데이터를 `Navigate` 지정하지 않으면 HTTP GET 트랜잭션을 발행합니다. *URL이* HTTP URL이 아닌 경우 이 매개 변수는 무시됩니다.

*dwPostDataLen*<br/>
HTTP POST 트랜잭션으로 보낼 데이터입니다. 예를 들어 POST 트랜잭션은 HTML 양식으로 수집된 데이터를 전송하는 데 사용됩니다. 이 매개 변수가 사후 데이터를 `Navigate` 지정하지 않으면 HTTP GET 트랜잭션을 발행합니다. *URL이* HTTP URL이 아닌 경우 이 매개 변수는 무시됩니다.

### <a name="remarks"></a>설명

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="chtmlviewnavigate2"></a><a name="navigate2"></a>CHtmlView::탐색2

이 멤버 함수를 호출하여 URL로 식별된 리소스 또는 전체 경로로 식별된 파일로 이동합니다.

```cpp
void Navigate2(
    LPITEMIDLIST pIDL,
    DWORD dwFlags = 0,
    LPCTSTR lpszTargetFrameName = NULL);

void Navigate2(
    LPCTSTR lpszURL,
    DWORD dwFlags = 0,
    LPCTSTR lpszTargetFrameName = NULL,
    LPCTSTR lpszHeaders = NULL,
    LPVOID lpvPostData = NULL,
    DWORD dwPostDataLen = 0);

void Navigate2(
    LPCTSTR lpszURL,
    DWORD dwFlags,
    CByteArray& baPostedData,
    LPCTSTR lpszTargetFrameName = NULL,
    LPCTSTR lpszHeader = NULL);
```

### <a name="parameters"></a>매개 변수

*Pidl*<br/>
ITEMIDLIST 구조에 대한 [포인터입니다.](/windows/win32/api/shtypes/ns-shtypes-itemidlist)

*dwFlags*<br/>
기록 목록에 리소스를 추가할지, 캐시에서 읽거나 쓸지 여부, 새 창에 리소스를 표시할지 여부를 지정하는 변수의 플래그입니다. 변수는 [BrowserNavConstants](/previous-versions/windows/internet-explorer/ie-developer/platform-apis/aa768360\(v=vs.85\)) 열거형에 의해 정의된 값의 조합일 수 있습니다.

*lpszTargetFrameName*<br/>
리소스를 표시할 프레임의 이름이 포함된 문자열에 대한 포인터입니다.

*lpszURL*<br/>
URL을 포함하는 문자열에 대한 포인터입니다.

*lpvPostData*<br/>
HTTP POST 트랜잭션으로 보낼 데이터입니다. 예를 들어 POST 트랜잭션은 HTML 양식으로 수집된 데이터를 전송하는 데 사용됩니다. 이 매개 변수가 사후 데이터를 `Navigate2` 지정하지 않으면 HTTP GET 트랜잭션을 발행합니다. *URL이* HTTP 또는 HTTPS URL이 아닌 경우 이 매개 변수는 무시됩니다.

*dwPostDataLen*<br/>
*lpvPostData* 매개 변수를 가리키는 데이터의 바이트 길이입니다.

*lpszHeaders*<br/>
서버에 보낼 HTTP 또는 HTTPS 헤더를 지정하는 값에 대한 포인터입니다. 이러한 헤더는 기본 Internet Explorer 헤더에 추가됩니다. 헤더는 서버에 필요한 작업, 서버에 전달되는 데이터 유형 또는 상태 코드와 같은 항목을 지정할 수 있습니다. *URL이* HTTP 또는 HTTPS URL이 아닌 경우 이 매개 변수는 무시됩니다.

*바게시데이터*<br/>
[CByteArray](../../mfc/reference/cbytearray-class.md) 개체에 대한 참조입니다.

### <a name="remarks"></a>설명

이 멤버 함수는 `Navigate` 매개 변수 *pIDL으로*표시되는 데스크톱 및 내 컴퓨터와 같은 특수 폴더에서 검색을 지원하여 멤버 기능을 확장합니다.

Internet Explorer 및 WebBrowser에 적용됩니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFCHtmlHttp#7](../../mfc/reference/codesnippet/cpp/chtmlview-class_1.cpp)]

## <a name="chtmlviewonbeforenavigate2"></a><a name="onbeforenavigate2"></a>CHtmlView::에 이전 탐색2

이 멤버 함수는 웹 브라우저에서 탐색이 발생하기 전에 이벤트가 발생하도록 프레임워크에서 호출됩니다.

```
virtual void OnBeforeNavigate2(
    LPCTSTR lpszURL,
    DWORD nFlags,
    LPCTSTR lpszTargetFrameName,
    CByteArray& baPostedData,
    LPCTSTR lpszHeaders,
    BOOL* pbCancel);
```

### <a name="parameters"></a>매개 변수

*lpszURL*<br/>
탐색할 URL이 포함된 문자열에 대한 포인터입니다.

*nFlags*<br/>
다음에 사용하도록 예약됩니다.

*lpszTargetFrameName*<br/>
리소스를 표시할 프레임의 이름이 포함된 문자열 또는 명명된 프레임이 리소스에 대한 대상이 없는 경우 NULL입니다.

*바게시데이터*<br/>
HTTP POST `CByteArray` 트랜잭션을 사용 하는 경우 서버에 보낼 데이터를 포함 하는 개체에 대 한 참조입니다.

*lpszHeaders*<br/>
서버에 보낼 추가 HTTP 헤더가 포함된 문자열에 대한 포인터입니다(HTTP URL만). 헤더는 서버에 필요한 작업, 서버에 전달되는 데이터 유형 또는 상태 코드와 같은 항목을 지정할 수 있습니다.

*pbCancel*<br/>
취소 플래그에 대한 포인터입니다. 응용 프로그램은 이 매개 변수를 0이 아닌 것으로 설정하여 탐색 작업을 취소하거나 계속진행할 수 있도록 0으로 설정할 수 있습니다.

## <a name="chtmlviewoncommandstatechange"></a><a name="oncommandstatechange"></a>CHtmlView::온커맨드스테이트체인지

이 멤버 함수는 프레임워크에서 호출되어 응용 프로그램에 웹 브라우저 명령의 사용 가능한 상태가 변경되었음을 알립니다.

```
virtual void OnCommandStateChange(
    long nCommand,
    BOOL bEnable);
```

### <a name="parameters"></a>매개 변수

*n 명령*<br/>
활성화된 상태가 변경된 명령의 식별자입니다.

*bEnable*<br/>
활성화된 상태입니다. 이 매개변수는 명령이 활성화된 경우 0이 아닌 경우, 비활성화된 경우 0입니다.

## <a name="chtmlviewondocumentcomplete"></a><a name="ondocumentcomplete"></a>CHtmlView::문서 완료

이 멤버 함수는 프레임워크에서 호출되어 문서가 READYSTATE_COMPLETE 상태에 도달했다는 것을 응용 프로그램에 알립니다.

```
virtual void OnDocumentComplete(LPCTSTR lpszURL);
```

### <a name="parameters"></a>매개 변수

*lpszURL*<br/>
URL, UNC 파일 이름 또는 탐색된 PIDL(항목 식별자 목록에 대한 포인터)을 평가하는 문자열에 대한 포인터입니다.

### <a name="remarks"></a>설명

모든 프레임이 이 이벤트를 발생시키는 것은 아니지만 [OnDownloadBegin](#ondownloadbegin) 이벤트를 `OnDocumentComplete` 발생시키는 각 프레임은 해당 이벤트를 발생시게 됩니다.

*lpszURL로* 표시된 URL은 이 URL이 정규화되고 정규화된 URL이기 때문에 브라우저가 탐색하라는 URL과 다를 수 있습니다. 예를 들어 응용 프로그램에서 탐색 또는 `"www.microsoft.com"` `OnNavigateComplete2` [Navigate2](#navigate) 호출에서 [Navigate2](#navigate2)의 URL을 지정하는 경우 `"<https://www.microsoft.com/>"`전달되는 URL은 .입니다. 또한 서버가 브라우저를 다른 URL로 리디렉션한 경우 리디렉션된 URL이 여기에 반영됩니다.

## <a name="chtmlviewondocwindowactivate"></a><a name="ondocwindowactivate"></a>CHtmlView::온닥윈도우활성화

컨테이너의 `IOleInPlaceActiveObject::OnDocWindowActivate`문서 창이 활성화되거나 비활성화될 때 활성 인- 장소 개체를 알립니다.

```
virtual HRESULT OnDocWindowActivate(BOOL fActivate);
```

### <a name="parameters"></a>매개 변수

*fActivate*<br/>
문서 창의 상태를 나타냅니다. 이 값이 0이 아닌 경우 창이 활성화됩니다. 이 값이 0이면 창이 비활성화됩니다.

### <a name="return-value"></a>Return Value

S_OK 성공하거나 OLE 정의 오류 코드가 그렇지 않으면 S_OK.

### <a name="remarks"></a>설명

재정의하여 `OnDocWindowActivate` Microsoft `OnDocWindowActivate` 웹 브라우저 컨트롤의 알림에 반응합니다. 자세한 내용은 [IDocHostUIHandler::OnDocWindowWindows에서](/previous-versions/windows/internet-explorer/ie-developer/platform-apis/aa753261\(v=vs.85\)) Windows SDK에서 활성화를 참조하십시오.

## <a name="chtmlviewondownloadbegin"></a><a name="ondownloadbegin"></a>CHtmlView::다운로드 시작

이 멤버 함수는 프레임워크에서 호출되어 문서 다운로드를 시작합니다.

```
virtual void OnDownloadBegin();
```

### <a name="remarks"></a>설명

이 이벤트는 탐색이 취소되지 않는 한 [OnBeforeNavigate2](#onbeforenavigate2) 이벤트 직후에 발생합니다. 컨테이너를 표시해야 하는 애니메이션 또는 "사용 중" 표시는 이 이벤트에 연결되어야 합니다.

## <a name="chtmlviewondownloadcomplete"></a><a name="ondownloadcomplete"></a>CHtmlView::에 다운로드완료

이 멤버 함수는 탐색 작업이 완료되거나 중지되거나 실패했음을 나타내기 위해 프레임워크에서 호출됩니다.

```
virtual void OnDownloadComplete();
```

## <a name="chtmlviewonenablemodeless"></a><a name="onenablemodeless"></a>CHtmlView::에인블모드리스

인터넷 익스플로러 또는 MSHTML 모달 UI를 표시 하는 경우 호출 됩니다.

```
virtual HRESULT OnEnableModeless(BOOL fEnable);
```

### <a name="parameters"></a>매개 변수

*fEnable*<br/>
호스트의 모덜리스 대화 상자가 활성화되어 있거나 비활성화되어 있는지 를 나타냅니다. 이 값이 0이 아닌 경우 모덜리스 대화 상자가 활성화됩니다. 이 값이 0이면 모덜리스 대화 상자가 비활성화됩니다.

### <a name="return-value"></a>Return Value

S_OK 성공하거나 OLE 정의 오류 코드가 그렇지 않으면 S_OK.

### <a name="remarks"></a>설명

컨테이너가 모달 대화 상자를 만들거나 파기할 때 모덜리스 대화 상자를 활성화하거나 사용하지 않도록 설정합니다. 재정의하여 `OnEnableModeless` Microsoft `EnableModeless` 웹 브라우저 컨트롤의 알림에 반응합니다. 자세한 내용은 [IDocHostUIHandler::사용 모드 없는](/previous-versions/windows/internet-explorer/ie-developer/platform-apis/aa753253\(v=vs.85\)) Windows SDK를 참조하십시오.

## <a name="chtmlviewonfilterdataobject"></a><a name="onfilterdataobject"></a>CHtmlView::온필터데이터오브젝트

호스트가 Internet Explorer 또는 MSHTML의 데이터 개체를 바꿀 수 있도록 하기 위해 Internet Explorer 또는 MSHTML에 의해 호스트에서 호출됩니다.

```
virtual HRESULT OnFilterDataObject(
    LPDATAOBJECT pDataObject,
    LPDATAOBJECT* ppDataObject);
```

### <a name="parameters"></a>매개 변수

*pDataObject*<br/>
인터넷 익스플로러 또는 MSHTML에서 제공하는 [IDataObject](/windows/win32/api/objidl/nn-objidl-idataobject) 인터페이스의 주소입니다.

*ppData개체*<br/>
호스트에서 `IDataObject` 제공하는 인터페이스 포인터를 받는 주소입니다. 메서드가 실패하더라도 이 매개 변수의 내용은 항상 NULL로 초기화되어야 합니다.

### <a name="return-value"></a>Return Value

데이터 개체가 대체되는 경우 S_OK 데이터 개체가 S_FALSE 경우 또는 오류가 발생하는 경우 OLE 정의 오류 코드가 있습니다.

### <a name="remarks"></a>설명

재정의하여 `OnFilterDataObject` Microsoft `FilterDataObject` 웹 브라우저 컨트롤의 알림에 반응합니다. 자세한 내용은 Windows SDK의 [IDocHostUIHandler::FilterDataObject를](/previous-versions/windows/internet-explorer/ie-developer/platform-apis/aa753254\(v=vs.85\)) 참조하십시오.

## <a name="chtmlviewonframewindowactivate"></a><a name="onframewindowactivate"></a>CHtmlView::온프레임 윈도우활성화

컨테이너의 최상위 프레임 창이 활성화 또는 비활성화될 경우 개체에 알리기 위해 [IOleInPlaceActiveObject::OnFrameWindowActivate](/windows/win32/api/oleidl/nf-oleidl-ioleinplaceactiveobject-onframewindowactivate) 에서 호출됩니다.

```
virtual HRESULT OnFrameWindowActivate(BOOL fActivate);
```

### <a name="parameters"></a>매개 변수

*fActivate*<br/>
컨테이너의 최상위 프레임 창의 상태를 나타냅니다. 이 값이 0이 아닌 경우 창이 활성화됩니다. 이 값이 0이면 창이 비활성화됩니다.

### <a name="return-value"></a>Return Value

S_OK 성공하거나 OLE 정의 오류 코드가 그렇지 않으면 S_OK.

### <a name="remarks"></a>설명

재정의하여 `OnFrameWindowActivate` Microsoft `OnFrameWindowActivate` 웹 브라우저 컨트롤의 알림에 반응합니다. 자세한 내용은 [IDocHostUIHandler::OnFrameWindowWindows에서](/previous-versions/windows/internet-explorer/ie-developer/platform-apis/aa753262\(v=vs.85\)) Windows SDK에서 활성화를 참조하십시오.

## <a name="chtmlviewonfullscreen"></a><a name="onfullscreen"></a>CHtmlView::온풀스크린

이 멤버 함수는 [FullScreen](/previous-versions/aa752119\(v=vs.85\)) 속성이 변경된 경우 프레임워크에서 호출됩니다.

```
virtual void OnFullScreen(BOOL bFullScreen);
```

### <a name="parameters"></a>매개 변수

*b전체 화면*<br/>
인터넷 익스플로러가 전체 화면 모드인 경우 영해가 아닙니다. 그렇지 않으면 0.

## <a name="chtmlviewongetdroptarget"></a><a name="ongetdroptarget"></a>CHtmlView::온겟드롭타겟

호스트가 대체 `IDropTarget`을 참조하십시오.

```
virtual HRESULT OnGetDropTarget(
    LPDROPTARGET pDropTarget,
    LPDROPTARGET* ppDropTarget);
```

### <a name="parameters"></a>매개 변수

*pDropTarget*<br/>
[아이드롭타겟](/windows/win32/api/oleidl/nn-oleidl-idroptarget) 인터넷 익스플로러 또는 MSHTML은 사용을 제안합니다.

*ppDropTarget*<br/>
호스트가 `IDropTarget` 제공하려는 `IDropTarget` 인터페이스 포인터를 수신하는 주소입니다.

### <a name="return-value"></a>Return Value

반환 코드 목록은 Windows SDK에서 [IDocHostUIHandler::GetDropTarget을](/previous-versions/windows/internet-explorer/ie-developer/platform-apis/aa753255\(v=vs.85\)) 참조하십시오.

### <a name="remarks"></a>설명

재정의하여 `OnGetDropTarget` Microsoft `GetDropTarget` 웹 브라우저 컨트롤의 알림에 반응합니다. 자세한 내용은 Windows SDK의 [IDocHostUIHandler::GetDropTarget을](/previous-versions/windows/internet-explorer/ie-developer/platform-apis/aa753255\(v=vs.85\)) 참조하십시오.

## <a name="chtmlviewongetexternal"></a><a name="ongetexternal"></a>CHtmlView::온겟외부

호스트의 `IDispatch` 인터페이스를 가져오기 위해 Internet Explorer 또는 MSHTML에 의해 호출됩니다.

```
virtual HRESULT OnGetExternal(LPDISPATCH* lppDispatch);
```

### <a name="parameters"></a>매개 변수

*lpp디스패치*<br/>
호스트 응용 프로그램의 `IDispatch` 인터페이스 포인터를 받는 주소에 대한 포인터입니다. 호스트가 자동화 인터페이스를 노출하는 경우 이 매개 변수를 통해 Internet Explorer 또는 MSHTML에 대한 참조를 제공할 수 있습니다. 메서드가 실패하더라도 이 매개 변수의 내용은 항상 NULL로 초기화되어야 합니다.

### <a name="return-value"></a>Return Value

S_OK 성공하거나 OLE 정의 오류 코드가 그렇지 않으면 S_OK.

### <a name="remarks"></a>설명

재정의하여 `OnGetExternal` Microsoft `GetExternal` 웹 브라우저 컨트롤의 알림에 반응합니다. 자세한 내용은 [IDocHostUIHandler::GetExternal](/previous-versions/windows/internet-explorer/ie-developer/platform-apis/aa753256\(v=vs.85\)) Windows SDK에서 확인할 수 있습니다.

## <a name="chtmlviewongethostinfo"></a><a name="ongethostinfo"></a>CHtmlView::온겟호스트정보

Internet Explorer 또는 MSHTML 호스트의 UI 기능을 검색합니다.

```
virtual HRESULT OnGetHostInfo(DOCHOSTUIINFO* pInfo);
```

### <a name="parameters"></a>매개 변수

*pInfo*<br/>
호스트의 UI 기능을 받는 [DOCHOSTUIINFO](/previous-versions/windows/internet-explorer/ie-developer/platform-apis/aa770044\(v=vs.85\)) 구조의 주소입니다.

### <a name="return-value"></a>Return Value

S_OK 성공하거나 OLE 정의 오류 코드가 그렇지 않으면 S_OK.

### <a name="remarks"></a>설명

재정의하여 `OnGetHostInfo` Microsoft `GetHostInfo` 웹 브라우저 컨트롤의 알림에 반응합니다. 자세한 내용은 Windows SDK의 [IDocHostUIHandler::GetHostInfo를](/previous-versions/windows/internet-explorer/ie-developer/platform-apis/aa753257\(v=vs.85\)) 참조하십시오.

## <a name="chtmlviewongetoptionkeypath"></a><a name="ongetoptionkeypath"></a>CHtmlView::온겟옵션키패스

이 멤버 함수를 호출하여 Internet Explorer 또는 MSHTML에서 사용자 기본 설정을 저장하는 레지스트리 키를 가져옵니다.

```
virtual HRESULT OnGetOptionKeyPath(
    LPOLESTR* pchKey,
    DWORD dwReserved);
```

### <a name="parameters"></a>매개 변수

*pchKey*<br/>
호스트가 `LPOLESTR` 기본 옵션을 저장하는 레지스트리 하위 키 문자열을 받는 주소입니다. 이 하위 키는 HKEY_CURRENT_USER 키 아래에 있습니다. [CoTaskMemAlloc를](/windows/win32/api/combaseapi/nf-combaseapi-cotaskmemalloc)사용하여 이 메모리를 할당합니다. 호출 응용 프로그램은 [CoTaskMemFree를](/windows/win32/api/combaseapi/nf-combaseapi-cotaskmemfree)사용하여이 메모리를 해제할 책임이 있습니다. 이 매개 변수는 메서드가 실패하더라도 항상 NULL로 초기화되어야 합니다.

*dwReserved*<br/>
다음에 사용하도록 예약됩니다. 현재 사용되지 않습니다.

### <a name="return-value"></a>Return Value

S_OK 성공하거나 그렇지 않은 경우 S_FALSE. S_FALSE 경우 Internet Explorer 또는 MSHTML은 기본적으로 자체 사용자 옵션으로 설정됩니다.

### <a name="remarks"></a>설명

재정의하여 `OnGetOptionKeyPath` Microsoft `GetOptionKeyPath` 웹 브라우저 컨트롤의 알림에 반응합니다. 자세한 내용은 [IDocHostUIHandler::GetOptionKeyPath](/previous-versions/windows/internet-explorer/ie-developer/platform-apis/aa753258\(v=vs.85\)) 를 참조하세요.

## <a name="chtmlviewonhideui"></a><a name="onhideui"></a>CHtmlView::온히데이

이 멤버 함수는 Internet Explorer 또는 MSHTML이 메뉴 및 도구 모음을 제거할 때 프레임워크에서 호출됩니다.

```
virtual HRESULT OnHideUI();
```

### <a name="return-value"></a>Return Value

S_OK 성공하거나 OLE 정의 오류 코드가 그렇지 않으면 S_OK.

### <a name="remarks"></a>설명

재정의하여 `OnHideUI` Microsoft `HideUI` 웹 브라우저 컨트롤의 알림에 반응합니다. 자세한 내용은 Windows SDK에서 [IDocHostUIHandler::HideUI를](/previous-versions/windows/internet-explorer/ie-developer/platform-apis/aa753259\(v=vs.85\)) 참조하십시오.

## <a name="chtmlviewonmenubar"></a><a name="onmenubar"></a>CHtmlView::온메뉴바

이 멤버 함수는 [MenuBar](/previous-versions/aa752131\(v=vs.85\)) 속성이 변경된 경우 프레임워크에서 호출됩니다.

```
virtual void OnMenuBar(BOOL bMenuBar);
```

### <a name="parameters"></a>매개 변수

*b메뉴바*<br/>
인터넷 익스플로러 메뉴 모음이 표시되는 경우 영하지 않음; 그렇지 않으면 0.

## <a name="chtmlviewonnavigatecomplete2"></a><a name="onnavigatecomplete2"></a>CHtmlView::OnNavigateComplete2

이 멤버 함수는 하이퍼링크에 대한 탐색이 완료된 후 프레임워크에서 호출됩니다(창 또는 프레임 집합 요소 중 하나).

```
virtual void OnNavigateComplete2(LPCTSTR strURL);
```

### <a name="parameters"></a>매개 변수

*스트URL*<br/>
탐색된 URL, UNC 파일 이름 또는 PIDL(항목 식별자 목록에 대한 포인터)을 평가하는 문자열 식입니다.

### <a name="remarks"></a>설명

URL 매개 변수는 URL 표현이 없는 셸 이름 공간 엔터티의 경우 PIDL일 수 있습니다.

*strURL에* 포함된 URL은 이 URL이 정규화되고 정규화된 URL이므로 브라우저가 탐색하라는 URL과 다를 수 있습니다. 예를 들어 응용 프로그램에서 탐색 또는 `"www.microsoft.com"` `OnNavigateComplete2` [Navigate2](#navigate) 호출에서 [Navigate2](#navigate2)의 URL을 지정하는 경우 `"<https://www.microsoft.com/>"`전달되는 URL은 .입니다. 또한 서버가 브라우저를 다른 URL로 리디렉션한 경우 리디렉션된 URL이 여기에 반영됩니다.

## <a name="chtmlviewonnavigateerror"></a><a name="onnavigateerror"></a>CHtmlView::에 대한 오류

하이퍼링크로 이동이 실패할 경우 프레임워크에 의해 호출됩니다.

```
virtual void OnNavigateError(
    LPCTSTR lpszURL,
    LPCTSTR lpszFrame,
    DWORD dwError,
    BOOL* pbCancel);
```

### <a name="parameters"></a>매개 변수

*lpszURL*<br/>
탐색에 실패한 URL입니다.

*lpszFrame*<br/>
리소스를 표시할 프레임의 이름 또는 명명된 프레임이 리소스에 대 한 대상으로 지정 되지 않은 경우 NULL입니다.

*dwError*<br/>
사용 가능한 경우 오류 상태 코드입니다. 가능한 HRESULT 및 HTTP 상태 코드 목록은 [NavigateError 이벤트 상태 코드를 참조하십시오.](/previous-versions/windows/internet-explorer/ie-developer/platform-apis/aa768365\(v=vs.85\))

*pbCancel*<br/>
오류 페이지로의 탐색을 취소할지 또는 추가 자동 검색을 취소할지 지정합니다. TRUE(기본값)인 경우 오류 페이지 또는 자동 검색으로 탐색을 계속합니다. FALSE인 경우 오류 페이지 또는 자동 검색으로 탐색을 취소합니다.

### <a name="remarks"></a>설명

사용자 지정 탐색 오류 처리를 제공 하려면이 메서드를 재정의 합니다.

자세한 내용은 [DWebBrowserEvents2::NavigateAA오류를](/previous-versions/windows/internet-explorer/ie-developer/platform-apis/aa768286\(v=vs.85\)) 참조하십시오.

## <a name="chtmlviewonnewwindow2"></a><a name="onnewwindow2"></a>CHtmlView::온뉴윈도우2

이 멤버 함수는 리소스를 표시하기 위해 새 창을 만들 때 프레임워크에서 호출됩니다.

```
virtual void OnNewWindow2(
    LPDISPATCH* ppDisp,
    BOOL* Cancel);
```

### <a name="parameters"></a>매개 변수

*ppDisp*<br/>
선택적으로 새 WebBrowser 또는 Internet Explorer `IDispatch` 개체의 인터페이스 포인터를 받는 인터페이스 포인터에 대한 포인터입니다.

*취소*<br/>
취소 플래그에 대한 포인터입니다. 응용 프로그램은 이 매개 변수를 0이 아닌 것으로 설정하여 탐색 작업을 취소하거나 계속진행할 수 있도록 0으로 설정할 수 있습니다.

### <a name="remarks"></a>설명

이 이벤트는 WebBrowser 내에서 새 창을 만들기 전에 발생합니다.

## <a name="chtmlviewonprogresschange"></a><a name="onprogresschange"></a>CHtmlView::진행 중 변경

이 멤버 함수는 다운로드 작업의 진행률을 업데이트했다는 것을 응용 프로그램에 알리기 위해 프레임워크에서 호출됩니다.

```
virtual void OnProgressChange(
    long nProgress,
    long nProgressMax);
```

### <a name="parameters"></a>매개 변수

*nProgress*<br/>
표시할 총 진행률 또는 진행이 완료되면 -1입니다.

*nProgressMax*<br/>
최대 진행률 값입니다.

### <a name="remarks"></a>설명

컨테이너는 이 이벤트에서 제공하는 정보를 사용하여 지금까지 다운로드한 바이트 수를 표시하거나 진행률 표시기를 업데이트할 수 있습니다.

## <a name="chtmlviewonpropertychange"></a><a name="onpropertychange"></a>CHtmlView::온프로퍼체인지

이 멤버 함수는 [Framework에서 호출되어 PutProperty가](#putproperty) 속성 값을 변경했다는 것을 응용 프로그램에 알립니다.

```
virtual void OnPropertyChange(LPCTSTR lpszProperty);
```

### <a name="parameters"></a>매개 변수

*lpszProperty*<br/>
속성의 이름을 포함하는 문자열에 대한 포인터입니다.

## <a name="chtmlviewonquit"></a><a name="onquit"></a>CHtmlView::에퀴드

이 멤버 함수는 프레임워크에서 호출되어 Internet Explorer 응용 프로그램이 종료할 준비가 되었음을 응용 프로그램에 알립니다.

```
virtual void OnQuit();
```

## <a name="chtmlviewonresizeborder"></a><a name="onresizeborder"></a>CHtmlView::온리사이즈보더

[IOleInPlaceActiveObject::ResizeBorder](/windows/win32/api/oleidl/nf-oleidl-ioleinplaceactiveobject-resizeborder)의 Internet Explorer 또는 MSHTML 구현에서 호출되며, 해당 테두리 공간 크기를 조정해야 함을 개체에 경고합니다.

```
virtual HRESULT OnResizeBorder(
    LPCRECT prcBorder,
    LPOLEINPLACEUIWINDOW pUIWindow,
    BOOL fFrameWindow);
```

### <a name="parameters"></a>매개 변수

*prcBorder*<br/>
테두리 공간에 대한 새 외부 사각형입니다.

*pUIWindow*<br/>
테두리가 변경된 프레임 또는 문서 창 개체에 대한 인터페이스에 대한 포인터입니다.

*fFrameWindow*<br/>
프레임 창이 [IOleInPlaceActiveObject::크기 조정경계,](/windows/win32/api/oleidl/nf-oleidl-ioleinplaceactiveobject-resizeborder)그렇지 않으면 FALSE를 호출하는 경우 TRUE입니다.

### <a name="return-value"></a>Return Value

S_OK 성공하거나 OLE 정의 오류 코드가 그렇지 않으면 S_OK.

### <a name="remarks"></a>설명

재정의하여 `OnResizeBorder` Microsoft `ResizeBorder` 웹 브라우저 컨트롤의 알림에 반응합니다. 자세한 내용은 [IDocHostUIHandler::Windows](/previous-versions/windows/internet-explorer/ie-developer/platform-apis/aa753263\(v=vs.85\)) SDK의 크기 조정 경계를 참조하십시오.

## <a name="chtmlviewonshowcontextmenu"></a><a name="onshowcontextmenu"></a>CHtmlView::온쇼컨텍스트메뉴

상황에 맞는 메뉴를 표시하려는 경우 Internet Explorer 또는 MSHTML에 의해 호출됩니다.

```
virtual HRESULT OnShowContextMenu(
    DWORD dwID,
    LPPOINT ppt,
    LPUNKNOWN pcmdtReserved,
    LPDISPATCH pdispReserved);
```

### <a name="parameters"></a>매개 변수

*dwID*<br/>
표시할 컨텍스트 메뉴의 식별자입니다. 값 `IDocHostUIHandler::ShowContextMenu` 목록은 Windows SDK에서 참조하십시오.

*Ppt*<br/>
메뉴의 화면 좌표를 클릭합니다.

*pcmdtReserved*<br/>
[IOleCommandTarget](/windows/win32/api/docobj/nn-docobj-iolecommandtarget) 인터페이스는 명령 상태를 쿼리하고 이 개체에서 명령을 실행하는 데 사용됩니다.

*pdispReserved*<br/>
화면 좌표에서 개체의 IDispatch 인터페이스입니다. 이를 통해 호스트는 특정 개체를 구분하여 보다 구체적인 컨텍스트를 제공할 수 있습니다.

### <a name="return-value"></a>Return Value

값 목록은 Windows SDK의 [IDocHostUIHandler::ShowContextMenu를](/previous-versions/windows/internet-explorer/ie-developer/platform-apis/aa753264\(v=vs.85\)) 참조하십시오.

### <a name="remarks"></a>설명

재정의하여 `OnShowContextMenu` Microsoft `ShowContextMenu` 웹 브라우저 컨트롤의 알림에 반응합니다. 자세한 내용은 Windows SDK의 [IDocHostUIHandler::ShowContextMenu를](/previous-versions/windows/internet-explorer/ie-developer/platform-apis/aa753264\(v=vs.85\)) 참조하십시오.

## <a name="chtmlviewonshowui"></a><a name="onshowui"></a>CHtmlView::온쇼이

Internet Explorer 또는 MSHTML이 해당 메뉴 및 도구 모음을 표시하기 전에 호출됩니다.

```
virtual HRESULT OnShowUI(
    DWORD dwID,
    LPOLEINPLACEACTIVEOBJECT pActiveObject,
    LPOLECOMMANDTARGET pCommandTarget,
    LPOLEINPLACEFRAME pFrame,
    LPOLEINPLACEUIWINDOW pDoc);
```

### <a name="parameters"></a>매개 변수

*dwID*<br/>
다음에 사용하도록 예약됩니다.

*pActiveObject*<br/>
[IOleInPlaceActive현재](/windows/win32/api/oleidl/nn-oleidl-ioleinplaceactiveobject) 활성 개체의 개체 인터페이스입니다.

*pCommandTarget*<br/>
개체의 [IOleCommandTarget](/windows/win32/api/docobj/nn-docobj-iolecommandtarget) 인터페이스입니다.

*pFrame*<br/>
개체의 [IOleInPlaceFrame](/windows/win32/api/oleidl/nn-oleidl-ioleinplaceframe) 인터페이스입니다. 이 메뉴 및 도구 모음에 대 한 필요 합니다.

*pDoc*<br/>
[개체에 대한 IOleInPlaceUIWindow](/windows/win32/api/oleidl/nn-oleidl-ioleinplaceuiwindow) 인터페이스입니다. 이 도구 모음에 필요한.

### <a name="return-value"></a>Return Value

값 목록은 Windows SDK에서 [IDocHostUIHandler::ShowUI를](/previous-versions/windows/internet-explorer/ie-developer/platform-apis/aa753265\(v=vs.85\)) 참조하십시오.

### <a name="remarks"></a>설명

재정의하여 `OnShowUI` Microsoft `ShowUI` 웹 브라우저 컨트롤의 알림에 반응합니다. 자세한 내용은 Windows SDK의 [IDocHostUIHandler::ShowUI를](/previous-versions/windows/internet-explorer/ie-developer/platform-apis/aa753265\(v=vs.85\)) 참조하십시오.

## <a name="chtmlviewonstatusbar"></a><a name="onstatusbar"></a>CHtmlView::온스테이바

이 멤버 함수는 [StatusBar](/previous-versions//aa768270\(v=vs.85\)) 속성이 변경된 경우 프레임워크에서 호출됩니다.

```
virtual void OnStatusBar(BOOL bStatusBar);
```

### <a name="parameters"></a>매개 변수

*b 상태 표시줄*<br/>
인터넷 익스플로러의 상태 표시줄이 표시되는 경우 비영또는 그렇지 않으면 0입니다.

## <a name="chtmlviewonstatustextchange"></a><a name="onstatustextchange"></a>CHtmlView::상태 텍스트 변경

이 멤버 함수는 WebBrowser 컨트롤과 연결된 상태 표시줄의 텍스트가 변경되었음을 응용 프로그램에 알리기 위해 프레임워크에서 호출됩니다.

```
virtual void OnStatusTextChange(LPCTSTR lpszText);
```

### <a name="parameters"></a>매개 변수

*lpszText*<br/>
새 상태 표시줄 텍스트가 포함된 문자열입니다.

## <a name="chtmlviewontheatermode"></a><a name="ontheatermode"></a>CHtmlView::온시어터모드

이 멤버 함수는 [TheaterMode](/previous-versions/aa768273\(v=vs.85\)) 속성이 변경될 때 프레임워크에서 호출됩니다.

```
virtual void OnTheaterMode(BOOL bTheaterMode);
```

### <a name="parameters"></a>매개 변수

*b극장 모드*<br/>
인터넷 익스플로러가 극장 모드에 있는 경우 비영; 그렇지 않으면 0.

## <a name="chtmlviewontitlechange"></a><a name="ontitlechange"></a>CHtmlView::온타이틀 체인지

이 멤버 함수는 WebBrowser 컨트롤에서 문서의 제목을 사용할 수 있게 되거나 변경되는 경우 응용 프로그램에 알리기 위해 프레임워크에서 호출됩니다.

```
virtual void OnTitleChange(LPCTSTR lpszText);
```

### <a name="parameters"></a>매개 변수

*lpszText*<br/>
새 문서 제목입니다.

### <a name="remarks"></a>설명

HTML의 경우 제목이 변경될 수 있습니다. HTML이 계속 다운로드되는 동안 문서의 URL이 제목으로 설정됩니다. 실제 제목(있는 경우)이 HTML에서 구문 분석된 후 제목이 실제 제목을 반영하도록 변경됩니다.

## <a name="chtmlviewontoolbar"></a><a name="ontoolbar"></a>CHtmlView::온툴바

이 멤버 함수는 [ToolBar](/previous-versions/aa768274\(v=vs.85\)) 속성이 변경된 경우 프레임워크에서 호출됩니다.

```
virtual void OnToolBar(BOOL bToolBar);
```

### <a name="parameters"></a>매개 변수

*b툴바*<br/>
인터넷 익스플로러의 도구 모음이 표시되는 경우 비영또는 그렇지 않으면 0입니다.

## <a name="chtmlviewontranslateaccelerator"></a><a name="ontranslateaccelerator"></a>CHtmlView::번역가속기

컨테이너의 메시지 큐에서 메뉴 바로 가기 키 메시지를 처리하기 위해 [IOleInPlaceActiveObject::TranslateAccelerator](/windows/win32/api/oleidl/nf-oleidl-ioleinplaceactiveobject-translateaccelerator) 또는 [IOleControlSite::TranslateAccelerator](/windows/win32/api/ocidl/nf-ocidl-iolecontrolsite-translateaccelerator) 가 호출될 경우 Internet Explorer 또는 MSHTML에 의해 호출됩니다.

```
virtual HRESULT OnTranslateAccelerator(
    LPMSG lpMsg,
    const GUID* pguidCmdGroup,
    DWORD nCmdID);
```

### <a name="parameters"></a>매개 변수

*lpMsg*<br/>
번역해야 할 메시지를 가리킵니다.

*pguidCmdGroup*<br/>
명령 그룹 식별자입니다.

*nCmdID*<br/>
명령 식별자입니다.

### <a name="return-value"></a>Return Value

S_OK 성공하거나 그렇지 않은 경우 S_FALSE.

### <a name="remarks"></a>설명

재정의하여 `OnTranslateAccelerator` Microsoft `TranslateAccelerator` 웹 브라우저 컨트롤의 알림에 반응합니다. 자세한 내용은 [IDocHostUIHandler::Windows](/previous-versions/windows/internet-explorer/ie-developer/platform-apis/aa753266\(v=vs.85\)) SDK의 번역가속기를 참조하십시오.

## <a name="chtmlviewontranslateurl"></a><a name="ontranslateurl"></a>CHtmlView::에 번역Url

호스트가 로드할 URL을 수정할 수 있도록 하기 위해 Internet Explorer 또는 MSHTML에 의해 호출됩니다.

```
virtual HRESULT OnTranslateUrl(
    DWORD dwTranslate,
    OLECHAR* pchURLIn,
    OLECHAR** ppchURLOut);
```

### <a name="parameters"></a>매개 변수

*dwTranslate*<br/>
다음에 사용하도록 예약됩니다.

*pchURLIn*<br/>
번역할 URL을 나타내는 Internet Explorer 또는 MSHTML에서 제공하는 문자열의 주소입니다.

*ppchURLOut*<br/>
번역된 URL의 주소를 받는 문자열 포인터의 주소입니다. 호스트는 작업 메모리 할당을 사용하여 버퍼를 할당합니다. URL이 번역되지 않거나 메서드가 실패하더라도 이 매개 변수의 내용은 항상 NULL로 초기화되어야 합니다.

### <a name="return-value"></a>Return Value

`S_OK`URL이 번역된 경우 S_FALSE URL이 번역되지 않은 경우 또는 오류가 발생한 경우 OLE 정의 오류 코드가 있습니다.

### <a name="remarks"></a>설명

재정의하여 `OnTranslateUrl` Microsoft `TranslateUrl` 웹 브라우저 컨트롤의 알림에 반응합니다. 자세한 내용은 [IDocHostUIHandler:::번역Url](/previous-versions/windows/internet-explorer/ie-developer/platform-apis/aa753267\(v=vs.85\)) 을 참조하십시오.

## <a name="chtmlviewonupdateui"></a><a name="onupdateui"></a>CHtmlView::OnUpdateUI

명령 상태가 변경되었음을 호스트에 알립니다.

```
virtual HRESULT OnUpdateUI();
```

### <a name="return-value"></a>Return Value

S_OK 성공하거나 OLE 정의 오류 코드가 그렇지 않으면 S_OK.

### <a name="remarks"></a>설명

호스트는 도구 모음 단추의 상태를 업데이트해야 합니다. 이 메서드는 에서 `ShowUI`반환 값에 관계 없이 호출 됩니다. 재정의하여 `OnUpdateUI` Microsoft `UpdateUI` 웹 브라우저 컨트롤의 알림에 반응합니다.

## <a name="chtmlviewonvisible"></a><a name="onvisible"></a>CHtmlView::눈에 보이는

WebBrowser의 창을 표시하거나 숨춰야 할 때 이 멤버 함수는 프레임워크에서 호출됩니다.

```
virtual void OnVisible(BOOL bVisible);
```

### <a name="parameters"></a>매개 변수

*bVisible*<br/>
그렇지 않으면 오브젝트가 표시되고 0인 경우 0이 아닙니다.

### <a name="remarks"></a>설명

이렇게 하면 개체 제어 호스트 창이 Internet Explorer 창이 있는 것과 동일한 방식으로 행동할 수 있습니다.

## <a name="chtmlviewputproperty"></a><a name="putproperty"></a>CHtmlView::Putproperty

지정된 개체와 연결된 속성을 설정하려면 이 멤버 함수를 호출합니다.

```cpp
void PutProperty(
    LPCTSTR lpszProperty,
    const VARIANT& vtValue);

void PutProperty(
    LPCTSTR lpszPropertyName,
    double dValue);

void PutProperty(
    LPCTSTR lpszPropertyName,
    long lValue);

void PutProperty(
    LPCTSTR lpszPropertyName,
    LPCTSTR lpszValue);

void PutProperty(
    LPCTSTR lpszPropertyName,
    short nValue);
```

### <a name="parameters"></a>매개 변수

*lpszProperty*<br/>
설정할 속성을 포함하는 문자열입니다.

*vtValue*<br/>
*lpszProperty로*표시된 속성의 새 값입니다.

*lpszPropertyName*<br/>
설정할 속성 의 이름을 포함하는 문자열에 대한 포인터입니다.

*d값*<br/>
속성의 새 값입니다.

*lValue*<br/>
속성의 새 값입니다.

*lpsz값*<br/>
속성의 새 값을 포함하는 문자열에 대한 포인터입니다.

*n값*<br/>
속성의 새 값입니다.

### <a name="remarks"></a>설명

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="chtmlviewqueryformscommand"></a><a name="queryformscommand"></a>CHtmlView::쿼리폼명령

사용자 인터페이스 이벤트에 의해 생성되는 하나 이상 명령의 상태를 쿼리합니다.

```
HRESULT QueryFormsCommand(
    DWORD dwCommandID,
    BOOL* pbSupported,
    BOOL* pbEnabled,
    BOOL* pbChecked);
```

### <a name="parameters"></a>매개 변수

*dwCommandID*<br/>
쿼리되는 명령의 식별자입니다.

*pb지원*<br/>
*명령(dwCommandID로*식별)이 지원되는지 지정하는 BOOL에 대한 포인터입니다. TRUE이면 명령이 지원됩니다. 그렇지 않으면 거짓.

*pbEnabled*<br/>
*명령(dwCommandID로*식별)이 활성화되어 있는지 지정하는 BOOL에 대한 포인터입니다. TRUE이면 명령이 지원됩니다. 그렇지 않으면 거짓.

*pbChecked*<br/>
*명령(dwCommandID로*식별됨)이 선택되었는지 여부를 지정하는 BOOL에 대한 포인터입니다. TRUE이면 명령이 지원됩니다. 그렇지 않으면 거짓.

### <a name="return-value"></a>Return Value

표준 HRESULT 값입니다. 가능한 값의 전체 목록은 Windows SDK의 [IOleCommandTarget::QueryStatus를](/windows/win32/api/docobj/nf-docobj-iolecommandtarget-querystatus) 참조하십시오.

### <a name="remarks"></a>설명

`QueryFormsCommand`[은 IOleCommandTarget::QueryStatus](/windows/win32/api/docobj/nf-docobj-iolecommandtarget-querystatus) 메서드의 동작을 구현합니다.

## <a name="chtmlviewquerystatuswb"></a><a name="querystatuswb"></a>CHtmlView::쿼리 상태WB

이 멤버 함수를 호출하여 명령 상태를 쿼리합니다.

```
OLECMDF QueryStatusWB(OLECMDID cmdID) const;
```

### <a name="parameters"></a>매개 변수

*Cmdid*<br/>
호출자에게 상태 정보가 필요한 명령의 [OLECMDID](/windows/win32/api/docobj/ne-docobj-olecmdid) 값입니다.

### <a name="return-value"></a>Return Value

명령의 상태를 수신하는 [OLECMDF](/windows/win32/api/docobj/ne-docobj-olecmdf) 값의 주소입니다.

### <a name="remarks"></a>설명

`QueryStatusWB`[은 IOleCommandTarget::QueryStatus](/windows/win32/api/docobj/nf-docobj-iolecommandtarget-querystatus) 메서드의 동작을 구현합니다.

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="chtmlviewrefresh"></a><a name="refresh"></a>CHtmlView::새로 고침

웹 브라우저가 현재 표시하고 있는 URL 또는 파일을 다시 로드합니다.

```cpp
void Refresh();
```

### <a name="remarks"></a>설명

`Refresh`은 새로 고침 수준을 설정하기위한 매개 변수가 없습니다.

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="chtmlviewrefresh2"></a><a name="refresh2"></a>CHtmlView::새로 고침2

Internet Explorer가 현재 표시중인 파일을 다시 로드합니다.

```cpp
void Refresh2(int nLevel);
```

### <a name="parameters"></a>매개 변수

*n레벨*<br/>
새로 고침 수준을 지정하는 변수의 주소입니다. 가능한 변수는 Windows SDK의 [RefreshConstantconstants에 정의됩니다.](/previous-versions/aa768363\(v=vs.85\))

### <a name="remarks"></a>설명

[새로 고침과](#refresh) `Refresh2` 달리 새로 고침 수준을 지정하는 매개 변수가 포함되어 있습니다.

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="chtmlviewsetaddressbar"></a><a name="setaddressbar"></a>CHtmlView::세트주소바

이 멤버 함수를 호출하여 Internet Explorer 개체의 주소 표시줄을 표시하거나 숨깁니다.

```cpp
void SetAddressBar(BOOL bNewValue);
```

### <a name="parameters"></a>매개 변수

*bNewValue*<br/>
주소 표시줄을 표시하는 비영점; 그렇지 않으면 0.

### <a name="remarks"></a>설명

인터넷 익스플로러에 적용됩니다. WebBrowser 컨트롤에서 이 호출을 사용하는 경우 오류가 반환되지 않지만 이 호출은 무시됩니다.

## <a name="chtmlviewsetfullscreen"></a><a name="setfullscreen"></a>CHtmlView::설정전체 화면

이 멤버 함수를 호출하여 Internet Explorer를 전체 화면 또는 일반 창 모드로 설정합니다.

```cpp
void SetFullScreen(BOOL bNewValue);
```

### <a name="parameters"></a>매개 변수

*bNewValue*<br/>
전체 화면 모드의 경우 0이 아닙니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

전체 화면 모드에서는 Internet Explorer 기본 창이 최대화되고 상태 표시줄, 도구 모음, 메뉴 모음 및 제목 표시줄이 숨김이 있습니다.

인터넷 익스플로러에 적용됩니다. WebBrowser 컨트롤에서 이 호출을 사용하는 경우 오류가 반환되지 않지만 이 호출은 무시됩니다.

## <a name="chtmlviewsetheight"></a><a name="setheight"></a>CHtmlView::설정높이

이 멤버 함수를 호출하여 Internet Explorer 기본 창의 높이를 설정합니다.

```cpp
void SetHeight(long nNewValue);
```

### <a name="parameters"></a>매개 변수

*n뉴밸류*<br/>
주 창의 높이(픽셀 단위)입니다.

### <a name="remarks"></a>설명

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="chtmlviewsetleft"></a><a name="setleft"></a>CHtmlView::설정 왼쪽

Internet Explorer 주 창의 가로 위치를 설정합니다.

```cpp
void SetLeft(long nNewValue);
```

### <a name="parameters"></a>매개 변수

*n뉴밸류*<br/>
주 창의 왼쪽 가장자리의 화면 좌표입니다.

## <a name="chtmlviewsetmenubar"></a><a name="setmenubar"></a>CHtmlView::세트메뉴바

이 멤버 함수를 호출하여 Internet Explorer 메뉴 모음을 표시하거나 숨깁니다.

```cpp
void SetMenuBar(BOOL bNewValue);
```

### <a name="parameters"></a>매개 변수

*bNewValue*<br/>
메뉴 바를 표시하는 비영; 그렇지 않으면 0.

### <a name="remarks"></a>설명

인터넷 익스플로러에 적용됩니다. WebBrowser 컨트롤에서 이 호출을 사용하는 경우 오류가 반환되지 않지만 이 호출은 무시됩니다.

## <a name="chtmlviewsetoffline"></a><a name="setoffline"></a>CHtmlView::설정 오프라인

이 멤버 함수를 호출하여 WebBrowser 컨트롤이 현재 오프라인 모드에서 작동하는지 여부를 나타내는 값을 설정합니다.

```cpp
void SetOffline(BOOL bNewValue);
```

### <a name="parameters"></a>매개 변수

*bNewValue*<br/>
로컬 캐시에서 읽을 수 있는 영하지 않은 것; 그렇지 않으면 0.

### <a name="remarks"></a>설명

오프라인 모드에서 브라우저는 원본 문서가 아닌 로컬 캐시에서 HTML 페이지를 읽습니다.

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="chtmlviewsetregisterasbrowser"></a><a name="setregisterasbrowser"></a>CHtmlView::세트레지스터브라우저

이 멤버 함수를 호출하여 WebBrowser 컨트롤이 대상 이름 확인을 위한 최상위 브라우저로 등록되어 있는지 여부를 나타내는 값을 설정합니다.

```cpp
void SetRegisterAsBrowser(BOOL bNewValue);
```

### <a name="parameters"></a>매개 변수

*bNewValue*<br/>
Internet Explorer가 최상위 브라우저로 등록되어 있는지 여부를 결정합니다. 0이 아닌 경우 웹 브라우저가 최상위 브라우저로 등록됩니다. 0이면 최상위 브라우저가 아닙니다. 기본값은 영입니다.

### <a name="remarks"></a>설명

최상위 브라우저는 레지스트리에 기본 브라우저로 설정된 브라우저입니다.

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="chtmlviewsetregisterasdroptarget"></a><a name="setregisterasdroptarget"></a>CHtmlView::세트레지스터스드롭타겟

이 멤버 함수를 호출하여 WebBrowser 컨트롤이 탐색을 위한 삭제 대상으로 등록되어 있는지 여부를 나타내는 값을 설정합니다.

```cpp
void SetRegisterAsDropTarget(BOOL bNewValue);
```

### <a name="parameters"></a>매개 변수

*bNewValue*<br/>
WebBrowser 컨트롤이 탐색을 위한 삭제 대상으로 등록되어 있는지 확인합니다. 영하지 않으면 개체가 놓기 대상으로 등록됩니다. 0이면 놓기 대상이 아닙니다.

### <a name="remarks"></a>설명

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="chtmlviewsetsilent"></a><a name="setsilent"></a>CHtmlView::설정침묵

이 멤버 함수를 호출하여 대화 상자를 표시할 수 있는지 여부를 나타내는 값을 설정합니다.

```cpp
void SetSilent(BOOL bNewValue);
```

### <a name="parameters"></a>매개 변수

*bNewValue*<br/>
0이 아닌 경우 대화 상자가 표시되지 않습니다. 0이면 대화 상자가 표시됩니다. 기본값은 영입니다.

### <a name="remarks"></a>설명

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="chtmlviewsetstatusbar"></a><a name="setstatusbar"></a>CHtmlView::설정 상태 표시줄

상태 표시줄을 표시하려면 이 멤버 함수를 호출합니다.

```cpp
void SetStatusBar(BOOL bNewValue);
```

### <a name="parameters"></a>매개 변수

*bNewValue*<br/>
상태 표시줄이 표시되는 경우 0이 아닙니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

인터넷 익스플로러에 적용됩니다. WebBrowser 컨트롤에서 이 호출을 사용하는 경우 오류가 반환되지 않지만 이 호출은 무시됩니다.

## <a name="chtmlviewsettheatermode"></a><a name="settheatermode"></a>CHtmlView::세트극장 모드

이 멤버 함수를 호출하여 WebBrowser 컨트롤이 극장 모드에 있는지 여부를 나타내는 값을 설정합니다.

```cpp
void SetTheaterMode(BOOL bNewValue);
```

### <a name="parameters"></a>매개 변수

*bNewValue*<br/>
웹 브라우저 컨트롤을 극장 모드로 설정하는 비영; 그렇지 않으면 0. 기본값은 영입니다.

### <a name="remarks"></a>설명

웹 브라우저가 극장 모드에 있으면 브라우저 주 창이 전체 화면을 채우고, 최소한의 탐색 도구 세트가 있는 도구 모음이 나타나고 상태 표시줄이 화면의 오른쪽 상단 모서리에 나타납니다.

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="chtmlviewsettoolbar"></a><a name="settoolbar"></a>CHtmlView::SetToolBar

이 멤버 함수를 호출하여 Internet Explorer 도구 모음을 표시하거나 숨깁니다.

```cpp
void SetToolBar(int nNewValue);
```

### <a name="parameters"></a>매개 변수

*n뉴밸류*<br/>
도구 모음을 표시할지 여부를 나타냅니다. 도구 모음을 표시할 경우 0이 아닙니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

인터넷 익스플로러에 적용됩니다. WebBrowser 컨트롤에서 이 호출을 사용하는 경우 오류가 반환되지 않지만 이 호출은 무시됩니다.

## <a name="chtmlviewsettop"></a><a name="settop"></a>CHtmlView::셋탑

이 멤버 함수를 호출하여 WebBrowser 컨트롤의 내부 상단 가장자리와 해당 컨테이너의 위쪽 가장자리 사이의 거리를 설정합니다.

```cpp
void SetTop(long nNewValue);
```

### <a name="parameters"></a>매개 변수

*n뉴밸류*<br/>
주 창의 위쪽 가장자리의 화면 좌표입니다.

### <a name="remarks"></a>설명

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="chtmlviewsetvisible"></a><a name="setvisible"></a>CHtmlView::가시설정

이 멤버 함수를 호출하여 WebBrowser 컨트롤의 가시성 상태를 설정합니다.

```cpp
void SetVisible(BOOL bNewValue);
```

### <a name="parameters"></a>매개 변수

*bNewValue*<br/>
컨트롤이 표시되는 경우 0이 아닙니다. 그렇지 않으면 0.

### <a name="remarks"></a>설명

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="chtmlviewsetwidth"></a><a name="setwidth"></a>CHtmlView::세트 폭

Internet Explorer 주 창의 너비를 설정합니다.

```cpp
void SetWidth(long nNewValue);
```

### <a name="parameters"></a>매개 변수

*n뉴밸류*<br/>
인터넷 익스플로러 메인 창의 너비(픽셀 단위)입니다.

## <a name="chtmlviewstop"></a><a name="stop"></a>CHtmlView::중지

보류 중인 탐색 또는 다운로드 작업을 취소하고 배경 소리 및 애니메이션과 같은 동적 페이지 요소를 중지하려면 이 멤버 함수를 호출합니다.

```cpp
void Stop();
```

### <a name="remarks"></a>설명

Internet Explorer 및 WebBrowser에 적용됩니다.

## <a name="see-also"></a>참조

[MFC 샘플 MFCIE](../../overview/visual-cpp-samples.md)<br/>
[CFormView 클래스](../../mfc/reference/cformview-class.md)<br/>
[계층 구조 차트](../../mfc/hierarchy-chart.md)<br/>
[IWebBrowser2](/windows/win32/api/exdisp/nn-exdisp-iwebbrowser2)
